# RESULT.md 模板 — 测试改修总结

> 本文件是 `code-unit` 技能产出的 `RESULT.md` 章节结构与填写规范。
> 模型在生成 `RESULT.md` 时应严格按本结构组织,作为本次任务测试工作的最终交付物。
>
> **版本感知**:本文件存放在 `./assistants/<版本号>/test/<任务编码>/RESULT.md`,文档头应明确记录版本号。

---

## 文档头
```
# <任务编码> — <任务标题> 测试总结

- 任务编码:<REQ-YYYY-NNNN-XXX>
- 需求编码:<REQ-YYYY-NNNN>
- 所属版本:<版本号>(如 v1.0.0)
- 任务来源:./assistants/<版本号>/plan/<需求编号>/PLAN.md §3
- 详细设计:./assistants/<版本号>/plan/<需求编号>/RESULT.md §X
- 代码改修正文:./assistants/<版本号>/code/<任务编号>/RESULT.md
- 测试状态:已运行-通过 / 已运行-失败 / 已编写 / 不适用 / 阻塞
- 状态推进:测试状态由"<旧>"→"<新>"
- 责任人:<测试者>
- 创建:<YYYY-MM-DD HH:mm>
- 完成:<YYYY-MM-DD HH:mm>
- 当前版本:v1
```

## 1. 任务概述
用 2-5 句话概括:**本任务为哪些功能编写/补充了单元测试,核心覆盖了哪些场景,关键测试设计决策是什么**。

## 2. 测试范围总览

### 2.1 本任务测试
- 被测模块/文件:<文件列表>
- 新增测试文件:<文件列表>
- 修改测试文件:<文件列表>

### 2.2 相关模块测试(防回归)
- 涉及的相关模块:<模块列表>
- 跑过的相关测试:<测试列表>
- 是否有相关测试失败:<是 / 否>;若否,简单说明

## 3. 测试覆盖的场景

按场景类别列出,每个场景标注对应的测试用例名与详细设计出处:

| 场景类别 | 场景描述 | 测试用例 | 对应设计 |
| --- | --- | --- | --- |
| 正常路径 | 用户名+密码正确 → 登录成功 | test_login_success | 详细设计 §7.1 |
| 边界条件 | 用户名为空 | test_login_empty_username | 详细设计 §8 异常路径 |
| 边界条件 | 密码超长(>256字符) | test_login_password_too_long | 详细设计 §8 |
| 异常路径 | 密码错误 | test_login_wrong_password | 详细设计 §8 |
| 异常路径 | 账号被锁定 | test_login_account_locked | 详细设计 §10 状态机 |
| 鉴权/授权 | 未登录访问受保护资源 | test_protected_resource_unauthenticated | 详细设计 §9 |
| ... | ... | ... | ... |

## 4. 关键测试设计决策
开发测试时做的**设计层面**选择:
- 决策:对外部 HTTP 调用使用什么 mock
  - 备选:nock / msw / 自建 stub
  - 选择:...
  - 理由:...
- 决策:用什么边界值
  - 备选:min / max / 0 / 负数
  - 选择:...
  - 理由:...

## 5. 偏离设计/规范的地方
指向 `deviations.md` 的条目,本节只放摘要:
- 偏离 1:...(详见 `deviations.md §1`)
- 偏离 2:...

## 6. 测试结果

### 6.1 运行结果
- 命令:<command>
- 时间:YYYY-MM-DD HH:mm
- 通过:<n> / 失败:<n> / 跳过:<n>
- 结论:✅ 全部通过 / ❌ 有失败(<list>详见 `test-results.md`)/ ⚠️ 部分跳过

### 6.2 覆盖率
- 整体覆盖率(本次测试运行的整体项目):行 <X>% / 分支 <Y>% / 函数 <Z>%
- 本任务代码覆盖率:
  - <src/auth/login.ts>:行 <X>% / 分支 <Y>% / 函数 <Z>%
  - ...
- 详细分析:`coverage-analysis.md`

## 7. 已知未覆盖的地方与理由
| 位置 | 原因 | 是否需补 |
| --- | --- | --- |
| src/utils/uuid.ts | 第三方库的薄包装 | 否(豁免) |
| src/auth/login.ts:42-45 | 需要 mock 时间与并发,场景过窄 | 是(建议在 code-it 修代码后补) |
| ... | ... | ... |

## 8. 发现的代码 bug 列表
指向 `deviations.md` 中"发现的代码 bug"部分(若步骤 11 触发):
- Bug 1:src/auth/login.ts:42 — 详见 `deviations.md §X`
  - 建议处理:回到 `code-it` 修复后,重跑本技能验证
- Bug 2:...

## 9. 关联任务
- 前置任务:<T-XXX>(code-it 的同任务)
- 后置任务:<T-XXX>(其他依赖本测试的任务)
- 关联 code-review 链接(若已发起):<PR/MR URL 或 commit>

## 10. 变更记录
| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-04 16:00 | v1 | 初始完成 | 编写/补充 <N> 个测试,全部通过,覆盖率 <X>%,测试状态→已运行-通过 | <测试者> |
| 2026-06-05 10:00 | v1.1 | 补充覆盖 | 发现一处边界未覆盖,补充 test_xxx,重跑通过 | <测试者> |
