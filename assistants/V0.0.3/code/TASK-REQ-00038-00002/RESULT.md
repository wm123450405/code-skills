# TASK-REQ-00038-00002 — [修改] code-it 步骤 8a 守卫位置 + 步骤 8.5 单测输出位置扩展

- 任务编码:TASK-REQ-00038-00002
- 需求编码:REQ-00038
- 所属版本:V0.0.3
- 任务类型:修改
- 触发/来源:详细设计
- 任务来源:./assistants/V0.0.3/plan/REQ-00038/PLAN.md §3
- 详细设计:./assistants/V0.0.3/plan/REQ-00038/RESULT.md §5 算法 2/3 + §4 模块 2/3
- 状态:已完成
- 责任人:wangmiao
- 创建:2026-06-22 14:00
- 完成:2026-06-22 14:10
- 当前版本:v1

## 1. 任务概述

把 `code-it` 步骤 8a 守卫检查位置从 CWD 根改为模块目录(7 项守卫字面字节级沿用 REQ-00034,仅位置扩展);扩展步骤 8.5 单测输出位置,识别各模块的约定测试目录(7 层优先级链 + CWD 根退化)。**关键产出**:`code-it/SKILL.md` 5 处字面改写(8a.1 / 8a.2 / 8a.4 / 8.5.2 / 8.5.5)+ 净变化 +37/-8 行。

## 2. 改修内容总览

### 2.1 文件变更

| 变更类型 | 文件路径 | 说明 |
| --- | --- | --- |
| 修改(5 处字面改写) | plugins/code-skills/skills/code-it/SKILL.md | §8a.1 / §8a.2 / §8a.4 / §8.5.2 / §8.5.5 既有逻辑 0 改,字面增量 |

### 2.2 提交记录

- 分支:main
- 提交哈希:17ba4ca
- 提交信息:chore(code-it): REQ-00038 TASK-2 code-it 步骤 8a 守卫位置 + 步骤 8.5 单测输出位置扩展

## 3. 详细改动

### 文件:plugins/code-skills/skills/code-it/SKILL.md

#### §步骤 8a.1 守卫检查项清单(L682)

- **变更类型**:字面改写
- **变更要点**:
 - 既有: "仅检查项目根,**不**递归子目录(避免误判)。命中任一 → 可测,全部不命中 → 不可测。"
 - 改为: "仅检查模块目录(对每个识别的模块独立执行 7 项检查),**不**递归子目录(避免误判)。命中任一 → 该模块可测,全部不命中 → 该模块不可测;**整体** testable = 至少 1 个模块命中。"
- **关键逻辑**:仅位置从 CWD 根 → 模块目录,7 项检查项字面字节级保留
- **对应设计章节**:详细设计 §5 算法 2 + §4 模块 2
- **依据规范**:`./assistants/rules/skill-conventions.md §规则 1/2` + NFR-4(7 项守卫字节级沿用)

#### §步骤 8a.2 守卫判定逻辑(L696)

- **变更类型**:字面改写
- **变更要点**:
 - 既有: "1. 顺序执行 7 项检查 ... 2. 命中任一 → testable = True ... 3. 全部不命中 → testable = False ..."
 - 改为: "1. 接收步骤 8a.0 模块识别结果 modules: string[] 2. 对每个模块独立执行 7 项检查 ... 3. 聚合:至少 1 个模块命中 → testable = True ... 4. 全部模块不命中 → testable = False ..."
- **关键逻辑**:
 - 步骤 1 接收 T-1 模块识别结果
 - 步骤 2-3 对每个模块独立执行 7 项检查
 - 步骤 4 聚合判定
- **对应设计章节**:详细设计 §5 算法 2 + §4 模块 2
- **依据规范**:同上

#### §步骤 8a.4 屏幕报告格式(L720)

- **变更类型**:字面增量(屏显格式扩展)
- **变更要点**:
 - 既有:单模块"守卫通过" 屏显格式(7 项检查结果列表)
 - 改为:2 种屏显格式(多模块命中示例 + 单模块命中示例,字节级沿用 REQ-00034 行为)
 - 多模块命中示例:每个模块独立显示 7 项检查结果 + 模块识别结果
 - 单模块命中示例:`模块识别:1 个模块 [. ]` + `模块 . :`(与 REQ-00034 字节级沿用)
- **关键逻辑**:`code-auto` 编排者期望看到"模块级守卫检查结果"而非"工程根级"
- **对应设计章节**:详细设计 §4 模块 2 + §6 接口 2 屏显契约
- **依据规范**:同上

#### §步骤 8.5.2 任务性质自动判定(L778)

- **变更类型**:字面改写
- **变更要点**:
 - 既有: "写单测到 CWD 下项目测试目录;跑通(`Bash` 跑项目测试命令);写 `unit-test-results.md`"
 - 改为: "对每个通过的模块识别其约定测试目录(7 层优先级链:package.json#scripts.test 含 jest/vitest/mocha → pyproject.toml#tool.pytest.ini_options → Cargo.toml → go.mod → pom.xml/build.gradle → 模块根 test/ → CWD 根 test/ 退化);多模块分别写单测;跑通(`Bash` 跑项目测试命令);写 `unit-test-results.md`"
- **关键逻辑**:
 - 7 层测试目录识别优先级链(主流语言:Node.js / Python / Rust / Go / Java / 通用 / 退化)
 - 多模块通过 → 多模块分别写单测
- **对应设计章节**:详细设计 §5 算法 3 + §4 模块 3
- **依据规范**:`./assistants/rules/module-conventions.md §规则 1` + NFR-2 兼容性

#### §步骤 8.5.5 产出物格式(L820)

- **变更类型**:字面改写
- **变更要点**:
 - 既有: "Markdown,7 字段(沿用`code-it/templates/RESULT.md` 模板新增"## 单元测试"小节)"
 - 改为: "Markdown,既有"## 单元测试(由 code-it 内化)"小节字节级保留(沿用 NFR-10),**追加**"## 各模块单测结果"小节(沿用`code-it/templates/RESULT.md` 模板多模块支持,字段:模块路径 / 守卫检查 / 检查位置 / 测试框架 / 测试文件 / 跑通情况)"
- **关键逻辑**:
 - 既有"## 单元测试"小节字节级保留(NFR-4 锁定)
 - 新增"## 各模块单测结果"小节(T-3 实施)
- **对应设计章节**:详细设计 §4 模块 5
- **依据规范**:`./assistants/rules/module-conventions.md §规则 1` + INV-4

## 4. 关键决策与权衡

开发过程中做的**设计层面**选择:

- **决策 1**:既有 7 项守卫字面**字节级沿用**,仅位置扩展
 - 备选:重新设计 7 项守卫(更细粒度,如按 monorepo 类型动态调整)
 - 选择理由:NFR-4 强约束(7 项守卫是已落地的产品需求,REQ-00009);检查项本身不改避免行为漂移
- **决策 2**:判定逻辑扩展为"任一模块通过即 testable = True"
 - 备选:全部模块通过才 testable = True(更严格)
 - 选择理由:原"工程根级单点判定"细化到"模块级任一通过";FR-2 锁定
- **决策 3**:7 层测试目录识别优先级链(从语言规范到退化)
 - 备选:固定写到模块根 test/(简化)
 - 选择理由:各语言测试约定差异大(Go `*_test.go` 同包 / Python `test_*.py` / Node `__tests__/` / Rust `tests/`);沿用既有规范化

## 5. 偏离设计/规范的地方

指向 `deviations.md` 的条目,本节只放摘要:
- 偏离 0:无偏离(详见 `deviations.md`)

## 6. 验证结果

### 6.1 编译
- 命令:`N/A`(纯 Markdown 改造)
- 结论:✅ 不适用(纯文档改动)

### 6.2 启动
- 命令:`N/A`(纯 Markdown 改造)
- 结论:✅ 不适用(纯文档改动)

### 6.3 测试(若适用)
- 命令:`N/A`(纯 Markdown 改造,无单测需求)
- 通过 / 失败 / 跳过:跳过(本任务测试状态 = 不适用)

## 7. 已知问题 / 未完成项

- [ ] 无(本任务为本需求 3 任务中的第 2 步,实施范围完整,0 已知问题)
- [ ] 待 T-3 验证(模板追加 + 端到端验证)

## 8. 过程文档清单(由 code-it 内化,BUG-00004 新增)

### 8.1 工作流上下文
- `decisions.workLog`:<'生成'>
- `decisions.compileAndRun`:<'不生成'>(纯 Markdown 改造)
- `decisions.deviations`:<'生成'>
- `decisions.testResults`:<'不生成'>(任务测试状态 = 不适用)
- `decisions.unitTestResults`:<'不生成'>(项目不可测 — 7 项守卫全不命中)
- `decisions.kanbanChangeLog`:<'生成'>(本轮有追加)
- `decisions.processDocDecisions`:<'生成'>

### 8.2 决策结果表

| 过程文档 | 决策 | 理由 |
| --- | --- | --- |
| `work-log.md` | ✅ 生成 | 任务实施日志是核心(始终生成) |
| `compile-and-run.md` | ❌ 不生成 | 本任务为纯 Markdown 改造 |
| `deviations.md` | ✅ 生成 | 评审要查(始终生成) |
| `test-results.md` | ❌ 不生成 | 本任务测试状态 = 不适用 |
| `unit-test-results.md` | ❌ 不生成 | 项目不可测(7 项守卫全不命中) |
| 看板"变更记录" | ✅ 生成 | 本轮有追加 |
| `process-doc-decisions.md` | ✅ 生成 | 其他任一不生成 → 本节生成 |

### 8.3 决策依据
- `decisions` 字典由 `code-it` 步骤 8.7 物化(`code-it/SKILL.md` line 805+ BUG-00004 新增)
- 判定准则沿用 `code-it/SKILL.md` "## 过程文档自适应判定"(line 101-138)

### 8.4 关联任务
- 前置任务:TASK-REQ-00038-00001(已完成,提供 `modules: string[]` 输入)
- 后置任务:TASK-REQ-00038-00003(本任务的产出被依赖)
- 取代任务:无
- 关联 code-check / code-unit 链接:无(待后续 code-check 评审)

## 9. 单元测试(由 code-it 内化,新增,

- 守卫判定:不可测(`code-it` 步骤 8a.2 沿用 `code-unit` 步骤 0a.2 — 本仓库 7 项守卫全不命中)
- 测试框架:N/A(项目不可测)
- 新增/修改的测试文件:无(本任务为纯 Markdown 改造,无代码改动)
- 跑通情况:N/A(未跑测试)
- 跳过的子任务:守卫不通过 → 步骤 8.5 跳过
- 发现的代码 bug:无

## 10. 逻辑行统计(由 code-it 内化,新增)

| 文件 | 逻辑行(新增) | 逻辑行(总规模) | 检测方式 |
| --- | --- | --- | --- |
| plugins/code-skills/skills/code-it/SKILL.md | 37 | 1408 | heuristic |
| **本任务汇总** | **37** | **1408** | — |

- 检测方式:heuristic(本仓库无 `tokei` / `cloc`)
- 阈值默认值:单文件逻辑行(总规模) ≤ 500 / 单文件逻辑行(新增) ≤ 200(沿用 `logic-loc-defaults.md`)
- 失败兜底:N/A(本任务 `git diff` 正常)

## 11. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-22 14:10 | v1 | 初始完成 | 完成 TASK-REQ-00038-00002 实施,共修改 1 个文件,5 处字面改写,所有验证通过(纯 Markdown 改造不生成 `compile-and-run.md` / `test-results.md`;7 项守卫全不命中 → `unit-test-results.md` 不生成;8.7 物化 → `process-doc-decisions.md` 生成;既有 7 项守卫字面字节级保留 INV-3 / NFR-4 满足) | wangmiao |
