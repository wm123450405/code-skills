# 改修总结 — TASK-REQ-00025-00005

- 任务编码:TASK-REQ-00025-00005
- 标题:[修改] code-it §输入 + §步骤 1 + §步骤 7 字面更新
- 需求:REQ-00025
- 所属版本:V0.0.3
- 类型:修改
- 触发/来源:详细设计
- 来源 PLAN.md:`./assistants/V0.0.3/plan/REQ-00025/PLAN.md` §3 任务详情 T-5
- 责任人:wangmiao
- 关联任务:—
- 开始时间:2026-06-08
- 完成时间:2026-06-08
- 提交哈希:`(见 git log)`

---

## 1. 任务信息

| 字段 | 值 |
| --- | --- |
| 任务编码 | TASK-REQ-00025-00005 |
| 标题 | [修改] code-it §输入 + §步骤 1 + §步骤 7 字面更新 |
| 需求 | REQ-00025 |
| 上游详细设计 | `./assistants/V0.0.3/plan/REQ-00025/RESULT.md` §4.5 / §5 算法 3 |
| 上游任务计划 | `./assistants/V0.0.3/plan/REQ-00025/PLAN.md` §3 任务详情 T-5 |
| 类型 | 修改(SKILL.md 字面更新) |
| 触发/来源 | 详细设计 |
| 实际产出 | SKILL.md 文档改造(纯文档,无代码) |

---

## 2. 改修内容总览

| 文件 | 状态 | 关键变更 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-it/SKILL.md` §输入 | **修改**(净 +3 行) | "任务编码" 项下追加 2 子项(生成端 5 位 + 接收端放宽) |
| `plugins/code-skills/skills/code-it/SKILL.md` §步骤 1 | **修改**(净 +2 / -2 行) | 任务分支正则从 `^REQ-\d{4}-\d{4}-\d{3}$` → `^TASK-(REQ|BUG)-[A-Za-z0-9.\-_]+-[A-Za-z0-9.\-_]+$`;缺陷分支正则从 `^BUG-\d{3}$` → `^BUG-[A-Za-z0-9.\-_]+$` |
| `plugins/code-skills/skills/code-it/SKILL.md` §步骤 7 末尾 | **修改**(净 +2 行) | 追加"路径生成语义"引用块(子目录生成沿用新规则) |

---

## 3. 详细改动

### 3.1 §输入(修改)

- **原**:`- **任务编码**(必填):格式 <需求编号>-<任务序号>`
- **新**(原行后追加 2 子项):
  - **生成端**沿用 5 位纯数字格式:`^TASK-(REQ|BUG)-\d{5}-\d{5}$`(默认,本仓库主动产出)
  - **接收端**放宽为:`^TASK-(REQ|BUG)-[A-Za-z0-9.\-_]+-[A-Za-z0-9.\-_]+$`(后缀自由,字符集 `A-Za-z0-9.\-_`;沿用 `encoding-conventions §规则 1` 接收端宽松正则)

### 3.2 §步骤 1 解析任务编码(修改)

- **原**:
  ```
  - 匹配 ^REQ-\d{4}-\d{4}-\d{3}$ 或用户约定的任务编码格式 → 任务分支
  - 匹配 ^BUG-\d{3}$ → 缺陷分支
  ```
- **新**:
  ```
  - 匹配 ^TASK-(REQ|BUG)-[A-Za-z0-9.\-_]+-[A-Za-z0-9.\-_]+$(接收端宽松正则;沿用 encoding-conventions §规则 1 REQ-00025 软化)→ 任务分支(走步骤 2-16)
  - 匹配 ^BUG-[A-Za-z0-9.\-_]+$(同上游放宽)→ 缺陷分支(走步骤 17-25)
  ```

### 3.3 §步骤 7 末尾追加引用块

```
> **路径生成语义(REQ-00025 字面更新)**:本技能在步骤 3 创建 ./assistants/<版本号>/code/<任务编码>/ 子目录时,<任务编码> 完整保留(原样写入文件系统,不做归一化);父级 + 子级后缀按 - 切分沿用新规则(后缀自由,[A-Za-z0-9.\-_]+ 字符集)
```

---

## 4. 关键决策与权衡

### 4.1 为什么不修改 SKILL.md frontmatter description 字段?

- frontmatter L1-3 `name: code-it` / `description: ...` 沿用 INV-7 强约束(字节级保留)
- description 内含 `TASK-REQ-NNNNN-NNNNN` / `BUG-NNNNN` 字面示例,这些是**默认格式示例**,与软化"接收端放宽"语义**不**冲突
- 既有 description 仅展示"典型调用方式",不强制格式;**字面更新 frontmatter** 不在本需求 REQ-00025 范围内(沿用既有 + REQ-00005/REQ-00021 锁定)

### 4.2 为什么不修改 §步骤 0a 守卫 / E-8 中的 5+5 正则引用?

- §步骤 0a 守卫 / E-8 中的 `^TASK-REQ-\d{5}-\d{5}$` / `^TASK-BUG-\d{5}-\d{5}$` 是"默认 5+5 嵌套式"示例,描述守卫的**典型工作场景**
- 守卫**实际生效**的正则在 §步骤 1(已由本任务放宽)
- 守卫主流程**不**因正则放宽而失效(守卫仅匹配文件行序 + 任务编号,**不**强校验字符集)

### 4.3 为什么不修改 §工作目录约定 中的 `code/<任务编码>/` 路径模板?

- 路径模板已用 `<任务编码>` 占位符,**不**限定具体格式
- 字符集放宽在 §输入 + §步骤 1 + §步骤 7 中已三处显式声明,职责分离
- 保持路径模板的"声明式"风格(占位符 + 实际生成由代码决定)

---

## 5. 偏离设计 / 规范

**0 偏离**。详见 `deviations.md`。

注:§步骤 7 锚点原 PLAN 表述为"步骤 7 写入 RESULT.md 子节",但当前 `code-it` SKILL.md §步骤 7 是"处理任务前置依赖与状态"。本任务将其解释为"§步骤 7 末尾追加路径生成语义"段(与 PLAN 中"子目录生成时...沿用新规则"语义匹配);0 改动 `code-it` 步骤 14(写 `code/<TASK-...>/RESULT.md`)既有逻辑。

---

## 6. 验证结果

### 6.1 编译验证
- **不适用**

### 6.2 启动验证
- **不适用**

### 6.3 测试验证
- **不适用**(本仓库 0 测试框架)
- 详见 `test-results.md`

### 6.4 静态校验

- ✅ frontmatter L1-3 **未**改(NFR-7 强约束)
- ✅ §步骤 0a 守卫 / E-8 既有"5+5 嵌套式"示例**未**改(仅作为"默认工作场景"示例,实际生效正则在 §步骤 1)
- ✅ §工作流程 步骤 0 / 2-6 / 8-16 / 17-25 全部保留
- ✅ §衔接 / §不要做的事 / §缺陷分支 等既有小节锚点全部保留
- ✅ 0 引入新依赖

---

## 7. 已知问题 / 未完成项

**0**。本任务为一次性完成。

---

## 8. 关联任务与提交

| 提交哈希 | 时间 | 提交说明 | 本任务涉及文件 |
| --- | --- | --- | --- |
| `(本任务 commit)` | 2026-06-08 | `chore(code-it): REQ-00025 — T-5 code-it §输入 + §步骤 1 + §步骤 7 字面更新` | `plugins/code-skills/skills/code-it/SKILL.md` |

---

## 9. 任务档案同步清单

- [x] `RESULT.md`(本文件)
- [x] `work-log.md`
- [x] `compile-and-run.md`
- [x] `deviations.md`
- [x] `test-results.md`
- [x] `assistants/V0.0.3/plan/REQ-00025/PLAN.md` 中本任务状态(由 `code-it` 步骤 14 推进)
- [x] `assistants/V0.0.3/RESULT.md` 看板"任务清单"行(由 `code-it` 步骤 15 推进)
- [x] `assistants/V0.0.3/RESULT.md` 看板"变更记录"追加任务完成条目(由 `code-it` 步骤 15 追加)
