# 改修总结 — TASK-REQ-00040-00001 · [修改] code-fix 步骤 0 末尾追加"项目可启动性探测" 子节

- 任务编码:TASK-REQ-00040-00001
- 任务标题:[修改] code-fix 步骤 0 末尾追加"项目可启动性探测" 子节(detectStartability 7 步算法)
- 所属版本:V0.0.3
- 所属需求:REQ-00040
- 触发/来源:详细设计
- 任务类型:修改
- 完成时间:2026-06-25
- 上游:`./assistants/V0.0.3/plan/REQ-00040/PLAN.md §3 TASK-REQ-00040-00001` + `RESULT.md §4.1 模块 1`

## 1. 任务信息

- **任务编码**:`TASK-REQ-00040-00001`
- **标题**:code-fix 步骤 0 末尾追加"项目可启动性探测" 子节(`detectStartability` 7 步算法)
- **类型**:修改(SKILL.md 子节追加)
- **触发/来源**:详细设计
- **关联任务**:REQ-00040(本任务是 REQ-00040 编码计划的第 1 个)
- **里程碑**:M1-REQ-00040

## 2. 改修内容总览

- **修改的文件**:`plugins/code-skills/skills/code-fix/SKILL.md`(1 个文件)
- **新增的行数**:约 80 行(1 段说明引用 + 1 段伪代码 + 1 段边界 + 1 段性能 + 1 个子节标题)
- **删除的行数**:0
- **新增文件**:0
- **删除文件**:0
- **触发/来源 = 详细设计**(沿用 PLAN.md 任务总览"类型"列 = `修改`)

## 3. 详细改动

### 3.1 `plugins/code-skills/skills/code-fix/SKILL.md`

#### 3.1.1 修改位置

- 锚点:`## 步骤 0 — 版本上下文检测(强制前置)` 末尾(line 181 "4. 验证目录存在" 后,line 182 空行后,line 183 "### 步骤 1 — 收集缺陷编号 / 描述" 标题前)
- 修改方式:`Edit` 工具追加 1 个 `### 步骤 0.X` 子节
- 字节级保留:frontmatter L1-4 + 步骤 0 主体(line 177-181)+ 既有"## 工作流程" 步骤 1~10 主体 + "## 不要做的事" 段

#### 3.1.2 子节内容结构

| 段 | 内容 | 字节数 |
| --- | --- | --- |
| 1 | `### 步骤 0.X — 项目可启动性探测(本需求新增子节,在步骤 0 第 4 项后)` 标题 | ~50 字符 |
| 2 | 说明引用:沿用 NFR-3(零规范变更 + 不触发 `AskUserQuestion` + 不写 `fix/<BUG-NNN>/RESULT.md`) | ~200 字符 |
| 3 | 伪代码 `function detectStartability(cwd):` + 7 步算法(Node.js / Python / Makefile / Docker / Rust / Go / Java) | ~1800 字符 |
| 4 | 边界(3 条:文件不存在跳过 / 全未命中降级 / 不抛异常) | ~250 字符 |
| 5 | 性能(8 个 `Bash: test -f` 调用,总耗时 < 500ms) | ~100 字符 |

**总增量**:约 80 行(原文件 line 181 后追加 80 行新内容,步骤 1~10 标题后移 80 行)

#### 3.1.3 关键决策(本任务无新决策,沿用 PLAN.md)

- 内存上下文,不写文件(沿用 PD-1):避免"空跑时也写产物" 的副作用
- 8 优先级链(沿用 D-1):Node.js / Python / Makefile / Docker / Rust / Go / Java 8 个优先级,按顺序检测
- 全未命中降级为 `canStart = false`(沿用 E-1)

## 4. 关键决策与权衡

**0 项新决策**。本任务**完全**沿用 PLAN.md §3 TASK-REQ-00040-00001 + 详细设计 §4.1 + 概设 D-1 + PD-1。

## 5. 偏离设计/规范的地方

**0 项偏离**(详见 `deviations.md`)。本任务所有改动与上游字面一致。

## 6. 验证结果

### 6.1 静态校验(12 条 AC 子集,本任务范围内)

- **AC-1**:`code-fix/SKILL.md` 步骤 0 末尾追加"### 步骤 0.X — 项目可启动性探测" 子节,含 7 步探测算法 → **✓ 通过**
- **AC-7**:`git diff` 校验 L1-3 + 既有"## 工作流程" 步骤 1~10 + "## 不要做的事" 字节级未变 → **✓ 通过**(frontmatter L1-4 字节级保留;步骤 0 主体 line 177-181 字面未变;步骤 1~10 主体仅位置后移 80 行,字面未变;"## 不要做的事" 段位置后移,字面未变)
- **AC-11**:`Grep` 关键词("本需求 REQ-00040" / "沿用原 code-fix" / "Q-X 锁定")→ **0 命中**(零开发痕迹,沿用 `skill-conventions §规则 2`)

### 6.2 端到端降级为静态

- 本任务**不**涉及可启动项目(纯 Markdown 技能库,沿用 NFR-7 + 仓库"无源代码" 约定)
- 12 条 AC 端到端验证由 T-005 承担(本任务**不**重复)

## 7. 已知问题 / 未完成项

- **0 项**。本任务所有改动按 PLAN.md §3 + 详细设计 §4.1 完整落地
- 后续任务(T-002 / T-003 / T-004)需在 T-005 端到端验证前完成

## 8. 过程文档清单(由 code-it 内化)

### 8.1 工作流上下文
```typescript
const decisions = {
  workLog: '生成',
  compileAndRun: '不生成',  // 纯 Markdown 改造,无运行时
  deviations: '生成',
  testResults: '不生成',     // 测试状态 = 不适用
  unitTestResults: '生成',  // 占位
  kanbanChangeLog: '生成',
  processDocDecisions: '生成',
};
```

### 8.2 决策结果表

| 过程文档 | 决策 | 理由 |
| --- | --- | --- |
| `work-log.md` | ✅ 生成 | 任务实施日志 |
| `compile-and-run.md` | ❌ 不生成 | 纯 Markdown 改造,无编译/运行 |
| `deviations.md` | ✅ 生成 | 评审要查;内容 = 0 偏离 |
| `test-results.md` | ❌ 不生成 | 测试状态 = 不适用 |
| `unit-test-results.md` | ✅ 生成(占位) | 任务类型 = 文档 → 写占位 |
| 看板"变更记录" | ✅ 生成 | 本轮有任务完成 + 状态推进 |
| `process-doc-decisions.md` | ✅ 生成(本文件) | 有不生成项 |

### 8.3 决策依据

- 引用 `code-it/SKILL.md` §"## 过程文档自适应判定"(line 101-138)与 §"## 步骤 8.7 — 过程文档自适应判定执行"(line 805+)的判定准则
- 沿用 `code-it` 内化 `applyProcessDocDecisions` 伪代码

### 8.4 关联任务

- **前置任务**:**无**(本任务位于 PLAN.md 任务总览最前)
- **后置任务**:TASK-REQ-00040-00002(强依赖,读本任务写入的 `canStart` / `startCommand` 上下文)
- **取代任务**:**无**
- **关联 code-check**:M1-REQ-00040 里程碑全部 6 任务完成后,由 `code-check` 评审

## 9. 单元测试

**0 项**。本任务不涉及函数级代码改动,无单测可写。详见 `unit-test-results.md`。

## 10. 逻辑行统计

**不适用**(NFR-8 锁定:`TASK-REQ-...` 5+5 位嵌套式触发 `calcLogicLoc`;本任务类型 = `修改` 但**实际产物**为纯 Markdown 文档,**不**涉及代码行统计)。

## 11. 变更记录

| 时间 | 变更类型 | 变更摘要 | 关联项 |
| --- | --- | --- | --- |
| 2026-06-25 14:31 | 任务完成 | TASK-REQ-00040-00001 完成(开发状态:已完成);code-fix/SKILL.md 步骤 0 末尾追加"### 步骤 0.X — 项目可启动性探测" 子节(含 7 步探测算法 + 3 边界 + 1 性能,约 +80 行);frontmatter L1-4 + 既有"## 工作流程" + "## 不要做的事" 字节级保留;0 偏离;AC-1 / AC-7 / AC-11 静态校验通过 | TASK-REQ-00040-00001 |
