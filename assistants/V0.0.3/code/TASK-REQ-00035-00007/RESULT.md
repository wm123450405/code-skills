# 改修总结 — TASK-REQ-00035-00007
- 任务:code-dashboard 解析兼容(变更记录行数自适应)
- 状态:已完成 | 时间:2026-06-15 20:07 | 提交:<待 commit>

## 1. 改修内容
- `plugins/code-skills/skills/code-dashboard/SKILL.md` +10 行(在 §工具使用约定后插入"## 解析兼容"小节)

## 2. 关键决策
- 兼容空表 / 短表 / 长表(沿用 `Grep "^| .* |$"` 匹配所有表格行)
- 沿用 `^## .*$` 定位 + `^\| .* \|$` 匹配锚点(零字段扩展,NFR-7)
- 不调用 Write / Edit / Bash(NFR-7 严守)

## 3. 偏离
- 0 偏离

## 4. 验证
- §工具使用约定既有内容字节级保留
- frontmatter L1-3 字节级保留
