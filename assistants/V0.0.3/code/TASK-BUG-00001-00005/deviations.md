# 偏离记录 — TASK-BUG-00001-00005
版本:V0.0.3
时间:2026-06-07 17:54

## 偏离:**0 偏离**(本任务)

本任务严格按 `fix/BUG-00001/PLAN.md §3.5` 任务详情实施,**0 偏离**。

## 关键决策

### D-1:与既有"## 工作目录约定"段中"唯一允许"短语的关系(code-it)

- **决策**:本任务新增独立小节"## 唯一允许的生产代码改动场景",与既有"## 工作目录约定"段(line 58)中"这是唯一允许的生产代码改动场景"短语**并存**
- **理由**:
  - 既有 line 58 是**内嵌**在"## 工作目录约定"段中的短语(描述工作空间边界)
  - 本任务新增的"## 唯一允许的生产代码改动场景"是**独立**声明小节(显式声明 code-it 的核心职责)
  - 两者语义完全一致,但粒度不同:前者是"工作目录约定"的内嵌,后者是"职责声明"的独立段
  - 不冲突,可读性更好(职责声明独立成段便于搜索)
- **影响**:0(纯文档组织差异,无功能影响)

### D-2:2 个 Edit 合并 1 任务(code-it + code-unit)

- **决策**:按 PLAN.md §3.5 合并为 1 任务(2 个 SKILL.md 各 1 段)
- **理由**:2 段声明存在"反向引用"(code-it 声明反向引用 code-unit,code-unit 声明反向引用 code-it),独立 2 任务会失去语义连贯性;且 2 段都很短(130-150 字符),独立任务的"文档开销"会大于实际工作
- **影响**:0(任务颗粒度"1 任务 = 1 个完整功能点"沿用 REQ-00014 原则,本任务即"2 个边界声明"作为一个完整功能点)

## 残余风险
**0 残余风险**

## 备注
本任务为 BUG-00001 修复的第 5 步(T-5,最后一步),完成"`code-it` 唯一可改 SKILL.md + `code-unit` 仅可改测试代码"的边界声明。完成本任务后,BUG-00001 5 任务全部实施完成,等待 `code-fix BUG-00001` 推进状态至"已修复-待验证"。
