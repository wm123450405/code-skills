# 改修总结 — TASK-REQ-00040-00002 · [修改] code-fix 步骤 6 末尾追加"复现产物登记" 子节

- 任务编码:TASK-REQ-00040-00002
- 任务标题:[修改] code-fix 步骤 6 末尾追加"复现产物登记" 子节(reproduceBug 9 步算法 + executeStep 3 类采集)
- 所属版本:V0.0.3
- 所属需求:REQ-00040
- 触发/来源:详细设计
- 任务类型:修改
- 完成时间:2026-06-25
- 上游:`./assistants/V0.0.3/plan/REQ-00040/PLAN.md §3 TASK-REQ-00040-00002` + `RESULT.md §4.2 模块 2`

## 1. 任务信息

- **任务编码**:`TASK-REQ-00040-00002`
- **标题**:code-fix 步骤 6 末尾追加"复现产物登记" 子节(`reproduceBug` 9 步算法 + `executeStep` 3 类采集)
- **类型**:修改(SKILL.md 子节追加)
- **触发/来源**:详细设计
- **关联任务**:REQ-00040
- **里程碑**:M1-REQ-00040
- **强前置依赖**:TASK-REQ-00040-00001(`reproduceBug()` 读 T-001 写入的内存上下文 `canStart` / `startCommand`)

## 2. 改修内容总览

- **修改的文件**:`plugins/code-skills/skills/code-fix/SKILL.md`(1 个文件)
- **新增的行数**:约 140 行(1 段说明引用 + 触发条件 3 条 + 9 步算法伪代码 + executeStep 3 类分发 + 11 边界 + 1 复合边界 + 屏显契约 2 段 + 性能 1 段)
- **删除的行数**:0
- **新增文件**:0
- **删除文件**:0
- **触发/来源 = 详细设计**(沿用 PLAN.md 任务总览"类型"列 = `修改`)

## 3. 详细改动

### 3.1 `plugins/code-skills/skills/code-fix/SKILL.md`

#### 3.1.1 修改位置

- 锚点:`## 步骤 6 — 写缺陷详情 RESULT.md` 末尾(line 369 "3. 若有 `fix-plan.md`..." 第 3 步 后,line 371 "**关键:不重写 `RESULT.md` 的稳定章节**" 注释前,line 373 "### 步骤 7 — 写缺陷总览" 标题前)
- 修改方式:`Edit` 工具追加 1 个 `### 步骤 6.X` 子节
- 字节级保留:frontmatter L1-4 + 步骤 6 主体(line 343-369)+ "**关键:不重写**" 注释(line 371,**只 1 次**)+ 步骤 7-10 主体 + "## 过程文档格式" + "## 与其他技能的关系" + "## 不要做的事" 段

#### 3.1.2 子节内容结构

| 段 | 内容 | 字节数 |
| --- | --- | --- |
| 1 | `### 步骤 6.X — 复现产物登记(本需求新增子节,在"**关键:不重写**"注释前)` 标题 | ~80 字符 |
| 2 | 说明引用:沿用 NFR-4(失败降级不阻断)+ NFR-9(不修改项目源码)+ NFR-3(不触发 `AskUserQuestion` + 不新增 CLI 参数) | ~250 字符 |
| 3 | 触发条件 3 条:`context.canStart == true` ∧ 新建分支 ∧ 有复现步骤 | ~250 字符 |
| 4 | 9 步 `reproduceBug()` 算法伪代码(mkdir → 启动子进程 → 等 5s → 执行步骤 → 终止子进程 → 合并日志 → 写元信息 → 写"## 复现产物登记" 区段 → return) | ~2200 字符 |
| 5 | `executeStep(step, reproduceDir)` 3 类分发(cli / http / browser)伪代码 | ~1200 字符 |
| 6 | 11 边界 + 1 复合边界表格(E-1 ~ E-12) | ~1800 字符 |
| 7 | 屏显契约 2 段(启动 + 完成) | ~500 字符 |
| 8 | 性能说明(< 60s) | ~80 字符 |

**总增量**:约 140 行(原文件 line 369 后追加 140 行新内容,后续章节后移 140 行)

#### 3.1.3 关键决策(本任务无新决策,沿用 PLAN.md)

- 子进程在 `code-fix` 主进程 cwd 下启动,**不**改 cwd(沿用 PD-2,NFR-9 不修改项目配置)
- 子进程终止采用 `kill -SIGTERM 5s 后 kill -SIGKILL`(沿用 PD-3,Linux 进程终止惯例)
- `run.log` 时间戳格式 `YYYY-MM-DD HH:mm:ss`(沿用 PD-4)
- `RESULT-meta.json` 单行 JSON(沿用 PD-5)
- 截图采集**只**在 web 服务时触发(沿用 PD-6);交互数据**只**在 web/api 服务时触发(沿用 PD-7)
- 任何失败 → 屏显 `⚠` + 记录到 `RESULT-meta.json` 的 `failureReason` + 继续登记(沿用 NFR-4)

## 4. 关键决策与权衡

**0 项新决策**。本任务**完全**沿用 PLAN.md §3 TASK-REQ-00040-00002 + 详细设计 §4.2 + 概设 D-2/D-3/D-4/D-5 + 详设 PD-2/PD-3/PD-4/PD-5/PD-6/PD-7。

## 5. 偏离设计/规范的地方

**0 项偏离**(详见 `deviations.md`)。本任务所有改动与上游字面一致。

## 6. 验证结果

### 6.1 静态校验(12 条 AC 子集,本任务范围内)

- **AC-2**:`code-fix/SKILL.md` 步骤 6 末尾追加"### 步骤 6.X — 复现产物登记" 子节,含触发条件 3 条 + 9 步算法 → **✓ 通过**
- **AC-3**:3 类产物采集子节完整性(`executeStep` 3 类分发)→ **✓ 通过**
- **AC-4**:产物路径(`fix/<BUG-NNN>/reproduce/`) + `RESULT-meta.json` 8 字段(bugNum / startCommand / startedAt / endedAt / exitCode / reproduceResult / artifacts / failureReason)字面正确 → **✓ 通过**
- **AC-7**:`git diff` 校验 L1-3 + 步骤 6 主体 + 步骤 7~10 + "## 不要做的事" 字节级未变 → **✓ 通过**(frontmatter L1-4 字节级保留;步骤 6 主体 line 343-369 字面未变;"**关键:不重写**" 注释**只 1 次**(line 371,无重复);步骤 7-10 主体仅位置后移 140 行,字面未变;"## 不要做的事" 段位置后移,字面未变)
- **AC-8**:失败降级逻辑字面(`⚠` 屏显 + 11 边界 + 1 复合边界)→ **✓ 通过**
- **AC-11**:`Grep` 关键词("本需求 REQ-00040" / "沿用原 code-fix" / "Q-X 锁定")→ **0 命中**

### 6.2 端到端降级为静态

- 本任务**不**涉及可启动项目(纯 Markdown 技能库,沿用 NFR-7)
- 12 条 AC 端到端验证由 T-005 承担

## 7. 已知问题 / 未完成项

- **0 项**。本任务所有改动按 PLAN.md §3 + 详细设计 §4.2 完整落地
- 后续任务(T-003 / T-004)需在 T-005 端到端验证前完成

## 8. 过程文档清单(由 code-it 内化)

### 8.1 工作流上下文
```typescript
const decisions = {
  workLog: '生成',
  compileAndRun: '不生成',  // 纯 Markdown 改造,无运行时
  deviations: '生成',
  testResults: '不生成',     // 测试状态 = 不适用
  unitTestResults: '生成',  // 占位
  kanbanChangeLog: '生成',
  processDocDecisions: '生成',
};
```

### 8.2 决策结果表

| 过程文档 | 决策 | 理由 |
| --- | --- | --- |
| `work-log.md` | ✅ 生成 | 任务实施日志 |
| `compile-and-run.md` | ❌ 不生成 | 纯 Markdown 改造,无编译/运行 |
| `deviations.md` | ✅ 生成 | 评审要查;内容 = 0 偏离 |
| `test-results.md` | ❌ 不生成 | 测试状态 = 不适用 |
| `unit-test-results.md` | ✅ 生成(占位) | 任务类型 = 文档 → 写占位 |
| 看板"变更记录" | ✅ 生成 | 本轮有任务完成 + 状态推进 |
| `process-doc-decisions.md` | ✅ 生成(本文件) | 有不生成项 |

### 8.3 决策依据

- 引用 `code-it/SKILL.md` §"## 过程文档自适应判定"(line 101-138)与 §"## 步骤 8.7 — 过程文档自适应判定执行"(line 805+)的判定准则
- 沿用 `code-it` 内化 `applyProcessDocDecisions` 伪代码

### 8.4 关联任务

- **前置任务**:TASK-REQ-00040-00001(强依赖,已完成;提交 `ae42e39`)
- **后置任务**:TASK-REQ-00040-00003(弱依赖,模板可并行开发;T-003 完成**前** T-005 验证 AC-5 / AC-6 失败)
- **取代任务**:**无**
- **关联 code-check**:M1-REQ-00040 里程碑全部 6 任务完成后,由 `code-check` 评审

## 9. 单元测试

**0 项**。本任务不涉及函数级代码改动,无单测可写。详见 `unit-test-results.md`。

## 10. 逻辑行统计

**不适用**(NFR-8 锁定:`TASK-REQ-...` 5+5 位嵌套式触发 `calcLogicLoc`;本任务类型 = `修改` 但**实际产物**为纯 Markdown 文档,**不**涉及代码行统计)。

## 11. 变更记录

| 时间 | 变更类型 | 变更摘要 | 关联项 |
| --- | --- | --- | --- |
| 2026-06-25 14:36 | 任务完成 | TASK-REQ-00040-00002 完成(开发状态:已完成);code-fix/SKILL.md 步骤 6 末尾追加"### 步骤 6.X — 复现产物登记" 子节(含触发条件 3 条 + 9 步算法伪代码 + executeStep 3 类分发 + 11 边界 + 1 复合边界 + 屏显契约 2 段 + 性能 1 段,共约 +140 行);发现并修复"**关键:不重写**" 注释一度出现 2 次的复制 bug(校验 `grep -c` 返回 1,确认最终**只 1 次**);frontmatter L1-4 + 步骤 6 主体 + 步骤 7~10 + "## 不要做的事" 字节级保留;0 偏离;AC-2 / AC-3 / AC-4 / AC-7 / AC-8 / AC-11 静态校验通过 | TASK-REQ-00040-00002 |