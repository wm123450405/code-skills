# 改修总结 — TASK-REQ-00023-00004([修改] 后续操作建议生成(算法 4))

- 任务编码:TASK-REQ-00023-00004
- 标题:[修改] 后续操作建议生成(算法 4)
- 类型:修改
- 提交:本任务执行时生成
- 触发/来源:详细设计

## 1. 改修内容
- 涉及文件:`plugins/code-skills/skills/code-dashboard/SKILL.md`
  - `§工作流程 步骤 4 段 4` — 改写为"后续操作建议(本需求改造)"+ algorithm 4 引用 + 5 类状态映射表 + AC-4 展示规则
  - `§附录 C 建议数据结构` 末尾 — 补 1 段"5 类状态映射规则"

## 2. 关键变更
- 引入算法 4:`buildSuggestions(items, allTasks): Suggestion[]`(沿用 plan/REQ-00023/RESULT.md §4.4)
- 5 类状态映射(AC-4.5 严格按既有 10 个 `code-*` SKILL.md frontmatter 真实语法):
  - 待概要设计 → `/code-design <REQ>`(缺陷跳过)
  - 待详细设计 → `/code-plan <REQ>` / `/code-plan <BUG>`
  - 待代码开发 → `/code-it TASK-...-00001`(首个未完成)
  - 待单元测试 → `/code-unit TASK-...-00001`
  - 待代码评审 → `/code-check <REQ>`(缺陷走 code-check REQ 路径)
- 展示规则(AC-4.1 ~ AC-4.4):
  - 同状态需求 + 缺陷并存 → 1 条(需求路径优先)
  - 状态无触发 → 不展示
  - 全部 5 类无触发 → "无后续动作" 1 行
  - 严格 5 行上限

## 3. 边界与异常
- E-8:全部 5 类状态无触发 → 显示"无后续动作" 1 行
- E-9:推荐命令对应的目标文件不存在 → 不报错(只读契约),屏显 `?` 标记

## 4. 验证
- ✅ 不适用(纯文档/屏显类技能,无源码改动)
- 验证手段:人工在 REPL 中调 `/code-dashboard`,屏显段 4 应符合 §6.1 契约
