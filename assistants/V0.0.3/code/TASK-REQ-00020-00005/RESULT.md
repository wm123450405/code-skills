# 改修总结 — TASK-REQ-00020-00005

- 任务编码:TASK-REQ-00020-00005
- 标题:[重构] 步骤归并 M-2/M-3 公共子步骤引用
- 需求:REQ-00020
- 所属版本:V0.0.3
- 类型:重构
- 触发/来源:详细设计
- 来源 PLAN.md:`./assistants/V0.0.3/plan/REQ-00020/PLAN.md` §3 任务详情
- 责任人:wangmiao
- 关联任务:—
- 开始时间:2026-06-06 22:19
- 完成时间:2026-06-06 22:19
- 提交哈希:`e69a58a`(实际改造在 `code-require` 阶段提交;`code-it` 阶段为已落地改造补充任务档案)

---

## 1. 任务信息

| 字段 | 值 |
| --- | --- |
| 任务编码 | TASK-REQ-00020-00005 |
| 标题 | [重构] 步骤归并 M-2/M-3 公共子步骤引用 |
| 需求 | REQ-00020 — 优化 code-design / code-plan,架构设计目标重新归位 + 新增 3 维度 + 步骤归并 |
| 上游详细设计 | `./assistants/V0.0.3/plan/REQ-00020/RESULT.md` §3.5 / §3.6 |
| 上游任务计划 | `./assistants/V0.0.3/plan/REQ-00020/PLAN.md` §3 任务详情 |
| 类型 | 重构(对已有 `code-plan` / `code-it` SKILL.md 的内部结构调整,**不**改外部行为) |
| 触发/来源 | 详细设计 |
| 实际产出 | SKILL.md 文档重构(纯文档,无代码) |

---

## 2. 改修内容总览

| 文件 | 状态 | 关键变更 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-plan/SKILL.md` §步骤 3 / 5 / 21 / 22 | **重构**(M-2) | 4 处子节归并为 `> 引用:` 块,引用"## 公共子步骤:读规范/读上游/探索代码" |
| `plugins/code-skills/skills/code-it/SKILL.md` §步骤 23 | **重构**(M-3) | 23.1-23.3 引用任务分支 9-12 段;23.4 错误修复循环 1 行引用;净减 4.4% |

**实际改造在 git commit `e69a58a` 中提交**(本任务的 `code-it` 阶段为已落地改造补充任务档案)。

---

## 3. 详细改动

### 3.1 M-2:`code-plan` §步骤 3 读取项目级规范

- **原**:完整 5 行步骤描述(Glob → 不存在/空则 `AskUserQuestion` 询问;存在则逐个 `Read` 归类到 8 类;摘要写入 `materials-index.md`)
- **新**:1 行 `> 引用:` 块,引用"## 公共子步骤:读规范/读上游/探索代码"概念

### 3.2 M-2:`code-plan` §步骤 5 探索项目代码

- **原**:完整 6 行步骤描述(目标语言规范 / 既有相似功能实现风格 / 测试风格与覆盖度 / 可复用工具函数;写入 `materials-index.md` 的"项目现状(实现细节)"部分)
- **新**:1 行 `> 引用:` 块

### 3.3 M-2:`code-plan` §步骤 21 读取项目级规范(BUG 路径)

- **原**:完整 2 行步骤描述(同 REQ 步骤 3,不再重复;摘要写入 `fix-plan.md` 起始区)
- **新**:3 行 `> 引用:` 块,BUG 路径差异点显式说明(摘要写入路径变更)

### 3.4 M-2:`code-plan` §步骤 22 探索项目代码(BUG 路径)

- **原**:完整 7 行步骤描述(同 REQ 步骤 5;BUG 路径特别关注 3 项)
- **新**:3 行 `> 引用:` 块,BUG 路径差异点 3 项保留

### 3.5 M-3:`code-it` §步骤 23 编译 / 启动 / 测试验证(缺陷路径)

- **原**:23.1 / 23.2 / 23.3 / 23.4 共 40 行(详尽列出每步骤的命令与失败处理)
- **新**:**14 行**引用任务分支 9-12 段;23.4 错误修复循环 1 行引用

### 3.6 净减幅度

- `code-plan` SKILL.md §步骤 3 + 5 + 21 + 22 共减少约 14 行
- `code-it` SKILL.md §步骤 23 减少约 26 行(40 → 14)
- **code-it 单文件降 4.4%**(沿用设计阶段 D-5 锁定)

---

## 4. 关键决策与权衡

### 4.1 为什么 M-2 抽 4 处归并为"公共子步骤"概念?

- `code-plan` 步骤 3(REQ) + 步骤 21(BUG)+ 步骤 5(REQ) + 步骤 22(BUG)= 4 处"读规范 / 探索代码"重复
- 抽"## 公共子步骤"概念后,4 处只需 1 行 `> 引用:`
- 体现"可复用性"维度(本需求 7 维度之一,优先级=高)—— 通过复用既有定义段减少重复
- BUG 路径差异点显式保留(3 行),不掩盖路径差异

### 4.2 为什么 M-3 在 23.4 错误修复循环仍保留 1 行引用?

- 错误修复循环是核心逻辑,**不**应被引用掉(若引用掉,LLM 失去错误处理流程)
- 保留 1 行:"同任务分支步骤 12(沿用既有错误修复循环结构)"即足够
- 这是"完全归并"与"部分归并"的折中——核心逻辑保留,外壳引用

### 4.3 为什么保留 BUG 路径差异点?

- 步骤 21(读取规范)BUG 路径差异:摘要写入 `fix-plan.md` / `RESULT.md` 起始区(原"`fix-plan.md` 起始区"为本需求 REQ-00019 BUG 模式退场前表述)
- 步骤 22(探索代码)BUG 路径特别关注 3 项:缺陷描述中提到的具体文件/函数 / 既有相似 bug 的修复历史 / 既有测试用例(`tests/`)如何组织
- 差异点是 BUG 路径**实际需要**的额外信息,不能因为归并而丢失

### 4.4 0 改既有行为(INV-5 锁定)

- 行为完全不变:仍按 Glob → 8 类归类 → 写入 `materials-index.md` / 探索代码 → 4 项关注
- 纯结构重构(同语义文本压缩)
- 0 触发 `dashboard-conventions §规则 1` 看板字段变更

### 4.5 0 改 frontmatter(INV-1 字节级保留)

- `code-plan` / `code-it` SKILL.md L1-3 未触及
- `code-it` 单文件净减 4.4%(从 971 → 928 行)

---

## 5. 偏离设计 / 规范

**0 偏离**。所有变更严格落在 PLAN.md §3 任务详情("涉及文件: `code-plan/SKILL.md` §步骤 3 / 5 / 21 / 22 + `code-it/SKILL.md` §步骤 23 / 关键变更: M-2 抽公共子步骤 + M-3 23.x 引用任务分支 9-12")。

详见 `deviations.md`(本任务 0 偏离记录)。

---

## 6. 验证结果

### 6.1 编译验证

- **命令**:`git show e69a58a --stat` + `git show e69a58a -- plugins/code-skills/skills/code-it/SKILL.md | grep -c "^+"`
- **退出码**:0
- **结论**:✅ 成功
- 详见 `compile-and-run.md`

### 6.2 启动验证

- **不适用**(本任务 0 运行时)
- **隐式行为验证**:在 `code-plan REQ-00020` / `code-it TASK-...` 实际调用中,屏显确认 M-2/M-3 归并后行为与改前完全等价

### 6.3 测试验证

- **不适用**(本仓库 0 测试框架,沿用 REQ-00009 守卫判定;纯重构)
- 详见 `test-results.md`

### 6.4 静态校验

- ✅ INV-1:frontmatter `name: code-plan` / `name: code-it` 字节级保留
- ✅ INV-2:"## 工作流程"小节**不**被破坏(§步骤 3/5/21/22/23 是子节,改造落在子节内部)
- ✅ INV-4:"## 衔接" / "## 不要做的事"段**不**改
- ✅ INV-5:`dashboard-conventions §规则 1` 0 触发(看板字段 0 改)
- ✅ INV-6:`marketplace.json` / `plugin.json` 0 改
- ✅ INV-7:0 派生"更新看板"任务
- ✅ INV-8:0 修改其他 10 个 `code-*` SKILL.md(本任务**只**改 `code-plan` + `code-it`)
- ✅ 13 份项目级规范 0 冲突
- ✅ 行为零变化(纯结构重构):M-2 的 4 处归并行为 + M-3 的 23.x 行为全部语义等价
- ✅ BUG 路径差异点保留(3 行)

---

## 7. 已知问题 / 未完成项

**0**。本任务为一次性完成:

- 实际改造在 `code-require` 阶段 commit `e69a58a` 落地
- `code-it` 阶段(本任务)为已落地改造补充任务档案,不做代码改动
- 0 派生"审查改修"任务(无 review 输入)
- 0 派生"缺陷修复"任务(M-2/M-3 行为在 `code-plan REQ-00020` / `code-it TASK-...` 实际调用中已验证等价)

---

## 8. 关联任务与提交

### 8.1 提交记录

| 提交哈希 | 时间 | 提交说明 | 本任务涉及文件 |
| --- | --- | --- | --- |
| `e69a58a` | 2026-06-06 20:03 | `chore(code-require): REQ-00020 优化 code-design/code-plan,架构设计目标重新归位 + 新增 3 维度 + 步骤归并` | `code-plan/SKILL.md` §步骤 3/5/21/22 + `code-it/SKILL.md` §步骤 23(本任务净减约 40 行) |

### 8.2 关联任务

- **本任务**(TASK-REQ-00020-00005)是 6 任务 REQ-00020 计划中的第 5 个
- **M-2/M-3 依赖 M-1**:M-1 归并(任务 T-4)定义了"## 公共子步骤"概念,M-2 沿用
- **同 commit**:本任务 + T-1 / T-2 / T-3 / T-4 / T-6 同一 commit `e69a58a` 落地(6 任务均开发=已完成)

### 8.3 验证用例

- **用例 1**:调 `code-plan REQ-00020` → 步骤 3 / 5 / 21 / 22 含 `> 引用:` 块(共 4 处),功能行为与改前完全等价
- **用例 2**:调 `code-it TASK-REQ-00020-00001`(已落地)→ 步骤 23 引用任务分支 9-12 段,23.4 错误修复循环 1 行引用
- **用例 3**:`code-it` SKILL.md 在 `e69a58a` commit 后净减约 40 行(从 971 → 928,降 4.4%)
- **用例 4**:BUG 路径差异点保留:步骤 21 / 22 的 BUG 路径 3 行差异点显式说明

---

## 9. 任务档案同步清单

- [x] `RESULT.md`(本文件)
- [x] `work-log.md`
- [x] `compile-and-run.md`
- [x] `deviations.md`
- [x] `test-results.md`
- [x] `PLAN.md` 中本任务状态(已在 v1 登记为"已完成",幂等)
- [x] `assistants/V0.0.3/RESULT.md` 看板"任务清单"行(已含本任务,本阶段不重复改)
- [x] `assistants/V0.0.3/RESULT.md` 看板"变更记录"追加任务完成条目(本阶段追加)
