# 改修总结 — TASK-REQ-00039-00002

- 任务编码:TASK-REQ-00039-00002
- 任务标题:[修改] code-it 步骤 8 末尾追加 detectLocTool + calcLogicLoc 子步骤 + 屏显契约
- 任务类型:修改
- 触发/来源:详细设计
- 所属需求:REQ-00039
- 所属版本:V0.0.3
- 执行时间:2026-06-22 15:05 ~ 15:08
- 执行人:wangmiao
- 完成时间:2026-06-22 15:08

## 1. 改修内容总览

按 PLAN.md §3 T-2 + 详细设计 §4 模块 3 + §5.5 算法聚合,在 `code-it/SKILL.md` §"## 步骤 8 实施开发"末尾追加 `### 步骤 8.6 — 逻辑行统计(由 code-it 内化)` 子步骤。

| 变更点 | 目标位置 | 状态 |
| --- | --- | --- |
| C-it-1 | `plugins/code-skills/skills/code-it/SKILL.md` line 716-803(在 步骤 8.5 之后、步骤 9 之前) | ✅ |
| C-it-2 | 既有"## 步骤 8 实施开发" / "## 步骤 8a 项目可测性守卫" / "## 步骤 8.5 按需写单测" 字节级沿用 | ✅ |
| C-it-3 | 缺陷分支(`^TASK-BUG-...`)**不**触达约束(NFR-8) | ✅(步骤 8.6.3 E-5 边界) |

- 涉及文件:`plugins/code-skills/skills/code-it/SKILL.md`(1 修改)
- `git diff --stat`:**1 file changed, +89 insertions(+), 0 deletions(-)**(纯追加)

## 2. 详细改动

### 2.1 C-it-1:`code-it/SKILL.md` 步骤 8.6 子步骤(新增)

**位置**:`plugins/code-skills/skills/code-it/SKILL.md` line 716-803

**内容结构**(7 个子节):
- **步骤 8.6.0**(锚点):适用对象 + 依据 + 触发条件 + 共享库引用
- **步骤 8.6.1 目标**:在 步骤 8 末尾收集逻辑行 metadata,写入 `code/<task>/RESULT.md` 新增"## 逻辑行统计"小节
- **步骤 8.6.2 算法**:8 步伪代码(字节级沿用 `logic-loc.md` §"调用方约定" 中的 `calcLogicLoc` 聚合)
- **步骤 8.6.3 边界与异常**:7 类 E 边界(E-1 工具检测 / E-2 非 git 仓库 / E-3 文件访问 / E-4 文件过大 / E-5 缺陷分支 / E-6 工具调用失败 / E-7 旧文件不存在)
- **步骤 8.6.4 性能(NFR-1)**:< 3 秒 + 跳过策略(单文件 > 10MB 跳过)
- **步骤 8.6.5 屏显契约(NFR-7)**:成功 6 行 / 失败 1 行
- **步骤 8.6.6 退出码契约**:3 种场景(0 / 0 / ≠ 0)
- **步骤 8.6.7 约束**:10 条**不**约束(frontmatter / 既有章节 / AskUserQuestion / CLI / 三同步 / 重写共享库 / 默认值)

**核心字面**:
- 8 步算法伪代码与详细设计 §5.5 完全一致(字节级沿用)
- 屏显契约与 `logic-loc.md` §函数 1 屏显契约字面一致(沿用既有)
- 触发条件严格"任务分支 only,缺陷分支 not"(`^TASK-REQ-...$` 5+5 位嵌套式;NFR-8)

## 3. 关键决策与权衡

1. **步骤编号选 8.6 而非 8.5.1**:保持 8a / 8.5 / 8.6 平级子节结构(8a / 8.5 是新增既有子节;本任务是同级别新增 — 沿用 8.5 命名约定 = 步骤 X.Y 模式)
2. **仅引用共享库 4 函数,不重写**:步骤 8.6.2 算法伪代码**完全沿用** `logic-loc.md` §"调用方约定" 中的 `calcLogicLoc` 聚合(避免双向维护,共享库是 single source of truth)
3. **失败不阻断(NFR-7 强约束)**:E-1 ~ E-7 全部边界统一为"屏显 ⚠ + 跳过(不阻断 code-it)";`code-auto` 不中断(退出码 0)
4. **缺陷分支不触达(NFR-8 强约束)**:步骤 8.6.3 E-5 明确"**不**触达(NFR-8 强约束,本步骤不执行)";`^TASK-BUG-...` 5+5 位嵌套式守卫
5. **frontmatter 字节级保留**:L1-3(line 1-3)与修改前完全一致(`name: code-it` + `description: ...` 字节级沿用)
6. **既有章节字节级沿用**:"## 步骤 0a" / "## 步骤 8" / "## 步骤 8a" / "## 步骤 8.5" / "## 步骤 9" / "## 步骤 10" 等既有 30 个章节标题**完全不变**
7. **不触发 `dashboard-conventions §规则 1` 三同步**:metadata 写入 `code/<task>/RESULT.md` 而非看板(沿用既有约定)
8. **不引入开发痕迹**:新写 89 行**不**含"本需求 REQ-NNNNN 新增" / "原 code-it 状态机" / "Q-N 锁定" / "YYYY-MM-DD 起生效" / 退场文件名引用 等 6 类开发痕迹(沿用 `skill-conventions §规则 2`)

## 4. 偏离设计/规范的地方

详见 `deviations.md`:
- §偏离 0:无偏离(NFR-3 零规范变更 — 所有新增严格在 PLAN.md §3 T-2 任务详情边界内)

## 5. 验证结果

- 静态校验:**全部通过**
 - frontmatter L1-3 字节级保留(与修改前完全一致)
 - 步骤 8.6 正确插入(line 716,在 8.5 之后、9 之前)
 - 既有 步骤 8 / 8a / 8.5 字节级沿用(`grep` 验证章节标题不变)
 - 屏显契约字面与 `logic-loc.md` §函数 1 一致
- 字段完整性:`grep -c "步骤 8.6"` ≥ 1 ✓
- AC-7 部分静态校验通过(本任务**不**触发完整 AC-1 / AC-6 端到端验证,由 T-5 统一执行)
- 测试状态:**不适用**(本任务为纯 Markdown 技能定义改造,无生产代码改动,沿用 V0.0.3 修订)

## 6. 已知问题/未完成项

- **AC-1 端到端验证**:调 `code-it TASK-REQ-NNNNN-NNNNN` → `code/<TASK-...>/RESULT.md` 新增"## 逻辑行统计"小节 — 由 T-5 端到端验证执行
- **AC-6 端到端验证**:调 `code-it TASK-BUG-NNNNN-NNNNN` → 缺陷分支完全跳过 `calcLogicLoc` — 由 T-5 端到端验证执行
- **T-3 / T-4 待执行**:依赖 T-1 / T-2

## 7. 关联任务与提交

- 关联任务:T-1(已完成)+ T-3 / T-4 / T-5(待执行)
- 提交哈希:(本会话末尾由 `code-it` 兜底统一提交 / T-5 一次性累积提交)
- 累计 commit 策略:沿用 REQ-00037 模式 — T-1 ~ T-5 全部累积在工作区,留给 T-5 兜底一次性提交

## 8. 过程文档清单

- ✅ `work-log.md`(项目现状 + 规范要点 + 任务设计要点 + 5 条开发过程 + 9 项静态校验)
- ✅ `compile-and-run.md`(无 — 纯 Markdown 改造,无构建/运行)
- ✅ `deviations.md`(§偏离 0 无偏离)
- ✅ `test-results.md`(测试状态=不适用,静态校验全部通过)
- ❌ `unit-test-results.md`(**不**生成 — 项目不可测 + 任务类型=修改不涉及"函数级"代码改动,沿用 `code-it` 步骤 8.5 占位规则)
- ✅ `process-doc-decisions.md`(**不**生成 — 5 项判定全部"生成",无"不生成"决策)
