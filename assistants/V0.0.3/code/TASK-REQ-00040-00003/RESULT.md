# 改修总结 — TASK-REQ-00040-00003 · [修改] bug.md 模板新增"## 复现产物登记" 区段 + 文档头 2 字段

- 任务编码:TASK-REQ-00040-00003
- 任务标题:[修改] bug.md 模板新增"## 复现产物登记" 区段(3 子项:产物清单/实际行为/复现结论)+ 文档头 2 字段(复现方式/产物路径)
- 所属版本:V0.0.3
- 所属需求:REQ-00040
- 触发/来源:详细设计
- 任务类型:修改
- 完成时间:2026-06-25
- 上游:`./assistants/V0.0.3/plan/REQ-00040/PLAN.md §3 TASK-REQ-00040-00003` + `RESULT.md §4.4 模块 4`

## 1. 任务信息

- **任务编码**:`TASK-REQ-00040-00003`
- **标题**:bug.md 模板新增"## 复现产物登记" 区段(3 子项)+ 文档头 2 字段
- **类型**:修改(Markdown 模板扩展)
- **触发/来源**:详细设计
- **关联任务**:REQ-00040
- **里程碑**:M1-REQ-00040

## 2. 改修内容总览

- **修改的文件**:`plugins/code-skills/skills/code-fix/templates/bug.md`(1 个文件)
- **新增的行数**:约 29 行(文档头 +2 行 + 新区段 27 行)
- **删除的行数**:0
- **新增文件**:0
- **删除文件**:0
- **触发/来源 = 详细设计**(沿用 PLAN.md 任务总览"类型"列 = `修改`)

## 3. 详细改动

### 3.1 `plugins/code-skills/skills/code-fix/templates/bug.md`

#### 3.1.1 修改位置 1:文档头表(line 22 后,line 24 状态枚举前)

| 操作 | 字面 |
| --- | --- |
| 追加 | `\| 复现方式 \| \`程序复现\` / \`文本复现\` / \`未复现\` \|` |
| 追加 | `\| 产物路径 \| \`reproduce/\`(子目录相对路径;空表示无产物) \|` |

#### 3.1.2 修改位置 2:在"## 缺陷描述" 段后(line 60 `---` 分隔符前)插入新区段

- **新区段标题**:`## 复现产物登记(由 code-fix 步骤 6 末尾)`
- **新区段结构**:
 - 3 行说明引用(>`复现方式:程序复现 / 文本复现 / 未复现` / >`启动命令:<command>(若启动成功)` / >`运行时间:YYYY-MM-DD HH:mm ~ HH:mm`)
 - 1 表格 4 列(产物类型 / 文件路径(相对 RESULT.md) / 大小 / 用途),含 3 行示例(日志 / 截图 / 交互数据)
 - 1 段"### 实际行为"(`<复现过程中观察到的现象,含错误信息/堆栈/UI 异常>`)
 - 1 段"### 复现结论"(`已复现 / 未复现 / 复现失败(<原因>)`)

#### 3.1.3 字节级保留(INV-4)

- **文档头既有 11 行表格**:字节级保留(缺陷编号 / 标题 / 严重度 / 报告人 / 报告时间 / 当前状态 / 当前负责人 / 涉及模块 / 修复提交 / 关闭时间 / 关闭理由)
- **既有 9 区段**字节级保留(## 文档头 / ## 缺陷描述 / ## 根因分析 / ## 修复方案 / ## 修复实施 / ## 验证结果 / ## 修复日志 / ## 关联项 / ## 变更记录)

#### 3.1.4 关键决策(本任务无新决策,沿用 PLAN.md)

- 新区段插入位置:`## 缺陷描述` 段后 + `---` 分隔符前(沿用 D-6,符合"用户原始 → 证据附件 → 分析" 阅读顺序)
- 文档头新增 2 字段(复现方式 / 产物路径),便于字段筛选(沿用 D-7 + NFR-10 可追溯)
- 字段值用占位符 `<N> KB` / `<command>` 风格(沿用既有 `<bugNum>` / `<command>` / `<时间>` 占位符)

## 4. 关键决策与权衡

**0 项新决策**。本任务**完全**沿用 PLAN.md §3 TASK-REQ-00040-00003 + 详细设计 §4.4 + 概设 D-6/D-7 + NFR-10。

## 5. 偏离设计/规范的地方

**0 项偏离**(详见 `deviations.md`)。本任务所有改动与上游字面一致。

## 6. 验证结果

### 6.1 静态校验(12 条 AC 子集,本任务范围内)

- **AC-5**:`Read bug.md` 检查新区段结构(`## 复现产物登记` 含 3 子项:产物清单 / 实际行为 / 复现结论)→ **✓ 通过**
- **AC-6**:`Read bug.md` 文档头表新增 2 行(复现方式 / 产物路径)→ **✓ 通过**
- **AC-7**:既有 9 区段字节级未变 → **✓ 通过**(## 文档头 / ## 缺陷描述 / ## 根因分析 / ## 修复方案 / ## 修复实施 / ## 验证结果 / ## 修复日志 / ## 关联项 / ## 变更记录 全部字节级保留,仅位置后移)
- **AC-11**:`Grep` 关键词("本需求 REQ-00040" / "沿用原 code-fix" / "Q-X 锁定")→ **0 命中**(零开发痕迹)

### 6.2 端到端降级为静态

- 本任务**不**涉及可启动项目(纯 Markdown 技能库,沿用 NFR-7)
- 12 条 AC 端到端验证由 T-005 承担

## 7. 已知问题 / 未完成项

- **0 项**。本任务所有改动按 PLAN.md §3 + 详细设计 §4.4 完整落地
- 后续任务(T-004)需在 T-005 端到端验证前完成

## 8. 过程文档清单(由 code-it 内化)

### 8.1 工作流上下文
```typescript
const decisions = {
  workLog: '生成',
  compileAndRun: '不生成',  // 纯 Markdown 模板改动,无运行时
  deviations: '生成',
  testResults: '不生成',     // 测试状态 = 不适用
  unitTestResults: '生成',  // 占位
  kanbanChangeLog: '生成',
  processDocDecisions: '生成',
};
```

### 8.2 决策结果表

| 过程文档 | 决策 | 理由 |
| --- | --- | --- |
| `work-log.md` | ✅ 生成 | 任务实施日志 |
| `compile-and-run.md` | ❌ 不生成 | 纯 Markdown 模板改动,无编译/运行 |
| `deviations.md` | ✅ 生成 | 评审要查;内容 = 0 偏离 |
| `test-results.md` | ❌ 不生成 | 测试状态 = 不适用 |
| `unit-test-results.md` | ✅ 生成(占位) | 任务类型 = 文档 → 写占位 |
| 看板"变更记录" | ✅ 生成 | 本轮有任务完成 + 状态推进 |
| `process-doc-decisions.md` | ✅ 生成(本文件) | 有不生成项 |

### 8.3 决策依据

- 引用 `code-it/SKILL.md` §"## 过程文档自适应判定"(line 101-138)与 §"## 步骤 8.7"(line 805+)的判定准则

### 8.4 关联任务

- **前置任务**:TASK-REQ-00040-00001 / TASK-REQ-00040-00002(已完成,提交 `ae42e39` / `b9afdba`)
- **后置任务**:TASK-REQ-00040-00004(弱依赖,模板可并行开发)
- **取代任务**:**无**
- **关联 code-check**:M1-REQ-00040 里程碑全部 6 任务完成后,由 `code-check` 评审

## 9. 单元测试

**0 项**。本任务不涉及函数级代码改动,无单测可写。详见 `unit-test-results.md`。

## 10. 逻辑行统计

**不适用**(NFR-8 锁定:`TASK-REQ-...` 5+5 位嵌套式触发 `calcLogicLoc`;本任务类型 = `修改` 但**实际产物**为纯 Markdown 模板改动,**不**涉及代码行统计)。

## 11. 变更记录

| 时间 | 变更类型 | 变更摘要 | 关联项 |
| --- | --- | --- | --- |
| 2026-06-25 14:42 | 任务完成 | TASK-REQ-00040-00003 完成(开发状态:已完成);`bug.md` 文档头表 line 22 后新增 2 行(复现方式 / 产物路径)+ line 60 `---` 分隔符前插入"## 复现产物登记" 区段(3 子项:产物清单 / 实际行为 / 复现结论),共约 +29 行;既有 9 区段字节级保留;0 偏离;AC-5 / AC-6 / AC-7 / AC-11 静态校验通过 | TASK-REQ-00040-00003 |