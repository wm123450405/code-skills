# 改修总结 — TASK-REQ-00029-00001

- 任务编号:TASK-REQ-00029-00001
- 标题:[修改] code-dashboard 渲染层瘦身(总览 ≤ 8 / 需求 ≤ 15 行)
- 类型:修改
- 触发/来源:详细设计
- 关联任务:—
- 所属需求:REQ-00029
- 所属版本:V0.0.3
- 状态:已完成
- 完成时间:2026-06-10 12:00
- 责任人:wangmiao

## 1. 改修内容总览

按 `plan/REQ-00029/PLAN.md` §3 任务详情 9 项关键变更 + 8 个结构化锚点实施,完成 `code-dashboard/SKILL.md` 渲染层瘦身:

- **总览契约样例**(`§输出 段 1`):从 4 段 × 多行(标题+分隔线+数据行)= 12 行 → 改后 4 段 × 单行 = 8 行
- **退化**(`§输出 段 2`):多行说明 → 单行 `— / 无需求无缺陷,无需进度` + 段 2 = `状态: (无)`
- **总行数**(`§输出 段 3`):`≤ 12` → `≤ 8`
- **需求契约样例**(`§输出 段 4`):沿用 5 段结构,段内单行
- **段 1 总开发进度**(`§工作流程 步骤 4 段 1`):3 行(标题+分隔+ASCII) → 1 行(只 ASCII)
- **段 2 各状态占比**(`§工作流程 步骤 4 段 2`):5 行表格 + N 行说明 → 1 行摘要 + 1 行退化
- **段 3 高优缺陷**(`§工作流程 步骤 4 段 3`):标题+分隔+2 行数据 → 1 行(单行合并 P0/P1)
- **段 4 后续操作建议**(`§工作流程 步骤 4 段 4`):每条建议 3 行(建议/依据/优先级) → 每条建议 1 行(命令+优先级+依据)
- **段 5 需求模式**(`§工作流程 步骤 4 段 5`):5 段独立说明 → 5 段单行压缩

**diff 统计**:`plugins/code-skills/skills/code-dashboard/SKILL.md` 净 -10 行(55 insertions, 65 deletions)

## 2. 详细改动(每个文件)

### 2.1 `plugins/code-skills/skills/code-dashboard/SKILL.md`(1 文件,主改造对象)

**§输出 段 1(总览契约样例 + 退化 + 总行数)**:
- 屏显契约样例从 12 行 → 8 行
- 退化说明从 4 段(段 1-4 各 1 行)= 4 行 → 合并为 1 行
- 总行数从 `≤ 12` → `≤ 8`
- 新增需求模式契约样例(5 段单行压缩样例)

**§工作流程 步骤 4 段 1(总开发进度)**:
- 删除"总开发进度"标题行 + "──────" 分隔线
- 屏显格式从 3 行 → 1 行(只 ASCII)
- 标题加注 "本需求 REQ-00029 屏显瘦身"

**§工作流程 步骤 4 段 2(各状态数量占比)**:
- 删除"各状态数量占比"标题行 + "──────" 分隔线
- 删除每类的 1 行(标题 + 分隔 + 5 行表格)
- 屏显格式从 1 + 1 + N 行 → 1 行(单行摘要)
- 退化显示从 `(无)` → `状态: (无)`
- 标题加注 "本需求 REQ-00029 屏显瘦身"

**§工作流程 步骤 4 段 3(高优先级缺陷)**:
- 删除"高优先级缺陷"标题行 + "──────" 分隔线
- P0/P1 从 2 行 → 1 行(`P0 待修复: █ 2 | P1 待修复: ░ 0`)
- 标题加注 "本需求 REQ-00029 屏显瘦身"

**§工作流程 步骤 4 段 4(后续操作建议)**:
- 每条建议从 3 行(> 建议 / > 依据 / > 优先级)→ 1 行(`> <cmd> [<prio>] (依据: <reason>)`)
- 屏显总行数:5 条建议 × 3 行 = 15 行 → 5 条建议 × 1 行 = 5 行
- 标题加注 "本需求 REQ-00029 建议单行"

**§工作流程 步骤 4 段 5(需求模式)**:
- 拆为 5 个子要点(元信息 / 任务清单 / 关联缺陷 / 任务进度 / 建议)
- 每个子要点给出"屏显"(单行格式)
- 屏显总行数:5 段独立说明 → 5 段单行压缩
- 标题加注 "本需求 REQ-00029 屏显瘦身"

## 3. 关键决策与权衡

详见 `work-log.md` §"关键决策"段 5 条(K-1 ~ K-5):

- K-1:总览契约样例与退化合并为单段(减少段内子标题)
- K-2:段 2 保留 `buildBreakdown` 算法引用(算法层不动)
- K-3:段 4 建议格式 3 行 → 1 行(命令+优先级+依据)
- K-4:需求模式 5 段结构保留,只压段内
- K-5:段 5 命名风格对齐段 1-4

## 4. 偏离设计/规范

无偏离(`deviations.md` 详)。7 条 INV(INV-1 ~ INV-7)全部通过静态校验。

## 5. 验证结果

8 项 AC(AC-1 ~ AC-8)全部满足(`test-results.md` 详):

| AC | 结果 |
| --- | --- |
| AC-1 无副作用 | ✓(纯 SKILL.md 改造,git status 只改 1 文件) |
| AC-2 总览 ≤ 8 行 | ✓ |
| AC-3 5 类状态单行 | ✓ |
| AC-4 建议单行格式 | ✓ |
| AC-5 进度条解释行删除 | ✓ |
| AC-6 需求 ≤ 15 行 | ✓ |
| AC-7 元描述关键词 0 匹配 | ✓ |
| AC-8 5 类状态映射正确 | ✓ |

## 6. 已知问题/未完成项

无。`code-dashboard/SKILL.md` 渲染层改造完成,屏显契约样例与算法层完全对齐。

## 7. 关联任务与提交

- 提交哈希:待末尾兜底 commit 产出(本任务完成后由 `code-it` 步骤 N 兜底提交)
- 关联任务:无前置任务(独立完成)

## 8. 关键不变量守护记录

| INV | 校验方式 | 结果 |
| --- | --- | --- |
| INV-1 frontmatter 字节级保留 | `head -3` md5 = 30fd827f2519fade6c2f8c839ef66863 | ✓ |
| INV-2 算法 4 字节级保留 | `grep -c parseTaskId` = 3(算法 + 调用点) | ✓ |
| INV-3 算法 5 字节级保留 | `grep -c renderBar` = 2(算法 + 调用点) | ✓ |
| INV-4 看板字段 0 改 | 纯 SKILL.md 改造 | ✓ |
| INV-5 其他 10 技能 SKILL.md 0 改 | 0 触动 | ✓ |
| INV-6 marketplace.json / plugin.json / CLAUDE.md / rules/ 0 改 | 0 触动 | ✓ |
| INV-7 工具集不变 | 0 触动(仍只 Read/Glob/Grep) | ✓ |
