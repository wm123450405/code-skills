# 改修总结 — TASK-REQ-00031-00005

- 任务编码:TASK-REQ-00031-00005
- 任务标题:[修改] templates/plan.md:任务类型移除 测试 + 测试状态字段收窄为 2 枚举
- 任务类型:修改
- 触发/来源:详细设计
- 需求:REQ-00031
- 所属版本:V0.0.3
- 状态:已完成
- 完成时间:2026-06-12 15:50
- 完成人:wangmiao

## 1. 改修内容总览

| 文件 | 类型 | 状态 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-plan/templates/plan.md` | 修改 | +2 行(模板顶部 line 7 后追加 1 段"任务粒度约束") |

## 2. 详细改动

### 2.1 锚点 1:模板顶部(line 7 后)(FR-1 / FR-2 / FR-3 副作用)
- **位置**:line 6 `> **版本感知**` 段后(line 7 为空行,line 8 为 `---` 分隔线)
- **变更类型**:追加 1 段"任务粒度约束"说明
- **现文**:
  ```
  > **任务粒度约束**(本需求 REQ-00031 FR-1 / FR-2 / FR-3 强约束,2026-06-12 起生效):每个任务的"完成定义"**显式含**"编译成功"或"运行成功";**不**单列独立的"编译运行检测"任务(冗余 — `code-it` 步骤 9-12 已强制每任务迭代修复至编译/启动正常)。任务类型**不含** `测试`(5 选 1:新增 / 修改 / 重构 / 修复 / 文档);任务"测试状态"枚举**收窄为 2 个**(`已运行-通过` / `不适用`),新拆分 REQ 统一填 `不适用`。
  ```

## 3. 关键决策与权衡

- **D-1 模板顶部"任务粒度约束"声明**:不直接修改模板内的任务类型表(模板中**没有**显式 6 类任务类型枚举,任务类型由 `code-plan` 步骤 15A 沿用 SKILL.md 行为生成);改为在模板顶部追加 1 段"任务粒度约束"说明,作为模板使用者的**指引**。
- **D-2 既有章节(若有)字节级保留**:本任务只追加 1 段说明,**不**修改模板任何既有章节(沿用 INV-3)。

## 4. 偏离设计 / 规范的地方

无 — 变更严格对齐 `plan/REQ-00031/PLAN.md §T-005 关键变更 1-2`。

## 5. 验证结果

| INV / AC | 校验方法 | 结果 |
| --- | --- | --- |
| INV-3 既有章节(若有)字节级保留 | `git diff` 只显示 line 6-7 之间 +2 行,无其他区域变更 | ✅ |
| AC-6.1 "任务类型"列**移除** `测试` | `grep "任务类型.*不含.*测试"` ≥ 1 | ✅(模板顶部说明段) |
| AC-6.2 测试状态字段(若含)枚举**收窄为 2 个** | `grep "测试状态.*枚举.*收窄为 2 个"` ≥ 1 | ✅ |
| 任务粒度约束 | `grep "任务粒度约束"` ≥ 1 | ✅ |
| FR-1(完成定义含编译/运行) | `grep "完成定义.*显式含"` ≥ 1 | ✅ |
| FR-2(任务类型不含测试) | `grep "任务类型.*不含.*测试"` ≥ 1 | ✅ |
| FR-3(测试状态收窄为 2 个) | `grep "测试状态.*枚举.*收窄为 2 个"` ≥ 1 | ✅ |
| AC-7.3 INV-3 字节级保留 | 详 §5.1 | ✅ |

## 6. 已知问题 / 未完成项

无。

## 7. 关联任务与提交

- 关联需求:REQ-00031
- 提交哈希:(待末尾提交后填入)
- 计划 vs 实际:0 偏离
