# 改修总结 — TASK-REQ-00024-00001

## 1. 任务信息
- 任务编码:TASK-REQ-00024-00001
- 标题:[修改] code-auto 步骤 1:用路径感知替代 from 关键字
- 类型:修改
- 触发/来源:详细设计
- 所属需求:REQ-00024
- 来源:`plan/REQ-00024/PLAN.md §3.1`
- 设计依据:`plan/REQ-00024/RESULT.md §算法 1` + `design/REQ-00024/RESULT.md §2 关键设计决策`

## 2. 改修内容总览

| 类别 | 文件 | 改动类型 |
| --- | --- | --- |
| 修改 | `plugins/code_skills/skills/code-auto/SKILL.md` | 3 处:`§输入与输出` + `§工作流步骤 步骤 1` + `§边界与异常` + `§退出码` |
| 0 新增 | — | — |
| 0 删除 | — | — |

**净变化**:`+60 / -30 行`(3 处 Edit 净增 30 行)

## 3. 详细改动

### 3.1 `§输入与输出`(line 56-119)

- **改动**:
  - 删除:模式 A/B 二元表 + `from REQ-NNNNN` 关键字行 + `[追加材料...]` 行 + "两种调用模式"小节 + "模式识别" 3 步算法(正则)
  - 新增:单一参数表(默认视为需求编号)+ 4 模式路径感知表 + "调用形式" 段 + "路径感知判定算法" 段(4 步 Bash test 命令)+ "屏显契约" 3 行格式
- **效果**:`/code-auto` 输入从"2 模式(关键字解析)"改为"4 模式(路径检测)"

### 3.2 `§工作流步骤 步骤 1`(line 239-309)

- **改动**:
  - 删除:1A 模式 A(`code-require` 全流程) + 1B 模式 B(`code-require` 续跑)
  - 新增:1A `req-skip-require` 续跑 / 1B `req-run-require` 走全流程 / 1C `fix-skip-require` 走缺陷修复详设 / 1D `req-content` 视为需求内容
- **效果**:步骤 1 内部 4 模式分流,屏显 3 行"路径感知判定"前缀

### 3.3 `§边界与异常 + §退出码`(line 137-147 + line 656+)

- **改动**:
  - 删除:退出码 5(模式 B 校验失败)
  - 删除:E-15 / E-16 / E-17(原模式 B 相关 3 条边界)
  - 新增:E-18(无版本工作空间)+ E-19(路径类型异常)
- **效果**:退出码表精简(5→取消);边界表对齐 4 模式路径感知

## 4. 关键决策与权衡

| 决策 | 选择 | 理由 |
| --- | --- | --- |
| 移除 `from` 关键字解析 | 完全删除正则,改用 `Bash: test -d` | 用户原始描述第 1 段明确 |
| 4 模式命名 | `req-skip-require` / `req-run-require` / `fix-skip-require` / `req-content` | 4 模式语义清晰,沿用 2 词结构 |
| E-15 降级 | "模式 B 缺 RESULT.md" 非异常,自动走 code-require | 用户原始描述第 2 段明确"则进入需求设计阶段" |
| 屏显契约 | 3 行"路径感知判定"前缀(沿用既有 3 行风格) | 透明性 + 可追溯;屏显 0 副作用 |
| frontmatter 保留 | 字节级 0 变化 | INV-16 强约束 |
| 其他 9 个 SKILL.md | 0 变化 | NFR-6 严守 |

## 5. 偏离设计 / 规范的地方

**0 偏离**(详 `deviations.md`)

## 6. 验证结果(8 项 AC 全通过)

| AC | 描述 | 结论 |
| --- | --- | --- |
| AC-1 | 4 种输入场景屏显模式名与判定一致 | ✅ |
| AC-2 | `from` 关键字移除后行为(整串视为需求内容) | ✅ |
| AC-3 | 屏显 3 行前缀 | ✅ |
| AC-4 | 退出码 5 不再触发;3/4 沿用 | ✅ |
| AC-5 | frontmatter 字节级保留 + 9 个其他 SKILL.md 0 变化 | ✅ |
| AC-6 | 6 步状态机 + 6 任务循环 + 评审循环均不变 | ✅ |
| AC-7 | `auto-report.md` 模板字段 0 变化 | ✅ |
| AC-8 | 文档中"模式 A / 模式 B"字面引用清理 | ✅ |

## 7. 已知问题 / 未完成项

**0 未完成项**。本任务为 REQ-00024 修复的**唯一任务**。

## 8. 关联任务与提交

- 关联任务(自查):REQ-00024
- 关联计划:`plan/REQ-00024/PLAN.md`
- 完成时间:2026-06-07 17:55
- git 提交:**待末尾兜底 commit**

## 9. 同步文件清单

| 步骤 | 文件 | 改动 |
| --- | --- | --- |
| 步骤 22 | `plugins/code_skills/skills/code-auto/SKILL.md` | 3 处 Edit(§输入与输出 + §工作流步骤 步骤 1 + §边界与异常) |
| 步骤 23 | (静态校验通过,无文件改动) | 8 项 AC 全通过 |
| 步骤 25 | `code/TASK-REQ-00024-00001/{work-log, compile-and-run, test-results, deviations}.md` | 4 份过程文档新写 |
| 步骤 15 (待执行) | 看板任务清单 T-1 行 | 开发状态"待开始"→"已完成" |
| 步骤 15 (待执行) | 看板变更记录 | "任务完成"行追加 |

## 10. 下一步建议

1. **REQ-00024 修复完成** → 调 `code-check REQ-00024` 做正式评审
2. 评审通过后(8 项 AC 全过 + 0 必须改):RELEASE REQ-00024
3. (并行)BUG-00001 推进:`code-fix BUG-00001` → 推进"修复编码中"→"已修复-待验证"→"已关闭"
4. (后续)REQ-00025 设计实施:软化编号正则(本任务不涉及,但与本任务可叠加)
