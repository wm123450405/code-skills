# 偏离记录 — TASK-REQ-00036-00003
版本:V0.0.3

## 偏离 1:T-3 范围内补清 R-1(扩展字符集)

- **类别**:执行结果与基线偏差 + 实施方式扩展
- **依据**:
  - `plan/REQ-00036/RESULT.md §5 算法 1`:R-1 正则 `/\s*\(\s*本需求\s+(REQ|BUG)-\d{5}[^)]*\)\s*[,、)]\s*$/m`,要求括号包围
  - `skill-conventions §规则 2 §1`:段尾括号 / 句中 / 段首 / 标题括号 **任何位置**都应清
  - T-2 报告:R-1 = 87 命中(段尾括号场景)
  - 实际残留:50 个「本需求 REQ-NNNNN」字面是**行内无括号**(如「**本需求 REQ-00034 FR-2/FR-3 改造**」)
- **实际做法**:
  - 写 `apply-v2.py` 补丁脚本,扩展 R-1 字符集为「**行内任意位置**」(不再要求括号包围)
  - 截断逻辑:用 Python 字符串逻辑,跳过主体后 1 个空格,然后扫描后续字符,遇到 CJK 字符或段尾符号(`。;；:：、,)，)】］]→\`"'([{<>*#!`)停止
  - max_len = 30 字符(覆盖 FR-X/FR-Y/M-N/NFR-X/BUG 模式)
  - 执行结果:**55 命中**(50 个用户报告的 + 5 个 T-2 漏网的同行内嵌套),40 个文件被改,0 二次验证失败,0 残缺回退
- **偏离理由**:
  - T-2 的 R1_PATTERN `\(本需求\s+(?:REQ|BUG)-\d{5}[^)]*\)` 要求括号包围 + 段尾,所以漏掉了行内无括号字面
  - 这是 T-2 实施时的**字符集细化问题**,不是设计阶段 §5 算法 1 的错(§5 算法 1 字面说"段尾",但实际语义应覆盖所有位置)
  - 沿用 T-2 的 deviations §偏离 1(R-1 正则放宽)的逻辑,但**比 T-2 更激进**:从"括号内任意位置"扩展到"行内任意位置"
- **影响**:
  - **正面**:AC-1 从 50 命中降为 0 命中,达成 NFR-9(不影响当前已激活版本流程)
  - **正面**:清理更彻底,符合 `skill-conventions §规则 2 §1` 字面意图("任何位置")
  - **中性**:不破坏 frontmatter(脚本分离 frontmatter,只在 body 应用);占位符 100% 保留
  - **风险**:如果后续需求中需要保留"本需求 REQ-NNNNN"主体(例如作为示例展示),需要手动恢复
- **授权**:用户在 `code-it` 阶段已授权"实现细节可在 code-it 阶段细化"(沿用既有约定);本偏离是字符集细化,无需额外授权
- **时间**:2026-06-16 17:33
- **已知遗留**:
  - **中文动词语义字面**保留(如「改造」「退场」「新增」等):这是**有意接受**的近似清理 — 因为"改造"是技能正常描述的一部分,不是开发痕迹字面(只是前面有"本需求 REQ-NNNNN"标记)
  - **FR-X / M-N / NFR-X 等英文修饰语**被清:这是**理想**的清理(「本需求 REQ-NNNNN FR-2」→ 删主体到 `)`)
