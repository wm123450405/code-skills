# 偏离记录 — TASK-BUG-00001-00001
版本:V0.0.3
时间:2026-06-07 17:32

## 偏离:**0 偏离**

本任务严格按 `fix/BUG-00001/PLAN.md §3.1` 任务详情实施,**0 偏离**:
- 目标文件:`plugins/code-skills/skills/code-require/SKILL.md`(一致)
- 锚点:`## 不要做的事` 段,新条目插入段首(一致)
- 文案:不修改 `plugins/code-skills/skills/*/SKILL.md` 任何文件(工程代码改动由 `code-it` 实施,本技能只写 `require/<REQ>/RESULT.md` 等工作空间文档)(一致,80 字符)
- 边界与异常:0 触发(无段标题缺失 / 无 frontmatter 误改)
- 验证手段:INV-10(grep)+ INV-16(git diff)全通过
- 回退方式:未触发(无失败)

## 关键决策

### D-1:段首插入位置

- **决策**:在 line 524(标题)与 line 525(原第一个条目)之间插入
- **理由**:PLAN.md §3.1 明确"作为新条目插在该段**最前面**";新条目应享有最高优先级(在 `code-version` 检查之前)
- **影响**:新条目成为段首,后续阅读时最先看到;无功能影响(纯文档)

### D-2:文案措辞

- **决策**:不沿用现有"不要"句式("不要 X"),改用"不修改 X"陈述句
- **理由**:本条目是"硬约束"声明而非"提醒",陈述句更有约束力;且 4 个技能(T-1~T-4)的硬约束文案统一为"不修改 X",可维护性=高
- **影响**:0(纯文案风格差异,语义与既有"不要"一致)

## 残余风险
**0 残余风险**

- 0 触发 INV 失败
- 0 偏离设计
- 0 触发规范冲突
- 历史 4 个违规 commit(e69a58a / 6dee813 / 3e1573e / e568328)仍存在(**不**回滚,沿用 PLAN.md §3.1 + 总体方案)

## 备注
本任务为 BUG-00001 修复的第 1 步(T-1)。后续 T-2~T-5 由独立 `code-it` 任务实施,本任务的实施记录可作为"基线参照"。
