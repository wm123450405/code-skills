# 改修总结 — TASK-REQ-00020-00001

- 任务编码:TASK-REQ-00020-00001
- 标题:[修改] code-design 步骤 0b 简化为 1 维度
- 需求:REQ-00020
- 所属版本:V0.0.3
- 类型:修改
- 触发/来源:详细设计
- 来源 PLAN.md:`./assistants/V0.0.3/plan/REQ-00020/PLAN.md` §3 任务详情
- 责任人:wangmiao
- 关联任务:—
- 开始时间:2026-06-06 21:35
- 完成时间:2026-06-06 21:35
- 提交哈希:`e69a58a`(实际改造在 `code-require` 阶段提交;`code-it` 阶段为已落地改造补充任务档案)

---

## 1. 任务信息

| 字段 | 值 |
| --- | --- |
| 任务编码 | TASK-REQ-00020-00001 |
| 标题 | [修改] code-design 步骤 0b 简化为 1 维度 |
| 需求 | REQ-00020 — 优化 code-design / code-plan,架构设计目标重新归位 + 新增 3 维度 + 步骤归并 |
| 上游详细设计 | `./assistants/V0.0.3/plan/REQ-00020/RESULT.md` §3.1 |
| 上游任务计划 | `./assistants/V0.0.3/plan/REQ-00020/PLAN.md` §3 任务详情 |
| 类型 | 修改(对已有 `code-design/SKILL.md` §步骤 0b 的行为变更) |
| 触发/来源 | 详细设计 |
| 实际产出 | SKILL.md 文档改造(纯文档,无代码) |

---

## 2. 改修内容总览

| 文件 | 状态 | 关键变更 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-design/SKILL.md` §步骤 0b | **修改**(6 行净变化) | 自适应问题数从 1/3/5 → 1/2/2;屏显模板从 4 维度 → 1 维度;新增"职责分离"段说明 |

**实际改造在 git commit `e69a58a` 中提交**(本任务的 `code-it` 阶段为已落地改造补充任务档案)。

---

## 3. 详细改动(`code-design/SKILL.md` §步骤 0b)

### 3.1 标题追加简注

- **原**:`### 步骤 0b — 设计目标确认(本需求 REQ-00011 新增,FR-1)`
- **新**:`### 步骤 0b — 设计目标确认(本需求 REQ-00011 新增,FR-1,本需求 REQ-00020 简化为 1 维度)`

### 3.2 步骤 1 自适应问题数收敛

- **原**:小 1 问 / 中 3 问(整体 + 功能性 + 扩展性)/ 大 5 问(整体 + 功能性 + 扩展性 + 健壮性 + 可维护性)
- **新**:小 1 问 / 中 2 问(整体 + 功能性)/ 大 2 问(整体 + 功能性;**不**展开为 5 问 — 架构维度全部下沉到 `code-plan` 阶段,见 NFR-2.8)

### 3.3 步骤 3 屏显模板精简

- **原**:`功能性:<高/中/低>  扩展性:<高/中/低>  健壮性:<高/中/低>  可维护性:<高/中/低>`
- **新**:`功能性:<高/中/低>`

### 3.4 追加"职责分离"说明段(本需求 REQ-00020 锁定)

```
> **职责分离**(本需求 REQ-00020 锁定):`code-design` 是**概设**阶段,只关注"系统长什么样 + 功能性 1 维度";架构维度(扩展性 / 健壮性 / 可维护性 / 封装性 / 可复用性 / 可读性)统一下沉到 `code-plan` **详设**阶段,由 `code-plan` 步骤 0b 询问。依据:在概设阶段过早问"未来怎么扩"会让 AI 倾向过度设计(用户原文:"不在概设阶段考虑架构设计目标")。
```

---

## 4. 关键决策与权衡

### 4.1 为什么保留 2 问而非 1 问?

- 用户原文"不在概设阶段考虑架构设计目标" = 去掉扩展性 / 健壮性 / 可维护性 3 问
- 整体设计目标(功能性)是"决定要不要做"的核心维度,必须留
- 因此最终:1 / 2 / 2 问(小 / 中 / 大需求)
- 大需求仍只问 2 问(整体 + 功能性),**不**展开为 5 问(原行为)

### 4.2 为什么"职责分离"段用 `>` 引用块?

- 与步骤归并 M-1 / M-2 / M-3 / M-4 保持引用风格一致(详 `code-plan/SKILL.md` §3.4-3.7 + `code-it/SKILL.md` §23)
- 引用块在 Markdown 中**视觉独立**,读者一眼可识别"这是引用 / 说明 / 责任归属",与正常步骤描述区分

### 4.3 0 改 frontmatter(INV-1 字节级保留)

- `code-design` SKILL.md L1-3(`name: code-design` / `description: ...` / 空行)**未**改动
- git diff 验证:本任务 `e69a58a` 整 commit 9 文件变更中,`code-design/SKILL.md` 净变化 6 行(L143-L158 区域),**未触及** L1-3

### 4.4 0 改既有章节(INV-2 锁定)

- "## 工作流程"小节原有锚点全部保留
- "## 命令行参数解析" / "## 模板填充步骤" / "## 工具使用约定" 等既有小节**未**改动
- 改造范围严格限定在"### 步骤 0b"小节内部

---

## 5. 偏离设计 / 规范

**0 偏离**。所有变更严格落在 PLAN.md §3 任务详情("涉及文件: `code-design/SKILL.md` §步骤 0b / 关键变更: 5 问 → 1-2 问 / 屏显模板从 4 维度 → 1 维度 / 追加职责分离说明")。

详见 `deviations.md`(本任务 0 偏离记录)。

---

## 6. 验证结果

### 6.1 编译验证

- **命令**:`git show --stat e69a58a`(代码即文档,本仓库 0 编译工具链)
- **退出码**:0
- **结论**:✅ 成功
- 详见 `compile-and-run.md`

### 6.2 启动验证

- **不适用**(本任务 0 运行时;`code-design` 技能的运行时是用户在 Claude Code 中调用)

### 6.3 测试验证

- **不适用**(本仓库 0 测试框架,沿用 REQ-00009 守卫判定;纯文档改造)
- 详见 `test-results.md`

### 6.4 静态校验

- ✅ INV-1:frontmatter `name` 字段字节级保留(`name: code-design` 未变)
- ✅ INV-2:"## 工作流程"小节**不**被破坏(本任务改后保留原有锚点结构)
- ✅ INV-4:"## 衔接" / "## 不要做的事"段**不**改
- ✅ INV-6:`marketplace.json` / `plugin.json` 0 改
- ✅ INV-8:0 修改其他 10 个 `code-*` SKILL.md
- ✅ 13 份项目级规范 0 冲突(`rule-compliance.md` §6 INV 自检全部通过)
- ✅ NFR-2.7:屏显模板不出现"本任务"指代词
- ✅ NFR-7:frontmatter L1-3 字节级保留

---

## 7. 已知问题 / 未完成项

**0**。本任务为一次性完成:

- 实际改造在 `code-require` 阶段 commit `e69a58a` 落地
- `code-it` 阶段(本任务)为已落地改造补充任务档案,不做代码改动
- 0 派生"审查改修"任务(无 review 输入)
- 0 派生"缺陷修复"任务(`code-design` 步骤 0b 在 `code-design REQ-00020` 调用中已验证)

---

## 8. 关联任务与提交

### 8.1 提交记录

| 提交哈希 | 时间 | 提交说明 | 本任务涉及文件 |
| --- | --- | --- | --- |
| `e69a58a` | 2026-06-06 20:03 | `chore(code-require): REQ-00020 优化 code-design/code-plan,架构设计目标重新归位 + 新增 3 维度 + 步骤归并` | `plugins/code-skills/skills/code-design/SKILL.md`(本任务 6 行净变化) |

### 8.2 关联任务

- **本任务**(TASK-REQ-00020-00001)是 6 任务 REQ-00020 计划中的第 1 个
- **下游**:`code-plan` 步骤 0b 的 7 维度扩展(任务 TASK-REQ-00020-00002)依赖本任务的"职责分离"基础
- **同提交**:本任务 + T-2 ~ T-6 同一 commit `e69a58a` 落地(6 任务均开发=已完成)

### 8.3 验证用例

- **用例 1**:调 `code-design REQ-00020` → 步骤 0b 触发 1 个 `AskUserQuestion`(Q1 整体)+ 1 个 `AskUserQuestion`(Q2 功能性)= 共 2 问
- **用例 2**:屏显显示 `功能性:<高/中/低>`(1 维度,非原 4 维度)
- **用例 3**:`design/REQ-00020/RESULT.md` 顶部"## 设计目标"小节**只**写入功能性(其他 6 维度由 `code-plan` 阶段补全)

---

## 9. 任务档案同步清单

- [x] `RESULT.md`(本文件)
- [x] `work-log.md`
- [x] `compile-and-run.md`
- [x] `deviations.md`
- [x] `test-results.md`
- [x] `PLAN.md` 中本任务状态(已在 v1 登记为"已完成",幂等)
- [x] `assistants/V0.0.3/RESULT.md` 看板"任务清单"行(已含本任务,本阶段不重复改)
- [x] `assistants/V0.0.3/RESULT.md` 看板"变更记录"追加任务完成条目(本阶段追加)
