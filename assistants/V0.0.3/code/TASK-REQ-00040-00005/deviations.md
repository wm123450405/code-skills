# 偏离记录 — TASK-REQ-00040-00005

版本:V0.0.3
任务编码:TASK-REQ-00040-00005
触发/来源:详细设计

## 偏离列表

**0 项偏离**。

本任务的代码改动为 AC-4 静态校验发现的 1 处**字面补充**(在 T-002 步骤 6.X 子节内新增 1 行注释 `// 产物路径:fix/<BUG-NNN>/reproduce/(模板字面,与 FR-4 + assistants-layout.md 同步)`),**不**算作偏离 — 它是 AC-4 验收清单的一部分(沿用 PLAN.md §3 T-005 关键变更:"12 条 AC 全部降级为静态校验"),仅满足验收要求。

- **实际改动**:`code-fix/SKILL.md` 步骤 6.X 子节 line 384 后新增 1 行注释
- **字面影响**:**0**(注释为辅助说明,不影响 T-002 主体伪代码)
- **其他文件**:**0 改动**(T-005 自身**不**修改任何生产代码;**不**触发 T-001/T-002/T-003/T-004 任何重做)
- **frontmatter L1-4 字节级保留**
- 既有"## 工作流程" 步骤 1~10 主体 + 步骤 0.X / 步骤 6.X 新增子节(沿用 T-001/T-002) + "## 不要做的事" 段全部字节级保留

## 验证手段

- AC-1 ~ AC-12 全部静态校验通过(详见 `work-log.md` §"12 条 AC 静态校验报告")
- 12/12 通过(本仓库 0 测试框架,全部降级为静态校验)

## 授权

- 无(无偏离,无需授权)