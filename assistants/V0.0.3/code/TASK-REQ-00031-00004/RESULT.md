# 改修总结 — TASK-REQ-00031-00004

- 任务编码:TASK-REQ-00031-00004
- 任务标题:[修改] code-auto/SKILL.md:任务循环步骤 4.b 改为"恒等跳过"
- 任务类型:修改
- 触发/来源:详细设计
- 需求:REQ-00031
- 所属版本:V0.0.3
- 状态:已完成
- 完成时间:2026-06-12 15:48
- 完成人:wangmiao

## 1. 改修内容总览

| 文件 | 类型 | 状态 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-auto/SKILL.md` | 修改 | +8 / -2(line 385-394 内) |

## 2. 详细改动

### 2.1 锚点:`### 步骤 4:任务循环` 子节(FR-6)
- **位置**:line 382-390
- **变更类型**:步骤 4.b 子步改写 + 末尾追加 1 段"还原指引"注释(INV-10)
- **原文**(变更前):
  ```
  2. 对每个任务编码(按表行顺序):
     a. Skill: code-it <任务编码>
     b. 若 code-it 输出含 "测试需要=Y" → Skill: code-unit <任务编码>
         否则 → 跳过 code-unit
  ```
- **现文**(变更后):步骤 4.b **改写**为"恒等跳过"(5-7 行)+ 末尾**追加** 1 段"还原指引"注释(3-4 行,含 INV-10 字节级还原说明)

## 3. 关键决策与权衡

- **D-1 步骤 4.b 改为"恒等跳过"**:由"按需调用 code-unit"改为"永不触发",由 code-plan 任务"测试状态"统一 = `不适用` 共同保证。理由:`code-plan` 不再产出测试任务 → code-unit 永远无调用必要。
- **D-2 保留原"按需调用"逻辑为还原指引注释**:以 `# ...` 注释形式保留,作为字节级还原能力。理由:INV-10 字节级沿用 + 未来若 `code-plan` 重新启用测试规划,可**字节级**还原本步骤 4.b。
- **D-3 屏幕日志格式字节级保留**:`(跳过,无需测试)` 字节级保留(INV-10,本需求新增)。理由:为后续若 `code-plan` 重新启用测试规划时,可**字节级**还原。

## 4. 偏离设计 / 规范的地方

无 — 变更严格对齐 `plan/REQ-00031/PLAN.md §T-004 关键变更 1-2`。

## 5. 验证结果

| INV / AC | 校验方法 | 结果 |
| --- | --- | --- |
| INV-1 frontmatter L1-3 | `git diff` L1-3 字节级未变(变更在 line 385-394) | ✅ |
| INV-2 "## 不要做的事" | `grep -c "^## 不要做的事"` = 1 | ✅ |
| INV-3 既有"## 工作流程"步骤 0a-3 / 5-7 | `git diff` 无步骤 0a-3 / 5-7 区域变更 | ✅ |
| INV-10 屏幕日志格式字节级保留 | `grep -c "(跳过,无需测试)"` = 4(详 §5.1) | ✅ |
| AC-5.1 步骤 4.b 描述**改为**"永不再调" | `grep "永不再调\|永不触发"` ≥ 1 | ✅ |
| AC-5.3 屏幕日志格式**保留** | `grep "(跳过,无需测试)"` ≥ 1 | ✅ |
| AC-7.1~7.3 | 详 §5.1 | ✅ |

### 5.1 INV-10 屏幕日志格式字节级校验

```
$ grep -n "(跳过,无需测试)" plugins/code-skills/skills/code-auto/SKILL.md
... (line 387)  → 1/N:code-unit TASK-...-... ✓ (跳过,无需测试)
... (line 403)  → 1/N:code-unit TASK-...-... ✓ (跳过,无需测试)
... (line 409)  → 1/N:code-unit TASK-...-... ✓ (跳过,无需测试)
... (line 429)  → 1/2:code-it F-1 ✓ + code-unit F-1 ✓ (跳过,无需测试)
```

4 处命中,字节级保留 ✅。

## 6. 已知问题 / 未完成项

无。

## 7. 关联任务与提交

- 关联需求:REQ-00031
- 提交哈希:(待末尾提交后填入)
- 计划 vs 实际:0 偏离
