# 偏离记录 — TASK-REQ-00036-00001

## 偏离 1:目标文件数估算偏差

- **类别**:任务范围细化(PLAN.md 估算与实盘不符)
- **依据**:
  - PLAN.md §3 T-001 涉及文件:`plugins/code-skills/skills/*/{SKILL.md, templates/*.md}`(37 文件)
  - 实盘 glob:14 SKILL.md + 33 templates = 47 文件
- **实际做法**:在 T-1 实施过程中用 `ls` + `Glob` 实测发现偏差,即时修正 PLAN.md / 看板中"T-1 涉及文件"列 + "T-2 涉及文件"列 = 47
- **偏离理由**:PLAN.md 编写阶段(`code-plan` SKILL.md)漏数了 `code-it/templates/` / `code-init/templates/` / `code-publish/templates/` 等子目录的文件(估算时把 templates 整目录按 1 个算)
- **影响**:
  - T-1 实际产物文件数 = 47(而非 37),但 T-1 任务的扫描工作量仅增加约 27%(实际 < 1 秒级 grep)
  - T-2 任务的处理对象 = 47 文件(而非 37),工作量增加约 27%
  - 里程碑 M1-REQ-00036 的预期时间不变(2026-06-16 当日)
- **授权**:用户在 `code-plan` 阶段已授权"占位规则文件可由 code-it 阶段细化"(沿用既有约定);本偏离无需额外授权
- **时间**:2026-06-16 17:33
- **修正动作**:
  - `assistants/V0.0.3/plan/REQ-00036/PLAN.md` L26 / L27(任务总览表)→ 37 → 47
  - `assistants/V0.0.3/RESULT.md` L258 / L259(看板任务清单)→ 37 → 47
  - PLAN.md §1 计划概述和 §2.1 触发/来源 不需改(只动了"涉及文件"列)

## 偏离 2:占位符命中确认(NFR-6 强约束)

- **类别**:规范遵循验证(NFR-6 占位符保留)
- **依据**:
  - NFR-6 占位符保留:`REQ-00001` / `BUG-00001` / `TASK-REQ-00001-00001` 必须保留
  - 实盘扫描:`REQ-00001` 字面在 templates/ 中出现 32 次,11 文件
- **实际做法**:在 T-1 扫描阶段,除 6 条清理规则外,额外 grep 占位符作为"白名单存在性确认",确保 T-2 阶段这些占位符不被误清
- **偏离理由**:PLAN.md §5 算法 1-6 的正则未显式包含"占位符例外",需 T-2 阶段在正则替换时手动加负向断言或白名单
- **影响**:T-2 阶段在应用 R-1 ~ R-6 时,需要保证占位符 `REQ-00001` 仍存在(32 处)
- **授权**:NFR-6 已强制要求保留;无需额外授权
- **时间**:2026-06-16 17:33
- **修正动作**:本偏离已记入本任务的"关键决策与权衡"和 RESULT.md §C,T-2 阶段实施时需主动核对