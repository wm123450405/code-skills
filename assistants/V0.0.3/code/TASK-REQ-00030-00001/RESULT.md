# 改修总结 — TASK-REQ-00030-00001

- 任务编码:TASK-REQ-00030-00001
- 任务标题:[修改] code-design/SKILL.md:步骤 0b 收敛 + 9A/10A/11A 输出深度收窄
- 所属需求:REQ-00030
- 所属版本:V0.0.3
- 改修日期:2026-06-12
- 改修者:用户
- 状态:已完成

## 1. 改修内容总览

修改 1 个文件:
- `plugins/code-skills/skills/code-design/SKILL.md`:
  - §步骤 0b "## 设计目标确认"小节:追加 FR-1 扩展性触发判定 + FR-2 自适应问路收紧
  - §步骤 0b "职责分离"声明:追加 FR-1 / FR-2 引用
  - §步骤 9A "模块拆分":模块表字段从 8 列收窄至 5 列
  - §步骤 10A "接口与数据结构":接口从 5 项 / 接口 收窄至 4 项;数据结构从 4 项 / 实体 收窄至 4 项
  - §步骤 11A "三方依赖评估":每行依赖从 8 项 收窄至 4 项

未修改 0 个文件:
- (无)

未新增任何文件。

## 2. 详细改动(按结构化语义定位)

### 2.1 §步骤 0b 修订

- **改前**:1 段(7 子项:3 规模评估 + 1 写设计目标 + 1 屏显 + 1 取消 + 2 不变量)
- **改后**:1 段(8 子项:1 扩展性触发判定 + 4 自适应问路细化 + 1 写设计目标 + 1 屏显 + 1 取消 + 2 不变量)

### 2.2 §步骤 0b "职责分离"声明修订

- **改前**:1 段说明(沿用 REQ-00020 既有)
- **改后**:1 段说明(沿用 REQ-00020 既有 + 追加 REQ-00030 FR-1 / FR-2 引用)

### 2.3 §步骤 9A 模块拆分修订

- **改前**:模块表 8 字段(模块名 / 路径 / 状态 / 职责 / 对外接口 / 依赖 / 关键决策 / 规范条款)
- **改后**:模块表 5 字段(模块名 / 路径 / 状态 / 职责 / 依赖);**不**展开"对外接口 / 关键决策 / 规范条款"3 字段,下沉到 `code-plan`

### 2.4 §步骤 10A 接口与数据结构修订

- **改前**:接口 5 项(端点签名 / 标注 / 数据格式与版本策略 / 错误码与异常约定 / 自检);数据结构 4 项(字段名类型约束 / 关系 / 自检)
- **改后**:接口 4 项(接口名 / 形式 / 状态 / 一句话职责);数据结构 4 项(实体名 / 状态 / 关键字段 ≤ 5 个 / 一句话关系)

### 2.5 §步骤 11A 三方依赖评估修订

- **改前**:依赖 8 项(名称版本来源 / 用途必要性 / 许可 / 活跃度 / 体积性能 / 安全态势 / 替代 / 规范对照)
- **改后**:依赖 4 项(依赖名 / 一句话用途 / 必要性 / 替代评估)

## 3. 关键决策与权衡

| 决策 | 内容 | 权衡 |
| --- | --- | --- |
| 0b 步骤 1 拆为"扩展性判定" + "自适应问路"两段 | 二者语义独立 | 避免一个段落承担 2 个职责,便于未来独立调整 |
| 9A 移除"对外接口"字段 | 下沉到 `code-plan` §接口细节 | 概设阶段不展开接口契约,避免越界做详设 |
| 10A 接口"一句话职责"替代"完整端点签名" | 概设不写 API 端点 | 同上 |
| 10A 数据结构"关键字段 ≤ 5 个" | 防止 AI 写完整字段表 | 同上;5 个上限避免越界 |
| 11A "许可 / 活跃度 / 体积 / 安全"全部下沉 | 概设阶段只做"必要性 + 替代"判断 | 这 4 项评估通常需要 N 天调研,不适合概设阶段 |

## 4. 偏离设计/规范的地方

**无偏离**:
- INV-1 frontmatter L1-3 字节级保留(本任务**不**改 L1-3)
- INV-2 "## 不要做的事" 字节级保留(本任务**不**改此小节)
- INV-3 既有"## 工作流程"步骤 0a / 0b.0 / 0 / 1-5 / 6 / 7A / 7B / 8A / 12A / 13A / 14A / 15A 字节级保留(本任务**只**改 0b / 9A / 10A / 11A 4 个子节)
- INV-4 其他 11 个 `code-*` 技能 SKILL.md 0 改
- INV-5 `./assistants/rules/*.md` 0 改
- INV-6 `marketplace.json` / `plugin.json` / `CLAUDE.md` / `README*.md` 0 改

## 5. 验证结果

### 5.1 编译验证

- **不适用**(本仓库**不**包含源代码 / 构建系统 / Lint 工具)
- 验证手段:人工 Read 检查文件结构 / Markdown 语法
- 结果:通过(本任务**只**修改 Markdown 文件,无编译步骤)

### 5.2 启动验证

- **不适用**(本任务不涉及运行时)

### 5.3 测试验证

- **不适用**(本仓库**不**包含测试框架)
- 验证手段:人工对照上游 `plan/REQ-00030/PLAN.md` §TASK-REQ-00030-00001 任务详情 + `plan/REQ-00030/RESULT.md` §3.3 + §3.9 INV-1~INV-3

## 6. 已知问题/未完成项

**无**。

## 7. 关联任务与提交

- 关联任务:T-001(本任务)
- 提交哈希:<待 git commit 后回填>

## 8. 变更记录(本任务对 code-design/SKILL.md 的变更摘要)

| 锚点 | 变更类型 | 变更摘要 |
| --- | --- | --- |
| §步骤 0b 步骤 1 | **追加** | 扩展性触发判定逻辑(FR-1 锁定) |
| §步骤 0b 步骤 2 | **追加** | 自适应问路收紧(FR-2 锁定;小需求 0 问) |
| §步骤 0b "职责分离" 声明 | **追加** | FR-1 / FR-2 引用一行 |
| §步骤 9A 模块表 | **收窄** | 8 字段 → 5 字段(移除"对外接口 / 关键决策 / 规范条款") |
| §步骤 10A 接口 | **收窄** | 5 项 / 接口 → 4 项 / 接口(移除"数据格式 / 错误码") |
| §步骤 10A 数据结构 | **收窄** | 4 项 / 实体 → 4 项 / 实体(关键字段列表化) |
| §步骤 11A 三方依赖 | **收窄** | 8 项 / 依赖 → 4 项 / 依赖(移除"版本 / 来源 / 许可 / 活跃度 / 体积 / 安全态势") |

## 9. 与 plan/REQ-00030/PLAN.md §T-001 的对照

| plan/PLAN.md §T-001 任务详情条目 | 本任务实施情况 |
| --- | --- |
| §步骤 0b 在"自适应问题数"小节**追加**本需求 FR-1 / FR-2 修订逻辑 | ✅ 已追加(原步骤 1 + 2 拆为 2 段) |
| §步骤 0b "职责分离"声明**追加**本需求"小需求 = 0 问默认"一行 | ✅ 已追加 |
| §步骤 9A 模块表**只**列 5 列 | ✅ 已收窄(8 → 5) |
| §步骤 10A 接口概要**只**列 4 项 | ✅ 已收窄(5 → 4) |
| §步骤 10A 数据结构**只**列 4 项 | ✅ 已收窄(4 → 4,字段列表化) |
| §步骤 11A 三方依赖每行**只**列 4 项 | ✅ 已收窄(8 → 4) |
| §## 不要做的事:**不**修改 | ✅ 未修改 |
| frontmatter L1-3:**不**修改 | ✅ 未修改 |
