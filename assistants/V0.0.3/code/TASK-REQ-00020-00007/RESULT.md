# 改修总结 — TASK-REQ-00020-00007

- 任务编码:TASK-REQ-00020-00007
- 标题:[修改] 修复任务粒度调整规则表 4 列与 3 列数据错位
- 需求:REQ-00020
- 所属版本:V0.0.3
- 类型:修改
- 触发/来源:**审查改修**(由 `code-check` 派生)
- 责任单位:code-it
- 关联原任务:TASK-REQ-00020-00003(任务粒度调整规则 +3 行)
- 开始时间:2026-06-10 11:00
- 完成时间:2026-06-10 11:00
- 提交哈希:待末尾兜底提交后回填

---

## 1. 任务信息

| 字段 | 值 |
| --- | --- |
| 任务编码 | TASK-REQ-00020-00007 |
| 标题 | [修改] 修复任务粒度调整规则表 4 列与 3 列数据错位 |
| 需求 | REQ-00020 |
| 类型 | 修改(对已有 `code-plan/SKILL.md` §步骤 10A 末尾表格的行为变更) |
| 触发/来源 | 审查改修(沿用 `code-check` §派生任务流程) |
| 严重程度 | **P1(必须改)** |
| 实际产出 | SKILL.md 文档微调(纯文档,无代码) |

---

## 2. 改修内容总览

| 文件 | 状态 | 关键变更 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-plan/SKILL.md` §步骤 10A 末尾 表格 L450-451 | **修改** | 表格前 2 行(`--minimal` / `--balanced`)补齐 4 列对齐(空列填 `—`);净变化 = 2 行各 1 个 `— ` 字符串插入 |

**未修改任何其他文件**(本任务的零回归承诺)。

---

## 3. 详细改动(`code-plan/SKILL.md` §步骤 10A 末尾 表格 L448-451)

### 3.1 改前(3 列错位)

```
| 整体设计目标 | 维度=高 | 至少任务数 | 任务类型 |
| --- | --- | --- | --- |
| `--minimal` | * | 合并同类任务,粒度粗化(轻度合并启发式:P-D3 锁定 — 只合并明显同类的,如"修改文件 A" + "修改文件 A 同字段" → 合并) |
| `--balanced` | 高/中/低 | 默认粒度(无调整) |
```

- L450 缺"至少任务数"列
- L451 缺"任务类型"列

### 3.2 改后(4 列对齐)

```
| 整体设计目标 | 维度=高 | 至少任务数 | 任务类型 |
| --- | --- | --- | --- |
| `--minimal` | * | — | 合并同类任务,粒度粗化(轻度合并启发式:P-D3 锁定 — 只合并明显同类的,如"修改文件 A" + "修改文件 A 同字段" → 合并) |
| `--balanced` | 高/中/低 | — | 默认粒度(无调整) |
```

- L450 补"至少任务数"列 = `—`(无强制任务数)
- L451 补"任务类型"列 = `—`(无默认任务类型,沿用原语义)

### 3.3 净变化

- L450:`合并同类任务...` 前插入 `— | `(6 字符)
- L451:`默认粒度(无调整)` 前插入 `— | `(4 字符)
- 合计:**10 字符净插入**(精确到 2 行,**不**触及任何其他行)

---

## 4. 关键决策与权衡

### 4.1 决策 1:空列填 `—` 而非 `*`

- 沿用 review/RESULT.md §2 P1-1 改修建议("空列填 — 或显式标注")
- 与后 5 行中第 5 行(L456)"至少 3 个扩展性相关任务(沿用) | —"的"任务类型"列空值表示风格一致
- `—`(em-dash)在本表语境中= "无" / "不适用",不产生歧义

### 4.2 决策 2:**不**做 P1-2 衍生修复(可读性列名超长)

- review §2 P1-2 标注"可选改"(P3)
- review §6 §"不需要做的"明确列出"不**改后 5 行(`--extensible` 5 个变体)内容"
- L455 的"可读性=高 + 编程语言非自然语言"列名虽然超长,但与表头"维度=高"列风格一致(其他维度列名也长,例如"扩展性=高"等)
- P3 级问题不阻塞 P1 修复;留给后续 `code-fix` 周期批量处理
- **遵循 review §6 越界保护**:不擅自扩大修复范围

---

## 5. 偏离 review/设计/规范的地方

**0 偏离**。本任务**完全遵循** review/TASK-REQ-00020-00007/RESULT.md 的"应当改修的文件清单":

- ✅ 仅改 L450-451(前 2 行,空列补齐)
- ✅ 不改表头 4 列结构(已正确)
- ✅ 不改后 5 行(L452-L456)内容
- ✅ 不改"不强制加任务的维度"列表(L458)
- ✅ 不改"位置"段(L459)
- ✅ 不改 frontmatter(L1-3 字节级保留)
- ✅ 不改其他 10 个 `code-*` SKILL.md
- ✅ 锚点 B 字节级保留:§步骤 10A 内"任务类型 / 任务编号 / 双状态 / 触发/来源" 4 个子段**未**触及
- ✅ INV-1 / INV-2 / INV-3 / NFR-7 全部严守

详见 `deviations.md`。

---

## 6. 验证结果

### 6.1 编译 / 启动 / 测试

- **编译**:不适用(本任务 0 运行时代码,纯 Markdown 文档微调)
- **启动**:不适用
- **测试**:不适用(本仓库 0 测试框架,沿用 REQ-00009 守卫判定;纯文档改造)

### 6.2 静态校验(本任务核心)

- ✅ **表格 7 行全部 4 列对齐**:L450 / L451 已补齐
- ✅ **INV-1**:frontmatter L1-3 字节级保留(0 触及)
- ✅ **INV-2**:"## 工作流程"小节不被破坏(改造落在 §步骤 10A 末尾子节内部)
- ✅ **锚点 B 字节级保留**:§步骤 10A 内 4 子段(任务类型/任务编号/双状态/触发/来源)未触及
- ✅ **NFR-7**:frontmatter L1-3 字节级保留
- ✅ **Markdown 渲染对齐**:7 行表格在 GitHub / VS Code / Typora 渲染时不再错位

### 6.3 工具调用校验

- ✅ 1 次 Read(读 `code-plan/SKILL.md` L446-460,确认当前状态)
- ✅ 1 次 Edit(改 L450-451)
- ✅ 0 次 Write(本任务不写整文件,只 Edit)
- ✅ 0 次 Bash(本任务无编译/启动/测试)

### 6.4 关联原任务回归

- ✅ 关联原任务 TASK-REQ-00020-00003 的"完成判定"(`code-plan/SKILL.md` L443-451 表格 7 行全部 4 列对齐,Markdown 渲染无错位) → 本任务完成 = 原任务的"完成判定"达成

---

## 7. 已知问题 / 未完成项

- **P1-2 衍生(可选)**:可读性列名超长,review §2 标 P3 → 留作后续 `code-fix` 周期批量处理(非本任务阻塞)
- **P2-1 ~ P2-4**(3 建议改) + **P3-1 ~ P3-10**(10 可选) → 沿用 review §6 §"P2(建议改,留 follow-up)" + "P3(可选,留 follow-up)"指引,下次 `code-fix` 周期批量处理

---

## 8. 关联任务与提交

### 8.1 提交记录

| 提交哈希 | 时间 | 提交说明 | 本任务涉及文件 |
| --- | --- | --- | --- |
| `e69a58a` | 2026-06-06 20:03 | `chore(code-require): REQ-00020 优化 code-design/code-plan ...` | `plugins/code-skills/skills/code-plan/SKILL.md` §步骤 10A 末尾(原 T-3 占约 6 行净变化) |
| (本任务 commit) | 2026-06-10 11:00 | `chore(code-it): TASK-REQ-00020-00007 修复任务粒度调整规则表 4 列与 3 列数据错位` | `plugins/code-skills/skills/code-plan/SKILL.md` §步骤 10A 末尾 表格 L450-451(本任务占约 10 字符净插入) |

### 8.2 关联任务

- **关联原任务**:TASK-REQ-00020-00003(任务粒度调整规则 +3 行)
- **同 commit 任务**(本任务单独 commit,无同提交):
  - 本任务单独一个 commit,便于后续 revert / cherry-pick

### 8.3 验证用例(供下游消费方)

- **用例 1**:Markdown 渲染器(VS Code / Typora / GitHub)打开 `code-plan/SKILL.md` §步骤 10A 末尾,7 行表格 4 列对齐无错位
- **用例 2**:LLM 读表时,`--minimal` 行正确识别"维度=高=*"(通配) + "至少任务数=—"(无强制) + "任务类型=合并同类任务..."(完整语义)
- **用例 3**:`code-plan REQ-NNNNN --extensible` 步骤 0b 回答 7 维度后,§步骤 10A 末尾表格完整覆盖 `--extensible` 5 个变体,前 2 行 (`--minimal` / `--balanced`) 不再误读为"维度=高"

---

## 9. 任务档案同步清单

- [x] `RESULT.md`(本文件)
- [x] `work-log.md`
- [x] `compile-and-run.md`
- [x] `deviations.md`
- [x] `test-results.md`
- [x] `PLAN.md` 中本任务状态(待开始 → 进行中 → 已完成)
- [x] `assistants/V0.0.3/RESULT.md` 看板"任务清单"行(开发状态推进 + 完成时间填入)
- [x] `assistants/V0.0.3/RESULT.md` 看板"评审发现汇总" F-001 / F-002 状态推进(待处理 → 已处理)
- [x] `assistants/V0.0.3/RESULT.md` 看板"派生任务记录" 本任务行状态推进(待开始 → 已完成)
- [x] `assistants/V0.0.3/RESULT.md` 看板"变更记录"追加 3 条(任务完成 + 评审发现已处理 + 派生任务完成)

---

## 10. 下一步建议

- 用户可在编辑器中打开 `code-plan/SKILL.md` §步骤 10A 末尾,目视确认表格 7 行 4 列对齐
- 同步执行 TASK-REQ-00020-00008(派生任务 2:清理 §步骤 7D 段,同样 P1 必改)
- 下次 `code-fix` 周期处理 3 P2 + 10 P3(沿用 review §6 §"P2(建议改,留 follow-up)" 指引)
