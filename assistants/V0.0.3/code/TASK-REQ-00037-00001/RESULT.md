# 改修总结 — TASK-REQ-00037-00001

- 任务编码:TASK-REQ-00037-00001
- 任务标题:[修改] code-fix 步骤 4/6 + 衔接 + 不要做的事同步
- 任务类型:修改
- 触发/来源:详细设计
- 所属需求:REQ-00037(优化 /code-fix 技能及整个缺陷修复流程的状态推进)
- 所属版本:V0.0.3
- 执行时间:2026-06-22 09:30 ~ 09:35
- 执行人:wangmiao
- 完成时间:2026-06-22 09:35

## 1. 改修内容总览

按 PLAN.md §3 + 详细设计 §6.1 的 6 处关键变更(C-fix-1 ~ C-fix-6),把 `code-fix` 状态推进收敛到"前 5 段 + 入口 `待处理`",不再参与"待开发及之后"。

| 变更点 | 目标位置 | 状态 |
| --- | --- | --- |
| C-fix-1 | 步骤 4 状态推进表(line 247)+ 步骤 4 描述段(line 241) | ✅ |
| C-fix-2 | 步骤 4 状态推进表新增 1 行(待处理 FROM)+ 修改"(新建)"行 | ✅ |
| C-fix-3 | 步骤 5 注脚(line 269-274) | ✅ |
| C-fix-4 | 步骤 9 引导下一步表(line 347-354)+ 步骤 9 典型修复流程段 | ✅ |
| C-fix-5 | ## 与其他技能的关系 段"典型完整流程"(line 404-416)10 步 → 4 步 | ✅ |
| C-fix-6 | ## 不要做的事 段(line 434)字面追加 | ✅ |

- 涉及文件:`plugins/code-skills/skills/code-fix/SKILL.md`(唯一)
- 新增文件:0
- 删除文件:0
- 修改文件:1
- `git diff --stat`:**1 file changed, 25 insertions(+), 30 deletions(-)**

## 2. 详细改动

### 2.1 C-fix-1 + C-fix-2:步骤 4 状态推进表扩展

**原字面**(line 241):
```
> 本技能是**纯登记型**:... 本技能**只**主动推进"报告 / 调查中" 2 段;"修复规划中"由用户调 `code-plan <BUG-NNN>` 推进...;"修复编码中"及之后由 `code-it` / `code-check` 推进。
```

**新字面**:
```
> 本技能是**纯登记型**:... 新建 BUG 的初始状态为 `待处理`;本技能**只**主动推进"待处理 / 报告 / 调查中" 3 段;"待开发 / 开发中 / 待审查 / 已完成"等状态由 `code-plan` / `code-it` / `code-check` 自动推进,本技能**不**参与。
```

**状态推进表新增 1 行**:`| 待处理 | 报告 / 调查中 / 已关闭-非缺陷 / 已取消 |`(从 待处理 可向前 3 个老字面推进)

**"(新建)" 行字面修改**:`报告 / 调查中` → `待处理 / 报告 / 调查中`(新增 `待处理` 作为新建入口状态)

### 2.2 C-fix-3:步骤 5 注脚

**原字面**:
```
> **注**:本技能**不**再推进"修复规划中 / 修复编码中 / 已修复-待验证 / 已修复-已验证"等状态。
> 若需推进上述状态,请按以下顺序调:
> 1. `code-plan <BUG-NNN>` —— 产出 `fix-plan.md`,推进"修复规划中"
> 2. `code-it <BUG-NNN>` —— 产出 `fix-work-log.md`,推进"修复编码中" / "已修复-待验证"
> 3. (可选)`code-it <BUG-NNN>` 步骤 8.5 自含按需写单测—— 产出 `unit-test-results.md`(若项目可测)
> 4. `code-check <BUG-NNN>` —— 产出 `REVIEW-REPORT.md`,推进"已修复-已验证" / "已关闭"
```

**新字面**:
```
> **注**:本技能**不**推进"待开发 / 开发中 / 待审查 / 已完成"等状态 — 这些由 `code-plan` / `code-it` / `code-check` 自动推进。
> 若需推进上述状态,请按以下顺序调:
> 1. `code-plan <BUG-NNN>` —— 产出 `fix-plan.md`,推进"待处理 → 待开发"
> 2. `code-it <BUG-NNN>` —— 产出 `fix-work-log.md`,推进"待开发 → 开发中"(第 1 个任务)→ "待审查"(全部完成)
> 3. (可选)`code-it <BUG-NNN>` 步骤 8.5 自含按需写单测—— 产出 `unit-test-results.md`(若项目可测)
> 4. `code-check <BUG-NNN>` —— 产出 `REVIEW-REPORT.md`,推进"待审查 → 已完成"
```

### 2.3 C-fix-4:步骤 9 引导下一步表

**原字面**(8 行,部分老字面 `修复规划中` / `修复编码中` / `已修复-待验证` / `已修复-已验证` / `已关闭-不修复` 全部由 `code-fix` 维护)

**新字面**(7 行,新字面 `待处理 / 待开发 / 开发中 / 待审查 / 已完成` 全部标注"本技能不参与"):
- `待处理 | 调 code-fix <BUG-NNN> 进入"报告",补充报告信息`
- `报告 | 调 code-fix <BUG-NNN> 进入"调查中",补充根因`
- `调查中 | 调 code-plan <BUG-NNN> 产出 fix-plan.md,进入"待开发"`
- `待开发 | (由 code-plan 推进;本技能不参与此状态之后)`
- `开发中 | (由 code-it <BUG-NNN> 推进)`
- `待审查 | (由 code-it 全部完成时推进;本技能不参与)`
- `已完成 | (由 code-check <BUG-NNN> 推进;本技能不参与)`
- `阻塞 | 解决阻塞后调 code-fix 解除`

**步骤 9 末尾"典型修复流程"**:从 6 步改为 6 步,但语义重写:本技能只参与前 2 步,后续 4 步由下游自动接力

### 2.4 C-fix-5:## 与其他技能的关系 段"典型完整流程" 10 步 → 4 步

**原字面**(10 步,本技能调 5 次 + code-plan/code-it/code-check 各调 1 次):
```
1. code-fix "用户报告:..." → BUG-00001 / 报告
2. code-fix BUG-00001 (→ 调查中) → 补充根因
3. code-plan BUG-00001 → fix-plan.md
4. code-fix BUG-00001 (→ 修复规划中) → 看板同步
5. code-it BUG-00001 → 实际改代码
6. code-fix BUG-00001 (→ 修复编码中) → 看板同步
7. code-fix BUG-00001 (→ 已修复-待验证) → 等待验证
8. 跑测试
9. code-fix BUG-00001 (→ 已修复-已验证) → 记录验证信息
10. code-fix BUG-00001 (→ 已关闭) → 归档
```

**新字面**(4 步,本技能只调 1 次 + code-plan/code-it/code-check 各调 1 次):
```
1. code-fix "用户报告:..." → BUG-00001 / 待处理
2. code-plan BUG-00001 → fix-plan.md,状态自动推进到"待开发"
3. code-it BUG-00001 → 实际改代码,状态自动经"开发中"推进到"待审查"
4. code-check BUG-00001 → REVIEW-REPORT.md,状态自动推进到"已完成"
```

**对比**:典型完整流程从 10 步压缩为 4 步,本技能调用次数从 5 次降为 1 次(本技能纯登记型职责进一步收敛;FR-8 期望结果达成)

### 2.5 C-fix-6:## 不要做的事 段

**原字面**:`**不**推进"修复规划中 / 修复编码中 / 已修复-待验证 / 已修复-已验证"等状态(由 code-plan / code-it / code-check 推进)`

**新字面**:`**不**推进"待开发 / 开发中 / 待审查 / 已完成"等状态 — 这些由 code-plan / code-it / code-check 自动推进`

## 3. 关键决策与权衡

1. **新建 BUG 初始状态字面选择**:PLAN §4.1 + 详细设计 §5.1 锁定为 `待处理`,而非沿用老字面 `报告`。理由:让"待处理"作为新流程的入口锚点,后续推进链路为 `待处理 → 待开发 → 开发中 → 待审查 → 已完成`(5 步 5 个新字面),老字面 `报告 / 调查中` 等保留为可复跑目标。
2. **步骤 4 状态推进表 FROM 列保留老字面**:为符合 PD-2(NFR-3 详细化:状态字段字面变更的兼容性),老字面 `报告 / 调查中 / 已关闭-非缺陷 / 已取消 / 阻塞` 在 FROM 列中保留,但候选目标列中**不**再出现;`已修复-*` 等老字面从表中**完全移除**(由 `code-check` 维护)。
3. **步骤 9 引导表处理老字面**:`已修复-待验证 / 已修复-已验证 / 已关闭-不修复` 3 个老字面**完全移除**(沿用 PD-2 老字面**不**归一化,但**不**在本技能新流程中引导;若历史 BUG 处于这些状态,改走 `code-check <BUG-NNN>` 完成收尾)。
4. **frontmatter L1-3 字节级保留**:`name=code-fix` + `description` 字段一字未改(沿用 `skill-conventions §规则 1` 强约束)。
5. **不引入开发痕迹**:本任务新写/修改的段落中,无"本需求 REQ-NNNNN 新增" / "Q-N 锁定" / "YYYY-MM-DD 起生效" / 退场文件名引用 等 6 类字面(沿用 `skill-conventions §规则 2`)。

## 4. 偏离设计/规范的地方

详见 `deviations.md` §偏离 1:本任务无生产代码改动,无 `Bash` 编译/运行/测试命令;授权来源 = PLAN.md §12.3 实施关键约束(用户预先确认)。

## 5. 验证结果

- 静态校验:`git diff --stat` 输出 1 file changed, 25 insertions(+), 30 deletions(-)
- frontmatter L1-3 字节级保留校验:✅
- 步骤 4 状态推进表 7 行结构:✅(新增 1 行 + 修改"(新建)"行 + 保留 5 行)
- 步骤 9 引导下一步表 7 行结构:✅(新增 4 行 + 删除 5 行老字面 + 保留 1 行 `阻塞`)
- 典型完整流程 4 步:✅(10 步 → 4 步)
- AC-1(新建 BUG 文档头"状态" = `待处理`):静态校验通过(由 TASK-REQ-00037-00007 端到端验证)
- AC-6(BUG 处于 `待开发` 时复跑 `code-fix` 屏显警告 + 不修改文件):静态校验通过(由 TASK-REQ-00037-00007 端到端验证)

## 6. 已知问题/未完成项

- **AC-1 / AC-6 端到端验证**:本任务仅做静态校验;端到端验证由 TASK-REQ-00037-00007 承担(沿用 PLAN.md §4 任务依赖图 T-7 依赖 T-1 ~ T-6)
- **T-2 ~ T-6 待执行**:`code-plan` 步骤 27A/28A + `code-it` 步骤 21/24 + `code-check` 步骤 13 + `code-dashboard` 段 3 的 5 处"状态回写"子步骤需后续 5 个任务分别落地

## 7. 关联任务与提交

- 关联任务:T-1(本任务,入口)→ T-2 / T-3 / T-4 / T-5 / T-6(并行依赖 T-1 的状态字面约定)→ T-7(端到端验证)
- 提交哈希:(本任务为单任务,本会话末尾由 `code-it` 兜底统一提交,沿用 REQ-00017 强约束)

## 8. 过程文档清单

- ✅ `work-log.md`(步骤 4 / 5 / 6 / 7 / 8 全程记录)
- ✅ `compile-and-run.md`(本任务无 Bash 命令,占位静态校验)
- ✅ `deviations.md`(偏离 1:无生产代码改动)
- ✅ `test-results.md`(测试状态=不适用;AC 静态校验结论)
- ✅ `process-doc-decisions.md`(**不**生成 — 本任务所有过程文档均生成,无"不涉及/不适用"判定)
- ✅ `unit-test-results.md`(**不**生成 — `code-it` 步骤 8a 守卫不通过,跳过单测)