# 改修总结 — TASK-REQ-00030-00006

- 任务编码:TASK-REQ-00030-00006
- 任务标题:[文档] 行数收敛观测:落地后 3 个新需求验证 NFR-1
- 所属需求:REQ-00030
- 所属版本:V0.0.3
- 改修日期:2026-06-12
- 状态:已完成(占位实施;真实验证待 REQ-00031+ 落地后)

## 1. 改修内容总览

**无文件源码修改**(纯观测任务)。

修改 1 个文档:
- `assistants/V0.0.3/plan/REQ-00030/RESULT.md`:
  - 末尾**追加**"## 16. 验证结果(本节为 T-006 占位,等 REQ-00031+ 落地后填写)"小节
  - 含 AC-8.1 / AC-8.2 阈值 + 实际值占位 + 观测方法

## 2. 观测方法

1. 收集 3 个新 REQ(REQ-00031 / REQ-00032 / REQ-00033)的 `design/.../RESULT.md` 行数(用 `wc -l`)
2. 收集同 3 个新 REQ 的 `plan/.../RESULT.md` 行数(用 `wc -l`)
3. 计算平均行数,与 AC-8 阈值对比:
   - AC-8.1:`design` 平均 ≤ 184(= 264 × 0.7)
   - AC-8.2:`plan` 平均 ≥ 340(= 284 × 1.2)
4. 若通过,看板"M2-REQ-00030 里程碑"状态推进为"已完成"
5. 若不通过,标记 M2 为"建议改",讨论:
   - 阈值是否过严(参考上游 Q-1 "≥ 30% 是否过严")
   - 是否有"新需求自身太大"需拆分

## 3. 关键决策

- 观测时**不**区分 `--extensible` / `--balanced` / `--minimal` 需求(行数预期会因 --extensible 更高,故 3 个新 REQ 应**全部**为 --balanced 才公平)
- 若 3 个新 REQ 不足,**不**强行凑数;M2 里程碑顺延
- 观测在用户调 `code-check` 时**自动追加**到 `REVIEW-REPORT.md` 的"## 行数收敛"区

## 4. 提交哈希

- 本任务**不**产生 git commit(无源码改动)
