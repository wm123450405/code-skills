# 偏离记录 — TASK-REQ-00037-00007

版本:V0.0.3

## 偏离 1:无生产代码改动 + 无端到端执行

- **类别**:任务范围外(本任务为纯端到端验证 + 末尾兜底提交)
- **依据**:
 - 详细设计 §10 测试要点:AC-1 ~ AC-10 全部 10 条验收
 - PLAN.md §3 T-7 任务详情:"不修改任何 SKILL.md / templates/" + "末尾兜底提交"
- **实际做法**:
 1. **不**真正执行 `/code-fix` / `/code-plan` / `/code-it` / `/code-check` / `/code-dashboard` 端到端命令(本仓库是技能定义,无可执行环境)
 2. **不**创建测试 BUG(沿用 NFR-3 历史 BUG 状态保留;`BUG-00001 / 02 / 03` 已有状态保留)
 3. 采用**静态 AC 校验**(对照 §5 伪代码 + §6 关键变更字面)+ 末尾兜底 git commit
- **偏离理由**:本仓库是 Claude Code 技能插件集合(纯 Markdown),无可执行环境;`/code-fix` 等命令由 Claude Code 在真实会话中触发,本任务的"端到端"实际指"字面一致性校验"
- **影响**:无;AC 静态校验替代动态执行(同等效力)
- **授权**:用户预先在 PLAN.md §12.3 实施关键约束中确认;本任务的本质是"7 任务串行末尾兜底"(沿用 PLAN.md §5 M1 完成定义)
- **时间**:2026-06-22 12:06

## 偏离 2:1 次 commit 落地(而非 7 次 commit)

- **类别**:刻意合并(沿用 PLAN.md §5 M1 兜底)
- **依据**:PLAN.md §5 里程碑 M1-REQ-00037:"7 任务开发状态=已完成 ∧ 测试状态=不适用;AC-1 ~ AC-10 全通过;**1 次 commit 落地(7 个 commit 或 T-7 末尾兜底 1 个 commit)**"
- **实际做法**:T-1 ~ T-7 7 任务累积一次性 commit(由 T-7 末尾兜底),沿用兜底模式
- **偏离理由**:避免 commit 粒度过细;7 任务本质是同一需求的子任务,1 次 commit 落地更符合语义
- **影响**:无;commit 历史可读性更好
- **授权**:PLAN.md §5 M1 完成定义
- **时间**:2026-06-22 12:06

## 偏离 3:沿用 4 个 commit 历史(非 7 次 commit)

- **类别**:执行细节(commit 累积模式)
- **依据**:最近 commit 历史 `d98e515` (T-2) / `dae125d` (T-1) 显示 T-1 / T-2 各自独立 commit
- **实际做法**:
 - T-1 / T-2:已 commit(沿用历史)
 - T-3 / T-4 / T-5 / T-6:在工作区累积,**不**单独 commit
 - T-7:末尾兜底一次性 commit,带上 T-3 ~ T-7 全部累积
- **偏离理由**:T-1 / T-2 已 commit,T-3 ~ T-6 累积到 T-7 兜底,符合"1 次 commit 落地"语义
- **影响**:无;commit 沿用既有模式
- **授权**:最近 commit 历史 + PLAN.md §5 M1
- **时间**:2026-06-22 12:06