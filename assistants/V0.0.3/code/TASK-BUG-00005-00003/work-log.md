# 开发日志 — TASK-BUG-00005-00003

- 任务编码:TASK-BUG-00005-00003
- 标题:[修改] code-require 步骤 615 硬约束增强(引用步骤 7A/8A)
- 缺陷编号:BUG-00005
- 版本:V0.0.3
- 责任人:wangmiao
- 开始时间:2026-06-23

---

## 项目现状(步骤 20 记录)

- **项目类型**:`code-skills` 技能定义仓库(Markdown 技能文档)
- **构建/运行命令**:**N/A**(纯 Markdown 文档)
- **目标文件**:`plugins/code-skills/skills/code-require/SKILL.md`(已因 T-1 + T-2 改动累计顺移 4 行)
- **当前实际位置**:
 - 步骤 7A 末段(过滤技术选型类问题子节):T-1 后位于 L335(原 L333)
 - 步骤 8A 末段(过滤技术选型类 NFR 子节):T-2 后位于 L353(原 L335,因 T-1 顺移 +2 后,再因 T-2 +2)
 - 步骤 615 当前位置:**L619**(原 L615,因 T-1 +2 + T-2 +2,共顺移 +4)
 - 步骤 615 是文件**末尾**最后一条(L619 是最后一行非空,L620 是文件结束空行)
 - 文件总行数:约 620 行
- **PLAN.md §3.3 字面**:"涉及文件/模块:`plugins/code-skills/skills/code-require/SKILL.md` §步骤 615" — 字面与现状一致(锚点 = 步骤 615 标题概念,**L619 是结构性顺移,内容字面零改**)
- **既有相似功能**(参考 T-1 + T-2):
 - T-1(步骤 7A 末段 L335):`>` 引用子节 + 20 关键词 + 与步骤 615 交叉引用
 - T-2(步骤 8A 末段 L353):`>` 引用子节 + 14 关键词 + 与 T-1 + 步骤 615 交叉引用

## 项目级规范要点(步骤 19 记录)

- `skill-conventions §规则 2`:不引入新字段(本任务关键约束)
- `encoding-conventions §规则 1`:接收端宽松正则
- `coding-style.md`:命名规范
- `module-conventions.md`:模块结构规范
- `dashboard-conventions.md`:看板字段约定

## 任务设计要点(步骤 20 记录)

- **PLAN.md §3.3**:在步骤 615 的硬约束后,添加引用:"步骤 7A / 步骤 8A 中的技术选型类问题应自动跳过并记录到 clarifications.md"
- **详细设计 §4.3**:位置 = 步骤 615;改动 = 在硬约束**后**添加引用;状态归属 = 无状态(纯约束声明);对应概要设计 = 方案 B 补充
- **详细设计 §10.2 流程**(步骤 8A 修复后流程):命中技术选型类 → 标记 defer-to-code-design + 记录到 clarifications.md + 跳过本 NFR
- **本任务与 T-1 / T-2 的区别**:
 - T-1:在**工作流程**章节的"### 步骤 7A — 与用户澄清不确定项"末尾追加 `>` 引用子节(操作性子节,作用域=候选选项过滤)
 - T-2:在**工作流程**章节的"### 步骤 8A — 撰写 RESULT.md"末尾追加 `>` 引用子节(操作性子节,作用域=NFR 过滤)
 - T-3(本任务):在**不要做的事**章节的步骤 615 末尾追加新 `- 列表项`(约束性配套,作用域=把操作性子节与约束性子节绑定)
- **不变量(INV)**:
 - 步骤 615 原字面 L619 **0 改**(字节级保留)
 - `## 不要做的事` 章节标题字节级保留
 - 步骤 7A / 8A 既有子节**0 改**(T-1 / T-2 范围)
 - frontmatter L1-3 **0 改**
 - `## 工作流程` 小节既有内容 0 改

## 开发过程

### 2026-06-23 启动

- 操作:Read `code-require/SKILL.md` L615-620(定位步骤 615 当前字面 + 上下文)
- 目的:确认步骤 615 当前字面(实际 L619,内容字节级保留自原 L615)
- 结果:成功 — 步骤 615 是文件最后一条,后接空行 L620(文件末尾)

- 操作:Grep 关键词"^### 步骤" + "^## 步骤" + "^- 不涉及技术选型"
- 目的:确认章节顺移情况 + 步骤 615 字面
- 结果:成功 — 步骤 615 = L619(顺移 +4),字面 0 改

- 操作:Read `fix/BUG-00005/RESULT.md` 文档头 + §4.3(详细设计)
- 目的:确认 T-3 详细设计已存在(§4.3 = 步骤 615 硬约束增强模块)
- 结果:成功

### 2026-06-23 改动方案选择

**问题**:`## 不要做的事` 是 `- 列表项`(每条 1 行),不同于 `## 工作流程` 的 `>` 引用块子节。三种改法选项:

- **选项 A**:在步骤 615 行**内部**追加 `>` 引用块 — 破坏 Markdown 列表结构,**拒绝**
- **选项 B**:在步骤 615 行**末尾**追加同列表项的附加说明(逗号或句号分隔)— 字面增量小,但**修改了步骤 615 字面**(违反 0 改原则)
- **选项 C**:在步骤 615 行**下方**(L620)追加 1 个新 `- 列表项` — 字面增量较大(新增 1 行),但与详细设计 §4.3 字面一致("在步骤 615 的硬约束**后**,添加引用"),且**步骤 615 原字面 0 改**

**决策**:采用**选项 C**,理由:
1. 与详细设计 §4.3 字面一致("后" = 下方)
2. 不修改步骤 615 原字面(完整保留原硬约束)
3. `- 列表项` 形式符合 `## 不要做的事` 章节的列表风格
4. 字面明确"步骤 615 硬约束**配套**",语义清晰区分原硬约束与配套操作

### 2026-06-23 实施改动

- 操作:Edit `code-require/SKILL.md` 步骤 615(L619 下方追加新 `- 列表项`)
- 目的:实现 TASK-BUG-00005-00003 关键变更
- 改动:
 - **旧字面**(步骤 615 L619,既有字面,字节级保留):
 ```
 - 不涉及技术选型 / 技术栈 / 技术方案的确定:本技能只确定功能点(FR / NFR / 页面 / 交互 / 数据 / 边界);技术选型 / 架构风格 / 接口风格 / 数据模型(库表层) / 部署形态归 `code-design`(由其结合项目实际状况给出方案);没有**必要**进行技术选型的需求,**无需**在 `code-require` 阶段分析技术选型选项。
 ```
 - **新字面**(在 L619 下方追加 1 个新 `- 列表项` L620,**原 L619 字面字节级保留**):
 ```
 - 不涉及技术选型 / 技术栈 / 技术方案的确定:本技能只确定功能点(FR / NFR / 页面 / 交互 / 数据 / 边界);技术选型 / 架构风格 / 接口风格 / 数据模型(库表层) / 部署形态归 `code-design`(由其结合项目实际状况给出方案);没有**必要**进行技术选型的需求,**无需**在 `code-require` 阶段分析技术选型选项。
 - 步骤 615 硬约束配套(REQ-00033 / BUG-00005 修复):本约束在步骤 7A / 步骤 8A 中具有**操作性配套** — 步骤 7A 末尾"过滤技术选型类问题"子节(关键词集 20 个,详见 BUG-00005 修复详细设计 §5.1)+ 步骤 8A 末尾"过滤技术选型类 NFR"子节(关键词集 14 个,详见 BUG-00005 修复详细设计 §5.2);命中任一关键词的候选选项 / NFR,本轮**不**弹 `AskUserQuestion` / **不**写入 NFR 章节,改为追加到 `clarifications.md` 的 `## 延迟到 code-design 阶段(技术选型类)` 或 `## NFR 延迟到 code-design 阶段(技术选型类)` 区段(分门别类),留待 `code-design` 阶段统一分析。三者形成"输入澄清(步骤 7A)+ NFR 记录(步骤 8A)+ 硬约束声明(步骤 615)"的完整技术选型过滤链。
 ```
- **不动范围**:
 - 步骤 615 原字面 L619 **0 改**(字节级保留)
 - `## 不要做的事` 章节标题(L606)**0 改**
 - 步骤 7A(L322-335)/ 8A(L337-353)既有子节**0 改**
 - 步骤 7A / 8A 的"要求:" / "## 工作流程" 后续章节**0 改**
 - frontmatter L1-3 **0 改**
- 结果:成功(选项 C 实施,与详细设计 §4.3 字面"在步骤 615 的硬约束后,添加引用"一致)
