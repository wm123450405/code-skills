# 改修总结 — TASK-REQ-00030-00002

- 任务编码:TASK-REQ-00030-00002
- 任务标题:[修改] templates/design.md:§7/§8/§9/§10 章节重写 + §7.5/§8.5/§9.5 新增
- 所属需求:REQ-00030
- 所属版本:V0.0.3
- 改修日期:2026-06-12
- 改修者:用户
- 状态:已完成

## 1. 改修内容总览

修改 1 个文件:
- `plugins/code-skills/skills/code-design/templates/design.md`:
  - 模板顶部注释**追加** 1 行(本模板只覆盖概要设计)
  - §7.1 模块总览表 6 列 → 5 列(移除"对外接口")
  - §7.2 新增模块 6 字段 → 4 字段(移除"关键设计点 / 对外接口 / 依赖"3 字段)
  - **新增** §7.5 设计边界
  - §8.1 新增接口 7 项(代码块) → 4 列(表格)
  - **新增** §8.5 设计边界
  - §9.1 新增实体 4 列(代码块) → 4 列(表格)
  - **新增** §9.5 设计边界
  - §10.2 新增依赖 6 列 → 4 列(移除"版本 / 许可 / 风险评估"3 列)

未修改:§3-§6 / §11-§16 / §## 模板顶部文档头标题等既有章节
未新增任何文件

## 2. 详细改动(按结构化语义定位)

### 2.1 模板顶部注释**追加** 1 行
- 改前:3 行(标题 / 描述 / 版本感知)
- 改后:4 行(+ 1 行 `> 本模板只覆盖概要设计...`)

### 2.2 §7 模块划分 4 个子节修订
- §7.1 模块总览表 6 列 → 5 列
- §7.2 新增模块 6 字段 → 4 字段(移除"关键设计点 / 对外接口 / 依赖")
- §7.3 / §7.4 沿用
- **新增** §7.5 设计边界(5 行)

### 2.3 §8 接口概要 4 个子节修订
- §8.1 新增接口:7 项代码块 → 4 列表格(接口名 / 形式 / 状态 / 一句话职责)
- §8.2 / §8.3 / §8.4 沿用
- **新增** §8.5 设计边界(10 行)

### 2.4 §9 数据结构 4 个子节修订
- §9.1 新增实体:代码块 → 4 列表格(实体名 / 状态 / 关键字段 ≤ 5 个 / 一句话关系)
- §9.2 / §9.3 沿用
- **新增** §9.5 设计边界(7 行)

### 2.5 §10 三方依赖 2 个子节修订
- §10.1 复用既有依赖沿用
- §10.2 新增依赖:6 列 → 4 列(依赖名 / 一句话用途 / 必要性 / 替代评估)

## 3. 关键决策与权衡

| 决策 | 内容 | 权衡 |
| --- | --- | --- |
| §7.2 "理由"字段**保留** | 概设阶段保留"为什么新增 / 为何这样切" | 这是设计决策的**论证**,不是实施细节;下游 plan 不重复 |
| §7.2 "职责"字段**保留** | 一句话职责属于"为什么"的范畴 | 实施细节(类签名/调用顺序)下沉到 plan |
| §8.1 改 7 项代码块为 4 列表格 | 表格化便于扫读;代码块是模板"示例",非强制结构 | 表格与"接口细节"职责正交 |
| §9.1 关键字段 ≤ 5 个上限 | 防止 AI 写完整字段表 | 5 个是经验值,典型实体在概设阶段只需突出 3-5 个核心字段 |
| §9.1 关键字段**不**写类型 | 类型 / 约束 / 索引全部下沉 | 概设不展开类型,避免越界 |
| §10.2 "必要性"和"替代评估"**保留** | 这是决策论证,不是评估项 | 与 §7.2 "理由"对称 |

## 4. 偏离设计/规范的地方

**无偏离**:INV-1 / INV-3 / INV-5 全部满足(本任务**不**改 frontmatter / "## 不要做的事" / 项目级规范)

## 5. 验证结果

- **不适用**(本仓库**不**含构建/测试框架)
- 验证手段:人工 Read 校验 9 个子节的字段数 + "## 设计边界" 3 个新增子节存在

## 6. 已知问题/未完成项

**无**。

## 7. 关联任务与提交

- 提交哈希:<待 commit 后回填>
