# 改修总结 — TASK-REQ-00037-00003

- 任务编码:TASK-REQ-00037-00003
- 任务标题:[修改] code-it 步骤 21 末尾追加状态回写:开发中
- 任务类型:修改
- 触发/来源:详细设计
- 所属需求:REQ-00037
- 所属版本:V0.0.3
- 执行时间:2026-06-22 09:50 ~ 09:55
- 执行人:wangmiao
- 完成时间:2026-06-22 09:55

## 1. 改修内容总览

按 PLAN.md §3 T-3 + 详细设计 §6.3 的 2 处关键变更(C-it-1 / C-it-3),把 `code-it` 启动 BUG 第 1 个任务时,自动推进缺陷状态到 `开发中`(新字面)/ `修复编码中`(老字面)。

| 变更点 | 目标位置 | 状态 |
| --- | --- | --- |
| C-it-1 | §"缺陷分支" 步骤 21 末尾追加"第 6 款 itStartStateRollback 子步骤" | ✅ |
| C-it-3 | ## 不要做的事 段字面追加"不修改稳定章节" | ✅ |

- 涉及文件:`plugins/code-skills/skills/code-it/SKILL.md`(唯一)
- `git diff --stat`:**1 file changed, 20 insertions(+), 6 deletions(-)**

## 2. 详细改动

### 2.1 C-it-1:步骤 21 末尾新增 `itStartStateRollback` 子步骤

**原字面**(line 891-900,5 步):
- 状态:硬编码 `修复规划中` → `修复编码中`

**新字面**(5 步原结构 + 末尾 14 行子步骤):
- 状态判定扩展为 `修复规划中` / `待开发` → 推进(按子步骤判定)
- 末尾追加 `itStartStateRollback` 子步骤(14 行):
 - 读 `fix/<BUG-NNN>/RESULT.md` 文档头"当前状态"字面 → `oldStatus`
 - 读 `fix/<BUG-NNN>/PLAN.md` 任务总览首条任务编号 → `firstTaskNum`
 - 判定推进策略矩阵:
 - `taskNum != firstTaskNum` → 状态维持 + 屏显
 - `待开发`(新)→ `开发中`
 - `修复规划中`(老)→ `修复编码中`(走既有路径)
 - 其他字面 → 跳过 + 屏显
 - 幂等检查 + 写状态 + 同步 `fix/RESULT.md` + 同步看板
 - 失败回退(NFR-2)

### 2.2 C-it-3:## 不要做的事 段字面追加

**原字面**:缺陷分支特殊禁止 6 条

**新字面**:在末尾追加 1 条:
- `**不**修改 fix/<BUG-NNN>/RESULT.md 的"缺陷描述" / "根因分析" 等稳定章节 — 只更新状态字段、修复日志、变更记录(沿用)`

## 3. 关键决策与权衡

1. **首条任务判定纳入子步骤**:仅当本任务 = `PLAN.md` 任务总览首条时,才推进 `待开发` → `开发中`(沿用详细设计 §5.3 E-1)。非首条任务不推进(避免误推进)。
2. **15 字面兼容矩阵**:沿用 T-2 推进策略矩阵(`待处理` 由 T-2 处理,本任务不重复),复用 PD-2 老字面保留。
3. **frontmatter L1-3 字节级保留**:未修改。
4. **不引入开发痕迹**:新写段落不含 6 类字面。

## 4. 偏离设计/规范的地方

详见 `deviations.md` §偏离 1:本任务无生产代码改动。

## 5. 验证结果

- 静态校验:`git diff --stat` 输出 1 file changed, 20 insertions(+), 6 deletions(-)
- frontmatter L1-3 字节级保留校验:✅
- 步骤 21 末尾 `itStartStateRollback` 子步骤 14 行字面:✅
- ## 不要做的事 段字面追加 1 行:✅
- AC-3(`待开发` → `开发中`,首条任务):静态校验通过(由 T-7 端到端验证)

## 6. 已知问题/未完成项

- **AC-3 端到端验证**:由 TASK-REQ-00037-00007 承担
- **T-4 / T-5 / T-6 待执行**:`code-it` 步骤 24 + `code-check` 步骤 13 + `code-dashboard` 段 3 的 3 处"状态回写"子步骤需后续 3 个任务分别落地

## 7. 关联任务与提交

- 关联任务:T-1 / T-2(本任务依赖)→ T-4 / T-5 / T-6(并行,可后续执行)→ T-7(端到端验证)
- 提交哈希:(本会话末尾由 `code-it` 兜底统一提交)

## 8. 过程文档清单

- ✅ `work-log.md` / `compile-and-run.md` / `deviations.md` / `test-results.md`
- ✅ `process-doc-decisions.md`(**不**生成)
- ✅ `unit-test-results.md`(**不**生成 — 守卫不通过,跳过单测)