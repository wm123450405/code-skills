# TASK-BUG-00005-00001 实施记录

- 任务编码:TASK-BUG-00005-00001
- 标题:[修改] code-require 步骤 7A 添加技术选型过滤器
- 缺陷编号:BUG-00005
- 所属版本:V0.0.3
- 类型:修改
- 触发/来源:缺陷修复
- 关联任务:BUG-00005
- 前置任务:无
- 责任人:wangmiao
- 创建:2026-06-23
- 最近更新:2026-06-23
- 当前版本:v1

---

## 1. 任务信息

- **任务编码**:TASK-BUG-00005-00001
- **任务标题**:[修改] code-require 步骤 7A 添加技术选型过滤器
- **任务类型**:修改
- **触发/来源**:缺陷修复
- **关联任务**:BUG-00005
- **依据文档**:
 - `./assistants/V0.0.3/fix/BUG-00005/RESULT.md` §4.1 + §5.1
 - `./assistants/V0.0.3/fix/BUG-00005/PLAN.md` §3.1
- **任务来源**:缺陷 BUG-00005(报告时间 2026-06-22 14:50;状态 修复规划中)

## 2. 改修内容总览

| 维度 | 值 |
| --- | --- |
| 涉及文件数 | 1 |
| 新增行数 | 2(L334 空行 + L335 新子节) |
| 删除行数 | 0 |
| 修改行数 | 0 |
| 字面改写 | 0(纯追加,既有 L322-333 字面 0 改) |
| 涉及文件 | `plugins/code-skills/skills/code-require/SKILL.md` |

## 3. 详细改动

### 3.1 改动文件:`plugins/code-skills/skills/code-require/SKILL.md`

**位置**:`## 工作流程` → `### 步骤 7A — 与用户澄清不确定项`(L322-333)

**改动方式**:**纯追加**(在步骤 7A 末尾的 `>` 引用行后追加 1 个新 `>` 引用子节)

**改动前**(L322-333,12 行,既有字面,字节级保留):
```
### 步骤 7A — 与用户澄清不确定项
对材料中**模糊、有歧义、缺失**的功能或交互,向用户确认:
- 能给出 2-4 个具体选项 → 使用 `AskUserQuestion` 工具
- 需要开放式描述 → 用自然语言提问
- **每轮问答**追加到 `clarifications.md`,字段:
 - `时间`
 - `问题`
 - `选项/上下文`
 - `用户回答`
 - `影响的 RESULT.md 章节`

> **不要一次问完所有问题**。每轮聚焦 1-3 个最阻塞的点,得到答复后先更新对应章节,再迭代出下一批问题。
```

**改动后**(L322-335,14 行,L322-333 字节级保留 + L334 空行 + L335 新子节):
```
### 步骤 7A — 与用户澄清不确定项
对材料中**模糊、有歧义、缺失**的功能或交互,向用户确认:
- 能给出 2-4 个具体选项 → 使用 `AskUserQuestion` 工具
- 需要开放式描述 → 用自然语言提问
- **每轮问答**追加到 `clarifications.md`,字段:
 - `时间`
 - `问题`
 - `选项/上下文`
 - `用户回答`
 - `影响的 RESULT.md 章节`

> **不要一次问完所有问题**。每轮聚焦 1-3 个最阻塞的点,得到答复后先更新对应章节,再迭代出下一批问题。
>
> **过滤技术选型类问题**(REQ-00033 / BUG-00005 修复):在生成 `AskUserQuestion` 候选选项后,先按关键词集 `{技术选型, 实现方式, 框架, 库, 工具, monorepo, single-package, pnpm, npm, yarn, Jest, Vitest, Mocha, React, Vue, Angular, 数据库, ORM, 消息队列, 缓存, Redis, MongoDB}` 逐项匹配。命中任一关键词的候选选项,本轮**不**弹出 `AskUserQuestion`,改为追加到 `clarifications.md` 的 `## 延迟到 code-design 阶段(技术选型类)` 区段(每条含:`时间` / `原始问题` / `命中的关键词` / `延迟到=code-design` 字段,留待 `code-design` 阶段统一分析);未命中关键词的候选选项照常弹 `AskUserQuestion`。本子节与"## 不要做的事"步骤 615 配套 — 步骤 615 声明"不涉及技术选型",本子节给出"发现技术选型类候选选项时的具体处理动作";算法与边界条件详见 BUG-00005 修复详细设计 §5.1。
```

**改动说明**:
- **L322** `### 步骤 7A — 与用户澄清不确定项`:字节级保留
- **L323-L332** 步骤 7A 既有"每轮问答"逻辑 8 项:字节级保留
- **L333** `> **不要一次问完所有问题**`:字节级保留
- **L334**(新增):空行(`>` 引用块后空行,符合 Markdown 规范)
- **L335**(新增):新 `>` 引用子节"过滤技术选型类问题"
  - 关键词集完整列出(20 个关键词,沿用详细设计 §5.1)
  - 触发动作:命中关键词 → 跳过 `AskUserQuestion` + 追加到 `clarifications.md` 的 `## 延迟到 code-design 阶段(技术选型类)` 区段
  - 字段:`时间` / `原始问题` / `命中的关键词` / `延迟到=code-design`
  - 与步骤 615 显式关联(交叉引用)
  - 引用 BUG-00005 详细设计 §5.1 算法
- **步骤 8A / 615 / 标题 / 章节**:字节级保留(顺移到 L337 / L617,内容字面不变)

## 4. 关键决策与权衡

### 4.1 决策:关键词集来源

**决策**:沿用 BUG-00005 详细设计 §5.1 关键词集(20 个关键词)。

**理由**:
- 与"## 不要做的事"步骤 615 显式关联 — 步骤 615 字面列举"技术选型 / 架构风格 / 接口风格 / 数据模型(库表层) / 部署形态" 5 类
- 关键词集是 5 类的具体化展开(覆盖主流前端/后端/工具/库/数据库)
- 显式列举 20 关键词,避免 LLM 自行扩展导致误判(明确边界)

### 4.2 决策:`>` 引用块 + 嵌套 vs 新增 `### 子节`

**决策**:使用 `>` 引用块(嵌套在步骤 7A 的"不要一次问完所有问题"行后)。

**理由**:
- 沿用步骤 7A 既有"嵌套规则"的字面风格(L333 用 `>` 引用)
- 新子节是步骤 7A 的**子规则**(与"不要一次问完所有问题"同性质)
- 避免新增 `### 步骤 7A.X — 过滤技术选型类问题` 子节标题(减少文档结构变化)
- 步骤 7A 标题 L322 字节级保留(不引入新章节)

### 4.3 决策:不引入新字段

**决策**:`clarifications.md` 沿用既有 `时间 / 问题 / 选项/上下文 / 用户回答 / 影响的 RESULT.md 章节` 5 字段,**不**新增"延迟到"字段。

**理由**:
- `skill-conventions §规则 2` 强约束:不引入新字段
- 用 `## 区段标题 ## 延迟到 code-design 阶段(技术选型类)` 而非字段区分(沿用既有"## 区段"分组范式)
- `code-design` 阶段读 `clarifications.md` 时,扫"## 延迟到"区段即识别技术选型类问题(无需新字段)

### 4.4 决策:不写 `compile-and-run.md` / `test-results.md` / `unit-test-results.md`

**决策**:本任务为纯 Markdown 改造,**不**生成 3 类测试/编译过程文档。

**理由**:
- 步骤 8.7 物化 `decisions` 判定:
 - `compileAndRun = '不生成'`(纯文档/纯配置)
 - `testResults = '不生成'`(测试状态=不适用)
 - `unitTestResults = '不生成'`(项目不可测 + 任务类型=修改 + 纯 Markdown 改动)
- 步骤 23 守卫:**不**调 `Bash` 跑编译/启动/测试
- `code-check` 评审时不依赖此 3 类文档(本任务为纯文档任务)

## 5. 偏离设计/规范的地方

**无**(纯追加,既有 L322-333 字节级保留,无任何字面改写)。

详见 `./deviations.md`(内容 = "无偏离" 1 行)。

## 6. 验证结果

- **编译/启动/测试**:N/A(纯 Markdown 改造,无此环节)
- **静态校验**(沿用 BUG-00004 步骤 8.7 + REQ-00034 守卫):
 - ✅ 步骤 7A 标题字面 L322 字节级保留
 - ✅ 步骤 7A 既有 L323-L333 字面 字节级保留
 - ✅ 步骤 8A 标题字面 字节级保留(顺移到 L337,内容字面不变)
 - ✅ 步骤 615 字面 字节级保留(顺移到 L617,内容字面不变)
 - ✅ frontmatter L1-3 字节级保留
 - ✅ `## 工作流程` 小节既有内容 0 改
 - ✅ `## 不要做的事` 小节既有内容 0 改
- **关键词集校验**:20 关键词与详细设计 §5.1 字节级一致
- **交叉引用校验**:新子节引用"## 不要做的事 步骤 615" + "BUG-00005 修复详细设计 §5.1"(均为有效锚点)

## 7. 已知问题/未完成项

**无**(本任务为纯文档任务,所有变更均在本任务范围内闭环)。

## 8. 过程文档清单(由 code-it 内化,BUG-00004 新增)

### 8.1 工作流上下文

`decisions` 字典(由 code-it 步骤 8.7 物化):
```json
{
  "workLog": "生成",
  "compileAndRun": "不生成",
  "deviations": "生成",
  "testResults": "不生成",
  "unitTestResults": "不生成",
  "kanbanChangeLog": "生成",
  "processDocDecisions": "生成"
}
```

### 8.2 决策结果表

| 过程文档 | 决策 | 判定理由 |
| --- | --- | --- |
| `workLog.md` | 生成 | 任务实施日志是核心(沿用 BUG-00004 强制) |
| `compile-and-run.md` | 不生成 | 纯 Markdown 改造,无运行/启动/编译动作(沿用 `code-it/templates/RESULT.md` 模板 §"compile-and-run.md" 判定准则) |
| `deviations.md` | 生成 | 评审要查;内容为"无偏离" 1 行 |
| `test-results.md` | 不生成 | 测试状态 = `不适用`(纯文档任务) |
| `unit-test-results.md` | 不生成 | 项目不可测(无 `package.json` scripts.test)+ 任务类型=修改 + 纯 Markdown 改动 |
| 看板"变更记录" | 生成 | 本轮有追加(任务完成 + 缺陷状态推进) |
| `process-doc-decisions.md` | 生成 | 存在"不生成"决策(`compile-and-run.md` / `test-results.md` / `unit-test-results.md`) |

### 8.3 决策依据

引用 `code-it/SKILL.md` §"## 过程文档自适应判定"(line 101-138)与 §"## 步骤 8.7 — 过程文档自适应判定执行"(line 805+)的判定准则。

### 8.4 关联任务

- **前置任务**:无(本任务为 TASK-BUG-00005-00001,首条)
- **后置任务**:TASK-BUG-00005-00002(依赖本任务)→ TASK-BUG-00005-00003(依赖 T-1 + T-2)
- **取代任务**:无
- **关联 code-check**:待 3 任务全部完成后调 `/code-check BUG-00005`

## 9. 关联任务与提交

- **关联需求**:REQ-00033(`code-require` 不应分析或记录技术选型相关内容)
- **关联缺陷**:BUG-00005
- **关联编码计划**:`./assistants/V0.0.3/fix/BUG-00005/PLAN.md`
- **关联详细设计**:`./assistants/V0.0.3/fix/BUG-00005/RESULT.md` §4.1 + §5.1
- **提交哈希**:见 `work-log.md` 末尾 git commit 输出
- **本次提交范围**:`plugins/code-skills/skills/code-require/SKILL.md` 步骤 7A(L322-335 区域)

## 10. 变更记录

| 时间 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-06-23 | 实施完成 | TASK-BUG-00005-00001 完成,code-require 步骤 7A 添加"过滤技术选型类问题"子节(纯追加,既有 L322-333 字面 0 改,顺移下游章节) | wangmiao |
