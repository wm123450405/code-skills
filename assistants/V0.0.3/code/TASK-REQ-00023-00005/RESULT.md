# 改修总结 — TASK-REQ-00023-00005([修改] 高优先级缺陷段保留 + 边界 E-1 ~ E-10)

- 任务编码:TASK-REQ-00023-00005
- 标题:[修改] 高优先级缺陷段保留 + 边界 E-1 ~ E-10
- 类型:修改
- 提交:本任务执行时生成
- 触发/来源:详细设计

## 1. 改修内容
- 涉及文件:`plugins/code-skills/skills/code-dashboard/SKILL.md`
  - `§工作流程 步骤 4 段 3` — 子标题 "缺陷(高优先级)" → "高优先级缺陷(本需求 REQ-00023 改造,沿用既有)" + 标题子标题补"沿用既有"声明 + 屏显契约标注 ░ 退化
  - `§边界与异常` 末尾追加 — "本需求 REQ-00023 新增边界(沿用概要设计 §6)"小节,10 条 E-1 ~ E-10

## 2. 关键变更
- 段 3 高优先级缺陷**沿用既有**(FR-6 锁定):
  - P0 标记:█(U+2588 实心)/ P1 标记:▓(U+2593 半实心)/ 退化:░(U+2591 空心)
  - 不展示具体缺陷标题(避免屏显膨胀)
  - 不展示 P2 / P3 数量
  - AC-5.3:P0 / P1 均为 0 时仍保留骨架 `░ 0`
- §边界与异常 追加 10 条边界(沿用概要设计 §6):
  - E-1:某需求"概要设计"列异常 → 视为"待概要设计",屏显 `?`
  - E-2:`plan/<REQ>/RESULT.md` 缺失 → 视为"待详细设计",屏显 `?`
  - E-3:`plan/<REQ>/PLAN.md` 缺失 → 视为"待代码开发",屏显 `?`
  - E-4:任务"开发状态"字段为空 → 视为"待开始",屏显 `?`
  - E-5:评审发现汇总"必须改"未链接原任务 → 视为"待代码评审",屏显 `?`
  - E-6(本需求):`RESULT.md` 完全缺失 → 退出 + 引导调 `code-version`
  - E-7:同需求多状态 → 归入"待代码开发"(更早阶段优先)
  - E-8:5 类状态全部无触发 → 显示"无后续动作" 1 行
  - E-9:推荐命令目标文件不存在 → 不报错(只读契约),屏显 `?`
  - E-10:缺陷"修复方案"缺失 → 视为"待详细设计",屏显 `?`

## 3. 边界与异常
- L1 / L2 / L3 三层组织**不**变(沿用既有)
- 既有 E-1 ~ E-10(L1 启动 + L2 数据 + L3 兜底) **不**变
- 新增的 E-1 ~ E-10(本需求)与既有 E-1 ~ E-10 编号冲突 — 沿用既有编号;新增段单独小节"本需求 REQ-00023 新增边界",**不**修改既有 E-1 ~ E-10

## 4. 验证
- ✅ 不适用(纯文档/屏显类技能,无源码改动)
- 验证手段:人工在 REPL 中调 `/code-dashboard`,段 3 + §边界与异常 应符合沿用既有契约
