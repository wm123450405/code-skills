# 偏离记录 — TASK-BUG-00001-00002
版本:V0.0.3
时间:2026-06-07 17:35

## 偏离:**0 偏离**

本任务严格按 `fix/BUG-00001/PLAN.md §3.2` 任务详情实施,**0 偏离**:
- 目标文件:`plugins/code-skills/skills/code-design/SKILL.md`(一致)
- 锚点:`## 不要做的事` 段,新条目插入段首(一致)
- 文案:不修改 `plugins/code-skills/skills/*/SKILL.md` 任何文件(工程代码改动由 `code-it` 实施,本技能只写 `design/<REQ>/RESULT.md` 等工作空间文档)(一致,80 字符)
- 边界与异常:0 触发
- 验证手段:INV-11(grep)+ INV-16(git diff)全通过
- 回退方式:未触发(无失败)

## 关键决策

### D-1:文案微调("本技能" vs "本工作流")

- **决策**:文案沿用 T-1 的"本技能只写 `design/<REQ>/RESULT.md` 等工作空间文档"
- **理由**:4 个技能(T-1~T-4)的硬约束文案"主句"完全统一为"不修改 SKILL.md 任何文件",只在"只写 X 文档"部分按各技能工作流不同略调;Q-3 已锁定"不强制完全统一"
- **影响**:0(纯文案风格差异,语义与 T-1 一致)

### D-2:段首插入位置(沿用 T-1)

- **决策**:在 `## 不要做的事` 标题后立即插入(段首)
- **理由**:PLAN.md §3.2 明确"段首";新条目享有最高阅读优先级
- **影响**:0

## 残余风险
**0 残余风险**

## 备注
本任务为 BUG-00001 修复的第 2 步(T-2),与 T-1(`code-require`)1:1 对称。后续 T-3(`code-plan`)+ T-4(`code-fix`)实施流程相同。
