# 澄清记录 — REQ-00031

## 2026-06-12 15:13 — Q-1 关键澄清(本轮一次性)

### Q-1 — "不需要在任务阶段编写单元测试功能"的实现路径
- **选项**:
  - A. 规则修订 + 元技能改(改 `code-plan` / `code-it` / `code-unit` / `code-auto` 4 技能 + `templates/plan.md` 1 模板)
  - B. 仅修订 `code-plan` 任务粒度原则(不改其他)
  - C. 仅修订 `code-auto` 不再调 `code-unit`(不改其他)
- **用户回答**:A
- **影响**:4 个被改技能 + 1 个被改模板;INV-1~INV-9 字节级保留约束适用于本需求

## 2026-06-12 15:18 — Q-2 ~ Q-5 用户原文补充(本轮一次性)

### Q-2 — `/code-unit` 技能的作用
- **选项**:
  - A. 编写单元测试 + 执行测试(独立、可选)
  - B. 包含性能测试 / 集成测试
  - C. 仅执行既有测试
- **用户回答**:A
- **影响**:`code-unit/SKILL.md` 文档头 "## 目标" 小节**追加**"独立、可选"声明

### Q-3 — `/code-it` 技能的作用
- **选项**:
  - A. 编码 + 确保正常编译/运行
  - B. 编码 + 编译 + 单元测试
  - C. 编码 + 编译 + 集成测试
- **用户回答**:A
- **影响**:`code-it/SKILL.md` 文档头 "## 目标" 小节**追加**"不含单元测试"声明

### Q-4 — `code-plan` 任务规划范围
- **选项**:
  - A. 只规划编码部分(不含单元测试)
  - B. 规划编码 + 单元测试
  - C. 按项目类型自适应
- **用户回答**:A
- **影响**:`code-plan/SKILL.md` §步骤 10A "任务类型" 列表**移除** `测试` 类型;**追加**"不规划单元测试任务"小段

### Q-5 — 单元测试技能的调用方
- **选项**:
  - A. 项目/用户按需在 `code-it` 之后**自行**决定
  - B. 由 `code-plan` 任务清单强制包含
  - C. 由 `code-auto` 任务循环强制调用
- **用户回答**:A
- **影响**:`code-auto` 步骤 4.b 任务循环**永不再调** `code-unit`;`code-plan` 任务"测试状态"统一收窄为 `不适用`

**结论**:5 个 Q 全部澄清;无遗留未决项。
