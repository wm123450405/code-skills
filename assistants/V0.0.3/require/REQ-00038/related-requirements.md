# 关联需求 — REQ-00038

## REQ-00034(移除 /code-unit 技能,能力整合进 /code-it,所属版本 V0.0.3)

- **关联点**:
 - REQ-00034 FR-2 定义了"项目可测性守卫 7 项"(字节级沿用 `code-unit` 步骤 0a,工程根级)
 - REQ-00034 FR-3 定义了"按需写单测"3 类任务自动判定(工程根级,写单测到 CWD 下的项目测试目录)
 - 本需求 REQ-00038 在此基础上**细化**两者为"模块粒度"
- **影响**:
 - REQ-00034 落地后:`code-it` 步骤 8a / 8.5 是工程根级判定 → monorepo / 多语言工程下子模块的单测需求被忽略
 - 本需求落地后:`code-it` 步骤 8a 扩展为"模块级守卫 7 项"(`§FR-2`)+ 步骤 8.5 扩展为"按模块写到各自测试目录"(`§FR-3`)
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00034/RESULT.md` line 92-130(FR-2 / FR-3 字面定义)

## REQ-00031(`code-unit` 退场为可选,所属版本 V0.0.3)

- **关联点**:
 - REQ-00031 是 REQ-00034 的前置:已声明 `code-unit` 为"独立、可选"技能
 - REQ-00034 真正删除 `code-unit` 整合进 `code-it`
 - 本需求 REQ-00038 在 REQ-00034 整合基础上进一步细化
- **影响**:
 - 历史 V0.0.2 既有 `code-unit` 行为已**完全被 REQ-00034 覆盖**
 - 本需求不涉及 `code-unit` 退场逻辑(已由 REQ-00034 落地)
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00031/RESULT.md` 摘要(仅关联,未读全文)

## 关联需求所在版本

- 全部位于 V0.0.3
- 不涉及跨版本关联(沿用既有 V0.0.3 内部版本空间)