# 澄清记录 — REQ-00028

更新时间:2026-06-10

## 2026-06-10
- **问题 1**:code-answer 扫描需求清单时,是否覆盖所有历史版本(包括基线 V0.0.0 + 全部 V0.0.x)?
- **选项**:
  - A. 全版本(推荐)
  - B. 当前版本 + 上一版本
  - C. 仅当前激活版本
- **用户回答**:A(全版本)
- **影响**:本技能扫描路径固定为 `./assistants/*/require/{EXISTING-*,REQ-*}/RESULT.md`,**不**做版本过滤
- **影响**:报告的"参考引用"区段会标注需求所在版本,让用户区分基线功能 vs 后续演进

## 2026-06-10
- **问题 2**:扫描源代码时,如何定位相关代码?
- **选项**:
  - A. 先询问项目类型(推荐)
  - B. Glob 常见源码目录(自动)
  - C. 仅返回需求信息,不扫描代码
- **用户回答**:B(Glob 常见源码目录,自动)
- **影响**:内置常见源码目录列表:`src/` `lib/` `app/` `pkg/` `cmd/` `internal/` `packages/` `modules/` `core/`,命中任一即用
- **影响**:扩展策略 — 第一次扫描命中某目录后,**记住**该目录为该项目"代码根",后续优先在已知根下 Grep 关键字(避免重复全量 Glob)
- **影响**:本仓库(代码技能仓库)本身**不含源码**,Glob 全部不命中,自动走"无源码可扫"分支,这是预期行为

## 2026-06-10
- **问题 3**:code-answer 的最终报告,在屏显之外是否同时持久化到某处?
- **选项**:
  - A. 仅屏显输出(推荐)
  - B. 屏显 + 项目根目录归档
  - C. 屏显 + 当前会话持久化
- **用户回答**:A(仅屏显输出)
- **影响**:工具集严格 `Read` / `Glob` / `Grep`,**不**调用 `Write` / `Edit`
- **影响**:完全对齐用户 args「过程不产生任何结果文件、不产生任何过程文件」字面要求
- **影响**:无副作用,可随时调用,多次执行幂等
