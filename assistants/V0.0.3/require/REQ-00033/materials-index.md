# 材料登记 — REQ-00033

更新时间:2026-06-15 11:10
版本:V0.0.3
需求编码:REQ-00033

## 1. 项目级规范(本需求相关)

(本节沿用 code-require 既有约束,无新增)

| 规范文件 | 类别 | 关键约束摘要(本需求相关) |
| --- | --- | --- |
| skill-conventions.md | 技能 | SKILL.md frontmatter L1-3 字节级保留(INV-1) |
| module-conventions.md | 模块 | 资源文件放 templates/ 子目录 |
| commit-conventions.md | 提交 | `chore(<skill>):` 前缀(INV-3) |
| dashboard-conventions.md | 看板 | 看板字段三方同步(本需求触发 1 次) |
| encoding-conventions.md | 编码 | 需求编号生成端 5 位纯数字;接收端可放宽(本需求沿用) |

## 2. 上游需求

- 来源:用户口头/文本输入(2026-06-15 11:00,经 1 轮 AskUserQuestion 澄清后定稿)
- 形式:1 段 ~80 字的技能范围说明
- 提取:无新材料,需求直接来自用户口头/文本输入
- 用户原始输入(verbatim):
  > 需求澄清技能 `/code-require` 时不涉及技术选型确定,只涉及功能点确定;技术选型确定放在 `/code-design` 技能阶段,没有必要进行技术选型的需求无需分析技术选型选项。

## 3. 项目现状(本次扫描)

### 3.1 code-require 现有职责边界

通读 `plugins/code-skills/skills/code-require/SKILL.md` 全文,识别出现状:

- **范围声明**:§"目标"段明确 `code-require` 产出"详尽、可执行、可追溯"的需求提示词文档,作为后续 `code-design` / `code-plan` / `code-it` / `code-unit` / `code-check` 的共同基线
- **功能点来源**:§"步骤 5A 首次分析"列出的提取对象 = 功能点(FR)/ 约束(NFR)/ 页面/ 交互/ 数据字段/ 边界条件
- **技术选型字面**:**0 处**出现(经 Grep 校验 `技术选型|技术栈|技术方案|选型` 在该文件中均无命中)
- **职责隐含**:由"产出物 = 需求提示词文档"推导,该技能**不**回答"用什么技术实现",只回答"要实现什么功能"

### 3.2 code-design 现有职责边界(技术选型的归属)

通读 `plugins/code-skills/skills/code-design/SKILL.md` 关键位置,识别出现状:

- **§"目标"段**:`code-require` 的需求基础上,结合**当前项目实际状况**(语言/框架/既有模块/既有接口)给出架构方案
- **§"工作目录约定"段第 233 行附近**:模板字段含"功能架构规范:整体架构风格(分层/微服务/事件驱动/六边形等)、组件划分原则、**技术选型偏好**"
- **§"工作目录约定"段第 262 行附近**:扫描需求中明确指定的**技术选型/接口风格/数据模型**
- **§"工作目录约定"段第 310 行附近**:**数据存储选型偏好**
- **职责显式确认**:`code-design` 才涉及"跨模块的方案选型"

### 3.3 现状结论(本需求基线)

| 技能 | 是否涉及技术选型 | 备注 |
| --- | --- | --- |
| `code-require` | **不**涉及(0 命中) | 只提取功能点(FR / NFR / 页面 / 交互 / 数据 / 边界) |
| `code-design` | 涉及(模板含"技术选型偏好"等 3+ 字段) | 跨模块方案选型 + 数据存储选型 + 接口风格 |

**结论**:用户的描述**与现状一致**,本需求属于"**明文契约化**"型:把隐含的职责边界落到 `code-require/SKILL.md` 的硬约束文本上,避免后续误读/越界。

### 3.4 待改造的技能

| 技能 | 路径 | 当前状态 | 改造范围 |
| --- | --- | --- | --- |
| `code-require` | `plugins/code-skills/skills/code-require/SKILL.md` | **不含**"不涉及技术选型"硬约束 | §"目标"段或 §"工作流程"步 5A / 步 8A 段内追加 1 段"## 不涉及"或等价硬约束 |

### 3.5 相关既有需求(同类型:技能职责边界显式化)

| 需求 | 版本 | 关联点 | 对本需求的影响 |
| --- | --- | --- | --- |
| BUG-00001 | V0.0.3 | code-require / code-design / code-plan / code-fix 各自加"不修改 SKILL.md"硬约束(类似:加边界硬约束) | 借鉴"硬约束加在 §不要做的事 小节"的范式 |
| REQ-00026 | V0.0.3 | SKILL.md 描述通用化扫除(10 SKILL.md + 3 templates + 1 INIT-REPORT) | 沿用"最小化变更"原则 |
| REQ-00030 | V0.0.3 | 元技能改 + 12 维度评审 + INV 字节级保留 | INV 字节级保留约束沿用 |
| REQ-00032 | V0.0.3 | code-require 步骤 10A/10B 末尾追加"下一步建议"段 | 同属 code-require 增量改造(参考追加位置 + 保留既有段) |

## 4. 用户原始输入(verbatim)

> 需求澄清技能 `/code-require` 时不涉及技术选型确定,只涉及功能点确定;技术选型确定放在 `/code-design` 技能阶段,没有必要进行技术选型的需求无需分析技术选型选项。
