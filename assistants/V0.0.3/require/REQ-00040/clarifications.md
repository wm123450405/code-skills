# 澄清记录 — REQ-00040

## 2026-06-25

### Q-1:如何判定"项目允许启动程序"
- **问题**:code-fix 启动复现的前置条件,需要在登记时显式确认;若探测机制选择不当,会反复弹窗或漏启动
- **选项**:
  - A. 探测 build/start/test 命令可用性(自动判定,失败再降级)(推荐)
  - B. 用户显式参数控制(--reproduce / --no-reproduce)
  - C. 复用既有 step 4 AskUserQuestion 多选一项
- **用户回答**:A(推荐)
- **影响**:FR-2 启动能力探测 / NFR-4 失败降级 / 步骤 0 末尾新增子节

### Q-2:复现过程中的日志/截图/交互数据存放于何处
- **问题**:产物存放方式决定模板的扩展点
- **选项**:
  - A. 产物放 fix/<BUG-NNN>/ 子目录(推荐)
  - B. 全部 inline 进 RESULT.md
  - C. 不新增子目录,沿用现有 bug.md 模板
- **用户回答**:A(推荐)
- **影响**:FR-3 产物子目录设计 / FR-4 模板扩展 / 步骤 6 末尾动作

### Q-3:复现动作触发哪个状态
- **问题**:code-fix 既定状态模型是"纯登记型",登记时主动推进只会到 待处理/报告/调查 中;复现动作与状态推进的关系
- **选项**:
  - A. 状态初始值仍是"报告",复现产物作为附件(推荐)
  - B. 状态初始值仍是"待处理",复现产物作为附件
  - C. 复现成功 → 初始状态 = "调查中"
- **用户回答**:A(推荐)
- **影响**:FR-5 状态语义 / NFR-2 与 REQ-00037 协同 / 步骤 6 末尾动作

### Q-4:启动复现失败如何降级
- **问题**:启动失败 / 程序崩溃 / 复现未触发 bug 时,code-fix 应如何处理
- **选项**:
  - A. 降级为文本复现(记录原因,继续登记)(推荐)
  - B. 弹窗让用户决定如何处理
  - C. 硬失败:启动失败 = code-fix 中止
- **用户回答**:A(推荐)
- **影响**:FR-2 失败降级 / NFR-4 失败降级 / E-1 / E-2 边界
