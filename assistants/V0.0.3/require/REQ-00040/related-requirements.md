# 关联需求 — REQ-00040

## REQ-00037(V0.0.3) 优化 /code-fix 技能及整个缺陷修复流程的状态推进
- **关联点**:同源改造对象(code-fix SKILL.md);REQ-00037 把状态推进收敛为"前 5 段 + 阻塞" + `待处理` 初始态
- **影响**:
  - 本需求**不**与 REQ-00037 冲突(本需求在 REQ-00037 **之后**的登记阶段追加"复现证据"机制,**不**改 REQ-00037 已确定的初始态)
  - 协同:RE界37 决定"code-fix 登记时初始状态 = `待处理`",本需求在 REQ-00037 之上叠加"复现产物子目录 + 复现日志作为登记附件"机制
  - 关键约束:本需求**不**改"修复日志" / "变更记录" 既有格式,只追加"复现产物登记" 区段

## REQ-00027(V0.0.3) `code-fix` 改为纯登记型 + 下游接力
- **关联点**:code-fix 当前"纯登记型"边界(本需求严守)
- **影响**:
  - 本需求**不**改 code-fix 纯登记型边界
  - 复现产物 = 登记时的"证据附件",不触发 code-plan / code-it / code-check 任何状态推进

## REQ-00036(V0.0.3) 清理 SKILL.md + templates/ 中的开发痕迹
- **关联点**:`skill-conventions.md §规则 2`(本仓库 13 份规范之一)
- **影响**:
  - 本需求**不**在 code-fix SKILL.md / bug.md 模板中写"本需求 REQ-00040 新增" 等开发痕迹字面
  - 沿用"无痕改造":新追加的"## 复现产物登记" 区段不含回溯注释;步骤 0 末尾的"启动能力探测" 子节不写"本轮新增" 等字面

## REQ-00033(V0.0.3) `code-require` 不做技术选型
- **关联点**:不涉及(本需求是 code-fix,非 code-require)
- **影响**:
  - **不**触发技术选型类问路
  - 复现动作可能涉及"用什么工具启动"(npm start / python main.py / docker compose up),这是 code-fix 的**操作步骤**而非"技术选型",**不**触发 §615 硬约束
  - 启动命令探测本身**不**触发 `AskUserQuestion`(沿用 NFR-3)

## BUG-00001 / 02 / 03 / 04 / 05(V0.0.3) 既有 5 个缺陷
- **关联点**:本需求改造的 fix/<BUG-NNN>/ 子目录结构
- **影响**:
  - 历史 BUG 的 `fix/<BUG-NNN>/RESULT.md` **不**追加"## 复现产物登记" 区段(沿用 NFR-5,字节级保留)
  - 未来新登记的 BUG 才会有该区段

## REQ-00022(V0.0.3) `code-review` → `code-check` 重命名
- **关联点**:code-fix 下游衔接
- **影响**:本需求**不**涉及 code-check(本需求在 code-fix 登记阶段)
