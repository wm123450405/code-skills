# 分析笔记 — REQ-00024
更新时间:2026-06-07

## 演化路径

```
REQ-00005(code-auto 初版)         → REQ-00007(模式 B from 关键字 v2)    → REQ-00024(本需求,移除 from,改用路径感知)
   2026-06-04                           2026-06-05                              2026-06-07
```

## 关键决策

### D-1:用路径感知替代正则匹配

- **决策**:模式识别从"正则匹配 `^from REQ-\d{5}$`"改为"`Bash: test -d <path>`"
- **理由**:
  - 文件系统是"单一事实来源" — 不依赖关键字字面值
  - 输入即目录名(最常见场景),无需额外记忆
  - 4 种模式自然分解为 4 个 `test -d` 命令
- **影响**:0

### D-2:默认输入 = 需求编号

- **决策**:用户输入首先按"需求编号"判定(`require/<input>/` 存在?)
- **理由**:
  - 需求续跑是最常见场景(沿用 REQ-00007 模式 B)
  - 缺陷续跑是次常见场景(走 `fix/<input>/`)
  - 全流程新需求是兜底场景(2 个目录都不存在)
- **影响**:0

### D-3:模式 A 拆为 2 子分支(req-skip-require / req-run-require)

- **决策**:`require/<input>/` 存在后,再判 `RESULT.md` 是否存在,二选一
- **理由**:
  - 现有 `from REQ-NNNNN` 续跑语义:跳过 code-require,直接进入概要设计
  - 用户新提出的 2 子分支:
    - `RESULT.md` 存在 → 同"模式 B 续跑"
    - `RESULT.md` 不存在 → 进入需求设计(走全流程)
  - 2 子分支的区分**不**引入新关键字,只通过文件存在性区分
- **影响**:0

### D-4:屏显增加 3 行"路径感知判定"前缀

- **决策**:步骤 1 屏显前 3 行明示判定依据
- **理由**:
  - 沿用既有"步骤 1/6:code-require"前缀格式
  - 路径感知是"非显然的"判定(用户看不到),需明示
  - 透明性 + 可追溯
- **影响**:屏显 0 副作用,仅屏幕

### D-5:FR-9 明确"破坏性变更"语义

- **决策**:`/code-auto from REQ-NNNNN` 调用方式不再被特殊处理,整串视为需求内容
- **理由**:
  - "移除 from 关键字"是用户明确要求
  - 既有调用方式改为 `/code-auto REQ-NNNNN` 即可(去掉 `from` 前缀)
  - 0 迁移路径(沿用既有"破坏性变更无兜底"原则)
- **影响**:
  - 既有"模式 B 续跑"4 个用户需调整(本仓库 0 文档调用过该模式)
  - 0 数据损失(`RESULT.md` 仍可直接续跑)

### D-6:沿用既有"步骤 0a git pull" + 步骤 0 + 步骤 1 顺序

- **决策**:`code-auto/SKILL.md` 步骤 0a → 0 → 1 的结构不变;只改步骤 1 的内部逻辑
- **理由**:
  - 0 结构性变更,降低实现风险
  - 6 步状态机保持稳定
  - 屏显格式微调(FR-6),整体外观不变
- **影响**:0

## 待澄清(沿用 RESULT.md §12)

- Q-1:`code-fix` 的 `from` 关键字是否同步移除 → **不**,本需求边界限定
- Q-2:任务编码输入 → `code-auto` 不接受,**不**改
- Q-3:`code-publish` 同步 → 0 关联
- Q-4:`auto-report.md` 模式名 → NFR-2 强约束不变

## 残余风险

- **R-1**:`from` 关键字移除后,既有调用方(若有)需调整 — 沿用既有"破坏性变更无兜底"
- **R-2**:路径感知与"用户原意"可能的偏差(用户输入"REQ-99999"但实际是 1 个目录名而非需求编号) → **0 修复**(沿用既有"用户有最终决定权")

## 命令行参数

- `--result`:无
- `--plan`:无
