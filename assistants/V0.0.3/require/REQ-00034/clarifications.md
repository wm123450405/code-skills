# 澄清记录 — REQ-00034

版本:V0.0.3
需求编码:REQ-00034
时间:2026-06-15 12:00

## 2026-06-15 12:00

### Q-1 按 code-require 流程应先创建新 REQ 登记;请确认本需求的登记方式

- **选项**:
  - A. 登记成新需求(创建 REQ-00034)
  - B. 是 REQ-00031 的补充(创 REQ-00034)
  - C. 不登记,直接走主流程
- **用户回答**:A. 登记成新需求(创建 REQ-00034)
- **影响**:分配 REQ-00034;走标准 code-require 流程(本文件即产物)
- **备注**:本需求是 V0.0.3 看板里"实际删除 code-unit"的最终落地,与 REQ-00031 的"声明性分离"是姐妹需求

### Q-2 本轮只到需求完成,还是同时推进主流程

- **选项**:
  - A. 仅需求(推荐)
  - B. 用 /code-auto 一键推进
- **用户回答**:A. 仅需求(推荐)
- **影响**:本轮只生成 RESULT.md;后续 `code-design` / `code-plan` / `code-it` / `code-check` 由用户手动调(或后续 `/code-auto`)
- **备注**:本需求是 5 SKILL.md + 2 JSON + 4 README + 7 项目级规范 + 11 技能描述段 + 1 目录删除,共 ~30 个文件的元技能改,全自动跑风险高

### Q-3 `code-unit` 退场后,项目可测性守卫(7 项检查)由谁接管?

- **选项**:
  - A. `code-it` 接管守卫逻辑(7 项检查同款)
  - B. `code-it` 不接管守卫,直接按"项目是否有单测文件"判定
  - C. `code-it` 走"AI 自主判定"路径
- **用户答复沿用原始输入**:"功能代码编写完成**根据需要**直接编写单元测试或**针对不适用的工程跳过**单元测试编写"
- **影响**:
  - 选 A:`code-it` 接管守卫 7 项检查(完整保留 `code-unit` 步骤 0a 行为),仍走"项目可测 → 写单测;不可测 → 跳过"路径
  - 选 B:简化;但需要 `code-it` 在 `code-it` 步骤 8 末尾"识别项目是否有测试目录"→ 不可测就跳
  - 选 C:AI 自由裁量;不可控
  - 隐含答复:**A**(`code-it` 接管守卫,字节级保留原 `code-unit` 步骤 0a 行为)
- **影响**:`code-it/SKILL.md` 步骤 9 前**新增**守卫步骤(沿用原 `code-unit` 步骤 0a 全部 7 项检查)
- **备注**:本需求**保留** 7 项守卫,与原 `code-unit` 步骤 0a 字节级一致

### Q-4 `code-it` 写单测的"按需"如何判定?

- **选项**:
  - A. 沿用 `code-unit` 原步骤 4 "测试状态"判定(4 枚举 → 收窄为 2 后由 `code-it` 步骤 8.5 自查)
  - B. `code-it` 步骤 8.5 末尾**主动询问**用户"本任务是否需要单测?"
  - C. `code-it` 步骤 8.5 末尾按"项目可测性 + 任务性质"自动判定
- **用户答复沿用原始输入**:"**根据需要**直接编写单元测试"
- **影响**:
  - 选 A:沿用 `code-unit` 步骤 4 行为(任务"测试状态"在 V0.0.3 已统一为 `不适用`,所以**永远**走"不写"路径 — 不合理)
  - 选 B:主动询问用户;但用户在原始输入已说"根据需要"——隐含**不主动询问**;`code-it` 应自行判定
  - 选 C:自动判定;但需要 `code-it` 有判据
  - 隐含答复:**C**(自动判定);`code-it` 步骤 8.5 新增"按需写单测"判定逻辑:
    - 项目可测(7 项守卫命中) **且** 任务涉及"纯函数 / 工具方法 / 业务逻辑"→ **写**单测
    - 项目可测 **且** 任务涉及"配置 / 类型定义 / 文档"→ **不**写单测
    - 项目不可测(7 项守卫不命中)→ **不**写单测(沿用原 `code-unit` E-2 行为)
  - 详见 RESULT.md FR-2 / FR-3 锁定
- **影响**:`code-it/SKILL.md` 步骤 9 前**新增**守卫步骤 + 步骤 9 后**新增**"按需写单测"步骤

### Q-5 本需求是否与 BUG-00001("不修改 SKILL.md"硬约束)冲突?

- **用户答复沿用原始输入**:"移除技能 `/code-unit`,将技能能力整合进 `/code-it` 技能中"
- **影响**:
  - BUG-00001 实际指"`code-require` / `code-design` / `code-plan` / `code-fix` / `code-it` / `code-unit` 不得修改工程代码"
  - 本需求**不**修改工程代码(本仓库是元技能仓库,无业务代码)
  - 本需求**修改** 5 个 SKILL.md 文本(本就是元技能仓库的核心改动对象)
  - **不冲突**;BUG-00001 的"不修改 SKILL.md"指"不要去修无授权的 SKILL.md",本需求经用户授权
- **影响**:RESULT.md Q-5 锁定"不冲突 BUG-00001"

### Q-6 历史 `code-unit` 产出物(`test/<TASK-...>/RESULT.md` + `code/<TASK-...>/test-results.md`)如何处理?

- **选项**:
  - A. 字节级保留(不追溯重写)
  - B. 迁移到 `code/<TASK-...>/test-results.md`(`code-unit` 退场后无 `test/` 目录)
  - C. 删除
- **用户答复沿用 REQ-00031 NFR-2 既有原则**:"既有 11 个 REQ 的 `PLAN.md` **不**重写"
- **影响**:
  - 选 A:历史文件保留;但 `code-unit` 退场后,新增任务的 `code-it` 不会写 `test/<TASK-...>/RESULT.md`,**只**会写 `code/<TASK-...>/test-results.md`(由 `code-it` 接管)
  - 选 B:迁移;但破坏 NFR-2 字节级保留约束
  - 选 C:删除;破坏历史可追溯
  - 隐含答复:**A**(字节级保留)
- **影响**:本需求**不**动 V0.0.2 / V0.0.3 既有 `test/` 目录 + `code/<TASK-...>/test-results.md`;新增任务的 `code-it` 产出物路径**仍**是 `code/<TASK-...>/test-results.md`(沿用 REQ-00031 既有路径)

## 总结

- **登记**:REQ-00034 全新
- **范围**:本轮仅到需求完成
- **守卫**:A 接管(字节级保留原 `code-unit` 步骤 0a 7 项检查)
- **按需写单测**:C 自动判定(项目可测 + 任务性质 → 写;否则不写)
- **BUG-00001**:不冲突
- **历史档案**:A 字节级保留
