# 关联需求 — REQ-00034

版本:V0.0.3
需求编码:REQ-00034
更新时间:2026-06-15 12:00

## REQ-00031 (V0.0.3):优化 /code-plan 任务粒度(内化编译/运行,外移单元测试)

- **关联点**:**强关联**;REQ-00031 已将 `code-unit` 声明为"独立、可选",但**没**真正删除。本需求是 REQ-00031 的"实际删除"补完
- **影响**:
  - 本需求复用 REQ-00031 落地的 5 个 SKILL.md / 1 模板的"任务粒度"基础
  - 本需求不重做 REQ-00031 的 5 任务;沿用其"测试状态 = 不适用"语义,但**反向**:`code-it` **重新接管** `code-unit` 的"编写单元测试"职责
  - 本需求**保留** REQ-00031 落地的 5 任务提交;**不**回滚其字节级变更
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00031/RESULT.md` §1 / §2 / §4 FR-4 / §13
- **关联强度**:**前置依赖**

## REQ-00009 (V0.0.2):`code-unit` 守卫"项目可测性"(7 项检查)

- **关联点**:**强关联**;`code-unit` 的步骤 0a 守卫(7 项构建/测试文件检查)随 `code-unit` 退场
- **影响**:
  - 候选 A:`code-it` **接管**该守卫逻辑(在编码前先做"项目可测性"检查,可测则按需写单测,不可测则跳过)
  - 候选 B:`code-it` **不接管**守卫逻辑,直接按"项目是否有单测文件"判定(简化)
  - 候选 C:`code-it` 走"AI 自主判定"路径,用户口头说"本项目需单测" → 写;否则跳
  - 见 RESULT.md FR-2 / Q-2 锁定 **候选 A**(接管守卫,与原 `code-unit` 步骤 0a 行为字节级一致)
- **来源**:扫描 `plugins/code-skills/skills/code-unit/SKILL.md` §步骤 0a.1 (L177-189)
- **关联强度**:**继承**

## BUG-00001 (V0.0.3):5 技能加"不修改 SKILL.md"硬约束

- **关联点**:**冲突**;BUG-00001 锁定"code-require / code-design / code-plan / code-fix / code-it / code-unit 不得修改 SKILL.md 以外的生产代码"
- **影响**:
  - 本需求**不突破** BUG-00001:`code-require` / `code-design` / `code-plan` / `code-fix` 仍不得修改工程代码
  - 本需求**突破** BUG-00001 的字面:5 个 SKILL.md 改造(`code-it` / `code-plan` / `code-auto` / `code-check` / `code-unit` 删除)涉及"修改 SKILL.md 文本"
  - 但 BUG-00001 实际指"不修改工程代码(SKILL.md 之外的生产代码)",本需求不涉及工程代码改动
  - 需在 RESULT.md Q-3 锁定"本需求不冲突 BUG-00001"
- **来源**:扫描 `./assistants/V0.0.3/fix/BUG-00001/RESULT.md` + 5 SKILL.md §"不要做的事" 段
- **关联强度**:**兼容性**

## REQ-00026 (V0.0.3):SKILL.md 描述通用化扫除

- **关联点**:**中等**;10 SKILL.md 描述段去专属化(占位符 `<本仓库>` + 概述段声明)
- **影响**:
  - 本需求**沿用** REQ-00026 的"最小化变更"原则
  - 本需求**复用** REQ-00026 落地的 10 SKILL.md frontmatter 字节级保留约束
  - 本需求**不**重做 REQ-00026 的 5 任务
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00026/RESULT.md` §5 NFR-6
- **关联强度**:**约束沿用**

## REQ-00030 (V0.0.3):元技能改 + 12 维度评审 + INV 字节级保留

- **关联点**:**强**;本需求是元技能改的另一类(技能合并)
- **影响**:沿用 INV 字节级保留约束(INV-1 ~ INV-9);本需求不新增 INV
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00030/RESULT.md` §5 NFR-1
- **关联强度**:**约束沿用**

## REQ-00032 (V0.0.3):`code-require` 屏显契约

- **关联点**:**弱**;本需求不涉及 `code-require` 屏显
- **影响**:无直接影响
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00032/RESULT.md`
- **关联强度**:**无影响**

## REQ-00033 (V0.0.3):`code-require` 不涉及技术选型

- **关联点**:**无**;本需求不涉及技术选型
- **影响**:无
- **来源**:扫描 `./assistants/V0.0.3/require/REQ-00033/RESULT.md`
- **关联强度**:**无影响**

## (本需求不跨版本扫描)

按 code-require 步骤 6A 约定,跨版本扫描**可选**。本需求范围窄(只在 V0.0.3 当前激活版本内做技能合并),不跨版本扫描以避免噪音。

## 关联点汇总

| 维度 | 关联点 |
| --- | --- |
| 强依赖(必须前置) | REQ-00031(任务粒度收窄基础) |
| 强继承(行为接管) | REQ-00009(`code-unit` 守卫逻辑) |
| 兼容性(无冲突) | BUG-00001("不修改 SKILL.md"硬约束) |
| 约束沿用 | REQ-00026(最小化变更) / REQ-00030(INV 字节级保留) |
| 无影响 | REQ-00032 / REQ-00033 |
