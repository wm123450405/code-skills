# 分析笔记 — REQ-00020

## 当前理解的疑点(已解决)

1. **Q-1 大需求 `code-plan` 步骤 0b 问 8 问是否过度?**
   - 解决:大需求"功能性 / 扩展性 / 健壮性 / 可维护性 / 封装性 / 可复用性 / 可读性"7 维度 + 整体 1 问 = 8 问;用户期望显式选择,避免 AI 自由裁量。沿用 REQ-00011 FR-6.3 "大需求可对不同细节功能分开提问" 灵活性。

2. **Q-2 默认值如何选?**
   - 解决:`code-auto` 上下文(E-4)默认"封装性=高 / 可复用性=高 / 可读性=高" + 其他维度=中 + 整体=--balanced(沿用 REQ-00011);用户手动调时由用户主动选择。

3. **Q-3 本仓库 `code-skills` 是 Markdown 自然语言,3 新维度如何取值?**
   - 解决:沿用 `code-design` 步骤 0b 的"`--minimal` 适合低复杂度需求"启发;本仓库元技能描述场景默认封装性 / 可复用性 / 可读性 = `不适用`(由项目级规范 `code-rule` 决定)。

## 候选方案权衡(已锁定)

### 方案 A:`code-design` 步骤 0b 完全删除(本需求未采纳)
- 优点:进一步减 token
- 缺点:用户希望"概设阶段也有功能性确认"(沿用 REQ-00011 FR-1)
- 决策:不采纳,保留 1 维度功能性

### 方案 B:`code-plan` 步骤 0b 扩展为 7 维度(本需求已采纳)
- 优点:封装性 / 可复用性 / 可读性是用户原文的核心诉求
- 缺点:`code-plan` SKILL.md 略增(+6 行,因屏显模板扩 + 任务粒度表 +3 行)
- 决策:采纳(用户显式要求"增加 3 维度")

### 方案 C:归并降幅 ≥ 10%(本需求部分达成)
- 实际:`code-it` 单文件降 4.4%,`code-plan` 略增(+0.6%),合计 -1.9%
- 决策:部分达成,权衡说明写入 §8.4 与 NFR-5.1(用户已确认)

## 临时假设(已采纳为默认值)
- 本仓库 `code-skills` 是 Markdown 自然语言,3 新维度默认 `不适用`
- `code-auto` 上下文默认全 3 维度=高(推荐值,用户可调)
- 大需求 `code-plan` 问 8 问(整体 + 7 维度)耗时 < 5 分钟(沿用 REQ-00011 NFR-8)

## 下一轮要深挖的方向
- 若用户要求"进一步降 token",可考虑:
  - 把 §"## 过程文档格式"段(7 份过程文档的格式说明,约 150 行)抽到独立 `templates/` 目录(从 `code-plan/SKILL.md` 中移出)
  - 把 §"## 缺陷分支"段(约 200 行)拆到独立 `code-plan-fix-branch.md`(子文档)
  - 把 §"## 衔接" + §"## 不要做的事"段精简为 1 张表
- 但本需求**不**做这些进一步拆分(避免引入新文件 / 触发 `module-conventions §规则 1`)
