# 风险分析 — BUG-00004

更新时间:2026-06-22 20:30
版本:V0.0.3

## 异常处理

| 异常路径 | 描述 | 检测手段 | 处理策略 | 监控指标 | 对应任务 |
| --- | --- | --- | --- | --- | --- |
| 1. `decisions` 字段缺失(防御性) | 步骤 8.7 物化失败 / 步骤 9/10/11 引用时 `decisions` 不存在 | `decisions` 变量检测 | 视为"按原行为执行"(NFR-4 幂等);不阻断;屏显 `⚠` | 屏显警告 | T-001 |
| 2. `process-doc-decisions.md` 写入失败 | `Write` 工具失败(权限/磁盘/路径错) | `Write` 工具退出码 | 屏显 `⚠`,不阻断(沿用 E-1) | 屏显警告 | T-001 |
| 3. 任务类型字段缺失 | `PLAN.md` 任务总览"类型"列无值 | `taskType` 检测 | 退化:按 `修改` 判定 | 屏显警告 | T-001 |
| 4. `changedFiles` 为空 | 步骤 8 实施开发**未**改任何文件 | 步骤 8 末尾汇总 | 退化:按"非纯文档"判定 | 屏显警告 | T-001 |
| 5. 步骤 8a 守卫未执行(缺陷分支) | 缺陷分支不触达步骤 8a,`testable` 未定义 | `testable` 检测 | 退化:`testable = False` → `unitTestResults = '不生成'` | 屏显警告 | T-001 |
| 6. 步骤 9/10/11 既有逻辑被误改 | 守卫改造时不小心删了既有逻辑 | `grep` 静态校验 | 步骤 12 错误修复循环 + 任务回退(`git revert`) | 静态校验失败 → 任务重做 | T-001 |
| 7. 模板改造触发 `dashboard-conventions §规则 1` 三同步 | 误改 `templates/version-RESULT.md` 或 `CLAUDE.md` | 文件变更检查 | 严格限定在 `code-it/templates/RESULT.md` | 文件 diff | T-002 |
| 8. 端到端验证重跑失败 | 任务状态非"已完成" / 路径冲突 | `code-it` 步骤 7 错误处理 | 退化:`AskUserQuestion` 询问用户 | 屏显警告 | T-003 |

## 安全边界

- **鉴权要求**:N/A(本需求改 SKILL.md,无鉴权)
- **输入校验**:N/A
- **敏感数据处理**:N/A
- **审计日志**:N/A
- **依据规范**:`skill-conventions §规则 2`(不含 6 类开发痕迹,本需求严格遵守)

## 性能与资源

- **关键路径耗时目标**:
 - 步骤 8.7 `decisions` 物化:< 0.1 秒(纯 O(1) 判定,无 IO)
 - 步骤 9/10/11 守卫判定:< 0.01 秒(单次 if-else)
 - `process-doc-decisions.md` 写入(若触发):< 0.5 秒(典型 1 KB 文件)
- **并发上限**:N/A
- **资源限制**:N/A
- **缓存策略**:N/A
- **批量/异步**:N/A
- **降级策略**:N/A
- **总耗时**:**< 1 秒**(NFR-8 类约束)

## 回退策略

| 触发条件 | 步骤 | 验证 |
| --- | --- | --- |
| 步骤 8.7 物化 `decisions` 失败 | 任务整体 `git revert` 单次 commit;恢复既有行为 | 重跑 TASK-REQ-00039-00003,观察既有 5 个过程文档仍生成 |
| 步骤 9/10/11 守卫误改既有逻辑 | 任务 T-001 `git revert` 单次 commit;恢复既有逻辑 | 静态校验:`grep` 验证既有 3 段(检测/执行/记录)字节级保留 |
| 步骤 13 / 16 改造触发 `dashboard-conventions §规则 1` | 任务 T-002 `git revert` 单次 commit;恢复模板 | 静态校验:`grep` 验证既有 9 段(## 1 ~ ## 11)字节级保留 |
| 端到端验证失败 | 任务 T-003 `git revert` 单次 commit(重跑产生的所有变更) | 重跑 TASK-REQ-00039-00003 2 次,观察一致 |

## 测试要点

- **单元测试**:N/A(本仓库无单元测试,沿用 `code-it` 步骤 8a 守卫)
- **集成测试**:N/A
- **端到端测试**(端到端降级为静态校验):
 1. **场景 1(纯 Markdown 改造)**:
 - 重跑 TASK-REQ-00039-00003,观察 7 个观察点(见 RESULT.md §12)
 2. **场景 2(真实代码类任务对照,若有)**:
 - 跑 1 个真实代码类任务,观察 3 个观察点
 3. **场景 3(纯文档任务,可选)**:
 - 跑 1 个任务类型=文档的任务(例如 V0.0.3 之前的 TASK-REQ-00028-00001),观察 3 个观察点
- **性能/压力测试**:N/A
- **安全测试**:N/A
- **回归测试**:6 个技能(`code-require` / `code-design` / `code-check` / `code-plan` / `code-fix` / `code-init` / `code-rule`)的过程文档行为**不**受影响(本需求仅改 `code-it`)
- **对应任务**:T-001, T-002, T-003
