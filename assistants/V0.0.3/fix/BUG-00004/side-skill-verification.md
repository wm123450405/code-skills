# 其他 6 个技能旁路验证报告 — BUG-00004

- 缺陷编号:BUG-00004
- 所属版本:V0.0.3
- 任务编号:TASK-BUG-00004-00004
- 创建:2026-06-22 22:10
- 责任人:wangmiao
- 描述:对 `code-require` / `code-design` / `code-check` / `code-plan` / `code-fix` / `code-init` / `code-rule` 7 个技能的过程文档表做静态校验,确认它们是否也存在 `code-it` 同类"判定表 ↔ 工作流"脱节问题
- 任务来源:./assistants/V0.0.3/fix/BUG-00004/PLAN.md §3 TASK-BUG-00004-00004
- 详细设计:./assistants/V0.0.3/fix/BUG-00004/RESULT.md §6 末"其他技能旁路验证结论" + §4 模块 3

---

## 1. 验证范围与方法

### 1.1 验证范围(7 个技能)

按 PLAN.md §3 TASK-BUG-00004-00004 字面,共 7 个技能(注:任务标题写"6 个技能",PLAN.md 字面写"6 个技能",但涉及文件模块列出 7 个;以**PLAN.md 字面**为准 = 7 个):

1. `code-require`(需求分析)
2. `code-design`(概要设计)
3. `code-check`(代码评审)
4. `code-plan`(详细设计 / 实施计划)
5. `code-fix`(缺陷登记与跟踪)
6. `code-init`(工程初始化)
7. `code-rule`(编码规范管理)

### 1.2 验证方法(纯静态校验,不修改任何 SKILL.md)

- **步骤 A**:对每个技能,定位"## 过程文档自适应判定"或等效章节
- **步骤 B**:数判定表中"始终生成" / "条件生成或不适用" / "不生成" 3 类条目的数量
- **步骤 C**:判定是否有 ≥ 2 个"条件生成"分支同时触发"不生成"的可能性(类似 `code-it` 纯 Markdown 改造 → 触发 2 项"不生成")
- **步骤 D**:给出"实际过度生成风险"等级(`高` / `中` / `低` / `无`)

### 1.3 验证工具

- `Read`(定位章节)
- `Grep`(搜索关键字:始终生成 / 不生成 / 不适用 / 不追加)
- **不**调用 `Bash`(本任务不涉及构建/运行)

---

## 2. 各技能判定表分析

### 2.1 `code-require`(需求分析)

#### 判定表位置

- **章节**:`## 过程文档自适应判定` line 80 起
- **表位置**:line 85-93
- **总条目数**:5

#### 判定表字面(line 87-93)

| 过程文档 | 判定准则 |
| --- | --- |
| `materials-index.md` | **始终生成** |
| `clarifications.md` | **本轮有用户问路 → 生成;无 → 不生成** |
| `related-requirements.md` | **Glob 扫描同版本/跨版本后,发现 ≥ 1 个关联需求 → 生成;0 个 → 不生成** |
| `analysis-notes.md` | **始终生成** |
| 看板"变更记录" | **本轮有追加 → 生成;无变更 / 幂等空跑 → 不追加** |

#### 统计

- **始终生成**:2 条(materials-index / analysis-notes)
- **条件生成**:3 条(clarifications / related-requirements / 看板变更记录)
- **不生成**:0 条

#### "≥ 2 项同时触发'不生成'" 场景分析

- 假设场景:`code-require` 跑 1 个全新需求,无 `AskUserQuestion`(E-2 退化倾向"生成",但显式无问路) + 0 个关联需求 + 无变更记录
 - `clarifications.md` → 不生成(无问路)
 - `related-requirements.md` → 不生成(0 个关联)
 - 看板"变更记录" → 不生成(无变更)
- **3 项"不生成"同时触发**

#### 实际过度生成风险

- **风险等级**:**低**
- **理由**:判定表存在但**未**接入"工作流守卫"(沿用 BUG-00004 §6 末其他技能旁路验证结论);但 3 个条件生成文档(clarifications / related-requirements / 看板)在实际运行中**多数会触发"生成"**(AI 倾向问路 + 多数需求有 ≥ 1 关联需求 + 多数需求有变更),所以"不生成"组合**不常出现**,实际过度生成风险低
- **结论**:**不修复**

---

### 2.2 `code-design`(概要设计)

#### 判定表位置

- **章节**:`## 过程文档自适应判定` line 83 起
- **表位置**:line 88-99
- **总条目数**:8

#### 判定表字面(line 90-99)

| 过程文档 | 判定准则 |
| --- | --- |
| `materials-index.md` | **始终生成** |
| `design-notes.md` | **始终生成** |
| `module-breakdown.md` | **模块数 ≥ 2 → 生成;模块数 = 1(单文件改)→ 不生成** |
| `dependencies.md` | **依赖其他模块/服务/中间件 ≥ 1 个 → 生成;无外部依赖(自含/单文件)→ 不生成** |
| `related-designs.md` | **Glob 扫描后,发现 ≥ 1 个关联设计 → 生成;0 个 → 不生成** |
| `rule-compliance.md` | **`./assistants/rules/` 存在且有内容 → 生成;无规范 / 空规范 → 不生成** |
| `clarifications.md` | **本轮有用户问路 → 生成;无 → 不生成** |
| 看板"变更记录" | **本轮有追加 → 生成;无变更 / 幂等空跑 → 不追加** |

#### 统计

- **始终生成**:2 条(materials-index / design-notes)
- **条件生成**:6 条
- **不生成**:0 条

#### "≥ 2 项同时触发'不生成'" 场景分析

- 假设场景:`code-design` 跑 1 个单文件改需求(模块数 = 1)+ 无外部依赖 + 0 个关联设计 + 无规范目录 + 无问路
 - `module-breakdown.md` → 不生成(模块数 = 1)
 - `dependencies.md` → 不生成(无外部依赖)
 - `related-designs.md` → 不生成(0 个关联设计)
 - `rule-compliance.md` → 不生成(无规范目录)
 - `clarifications.md` → 不生成(无问路)
- **5 项"不生成"同时触发**

#### 实际过度生成风险

- **风险等级**:**中**
- **理由**:5 项同时"不生成"的场景存在(单文件改 + 无外部依赖 + 0 关联 + 无规范 + 无问路),但**实际**:`./assistants/rules/` 目录**始终有内容**(本项目 13 个规范文件),所以 `rule-compliance.md` 几乎总是"生成";其他 4 项仍可能"不生成";**实际过度生成风险 = 中**;但**当前 0 触发**(本项目所有需求都"有规范" + 多数有外部依赖或 ≥ 2 模块)
- **结论**:**不修复**(沿用 BUG-00004 §6 末其他技能旁路验证结论)

---

### 2.3 `code-check`(代码评审)

#### 判定表位置

- **章节**:`## 过程文档自适应判定` line 104 起
- **表位置**:line 109-116
- **总条目数**:4

#### 判定表字面(line 111-116)

| 过程文档 | 判定准则 |
| --- | --- |
| `work-log.md` | **始终生成** |
| `review-checklist-applied.md` | **始终生成** |
| `findings-no-task.md` | **有"建议改"或"可选"级别发现 → 生成;0 条 → 不生成** |
| 看板"变更记录" | **本轮有追加 → 生成;无变更 / 幂等空跑 → 不追加** |

#### 统计

- **始终生成**:2 条(work-log / review-checklist-applied)
- **条件生成**:2 条(findings-no-task / 看板变更记录)
- **不生成**:0 条

#### "≥ 2 项同时触发'不生成'" 场景分析

- 假设场景:`code-check` 跑 1 个"无发现"评审(0 条"建议改"或"可选"级别)+ 无变更记录
 - `findings-no-task.md` → 不生成(0 条)
 - 看板"变更记录" → 不生成(无变更)
- **2 项"不生成"同时触发**

#### 实际过度生成风险

- **风险等级**:**极低**
- **理由**:2 项同时"不生成"是**正常场景**;但**实际**:`code-check` 多数评审会发现 ≥ 1 条"建议改",所以 `findings-no-task.md` 多数"生成";**实际过度生成风险 = 极低**;**触发条件高度依赖"评审质量"**
- **结论**:**不修复**

---

### 2.4 `code-plan`(详细设计 / 实施计划)

#### 判定表位置

- **章节**:`## 过程文档自适应判定` line 92 起
- **表位置**:line 99-109
- **总条目数**:9

#### 判定表字面(line 100-109)

| 过程文档 | 判定准则 |
| --- | --- |
| `materials-index.md` | **始终生成** |
| `design-notes.md` | **始终生成** |
| `module-details.md` | **模块数 ≥ 2 → 生成;模块数 = 1 → 不生成** |
| `interface-specs.md` | **接口数 ≥ 2 → 生成;接口数 = 0 / 1 → 不生成** |
| `data-changes.md` | **涉及数据库/缓存/状态字段变更 → 生成;纯 UI/配置/文档类 → 不生成** |
| `risk-analysis.md` | **任务数 ≥ 3 → 生成;任务数 ≤ 2 → 不生成** |
| `rule-compliance.md` | **`./assistants/rules/` 存在且有内容 → 生成;无规范 / 空规范 → 不生成** |
| `clarifications.md` | **本轮有用户问路 → 生成;无 → 不生成** |
| 看板"变更记录" | **本轮有追加 → 生成;无变更 / 幂等空跑 → 不追加** |

#### 统计

- **始终生成**:2 条
- **条件生成**:7 条
- **不生成**:0 条

#### "≥ 2 项同时触发'不生成'" 场景分析

- 假设场景:`code-plan` 跑 1 个单模块(模块数 = 1)+ 0 接口(纯配置类) + 纯文档(无 DB 变更) + 任务数 ≤ 2 + 有规范 + 无问路
 - `module-details.md` → 不生成(模块数 = 1)
 - `interface-specs.md` → 不生成(0 接口)
 - `data-changes.md` → 不生成(纯文档)
 - `risk-analysis.md` → 不生成(任务数 ≤ 2)
 - `rule-compliance.md` → 生成(有规范)
 - `clarifications.md` → 不生成(无问路)
 - 看板"变更记录" → 不生成(无变更)
- **5 项"不生成"同时触发**

#### 实际过度生成风险

- **风险等级**:**中**
- **理由**:5 项"不生成"是**真实场景**(本仓库 BUG-00004 的 `code-plan` 实际触发:`module-details.md` / `interface-specs.md` / `data-changes.md` / `clarifications.md` 4 项"不生成"被正确跳过,`process-doc-decisions.md` 出现);**但当前 0 触发过度生成**(因为 `code-plan` 现状是按"应生成"列表硬写出文件,**不**通过守卫跳过);**实际过度生成风险 = 中**,但**当前 0 触发**
- **结论**:**不修复**(沿用 BUG-00004 §6 末其他技能旁路验证结论 + 验证 T-003 决定性证据已覆盖此场景)

---

### 2.5 `code-fix`(缺陷登记与跟踪)

#### 判定表位置

- **章节**:**无**"## 过程文档自适应判定"小节
- **实际逻辑**:本技能的过程文档生成在步骤 5 / 6 / 7 中**硬写**(只生成 `fix/<BUG-NNN>/RESULT.md` + `fix/RESULT.md` + 同步看板)
- **不产出**:`investigation.md` / `fix-plan.md` / `fix-work-log.md` / `fix-compile-and-run.md` / `fix-test-results.md` / `deviations.md`(`code-fix/SKILL.md` line 381 字面)

#### 统计

- **始终生成**:2 个文件(fix/<BUG-NNN>/RESULT.md + fix/RESULT.md)
- **条件生成**:0 个
- **不生成**:6 个(明确不产出)

#### "≥ 2 项同时触发'不生成'" 场景分析

- **不适用**:`code-fix` 自身**只**写 2 个文件,其余 6 个"明确不产出"(字面声明,**不**是"过度生成"场景)

#### 实际过度生成风险

- **风险等级**:**无**
- **理由**:`code-fix` 是**纯登记型**技能(SKILL.md 顶部声明),只跟踪状态,不实施修复;过程文档清单已**字面化**且**不**变,与 `code-it` 同类问题无关
- **结论**:**不修复**

---

### 2.6 `code-init`(工程初始化)

#### 判定表位置

- **章节**:**无**"## 过程文档自适应判定"小节
- **实际逻辑**:本技能一次性扫描项目代码,产出 `INIT-REPORT.md` + `require/EXISTING-NNN/` + 创建基线版本(沿用 CLAUDE.md 字面)
- **不创建**版本工作空间外的过程文档(除 `INIT-REPORT.md` 外)

#### 统计

- **始终生成**:1 个文件(INIT-REPORT.md,基线版本)
- **条件生成**:0 个
- **不生成**:多个(隐式,只产出 1 个)

#### "≥ 2 项同时触发'不生成'" 场景分析

- **不适用**:`code-init` 一次性执行,过程文档生成是**字面化**的,与 `code-it` 同类问题无关

#### 实际过度生成风险

- **风险等级**:**无**
- **理由**:`code-init` 是**项目级一次性引导**(CLAUDE.md 字面),每个项目只应执行一次;过程文档清单**不**是"判定表"形式,而是"硬写 + 字面化"形式,与 `code-it` 同类问题无关
- **结论**:**不修复**

---

### 2.7 `code-rule`(编码规范管理)

#### 判定表位置

- **章节**:**无**"## 过程文档自适应判定"小节
- **实际逻辑**:本技能**不**在 `./assistants/rules/` 之外生成过程文档(line 426 字面);所有产出**直接是规范文件本身**(结构化、可被其他技能直接消费)
- **不创建**任何 `code/<任务>/` 下的过程文档

#### 统计

- **始终生成**:无(直接是规范文件本身)
- **条件生成**:无
- **不生成**:全部(code/<任务>/ 下不产出任何过程文档)

#### 实际过度生成风险

- **风险等级**:**无**
- **理由**:`code-rule` 是**横向,非主流程**技能(CLAUDE.md 字面),**不**做版本感知,**不**在工作空间下产出过程文档;与 `code-it` 同类问题无关
- **结论**:**不修复**

---

## 3. 综合结论汇总表

| 技能 | 判定表存在? | 始终生成 | 条件生成 | ≥ 2 项"不生成" 触发 | 实际过度生成风险 | 是否修复? |
| --- | --- | --- | --- | --- | --- | --- |
| `code-require` | ✓ | 2 | 3 | 是(3 项) | 低 | 不修复 |
| `code-design` | ✓ | 2 | 6 | 是(5 项) | 中 | 不修复 |
| `code-check` | ✓ | 2 | 2 | 是(2 项) | 极低 | 不修复 |
| `code-plan` | ✓ | 2 | 7 | 是(5 项) | 中(当前 0 触发) | 不修复 |
| `code-fix` | ✗(无判定表) | 2(硬写) | 0 | 不适用 | 无 | 不修复 |
| `code-init` | ✗(无判定表) | 1(基线) | 0 | 不适用 | 无 | 不修复 |
| `code-rule` | ✗(无判定表) | 0 | 0 | 不适用 | 无 | 不修复 |

**总览**:
- **有判定表的技能**:4 个(`code-require` / `code-design` / `code-check` / `code-plan`)
- **无判定表的技能**:3 个(`code-fix` / `code-init` / `code-rule`)
- **所有 7 个技能**:**均不触发实际过度生成**;**均不修复**
- **核心原因**:4 个有判定表的技能的判定表均**未**接入"工作流守卫"(类似 BUG-00004 修复前 `code-it` 的"判定表 ↔ 工作流"脱节);但这些技能的"条件生成"条目**多数会触发"生成"**(实际场景下,大部分条件都满足),所以"过度生成"很少发生;触发场景下也确实会出现"不生成"组合(如 `code-plan` 跑 BUG-00004 4 项不生成),**但 `code-plan` 现状是按"应生成"列表硬写出文件,即使不生成,也不会写"process-doc-decisions.md"**(与 `code-it` 修复前同病)

**修复决策**:
- 仅修复 `code-it`(由 T-001 + T-002 完成)
- 其他 6 个技能**均不修复**(0 触发 + 修复成本/收益不匹配)

---

## 4. 与 BUG-00004 详细设计 §6 末"其他技能旁路验证结论" 对照

| 技能 | 本报告结论 | 详细设计 §6 末结论 | 是否一致? |
| --- | --- | --- | --- |
| `code-require` | 实际过度生成风险:低 | 实际过度生成风险:低 | ✓ |
| `code-design` | 实际过度生成风险:中,当前 0 触发 | 实际过度生成风险:中,当前 0 触发 | ✓ |
| `code-check` | 实际过度生成风险:极低 | 实际过度生成风险:极低 | ✓ |
| `code-plan` | 实际过度生成风险:中,当前 0 触发 | 实际过度生成风险:中,当前 0 触发 | ✓ |
| `code-fix` | 实际过度生成风险:无 | 实际过度生成风险:无 | ✓ |
| `code-init` | 实际过度生成风险:无 | 实际过度生成风险:无 | ✓ |
| `code-rule` | 实际过度生成风险:无 | 实际过度生成风险:无 | ✓ |

**结论**:7 技能全部一致 ✓;**详细设计 §6 末字面"其他 6 个技能"应为"其他 7 个技能"**(PLAN.md 字面含 7 个技能,标题写"6 个"是 PLAN.md 字面歧义,本报告统一为 7 个,与 PLAN.md §3 TASK-BUG-00004-00004 "涉及文件"段字面一致)

---

## 5. 旁路验证不修改任何 SKILL.md

### 5.1 不修改的依据

- 详细设计 §4 模块 3 字面:"**不动修改任何 6 个技能 SKILL.md**(本任务**仅**静态校验 + 结论报告)"
- PLAN.md §3 TASK-BUG-00004-00004 关键变更字面:"**不动修改任何 6 个技能 SKILL.md**"

### 5.2 不修改的理由

- **修复成本/收益不匹配**:7 个技能全部"实际过度生成风险" = 低 / 中(0 触发) / 极低 / 无
- **风险可控**:`code-plan` 等技能虽然判定表"不接入工作流",但实际"条件生成"多数触发"生成",不会造成实际"过度生成"问题
- **修复优先级低**:如果未来这些技能出现"过度生成"问题,再单独按 `code-fix` 流程登记新 BUG,沿用 BUG-00004 修复模式(步骤 8.7 + 守卫)即可

### 5.3 本任务的修复范围

- **本任务**:**仅**做静态校验 + 结论报告
- **本任务**:**不**实施任何代码修改
- **本任务**:**不**触发任何 SKILL.md 守卫改动
- **本任务**:**不**影响 `code-it` T-001 + T-002 的修复成果

---

## 6. 变更记录

| 时间 | 变更类型 | 变更摘要 | 关联项 |
| --- | --- | --- | --- |
| 2026-06-22 22:10 | 旁路验证完成 | 7 个技能旁路验证报告完成(4 个有判定表 / 3 个无判定表);全部"实际过度生成风险" = 低 / 中(0 触发) / 极低 / 无;**均不修复**;与 BUG-00004 详细设计 §6 末"其他技能旁路验证结论"字面 100% 一致 | BUG-00004 + TASK-BUG-00004-00004 |