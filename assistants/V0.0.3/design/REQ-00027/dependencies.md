# 三方依赖 — REQ-00027

## 1. 本次新增依赖

**无。**

本需求为纯 SKILL.md 文字重写,不引入任何第三方依赖。

## 2. 自检

- 仓库根无 `package.json` / `requirements.txt` / `Cargo.toml` / `pom.xml` / `go.mod`(本仓库**不**包含任何包管理配置,沿用 `CLAUDE.md` 显式声明)
- 既有 `code-plan` / `code-it` / `code-unit` / `code-check` 已具备的依赖:`<本仓库>/assistants/rules/`(本轮不修改)
