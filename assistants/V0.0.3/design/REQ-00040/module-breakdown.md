# 模块拆分 — REQ-00040

更新时间:2026-06-25
版本:V0.0.3

## 改造对象(2 个文件,模块数 = 2)

| 模块名 | 路径 | 状态 | 职责 | 依赖 |
| --- | --- | --- | --- | --- |
| `code-fix` 技能入口 | `plugins/code-skills/skills/code-fix/SKILL.md` | **修改既有** | 步骤 0 末尾追加"项目可启动性探测" 子节;步骤 6 末尾追加"复现产物登记" 子节 | 对内:无新增;对外:无新增 |
| `code-fix` 缺陷详情模板 | `plugins/code-skills/skills/code-fix/templates/bug.md` | **修改既有** | 新增"## 复现产物登记" 区段;文档头新增 2 字段(复现方式 / 产物路径) | 对内:无新增;对外:无新增 |
| `code-fix` 目录结构说明 | `plugins/code-skills/skills/code-fix/templates/assistants-layout.md` | **修改既有** | 在 `fix/<BUG-NNN>/` 子目录列表中追加 `reproduce/` 行 | 对内:无新增;对外:无新增 |

## 新增产物子目录(运行时产生,**不**作为项目代码)

| 子目录 | 路径 | 状态 | 职责 | 依赖 |
| --- | --- | --- | --- | --- |
| `reproduce/` 产物子目录 | `fix/<BUG-NNN>/reproduce/` | **新增(运行时)** | 存放复现过程的 4 类产物(`run.log` / `screenshot-N.png` / `interaction-N.{json|txt}` / `RESULT-meta.json`) | 由 `code-fix` 步骤 6 末尾产生;由 `code-plan` / `code-it` / `code-check` 读 |

## 模块边界与职责分离

### `code-fix` SKILL.md 改造边界

- **追加**(不修改):
 - 步骤 0 末尾:"### X.项目可启动性探测" 子节(FR-1)
 - 步骤 6 末尾:"### X.复现产物登记" 子节(FR-2)
- **不修改**(字节级保留):
 - frontmatter L1-3(NFR-3)
 - 既有"## 工作流程" 小节步骤 1~5(NFR-3)
 - 既有"## 工作流程" 小节步骤 0 主体(只追加末尾子节,不改主体)
 - 既有"## 工作流程" 小节步骤 6 主体(只追加末尾子节,不改主体)
 - "## 不要做的事" 小节(NFR-3)
 - "## 工具使用约定" / "## 标题解析" / "## 缺陷标题生成" / "## 过程文档格式" / "## 与其他技能的关系" / "## 衔接" 等其他段落(NFR-3)

### `bug.md` 模板改造边界

- **追加**(不修改):
 - 文档头"## 文档头" 表新增 2 行(FR-6:复现方式 / 产物路径)
 - 在"## 缺陷描述" 段后(在"---" 分隔符 + "## 根因分析" 段前)新增"## 复现产物登记" 区段(FR-5)
- **不修改**(字节级保留):
 - 文档头"## 文档头" 表既有 11 行(NFR-3)
 - 既有"## 缺陷描述" 段(沿用)
 - 既有"## 根因分析" 段(沿用)
 - 既有"## 修复方案" / "## 修复实施" / "## 验证结果" / "## 修复日志" / "## 关联项" / "## 变更记录" 段(沿用)

### `assistants-layout.md` 改造边界

- **追加**(不修改):
 - 在 `fix/<BUG-NNN>/` 子目录列表中追加 `reproduce/` 行
- **不修改**:
 - 顶层目录结构(NFR-3)
 - 其他 6 个 `code-fix` 子目录(`RESULT.md` / `investigation.md` / `fix-plan.md` 等,沿用)

## 不新增模块(沿用既有)

- **不**新增 `code-fix/lib/` 共享库(本需求探测逻辑简单,SKILL.md 内联伪代码即可)
- **不**新增 `code-fix/checklists/` 校验清单(纯产物登记,无强校验需求)
- **不**新增 `code-fix/guidelines/` 规则文档(沿用 `assistants/rules/` 既有 13 份规范)
