# 设计目标 — REQ-00022

更新时间:2026-06-07
版本:V0.0.3

## 调用上下文
- 来源:code-design 步骤 0b.0 检测 `./assistants/.code-auto-running` **存在** → code-auto 上下文
- 检测时间:2026-06-07
- AskUserQuestion 触发:**否**(code-auto 上下文,跳过问路,采纳 --balanced 默认)
- **但本会话已在 code-require 阶段收集用户回答(Q-1 ~ Q-4,全部采纳推荐项)→ 沿用本会话决策**

## 整体设计目标
**--balanced**(本会话 code-require 阶段未问"整体设计目标",code-auto 上下文默认 --balanced;但本需求实际工作量 = 全局字符串替换,**不涉及架构决策**,整体目标对结果无实质影响)

依据:code-auto 上下文默认 --balanced;本需求为"硬重命名 + 全局字符串同步",**不涉及架构层决策**,整体目标在本需求中**N/A**

## 维度优先级

### 功能性
**优先级:中**

依据:code-auto 上下文默认功能性=中;本需求改造 11 类引用方 + 1 目录重命名,功能强度中等

### 其它维度
- **扩展性 / 健壮性 / 可维护性 / 封装性 / 可复用性 / 可读性**:本需求为"字面量重命名",**不涉及架构维度**,由 code-plan 阶段确认

## 用户授权的偏离(本目标对应)
- (无)本需求为"硬重命名",**不**偏离任何规范

## 回写算法(NFR-3 幂等)
- 本节由 code-design 步骤 0b 写入,幂等覆盖
