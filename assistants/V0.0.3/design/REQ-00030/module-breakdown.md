# 模块拆分 — REQ-00030

更新时间:2026-06-12 14:25
版本:V0.0.3

> **职责声明**:本节是**概设阶段**模块拆分,只列"模块名 / 路径 / 状态 / 职责 / 依赖"5 列。
> 完整字段(关键类/函数签名、调用顺序、并发模型、状态归属、错误处理范式、日志埋点)由 `code-plan` 在 `plan/.../RESULT.md §3` + `module-details.md` 中展开。

## 1. 模块总览

| 模块名 | 路径 | 状态 | 职责 | 依赖 |
| --- | --- | --- | --- | --- |
| `code-design` 步骤定义 | `plugins/code-skills/skills/code-design/SKILL.md` | 修改既有 | 步骤 0b 收敛 + 步骤 9A/10A/11A 输出深度收窄 | 无(对内)/ 无(对外) |
| `code-design` 模板 | `plugins/code-skills/skills/code-design/templates/design.md` | 修改既有 | §7 / §8 / §9 章节重写 + 顶部注释 | 无 |
| `code-plan` 步骤定义 | `plugins/code-skills/skills/code-plan/SKILL.md` | 修改既有 | 步骤 7A 补做强约束 + 步骤 10A 架构骨架触发收紧 | 无 |
| `code-plan` 模板 | `plugins/code-skills/skills/code-plan/templates/plan.md` | 修改既有 | §4-§10 章节由"建议"改"必填" | 无 |
| `code-check` 步骤定义 | `plugins/code-skills/skills/code-check/SKILL.md` | 修改既有 | 评审清单追加 3 个校验点 | 无 |
| (其他 11 个 `code-*` 技能) | `plugins/code-skills/skills/<other>/SKILL.md` | 0 改 | — | — |
| (7 个项目级规范) | `./assistants/rules/*.md` | 0 改 | — | — |
| (版本看板) | `./assistants/V0.0.3/RESULT.md` | 修改既有 | "概要设计清单" + "变更记录" 2 区段追加 | 无 |

## 2. 关键设计点(本节为概设,**不**深入字段级)

### 2.1 `code-design/SKILL.md` 修改范围

- **步骤 0b**(已有小节,**不**重写既有结构):
  - 在"自适应问题数"小节**追加**本需求 FR-1 / FR-2 的修订逻辑
  - 在"职责分离"声明**追加**本需求"小需求 = 0 问默认"一行
- **步骤 9A / 10A / 11A**(已有小节,**不**重写):
  - 步骤 9A:模块表**只**列 5 列(沿用既有"模块名 / 路径 / 状态 / 职责 / 依赖"列名)
  - 步骤 10A:接口概要**只**列 4 项;数据结构**只**列 4 项
  - 步骤 11A:三方依赖每行**只**列 4 项(依赖名 / 一句话用途 / 必要性 / 替代评估)
- **既有"## 不要做的事"小节**:**不**修改(NFR-3 字节级保留)
- **frontmatter L1-3**:**不**修改(`skill-conventions §规则 1`)

### 2.2 `templates/design.md` 修改范围

- **§7.1 模块总览表**:5 列(模块名 / 路径 / 状态 / 职责 / 依赖)
- **§7.2 新增模块**:移除"关键设计点 / 对外接口 / 依赖 / 理由 / 符合的规范条款"等字段
- **§7.3 / §7.4 复用 / 修改既有模块**:保留,行内容简化
- **新增 §7.5 设计边界**:明确"以下细节由 code-plan 在 plan/.../RESULT.md §4 + module-details.md 展开"
- **§8 接口概要**:每个接口**只**列 4 项;新增 §8.5 设计边界
- **§9 数据结构**:每个实体**只**列 4 项;新增 §9.5 设计边界
- **§10 三方依赖**:每行**只**列 4 项
- **§3 / §4 / §5 / §6 / §11 / §12 / §13 / §14 / §15 / §16**:**不**修改
- **模板顶部注释**:**追加**一行 `> 本模板只覆盖概要设计...`

### 2.3 `code-plan/SKILL.md` 修改范围

- **步骤 7A**(已有小节):
  - "按概要设计逐模块展开" → 改为强约束"完整可直接编码的细节:伪代码 / 字段 / 接口 schema"
  - 强调"**任何概要设计未覆盖的细节必须在详设阶段补全**"
  - 4 份过程文档(`module-details.md` / `interface-specs.md` / `data-changes.md` / `risk-analysis.md`)由"可选"改"必选"
- **步骤 10A**(已有小节):
  - "架构任务作为首个任务(条件性)"子段**追加**本需求 FR-6 的 2 个新触发条件
  - 调整"可省略"小节语义:小需求 / 整体 `--balanced` / 整体 `--minimal` **不**插入架构骨架

### 2.4 `templates/plan.md` 修改范围

- **§4 / §5 / §6 / §7 / §8 / §9 / §10 / §11 / §12**:由"建议填"改为"必填";若某章节无内容,显式写"本需求不涉及"
- **§13 / §14 / §15**:**不**修改

### 2.5 `code-check/SKILL.md` 修改范围

- 评审清单**追加** 3 个校验点(FR-7):
  1. **详设完整性**:`plan/.../RESULT.md` 中每条任务,必须能在 §4-§10 找到对应章节
  2. **概设越界检测**:`design/.../RESULT.md` §7 / §8 / §9 不应出现详设深度内容
  3. **行数比例警告**:`design > plan * 1.2` → 标黄
- 既有评审清单**不**修改;**追加**(不重写)

## 3. 自检(对照 `module-conventions §规则 1` 沿用,本仓库为 DEPRECATED 状态以 `directory-conventions` 替代)

- ✅ 资源文件放 `templates/` 子目录(5 个被改的 templates 都已在对应子目录)
- ✅ 技能根目录只放 `SKILL.md`,无散落资源
- ✅ 无新增依赖 / 无新增三方 / 无循环依赖

## 4. 与概要设计 §3.1 / §3.2 / §3.3 的对应

- 步骤 0b 自适应问路 → §3.2(本设计 RESULT.md)
- 步骤 9A / 10A / 11A 输出深度 → §3.3(本设计 RESULT.md)
- 模板重写 → §3.4(本设计 RESULT.md)
- code-plan 补做 + code-check 校验 → §3.5 / §3.7(本设计 RESULT.md)
