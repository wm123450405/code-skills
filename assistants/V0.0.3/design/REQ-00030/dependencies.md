# 三方依赖评估 — REQ-00030

更新时间:2026-06-12 14:25
版本:V0.0.3

> **本节是概设阶段三方依赖评估,只列 4 项 / 依赖(依赖名 / 一句话用途 / 必要性 / 替代评估)。**
> 完整评估(许可 / 活跃度 / 体积 / 安全态势)由 `code-plan` 在 `plan/.../RESULT.md` 展开。

## 1. 复用既有依赖(本需求 0 改)

- 本需求**不**引入任何新三方依赖(不写 `package.json` / `requirements.txt` / `go.mod` 等)
- 本需求**不**修改任何既有依赖声明
- 沿用既有:`commit-conventions.md`(`chore(<skill>):` 前缀,无版本号锁定)

## 2. 新增依赖

| 依赖 | 一句话用途 | 必要性 | 替代评估 |
| --- | --- | --- | --- |
| (无) | — | — | — |

## 3. 拒绝引入的依赖及理由

- **拒绝引入任何新依赖**:
  - 本需求仅修改 5 个 markdown 文件(SKILL.md + templates),无需运行时依赖
  - 本仓库本身**不**包含源代码、构建系统、测试框架、Lint 工具或包管理配置(`CLAUDE.md` §"需与用户确认的约定"明确锁定)
  - 引入任何依赖都违反"无工具链"约束
