# 设计目标 — REQ-00021

更新时间:2026-06-06 17:10
版本:V0.0.3

## 调用上下文
- 来源:code-design 步骤 0b.0 检测 `./assistants/.code-auto-running` 不存在 → 用户手动调子技能
- 检测时间:2026-06-06 17:10
- AskUserQuestion 触发:是

## 整体设计目标
**--extensible(功能性最大)**

依据:用户选择 --extensible,本需求在 balanced 基础上,加占位符嵌套 / 自定义占位符映射文件(--map) / 模板变量文件(--vars) 等扩展点;为后续二进制 follow-up 预留 CLI 接口(`--script` 指定填充脚本)。

## 维度优先级

### 功能性
**优先级:高**

依据:用户选择"覆盖全部主流程场景",覆盖 FR-1 ~ FR-7 + 30 AC + 9 INV,内置 15 个常用占位符,屏显 + 过程文档同步;遵守 NFR-3.3(code-auto 不传) + INV-7(0 派生任务) + INV-8(0 改其他 10 技能)。

### 其它维度
- **扩展性 / 健壮性 / 可维护性 / 封装性 / 可复用性 / 可读性**:沿用 REQ-00020 职责分离,统一下沉到 `code-plan` 详设阶段;本概设不展开

## 用户授权的偏离(本目标对应)

| 维度 | 规范条款 | 偏离内容 | 理由 | 授权时间 |
| --- | --- | --- | --- | --- |
| 可维护性 | NFR-5.1(SKILL.md 行数变化 -5% ~ +15%) | --extensible 目标下,3 个 SKILL.md 行数变化可能 +20% ~ +30%(因新增 --map / --vars / --script 扩展点 + 占位符嵌套逻辑) | 用户选择 --extensible,要求为后续二进制 follow-up 预留 CLI 接口;新增扩展点不可避免 | 2026-06-06 17:10 |

## 回写算法(NFR-3 幂等)
- 本节由 code-design 步骤 0b 写入,幂等覆盖(同 REQ 同目标重复执行,结果一致)
- 写入方式:`Edit` 替换本文件整段;不修改 frontmatter;不修改 `## 工作流程` 小节
