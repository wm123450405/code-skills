# 澄清记录 — REQ-00021

更新时间:2026-06-06 17:50
版本:V0.0.3

## 概设阶段澄清(本概设新增)

### 2026-06-06 17:10 — 设计目标确认
- **问题**:REQ-00021 是为 code-require / code-design / code-plan 3 个技能新增 --result / --plan 可选 CLI 参数,实现"按用户提供的模板格式(后缀)输出填充后的 REQUIRE/DESGIN/PLAN 文件"。本需求范围小、改动集中在 3 个 SKILL.md,整体设计目标取哪个?
- **用户回答**:`--extensible`(功能性最大)— 在 balanced 基础上加占位符嵌套 / 自定义占位符映射文件(--map)/ 模板变量文件(--vars) 等扩展点;为后续二进制 follow-up 预留 CLI 接口
- **影响**:本概设 §设计目标 = `--extensible` + 功能性=高;NFR-5.1 SKILL.md 行数变化 -5% ~ +15% **用户授权偏离**到 +20% ~ +30%(rule-compliance.md §4 记录)

### 2026-06-06 17:12 — 功能性维度
- **问题**:本需求的功能性维度(占位符覆盖度 / 格式支持范围 / BUG 路径兼容 / code-auto 协同)取哪个优先级?
- **用户回答**:高(覆盖全部主流程场景)
- **影响**:本概设覆盖 FR-1 ~ FR-7 + 30 AC + 9 INV,内置 15 个常用占位符,屏显 + 过程文档同步

## 需求阶段澄清(沿用,本概设不重做)

### 2026-06-06 16:50(REQ-00021 需求分析)
- **问题**:用户提供的模板 .docx 文件实际是哪种?Claude Code 的 Write 工具只能写纯文本(无法写二进制 .docx zip),需要先确定模板格式。
- **用户回答**:docx只是例子,可能是其他格式文件如 xlsx、pdf 等,按照实际用户给定的模板格式输出,如用户提供的是.docx文件,则输出格式也为.docx文件,按实际二进制(对应数据格式)内容填充
- **影响**:FR-2 / FR-3 — 本需求**仅**实现可文本化格式(.md / .html / .txt / .json / .xml / .csv / .yaml);二进制格式(.docx / .xlsx / .pdf / .pptx)屏显 `⚠ 跳过`,**留作 follow-up**

### 2026-06-06 16:52(REQ-00021 需求分析)
- **问题**:3 技能在传 --result / --plan 时,生成的文件名应如何命名?
- **用户回答**:后缀名和用户提供的文件名保持一直,最终文件名按照 REQUIRE、DESGIN(详设、概设都是)、PLAN 命名
- **影响**:FR-1 表格 + FR-2 写出规则 — `code-require` → `REQUIRE.<ext>`,`code-design` → `DESGIN.<ext>`,`code-plan --result` → `DESGIN.<ext>`(详设),`code-plan --plan` → `PLAN.<ext>`

### 2026-06-06 17:00(REQ-00021 需求分析)
- **问题**:DESGIN 拼写(用户原文)是否纠正为 DESIGN?
- **用户回答**:不纠正,沿用用户原文拼写 "DESGIN"
- **影响**:RESULT.md / SKILL.md 全部沿用 "DESGIN" 拼写(不"自动修正")

### 2026-06-06 17:05(REQ-00021 需求分析)
- **问题**:二进制格式(.docx / .xlsx / .pdf)本需求**不**实现(因本仓库无 Python/Node 工具链),是否同意留作 follow-up?
- **用户回答**:同意(本需求范围只实现可文本化格式;二进制格式屏显 `⚠ 跳过` 不报错)
- **影响**:FR-3 锁定为本需求**不**实现,留作 follow-up
