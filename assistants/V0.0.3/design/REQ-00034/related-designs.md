# 关联概要设计 — REQ-00034

更新时间:2026-06-15 13:30
版本:V0.0.3

## 关联概要设计(同版本)

| 需求 | 关联点 | 影响 |
| --- | --- | --- |
| REQ-00031 | "外移单元测试"+"任务粒度收窄"(5 任务已落地) | **前置依赖**;本需求是其"实际删除"补完 |
| REQ-00009 | `code-unit` 守卫"项目可测性"(7 项检查) | **强继承**;由 `code-it` 接管,字节级沿用 |
| BUG-00001 | 5 技能加"不修改 SKILL.md"硬约束 | **兼容性**;本需求不冲突(NFR-7 锁定) |
| REQ-00026 | SKILL.md 描述通用化扫除 | 沿用最小化变更原则 |
| REQ-00030 | 元技能改 + 12 维度评审 + INV 字节级保留 | 沿用 INV-1 ~ INV-12 校验点 |
| REQ-00032 | `code-require` 屏显契约 | 沿用 |
| REQ-00033 | `code-require` 不涉及技术选型 | 沿用(本需求不涉及) |
| REQ-00020 | 元技能改首条需求 | 沿用 |

## 跨版本扫描(可选)

按 `code-design` 步骤 12A 约定,跨版本扫描**可选**。本设计范围广(29 个文件),但仍只在 V0.0.3 当前激活版本内改造,不跨版本扫描以避免噪音。
