# 设计笔记 — REQ-00034

## 1. 候选方案对比

| 方案 | 行为 | 优点 | 缺点 | 选定 |
| --- | --- | --- | --- | --- |
| **激进删除**(本设计锁定) | `code-unit` 整体删除;`code-it` 接管 | 契合"移除"字面;职责单一;屏幕日志减少 1 行/任务 | 29 个文件跨 5 类别改造,易引入回归 | **选** |
| 保留兼容壳 | `code-unit` 改占位 | 兼容旧用户路径 | 与"移除"字面冲突;占位技能易混淆 | 拒 |
| 改名非删除 | `code-unit` → `code-test` | 保持技能数量 | 与"移除"+"整合"字面冲突;只换壳 | 拒 |

## 2. 候选 14 任务拆分(由 `code-plan` 阶段细化)

| 候选 | 文件 | 备注 |
| --- | --- | --- |
| T-001 | `code-unit/SKILL.md` + `code-unit/templates/` 硬删除 | 沿用 `rm -rf` 风格 |
| T-002 | `code-it/SKILL.md` 步骤 8a 守卫接管 | 字节级沿用 `code-unit` 步骤 0a |
| T-003 | `code-it/SKILL.md` 步骤 8.5 按需写单测接管 | 3 类自动判定 |
| T-004 | `code-it/SKILL.md` 文档头 + L18/L795/L907-908 字面改写 | 反向声明删除 |
| T-005 | `code-it/templates/RESULT.md` 新增"## 单元测试"小节 | +20~+40 行 |
| T-006 | `code-plan/SKILL.md` L368/431/445/454/1105 字面改写 | -10~+20 |
| T-007 | `code-auto/SKILL.md` 步骤 4.b + 子技能调用表 4 行删除 | -50~+80 |
| T-008 | `code-auto/SKILL.md` 衔接 + 不要做的事段 `code-unit` 字面删除 | 沿用 T-007 |
| T-009 | `code-check/SKILL.md` L3/21/40-41/56/72/96/151/281/608/615 字面改写 | -10~+20 |
| T-010 | `plugin.json` + `marketplace.json` 注册项删除 | 3 处字面 |
| T-011 | 4 README + CLAUDE.md 字面改写 | -10~+30 |
| T-012 | 7 项目级规范字面改写 | -5~+15 |
| T-013 | 11 技能描述段字面改写 | -10~+30 |
| T-014 | 看板同步 | 1 行 + 1 条 |

**说明**:本表是**候选**;实际由 `code-plan` 阶段按"既改 1 个文件 = 1 任务"原则细化。

## 3. 关键设计决策矩阵

| ID | 候选 | 选定 | 理由 |
| --- | --- | --- | --- |
| D-1 | 3 方案 | 激进删除 | 契合"移除"字面 |
| D-2 | 守卫接管 / 不接管 | 接管 | 字节级沿用 `code-unit` 步骤 0a;RE-00009 强继承 |
| D-3 | 自动 / 询问用户 | 自动判定 | 用户"根据需要"隐含不问 |
| D-4 | 2 状态 / 5 状态 | 2 状态 | 沿用 REQ-00031 FR-3 |
| D-5 | 接管 / 不接管错误修复 | 接管 | 写单测固有环节 |
| D-6 | 接管 / 不接管覆盖率 | 不接管 | 避免 `code-it` 净增过多;`code-check` 评审时按需补 |
| D-7 | 沿用 `test-results.md` / 新增 `unit-test-results.md` | 新增 | 语义分离清晰 |
| D-8 | 5 处字面改写 | 改写 | 旧语义过时 |
| D-9 | 整段删除 / 保留为恒等跳过 | 删除 | `code-plan` 不产测试任务 + `code-unit` 退场 |
| D-10 | 10 处字面改写 | 改写 | 评审直接读 `code/.../unit-test-results.md` |
| D-11 | 删除 3 处注册项 | 删除 | 物理删除同步 |
| D-12 | 4 README + CLAUDE.md 字面 | 改写 | `doc-conventions.md` 规则 1/2 |
| D-13 | 7 规范字面 | 改写 | 核心约束保留 |
| D-14 | 11 描述段字面 | 改写 | frontmatter L1-3 字节级保留 |
| D-15 | 8 个 SKILL.md 核心工作流 | 0 改 | 仅描述段去 `code-unit` |

## 4. 风险与权衡

| 风险 | 等级 | 缓解 |
| --- | --- | --- |
| 措辞过严,误伤"既有 `code-unit` 引用但语义已变" | 中 | 改写时**只**改字面,核心约束保留 |
| `code-it` 接管守卫后 7 项检查与原行为不一致 | 中 | 字节级沿用 |
| `code-auto` 步骤 7 输出报告"单元测试(code-unit):N2 次"如何处理 | 中 | 字段**改写**为"单元测试(由 `code-it` 内化):N2 次" |
| 历史 `auto-report.md` 中"code-unit"日志字节级保留? | 低 | NFR-2 沿用;**不**追溯重写 |
| `code-it` 净增 ~200 行,可能突破单 SKILL.md 加载阈值 | 低 | `code-it` 当前 938 行,加 200 后 ~1138,仍在范围内 |
| 5 SKILL.md 改造,易引入回归 | 中 | 字节级保留 frontmatter / §"工作流程" 既有步骤;INV-1 ~ INV-3 严守 |
| `code-check` 评审时仍读历史 `test/<TASK-...>/RESULT.md` 是否行为变化 | 低 | FR-7 字面改写**只**改文案,不影响历史路径 |
