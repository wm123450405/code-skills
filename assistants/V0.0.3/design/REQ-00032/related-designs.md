# 关联设计 — REQ-00032

更新时间:2026-06-12 16:25
版本:V0.0.3

## 1. 同版本关联设计(优先)

> `Glob ./assistants/V0.0.3/design/*/RESULT.md` 列出 12 个设计(含本需求 REQ-00032)

| 需求 | 设计标题 | 状态 | 关联点 | 对本设计的影响 |
| --- | --- | --- | --- | --- |
| REQ-00020 | 优化 code-design / code-plan,架构设计目标重新归位 + 新增 3 维度 + 步骤归并 | 已完成 | code-design / code-plan 步骤归并;7 维度评估;code-dashboard 引导 N 行为约定 | 本需求"建议只 2 条"借鉴 REQ-00020 的"职责分离 / 维度收窄"思路(不展开为多维度建议) |
| REQ-00021 | 优化 3 技能 --result / --plan 模板参数,按用户模板格式输出填充后文档 | 已完成 | `--result` / `--plan` 模板参数;DESGIN 拼写锁定 | **不涉及**(本需求无模板参数) |
| REQ-00022 | 修改 /code-review 技能名称为 /code-check | 已完成 | 技能重命名 | **不涉及** |
| REQ-00023 | 简化 /code-dashboard 输出:总开发进度 + 5 类状态占比 + 后续操作建议 | 已完成 | 后续操作建议 | 借鉴"`code-dashboard` 后续操作建议"的"中点起首"风格(本需求简化为 2 条) |
| REQ-00024 | 移除 /code-auto 的 from 关键字逻辑,改用路径感知判定 | 已完成 | code-auto 路径感知 | **不涉及**(本需求不修改 code-auto) |
| REQ-00025 | 软化编号正则约束,允许用户自定义编号格式(仅前缀固定) | 已完成 | 编号正则软化 | **不涉及** |
| REQ-00026 | 技能描述通用化扫除(10 SKILL.md 描述性段落去 plugins/code-skills 强关联指代) | 已完成 | 技能描述通用化 | 沿用(本需求不修改技能 description) |
| REQ-00027 | 优化 code-fix 流程(纯登记型)+ code-auto BUG 路径编排 | 已完成 | code-fix / code-auto BUG 路径 | **不涉及** |
| REQ-00028 | 新增 code-answer 技能(只读功能查询,跨版本需求 + 源码补足) | 已完成 | code-answer 新增 | **不涉及** |
| REQ-00029 | 优化 /code-dashboard 屏显报告(只展示结果 + 去除不必要换行) | 进行中 | code-dashboard 屏显精简 | 借鉴"屏显精简"思路(本需求屏幕日志建议 2 行 ≤ 80 字) |
| REQ-00030 | 优化 /code-design 与 /code-plan 职责分离(概设只做概设,详设开始做详设;扩展性三阶段下沉) | 已完成 | 概设/详设职责分离;12 维度评审清单 | **INV 字节级保留约束沿用**(INV-1~INV-10) |
| REQ-00031 | 优化 /code-plan 任务粒度(内化编译/运行,外移单元测试) | 已完成 | 元技能改;code-plan / code-it / code-unit / code-auto + templates/plan.md | **元技能改路径的同类参考**(本需求沿用"步骤末尾追加"模式) |

## 2. 跨版本关联(可选)

> `Glob ./assistants/*/design/*/RESULT.md` 列出全部版本的设计

| 需求 | 所在版本 | 关联点 | 对本设计的影响 |
| --- | --- | --- | --- |
| REQ-00003 | V0.0.1 | 早期元技能改;命名规范占位模式(naming-conventions.md) | 沿用占位模式;**不修改**naming-conventions.md |

## 3. 关联点分类(本需求影响面)

| 关联点 | 类型 | 关联需求 | 复用/扩展/互斥 |
| --- | --- | --- | --- |
| 屏显格式契约(中点 `·`、≤ 30 字) | 横向 | REQ-00013 / REQ-00025 | 沿用(本需求不涉及标题解析) |
| 屏幕日志"中点/箭头起首"风格 | 横向 | REQ-00023 / REQ-00029 | 沿用(本需求屏幕建议起首 `→`) |
| 后续操作建议 5 类优先级 | 借鉴 | REQ-00023 | **不沿用**(本需求简化为 2 条) |
| 元技能改路径(步骤末尾追加) | 路径 | REQ-00031 | 沿用(本需求与 REQ-00031 同构) |
| INV 字节级保留 | 约束 | REQ-00030 | 沿用(INV-1~INV-10 字节级保留) |
| code-auto 步骤 4.b 恒等跳过 | 横向 | REQ-00031 FR-6 | 沿用(本需求不修改 code-auto) |
| 命令行参数解析 `--result` | 不涉及 | REQ-00021 | 本需求不涉及模板参数 |

## 4. 总结

- **沿用**:屏显格式契约 / 元技能改路径 / INV 字节级保留 / code-auto 协同
- **不沿用**:5 类优先级(简化为 2 条) / 模板参数 / 编号正则软化
- **不修改**:既有 9 个 `code-*` 技能 SKILL.md / 12 个 REQ 的 RESULT.md / 7 个项目级规范
