# 关联概要设计 — REQ-00038

更新时间:2026-06-22 13:30
版本:V0.0.3

## 同版本关联设计(关联点详述)

### REQ-00034:移除 /code-unit 技能,能力整合进 /code-it(按需写单测/不适用工程跳过)

- 路径:`./assistants/V0.0.3/design/REQ-00034/RESULT.md`
- 关联点:`code-unit` 整合进 `code-it`,7 项守卫工程根级框架;本需求在其基础上细化到模块粒度
- 协同动作:
 - 7 项守卫**字节级沿用**(NFR-4);仅位置从 CWD 根 → 模块目录
 - `code-it` 步骤 8a / 8.5 既有行为**0 改**;仅扩展位置 + 多模块结果记录
 - 单模块工程 = 单模块识别 = 字节级沿用 REQ-00034(零回归,AC-4 锁定)

## 跨版本关联设计(可选,本次未涉及)

- 无

## 与其他设计的引用网

- 与 REQ-00034 直接引用(7 项守卫字节级沿用)
- 与 REQ-00031 / REQ-00037 间接关联(本需求不涉及 `code-unit` 退场逻辑 / 缺陷修复流程)
- 与 BUG-00001 间接关联("不修改 SKILL.md"硬约束,本需求仅消费既有 SKILL.md 不修改 frontmatter)