# 关联概要设计 — REQ-00033

更新时间:2026-06-15 12:20
版本:V0.0.3

## 关联概要设计(同版本)

| 需求 | 关联点 | 影响 |
| --- | --- | --- |
| BUG-00001 | "不修改 SKILL.md"硬约束范式(§"不要做的事" 段) | 沿用同套范式;本设计锚点 = §"不要做的事" 小节 |
| REQ-00026 | SKILL.md 描述通用化扫除 | 沿用最小化变更原则;本设计净增 +2 ~ +4 行 |
| REQ-00030 | 元技能改 + 12 维度评审 + INV 字节级保留 | 沿用 INV-1 ~ INV-9 校验点 |
| REQ-00031 | 元技能改 /code-plan /code-it /code-unit /code-auto | 沿用元技能改路径;本设计**不**涉及 /code-unit |
| REQ-00032 | `code-require` 屏显契约(锚点不交叉) | 沿用;本设计**不**涉及屏显 |
| REQ-00020 | 元技能改规范(首条元技能改需求) | 沿用整体改造模式 |

## 跨版本扫描(可选)

按 `code-design` 步骤 12A 约定,跨版本扫描**可选**。本设计范围窄(只在 V0.0.3 当前激活版本内显式化 `code-require` 边界),不跨版本扫描以避免噪音。
