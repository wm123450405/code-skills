# 规范遵循记录 — REQ-00037

更新时间:2026-06-22 09:18
版本:V0.0.3

## 1. 本次参考的规范文件

(共 13 份,详见 `materials-index.md` "项目级规范"部分)

## 2. 规范 vs 现状偏离

(本设计**无**规范与现状偏离——所有修改都是"修改既有 SKILL.md"而非"新建",且修改都遵循既有结构)

## 3. 规范 vs 需求冲突

### C-1:是否触发 `dashboard-conventions §规则 1` 三方同步?

- **规范条款**:`./assistants/rules/dashboard-conventions.md §规则 1`:看板字段约定扩展需**三方同步**(模板 + CLAUDE.md + 本规范)
- **触发条件**:
  1. 新增/删除/重命名"区段"
  2. 新增/删除/重命名"表格列"
  3. 新增/删除/修改"枚举值"
  4. 调整"字段语义说明"
- **本设计动作**:
  - FR-1 ~ FR-5 在 `fix/<BUG-NNN>/RESULT.md` 文档头"状态"字段写新字面(`待处理 / 待开发 / 开发中 / 待审查 / 已完成`)
  - FR-7 扩展 `code-dashboard` 的"5 类状态占比"分类规则
  - 看板"缺陷清单"区段的"状态"列字面**直接**写新字面,**不**新增列、**不**修改列
- **判定**:
  - 看板"缺陷清单"区段的"状态"列原本就是**自由字符串列**(每个 BUG 写当前字面,沿用 V0.0.1 ~ V0.0.3 既有约定)
  - "状态"列字面变更 ≠ "新增枚举值列"(后者是"开发状态"列那种**枚举值列**的变更,需要三方同步)
  - **不触发** §规则 1
- **解决时间**:2026-06-22 09:18
- **解决依据**:看板"任务清单"区段的"开发状态"列是**枚举值列**(枚举值变更需三方同步);"缺陷清单"区段的"状态"列是**自由字符串列**(字面变更不需三方同步,只需在 `code-dashboard` 扩展分类规则)

### C-2:是否触发 `skill-conventions §规则 2`(SKILL.md 不含开发痕迹)?

- **规范条款**:`./assistants/rules/skill-conventions.md §规则 2`:SKILL.md + templates/ 中**不得**包含 6 类开发痕迹(段尾引用 / 回溯注释 / 决策记录 / 生效日标记 / 退场文件名引用 / 杂项)
- **本设计动作**:
  - 本轮在 4 个 SKILL.md 中**新写**若干段落(状态推进表新增行 / 状态回写子步骤 / 典型完整流程新举例)
  - **不**写"本需求 REQ-00037 新增"类尾注
  - **不**写"原 code-fix 状态机"类回溯
  - **不**写"Q-N 锁定"决策记录
  - **不**写"YYYY-MM-DD 起生效"标记
  - **不**引用已退场文件名(仍可引用 `fix-plan.md` / `fix-work-log.md` 等目录级目录名)
- **判定**:**不触发** §规则 2(本设计新写段落全部符合要求)
- **解决时间**:2026-06-22 09:18

## 4. 用户授权的偏离

(无)

## 5. 规范变更响应(增量更新时填写)

(本轮为首次设计,无规范变更)
