# 依赖与接口 — REQ-00029
更新时间:2026-06-10 11:30
版本:V0.0.3

## 1. 三方依赖

**本需求不引入任何新三方依赖**。

## 2. 接口契约(屏显输出契约)

### 2.1 总览模式(无参数调用 `/code-dashboard`)

| 区段 | 改后格式 | 屏显总行数 |
| --- | --- | --- |
| 段 1:总开发进度 | `[███████████░] 92%`(1 行,只 ASCII) | 1 |
| 段 2:5 类状态占比 | `状态: 待代码开发 0 / 待代码评审 0`(1 行摘要) | 1 |
| 段 3:高优缺陷 | `P0 待修复: █ 2 \| P1 待修复: ░ 0`(1 行合并) | 1 |
| 段 4:后续操作建议 | `> <命令> [<优先级>] (依据: <依据>)`(每条 1 行,≤ 5 条) | ≤ 5 |
| **满负载合计** | — | **≤ 8 行** |

**退化场景**:
- 无需求无缺陷:段 1 = `— / 无需求无缺陷,无需进度`;段 2 = `(无)`;段 3 = 骨架;段 4 = `> 无后续动作`
- 5 类状态全 0:段 2 = `状态: (无)`
- 5 条建议无触发:段 4 = `> 无后续动作`

### 2.2 需求模式(参数 `REQ-NNNNN`)

| 区段 | 改后格式 | 屏显行数 |
| --- | --- | --- |
| 段 1:元信息 | `REQ-00028 · 新增 code-answer 技能 [已完成] 概要: ✓ 详细: ✓`(1 行合并) | 1 |
| 段 2:任务清单 | `任务: TASK-REQ-... × 1(已完成 1 / 不适用 1)`(1 行摘要) | 1 |
| 段 3:关联缺陷 | `缺陷: (无)` 或 `缺陷: F-001 [已处理]`(1 行) | 1 |
| 段 4:任务进度 | `进度: 1/1 (100%)`(1 行) | 1 |
| 段 5:建议 | `> <命令> [<优先级>] (依据: <依据>)`(每条 1 行,≤ 5 条) | ≤ 5 |
| **满负载合计** | — | **≤ 9 行** |

### 2.3 建议格式契约(强约束)

```
> /code-fix BUG-00001 [高] (依据: P0 待修复 2 个)
> /code-it TASK-REQ-00020-00007 [低] (依据: 2 个派生改修任务待开始)
> /code-version V0.0.4 [中] (依据: 8 需求已完成)
> 无后续动作
```

- 命令:严格匹配既有 `code-*` SKILL.md frontmatter 真实语法
- 优先级:高 / 中 / 低 / —
- 依据:1 行内含(括号嵌入),不另起一行

## 3. 算法契约(字节级保留,沿用既有)

| 算法 | 路径 | 不变量 |
| --- | --- | --- |
| 算法 0 | §步骤 1 参数解析 | INV-2 |
| 算法 1 | §步骤 3 区段解析 | INV-2 |
| 算法 2 | `classifyState()` 5 类状态判定 | INV-2 |
| 算法 3 | `buildBreakdown()` 占比计算 | INV-2 |
| 算法 4 | §附录 A `parseTaskId()` 双正则兼容 | INV-2 |
| 算法 5 | §附录 B `renderBar()` ASCII 比例条 | INV-3 |

## 4. 工具集契约(NFR-7 严守)

```
读: Read / Glob / Grep
不调用: Write / Edit / Bash / WebFetch / WebSearch / Task / Agent
不调用任何 MCP 工具
```

— 与 INV-7 一致