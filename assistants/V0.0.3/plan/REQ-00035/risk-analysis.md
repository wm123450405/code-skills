# 风险分析 — REQ-00035

版本:V0.0.3
更新时间:2026-06-15 19:15

## 异常处理

| 异常路径 | 处理策略 | 监控指标 |
| --- | --- | --- |
| AI 过度"不生成"过程文档 | `code-check` 8.13 评审维度派生"建议改"任务(不阻断) | "建议改"任务数 |
| `process-doc-decisions.md` 写入失败 | 屏显警告,不阻断(沿用 `code-require` E-1 边界) | 写入失败计数 |
| 看板变更记录 0 追加但实际有变更 | 步骤 N 末尾兜底检查(NFR-4 强约束) | "无变更却追加"计数 |
| 历史需求重跑触发改造 | NFR-5 字节级保留(子技能不感知) | "历史需求被改"计数 |
| 模板文件不存在的派生任务 | 沿用 E-2 / E-3 边界(屏显警告,不阻断) | 模板缺失计数 |

## 安全边界

- **不触发**(本需求为元技能改,不涉及运行时安全边界)
- 静态分析:无敏感数据;无运行时输入校验需求

## 性能与资源

- **不触发**(本需求为元技能改,不涉及运行时性能)
- 静态分析:7 个 SKILL.md 各加 ~30 行(共 ~210 行)+ 5 模板各 ~50 行(共 ~250 行)+ 2 SKILL.md 改写 ~10 行(共 ~20 行)= **~480 行新增**
- Token 消耗节省预估:基于 V0.0.3 既有 15 需求均值 750 行过程文档,目标减少 30% → 单需求节省 ~225 行 / ~7.5K tokens

## 回退策略

- **触发条件**:过程文档自适应生成改造后,某需求的过程文档生成异常(空文件 / 内容不全)
- **步骤**:
  1. `code-check` 8.13 评审派生"建议改"任务
  2. 用户接受"建议改" → 派生"必须改"任务 → `code-it` 修复
  3. 严重时 → `git revert` 本需求所有 commit(沿用 REQ-00030 评审约束)
- **验证**:对 REQ-00035 本身走完整 `code-auto` 流程(本轮正在执行)

## 测试要点

### 静态测试
- `grep` 确认 frontmatter L1-3 字节级保留
- `grep` 确认 7 步状态机 ASCII/Mermaid 图字节级保留
- `Read` 8.1-8.12 维度确认字节级保留
- `Read` "## 解析锚点"小节确认纯追加

### 端到端测试
- 对 REQ-00035 走完整 `code-auto` 流程(本轮)
- 验证:7 任务全部完成 + code-check 12+1 维度全过 + 0 必须改 + 0 派生任务

### 回归测试
- 对 V0.0.3 既有 15 个需求(REQ-00020~REQ-00034),`code-check` 评审时**不**触发 8.13(NFR-5 字节级保留)
- 验证:`code-dashboard` 解析既有看板 `RESULT.md` 仍能正常输出
