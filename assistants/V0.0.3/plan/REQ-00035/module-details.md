# 模块详细化 — REQ-00035

版本:V0.0.3
更新时间:2026-06-15 19:15

## 模块:code-require 步骤 0a 过程文档判定

- **路径**:`plugins/code-skills/skills/code-require/SKILL.md`
- **关键类/函数**:N/A(markdown 文件,无类/函数)
- **关键变更锚点**:`## 工具使用约定` 段后 + `## 工作流程` 段前
- **新增内容**:
  - 小节标题:`## 过程文档自适应判定(本需求 REQ-00035 新增,2026-06-15 起生效)`
  - 引用 `process-doc-decisions.md` 数据结构(§5.2 算法)
  - 引用 §6.1 准则(5 类过程文档判定规则)
  - 示例伪代码(沿用 design §5.1)
- **调用顺序**:
  1. 步骤 0(版本检测)完成后
  2. → 步骤 0a(过程文档判定,新增):读上游输入 → 对每类过程文档按 §6.1 准则判定 → 写 process-doc-decisions.md(若有不生成)
  3. → 步骤 1(收集需求编码)— 既有
- **状态归属**:N/A(无状态)
- **与概要设计的对应**:§4 M1-code-require + §10 状态机
- **符合的规范**:`skill-conventions.md`(锚点约定)

## 模块:code-design 步骤 0a.5 过程文档判定

- **路径**:`plugins/code-skills/skills/code-design/SKILL.md`
- **关键变更锚点**:`## 工具使用约定` 段后 + `## 工作流程` 段前
- **新增内容**:同 T-001 结构,引用 §6.2 准则(7 类过程文档)
- **调用顺序**:步骤 0(版本检测) → 步骤 0b 设计目标确认(既有) → 步骤 0b.0 调用上下文检测(既有) → 步骤 0a.5(过程文档判定,新增) → 步骤 1 — 既有
- **与概要设计的对应**:§4 M1-code-design

## 模块:code-plan 步骤 0a.5 过程文档判定

- **路径**:`plugins/code-skills/skills/code-plan/SKILL.md`
- **关键变更锚点**:`## 工具使用约定` 段后 + `## 工作流程` 段前
- **新增内容**:同 T-001,引用 §6.3 准则(8 类过程文档)
- **调用顺序**:步骤 0(版本检测) → 步骤 0b.0 调用上下文检测(既有) → 步骤 0a.5(过程文档判定,新增) → 步骤 0b 设计目标确认(既有) → 步骤 1 — 既有
- **与概要设计的对应**:§4 M1-code-plan

## 模块:code-it 步骤 0a 任务级过程文档判定

- **路径**:`plugins/code-skills/skills/code-it/SKILL.md`
- **关键变更锚点**:`## 工具使用约定` 段后 + `## 工作流程` 段前
- **新增内容**:同 T-001,引用 §6.4 准则(4 类任务级过程文档 + 看板变更记录)
- **调用顺序**:步骤 0(版本检测) → 步骤 0a(过程文档判定,新增) → 既有 8a 前置任务守卫(步骤 8a 沿用) → 步骤 1-N — 既有
- **与概要设计的对应**:§4 M1-code-it

## 模块:code-check 步骤 0a + 8.13 评审维度

- **路径**:`plugins/code-skills/skills/code-check/SKILL.md`
- **关键变更锚点**:
  - 锚点 1:`## 工具使用约定` 段后 + `## 工作流程` 段前(步骤 0a)
  - 锚点 2:`### 8.12 行数比例警告` 段后(8.13 新增)
- **新增内容**:
  - 步骤 0a:同 T-001,引用 §6.5 准则(3 类评审级过程文档 + 看板变更记录)
  - 8.13 过程文档适配性:派生"建议改"任务(不阻断),引用 §7.1 评审维度定义
- **调用顺序**:
  - 启动:步骤 0 → 步骤 0a(过程文档判定,新增) → 既有 0-7
  - 步骤 8 末尾:8.1-8.12 → **8.13 过程文档适配性(新增)**
- **与概要设计的对应**:§4 M1-code-check

## 模块:code-auto 编排同步

- **路径**:`plugins/code-skills/skills/code-auto/SKILL.md`
- **关键变更锚点**:`## 子技能调用表` 表格的"备注"列
- **新增内容**:
  - 子技能调用表"备注"列加 1 行(对每个子技能调用行):
    ```
    过程文档自适应:已纳入(沿用本技能约定)
    ```
- **不修改**:
  - 7 步状态机(NFR-4 强约束)
  - "## 中断与异常"小节
  - 子技能调用表的其他列
- **与概要设计的对应**:§4 M2-code-auto

## 模块:code-dashboard 解析兼容

- **路径**:`plugins/code-skills/skills/code-dashboard/SKILL.md`
- **关键变更锚点**:`## 解析锚点` 小节末尾
- **新增内容**:
  - 1 条说明(纯追加,~3 行):
    ```
    - **变更记录行数自适应**(本需求 REQ-00035 起生效):
      - 空表 / 短表 / 长表均能正常解析
      - 沿用 `^## .*$` 定位 + `^\| .* \|$` 匹配锚点(零字段扩展)
    ```
- **不修改**:
  - "## 解析锚点"小节既有内容
  - 其他区段
- **与概要设计的对应**:§4 M3-code-dashboard
