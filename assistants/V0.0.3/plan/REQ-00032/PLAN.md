# 编码计划 — REQ-00032(优化 /code-require 登记结束后报告)

- 需求编码:REQ-00032
- 所属版本:V0.0.3
- 文档创建时间:2026-06-12 16:35
- 最近更新:2026-06-12 16:52
- 文档状态:已完成
- 上游:详设 `./assistants/V0.0.3/plan/REQ-00032/RESULT.md`

## 1. 计划概述

本计划把"优化 /code-require 登记结束后报告(输出下一步建议)"拆分为 **1 个单任务**(`TASK-REQ-00032-00001`),沿用 `--minimal` 粒度粗化原则,合并同文件同字段的 2 处追加(步骤 10A / 10B)。

整体设计目标 = `--minimal` + 功能性=中(沿用 design)
触发/来源 = `详细设计`(全部任务,沿用 REQ-00017 强约束)
任务总数 = 1
里程碑 = 1(M1:实施完成)

## 2. 任务总览

| 任务编号 | 需求 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件 | 关联任务 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TASK-REQ-00032-00001 | REQ-00032 | 修改 | 详细设计 | [修改] code-require 步骤 10A/10B 末尾追加下一步建议段 | 待开始 | 不适用 | `plugins/code-skills/skills/code-require/SKILL.md` > §步骤 10A 段内文末 / §步骤 10B 段内文末 | — |

**统计**:
- 总数:1
- 已完成:0
- 待开始:1
- 阻塞:0
- 已取消:0
- 待重新评估:0

## 3. 任务详情

### TASK-REQ-00032-00001:`[修改] code-require 步骤 10A/10B 末尾追加下一步建议段`

#### 3.1 目标

在 `code-require/SKILL.md` 步骤 10A / 10B 段内文末各追加 1 段"### 下一步建议(本需求 REQ-00032 新增,2026-06-12 起生效)",使 code-require 在完成需求分析(首次/增量)后,屏幕输出 1 组"下一步建议":
- 微小需求 → 建议直接执行 `/code-auto`
- 其他需求 → 建议先执行 `/code-design`

#### 3.2 涉及文件(语义化定位)

- `plugins/code-skills/skills/code-require/SKILL.md`:
  - **§步骤 10A 段内文末**(在既有"向用户汇报:本次新增了哪些 FR/AC、哪些被列为待澄清、关联了哪些需求、版本看板的更新点"句号后)
  - **§步骤 10B 段内文末**(在既有"- **版本看板的同步情况**"句号后)

#### 3.3 关键变更(精确到锚点)

**变更 1:步骤 10A 段内文末追加**
- **目标位置**:`code-require/SKILL.md` > §步骤 10A 段内文末
- **已有结构**:"向用户汇报:本次新增了哪些 FR/AC、哪些被列为待澄清、关联了哪些需求、版本看板的更新点"(字节级保留)
- **新增内容**:1 段"### 下一步建议(本需求 REQ-00032 新增,2026-06-12 起生效)"(约 6 行)

**变更 2:步骤 10B 段内文末追加**
- **目标位置**:`code-require/SKILL.md` > §步骤 10B 段内文末
- **已有结构**:"- **版本看板的同步情况**"(字节级保留)
- **新增内容**:1 段"### 下一步建议(本需求 REQ-00032 新增,2026-06-12 起生效)"(约 6 行)

**新增内容模板**(2 段内容完全相同,因触发条件由 isTiny 决定):
```markdown
### 下一步建议(本需求 REQ-00032 新增,2026-06-12 起生效)

> **本段职能**(FR-3 强约束,2026-06-12 起生效):在 code-require 完成需求分析(首次/增量)后,屏幕输出"下一步建议",引导用户选择后续路径:
> - 微小需求(AI 自主判定) → 直接执行 `/code-auto`(跳过独立的概设/详设,直接进入自动流水线)
> - 其他需求 → 先执行 `/code-design`(沿用既有主流程)
>
> **判定启发式**(FR-1 推荐判据,非强制阈值):
> 1. 需求材料数量 ≤ 1
> 2. FR(功能需求)数量 ≤ 2
> 3. AC(验收标准)数量 ≤ 5
> 同时满足 → `isTiny = true`,否则 `isTiny = false`;AI 可基于上下文调整;失败降级 → `isTiny = false`
>
> **屏幕输出契约**(FR-3.1 / FR-3.2,字符数 ≤ 80 字/行,行数 ≤ 2):
> - FR-3.1(微小):`→ 下一步建议:本需求判定为"微小需求",建议直接执行 \`/code-auto\` 完成开发任务` + `提示:code-auto 会自动跳过独立的概设/详设步骤,直接进入编码+评审流水线`
> - FR-3.2(其他):`→ 下一步建议:本需求判定为"非微小需求",建议先执行 \`/code-design\` 概设` + `提示:概设完成后,code-plan → code-it → code-check 仍按既有主流程推进`
>
> **不修改**:`require/<REQ>/RESULT.md` 文档结构(FR-2);`templates/requirements.md` 模板(NFR-2);`isTiny` 不持久化(FR-2)
```

> **格式说明**:以上"模板"是建议写法;`code-it` 实施时**可**根据 SKILL.md 既有风格调整段落排版(表格 / 引用块 / 列表),但**必须**保留 4 个核心要素:(1) FR-3.1 / FR-3.2 屏幕输出契约原文;(2) 判定启发式 3 条;(3) 不修改 RESULT.md 声明;(4) `本需求 REQ-00032 新增,2026-06-12 起生效` 标注(REQ-00031 沿用模式)

#### 3.4 边界与异常

- **不动 frontmatter**(INV-1)
- **不动既有"向用户汇报"段字节**(INV-2)
- **不动其他 9 个 `code-*` 技能 SKILL.md**(INV-4)
- **不动 7 个项目级规范**(INV-5)
- **不动 4 个 README/marketplace/plugin/CLAUDE**(INV-6)
- **不动既有 12 个 REQ 的 RESULT.md**(INV-7)
- **不动 `templates/requirements.md` 模板**(NFR-2)
- **不持久化 `isTiny` 到任何文档**(FR-2 + NFR-1)
- **不联动 `/code-unit`**(Q-4 锁定 A;沿用 REQ-00031 FR-6)

#### 3.5 验证手段(任务完成定义 3 子句)

**子句 1:SKILL.md 修改正确**
- `Read` 修改后的 `code-require/SKILL.md` 步骤 10A / 10B 段内文末
- 验证:每段含 FR-3.1 / FR-3.2 屏幕输出契约 + 判定启发式 + 不修改 RESULT.md 声明 + REQ-00032 标注

**子句 2:INV 字节级保留(7 项 git diff 校验)**
- `git diff HEAD~1..HEAD -- plugins/code-skills/skills/code-require/SKILL.md`:只显示 2 处段内文末追加
- `git diff HEAD~1..HEAD -- plugins/code-skills/skills/`:只显示 `code-require/SKILL.md` 1 个文件
- `git diff HEAD~1..HEAD -- ./assistants/rules/`:0 命中
- `git diff HEAD~1..HEAD -- ./assistants/V0.0.3/require/`:0 命中
- `git diff HEAD~1..HEAD -- ./plugins/code-skills/CLAUDE.md ./plugins/code-skills/README*.md ./plugins/code-skills/.claude-plugin/plugin.json`:0 命中
- frontmatter L1-3 (`---\nname: code-require\ndescription: ...\n---`) 字节级保留
- 既有"向用户汇报"段(步骤 10A / 10B)字节级保留

**子句 3:18 AC 通过(手工目检)**
- AC-1.x(4 条):isTiny 判定逻辑
- AC-2.x(5 条):屏幕日志输出 + 字符数 + 二选一 + 不修改 RESULT.md + 不用指代词
- AC-3.x(6 条):零变更校验(INV-1/2/4/5/6/7)
- AC-4.x(4 条):与既有规则协同(沿用 REQ-00031 / 不收集单测 / 不改模板 / 不增字段)

#### 3.6 回退方式

- **触发条件**:实施后 SKILL.md 解析失败 / 屏幕输出格式错误 / 18 AC 中 1 条不通过
- **步骤**:`git revert <commit_hash>` + 重新评估
- **验证**:启动 `code-require` 不报错;步骤 10A / 10B 末尾**不**输出"### 下一步建议"

## 4. 任务依赖图

```
TASK-REQ-00032-00001(无前置任务,起点)
```

本任务无依赖;按既有序号分配 `00001`。

## 5. 里程碑

### M1:实施完成

- **任务**:TASK-REQ-00032-00001
- **通过条件**:3 子句全部满足(SKILL.md 修改正确 + INV 字节级保留 + 18 AC 通过)
- **关闭时间**:任务开发状态推进到 `已完成` 时

## 6. 状态管理规则

- **开发状态**(沿用 6 枚举):`待开始` / `进行中` / `已完成` / `已取消` / `阻塞` / `待重新评估`
- **测试状态**(沿用 REQ-00031 FR-3 收窄为 2 枚举):`已运行-通过` / `不适用`
- **本任务初值**:开发=`待开始`,测试=`不适用`
- **真正可发布**:开发=`已完成` ∧ 测试=`不适用`(沿用 REQ-00031 D-4 双状态语义)
- **看板推进职责**:`/code-it` 末尾兜底后 P-1 小步自行推进(沿用既有);**不**通过"更新看板"派生任务

## 7. 关联计划

| 需求 | 关联点 | 影响 |
| --- | --- | --- |
| REQ-00030 | 12 维度评审 + INV 字节级保留 | 沿用(本任务评审时 10 INV 校验) |
| REQ-00031 | 元技能改路径(本任务同构) | 沿用("步骤末尾追加"模式) |

## 8. 变更记录

```
2026-06-12 16:52  设计新增  REQ-00032 详细设计与编码计划完成(共 1 个任务,开发=待开始,测试=不适用);整体=--minimal + 功能性=中(沿用 design);0 单列"编译运行检测"任务(沿用 REQ-00031 FR-1 内化);0 单元测试任务(沿用 REQ-00031 FR-6 外移);0 数据结构变更;1 里程碑(M1:实施完成);design/plan ratio = 1.08 ≤ 1.2 通过  REQ-00032
```
