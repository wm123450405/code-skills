# 数据结构完整变更 — REQ-00032

更新时间:2026-06-12 16:44
版本:V0.0.3

## 摘要

**本需求 0 数据结构变更**。

| 操作 | 实体 | 字段 | 索引 | 迁移 |
| --- | --- | --- | --- | --- |
| 新增 | (无) | — | — | — |
| 修改 | (无) | — | — | — |
| 删除 | (无) | — | — | — |

## 1. 不新增实体

本需求**不新增**任何实体 / 表 / 字段 / 索引。原因:

- 屏幕日志输出是无 I/O 语义的 stdout
- `isTiny` 是运行时内存变量,不持久化(FR-2 + NFR-1 锁定)
- 改造文件是 SKILL.md 文档(无数据结构)

## 2. 不修改既有实体

本需求**不修改**任何既有实体 / 表 / 字段。原因:

- 屏幕日志建议不涉及任何数据存储
- `code-require/SKILL.md` 是文档,不是数据模型
- 既有 12 个 REQ 的 RESULT.md 0 改(INV-7)

## 3. 内部变量(非持久化,非数据模型)

| 变量 | 类型 | 作用域 | 来源 | 持久化 |
| --- | --- | --- | --- | --- |
| `isTiny` | `boolean` | 内存(本轮) | AI 自主判定 | **否** |

> 注:`isTiny` **不**是数据模型,只是运行时内存变量。本节列出仅为完整性,实际"0 数据结构变更"。

## 4. 存储选型

不适用(本需求 0 数据持久化)。

## 5. 迁移脚本

不适用(本需求 0 数据迁移)。

## 6. 与既有实体的关系

不适用(本需求不涉及任何实体)。

## 7. 依据规范

- `data-modeling`(无既有规范文件,本仓库无此规范)
- `encoding-conventions.md §规则 1`(5 位纯数字,本需求任务编号 `TASK-REQ-00032-00001` 沿用)
- FR-2 + NFR-1 锁定(`isTiny` 不持久化)
- INV-7(既有 12 个 REQ 的 RESULT.md 0 改)

## 8. 结论

本需求**0**数据结构变更,与概设 §5 "0 数据结构" 一致;与需求 FR-2 "不修改 RESULT.md 文档结构" 一致。
