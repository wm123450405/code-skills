# 接口详细规格 — REQ-00032

更新时间:2026-06-12 16:42
版本:V0.0.3

## 接口 1:屏幕日志"→ 下一步建议:微小路径"

- **形式**:屏幕日志(无 I/O 语义,纯 stdout)
- **路径/签名**:无
- **触发位置**:`code-require` 步骤 10A / 步骤 10B 末尾,`isTiny = true` 时
- **入参**:无(无参)
- **出参**:
  ```
  → 下一步建议:本需求判定为"微小需求",建议直接执行 `/code-auto` 完成开发任务
    提示:code-auto 会自动跳过独立的概设/详设步骤,直接进入编码+评审流水线
  ```
- **错误码**:不适用(无异常)
- **示例**:
  - 正常:步骤 10A 完成后,屏幕输出上述 2 行
  - 异常:不适用
- **版本策略**:不涉及(无 API 版本)
- **兼容策略**:不涉及(无外部消费方)
- **依据规范**:REQ-00013(屏显格式契约)
- **字节级保留(INV-10)**:
  - 中点 `→`(U+2192):1 字符,起首
  - `/code-auto`:9 字符,1 个反引号包裹
  - 中文标点 `"微小需求"`(U+201C / U+201D):字节级保留
  - 字符数:第 1 行 45 字 / 第 2 行 36 字

## 接口 2:屏幕日志"→ 下一步建议:其他路径"

- **形式**:屏幕日志(无 I/O 语义,纯 stdout)
- **路径/签名**:无
- **触发位置**:`code-require` 步骤 10A / 步骤 10B 末尾,`isTiny = false` 时
- **入参**:无(无参)
- **出参**:
  ```
  → 下一步建议:本需求判定为"非微小需求",建议先执行 `/code-design` 概设
    提示:概设完成后,code-plan → code-it → code-check 仍按既有主流程推进
  ```
- **错误码**:不适用(无异常)
- **示例**:
  - 正常:步骤 10A 完成后,屏幕输出上述 2 行
  - 异常:不适用
- **版本策略**:不涉及
- **兼容策略**:不涉及
- **依据规范**:REQ-00013(屏显格式契约)
- **字节级保留(INV-10)**:
  - 中点 `→`(U+2192):1 字符,起首
  - `/code-design`:12 字符,1 个反引号包裹
  - `/code-plan` / `/code-it` / `/code-check`:字节级保留
  - 中文标点 `"非微小需求"`(U+201C / U+201D):字节级保留
  - 字符数:第 1 行 45 字 / 第 2 行 36 字

## 内部变量:`isTiny`

- **形式**:内存变量(本技能本轮)
- **类型**:`boolean`
- **作用域**:本轮 `code-require` 执行
- **来源**:AI 自主判定(FR-1)+ determineIsTiny 算法(module-details §算法 1)
- **取值**:`true` / `false`(无第三种状态)
- **持久化**:**不**写入 `require/<REQ>/RESULT.md` / `require/<REQ>/analysis-notes.md` / 任何其他文档
- **依据规范**:FR-2(不修改 RESULT.md 文档结构)+ NFR-1(零文档变更)

## 契约边界表

| 接口 | 调用方 | 触发频次 | 错误降级 |
| --- | --- | --- | --- |
| 接口 1(微小路径) | code-require 步骤 10A / 10B | 每轮 1 次 | 不适用(纯 stdout) |
| 接口 2(其他路径) | code-require 步骤 10A / 10B | 每轮 1 次 | 不适用(纯 stdout) |
| 内部变量 isTiny | code-require 步骤 10A / 10B | 每轮 1 个 | determineIsTinySafe 失败降级 → false |

## 关键约束

- 屏幕输出**单向**(无返回值被消费方使用)
- **不**写入任何文件
- **不**触发任何回调 / 事件 / IPC
- **不**跨进程 / 跨技能调用
- **不**修改任何全局状态
