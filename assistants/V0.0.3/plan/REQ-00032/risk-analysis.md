# 风险分析 — REQ-00032

更新时间:2026-06-12 16:46
版本:V0.0.3

## 1. 异常处理

| 异常路径 | 描述 | 处理策略 | 监控指标 |
| --- | --- | --- | --- |
| E-1 | 材料数为 0 | `isTiny = false` → 走 /code-design 建议 | 不适用(无监控) |
| E-2 | FR 数为 0 | 沿用 code-require 既有"中止"逻辑(本需求不介入) | 不适用 |
| E-3 | code-require 重入 | `isTiny` 不累积(沿用 NFR-4) | 不适用 |
| E-4 | 增量更新(步骤 5B) | 同样触发 FR-1 判定,输出 FR-3 建议 | 不适用 |
| E-5 | 判定失败(异常) | `isTiny = false` 退化 → 走 /code-design 建议 | 不适用 |
| E-6 | 用户在建议前中断 | 屏幕日志建议不输出(沿用既有"中止"逻辑) | 不适用 |
| E-7 | 本仓库无项目级规范 | 沿用 code-require 既有 AskUserQuestion(本需求不介入) | 不适用 |

## 2. 安全边界

| 边界 | 评估 |
| --- | --- |
| 鉴权要求 | 不适用(无用户身份认证) |
| 输入校验 | 不适用(无外部输入) |
| 敏感数据处理 | 不适用(无敏感数据) |
| 审计日志 | 不适用(无审计需求) |
| 注入防护 | 不适用(无 SQL / NoSQL / 命令 / 反序列化) |
| 加密 | 不适用 |
| 跨站脚本(XSS) | 不适用(屏幕 stdout,非 Web) |
| 依赖已知 CVE | 不适用(0 新增依赖) |

**安全结论**:本需求 0 安全风险。

## 3. 性能与资源

### 3.1 关键路径预估

- **总耗时**:< 1ms(纯字符串拼接 + 内存变量判定)
- **I/O**:0
- **CPU**:可忽略
- **内存**:+2 行字符串(单次输出)

### 3.2 资源限制

- 无网络资源
- 无磁盘资源
- 无外部依赖

### 3.3 缓存策略

不适用(无缓存需求)。

## 4. 回退策略

### 4.1 触发条件

- 实施后 `code-require` 步骤 10A / 10B 末尾"### 下一步建议"段导致 SKILL.md 解析失败
- 实施后 `code-require` 屏幕输出格式错误(字符数超限 / 路径串错乱)

### 4.2 回退步骤

1. `git revert <commit_hash>` 回退本次提交
2. 验证 `code-require` 恢复原行为
3. 重新评估改造方案

### 4.3 回退验证

- 启动 `code-require` 不报错
- `code-require` 步骤 10A / 10B 末尾**不**输出"### 下一步建议"
- 18 AC 不再要求通过(本需求是新增,回退后无新增)

### 4.4 风险评估

- 回退难度:**低**(1 个 commit revert 即可)
- 业务影响:**无**(本需求是元技能改,不改业务逻辑)
- 数据影响:**无**(0 数据变更)

## 5. 测试要点

### 5.1 单元测试

不适用(本仓库无测试框架,沿用 REQ-00031 NFR-2)。

### 5.2 手工目检范围(本需求实际测试方法)

#### 5.2.1 AC-1.x 微小需求判定(4 条)

- **AC-1.1** 当材料数 = 0 → `isTiny = false`
  - 验证:模拟材料 = [],frs = [1],acs = [1] 调用 `determineIsTiny` → False
- **AC-1.2** 当材料数 ≤ 1 + FR ≤ 2 + AC ≤ 5(同时满足) → `isTiny = true`
  - 验证:模拟 materials = ['doc.md'],frs = [FR-1, FR-2],acs = [AC-1, AC-2] → True
- **AC-1.3** 当 FR 数 ≥ 3 → `isTiny = false`
  - 验证:frs = [FR-1, FR-2, FR-3] → False
- **AC-1.4** 当 AC 数 ≥ 6 → `isTiny = false`
  - 验证:acs = [AC-1..AC-6] → False

#### 5.2.2 AC-2.x 屏幕日志输出(5 条)

- **AC-2.1** 微小路径(`isTiny = true`)→ 输出 FR-3.1 建议 2 行
- **AC-2.2** 其他路径(`isTiny = false`)→ 输出 FR-3.2 建议 2 行
- **AC-2.3** 每次只输出一组建议(不同时输出 2 条)
- **AC-2.4** 不修改 `RESULT.md` 文档结构(`git diff` 校验)
- **AC-2.5** 不在屏幕日志使用"本需求"等指代词(改为"本需求"/"已分析需求"等明确指代;本需求屏幕日志使用"本需求"作为指代)

#### 5.2.3 AC-3.x 零变更校验(6 条)

- **AC-3.1** `code-require/SKILL.md` frontmatter L1-3 字节级保留(`git diff` 校验)
- **AC-3.2** `code-require/SKILL.md` 既有"## 工作流程"小节 0 改(`git diff` 校验)
- **AC-3.3** 既有 7 个项目级规范 0 改(`git diff ./assistants/rules/` 校验)
- **AC-3.4** 4 个 README/marketplace/plugin/CLAUDE 0 改(`git diff` 校验)
- **AC-3.5** 既有 12 个 REQ 的 RESULT.md 0 改(`git diff ./assistants/V0.0.3/require/` 校验)
- **AC-3.6** 其他 9 个 `code-*` 技能 SKILL.md 0 改(`git diff ./plugins/code-skills/skills/` 校验)

#### 5.2.4 AC-4.x 与既有规则协同(4 条)

- **AC-4.1** 沿用 REQ-00031 元技能改规则:`code-auto` 步骤 4.b 恒等跳过
- **AC-4.2** 不收集"本项目是否需要单测"偏好(Q-4 锁定 A)
- **AC-4.3** 不修改 `templates/requirements.md` 模板(NFR-2)
- **AC-4.4** 不在 RESULT.md 新增 `isTiny` 字段(FR-2)

### 5.3 集成测试

不适用(本需求是 1 个单文件文字追加,无跨模块集成)。

### 5.4 端到端测试

不适用(本需求无端到端流程)。

### 5.5 性能/安全测试

不适用(无性能 / 安全考量)。

## 6. 风险总结

| 风险类别 | 等级 | 缓解措施 |
| --- | --- | --- |
| 功能风险 | 低 | 18 AC 手工目检 |
| 安全风险 | 无 | 0 网络 / 0 I/O / 0 外部输入 |
| 性能风险 | 无 | < 1ms 耗时 |
| 兼容性风险 | 低 | 既有 9 个 `code-*` 技能 SKILL.md 0 改 |
| 数据风险 | 无 | 0 数据变更 |
| 回退难度 | 低 | git revert 即可 |
| **整体** | **低** | — |
