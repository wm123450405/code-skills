# 设计笔记 — REQ-00032

更新时间:2026-06-12 16:38
版本:V0.0.3

## 1. 关键设计问题清单

| # | 问题 | 候选方案 | 选定 | 依据 |
| --- | --- | --- | --- | --- |
| Q1 | 任务拆分数? | A. 2 任务(按 10A / 10B 拆);B. 1 任务(整体) | **B. 1 任务** | `--minimal` 粒度粗化;P-D3 锁定:同文件同字段合并;2 段"### 下一步建议"是同构修改,合并后 0 信息丢失 |
| Q2 | 任务类型? | A. 修改;B. 新增;C. 文档 | **A. 修改** | 改造既有 `code-require/SKILL.md`,不是新文件/新接口 |
| Q3 | 触发/来源? | A. 详细设计;B. 需求新增;C. 主动优化 | **A. 详细设计** | 任务总览"触发/来源"列 = `详细设计`(沿用 REQ-00017 强约束) |
| Q4 | 测试状态? | A. 未编写;B. 不适用;C. 已运行-通过 | **B. 不适用** | 本仓库无测试框架(沿用 REQ-00031 NFR-2);FR-3 收窄为 2 个值:已运行-通过 / 不适用 |
| Q5 | 任务完成定义? | A. 屏幕日志 2 路径输出正确;B. SKILL.md 修改字节级符合 INV;C. 18 AC 全通过 | **A+B+C 全部**(3 子句) | 任务完成定义内化编译/运行(沿用 REQ-00031 FR-1);本需求是元技能文字修订,"编译/运行"对应"字符级字节级符合 INV" |

## 2. 候选方案(关键问题展开)

### Q1 候选展开:任务拆分数

- **方案 A(被否决)**:2 任务(10A / 10B 各自 1 任务)
  - 优点:结构对称,易于跟踪
  - 缺点:**过度拆分**(违反 REQ-00014 "0.5–2 天可完成");2 任务各自 1 段追加 < 1 小时工作量
- **方案 B(选定)**:1 任务(整体)
  - 优点:粒度合理(0.5–2 天可完成);2 段追加 + INV 校验 + 18 AC 目检 ≈ 1 小时工作量,但合并为 1 任务更符合"按功能点拆分"原则
  - 缺点:无明显缺点

### Q4 候选展开:测试状态

- **方案 A(被否决)**:未编写
  - 缺点:本仓库无测试框架,无法"编写"测试
- **方案 B(选定)**:不适用
  - 优点:与 REQ-00031 FR-3 收窄一致(2 个值:已运行-通过 / 不适用);本需求"测试"由"手工目检 18 AC"承担
- **方案 C(被否决)**:已运行-通过
  - 缺点:无自动化测试,无法声称"已运行-通过"

## 3. 选定方案与理由(整体)

### 3.1 方案:1 个单任务(沿用 `--minimal` 粒度粗化)

- **任务编号**:`TASK-REQ-00032-00001`
- **任务标题**:`[修改] code-require 步骤 10A/10B 末尾追加下一步建议段`
- **任务类型**:修改
- **触发/来源**:详细设计
- **关联任务**:无
- **涉及文件**:`plugins/code-skills/skills/code-require/SKILL.md` > 步骤 10A 段内文末 / 步骤 10B 段内文末
- **测试状态**:不适用(本仓库无测试框架,沿用 REQ-00031 FR-3)

### 3.2 任务完成定义(3 子句,内化字节级校验)

1. **SKILL.md 修改正确**:`code-require/SKILL.md` 步骤 10A / 10B 段内文末各追加 1 段"### 下一步建议(本需求 REQ-00032 新增,2026-06-12 起生效)",每段含 isTiny 判定 + FR-3.1 / FR-3.2 模板
2. **INV 字节级保留**:`git diff` 校验 frontmatter L1-3 / 既有 9 个 `code-*` 技能 SKILL.md / 7 个项目级规范 / 既有 12 个 REQ 的 RESULT.md / 4 个 README / 0 三方依赖 全部 0 改
3. **18 AC 通过**:手工目检 18 条 AC(AC-1.x 4 条 + AC-2.x 5 条 + AC-3.x 6 条 + AC-4.x 4 条)全部通过

## 4. 备选方案被否决的理由

| 备选 | 否决理由 |
| --- | --- |
| 2 任务(按 10A / 10B 拆) | 过度拆分(违反 REQ-00014 "0.5–2 天可完成") |
| 类型 = 新增 | 错误:改造既有 SKILL.md,不是新文件 |
| 触发/来源 = 需求新增 | 错误:任务总览"触发/来源"列 = `详细设计`(沿用 REQ-00017) |
| 测试状态 = 未编写 | 错误:本仓库无测试框架 |
| 测试状态 = 已运行-通过 | 错误:无自动化测试 |

## 5. 关键决策汇总

| 决策 | 内容 | 依据 |
| --- | --- | --- |
| **D-1** | 任务拆分数 = 1 | Q1 选定 B;`--minimal` 粒度粗化 |
| **D-2** | 任务类型 = 修改 | Q2 选定 A;改造既有 SKILL.md |
| **D-3** | 触发/来源 = 详细设计 | Q3 选定 A;沿用 REQ-00017 强约束 |
| **D-4** | 测试状态 = 不适用 | Q4 选定 B;沿用 REQ-00031 FR-3 收窄 |
| **D-5** | 任务完成定义 = SKILL.md + INV + 18 AC(3 子句) | Q5 选定 A+B+C;沿用 REQ-00031 FR-1"任务完成定义内化" |
| **D-6** | 任务编号 = TASK-REQ-00032-00001 | 5 位纯数字生成端(沿用既有 12 个 REQ 的 5 位序号) |
| **D-7** | 关联任务 = 无 | 本需求是独立元技能改,无前置任务 |
| **D-8** | 里程碑 = M1(实施完成) | 1 任务,单里程碑即可 |
