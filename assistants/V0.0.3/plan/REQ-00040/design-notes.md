# 设计笔记 — REQ-00040

更新时间:2026-06-25
版本:V0.0.3

## 1. 详设 vs 概设的职责边界(沿用 REQ-00011 收紧)

| 维度 | 概设(`design/.../RESULT.md`) | 详设(`plan/.../RESULT.md`,本文件) |
| --- | --- | --- |
| 模块拆分 | ✓ 列出 4 模块 | ✓ 列出 + 每个模块的**关键类/函数/调用顺序/状态归属** |
| 接口概要 | ✓ 列 5 个内部伪代码接口 | ✓ 列 + **完整签名 + 入参/出参 + 错误码 + 示例** |
| 数据结构概要 | ✓ 列 3 数据结构 | ✓ 列 + **完整字段类型 + 约束 + 索引 + 元信息** |
| 算法 | 概要级(6 步探测 + 9 步复现) | **可直接编码的伪代码**(本文件 §5) |
| 异常路径 | 列 11 个边界 | 列 + **每个边界的处理策略 + 监控指标** |
| 测试策略 | N/A(本仓库 0 测试框架) | ✓ 列**测试要点**(静态校验 + 端到端降级) |

## 2. 关键详设决策(本轮新定,沿用 D-1~D-10 + 4 项新增)

### 2.1 详设决策(本轮新增)

| 决策 ID | 决策 | 依据 |
| --- | --- | --- |
| **PD-1** | `detectStartability(cwd)` 探测结果**不**写入文件,只存内存上下文 | 避免"空跑时也写产物" 的副作用(沿用上游 FR-1 第 7 步) |
| **PD-2** | `reproduceBug()` 在 `code-fix` 主进程 cwd 下**用 Bash 调 `&`** 启动子进程,**不**改 cwd | NFR-9(不修改项目配置);不污染仓库 cwd |
| **PD-3** | `reproduceBug()` 用 `kill -SIGTERM 5s 后 kill -SIGKILL` 终止子进程 | Linux 进程终止惯例;5s 给程序 graceful shutdown 时间 |
| **PD-4** | `run.log` 时间戳前缀格式 `YYYY-MM-DD HH:mm:ss`(24h 制,秒级精度) | 沿用 `bug.md` 模板的"报告时间" 字段格式;便于 grep |
| **PD-5** | `RESULT-meta.json` 用**单行 JSON**(无缩进) | 减小文件大小(产物可能多);便于 `code-plan` / `code-check` 读 |
| **PD-6** | 截图工具探测**只**在"启动 web 服务" 时触发(N=0 跳过) | 避免给命令行程序也尝试截图(避免无关探测) |
| **PD-7** | 交互数据采集**只**在"启动 web/api 服务" 时触发(N=0 跳过) | 同 PD-6 |
| **PD-8** | `reproduce/` 子目录**不**加 `.gitignore` | 沿用 NFR-9;产物入 git 便于审计追溯;用户后续可手动调整 |

### 2.2 沿用概设决策(字面未变)

| 决策 ID | 字面 | 详见 |
| --- | --- | --- |
| D-1 | 启动能力自动探测 | `design/.../RESULT.md §3.1` |
| D-2 | 产物放 `fix/<BUG-NNN>/reproduce/` 子目录 | `design/.../RESULT.md §3.1` |
| D-3 | 复现动作不触发状态推进 | `design/.../RESULT.md §3.1` |
| D-4 | 失败降级链 | `design/.../RESULT.md §3.1` |
| D-5 | 截图工具链式降级 | `design/.../RESULT.md §3.1` |
| D-6 | `bug.md` 新区段插入位置 | `design/.../RESULT.md §3.1` |
| D-7 | 文档头新增 2 字段 | `design/.../RESULT.md §3.1` |
| D-8 | 同步更新 `assistants-layout.md` | `design/.../RESULT.md §3.1` |
| D-9 | 不新建 `code-fix/lib/` 共享库 | `design/.../RESULT.md §3.1` |
| D-10 | `reproduce/` 不加 `.gitignore` | `design/.../RESULT.md §3.1` |

## 3. 任务拆分的设计依据

### 3.1 任务粒度判定(沿用 REQ-00014)

| 判定项 | 判定结果 |
| --- | --- |
| 整体设计目标 | `--balanced` |
| 是否需要架构任务(首个 TASK-001) | **否**(--extensible 才需要;沿用 SKILL.md 步骤 10A "## 按'## 设计目标'小节调整任务粒度" 段) |
| 0.5~2 天可完成 | ✓(每个任务 < 1 天) |
| 1 任务 = 1 功能点 | ✓(每个任务 1 个具体产物) |
| 1 任务含完整"展示+逻辑+说明" | ✓(任务清单含涉及文件 + 关键变更 + 验证手段) |

### 3.2 任务类型分布

| 类型 | 任务数 | 占比 |
| --- | --- | --- |
| 新增 | 0 | 0% |
| 修改 | 4 | 67% |
| 重构 | 0 | 0% |
| 修复 | 0 | 0% |
| 文档 | 2 | 33% |
| **合计** | **6** | **100%** |

### 3.3 触发/来源分布

| 触发/来源 | 任务数 | 占比 |
| --- | --- | --- |
| 详细设计 | 6 | 100% |
| 缺陷修复 | 0 | 0% |
| 审查改修 | 0 | 0% |
| **合计** | **6** | **100%** |

(全部 = "详细设计",**不**出现"更新看板",沿用 REQ-00017 强约束)

## 4. 任务依赖关系的关键判定

- **T-001(步骤 0 末尾子节)** → T-002(步骤 6 末尾子节):强依赖(T-001 写入 `canStart` / `startCommand` 上下文,T-002 读上下文)
- **T-002(步骤 6 末尾子节)** → T-003(产物采集算法):弱依赖(T-002 内的 `reproduceBug()` 调 T-003 的 3 个采集函数)
- **T-004(bug.md 模板)** / T-005(assistants-layout.md 同步) / T-006(端到端验证):与 T-001~T-003 弱依赖(模板与算法可并行开发;T-006 必须在 T-001~T-005 全部完成后)
- **里程碑**:M1 = 全部 6 任务完成 + 端到端验证通过

## 5. 与 `code-auto` 编排的协同

- 模式 B 续跑(本需求实际):`code-require` + `code-design` 已完成,`code-plan` 产出 6 任务 → `code-it` 逐任务执行 → `code-check` 评审
- 模式 A(新需求):`code-require` 分配新编号 + 后续同样流程
- 零修改 9 个子技能 SKILL.md(沿用 `code-auto` NFR-7 强约束)
