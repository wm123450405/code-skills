# 过程文档生成决策记录 — REQ-00040

更新时间:2026-06-25
版本:V0.0.3

## 1. 决策汇总

| 过程文档 | 决策 | 判定依据 |
| --- | --- | --- |
| `materials-index.md` | **生成** | 始终生成(上游引用 + 规范登记是核心) |
| `design-notes.md` | **生成** | 始终生成(详细设计权衡笔记是核心) |
| `module-details.md` | **生成** | 模块数 = 5(模块 1-3 = SKILL.md 步骤 0/6 末尾子节 + 步骤采集;模块 4-5 = 2 模板改造)≥ 2 |
| `interface-specs.md` | **生成** | 接口数 = 5(内部伪代码函数)≥ 2 |
| `data-changes.md` | **不生成** | 本需求**不**涉及数据库 / 缓存 / 状态字段变更;纯 UI / 配置 / 文档类(仅文档头 2 字段 + 新区段) |
| `risk-analysis.md` | **生成** | 任务数 = 6(详细任务清单)≥ 3 |
| `rule-compliance.md` | **生成** | `./assistants/rules/` 存在且有 13 个文件 |
| `clarifications.md` | **不生成** | 本轮无用户问路(`code-plan` 步骤 0b 沿用 design 设计目标,跳过 7 维度 `AskUserQuestion`;`code-auto` 上下文不触发问路) |
| 看板"变更记录" | **追加** | 本轮有 1 条追加(详细设计与编码计划完成) |

## 2. 不生成项的详细理由

### 2.1 `data-changes.md` 不生成

- **依据规范**:`code-plan/SKILL.md` 步骤 6 "## 适用过程文档清单" → `data-changes.md` "涉及数据库 / 缓存 / 状态字段变更 → 生成;纯 UI / 配置 / 文档类 → 不生成"
- **本设计实际**:本需求**不**涉及数据库 schema / 缓存结构 / 状态字段变更,仅在 `bug.md` 文档头表追加 2 行(纯 Markdown 字段),属于"文档类变更"
- **沿用概设**:`design/.../RESULT.md` 中也未列出数据库 / 缓存 / 状态字段相关数据结构
- **不**生成该文档

### 2.2 `clarifications.md` 不生成

- **依据规范**:`code-plan/SKILL.md` 步骤 6 "## 适用过程文档清单" → `clarifications.md` "本轮有用户问路 → 生成;无 → 不生成"
- **本设计实际**:
 - `code-plan` 步骤 0b 沿用 `design/.../RESULT.md` 的"## 设计目标"小节(已含 `--balanced` 与 7 维度优先级),**不**触发 `AskUserQuestion`
 - `code-plan` 步骤 8A"澄清冲突与不确定项" — 0 冲突 / 0 不确定项
- **不**生成该文档

## 3. 已生成的过程文档清单

本轮共生成 **5 份**过程文档:

1. `materials-index.md` — 13 份规范摘要 + 上游需求 / 设计关键交叉点 + 项目现状锚点
2. `design-notes.md` — 8 项详设决策(PD-1~PD-8)+ 10 项沿用概设决策 + 任务拆分设计依据
3. `module-details.md` — 5 个模块详细化(SKILL.md 步骤 0 末尾 / 步骤 6 末尾 / 步骤采集 / bug.md / assistants-layout.md)
4. `interface-specs.md` — 5 个内部伪代码函数完整规格(签名 / 入参 / 出参 / 错误码 / 示例)
5. `risk-analysis.md` — 11 边界 + 1 复合边界 + 安全 / 性能 / 回退 / 测试要点

## 4. 看板"变更记录"追加

- 本轮追加 1 条:`YYYY-MM-DD HH:mm 设计新增 <REQ> 详细设计与编码计划完成(共 N 个任务) <REQ>`

## 5. 边界与异常

- **E-1**(本决策文件写入失败):不阻断主流程(已成功 Write,无失败)
- **E-2**(AI 判定犹豫):已按"适用过程文档清单"逐项判定,无犹豫项
- **E-3**(`code-auto` 上下文):本决策仍执行(子技能零修改契约不覆盖过程文档生成逻辑)
