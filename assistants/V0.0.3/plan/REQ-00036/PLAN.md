# 编码计划 — REQ-00036 — 清理 SKILL.md + templates/ 中的开发痕迹

- 需求编码:REQ-00036
- 所属版本:V0.0.3
- 详细设计:./assistants/V0.0.3/plan/REQ-00036/RESULT.md (v1)
- 状态:草稿
- **开发完成度**:0 / 3
- **测试完成度**:0 / 3(全部任务测试状态=不适用)
- 创建:2026-06-16
- 最近更新:2026-06-16 17:33
- 当前版本:v1

## 1. 计划概述

- **任务总数**:3
- **类型分布**:文档 × 3
- **关键里程碑数**:1(M1 清理完成)
- **开发完成度**:2 / 3
- **测试完成度**:2 / 3(全部任务测试状态=不适用,纯文档任务)
- **真正可发布任务数**:2 / 3(开发=已完成 ∧ 测试=不适用)

## 2. 任务总览

| 任务编号 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件/模块 | 前置任务 | 估算 | 责任人 | 关联任务 | 对应设计章节 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TASK-REQ-00036-00001 | 文档 | 需求新增 | 扫描并产出 14 技能 SKILL.md + templates/ 待清理文件表与命中计数基线 | 已完成 | 不适用 | `plugins/code-skills/skills/*/{SKILL.md, templates/*.md}`(14 SKILL.md + 33 templates = 47 文件) | — | 0.3d | 用户 | — | RESULT.md §5 算法 0 |
| TASK-REQ-00036-00002 | 文档 | 需求新增 | 应用 6 条清理规则(R-1 ~ R-6)到 T-1 清单,原地改写 + diff 校验 + 残缺回退 | 已完成 | 不适用 | 同 T-1(47 文件) | T-1 | 0.5d | 用户 | — | RESULT.md §5 算法 1-6 |
| TASK-REQ-00036-00003 | 文档 | 需求新增 | 跑 AC-1 ~ AC-8 验证 + 1 次 commit + 看板同步 + 末尾兜底 | 待开始 | 不适用 | `assistants/V0.0.3/RESULT.md` + git 仓库 | T-2 | 0.2d | 用户 | — | RESULT.md §5 算法 7 |

**字段说明**:
- 任务编号:`TASK-REQ-00036-NNNNN`(5+5 位嵌套式,沿用 `encoding-conventions §规则 1/3`)
- 类型:`文档`(本仓库纯文档任务)
- 测试状态:`不适用`(纯文档任务;沿用 REQ-00031 FR-3 + REQ-00034 FR-5,新拆分 REQ 统一填)

### 2.1 触发/来源分布
- 需求新增:3 条(本需求 REQ-00036 的 8 FR / 10 NFR 落地为 3 条文档级任务)

## 3. 任务详情

### TASK-REQ-00036-00001:[文档] 扫描并产出待清理文件表与命中计数基线

#### 基础信息
- **类型**:文档
- **触发/来源**:需求新增
- **触发任务**:—
- **开发状态**:待开始
- **目标**:用 grep 扫描 14 技能 × (SKILL.md + templates/*.md),输出"待清理文件表 + 每条规则预估命中数"基线
- **涉及文件/模块**:
  - `plugins/code-skills/skills/*/SKILL.md` × 14
  - `plugins/code-skills/skills/*/templates/*.md` × 23
  - 本任务的 `code/<TASK>/work-log.md` + `RESULT.md`
- **前置任务**:—
- **关联任务**:—
- **关键变更**:
  - 不修改任何 SKILL.md / templates/(只读)
  - 产出 1 个清单文件 + 1 个 commit
- **关键逻辑**:
  1. 步骤 1:遍历 14 技能目录,glob `SKILL.md` + `templates/*.md` → 输出文件表
  2. 步骤 2:对每条 R-1 ~ R-6 规则的正则,grep 全文件,统计命中行数
  3. 步骤 3:应用算法 0(范围过滤器)排除非目标文件
  4. 步骤 4:写 `code/<TASK>/work-log.md` + `RESULT.md`(含清单 + 命中计数)
- **边界与异常**:
  - 范围过滤误判 → 手动核对(算法 0 已收敛 5 条特例)
  - grep 0 命中 → 正常(说明该条规则无目标)
- **验证手段**:
  - 文件表与实际文件数对账(期望 37 文件)
  - 命中计数 ≥ 0(各规则独立)
- **回退方式**:无(N/A — 任务只产出清单,不修改任何文件)
- **对应设计章节**:RESULT.md §5 算法 0
- **依据规范**:`skill-conventions §规则 2`(范围定义)

#### 测试状态
- **测试状态**:不适用
- **不适用理由**:纯文档任务,本仓库无可测代码载体
- **覆盖的测试场景**:N/A

---

### TASK-REQ-00036-00002:[文档] 应用 6 条清理规则(R-1 ~ R-6)到 T-1 清单

#### 基础信息
- **类型**:文档
- **触发/来源**:需求新增
- **触发任务**:T-1
- **开发状态**:待开始
- **目标**:按 §5 算法 1-6 的精确正则,逐文件清理,确保 frontmatter / 跨技能契约 / 占位符 / 合规条款 全部保留
- **涉及文件/模块**:同 T-1(47 文件 = 14 SKILL.md + 33 templates)
- **前置任务**:T-1
- **关联任务**:—
- **关键变更**:
  - 每个被清理的文件:行级改动(R-1 ~ R-6 删除字面)
  - frontmatter L1-3 字节级保留(不动)
  - `code-fix` 技能 SKILL.md / templates/ **跳过 R-5**
- **关键逻辑**:
  1. 步骤 1:读 T-1 清单,逐文件处理
  2. 步骤 2:对每个文件依次应用 R-1 → R-2 → R-3 → R-4 → R-5 → R-6(算法 1-6 的伪代码)
  3. 步骤 3:对每个文件 `git diff --stat`,屏显改动行数
  4. 步骤 4:对每个文件,若改动后行数减少 > 30% 且无实质功能内容 → 回退该文件原状,记 `work-log.md`
  5. 步骤 5:对每个文件,`git add` 暂存
  6. 步骤 6:任务收尾,写 `code/<TASK>/work-log.md`(含每文件命中数 + 是否回退)
- **边界与异常**:
  - 任一正则应用失败 → 跳过该文件,记 `work-log.md`
  - `code-fix` 技能整体豁免 R-5(算法 5)
  - 整行被 R-1 删空 → 删行(算法 1 步骤)
  - 改动后 frontmatter 字节级变化 → 立即回退该文件(硬约束 C-1)
- **验证手段**:
  - T-1 清单的每个文件都已处理(全 47 个)
  - 每个文件的 `git diff --stat` 屏显
  - frontmatter 抽查 5 个文件确认字节级一致
- **回退方式**:
  - 单文件回退:`git checkout HEAD -- <file>`
  - 整体回退:T-3 末尾若 AC 失败 → 整体回退
- **对应设计章节**:RESULT.md §5 算法 1-6
- **依据规范**:`skill-conventions §规则 2` + REQ-00036 NFR-2/4/6

#### 测试状态
- **测试状态**:不适用
- **不适用理由**:纯文档任务
- **覆盖的测试场景**:N/A

---

### TASK-REQ-00036-00003:[文档] 跑 AC-1 ~ AC-8 验证 + 1 次 commit + 看板同步

#### 基础信息
- **类型**:文档
- **触发/来源**:需求新增
- **触发任务**:T-2
- **开发状态**:待开始
- **目标**:验证 8 条 AC 全过,1 次 `git commit`,同步 V0.0.3 看板
- **涉及文件/模块**:
  - `git` 仓库(1 次 commit)
  - `assistants/V0.0.3/RESULT.md`(看板同步)
- **前置任务**:T-2
- **关联任务**:—
- **关键变更**:
  - git:1 次 commit(commit message 形如 `chore(code-it): REQ-00036 清理 SKILL.md + templates/ 中的开发痕迹(37 文件 M 行)`)
  - 看板:`RESULT.md` "任务清单" 3 行(本计划 3 任务) + "里程碑" 1 条 + "变更记录" 1 条
- **关键逻辑**:
  1. 步骤 1:跑 AC-1 ~ AC-7 grep 验证(算法 7)
  2. 步骤 2:AC-5 人工抽查 5 个跨技能引用未坏
  3. 步骤 3:AC-7 调 `/code-dashboard` 验证可解析
  4. 步骤 4:任一 AC 失败 → 整体 `git revert` + 中止
  5. 步骤 5:全过 → `git status` 检查 + `git add` + `git commit` + 同步看板
- **边界与异常**:
  - AC-2 / AC-4 允许 ≤ 3 处例外(合规条款保留)→ 例外数 = 0 时通过;1-3 处时通过(在 `work-log.md` 列出);> 3 处 → 不通过
  - AC-7 `/code-dashboard` 失败 → 整体回退
- **验证手段**:
  - 8 条 AC 全过(算法 7)
  - 1 次 commit 落地
  - 看板同步完成
- **回退方式**:
  - AC 失败:`git revert HEAD`(NFR-10)
  - 看板同步失败:仅该步骤回退,commit 保留
- **对应设计章节**:RESULT.md §5 算法 7 + §10 状态机
- **依据规范**:REQ-00036 AC-1 ~ AC-8 + NFR-8 + NFR-9

#### 测试状态
- **测试状态**:不适用
- **不适用理由**:纯文档任务
- **覆盖的测试场景**:N/A

## 4. 任务依赖图

```mermaid
graph LR
  TASK-REQ-00036-00001[T-1 扫描] --> TASK-REQ-00036-00002[T-2 应用规则]
  TASK-REQ-00036-00002 --> TASK-REQ-00036-00003[T-3 验证+提交]
```

3 条任务严格串行(T-1 → T-2 → T-3),无并发,无分支。

## 5. 里程碑

| 里程碑 | 包含任务 | 完成定义 | 预期时间 |
| --- | --- | --- | --- |
| M1:清理完成 | T-1, T-2, T-3 | 3 任务开发状态=已完成 ∧ 测试状态=不适用;AC-1 ~ AC-8 全过(AC-2 / AC-4 允许 ≤ 3 处例外);1 次 commit 落地;`code/<TASK>/work-log.md` 完整 | 2026-06-16 |

> 里程碑的"完成定义"显式列出两轴状态要求,避免把"开发完成"误当"可发布"。

## 6. 状态管理规则

### 6.1 开发状态(主状态)
- **状态推进**:`待开始` → `进行中` → `已完成`
- **已完成不可逆**:开发状态为"已完成"的任务,其**描述/关键变更/依赖等字段不可修改**

### 6.2 测试状态(平行状态)
- **初始化**:新建任务时统一填 `不适用`(纯文档任务;沿用 REQ-00031 FR-3 + REQ-00034 FR-5)
- **不适用不可逆**:一旦标为 `不适用`,不应再变为其他值(除非业务变化重新评估)

### 6.3 任务"真正可发布"定义
```
任务真正可发布 ⟺
    开发状态 = 已完成
    ∧ 测试状态 ∈ {已运行-通过, 不适用}
```

- 本计划 3 任务的目标状态:`已完成` + `不适用`

### 6.4 状态字段更新责任分工
| 字段 | 主要更新方 | 触发时机 |
| --- | --- | --- |
| 开发状态(待开始→进行中) | `code-it` | 步骤 7 任务开始 |
| 开发状态(进行中→已完成) | `code-it` | 步骤 14 任务完成 |
| 测试状态(任意→不适用) | `code-plan` | 首次拆分(本计划 3 任务已统一) |
| 任务标题、关键变更等描述 | `code-plan` 增量更新 | 步骤 9B |

## 7. 关联计划

(无)

## 8. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-16 17:33 | v1 | 初始创建 | 完成首次详细设计与编码计划;3 任务(扫描/应用/验证)严格串行;0 新增模块/接口/数据结构/依赖;整体=--minimal,功能性=中;0 触发 §规则 1 三同步;0 派生"更新看板"任务 | 用户 |
| 2026-06-16 17:33 | v1.1 | 开发状态更新 | TASK-REQ-00036-00001 状态"待开始"→"已完成",产出 47 文件表 + 286 行命中基线;修正文件数 37→47(PLAN.md L26/L27 + 看板 L258/L259) | 用户 |
| 2026-06-16 17:33 | v1.2 | 开发状态更新 | TASK-REQ-00036-00002 状态"待开始"→"已完成",应用 6 条规则,188 总命中(15 文件被改),0 残缺回退,占位符 100% 保留,frontmatter 字节级一致,git diff +175/-175 | 用户 |