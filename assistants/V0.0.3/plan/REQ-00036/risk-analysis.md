# 风险分析 — REQ-00036

更新时间:2026-06-16 17:33
版本:V0.0.3

## 异常处理

| 异常路径 | 触发条件 | 检测手段 | 处理策略 | 监控指标 | 对应任务 |
| --- | --- | --- | --- | --- | --- |
| 规则应用失败 | 某条规则的正则抛错 | `code-it` 步骤 8 异常捕获 | 跳过该文件,记 `work-log.md` | T-2 任务行数 / T-2 应用行数 | T-2 |
| 改动后大段残缺 | 行数减少 > 30% | `git diff --stat` | 回退该文件,记 `work-log.md` | T-2 work-log | T-2 |
| frontmatter 字节级变化 | frontmatter L1-3 改动 | 抽查 5 个文件 | 立即回退该文件 | T-2 抽查 | T-2 |
| 跨技能契约被改坏 | AC-5 抽查失败 | 人工 + grep | 整体回退(`git revert`) | T-3 AC-5 | T-3 |
| AC 验证失败 | AC-1 ~ AC-8 任意失败 | 8 条 grep | 整体回退 | T-3 AC 验证 | T-3 |
| `code-dashboard` 解析失败 | NFR-9 回归 | 调 `/code-dashboard` | 整体回退 | T-3 AC-7 | T-3 |
| 看板同步失败 | `Edit` 失败 | Git 错误 | 仅该步骤回退,commit 保留 | T-3 同步 | T-3 |
| Git 命令失败 | 网络 / 凭据 | 退出码非 0 | 透传 stderr 中止 | T-2 git add / T-3 commit | T-2 / T-3 |

## 安全边界
- 鉴权:无
- 授权:无
- 输入校验:本仓库纯文本,无外部输入
- 敏感数据处理:无
- 防注入:无 SQL / NoSQL / 命令注入面
- 审计:git 记录 + commit message

## 性能与资源
- 关键路径耗时:整体 < 30 秒
- 资源限制:无
- 缓存策略:无
- 批量/异步:无
- 降级策略:无

## 回退策略

| 触发条件 | 步骤 | 验证 |
| --- | --- | --- |
| 单文件改动 > 30% | `git checkout HEAD -- <file>` + 重跑该文件 | diff 重新统计 |
| 整体 AC 失败 | `git revert HEAD` | `git log` 看回退 commit |
| 看板同步失败 | 仅该步骤回退,commit 保留;手动补 | 看板对比 |

## 测试要点
- **单元测试**:不适用(本仓库纯文档)
- **集成测试**:AC-7 — `/code-dashboard` 可运行
- **端到端测试**:AC-5 — 跨技能引用未坏
- **性能/压力测试**:不适用
- **安全测试**:不适用
- **回归测试**:AC-5 + AC-7 共同构成回归
