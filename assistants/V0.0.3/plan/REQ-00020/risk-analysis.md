# 风险分析 — REQ-00020
版本:V0.0.3

## 异常处理

- **异常路径 1**:`AskUserQuestion` 大需求 8 问时,用户取消中途 → 处理:中止 + 回写空"## 设计目标"小节(沿用 REQ-00011 E-3) → 监控:N/A(纯文档)
- **异常路径 2**:步骤归并 M-1 / M-2 / M-3 / M-4 后,后续维护者读 SKILL.md 时找不到原逻辑 → 处理:`> 引用:` 块明确指向原段位置 → 监控:0(职责清晰,引用约定)
- **异常路径 3**:`code-plan` 步骤 0b 改后略增(+0.6%)→ 处理:接受(因 7 维度扩展必要)→ 监控:N/A
- **异常路径 4**:任务粒度调整 +3 行,`code-auto` 上下文默认推荐值 = 7 维度全=中(中性默认)→ 处理:沿用 REQ-00011 Q-4 锁定 A → 监控:N/A

## 安全边界

- 鉴权要求:无(本需求纯文档重构)
- 输入校验:无
- 敏感数据处理:无
- 审计日志:无
- 依据规范:`skill-conventions §规则 1`(frontmatter 不改)

## 性能与资源

- **关键路径预估**:
  - `AskUserQuestion` 大需求 8 问耗时 < 5 分钟(沿用 REQ-00011 NFR-8)
  - SKILL.md 改后:`code-design` 526 行 / `code-plan` 1015 行(+0.6%) / `code-it` 928 行(-4.4%)
  - 单次技能调用加载行数与改前基本持平
- **资源限制**:无
- **缓存策略**:无

## 回退策略

- **触发条件**:用户要求回退 7 维度扩展 / 步骤归并
- **步骤**:
  1. 用 `git revert e69a58a` 回退本需求 commit
  2. 重新跑 `code-plan REQ-00020` 按原 4 维度 + 1 步归并
- **验证**:跑 `code-design REQ-00020` 步骤 0b 应触发 5 问(原行为)

## 测试要点

### 单元测试范围(本仓库 0 测试框架,测试状态="不适用")

- `code-design` 步骤 0b 改造验证:1-2 问,功能性=中
- `code-plan` 步骤 0b 改造验证:1-8 问,7 维度齐全
- 任务粒度调整规则 +3 行验证
- 步骤归并 M-1 / M-2 / M-3 / M-4 引用约定验证

### 集成测试范围

- `code-auto` 上下文默认推荐值验证
- 完整流程:`code-require REQ-00020` → `code-design REQ-00020` → `code-plan REQ-00020` → 实际产出 3 技能改造

### 端到端测试范围

- 跨版本迁移:在 V0.0.4(未来版本)中跑本需求已落地的 3 个 SKILL.md,确认无回归
- 多需求并发:同时跑 REQ-00020(本需求)+ REQ-00021(已落地)+ REQ-00022(未来),确认无冲突

### 性能/安全测试范围

- 屏显契约(REQ-00013 沿用):新增维度问 8 问时耗时 < 5 分钟
- 7 维度存储在 SKILL.md 顶部"## 设计目标"小节,**不**触发 `dashboard-conventions §规则 1`
