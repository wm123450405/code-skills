# 设计笔记 — REQ-00020
更新时间:2026-06-06 18:00

## 关键设计问题清单(已解决)

1. **Q-1:任务粒度调整规则 +3 行具体内容?**
   - 决定:封装性=高 → 加"封装抽象层 / 接口契约 / 扩展点";可复用性=高 → 加"抽取公共逻辑 / 归并相似代码 / 复用既有模块";可读性=高 + 非自然语言 → 加"关键代码注释 / 复杂逻辑说明"

2. **Q-2:大需求 8 问时,7 维度如何分组?**
   - 决定:整体 1 问 + 7 维度各 1 问 = 8 问(按顺序问,每轮 4 问,共 2 轮)
   - 替代:分组按"功能性 / 扩展性" / "健壮性 / 可维护性" / "封装性 / 可复用性 / 可读性"3 组 — 否决(增加交互成本,用户预期下降)

3. **Q-3:步骤归并 M-1 / M-2 / M-3 / M-4 的边界?**
   - 决定:不重写稳定章节,只在必要位置编辑;`> 引用:` 块引用同源步骤
   - 替代:抽独立 `code-plan-common-substeps.md` 文件 — 否决(触发 `module-conventions §规则 1` 引入新文件)

4. **Q-4:实际"代码改造"已落地,本计划任务的"开发状态"?**
   - 决定:全部"已完成"(实际改造在 REQ-00020 阶段 commit `e69a58a` 已完成)
   - 替代:全部"待开始" + 后续 `code-it` 重跑 — 否决(浪费 token,实际已无代码可改)

5. **Q-5:7 维度本仓库 Markdown 场景下"可读性"维度取值?**
   - 决定:"不适用"(本仓库 Markdown 自然语言,语义即注释)
   - 替代:"高" — 否决(无实际意义,会导致后续 `code-it` 加无意义注释任务)

## 候选方案(对每个关键问题)

### Q-1:任务粒度调整 +3 行,如何与既有 1 行(扩展性)整合?

- 候选 A:沿用原 1 行格式(扩展性),新加 3 行同格式 — **采纳**
- 候选 B:重写整个表格 — 否决(违反"不重写稳定章节")
- 候选 C:把扩展性 + 3 行合并为"## 架构设计目标"子节 — 否决(违反"任务粒度调整规则"既有结构)

### Q-2:大需求 `code-plan` 步骤 0b 问 8 问,如何分组?

- 候选 A:整体 1 + 7 维度 7 问(分 2 轮,每轮 4 问)— **采纳**
- 候选 B:整体 1 + 7 维度 7 问(分 1 轮 8 问)— 否决(`AskUserQuestion` 限制 4 问)
- 候选 C:整体 1 + 6 维度 6 问 = 7 问(分 2 轮,2+5)— 否决(用户预期下降)

### Q-3:任务粒度调整规则在"`code-auto` 上下文"下默认值?

- 候选 A:整体=--balanced + 7 维度全=中(中性默认)— **采纳**(沿用 REQ-00011 Q-4)
- 候选 B:整体=--extensible + 7 维度全=高(高默认)— 否决(过度设计风险)
- 候选 C:整体=--minimal + 7 维度全=低(低默认)— 否决(失去设计目标意义)

## 选定方案 + 理由

- **任务粒度 +3 行** — 沿用既有 1 行格式,新加 3 行同格式(用户原文 + FR-3)
- **大需求 8 问分 2 轮** — `AskUserQuestion` 工具限制 4 问,沿用既有 4 问/轮
- **`code-auto` 上下文 7 维度全=中** — 沿用 REQ-00011 Q-4 锁定 A "总选推荐项"
- **步骤归并用 `> 引用:` 块** — 沿用 REQ-00019 同源段引用约定
- **本计划任务"开发状态"="已完成"** — 实际改造已 commit `e69a58a`,无未完成工作
- **可读性维度="不适用"** — 本仓库 Markdown,语义即注释

## 备选方案被否决的理由

- 候选 B / 候选 C(任务粒度调整 +3 行):违反"不重写稳定章节"
- 候选 B / 候选 C(`code-auto` 默认):过度设计 / 失去意义
- 候选 B / 候选 C(大需求 8 问分组):用户预期下降 / 工具限制

## 临时假设

- `code-skills` 仓库是 Markdown 自然语言 → 封装性 / 可复用性 / 可读性维度默认 `不适用`
- 实际改造在 REQ-00020 阶段已落地,本计划任务"开发状态"全部"已完成"
- `code-auto` 上下文默认 7 维度全=中(中性默认)

## 下一轮要深挖的方向

- 若用户要求"进一步降 token",可考虑:
  - 抽"## 过程文档格式"段(7 份过程文档,约 150 行)到 `templates/`
  - 抽"## 缺陷分支"段(约 200 行)到 `code-plan-fix-branch.md`(子文档)
  - 精简"## 衔接" + "## 不要做的事"为 1 张表
- 但本需求**不**做这些拆分(避免触发 `module-conventions §规则 1` 引入新文件)
