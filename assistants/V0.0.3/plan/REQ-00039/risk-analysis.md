# 风险分析 — REQ-00039

更新时间:2026-06-22 15:00
版本:V0.0.3

## 异常处理

| 异常路径 | 处理策略 | 监控指标 |
| --- | --- | --- |
| tokei 不存在 + cloc 不存在 | 屏显 `⚠ 建议安装 tokei/cloc 以获得准确逻辑行统计;本次回退到启发式` + 使用启发式 | `code-it` 步骤 8 末尾屏显契约的"检测方式"字段 |
| git diff 失败(非 git 仓库) | 屏显 `⚠ 非 git 仓库,无法获取变更文件列表;本次跳过 calcLogicLoc` + 跳过 | 同上 |
| 变更文件无法访问(权限/不存在) | 跳过该文件,屏显 `⚠` 警告 | 单文件级 |
| 单文件过大(> 10MB) | 跳过该文件,屏显 `⚠` 警告(性能) | 单文件级 |
| 无 `code/<task>/RESULT.md` 的"## 逻辑行统计"小节 | `code-check` 步骤 8.13 跳过该任务,不派生发现 | 任务级 |
| 阈值配置缺失 | 默认 500 / 200(沿用 `logic-loc-defaults.md`) | `code-check` 步骤 8.13 屏显契约的"阈值"字段 |
| 缺陷分支(`^TASK-BUG-...`) | 完全跳过 `calcLogicLoc`(NFR-8) | 任务级 |
| 启发式不支持的语言 | 退化到"仅过滤空行"(纯字符串匹配) | 启发式精度 ~95% 退化 |
| `code-check` 8.13 派生发现异常 | 屏显 `⚠` + 跳过,不阻断 `code-check` 主流程 | 评审级 |

## 安全边界

- **鉴权要求**:N/A(本需求是 Markdown 技能定义,无运行时鉴权)
- **输入校验**:`calcLogicLoc` 校验 `taskNum` 匹配 `^TASK-REQ-\d{5}-\d{5}$`(任务分支);否则跳过(NFR-8 缺陷分支)
- **敏感数据处理**:N/A(Markdown 文档,无敏感数据)
- **审计日志**:N/A(无运行时)
- **依据规范**:`./assistants/rules/skill-conventions.md §规则 1`

## 性能与资源

- **关键路径预估**:`calcLogicLoc` 在 `code-it` 步骤 8 末尾执行,典型 100 文件下 < 3 秒
- **资源限制**:
 - tokei / cloc 调一次耗时 < 1 秒
 - 启发式耗时 < 2 秒(100 文件典型)
 - 单文件 > 10MB 跳过(避免内存压力)
- **缓存策略**:无(每次 `code-it` 执行重新计算)
- **并发**:无(同步执行)

## 回退策略

- **触发条件**:`calcLogicLoc` 引发 `code-it` 步骤 9-12 异常 / `code-check` 步骤 8.13 误触发
- **步骤**:
 1. `Bash: git revert HEAD`(单 commit 模式)
 2. 人工编辑 `fix/<BUG-NNN>/RESULT.md` 还原(若状态误推进)
- **验证**:`git diff` 校验 5 SKILL.md / 1 模板回退到本需求前状态
- **自动回退**:**不**自动(避免连锁反应);用户人工决策

## 测试要点

### 单元测试

- **不适用**(沿用 `code-it` 步骤 8.5 自含按需写单测守卫判定)
- 本仓库无 `package.json` 含 `scripts.test` / `pyproject.toml` 含 `[tool.pytest]` 等 → 守卫不通过 → 任务"测试状态"列 = `不适用`

### 集成测试

- **AC-1**:调 `code-it TASK-REQ-NNNNN-NNNNN` 实施 1 个功能文件 → `code/<TASK-...>/RESULT.md` 新增"## 逻辑行统计"小节 + 屏显契约存在
- **AC-2**:逻辑行 = 总行 - 空行 - 注释行(静态校验)
- **AC-3**:启发式回退(tokei/cloc 均不存在 → 屏显 `⚠` + 输出"检测方式" = `heuristic`)
- **AC-4**:调 `code-check` 评审 1 个 600 行单文件 → 派生"代码行数超标"发现(P3,级别"建议改")
- **AC-5**:阈值配置生效(用户配置 300 → 400 行单文件触发"建议改")
- **AC-6**:缺陷分支(`code-it TASK-BUG-NNNNN-NNNNN`)→ `code/<TASK-BUG-...>/RESULT.md` **无**"## 逻辑行统计"小节
- **AC-7**:`code-it` / `code-check` frontmatter 字节级保留
- **AC-8**:性能 < 3 秒(100 文件工程)

### 性能/安全

- **NFR-1(性能)**:逻辑行计算总耗时 < 3 秒
- **NFR-7(不阻断)**:逻辑行计算失败 → 屏显 `⚠` + 跳过,**不**阻断 `code-it` / `code-check`
- **NFR-8(缺陷分支不触达)**:缺陷路径**不**走 `calcLogicLoc`(沿用既有步骤 8a 守卫规则)