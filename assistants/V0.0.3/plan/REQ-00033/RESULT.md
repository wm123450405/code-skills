# 详细设计 — REQ-00033 明文契约化 `code-require` 不涉及技术选型(只确定功能点)

- 需求编码:REQ-00033
- 所属版本:V0.0.3
- 文档创建时间:2026-06-15 12:30
- 最近更新:2026-06-15 12:30
- 文档状态:已完成
- 上游需求:`./assistants/V0.0.3/require/REQ-00033/RESULT.md`
- 上游概要设计:`./assistants/V0.0.3/design/REQ-00033/RESULT.md`
- 遵循规范:`./assistants/rules/` 下 12 个文件(全沿用,无新增)
- 涉及技能:`code-require`(本设计唯一改造对象)

## 1. 设计目标

- **整体设计目标**:`--balanced`(沿用 `design/.../RESULT.md` §1)
- **维度优先级**:功能性 = 低(职责边界显式化型,非功能性需求)
- **沿用路径**:`code-plan` 步骤 0b 0 问(沿用 `design` 的"## 设计目标"小节)

## 2. 概述

本设计是"职责边界显式化"型(把隐含契约落字),**0 模块新增 / 0 接口新增 / 0 数据结构新增**,**唯一改造对象** = `plugins/code-skills/skills/code-require/SKILL.md` §"不要做的事" 小节末尾**纯追加** 1 条硬约束(锁定措辞)。

详细设计与概要设计 1:1 对应;**0 展开**"完整字段 / 接口 schema / 算法伪代码 / 迁移脚本"(本设计是文字改造,无模块/接口/数据结构变更,无算法/逻辑变更)。

## 3. 模块详细化

### 3.1 唯一改造:`code-require/SKILL.md` §"不要做的事" 小节末尾追加 1 条

#### 3.1.1 关键"代码"(Markdown 列表项)

**新增条目措辞**(锁定,FR-2 锁定的 1 条):

```
- 不涉及技术选型 / 技术栈 / 技术方案的确定:本技能只确定功能点(FR / NFR / 页面 / 交互 / 数据 / 边界);技术选型 / 架构风格 / 接口风格 / 数据模型(库表层) / 部署形态归 `code-design`(由其结合项目实际状况给出方案);没有**必要**进行技术选型的需求,**无需**在 `code-require` 阶段分析技术选型选项。
```

#### 3.1.2 关键"类/函数"语义

| 元素 | 语义 |
| --- | --- |
| 条目 | 1 条 Markdown 列表项(`-` 起首) |
| 位置 | `code-require/SKILL.md` §"不要做的事" 小节末尾(紧跟既有第 8 条列表项之后) |
| 3 个语义子句(AC-2.1 锁定) | (1) 不涉及技术选型 / 技术栈 / 技术方案的确定;(2) 技术选型归 `code-design`;(3) 没有**必要**进行技术选型的需求,**无需**在 `code-require` 阶段分析技术选型选项 |
| 包含的对偶引用 | "`code-design`(由其结合项目实际状况给出方案)"(FR-3 锁定) |
| Markdown 加粗 | "**必要**" + "**无需**"(NFR-7 语义强调) |

#### 3.1.3 调用顺序

不适用(本设计是 SKILL.md 文字改造,无函数调用顺序)

#### 3.1.4 状态归属

不适用(本设计是 SKILL.md 文字改造,无运行时状态)

#### 3.1.5 错误处理范式

不适用(本设计是 SKILL.md 文字改造,无错误处理)

#### 3.1.6 日志埋点

不适用(本设计是 SKILL.md 文字改造,无日志)

#### 3.1.7 与概要设计的对应

`design/.../RESULT.md` §3 决策 D-1(锚点 = §"不要做的事" 小节末尾)+ D-2(对偶引用 `code-design`)

#### 3.1.8 符合的规范

- `skill-conventions.md §规则 1`(SKILL.md frontmatter 字节级保留;本设计**不**改 frontmatter)
- `module-conventions.md`(资源放 `templates/`;本设计**不**改模板)
- `commit-conventions.md`(`chore(<skill>):` 前缀;本设计落地后 commit message 候选 = `chore(code-it): REQ-00033 ...`)

## 4. 算法与逻辑

### 4.1 "code-require 不产出技术选型"判定逻辑(本设计不实施,FR-1 锁定为"约束")

```
材料进入 code-require
  ↓
提取候选需求(FR / NFR / 页面 / 交互 / 数据 / 边界)
  ↓
本技能**不**判定"是否需要技术选型"(由 code-design 自由裁量)
  ↓
输出 RESULT.md(0 包含技术选型)
  ↓
(code-design 阶段)若需技术选型,code-design 给出方案
```

### 4.2 复杂度分析

- 时间复杂度:O(1)(本设计是"不产出"约束,无算法)
- 空间复杂度:O(1)(本设计是"不产出"约束,无状态)

## 5. 数据结构完整变更

**0 新增实体** / **0 修改实体**

| 实体名 | 状态 | 关键字段 | 关系 |
| --- | --- | --- | --- |
| — | — | — | — |

## 6. 接口细节

**0 新增接口** / **0 修改接口**

| 接口名 | 形式 | 状态 | 职责 |
| --- | --- | --- | --- |
| — | — | — | — |

## 7. 异常处理

| 异常场景 | 处理 | 监控 |
| --- | --- | --- |
| 材料含技术选型字面(E-1) | `code-require` **不**主动提取;若用户**强提示**,放进"## 12 待澄清" | 屏显 |
| 增量更新(E-2) | 同样触发本硬约束 | 屏显 |
| `code-require` 误产出技术选型(E-3) | `code-check` 评审时由"§不要做的事"小节兜底校验(本设计**不**实现 `code-check` 改造,留作 follow-up) | `code-check` 评审报告 |
| 用户在建议前中断(E-4) | `code-require` 既有"中止"逻辑(本设计不介入) | exit 130 |

## 8. 安全要求

**不适用**(本设计是 SKILL.md 文字改造,无安全变更)

## 9. 状态机 / 流程

```
+-------------------------+        +-------------------------+
|     code-require         |        |     code-design          |
|  - 提取功能点             | -----> |  - 技术选型               |
|  - 写 NFR                |  交接  |  - 架构风格               |
|  - 0 产出技术选型         |        |  - 接口风格               |
|  (本硬约束,FR-1 锁定)    |        |  - 数据模型(库表层)       |
+-------------------------+        +-------------------------+
```

## 10. 性能与资源

**不适用**(本设计是 SKILL.md 文字改造,无性能变更)

## 11. 测试要点

### 11.1 单元测试范围

- **不适用**(`code-plan` 不规划单元测试任务,沿用 REQ-00031 FR-2;`code-unit` 退场后 `code-it` 接管)
- 本设计无 `code-unit` 任务(测试状态 = `不适用`)

### 11.2 集成测试范围

- **不适用**(本设计是 SKILL.md 文字改造,无集成变更)

### 11.3 端到端测试范围

- **不适用**

### 11.4 性能/安全测试

- **不适用**

### 11.5 校验要点(`code-it` 步骤 13 + `code-check` 评审)

| 校验点 | 验证方式 | 预期 |
| --- | --- | --- |
| `code-require/SKILL.md` §"不要做的事" 新增 1 条 | `Read` + 校验 3 个语义子句 | AC-2.1 全包含 |
| `code-require/SKILL.md` frontmatter L1-3 字节级保留 | `git diff` 校验 | AC-2.2 |
| `code-require/SKILL.md` §"工作流程" 既有段 0 改 | `git diff` 校验 | AC-2.3 |
| 净增行数 +2 ~ +4 行 | `git diff --stat` | AC-2.4 |
| 11 个其他 `code-*` 技能 SKILL.md 0 改 | `git diff` 校验 | AC-3.1 |
| 既有 12 个 REQ 的 RESULT.md 0 改 | `git diff` 校验 | AC-3.2 |

## 12. 规范遵循

- `skill-conventions.md §规则 1`:**遵守**(不碰 frontmatter)
- `module-conventions.md`:**遵守**(不碰 templates/)
- `doc-conventions.md`:**不适用**(本设计不动 README)
- `dashboard-conventions.md`:**遵守**(仅追加 1 行 + 1 条;不触发字段扩展)
- `commit-conventions.md`:**遵守**(`chore(<skill>):` 前缀)
- `encoding-conventions.md`:**遵守**(本需求编号沿用 5 位纯数字)
- `marketplace-protocol.md`:**不适用**(本设计不动 plugin.json / marketplace.json)
- 其他 5 个规范:本设计不涉及

## 13. 任务依赖图(单任务,无依赖)

不适用(本设计只拆 1 个任务)

## 14. 关联计划

| 需求 | 关联点 |
| --- | --- |
| BUG-00001 | "不修改 SKILL.md"硬约束范式 |
| REQ-00026 | 最小化变更原则 |
| REQ-00030 | 元技能改 + 12 维度评审 + INV 字节级保留 |
| REQ-00031 | 元技能改(任务粒度) |
| REQ-00032 | `code-require` 屏显契约(锚点不交叉) |

## 15. 待澄清 / 未决项

- **Q-1**(`code-check` 是否需要加"`code-require` 0 产出技术选型"校验点):本需求**不**实现(避免越过本需求边界到 `code-check` 改造);留作 follow-up
- **Q-2**(本需求是否需要 `code-require` 步骤 7A 澄清中新增"本需求是否需要技术选型"问路):**不**新增(用户已显式回答"无需分析";NFR-8 锁定)
- **Q-3**(`code-design` 端是否需要加"## 必做"段对偶声明):本需求**不**实现(避免越界 `code-design` 改造);留作 follow-up
- **Q-4**(`templates/requirements.md` 是否需要在 §4 / §5 章节追加"不含技术选型"提示):本需求**不**实现(NFR-2 零规范变更);留作 follow-up
- **Q-5**(本需求对偶引用 `code-design` 时,是否在 `code-design/SKILL.md` 加反向引用):**不**修改 `code-design` SKILL.md(FR-3 锁定);由 `code-design` 端在后续 REQ 自由裁量

## 16. 变更记录

```
2026-06-15 12:30  详细设计与编码计划完成  REQ-00033(共 1 任务: 1 修改类);整体=--balanced,功能性=低;唯一改造 = code-require/SKILL.md §"不要做的事" 小节末尾追加 1 条(锁定措辞);0 模块/0 接口/0 数据结构/0 依赖;8 INV 全部严守(沿用 design 9 INV 中的 1-9)  REQ-00033
```
