# 编码计划 — REQ-00033 明文契约化 `code-require` 不涉及技术选型(只确定功能点)

- 需求编码:REQ-00033
- 所属版本:V0.0.3
- 文档创建时间:2026-06-15 12:30
- 最近更新:2026-06-15 12:30
- 文档状态:已完成
- 计划标题:明文契约化 `code-require` 不涉及技术选型
- 整体设计目标:`--balanced`
- 维度优先级:功能性 = 低
- 任务总数:1
- 任务类型:修改

---

## 1. 计划概述

在 `plugins/code-skills/skills/code-require/SKILL.md` §"不要做的事" 小节末尾**纯追加** 1 条硬约束,把"不涉及技术选型"的隐含契约显式落字。**1 任务,纯 SKILL.md 文档改造,测试 = 不适用**。

## 2. 任务总览

| 任务编号 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件 | 关联任务 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| TASK-REQ-00033-00001 | 修改 | 详细设计 | [修改] code-require §"不要做的事" 小节追加"不涉及技术选型"硬约束 | 待开始 | 不适用 | `plugins/code-skills/skills/code-require/SKILL.md` §"不要做的事" 小节末尾 | — |

**统计**:
- 任务总数:1
- 开发=待开始:1
- 测试=不适用:1
- 完成定义:任务开发=已完成 ∧ 测试∈{已运行-通过, 不适用}(沿用 REQ-00031 FR-3)

## 3. 任务详情

### TASK-REQ-00033-00001:[修改] code-require §"不要做的事" 小节追加"不涉及技术选型"硬约束

- **目标**:在 `plugins/code-skills/skills/code-require/SKILL.md` §"不要做的事" 小节末尾**纯追加** 1 条硬约束(锁定措辞),把"不涉及技术选型"的隐含契约显式落字
- **涉及文件**:`plugins/code-skills/skills/code-require/SKILL.md` §"不要做的事" 小节末尾
- **关键变更**:
  - 在既有第 8 条列表项之后,追加 1 条新列表项(锁定措辞,见 §3.1)
  - 既有第 1 ~ 8 条列表项**字节级保留**
  - frontmatter L1-3 字节级保留(INV-1)
  - §"工作流程" 既有段 0 改(INV-2 / INV-3)
- **边界与异常**:
  - 若既有第 8 条位置不确定 → `Read` 文件后取"## 不要做的事" 小节末尾**实际最后一条列表项**;在该行**之后**插入(不删除任何既有内容)
  - 净增行数 +2 ~ +4 行(1 条 markdown 列表项,可能跨多行因中英混排)
- **验证手段**:
  - `Read` 文件后,新条目包含 3 个语义子句(AC-2.1)
  - `git diff` 校验:`code-require/SKILL.md` 仅 §"不要做的事" 小节末尾有变化,其他位置 0 diff
  - `git diff --stat` 校验净增行数 +2 ~ +4 行
- **回退方式**:`git revert <commit-hash>`(单 commit 回退,无副作用)
- **完成定义**:
  - 措辞与 FR-2 锁定措辞**完全一致**
  - frontmatter L1-3 字节级保留
  - §"工作流程" 既有段 0 改
  - 净增行数 +2 ~ +4 行
  - 11 个其他 `code-*` 技能 SKILL.md 0 改
  - 既有 12 个 REQ 的 RESULT.md 0 改
  - 12 个项目级规范 0 改
  - 4 README / CLAUDE.md / plugin.json / marketplace.json 0 改
  - `plugins/code-skills/skills/code-require/templates/requirements.md` 0 改

### 3.1 锁定措辞(待 `code-it` 步骤 7 写入 RESULT.md)

```
- 不涉及技术选型 / 技术栈 / 技术方案的确定:本技能只确定功能点(FR / NFR / 页面 / 交互 / 数据 / 边界);技术选型 / 架构风格 / 接口风格 / 数据模型(库表层) / 部署形态归 `code-design`(由其结合项目实际状况给出方案);没有**必要**进行技术选型的需求,**无需**在 `code-require` 阶段分析技术选型选项。
```

## 4. 任务依赖图

不适用(本计划只拆 1 个任务,无依赖)

## 5. 里程碑

| 里程碑 | 包含任务范围 | 完成定义 | 状态 | 计划时间 | 实际完成 |
| --- | --- | --- | --- | --- | --- |
| M1-REQ-00033 | TASK-REQ-00033-00001(1 任务,1 个被改文件) | 1 任务开发=已完成 ∧ 测试=不适用 | 待开始 | 2026-06-15 | — |

## 6. 状态管理规则

- **开发状态**:初值 = `待开始`;`code-it` 完成后 → `已完成`
- **测试状态**:初值 = `不适用`(沿用 REQ-00031 FR-3 收窄,本设计是纯 SKILL.md 文字改造,无单元测试)
- **不**重写已分配的任务编号
- **不**修改已完成任务的描述或状态(只能追加"修改类"任务)

## 7. 关联计划

| 计划 | 关联点 |
| --- | --- |
| BUG-00001 | "不修改 SKILL.md"硬约束范式 |
| REQ-00026 | 最小化变更原则 |
| REQ-00030 | 元技能改 + 12 维度评审 + INV 字节级保留 |
| REQ-00031 | 元技能改(任务粒度) |
| REQ-00032 | `code-require` 屏显契约(锚点不交叉) |
| REQ-00020 | 元技能改首条需求 |

## 8. 变更记录

| 时间 | 变更类型 | 变更摘要 | 关联项 |
| --- | --- | --- | --- |
| 2026-06-15 12:30 | 计划新增 | REQ-00033 详细设计与编码计划完成(共 1 任务,1 修改类,1 个被改文件) | REQ-00033 |
