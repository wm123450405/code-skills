# 风险分析 — REQ-00024
更新时间:2026-06-07
版本:V0.0.3

> 本文档是 `code-plan BUG-00001` 阶段的辅助过程文档。核心内容已并入 `plan/REQ-00024/RESULT.md §异常处理 / §安全要求 / §性能与资源`,本文档为索引。

## 异常处理
- 路径检测为**只读**操作,0 异常抛出
- 输入空 / 输入含特殊字符 / Bash 不可用等场景:沿用既有(退出码 4 / 2)
- 详见 `plan/REQ-00024/RESULT.md §异常处理`

## 安全边界
- 路径检测只读,0 文件改动,0 副作用
- 输入是用户提供的任意字符串,但**仅**作为路径检测的字面值,**不**作为命令执行
- 路径遍历防护:本技能不解析 `..` / 绝对路径,只检测 `./assistants/<version>/<类型>/<userInput>` 的字面值存在性
- 无敏感数据泄露
- 详见 `plan/REQ-00024/RESULT.md §安全要求`

## 性能与资源
- 路径检测 2-3 次 `Bash: test -d` 命令(每条 < 0.01 秒)
- 总耗时 < 0.1 秒
- 整体 `code-auto` 6 步状态机性能不变
- 详见 `plan/REQ-00024/RESULT.md §性能与资源`

## 回退策略
- 触发条件:INV 失败 / 屏显契约破坏 / 6 步状态机破坏
- 回退步骤:`git checkout HEAD~1 -- plugins/code-skills/skills/code-auto/SKILL.md`(单文件回退)
- 验证:重新跑 8 项 AC

## 测试要点
- 本仓库 0 测试框架,验证手段 = 静态校验(详 `plan/REQ-00024/RESULT.md §规范遵循` + `§测试要点`)
- 8 项 INV 静态校验(AC-1~AC-8)全部通过 → 缺陷可关闭

## 残余风险(本轮不解决)
| 风险 | 严重度 | 本轮处理 | 后续触发 |
| --- | --- | --- | --- |
| `/code-auto from REQ-NNNNN` 调用方式破坏性变更 | 中 | 接受(用户明确要求移除 from) | 既有调用方需调整为 `/code-auto REQ-NNNNN` |
| 第三方平台前缀未登记 | 低 | 接受(沿用既有"用户有最终决定权") | 用户调 `code-rule` 登记 |
