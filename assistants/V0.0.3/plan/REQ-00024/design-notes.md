# 设计笔记 — REQ-00024
更新时间:2026-06-07
版本:V0.0.3

> 本文档是 `code-plan BUG-00001` 阶段的辅助过程文档。关键设计问题与决策已并入 `plan/REQ-00024/RESULT.md §算法 1 / §关联`,本文档为索引。

## 关键设计问题与候选

### Q1:步骤 1 模式识别方式?
- **候选 A**(原):正则 `^from REQ-\d{5}$` 匹配关键字
- **候选 B**(新):路径感知(`Bash: test -d <path>`)
- **选定**:B(用户原始描述明确要求移除 from 关键字,改用路径感知)

### Q2:路径判定优先级?
- **候选 A**(新):默认输入 = 需求编号(先按需求判 → 再按缺陷判 → 最后视为需求内容)
- **选定**:A(用户原始描述第 1 段明确)

### Q3:`require/<input>/` 存在但 `RESULT.md` 不存在的处理?
- **候选 A**(新):进入"未完成需求设计"分支(走 code-require)
- **选定**:A(用户原始描述第 2 段明确)

### Q4:`from` 关键字调用方迁移?
- **候选 A**:保留 `from` 关键字作为别名(向后兼容)
- **候选 B**:不保留(破坏性变更)
- **选定**:B(用户明确要求移除)

## 备选方案被否决的理由

- 候选 A(正则):`from` 与自然语言动词冲突,用户已明确要求移除
- 候选 A(默认=需求内容):违反用户"默认输入=需求编号"明确要求
- 候选 A(RESULT.md 缺失 → 异常):与用户"则进入需求设计阶段"不符
- 候选 A(保留 from 别名):违反"移除 from 关键字"明确要求

## 关键设计决策

### D-1:屏显契约 — 3 行前缀
- 决策:屏显前 3 行明示判定依据
- 理由:透明性 + 可追溯;屏显 0 副作用

### D-2:错误码收敛
- 决策:删除 E-15/E-16/E-17,新增 E-18/E-19
- 理由:沿用既有错误码表(0 字段扩展)

## 关键设计问题(详情)

### Q-A1:`code-auto` 屏显契约
- 现状:`[code-auto] 步骤 1/6:code-require <编码>`
- 新行为:`[code-auto] 步骤 1:路径感知判定` + `[code-auto]   → 模式:<X>` + `[code-auto]   → 依据:<X>`
- 决策:沿用既有"步骤 N/6"格式 + 屏显微调

### Q-A2:`code-fix` 协同
- 决策:`code-fix` **不**修改(本需求边界限定)
- 理由:本需求是 code-auto 改造,不是 code-fix 改造;code-fix 自身的 `from` 关键字另立需求

## 不变量

- INV-1:SKILL.md frontmatter 字节级保留
- INV-2:9 个 `code-*` 技能 SKILL.md 字节级 0 变化
- INV-3:`auto-report.md` 模板字节级 0 变化
- INV-4:0 字段扩展
- INV-5:6 步状态机 + 6 子技能编排不变

## 残余风险
- **R-1**:`/code-auto from REQ-NNNNN` 调用方式破坏性变更(本需求 NFR 严守)
- **R-2**:第三方平台前缀未登记时,屏显"未登记"提示(沿用既有)

## 备注
本任务为 REQ-00024 修复的 1 任务计划(单文件改造),与 BUG-00001 修复的 5 任务计划不同。
