# 编码计划 — REQ-00031 优化 /code-plan 任务粒度(内化编译/运行,外移单元测试)

- 需求编码:REQ-00031
- 所属版本:V0.0.3
- 计划状态:草稿
- 创建:2026-06-12
- 最近更新:2026-06-12 15:32

## 计划概述

按"5 个被改文件各 1 任务"拆分为 **5 个任务**(T-001 ~ T-005),全部为纯文档/技能定义/模板类任务,测试状态=`不适用`。
**不**单列独立的"编译运行检测"任务(本就是冗余 — 5 任务自身的"修改后 Read + INV 字节级校验"已内化为"运行成功"语义)。
**不**规划单元测试任务(沿用本需求新规则 — code-plan 不再规划测试,`code-unit` 独立、可选)。

**整体设计目标**:`--balanced`(沿用 design)
**功能性**:中 / **扩展性**:低 / **健壮性**:低 / **可维护性**:中 / **封装性**:不适用 / **可复用性**:不适用 / **可读性**:不适用

> **架构骨架任务处理(--extensible 触发)**:本需求**不**触发架构骨架(`--balanced` + 无新增三方依赖 + 跨模块数 ≤ 3 → 5 任务独立即可)。

## 任务总览

| 任务编号 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件 |
| --- | --- | --- | --- | --- | --- | --- |
| TASK-REQ-00031-00001 | 修改 | 详细设计 | [修改] code-plan/SKILL.md:任务粒度原则内化编译/运行 + 移除测试类型 + 收窄测试状态枚举 | 已完成 | 不适用 | plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A |
| TASK-REQ-00031-00002 | 修改 | 详细设计 | [修改] code-it/SKILL.md:文档头 ## 目标 追加"不含单元测试"职责声明 | 已完成 | 不适用 | plugins/code-skills/skills/code-it/SKILL.md ## 目标 |
| TASK-REQ-00031-00003 | 修改 | 详细设计 | [修改] code-unit/SKILL.md:文档头 ## 目标 追加"独立、可选"职责声明 | 已完成 | 不适用 | plugins/code-skills/skills/code-unit/SKILL.md ## 目标 |
| TASK-REQ-00031-00004 | 修改 | 详细设计 | [修改] code-auto/SKILL.md:任务循环步骤 4.b 改为"恒等跳过" | 已完成 | 不适用 | plugins/code-skills/skills/code-auto/SKILL.md §步骤 4 任务循环 |
| TASK-REQ-00031-00005 | 修改 | 详细设计 | [修改] templates/plan.md:任务类型移除 测试 + 测试状态字段收窄为 2 枚举 | 待开始 | 不适用 | plugins/code-skills/skills/code-plan/templates/plan.md |

**统计**:5 个任务 / 0 派生"更新看板"任务(沿用 REQ-00017 强约束) / 0 单元测试任务(本需求主动外移) / 0 独立"编译运行检测"任务(本就是冗余)

## 任务详情

### TASK-REQ-00031-00001 · [修改] code-plan/SKILL.md:任务粒度原则内化编译/运行 + 移除测试类型 + 收窄测试状态枚举

**目标**:在 `code-plan/SKILL.md` §步骤 10A"任务拆分原则"小节**追加** 1 段(FR-1)+ "任务类型"列表**移除** `测试`(FR-2)+ "任务双状态字段"**收窄**"测试状态"枚举为 2 个(FR-3)+ "双状态语义"**简化**(FR-3 副作用)。**不**改既有"## 工作流程"步骤 0a / 0b.0 / 0b / 0-6 / 8A-9A / 11A-18A。

**涉及文件**:
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A "任务拆分原则"` 子段**追加** 1 段
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A "任务类型"` 子段列表**移除** `测试` + 列表后**追加** 1 段说明
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A "任务双状态字段"` 子段**修订**"测试状态"枚举 + 追加 1 段说明
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A "双状态语义"` 子段**简化**
- `plugins/code-skills/skills/code-plan/SKILL.md ## 不要做的事`:**不**修改(NFR-3 字节级保留)
- `plugins/code-skills/skills/code-plan/SKILL.md frontmatter L1-3`:**不**修改(INV-1)

**关键变更**(按结构化语义定位):
1. **§步骤 10A "任务拆分原则"** → 末尾**追加** 1 段"#### 任务完成定义内化编译/运行"(4 行)
2. **§步骤 10A "任务类型"** → 列表**移除** `测试` + 列表后**追加** 1 段引用说明(3 行)
3. **§步骤 10A "任务双状态字段"** → 修订"测试状态"枚举为 2 个(3 行)+ 追加 1 段说明(4 行)
4. **§步骤 10A "双状态语义"** → 简化 1 句(2 行)
5. **§步骤 0a / 0b.0 / 0b / 0-6 / 8A-9A / 11A-18A** → 既有"## 工作流程"步骤**不**重写
6. **## 不要做的事** → 字节级保留(INV-2)
7. **frontmatter L1-3** → 字节级保留(INV-1)

**结构化语义锚点**:
- 锚点 1:`§步骤 10A` 子标题(以"### 步骤 10A"开头)
- 锚点 2:`§步骤 10A "任务拆分原则"` 子段(以"**任务拆分原则"开头)
- 锚点 3:`§步骤 10A "任务类型"` 子段(列表含 6 类)
- 锚点 4:`§步骤 10A "任务双状态字段"` 子段
- 锚点 5:`§步骤 10A "双状态语义"` 子段

**边界与异常**:
- INV-1:frontmatter L1-3 字节级保留
- INV-2:"## 不要做的事" 字节级保留
- INV-3:既有"## 工作流程"步骤字节级保留

**任务完成定义**(本需求新规则,内化"运行成功"语义):
- Read `code-plan/SKILL.md` 最新内容(避免与既有行号冲突)
- 按 5 个锚点顺序**追加 / 修订**内容
- 验证:`grep -c "任务完成定义内化编译/运行" code-plan/SKILL.md` ≥ 1 + `grep -c "^| 测试 |" code-plan/SKILL.md` = 0 + `grep -c "未编写 / 已编写" code-plan/SKILL.md` ≤ 1(只在说明段中出现)
- 校验:`git show HEAD:plugins/code-skills/skills/code-plan/SKILL.md | head -3` vs `git show HEAD:plugins/code-skills/skills/code-plan/SKILL.md | head -3` 字节级一致(INV-1)+ `git show HEAD:plugins/code-skills/skills/code-plan/SKILL.md | grep -c "^## 不要做的事"` ≥ 1(INV-2)

**验证手段**(对应 AC-1 + AC-2 + AC-7.1~7.3):
- AC-1.1:既有"可独立验证"小节**追加** 1 段
- AC-1.2:"任务类型"列表**移除** `测试`
- AC-1.3:列表后**追加** 1 段说明
- AC-2.1:"测试状态"枚举**收窄为 2 个**
- AC-2.2:"双状态语义"**简化**
- AC-2.3:**追加** 1 段"V0.0.2 → V0.0.3 收窄"说明
- AC-7.1:INV-1 字节级保留(frontmatter L1-3)
- AC-7.2:INV-2 字节级保留("## 不要做的事")
- AC-7.3:INV-3 字节级保留(既有"## 工作流程"步骤)

**回退方式**:`git revert <commit>`,无副作用(纯 markdown 改动)。

---

### TASK-REQ-00031-00002 · [修改] code-it/SKILL.md:文档头 ## 目标 追加"不含单元测试"职责声明

**目标**:在 `code-it/SKILL.md` 文档头 `## 目标` 小节**追加** 1 段"不含单元测试"职责声明(FR-4)。**不**改既有"## 工作流程"步骤 0a / 0-16(字节级保留 INV-3)。

**涉及文件**:
- `plugins/code-skills/skills/code-it/SKILL.md ## 目标` 小节**追加** 1 段(3 行)
- `plugins/code-skills/skills/code-it/SKILL.md ## 工作流程` 步骤 0a / 0-16:**不**修改
- `plugins/code-skills/skills/code-it/SKILL.md ## 不要做的事`:**不**修改(NFR-3 字节级保留)
- `plugins/code-skills/skills/code-it/SKILL.md frontmatter L1-3`:**不**修改(INV-1)

**关键变更**(按结构化语义定位):
1. **## 目标** 末尾**追加** 1 段"本技能职责范围"(3 行)

**结构化语义锚点**:
- 锚点 1:`## 目标` 文档头小节(以"## 目标"开头)
- 锚点 2:`## 工作流程` 小节(以"## 工作流程"开头,本需求**不**修改)

**边界与异常**:
- INV-1:frontmatter L1-3 字节级保留
- INV-2:"## 不要做的事" 字节级保留
- INV-3:既有"## 工作流程"步骤字节级保留

**任务完成定义**:
- Read `code-it/SKILL.md` 最新内容
- 按锚点 1 **追加** 1 段
- 验证:`grep "本技能职责范围" code-it/SKILL.md` ≥ 1 + `grep "不含单元测试" code-it/SKILL.md` ≥ 1
- 校验:`git show HEAD:plugins/code-skills/skills/code-it/SKILL.md | head -3` vs 改后 L1-3 字节级一致(INV-1)+ `git show HEAD:plugins/code-skills/skills/code-it/SKILL.md | grep -c "^## 不要做的事"` ≥ 1(INV-2)

**验证手段**(对应 AC-3 + AC-7.1~7.3):
- AC-3.1:## 目标小节**追加** 1 行
- AC-3.2:既有"## 工作流程"步骤 9-12 字节级保留
- AC-7.1~7.3:INV-1 / INV-2 / INV-3 字节级保留

**回退方式**:`git revert <commit>`。

---

### TASK-REQ-00031-00003 · [修改] code-unit/SKILL.md:文档头 ## 目标 追加"独立、可选"职责声明

**目标**:在 `code-unit/SKILL.md` 文档头 `## 目标` 小节**追加** 1 段"独立、可选"职责声明(FR-5)。**不**改既有"## 工作流程"步骤 0-15(字节级保留 INV-3)。

**涉及文件**:
- `plugins/code-skills/skills/code-unit/SKILL.md ## 目标` 小节**追加** 1 段(3 行)
- `plugins/code-skills/skills/code-unit/SKILL.md ## 工作流程` 步骤 0-15:**不**修改
- `plugins/code-skills/skills/code-unit/SKILL.md ## 不要做的事`:**不**修改
- `plugins/code-skills/skills/code-unit/SKILL.md frontmatter L1-3`:**不**修改

**关键变更**(按结构化语义定位):
1. **## 目标** 末尾**追加** 1 段"本技能职责范围"(3 行)

**结构化语义锚点**:
- 锚点 1:`## 目标` 文档头小节
- 锚点 2:`## 工作流程` 小节(本需求**不**修改)

**边界与异常**:INV-1 / INV-2 / INV-3(同 T-002)

**任务完成定义**:
- Read `code-unit/SKILL.md` 最新内容
- 按锚点 1 **追加** 1 段
- 验证:`grep "本技能职责范围" code-unit/SKILL.md` ≥ 1 + `grep "独立、可选" code-unit/SKILL.md` ≥ 1
- 校验:INV-1 / INV-2 / INV-3 字节级保留

**验证手段**(对应 AC-4 + AC-7.1~7.3):
- AC-4.1:## 目标小节**追加** 1 行
- AC-4.2:既有"## 工作流程"步骤 0-15 字节级保留

**回退方式**:`git revert <commit>`。

---

### TASK-REQ-00031-00004 · [修改] code-auto/SKILL.md:任务循环步骤 4.b 改为"恒等跳过"

**目标**:在 `code-auto/SKILL.md` §步骤 4 任务循环步骤 4.b 子步**改写**为"恒等跳过"(FR-6);屏幕日志格式`(跳过,无需测试)`字节级保留(INV-10)。**不**改其他 6 步(0a / 0b / 0 / 1-3 / 5-7)。

**涉及文件**:
- `plugins/code-skills/skills/code-auto/SKILL.md §步骤 4 任务循环` 子节步骤 4.b **改写**
- 步骤 4.b 末尾**追加** 1 段"还原指引"注释(3-4 行,本需求 INV-10)
- 其他 6 步:**不**修改
- `## 不要做的事`:**不**修改
- frontmatter L1-3:**不**修改

**关键变更**(按结构化语义定位):
1. **§步骤 4 任务循环 步骤 4.b** → **改写**为"code-unit 步骤(本需求 REQ-00031 FR-6 修订):永不触发"(5-7 行)
2. **§步骤 4 任务循环 步骤 4.b 末尾** → **追加** 1 段"还原指引"注释(3-4 行,含原"按需调用"逻辑以备还原)

**结构化语义锚点**:
- 锚点 1:`§步骤 4 任务循环` 子节
- 锚点 2:子步 b(既有"若 code-it 输出含'测试需要=Y'")

**边界与异常**:
- INV-1:frontmatter L1-3 字节级保留
- INV-2:"## 不要做的事" 字节级保留
- INV-3:其他 6 步字节级保留
- INV-10:屏幕日志格式字节级保留

**任务完成定义**:
- Read `code-auto/SKILL.md` 最新内容
- 按锚点 2 **改写**为"恒等跳过" + 锚点 2 末尾**追加** "还原指引"注释
- 验证:`grep "永不再调" code-auto/SKILL.md` ≥ 1 + `grep "(跳过,无需测试)" code-auto/SKILL.md` ≥ 1(INV-10 字节级保留)
- 校验:INV-1 / INV-2 / INV-3 / INV-10 字节级保留

**验证手段**(对应 AC-5 + AC-7.1~7.3 + INV-10):
- AC-5.1:步骤 4.b 描述**改为**"永不再调 code-unit"
- AC-5.2:`code-auto` 执行时,所有任务循环屏幕日志均出现 `(跳过,无需测试)`(由 `code-auto` 步骤 4 行为自动验证)
- AC-5.3:屏幕日志格式**保留**(`(跳过,无需测试)` 字节级保留,INV-10)

**回退方式**:`git revert <commit>` + 删除"还原指引"注释。

---

### TASK-REQ-00031-00005 · [修改] templates/plan.md:任务类型移除 测试 + 测试状态字段收窄为 2 枚举

**目标**:在 `templates/plan.md` ## 任务总览 表格"任务类型"列**移除** `测试` + 模板顶部**追加** 1 段"任务粒度约束"(FR-7)。**不**改其他章节。

**涉及文件**:
- `plugins/code-skills/skills/code-plan/templates/plan.md ## 任务总览` 表格"任务类型"列**移除** `测试`
- `plugins/code-skills/skills/code-plan/templates/plan.md` 模板顶部**追加** 1 段"任务粒度约束"(3 行)
- 其他章节(若有):**不**修改

**关键变更**(按结构化语义定位):
1. **## 任务总览 表格"任务类型"列** → 既有 6 类 `新增 / 修改 / 重构 / 修复 / 测试 / 文档` 收窄为 5 类 `新增 / 修改 / 重构 / 修复 / 文档`
2. **模板顶部** → **追加** 1 段引用(3 行)

**结构化语义锚点**:
- 锚点 1:`## 任务总览` 表格
- 锚点 2:模板顶部(以"---"分隔线为锚点)

**边界与异常**:
- INV-3:既有章节(若有)字节级保留
- `module-conventions.md`:资源文件放 templates/ 子目录(本任务**不**触发)

**任务完成定义**:
- Read `templates/plan.md` 最新内容
- 按锚点 1 **移除** `测试` 类型行
- 按锚点 2 **追加** 1 段
- 验证:`grep -c "^| 测试 |" templates/plan.md` = 0 + `grep "任务粒度约束" templates/plan.md` ≥ 1
- 校验:既有章节(若有)字节级保留

**验证手段**(对应 AC-6):
- AC-6.1:"任务类型"列**移除** `测试`
- AC-6.2:测试状态字段(若含)枚举**收窄为 2 个**

**回退方式**:`git revert <commit>`。

---

## 任务依赖图

```
T-001 (无前置,可独立完成) ─┐
T-002 (无前置,可独立完成) ─┤
T-003 (无前置,可独立完成) ─┼─→ (全部完成后验证 INV-1~INV-10)
T-004 (无前置,可独立完成) ─┤
T-005 (无前置,可独立完成) ─┘
```

**说明**:5 个任务**无强依赖**——T-001 ~ T-005 各自独立完成(每个被改文件都是不同文件,**不**涉及同一文件并发改动)。

**说明**:本需求**不**单列"编译运行检测"任务(每任务自身的"修改后 Read + INV 字节级校验"已内化为"运行成功"语义 — `code-it` 步骤 9-12 强制)。

**说明**:本需求**不**规划"单元测试"任务(`code-unit` 独立、可选,由项目/用户按需在 `code-it` 后自行决定)。

## 里程碑

| 里程碑 | 包含任务 | 完成定义 | 计划时间 | 实际完成 |
| --- | --- | --- | --- | --- |
| M1-REQ-00031 | T-001 ~ T-005(5 任务) | 5 任务开发=已完成 ∧ 测试=不适用 ∧ INV-1~INV-10 全部满足 | 2026-06-12 | — |

## 状态管理规则

- **开发状态**:本批 5 任务从 `待开始` → `进行中` → `已完成`
- **测试状态**:本批 5 任务为纯文档/技能定义/模板类,保持 `不适用`
  - **不适用理由**(逐任务):
    - T-001 ~ T-005:纯 SKILL.md / templates 文档改造,无新代码 / 无新依赖 / 无新接口契约 → `不适用`

## 关联计划

- 上游:`./assistants/V0.0.3/require/REQ-00031/RESULT.md`(需求)
- 上游:`./assistants/V0.0.3/design/REQ-00031/RESULT.md`(概要设计)
- 上游:`./assistants/V0.0.3/plan/REQ-00031/RESULT.md`(本文件对应的详细设计)
- 横向:`./assistants/V0.0.3/plan/REQ-00030/PLAN.md`(本需求与 REQ-00030 共同收敛"职责边界",沿用 INV-1~INV-9)
- 横向:`./assistants/V0.0.2/plan/REQ-00014/PLAN.md`(`code-plan` 步骤 10A "架构任务"子段字节级保留)
- 横向:`./assistants/V0.0.2/plan/REQ-00007/PLAN.md`(`code-auto` 任务循环步骤 4.b 本需求改写)

## 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-12 15:32 | v1 | 初始创建 | 详细设计与编码计划完成;5 任务(T-001 ~ T-005)对应 5 个被改文件;整体=--balanced + 功能性=中;架构骨架**不**触发;0 单列"编译运行检测"任务(本就是冗余);0 单元测试任务(主动外移);2 数据结构变更(任务类型 + 测试状态);1 里程碑(M1:实施完成) | 用户 |
