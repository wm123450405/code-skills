# 风险分析 — REQ-00031

更新时间:2026-06-12 15:32
版本:V0.0.3

## 异常处理

- **异常路径 1**:某任务 INV 字节级校验失败 → 处理:`code-it` 步骤 12 错误修复循环;监控:`git show` L1-3 + `grep -c` 9 项
- **异常路径 2**:`code-auto` 任务循环中 code-unit 步骤恒等跳过失败(理论上不可能)→ 处理:回退到无 code-unit 调用的旧逻辑;监控:屏幕日志

## 安全边界

- **鉴权要求**:不适用(本需求不涉及代码改动)
- **输入校验**:不适用
- **敏感数据处理**:不适用
- **审计日志**:不适用
- **依据规范**:**不适用**

## 性能与资源

- **关键路径预估**:5 任务 × 每任务 ~1 分钟(纯 markdown 编辑)= 5 分钟总耗时
- **资源限制**:不适用
- **缓存策略**:不适用

## 回退策略

- **触发条件**:INV 字节级校验失败,或用户授权偏离
- **步骤**:`git revert <commit>`,无副作用(纯 markdown 改动)
- **验证**:`git show` 比对回退前 vs 回退后内容

## 测试要点

### 单元测试
- **0 项**(本需求主动外移单元测试职责,FR-5;任务"测试状态"统一 `不适用`)

### 集成测试
- **0 项**(同上)

### 端到端测试(由 `code-check` 评审承担)

| AC | 测试方法 | 验证工具 |
| --- | --- | --- |
| AC-1.1 ~ AC-1.3 | `code-plan` 任务粒度原则修订校验 | `git diff` + `grep` |
| AC-2.1 ~ AC-2.3 | `code-plan` 任务"双状态"收窄校验 | `grep -c "未编写 / 已编写"` |
| AC-3.1 ~ AC-3.2 | `code-it` 职责声明校验 | `grep "不含单元测试"` |
| AC-4.1 ~ AC-4.2 | `code-unit` 职责声明校验 | `grep "独立、可选"` |
| AC-5.1 ~ AC-5.3 | `code-auto` 任务循环步骤 4.b 修订校验 | `grep "永不再调"` + 跑 `code-auto` 任务循环 |
| AC-6.1 ~ AC-6.2 | `templates/plan.md` 同步收窄校验 | `grep -c "^| 测试 |"` |
| AC-7.1 ~ AC-7.9 | INV 字节级保留校验 | `git show` + `grep -c` 9 项 |
| AC-8.1 ~ AC-8.3 | `code-auto` 全自动跑通 | `code-auto REQ-00031` + 退出码 0 |

### 性能/安全测试
- **不适用**

## 关键风险与缓解

| 风险 | 可能性 | 影响 | 缓解 |
| --- | --- | --- | --- |
| 既有 11 个 REQ 的 `plan/PLAN.md` 任务"测试状态"列出现"未编写 / 已编写 / 已运行-通过 / 已运行-失败" | 高 | 既有任务"测试状态"列与新规则的"2 选 1"不兼容 | NFR-2 沿用:**不**追溯重写;新拆分 REQ(REQ-00031+)应用新规则 |
| 既有任务的"任务类型"列含 `测试` | 中 | 既有"测试"类型任务与新规则的"5 选 1"不兼容 | NFR-2 沿用:不追溯重写;code-check §8.7 字节级沿用 |
| `code-auto` 步骤 4.b 改成"恒等跳过"后,`code-unit` 屏幕日志**(跳过,无需测试)** 出现频率显著升高 | 高 | 屏幕日志视觉冗余 | INV-10 字节级保留:屏幕日志格式**不**改 |
| `code-unit` 文档头追加"独立、可选"声明可能被误解为"code-unit 即将弃用" | 低 | 用户/AI 误判 | 声明明确"由项目/用户按需在 code-it 之后**自行**调用" |
| `code-auto` 子技能零修改契约(NFR-4)被破坏 | 低 | 4 个被改 SKILL.md 实际是被 code-auto 间接修改 | 校验:`code-auto` 自身 SKILL.md **不**在本需求的 4 个被改 SKILL.md 之列(本需求改的是 4 个**子**技能,不改编排者) |
