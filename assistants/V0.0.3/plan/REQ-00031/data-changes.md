# 数据结构完整变更 — REQ-00031

更新时间:2026-06-12 15:32
版本:V0.0.3

## 新增实体(无)

本需求**不**新增任何实体 / 字段 / 表结构。

## 修改实体 1:`PLAN.md 任务总览.任务类型` 字段

| 字段 | 类型 | 约束 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| 任务类型 | enum | 5 选 1:新增 / 修改 / 重构 / 修复 / 文档 | — | 由原 6 选 1 收窄(移除 `测试`) |

- **关系**:与 `code-plan §步骤 10A "任务类型"` 子段 + `templates/plan.md` 任务类型字段**对齐**
- **存储选型**:**不适用**(本仓库是 Markdown,无存储)
- **迁移脚本**:**不适用**
- **依据规范**:`encoding-conventions.md`(5 位纯数字)

### 修改前
```markdown
- 任务类型:`新增` / `修改` / `重构` / `修复` / `测试` / `文档`(6 类)
```

### 修改后
```markdown
- 任务类型:`新增` / `修改` / `重构` / `修复` / `文档`(5 类;本需求 REQ-00031 FR-2 移除 `测试`)
```

## 修改实体 2:`PLAN.md 任务总览.测试状态` 字段

| 字段 | 类型 | 约束 | 索引 | 说明 |
| --- | --- | --- | --- | --- |
| 测试状态 | enum | 2 选 1:已运行-通过 / 不适用 | — | 由原 6 选 1 收窄(移除 4 个"中间态") |

- **关系**:与 `code-plan §步骤 10A "任务双状态字段"` 子段 + `templates/plan.md` 测试状态字段**对齐**
- **存储选型**:**不适用**
- **迁移脚本**:**不适用**
- **依据规范**:`encoding-conventions.md`

### 修改前
```markdown
- 测试状态:`未编写` / `已编写` / `已运行-通过` / `已运行-失败` / `不适用` / `阻塞`(6 类)
```

### 修改后
```markdown
- 测试状态:`已运行-通过` / `不适用`(2 类;本需求 REQ-00031 FR-3 收窄;新拆分 REQ 统一填 `不适用`)
```

## 删除实体(无)

本需求**不**删除任何实体 / 字段。

## 迁移策略

- **NFR-2 沿用**:既有 11 个 REQ 的 `PLAN.md` 任务"测试状态"列保留原值(可能含 6 选 1 中任一个);**不**追溯重写
- **新拆分 REQ**:从 REQ-00031 起,应用 2 选 1 规则
