# REQ-00037 — 编码计划 — 优化 /code-fix 技能及整个缺陷修复流程的状态推进

- 需求编码:REQ-00037
- 所属版本:V0.0.3
- 上游详细设计:./assistants/V0.0.3/plan/REQ-00037/RESULT.md (v1)
- 任务总数:7
- 估算工作量:5-6 天
- 状态:草稿
- 创建:2026-06-22
- 最近更新:2026-06-22 09:28
- 当前版本:v1

## 1. 计划概述

本计划基于详细设计 `RESULT.md` 的 7 个任务候选(沿用概设 §12 + 用户确认),把整项工作拆分为 7 条可独立追踪的 `TASK-REQ-00037-NNNNN` 任务。每条任务有明确的"目标 / 涉及文件 / 关键变更 / 边界与异常 / 验证手段 / 回退方式",严格串行(T-1 → T-2 → T-3 → T-4 → T-5 → T-6 → T-7),由 `code-it` 步骤 0a 前置任务守卫按文件行序强制执行。

**任务双状态语义**(沿用既有):**任务"真正可发布" = 开发状态=已完成**;V0.0.3 修订后测试状态字段统一填 `不适用`(`code-it` 步骤 8.5 自含按需写单测)。

**触发/来源**:全部任务 = `详细设计`(沿用 REQ-00017 强约束 — **不**出现 `更新看板` 等纯协调类触发源)。

## 2. 任务总览

| 任务编号 | 需求 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件 | 关联任务 | 估算 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TASK-REQ-00037-00001 | REQ-00037 | 修改 | 详细设计 | [修改] code-fix 步骤 4/6 + 衔接 + 不要做的事同步 | 已完成 | 不适用 | `plugins/code-skills/skills/code-fix/SKILL.md` | — | 1 天 |
| TASK-REQ-00037-00002 | REQ-00037 | 修改 | 详细设计 | [修改] code-plan 步骤 27A/28A 末尾追加状态回写 | 已完成 | 不适用 | `plugins/code-skills/skills/code-plan/SKILL.md` | T-1 | 0.5 天 |
| TASK-REQ-00037-00003 | REQ-00037 | 修改 | 详细设计 | [修改] code-it 步骤 21 末尾追加状态回写:开发中 | 已完成 | 不适用 | `plugins/code-skills/skills/code-it/SKILL.md` | T-1 | 0.5 天 |
| TASK-REQ-00037-00004 | REQ-00037 | 修改 | 详细设计 | [修改] code-it 步骤 24 末尾追加状态回写:待审查 | 已完成 | 不适用 | `plugins/code-skills/skills/code-it/SKILL.md` | T-1 | 0.5 天 |
| TASK-REQ-00037-00005 | REQ-00037 | 修改 | 详细设计 | [修改] code-check 步骤 1.5 BUG-NNN 识别 + 步骤 13 状态回写 | 已完成 | 不适用 | `plugins/code-skills/skills/code-check/SKILL.md` | T-1 | 1 天 |
| TASK-REQ-00037-00006 | REQ-00037 | 修改 | 详细设计 | [修改] code-dashboard 步骤 4 段 3 扩展待修复/已修复分类 | 已完成 | 不适用 | `plugins/code-skills/skills/code-dashboard/SKILL.md` | T-1 | 0.5 天 |
| TASK-REQ-00037-00007 | REQ-00037 | 文档 | 详细设计 | [文档] 端到端验证 AC-1 ~ AC-10 + 末尾兜底提交 | 已完成 | 不适用 | (无生产代码改动) | T-1 ~ T-6 | 1 天 |

> **任务双状态字段解释**:
> - **开发状态**:6 枚举(`待开始` / `进行中` / `已完成` / `已取消` / `阻塞` / `待重新评估`)
> - **测试状态**:沿用 V0.0.3 修订 — 2 选 1(`已运行-通过` / `不适用`);本需求 7 任务统一填 `不适用`(纯文档 / 配置变更任务,**不**写单测;由 `code-it` 步骤 8.5 自含按需写单测判定)

## 3. 任务详情

### TASK-REQ-00037-00001:[修改] code-fix 步骤 4/6 + 衔接 + 不要做的事同步

- **目标**:把 `code-fix` 状态推进收敛到"前 5 段 + 入口 `待处理`",不再参与"待开发及之后"
- **涉及文件**:
  - `plugins/code-skills/skills/code-fix/SKILL.md`
- **关键变更**(语义化定位):
  - C-fix-1:`code-fix` SKILL.md 步骤 6 "新建分支" → "文档头" 字段写盘(line 281)— 字面 `报告` → `待处理`
  - C-fix-2:`code-fix` SKILL.md 步骤 4 状态推进表(line 247-251)— 新增 5 行(`待处理` 行可推进 / `待开发 / 开发中 / 待审查 / 已完成` 行不推进)
  - C-fix-3:`code-fix` SKILL.md 步骤 5 注脚(line 269)— 字面替换为 `不推进"待开发 / 开发中 / 待审查 / 已完成"等状态`
  - C-fix-4:`code-fix` SKILL.md 步骤 9 引导下一步表(line 347-354)— 4 行字面改为 `(由 ... 推进,本技能不参与)`
  - C-fix-5:`code-fix` SKILL.md ## 衔接 段"典型完整流程"(line 356-362)— 10 步 → 4 步
  - C-fix-6:`code-fix` SKILL.md ## 不要做的事 段(line 433-434)— 字面追加 `不推进"待开发 / 开发中 / 待审查 / 已完成" 状态`
- **边界与异常**:
  - **E-1**:历史 BUG 状态保留 — 本任务的修改**不**影响 `BUG-00001 / 02 / 03` 的 `fix/<BUG-NNN>/RESULT.md` 文件内容
  - **E-2**:用户复跑 `code-fix BUG-NNN`,BUG 处于 `待开发 / 开发中 / 待审查` 中段 → 屏显警告,**不**修改文件
  - **E-3**:`code-fix` 步骤 4 候选目标状态表新增 5 行(沿用既有 6 行结构);**不**重排既有行
- **验证手段**:
  - **AC-1**:调 `/code-fix "测试"` → 期望 `fix/BUG-NNNNN/RESULT.md` 文档头"状态" = `待处理`
  - **AC-6**:调 `/code-fix BUG-NNNNN`(BUG 处于 `待开发`)→ 屏显警告,**不**修改文件
- **回退方式**:`git revert HEAD`(单 commit 模式);若发现"步骤 6 状态字段"误改,人工还原 line 281 的字面

### TASK-REQ-00037-00002:[修改] code-plan 步骤 27A/28A 末尾追加状态回写

- **目标**:在 `code-plan` 完成 9 份文档产出后,自动推进缺陷状态到 `待开发`
- **涉及文件**:
  - `plugins/code-skills/skills/code-plan/SKILL.md`
- **关键变更**(语义化定位):
  - C-plan-1:`code-plan` SKILL.md §"缺陷分支" 步骤 27A "用 Edit 更新" 段尾(line 820-837)— 追加 `planStateRollback` 子步骤(详 §5.2 伪代码)
  - C-plan-2:`code-plan` SKILL.md §"缺陷分支" 步骤 28A 末尾(line 839-848)— 追加 `syncKanbanBugList` 调用(沿用既有)
  - C-plan-3:`code-plan` SKILL.md ## 不要做的事 段— 字面追加 `不修改 fix/<BUG-NNN>/RESULT.md 的"缺陷描述" 等稳定章节`
- **边界与异常**:
  - **E-1**:历史 BUG 状态保留 — 步骤 27A 判定若旧状态 ∈ {`报告`, `调查中`, `修复规划中`} 等老字面,**维持**原字面(沿用 NFR-3)
  - **E-2**:BUG 状态已推进过(`待开发 / 开发中 / 待审查 / 已完成`)→ 跳过,**不**重复动作
  - **E-3**:BUG 状态字段缺失或无法解析 → 屏显 `⚠ code-plan 状态回写:无法识别当前状态字面`
- **验证手段**:
  - **AC-2**:调 `/code-plan BUG-NNNNN` → 期望 `fix/<BUG-NNN>/RESULT.md` 文档头"状态" 从 `待处理` 推进到 `待开发`
- **回退方式**:若发现"状态回写"误推进(把老字面推成 `待开发`),人工编辑 `fix/<BUG-NNN>/RESULT.md` 文档头"状态"字段还原;同时撤销本任务 commit

### TASK-REQ-00037-00003:[修改] code-it 步骤 21 末尾追加状态回写:开发中

- **目标**:在 `code-it` 启动 BUG 第 1 个任务时,自动推进缺陷状态到 `开发中`
- **涉及文件**:
  - `plugins/code-skills/skills/code-it/SKILL.md`
- **关键变更**(语义化定位):
  - C-it-1:`code-it` SKILL.md §"缺陷分支" 步骤 21 "处理缺陷状态与本轮起点" 末尾(line 891-900)— 追加 `itStartStateRollback` 子步骤(详 §5.3 伪代码)
- **边界与异常**:
  - **E-1**:本任务**非**PLAN.md 任务总览首条 → 状态维持,**不**推进
  - **E-2**:BUG 当前状态 ≠ `待开发` → 跳过(已推进过 或 老字面)
  - **E-3**:`code-it` 步骤 0a 前置任务守卫已拦截(沿用既有 E-2:用户跳过中间任务)
- **验证手段**:
  - **AC-3**:调 `/code-it TASK-BUG-NNNNN-00001` → 期望完成实施后,BUG 状态推进为 `开发中`
- **回退方式**:同 T-2

### TASK-REQ-00037-00004:[修改] code-it 步骤 24 末尾追加状态回写:待审查

- **目标**:在 `code-it` 完成 BUG 全部任务后,自动推进缺陷状态到 `待审查`
- **涉及文件**:
  - `plugins/code-skills/skills/code-it/SKILL.md`
- **关键变更**(语义化定位):
  - C-it-2:`code-it` SKILL.md §"缺陷分支" 步骤 24 "同步 fix/<缺陷编号>/RESULT.md 与看板" 末尾(line 955-985)— 追加 `itEndStateRollback` 子步骤(详 §5.4 伪代码)
- **边界与异常**:
  - **E-1**:仍有任务未完成(开发状态 ∈ {`待开始`, `进行中`, `阻塞`})→ 状态维持
  - **E-2**:BUG 当前状态 ∉ {`待开发`, `开发中`} → 跳过
- **验证手段**:
  - **AC-4**:完成所有 `TASK-BUG-NNNNN-NNNNN` 任务后 → 期望 BUG 状态推进为 `待审查`
- **回退方式**:同 T-2

### TASK-REQ-00037-00005:[修改] code-check 步骤 1.5 BUG-NNN 识别 + 步骤 13 状态回写

- **目标**:扩展 `code-check` 接受 BUG-NNN 入参;评审完成后自动推进缺陷状态到 `已完成`
- **涉及文件**:
  - `plugins/code-skills/skills/code-check/SKILL.md`
- **关键变更**(语义化定位):
  - C-check-1:`code-check` SKILL.md 步骤 1.5 "模式选择"(line 230-239)— 新增第 3 态 `^BUG-\d{5}$` → BUG 路径
  - C-check-2:`code-check` SKILL.md 步骤 5-12(评审 8.1 ~ 8.13)— **字节级沿用**(本任务**不**修改)
  - C-check-3:`code-check` SKILL.md 步骤 13 "同步版本看板" 末尾(line 498-512)— 追加第 6 款 `checkStateRollback` 子步骤(详 §5.5 伪代码)
  - C-check-4:`code-check` SKILL.md ## 不要做的事 段— 字面追加 `不修改 fix/<BUG-NNN>/RESULT.md 的"缺陷描述" 等稳定章节`
- **边界与异常**:
  - **E-1**:`code-check REQ-NNNNN` 既有行为**不**被破坏(沿用 C-check-2 字节级不变)
  - **E-2**:BUG 当前状态 ≠ `待审查` → 跳过(已推进过 或 老字面)
  - **E-3**:`code-check` 评审发现"必须改"派生改修任务时,BUG 状态维持 `待审查`(沿用 E-3 风险)
- **验证手段**:
  - **AC-5**:调 `/code-check BUG-NNNNN` → 期望产出 `review/<BUG-NNN>/REVIEW-REPORT.md` + 状态推进为 `已完成`
  - **AC-9**:调 `/code-check REQ-NNNNN` → 期望既有行为字节级不变
- **回退方式**:若发现"BUG 路径" 误触发(把 REQ 任务识别为 BUG),人工检查步骤 1.5 模式选择逻辑;若发现"步骤 13 状态回写"误推进,人工编辑 `fix/<BUG-NNN>/RESULT.md` 还原

### TASK-REQ-00037-00006:[修改] code-dashboard 步骤 4 段 3 扩展待修复/已修复分类

- **目标**:扩展 `code-dashboard` 段 3"高优先级缺陷"判定逻辑,纳入 5 新状态字面
- **涉及文件**:
  - `plugins/code-skills/skills/code-dashboard/SKILL.md`
- **关键变更**(语义化定位):
  - C-dash-1:`code-dashboard` SKILL.md 步骤 4 段 3 "高优先级缺陷"(line 229-240)— `classifyState` 判定逻辑扩展:`b.status !== "已修复" → b.status !== "已完成" && b.status !== "已修复-已验证" && b.status !== "已关闭"`
- **边界与异常**:
  - **E-1**:`code-dashboard` 步骤 4 段 2 "5 类状态占比"分类骨架**不**修改(沿用 NFR-8)
  - **E-2**:老字面(10 个)继续按既有路径归类(沿用 PD-2 锁定,**不**归一化)
- **验证手段**:
  - **AC-10**(部分):调 `/code-dashboard` → 期望段 3 屏显"P0 待修复: N"包含 5 新字面 + 老字面 BUG 数
- **回退方式**:若发现"段 3 屏显异常",人工检查 `classifyState` 函数逻辑

### TASK-REQ-00037-00007:[文档] 端到端验证 AC-1 ~ AC-10 + 末尾兜底提交

- **目标**:验证 6 个修改任务(T-1 ~ T-6)落地后满足 AC-1 ~ AC-10 全部验收标准;末尾兜底提交
- **涉及文件**:
  - (无生产代码改动;仅验证)
- **关键变更**:
  - **不**修改任何 SKILL.md / templates/
  - 验证步骤:
    1. 创建 1 个测试 BUG,跑完整 4 步流程
    2. 调 `/code-dashboard` 看段 3 屏显
    3. 跑 AC-1 ~ AC-10 全部 10 条验收
- **边界与异常**:
  - **E-1**:AC-1 ~ AC-10 任一条不通过 → 不提交,修复后重跑
  - **E-2**:历史 BUG 状态保留 — 验证完不修改 `BUG-00001 / 02 / 03` 的文件内容(沿用 NFR-3)
- **验证手段**:沿用 `risk-analysis.md` §"测试要点"的 AC-1 ~ AC-10
- **回退方式**:若 1 个或多个 AC 不通过,**不**提交;回到对应任务(T-1 ~ T-6)修复

## 4. 任务依赖图

```
T-1 ──────┬─────────┬─────────┬─────────┬─────────┐
          ↓         ↓         ↓         ↓         ↓
         T-2       T-3       T-4       T-5       T-6
          │         │         │         │         │
          └─────────┴─────────┴─────────┴─────────┘
                                  │
                                  ↓
                                 T-7
```

**依赖关系表**:

| 任务 | 依赖 | 备注 |
| --- | --- | --- |
| T-1 | — | 入口任务;定义"状态字段约定"(5 新字面 + 老字面共存) |
| T-2 | T-1 | `code-plan` 步骤 27A 判定逻辑依赖 T-1 的状态字面约定 |
| T-3 | T-1 | `code-it` 步骤 21 判定逻辑依赖 T-1 |
| T-4 | T-1 | `code-it` 步骤 24 判定逻辑依赖 T-1 |
| T-5 | T-1 | `code-check` 步骤 13 状态回写依赖 T-1 |
| T-6 | T-1 | `code-dashboard` 段 3 字面集合依赖 T-1 |
| T-7 | T-1 ~ T-6 | 端到端验证依赖前 6 任务的代码落地 |

**并行性**:T-2 / T-3 / T-4 / T-5 / T-6 之间**无依赖**;理论上可并行(但本仓库习惯串行执行 `code-it` 任务,沿用既有约定)。

## 5. 里程碑

| 里程碑 | 包含任务 | 完成定义 | 状态 | 计划时间 | 实际完成 |
| --- | --- | --- | --- | --- | --- |
| M1-REQ-00037 | T-1 ~ T-7(全部 7 任务) | 7 任务开发状态=已完成 ∧ 测试状态=不适用;AC-1 ~ AC-10 全通过;1 次 commit 落地(7 个 commit 或 T-7 末尾兜底 1 个 commit) | 待开始 | 2026-06-22 ~ 2026-06-28(5-6 天) | — |

## 6. 状态管理规则

### 6.1 双状态字段语义(沿用既有)

- **开发状态**:6 枚举(`待开始` / `进行中` / `已完成` / `已取消` / `阻塞` / `待重新评估`)
- **测试状态**:沿用 V0.0.3 修订 — 2 选 1(`已运行-通过` / `不适用`);本需求 7 任务统一填 `不适用`(沿用既有)
- **任务"真正可发布" = 开发状态=已完成**

### 6.2 状态变更规则(沿用既有)

- **任务初始状态**:开发状态=`待开始`;测试状态=`不适用`
- **任务完成流程**:开发状态 `待开始` → `进行中` → `已完成`;测试状态维持 `不适用`
- **任务取消**:用户主动调 `code-plan` 增量更新 → 任务标 `已取消`(开发状态)
- **任务阻塞**:用户报告阻塞原因 → 任务标 `阻塞`(开发状态);解除后回 `进行中`

### 6.3 触发/来源字段(沿用既有 13 枚举)

- 本需求 7 任务全部填 `详细设计`(沿用 REQ-00017 强约束 — **不**出现 `更新看板` 等纯协调类)

## 7. 关联计划

| 关联计划 | 关联点 | 影响 |
| --- | --- | --- |
| REQ-00027(已落地) | `code-fix` 改为纯登记型 + 下游接力 | 本计划是 REQ-00027 落地的"用户体验改进";FR-6 收敛 `code-fix` 状态推进范围 |
| REQ-00022(已落地) | `code-review` → `code-check` 重命名 | T-5 扩展 `code-check` 接受 BUG-NNN 入参依赖此重命名 |
| REQ-00034(已落地) | 移除 `code-unit`,整合进 `code-it` | 与 T-3 / T-4 协同一致(单测在 `code-it` 步骤 8.5 内部完成) |
| REQ-00036(已落地) | 清理 SKILL.md + templates/ 中的开发痕迹 | 本计划 7 任务的代码落地后,SKILL.md 仍按 REQ-00036 的清理标准 |

## 8. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-22 09:28 | v1 | 初始创建 | 完成首次编码计划;7 任务严格串行;AC-1 ~ AC-10 全部纳入 T-7 验证;`--balanced` + 功能性=高 + 健壮性=高 + 可维护性=高 | 用户 |
| 2026-06-22 09:35 | v1 | 状态更新 | TASK-REQ-00037-00001 状态"待开始"→"已完成";`code-fix/SKILL.md` 6 处关键变更落地;`git diff --stat` 1 file changed, 25 insertions(+), 30 deletions(-) | wangmiao |
| 2026-06-22 09:45 | v1 | 状态更新 | TASK-REQ-00037-00002 状态"待开始"→"已完成";`code-plan/SKILL.md` 步骤 27A 末尾 `planStateRollback` 子步骤 + 步骤 28A `syncKanbanBugList` 调用 + ## 不要做的事 段字面追加落地;`git diff --stat` 1 file changed, 24 insertions(+), 7 deletions(-);AC-2 静态校验通过 | wangmiao |
| 2026-06-22 09:55 | v1 | 状态更新 | TASK-REQ-00037-00003 状态"待开始"→"已完成";`code-it/SKILL.md` §"缺陷分支" 步骤 21 末尾 `itStartStateRollback` 子步骤 + ## 不要做的事 段字面追加落地;`git diff --stat` 1 file changed, 20 insertions(+), 6 deletions(-);AC-3 静态校验通过 | wangmiao |
| 2026-06-22 10:05 | v1 | 状态更新 | TASK-REQ-00037-00004 状态"待开始"→"已完成";`code-it/SKILL.md` §"缺陷分支" 步骤 24 末尾 `itEndStateRollback` 子步骤落地;`git diff --stat` 1 file changed, 32 insertions(+), 6 deletions(-);AC-4 静态校验通过 | wangmiao |
| 2026-06-22 10:35 | v1 | 状态更新 | TASK-REQ-00037-00005 状态"待开始"→"已完成";`code-check/SKILL.md` 步骤 1.5 三态机 → 四态机(新增 BUG-NNNNN)+ 步骤 13 末尾 `checkStateRollback` 子步骤 + ## 不要做的事 段字面追加 2 行落地;`git diff --stat` 1 file changed, 14 insertions(+), 1 deletion(-);AC-5 / AC-9 静态校验通过 | wangmiao |
| 2026-06-22 11:05 | v1 | 状态更新 | TASK-REQ-00037-00006 状态"待开始"→"已完成";`code-dashboard/SKILL.md` §步骤 4 段 3 `classifyState` 判定逻辑扩展(字面集合:`已完成` + `已修复-已验证` + `已关闭` 算"已修复",其他字面都算"待修复");`git diff --stat` 1 file changed, 1 insertion(+), 1 deletion(-);AC-10(部分)静态校验通过 | wangmiao |
| 2026-06-22 12:06 | v1 | 状态更新 | TASK-REQ-00037-00007 状态"待开始"→"已完成";端到端 AC 静态校验通过 10 / 10(AC-1 ~ AC-10);T-1 ~ T-7 全部 7 任务开发状态=已完成 ∧ 测试状态=不适用;末尾兜底 1 次 commit 落地(T-3 ~ T-7 累积,沿用 PLAN.md §5 M1);M1-REQ-00037 里程碑达成 | wangmiao |