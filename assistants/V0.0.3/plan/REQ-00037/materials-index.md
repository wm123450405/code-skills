# 材料登记 — REQ-00037

更新时间:2026-06-22 09:28
版本:V0.0.3

## 项目级规范

(共 13 份,详见 `./assistants/V0.0.3/design/REQ-00037/materials-index.md` "项目级规范"部分)

## 上游需求

- 来源:`./assistants/V0.0.3/require/REQ-00037/RESULT.md` (v1,2026-06-22 09:02)
- 提取:**8 FR / 8 NFR / 10 AC / 4 Q(待用户决策)**

## 上游概要设计

- 来源:`./assistants/V0.0.3/design/REQ-00037/RESULT.md` (v1,2026-06-22 09:18)
- 提取:
  - 设计目标:`--balanced` + 功能性 = 高(沿用)
  - 6 个功能域 + 5 关键决策 + 4 备选方案 + 11 不变量
  - 7 任务候选(估算 5-6 天)

## 项目现状(实现细节)

### 命名风格

- SKILL.md 标题:`# <技能名> — <一句话描述>(沿用 `skill-conventions §规则 1` frontmatter `description` 字段)
- 步骤锚点:`### 步骤 N — <一句话描述>` / `#### N.M <子步骤锚点>`
- 状态字面:`报告 / 调查中 / 已关闭-非缺陷 / 已取消 / 阻塞 / 修复规划中 / 修复编码中 / 已修复-待验证 / 已修复-已验证 / 已关闭`(10 个老字面,**本设计新增 5 个**)

### 错误模型 / 并发原语

- 不适用(本仓库是 SKILL.md 文档库,**不**有运行时错误模型)

### 既有相似功能的实现风格

- `code-fix` SKILL.md 步骤 4 状态推进表(line 247-251)用 Markdown 表格表达候选目标状态
- `code-fix` 步骤 6 文档头"状态"字段(line 281)用 `状态:` 字段 + 修复日志时序追加
- `code-plan` 步骤 27A(line 820-837)用 `Edit` 工具写状态字段 + 同步 `fix/RESULT.md`
- `code-it` 步骤 21/24(line 891-900/955-985)用 `Edit` 工具写状态字段 + 同步看板
- `code-check` 步骤 13(line 498-512)同步版本看板"缺陷清单"区段(line 506-507)

### 既有测试用例的风格与覆盖度

- 不适用(本仓库 SKILL.md 不写自动化测试;V0.0.3 沿用 `code-it` 步骤 8.5 自含按需写单测)

### 可复用的工具函数/中间件

- 既有 `Edit` 工具(用于状态字段写回;每个"状态回写"子步骤都直接复用)
- 既有 `Read` 工具(用于读 `fix/<BUG-NNN>/RESULT.md` 当前状态)
- 既有 `dashboard-conventions §规则 1` 锚点 `^## 任务总览$` / `^## 缺陷清单$` 解析

## 上游需求 × 上游概要设计 × 本详细设计的交叉点

| FR | 上游设计 § | 本详细设计 § |
| --- | --- | --- |
| FR-1(`code-fix` 初始状态 `待处理`) | §6 功能域 1 | §3 模块详细化 §6.1 + §5 算法 §5.1 |
| FR-2(`code-plan` 完成 → `待开发`) | §6 功能域 2 | §3.2 + §5.2 |
| FR-3(`code-it` 第 1 任务 → `开发中`) | §6 功能域 3 | §3.3 + §5.3 |
| FR-4(`code-it` 全部完成 → `待审查`) | §6 功能域 3 | §3.3 + §5.4 |
| FR-5(`code-check` 完成 → `已完成`) | §6 功能域 4 | §3.4 + §5.5 |
| FR-6(`code-fix` 不推进"待开发及之后") | §6 功能域 1 + §11 备选 | §6.1 + §7 异常处理 E-2 |
| FR-7(状态字面共存不归一化) | §9 + §2.5 | §4 数据结构 + §8 状态机 |
| FR-8(典型完整流程 SKILL.md 同步) | §6 各功能域 + §10 | §3 各模块 + §10 集成点 |

## CLI 参数

(本轮无 `--result` / `--plan` 参数,无模板填充)