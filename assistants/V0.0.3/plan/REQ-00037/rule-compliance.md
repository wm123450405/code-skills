# 规范遵循记录 — REQ-00037

更新时间:2026-06-22 09:28
版本:V0.0.3

## 1. 本次参考的规范文件

(共 13 份,详见 `materials-index.md` "项目级规范"部分)

## 2. 规范 vs 现状偏离

(本详设**无**规范与现状偏离——所有修改都是"修改既有 SKILL.md"而非"新建",且修改都遵循既有结构)

## 3. 规范 vs 需求冲突

### C-1:是否触发 `dashboard-conventions §规则 1` 三方同步?

- **规范条款**:`./assistants/rules/dashboard-conventions.md §规则 1`:看板字段约定扩展需**三方同步**(模板 + CLAUDE.md + 本规范)
- **触发条件**(沿用 §规则 1):
  1. 新增/删除/重命名"区段"
  2. 新增/删除/重命名"表格列"
  3. 新增/删除/修改"枚举值"
  4. 调整"字段语义说明"
- **本详设动作**:
  - FR-1 ~ FR-5 在 `fix/<BUG-NNN>/RESULT.md` 文档头"状态"字段写新字面
  - FR-7 扩展 `code-dashboard` 步骤 4 段 3 判定规则
  - 看板"缺陷清单"区段"状态"列字面**直接**写新字面
- **判定**:**不触发** §规则 1(沿用概设 §2.5.2 详细论证 — 状态字段是"自由字符串列",字面变更**不**算"枚举值变更")
- **解决时间**:2026-06-22 09:28
- **解决依据**:看板"任务清单"区段"开发状态"列是**枚举值列**(枚举值变更需三方同步);"缺陷清单"区段"状态"列是**自由字符串列**(字面变更不需三方同步)

### C-2:是否触发 `skill-conventions §规则 2`(SKILL.md 不含开发痕迹)?

- **规范条款**:`./assistants/rules/skill-conventions.md §规则 2`:SKILL.md + templates/ 中**不得**包含 6 类开发痕迹
- **本详设动作**:
  - 4 个 SKILL.md 中**新写**若干段落(状态回写子步骤 / 状态推进表新增行 / 典型完整流程新举例)
  - **不**写"本需求 REQ-00037 新增"类尾注
  - **不**写"原 code-fix 状态机"类回溯
  - **不**写"Q-N 锁定"决策记录
  - **不**写"YYYY-MM-DD 起生效"标记
  - **不**引用已退场文件名
- **判定**:**不触发** §规则 2(本设计新写段落全部符合要求)
- **解决时间**:2026-06-22 09:28

### C-3:详细化是否越界(逾越 `code-design §9 数据结构`)?

- **规范条款**:`code-plan` 是**详细化**(展开字段类型 / 约束 / 索引 / 接口 schema / 错误码 / 完整入参出参),与 `code-design` 概设(≤ 5 字段 / 一句话关系)职责正交
- **本详设动作**:
  - §3 模块详细化:展开每个 SKILL.md 修改的"目标位置 / 已有结构 / 新增内容 / 相对位置"(语义化定位,沿用 `code-plan` "## 修改文件定位的语义化约定")
  - §5 算法:伪代码 / 流程图 / 状态机(本设计核心状态机)
  - §4 数据结构:15 个状态字面的枚举值集合 + 字段位置 + 状态推进归属(沿用概设 §9.1)
  - §6 接口:5 处"状态回写"子步骤的接口(仅给出接口名 / 入参 / 出参 / 错误处理,**不**展开完整 JSON schema / 错误码)
- **判定**:**不**越界(详细化深度符合 `code-plan` 职责)
- **解决时间**:2026-06-22 09:28

## 4. 用户授权的偏离

(无)

## 5. 规范变更响应(增量更新时填写)

(本轮为首次设计,无规范变更)