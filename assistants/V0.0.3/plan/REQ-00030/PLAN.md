# 编码计划 — REQ-00030 优化 /code-design 与 /code-plan 职责分离

- 需求编码:REQ-00030
- 所属版本:V0.0.3
- 计划状态:已完成(待 code-it 实施)
- 创建:2026-06-12
- 最近更新:2026-06-12 14:31

## 计划概述

按"5 个被改文件各 1 任务 + 1 个验证任务"拆分为 **6 个任务**(T-001 ~ T-006),全部为纯文档/技能定义类任务,测试状态=`不适用`。

**整体设计目标**:`--extensible`
**功能性**:高
**扩展性**:高(用户主动选 → 触发 FR-8 三阶段下沉)

> **架构骨架任务处理(--extensible 触发)**:本需求**不**实际写"架构骨架"代码(本需求**改**的是元技能定义,而非写新代码);"扩展点预留"作为 T-001 的子项体现(`code-design/SKILL.md` 步骤 0b 的"## 设计目标"小节沿用 + 7 维度问路沿用 + 字节级保留 INV-1~INV-9)。

## 任务总览

| 任务编号 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件 |
| --- | --- | --- | --- | --- | --- | --- |
| TASK-REQ-00030-00001 | 修改 | 详细设计 | [修改] code-design/SKILL.md:步骤 0b 收敛 + 9A/10A/11A 输出深度收窄 | 已完成 | 不适用 | plugins/code-skills/skills/code-design/SKILL.md §步骤 0b + §步骤 9A/10A/11A |
| TASK-REQ-00030-00002 | 修改 | 详细设计 | [修改] templates/design.md:§7/§8/§9/§10 章节重写 + §7.5/§8.5/§9.5 新增 | 已完成 | 不适用 | plugins/code-skills/skills/code-design/templates/design.md §7-§10 + 顶部注释 |
| TASK-REQ-00030-00003 | 修改 | 详细设计 | [修改] code-plan/SKILL.md:步骤 7A 补做强约束 + 步骤 10A 架构骨架触发收紧 | 待开始 | 不适用 | plugins/code-skills/skills/code-plan/SKILL.md §步骤 7A + §步骤 10A |
| TASK-REQ-00030-00004 | 修改 | 详细设计 | [修改] templates/plan.md:§4-§10 章节由"建议"改"必填" | 待开始 | 不适用 | plugins/code-skills/skills/code-plan/templates/plan.md §4-§12 |
| TASK-REQ-00030-00005 | 修改 | 详细设计 | [修改] code-check/SKILL.md:评审清单追加 3 校验点(详设完整性 / 概设越界 / 行数比例) | 待开始 | 不适用 | plugins/code-skills/skills/code-check/SKILL.md §步骤 6 评审清单 |
| TASK-REQ-00030-00006 | 文档 | 详细设计 | [文档] 行数收敛观测:落地后 3 个新需求验证 NFR-1 | 待开始 | 不适用 | (无文件修改,纯人工观测) |

**统计**:6 个任务 / 0 派生"更新看板"任务(沿用 REQ-00017 强约束) / 0 真实代码任务(全部为技能定义/模板/观测类)

## 任务详情

### TASK-REQ-00030-00001 · [修改] code-design/SKILL.md:步骤 0b 收敛 + 9A/10A/11A 输出深度收窄

**目标**:在 `code-design/SKILL.md` 步骤 0b 收敛(小需求 0 问默认)+ 步骤 9A 模块表收窄(5 列)+ 步骤 10A 接口/数据结构收窄(4 项)+ 步骤 11A 三方依赖收窄(4 项)。**不**改既有"## 工作流程"步骤 0a / 0b.0 / 0 / 1-5。

**涉及文件**:
- `plugins/code-skills/skills/code-design/SKILL.md §步骤 0b` 在"自适应问题数"小节**追加**本需求 FR-1 / FR-2 修订逻辑
- `plugins/code-skills/skills/code-design/SKILL.md §步骤 0b` "职责分离"声明**追加**本需求"小需求 = 0 问默认"一行
- `plugins/code-skills/skills/code-design/SKILL.md §步骤 9A` 模块表**只**列 5 列(沿用既有"模块名 / 路径 / 状态 / 职责 / 依赖"列名)
- `plugins/code-skills/skills/code-design/SKILL.md §步骤 10A` 接口概要**只**列 4 项;数据结构**只**列 4 项
- `plugins/code-skills/skills/code-design/SKILL.md §步骤 11A` 三方依赖每行**只**列 4 项(依赖名 / 一句话用途 / 必要性 / 替代评估)
- `plugins/code-skills/skills/code-design/SKILL.md §## 不要做的事`:**不**修改(NFR-3 字节级保留)
- `plugins/code-skills/skills/code-design/SKILL.md frontmatter L1-3`:**不**修改(INV-1)

**关键变更**(按结构化语义定位):

1. **§步骤 0b"自适应问题数"小节** → **追加**本需求 FR-1 / FR-2 修订逻辑(5 行 ~ 10 行)
2. **§步骤 0b"职责分离"声明** → **追加**本需求"小需求 = 0 问默认"一行(1 行)
3. **§步骤 9A"模块拆分"** → 移除"关键设计点 / 对外接口 / 依赖 / 理由"等字段描述(沿用既有表格,改"填表要求"描述)
4. **§步骤 10A"接口与数据结构"** → 移除"数据格式 / 错误码 / 鉴权 / 版本策略 / 字段类型 / 约束 / 索引 / 存储选型"等字段描述
5. **§步骤 11A"三方依赖评估"** → 移除"许可协议 / 维护活跃度 / 体积 / 安全态势"等评估项,改"只列 4 项"
6. **§步骤 7B / 8B / 9B / 10B / 步骤 N** → 既有"## 工作流程"步骤**不**重写

**结构化语义锚点**:
- 锚点 1:`§步骤 0b` 子标题(以"### 步骤 0b"开头)
- 锚点 2:`§步骤 0b` "自适应问题数"小节(以"自适应问题数"开头)
- 锚点 3:`§步骤 0b` "职责分离"声明(以"> 职责分离"开头)
- 锚点 4:`§步骤 9A` "模块拆分"小节(以"### 步骤 9A"开头)
- 锚点 5:`§步骤 10A` "接口与数据结构"小节(以"### 步骤 10A"开头)
- 锚点 6:`§步骤 11A` "三方依赖评估"小节(以"### 步骤 11A"开头)

**边界与异常**:
- INV-1:frontmatter L1-3 字节级保留
- INV-2:"## 不要做的事" 字节级保留
- INV-3:既有"## 工作流程"步骤 0a / 0b.0 / 0 / 1-5 字节级保留

**验证手段**(对应 AC-1 / AC-2):
- AC-1.1:对 1 任务 REQ 调 code-design,不触发 AskUserQuestion
- AC-1.2:对 2-5 任务 REQ 调 code-design,触发 1 个 AskUserQuestion
- AC-1.3:对 ≥6 任务 REQ 调 code-design,触发 2 个 AskUserQuestion
- AC-1.4:对含"待评估三方依赖" REQ 调 code-design,展示 --extensible 选项
- AC-2.1:对一个新 REQ 调 code-design,生成的 design/.../RESULT.md §7.2 模块表只含 5 列
- AC-2.2:§8 接口概要只列 4 项 / 接口
- AC-2.3:§9 数据结构只列 4 项 / 实体
- AC-2.4:§10 三方依赖每行只列 4 项
- AC-2.5:整体行数较既有 10 个 REQ 平均值减 ≥ 30%

**回退方式**:`git revert <commit>`,无副作用(纯 markdown 改动)。

---

### TASK-REQ-00030-00002 · [修改] templates/design.md:§7/§8/§9/§10 章节重写 + §7.5/§8.5/§9.5 新增

**目标**:重写 `templates/design.md` §7 模块表(7→5 列)/ §8 接口概要(6→4 列)/ §9 数据结构(6→4 列)/ §10 三方依赖(7→4 列),并新增 §7.5 / §8.5 / §9.5 "## 设计边界"小节。**不**改 §1-§6 / §11-§16 既有章节。

**涉及文件**:
- `plugins/code-skills/skills/code-design/templates/design.md` 模板顶部注释**追加**一行
- `plugins/code-skills/skills/code-design/templates/design.md §7 模块划分 §7.1` 模块总览表列数 7→5
- `plugins/code-skills/skills/code-design/templates/design.md §7.2 新增模块` 列数 7→5
- `plugins/code-skills/skills/code-design/templates/design.md §7.3 / §7.4 复用既有 / 修改既有` 沿用
- `plugins/code-skills/skills/code-design/templates/design.md §7.5 设计边界` **新增**(本需求)
- `plugins/code-skills/skills/code-design/templates/design.md §8 接口概要` 6→4 项
- `plugins/code-skills/skills/code-design/templates/design.md §8.5 设计边界` **新增**
- `plugins/code-skills/skills/code-design/templates/design.md §9 数据结构` 6→4 项
- `plugins/code-skills/skills/code-design/templates/design.md §9.5 设计边界` **新增**
- `plugins/code-skills/skills/code-design/templates/design.md §10 三方依赖` 7→4 项
- `plugins/code-skills/skills/code-design/templates/design.md §3 / §4 / §5 / §6 / §11 / §12 / §13 / §14 / §15 / §16`:**不**修改

**关键变更**(按结构化语义定位):

1. **模板顶部注释**(`# RESULT.md 模板 — 概要设计` 之后)→ **追加**一行 `> 本模板只覆盖概要设计。完整字段/接口 schema/算法伪代码/迁移脚本等详见 code-plan 的 templates/plan.md §4-§10`
2. **§7.1 模块总览表**(`### 7.1 模块总览` 子节)→ 7 列改 5 列(移除"对外接口" / "理由" / "符合的规范条款")
3. **§7.2 新增模块**(`### 7.2 新增模块` 子节)→ 列数 7→5(移除"关键设计点" / "对外接口" / "依赖" / "理由")
4. **§7.5 设计边界**(`### 7.5 设计边界` 子节)→ **新增**,内容:"以下细节由 code-plan 在 plan/.../RESULT.md §4 + module-details.md 展开:模块的内部类/函数签名、调用顺序、并发模型、状态归属、错误处理范式"
5. **§8 接口概要**(`### 8 接口概要` 子节)→ 6→4 项(移除"数据格式" / "错误码" / "鉴权" / "版本策略")
6. **§8.5 设计边界** → **新增**,内容:"完整入参/出参 schema、错误码、示例、版本策略、鉴权、限流、幂等、链路追踪字段由 code-plan 在 plan/.../RESULT.md §7 + interface-specs.md 展开"
7. **§9 数据结构**(`### 9 数据结构` 子节)→ 6→4 项(移除"字段类型" / "约束" / "索引" / "存储选型")
8. **§9.5 设计边界** → **新增**,内容:"完整字段类型/约束/索引/迁移脚本/存储选型由 code-plan 在 plan/.../RESULT.md §6 + data-changes.md 展开"
9. **§10 三方依赖**(`### 10 三方依赖` 子节)→ 7→4 项(移除"版本" / "来源" / "许可" / "活跃度")

**结构化语义锚点**:
- 锚点 1:`模板顶部注释`(以"---"分隔线为锚点)
- 锚点 2:`§7.1` 子标题(以"### 7.1 模块总览"开头)
- 锚点 3:`§7.2` 子标题(以"### 7.2 新增模块"开头)
- 锚点 4:`§7.5` 子标题(以"### 7.5 设计边界"开头,**新增**)
- 锚点 5:`§8` 子标题(以"### 8 接口概要"开头)
- 锚点 6:`§8.5` 子标题(以"### 8.5 设计边界"开头,**新增**)
- 锚点 7:`§9` 子标题(以"### 9 数据结构"开头)
- 锚点 8:`§9.5` 子标题(以"### 9.5 设计边界"开头,**新增**)
- 锚点 9:`§10` 子标题(以"### 10 三方依赖"开头)

**边界与异常**:
- INV-3:既有 §3 / §4 / §5 / §6 / §11 / §12 / §13 / §14 / §15 / §16 字节级保留
- INV-5:`./assistants/rules/*.md` 0 改(本任务**不**触发)

**验证手段**(对应 AC-3):
- AC-3.1:`templates/design.md` §7 / §8 / §9 章节按修订后结构生成
- AC-3.2:§7.5 / §8.5 / §9.5 "## 设计边界"小节存在
- AC-3.3:模板顶部注释含 `> 本模板只覆盖概要设计...` 一行

**回退方式**:`git revert <commit>`。

---

### TASK-REQ-00030-00003 · [修改] code-plan/SKILL.md:步骤 7A 补做强约束 + 步骤 10A 架构骨架触发收紧

**目标**:在 `code-plan/SKILL.md` 步骤 7A 强化"完整可直接编码的细节"补做职责;步骤 10A 架构骨架触发条件**追加**第 4 / 第 5 条件(FR-6)。

**涉及文件**:
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 7A` 修订"按概要设计逐模块展开"为强约束"完整可直接编码的细节:伪代码 / 字段 / 接口 schema"
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 7A` 强调"任何概要设计未覆盖的细节必须在详设阶段补全"
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 7A` 4 份过程文档由"可选"改"必选"
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A` 架构骨架"可省略"小节语义调整(小需求 / 整体 --balanced / 整体 --minimal 不插入架构骨架)
- `plugins/code-skills/skills/code-plan/SKILL.md §步骤 10A` "架构任务作为首个任务(条件性)"子段**追加**第 4 / 第 5 条件
- `plugins/code-skills/skills/code-plan/SKILL.md §## 不要做的事`:**不**修改(NFR-3)
- `plugins/code-skills/skills/code-plan/SKILL.md frontmatter L1-3`:**不**修改(INV-1)

**关键变更**(按结构化语义定位):

1. **§步骤 7A 首次:详细化设计**(`### 步骤 7A` 子节)→ 修订"按概要设计逐模块展开,产出可直接编码的细节"为强约束
2. **§步骤 7A** 4 份过程文档描述(`module-details.md` / `interface-specs.md` / `data-changes.md` / `risk-analysis.md`)→ 由"可选"改"必选"
3. **§步骤 10A 架构任务作为首个任务(条件性)**(`#### 架构任务作为首个任务(条件性)` 子段)→ **追加**第 4 / 第 5 触发条件
4. **§步骤 10A 触发条件 1-3** → 沿用既有(不重写)
5. **§步骤 10A 可省略**(`#### 可省略` 子段)→ 调整语义:小需求 / 整体 --balanced / 整体 --minimal 不插入架构骨架

**结构化语义锚点**:
- 锚点 1:`§步骤 7A` 子标题(以"### 步骤 7A"开头)
- 锚点 2:`§步骤 10A` 子标题(以"### 步骤 10A"开头)
- 锚点 3:`§步骤 10A 架构任务作为首个任务(条件性)` 子段(以"#### 架构任务作为首个任务"开头)
- 锚点 4:`§步骤 10A 可省略` 子段(以"#### 可省略"开头)

**边界与异常**:
- INV-1:frontmatter L1-3 字节级保留
- INV-2:"## 不要做的事" 字节级保留
- INV-3:既有"## 工作流程"步骤 0a / 0b.0 / 0b / 0-6 / 8A-9A / 11A-18A 字节级保留

**验证手段**(对应 AC-4 / AC-5):
- AC-4.1:`plan/.../RESULT.md` §4 / §5 / §6 / §7 / §8 / §9 / §10 / §11 / §12 全部存在
- AC-4.2:§4 模块详细化至少有 1 个模块的完整类/函数签名
- AC-4.3:§5 算法与逻辑至少有 1 个算法的伪代码块
- AC-4.4:§6 数据结构每个实体的字段表至少含 5 个字段
- AC-4.5:§7 接口细节至少有 1 个接口的完整入参/出参 JSON schema
- AC-4.6:4 份必选过程文档均存在且非空
- AC-5.1:1 任务 + --balanced REQ 调 code-plan,PLAN.md 任务总览不含"架构骨架"作为首个任务
- AC-5.2:含"新增三方依赖" REQ 调 code-plan,PLAN.md 任务总览第一个任务是"架构骨架"任务
- AC-5.3:--extensible + 跨 ≥3 模块 REQ 调 code-plan,PLAN.md 任务总览第一个任务是"架构骨架"任务

**回退方式**:`git revert <commit>`。

---

### TASK-REQ-00030-00004 · [修改] templates/plan.md:§4-§10 章节由"建议"改"必填"

**目标**:在 `templates/plan.md` §4-§10 章节由"建议填"改为"必填";若某章节无内容,显式写"本需求不涉及"。

**涉及文件**:
- `plugins/code-skills/skills/code-plan/templates/plan.md §4 / §5 / §6 / §7 / §8 / §9 / §10 / §11 / §12` 章节描述由"建议填"改"必填"
- `plugins/code-skills/skills/code-plan/templates/plan.md §1 / §2 / §3 / §13 / §14 / §15`:**不**修改

**关键变更**(按结构化语义定位):

1. **§4 模块详细化**(`### 4 模块详细化` 子节)→ 描述由"详见过程文档 module-details.md"改"必填,过程文档 module-details.md 必选"
2. **§5 算法与逻辑**(`### 5 算法与逻辑` 子节)→ 描述"建议填"改"必填,至少 1 个伪代码块"
3. **§6 数据结构完整变更**(`### 6 数据结构完整变更` 子节)→ 描述"建议填"改"必填,字段表至少 5 个字段"
4. **§7 接口细节**(`### 7 接口细节` 子节)→ 描述"建议填"改"必填,至少 1 个完整入参/出参 JSON schema"
5. **§8 异常处理**(`### 8 异常处理` 子节)→ 描述"建议填"改"必填"
6. **§9 安全要求**(`### 9 安全要求` 子节)→ 描述"建议填"改"必填"
7. **§10 状态机/流程**(`### 10 状态机/流程` 子节)→ 描述"建议填"改"必填"
8. **§11 性能与资源**(`### 11 性能与资源` 子节)→ 描述"建议填"改"必填"
9. **§12 测试要点**(`### 12 测试要点` 子节)→ 描述"建议填"改"必填"

**结构化语义锚点**:
- 锚点 1:`§4` 子标题(以"### 4 模块详细化"开头)
- 锚点 2:`§5` 子标题(以"### 5 算法与逻辑"开头)
- 锚点 3:`§6` 子标题(以"### 6 数据结构完整变更"开头)
- 锚点 4:`§7` 子标题(以"### 7 接口细节"开头)
- 锚点 5:`§8` 子标题(以"### 8 异常处理"开头)
- 锚点 6:`§9` 子标题(以"### 9 安全要求"开头)
- 锚点 7:`§10` 子标题(以"### 10 状态机/流程"开头)
- 锚点 8:`§11` 子标题(以"### 11 性能与资源"开头)
- 锚点 9:`§12` 子标题(以"### 12 测试要点"开头)

**边界与异常**:
- INV-3:既有 §1 / §2 / §3 / §13 / §14 / §15 字节级保留
- INV-5:`./assistants/rules/*.md` 0 改

**验证手段**(对应 AC-4):
- AC-4.1:模板 §4-§12 9 个章节描述由"建议填"改"必填"
- AC-4.6:过程文档 4 份从"可选"改"必选"

**回退方式**:`git revert <commit>`。

---

### TASK-REQ-00030-00005 · [修改] code-check/SKILL.md:评审清单追加 3 校验点(详设完整性 / 概设越界 / 行数比例)

**目标**:在 `code-check/SKILL.md` 评审清单**追加** 3 个校验点(不重写既有清单);既有的 5 个校验点**不**修改。

**涉及文件**:
- `plugins/code-skills/skills/code-check/SKILL.md §步骤 6 评审清单` **追加** 3 个校验点
- 既有 5 个校验点:**不**修改
- `plugins/code-skills/skills/code-check/SKILL.md frontmatter L1-3`:**不**修改(INV-1)
- `plugins/code-skills/skills/code-check/SKILL.md §## 不要做的事`:**不**修改(NFR-3)

**关键变更**(按结构化语义定位):

1. **§步骤 6 评审清单**(`### 步骤 6 评审清单` 子节)→ **追加** 3 个校验点
2. **校验点 1 详设完整性** → 校验 `plan/.../RESULT.md` 中每条任务的"涉及文件"在 §4-§10 找对应章节;不通过则派生"重构"类型任务(`validatePlanIntegrity`)
3. **校验点 2 概设越界检测** → 校验 `design/.../RESULT.md §7/§8/§9` 不出现"完整字段类型 / 完整错误码 / 完整约束 / 完整索引";不通过则派生"重构"类型任务(`validateDesignBoundary`)
4. **校验点 3 行数比例警告** → 校验 `design < plan × 1.2`;不通过则标黄警告(`validateLineRatio`,**不**阻断)

**结构化语义锚点**:
- 锚点 1:`§步骤 6 评审清单` 子标题(以"### 步骤 6"开头)
- 锚点 2:既有 5 个校验点列表(既有锚点,本需求**不**修改)
- 锚点 3:本需求**追加**的 3 个校验点(在既有 5 个校验点**之后**)

**边界与异常**:
- INV-1:frontmatter L1-3 字节级保留
- INV-3:既有"## 工作流程"步骤 0a / 0b.0 / 0-5 / 7 派生任务循环字节级保留
- INV-4:其他 11 个 `code-*` 技能 SKILL.md 0 改

**验证手段**(对应 AC-6):
- AC-6.1:code-check 评审新 REQ,若 plan/.../RESULT.md 某条任务"涉及文件"未在 §4-§10 找到对应章节,派生"详设缺失"任务
- AC-6.2:code-check 评审新 REQ,若 design/.../RESULT.md §7 / §8 / §9 出现"完整字段类型 / 完整错误码",派生"概设越界"任务
- AC-6.3:code-check 评审新 REQ,若 design 行数 > plan 行数 × 1.2,REVIEW-REPORT.md 标黄警告

**回退方式**:`git revert <commit>`。

---

### TASK-REQ-00030-00006 · [文档] 行数收敛观测:落地后 3 个新需求验证 NFR-1

**目标**:在落地后,从 REQ-00031 起,取 3 个新需求,验证其 `design/.../RESULT.md` 平均行数 ≤ 184 / `plan/.../RESULT.md` 平均行数 ≥ 340(对应 AC-8.1 / AC-8.2 / NFR-1)。

**涉及文件**:
- **无文件修改**(纯人工观测)
- 观测数据写入对应新 REQ 的 `plan/.../RESULT.md` "## 验证结果" 章节(由后续 `code-plan` / `code-check` 自动追加)

**关键变更**(观测清单):

1. **观测项 1:design 行数** → 取 3 个新 REQ 的 `design/.../RESULT.md` 行数平均值
2. **观测项 2:plan 行数** → 取 3 个新 REQ 的 `plan/.../RESULT.md` 行数平均值
3. **观测项 3:AC-8 验证** → 平均 design ≤ 264 × 0.7 = 184 / 平均 plan ≥ 284 × 1.2 = 340

**结构化语义锚点**:
- **不适用**(本任务**不**修改任何文件)

**边界与异常**:
- 新 REQ 数 < 3 → 观测顺延(待积累到 3 个再校验)
- 新 REQ 中含 `--extensible` → 行数预期会更高(因详设含"扩展性设计"小节),观测时**不**与 `--balanced` 混合统计

**验证手段**(对应 AC-8):
- AC-8.1:3 个新需求 design/.../RESULT.md 平均行数 ≤ 184
- AC-8.2:3 个新需求 plan/.../RESULT.md 平均行数 ≥ 340

**回退方式**:**不适用**(纯观测任务,无文件修改)。

## 任务依赖图

```
T-001 (无前置,可独立完成) ─┐
T-002 (无前置,可独立完成) ─┤
T-003 (无前置,可独立完成) ─┤
T-004 (无前置,可独立完成) ─┼─→ T-005 (无前置,可独立完成) ─→ T-006 (无前置,可独立完成)
                            │
                            └─→ (全部完成后验证 INV-1~INV-9)
```

**说明**:6 个任务**无强依赖**——T-001 ~ T-006 各自独立完成;T-006 在 T-001 ~ T-005 全部落地后观测(但**不**是 T-006 的"前置任务",而是 T-006 的"观测时机")。

## 里程碑

| 里程碑 | 包含任务 | 完成定义 | 计划时间 | 实际完成 |
| --- | --- | --- | --- | --- |
| M1-REQ-00030 | T-001 ~ T-005(5 任务) | 5 任务开发=已完成 ∧ 测试=不适用 | 2026-06-12 | — |
| M2-REQ-00030 | T-006(1 任务) | 3 个新 REQ 落地后,行数收敛 AC-8.1 / AC-8.2 验证 | 落地后 3 个新 REQ 完成时 | — |

## 状态管理规则

- **开发状态**:本批 6 任务从 `待开始` → `进行中` → `已完成`
- **测试状态**:本批 6 任务为纯文档/技能定义/观测类,保持 `不适用`
  - **不适用理由**(逐任务):
    - T-001 ~ T-005:纯 SKILL.md / templates 文档改造,无新代码 / 无新依赖 / 无新接口契约 → `不适用`
    - T-006:纯人工观测,无文件修改 → `不适用`

## 关联计划

- 上游:`./assistants/V0.0.3/require/REQ-00030/RESULT.md`(需求)
- 上游:`./assistants/V0.0.3/design/REQ-00030/RESULT.md`(概要设计)
- 上游:`./assistants/V0.0.3/plan/REQ-00030/RESULT.md`(本文件对应的详细设计)
- 横向:`./assistants/V0.0.2/plan/REQ-00020/PLAN.md`(code-design / code-plan 步骤 0b 7 维度,本需求沿用)
- 横向:`./assistants/V0.0.2/plan/REQ-00014/PLAN.md`(架构骨架作为首个任务,本需求沿用)

## 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-12 14:31 | v1 | 初始创建 | 详细设计与编码计划完成;6 任务(T-001 ~ T-006)对应 5 个被改文件 + 1 个观测任务;整体=--extensible + 功能性=高;架构骨架作为 T-001 子项(--extensible 触发,FR-6 条件 5 满足);4 算法伪代码;2 里程碑(M1:实施完成 / M2:行数收敛观测) | 用户 |
| 2026-06-12 14:45 | 任务完成 | TASK-REQ-00030-00001 开发状态"待开始"→"已完成";提交 e203023;code-design/SKILL.md 步骤 0b / 9A / 10A / 11A 已修订;INV-1 / INV-2 / INV-3 字节级保留校验通过 | TASK-REQ-00030-00001 |
