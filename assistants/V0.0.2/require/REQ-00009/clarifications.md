# 澄清记录 — REQ-00009

更新时间:2026-06-04 14:18

## 2026-06-04 14:18(本轮)

### Q-1:守卫判定信号
- **问题**:"项目可测性"如何判定?
- **选项**:
  - A. 只检查项目根构建/测试文件(推荐)— `package.json` / `pyproject.toml` / `Cargo.toml` / `go.mod` / `pom.xml` / `build.gradle` / `test/` 目录等
  - B. 只检查 `code/` 目录下是否有代码文件
  - C. 两者都检查(推荐)
- **用户回答**:A,**只检查项目根构建/测试文件**
- **影响**:RESULT.md §FR-1(守卫逻辑);§8 数据(检查项清单)
- **关键解读**:用户选最简 — `code-unit` 启动时仅看项目根是否有"可测"信号,不深入 `code/` 内部细节;若项目根有 `package.json` 等 → 可测,走原流程;否则跳过

### Q-2:跳过时的测试状态值
- **问题**:`code-unit` 跳过时,看板"测试状态"列填什么?
- **选项**:
  - A. 复用"不适用"(推荐)— 现有枚举,`code-dashboard` 0 改动
  - B. 新增"已完成(跳过)" — 触发 `dashboard-conventions §规则 1` 三方同步
  - C. 标记"已运行-通过"+ 备注 — 虚增"已通过"计数
- **用户回答**:A,**复用"不适用"**
- **影响**:RESULT.md §FR-2(状态写入);§NFR(与 dashboard 兼容)
- **关键解读**:与 V0.0.1 看板"任务清单"区段既有枚举完全兼容;`code-dashboard` 现有统计逻辑**0 改动**

### Q-3:跳过时是否写 `test/<任务编码>/RESULT.md`
- **问题**:跳过时是否在 `test/` 留痕?
- **选项**:
  - A. 不写(推荐)— 看板"不适用"已足够
  - B. 写"跳过原因" RESULT.md
  - C. 在 `code-it` deviations.md 追加一行
- **用户回答**:A,**不写 `test/<任务编码>/RESULT.md`**
- **影响**:RESULT.md §FR-3(留痕策略);§NFR(零新增产物)
- **关键解读**:最简实现 — `code-unit` 跳过时**不**产生任何新文件,仅在看板"任务清单"区段写"测试状态=不适用"

### Q-4(本轮未结构化提问,默认采纳):"强制调用"参数
- **建议默认**:**不**提供 `--force` 参数(本需求范围外)
- **状态**:采纳默认,不阻塞
- **回退路径**:v2 可加 `--force` 参数

### Q-5(本轮未结构化提问,默认采纳):与 `code-auto` 协同的"测试需要=Y"判断
- **建议默认**:`code-auto` FR-4.AC-4.2 的"若 `code-it` 输出含'测试需要=Y'则调 `code-unit`"逻辑**不**变;本需求落地后,该判断**仍**触发 `code-unit` 调用,但 `code-unit` 内部守卫可能不通过 → 跳过
- **状态**:采纳默认,不阻塞
- **影响**:`code-auto` 仍按"测试需要=Y"决定**是否调** `code-unit`;但**调** 后由 `code-unit` 自身决定**是否真做**
- **建议派生**:在 `code-review REQ-00009` 阶段,可派生"`code-auto` 升级:增加'任务级跳过单测'感知"任务(由用户决定)

### Q-6(本轮未结构化提问,默认采纳):与 REQ-00005 的协同
- **建议默认**:`code-unit` 不在 REQ-00005 改写范围;本需求是"独立"新增,与 REQ-00005 并行
- **状态**:采纳默认,不阻塞

### Q-7(本轮未结构化提问,默认采纳):`commit-conventions.md` 与 CLAUDE.md 追加
- **建议默认**:**不**主动追加(由 `code-rule` 沉淀,本需求不触发 `dashboard-conventions §规则 1`)
- **状态**:采纳默认,不阻塞
