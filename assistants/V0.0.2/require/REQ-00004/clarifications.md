# 澄清记录 — REQ-00004

更新时间:2026-06-04 12:50

## 2026-06-04 12:50(本轮)

### Q-1:版本总览的展示形态
- **问题**:不指定需求编号时,期望的"图表/进度表"主要形态?
- **选项**:
  - A. ASCII 进度表 + 文本柱状图(推荐)— 零依赖,终端友好
  - B. Mermaid 图表 — 需要渲染器
  - C. 两者都输出
- **用户回答**:A,ASCII 进度表 + 文本柱状图
- **影响**:RESULT.md §FR-2(版本总览)输出形态;§6 页面与界面(纯控制台);NFR-1 兼容性(无外部依赖)

### Q-2:"下一步执行建议"的输出形式
- **问题**:建议的输出形式?
- **选项**:
  - A. 可执行命令推荐(推荐)— `> 建议: 执行 /code-it REQ-00004-001`
  - B. 优先级文本提示 — 不绑定 command
  - C. 两者都输出
- **用户回答**:A,可执行命令推荐
- **影响**:RESULT.md §FR-3 / §FR-4(下一步建议);§7 交互逻辑(命令格式);§8 数据(每条建议的字段:命令 / 依据 / 优先级)

### Q-3:缺陷清单是否按严重度分层
- **问题**:是否按 P0/P1/P2/P3 分层展示?
- **选项**:
  - A. 是,按严重度单独分层(推荐)— 4 个子表
  - B. 只突出 P0/P1 高优先级 — 总计 + 高优醒目
  - C. 不分层,平面列表
- **用户回答**:B,只突出 P0/P1 高优先级
- **影响**:RESULT.md §FR-2.3(缺陷展示区);NFR-X 简化(无 P2/P3 子表);NFR-Y 醒目标记(P0 用 `█` 实心,P1 用 `▓` 半实心)

## 待澄清项(本轮暂不处理,留作 follow-up 或下轮)

### Q-4(留作 follow-up):"无激活版本"边界
- **背景**:用户原话未明示"如果 `./assistants/.current-version` 不存在怎么办"
- **建议默认**:在 `code-dashboard` 启动时检测 `.current-version`,若不存在 → 打印引导信息并退出(提示用户先调 `code-version`)
- **影响**:NFR-2 错误处理;§9 边界与异常
- **状态**:采用默认(进入下一步的 RESULT.md),不阻塞 `code-design` 阶段;若用户后续要变,再以 v2 增量更新

### Q-5(留作 follow-up):多需求(>=3)时是否分页/折叠
- **建议默认**:不折叠,全部显示(ASCII 终端用户可滚动);超过 20 个需求时加"显示前 10 + 概要 11+"提示
- **状态**:采用默认,不阻塞
