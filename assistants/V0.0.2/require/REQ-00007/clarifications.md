# 澄清记录 — REQ-00007

更新时间:2026-06-04 13:59

## 2026-06-04 13:59(本轮)

### Q-1:循环终止条件
- **问题**:`code-review` 循环的终止条件是?
- **选项**:
  - A. 仅按状态终止(无"必须改"即结束)— 无轮数上限
  - B. 状态 + 轮数上限(5 轮)— 平衡防环与收敛
  - C. 状态 + 轮数 + 时间(10 轮 / 1 小时)— 最全
- **用户回答**:A,**仅按状态终止**
- **影响**:RESULT.md §FR-5(循环终止);§9 边界(无轮数上限,但有"陷入循环"风险,需 NFR 提示)
- **关键解读**:用户选最简,**接受**"修改引入新问题"的潜在风险;这与"全自动"原意最一致

### Q-2:异常中断策略
- **问题**:子技能崩溃/git 冲突/网络失败等异常如何处理?
- **选项**:
  - A. 立即中断 + 报告(推荐)— 保留中间产物
  - B. 跳过失败 + 继续
  - C. 询问用户(不推荐,与全自动冲突)
- **用户回答**:A,立即中断 + 报告
- **影响**:RESULT.md §FR-7(异常处理);§9 边界(E-1~E-N)

### Q-3:用户中止方式
- **问题**:`code-auto` 跑长时,用户如何中止?
- **选项**:
  - A. 输出报告 + Ctrl+C 中止(推荐)
  - B. 锁定文件 + 报告
  - C. 启动前告知预期
- **用户回答**:A,输出报告 + Ctrl+C 中止
- **影响**:RESULT.md §FR-9(报告);NFR-7(中止约定)
- **关键解读**:最简实现,符合"全自动"原意

### Q-4:用户确认自动化
- **问题**:子技能遇到"需要用户确认"场景时如何"全自动"?
- **选项**:
  - A. 总选推荐项(推荐)— 选 AskUserQuestion 第一个选项
  - B. 启动时全问一遍(不推荐)
  - C. 总选推荐项 + 报告记录
- **用户回答**:A,总选推荐项
- **影响**:RESULT.md §FR-6(自动化策略);§8 数据(自动选择记录字段)
- **关键解读**:不增加"报告记录"开销,与"全自动"原意最一致

### Q-5:与 REQ-00005 的协同(批量模式)
- **问题**:子技能被频繁调用时,REQ-00005 的"首步拉取+末步提交"是否需要"批量模式"?
- **选项**:
  - A. 不引入批量模式(推荐)— 子技能按 REQ-00005 原始设计运行
  - B. 引入批量模式
  - C. 子技能可选关闭拉取与提交
- **用户回答**:A,不引入批量模式
- **影响**:RESULT.md §NFR-4(与 REQ-00005 协同 — 不引入批量模式);子技能边界**不变**

### Q-6:与 REQ-00006 的衔接
- **建议默认**:`code-auto` 完成时提示用户调 `code-publish`(`code-publish` 自身不调)
- **状态**:采纳默认,不阻塞

### Q-7:`code-auto` 落地的"循环驱动"机制
- **建议默认**:在 Claude Code 中,通过 `Skill` 工具**串行**调用子技能(每次调一个子技能,等待其完成,再调下一个);**不**并发
- **理由**:并发调用会导致"上下文冲突"(多个子技能同时写看板的同一区段)
- **状态**:采纳默认,不阻塞

### Q-8:本需求是否在 CLAUDE.md 追加
- **建议默认**:**不**追加(由 `code-rule` 沉淀,本需求不触发 `dashboard-conventions §规则 1`)
- **状态**:采纳默认,不阻塞

### Q-9(本轮未结构化提问,默认采纳):`code-auto` 是否在 `commit-conventions.md` 沉淀规则
- **建议默认**:**不**沉淀(留作 follow-up)
- **状态**:采纳默认,不阻塞

### Q-10(本轮未结构化提问,默认采纳):轮次计数器的存放位置
- **建议默认**:不显式存;依靠看板"任务清单"区段的"开发状态"自动判断(每轮 = `code-review` 一次)
- **状态**:采纳默认,不阻塞

### Q-11(新增):用户中止后"恢复"行为
- **建议默认**:**不**支持恢复(中止后,用户需重跑 `code-auto`,从头开始;但 `code-review` 的"必须改"任务仍在看板,可被重跑识别)
- **状态**:采纳默认,不阻塞

### Q-12(新增):`code-auto` 自身的"末尾提交"行为
- **建议默认**:`code-auto` 自身**不**自动 commit(沿用"编排者不提交"原则,各子技能各自提交)
- **状态**:采纳默认,不阻塞
- **影响**:RESULT.md §NFR-3(沿用 REQ-00006 的 NFR-3 边界)
