# 澄清记录 — REQ-00018

更新时间:2026-06-06 12:45
版本:V0.0.2
需求编码:REQ-00018

## 本轮澄清

### Q1:需求中"修改当前项目和模块相关描述文件"是指修改什么范围?
- **时间**:2026-06-06 12:45
- **问题**:需求范围是"通用技能增强"还是"只改 `code-skills` 自身"
- **选项**:
  - A. **(推荐)** 通用技能增强 + 扫描 CWD 项目类型 — 本需求是优化 `code-version` 技能本身,不动 `code-skills` 自身;技能逻辑:代码后扫描当前 cwd 下的项目类型,选取对应描述文件(如 Maven 项目调 `pom.xml`,Node 项目调 `package.json`),修改版本号(推荐默认值=新建的 `<版本号>`)。CWD 可由用户在调 `code-version` 时指定(可选参数)或由技能自动检测
  - B. 只改 `code-skills` 仓库自身产物(类似 REQ-00015 `code-merge` 加入 marketplace 时的 "+1 项" 模式)
  - C. 两者兼顾
- **用户回答**:A(推荐)通用技能增强 + 扫描 CWD 项目类型
- **影响**:RESULT.md §1 / §2 / §3 / §4(FR-1/FR-2/FR-3 围绕"通用技能增强 + 扫描"展开);不修改 `code-skills` 自身的 `marketplace.json` / `plugin.json` / README

### Q2:用户原文 "mainfest.json" 是笔误(主流拼写是 manifest.json)吗?
- **时间**:2026-06-06 12:45
- **问题**:术语命名
- **选项**:
  - A. **(推荐)** `manifest.json`(社区主流拼写)— Web App / VSCode Extension / npm package 等
  - B. `META-INF/MANIFEST.MF`(Java 清单)
  - C. `mainfest.json`(保留原拼写)
- **用户回答**:A(推荐)manifest.json(社区主流拼写)
- **影响**:RESULT.md §1 列举的描述文件清单用 `manifest.json`;不保留 `mainfest` 别名

## 待澄清项(本轮未处理 / 留作默认)

### Q-3:`code-version` 切换版本时,是否同时修改"项目"和"模块"两类描述文件?
- **状态**:采纳默认(同时改,按需)
- **依据**:用户原文"项目"和"模块相关描述文件"并列提及 → 默认两者都支持
- **回退路径**:v2 由 `code-rule` 评估是否拆分为"项目级"和"模块级"两组配置

### Q-4:CWD 项目类型扫描,是否包含单仓库多项目(monorepo)?
- **状态**:采纳默认(不特殊处理 monorepo,沿用 `Glob` 找到的所有匹配描述文件)
- **回退路径**:v2 由 `code-rule` 评估是否需要 `--monorepo` 参数或自动检测

### Q-5:修改描述文件时,版本号取自哪里?
- **状态**:采纳默认(默认 = 新建的 `<版本号>`,与 `.current-version` 一致)
- **依据**:用户原文"各版本号的声明同步"→ 默认值 = 当前激活版本
- **回退路径**:v2 由 `code-version` 接受 `--version-string` 参数覆盖

### Q-6:若 CWD 找不到任何描述文件(`pom.xml` / `package.json` / `manifest.json` / 等),如何处理?
- **状态**:采纳默认(屏幕打印 `⚠ CWD 下未检测到任何已知工程类型描述文件,跳过同步`,不报错不阻断)
- **回退路径**:v2 由 `code-rule` 评估是否需要 `--strict` 参数或对未知工程类型主动询问用户

## 边界与异常(本轮锁定)

| ID | 场景 | 处理 |
| --- | --- | --- |
| E-1 | CWD 下有多个描述文件(单仓多项目 / monorepo) | 修改**所有**匹配的描述文件,屏幕打印每个文件的修改日志 |
| E-2 | CWD 下找不到任何描述文件 | 屏幕打印 `⚠ CWD 下未检测到任何已知工程类型描述文件,跳过同步`,不报错 |
| E-3 | 描述文件存在但格式不可解析(非 JSON / 非标准 XML) | 屏幕打印 `⚠ <文件> 格式不可解析,跳过`,继续处理其他文件 |
| E-4 | 描述文件存在但**无**"版本号"字段(罕见,可能 `package.json` 缺 `"version"`) | 屏幕打印 `⚠ <文件> 未找到版本号字段,跳过`,继续处理其他文件 |
| E-5 | 用户通过 `code-version` 切换但 `--skip-cwd-sync` 参数指定 | 跳过 CWD 同步,只切版本(沿用 `--skip-*` 命名约定) |
