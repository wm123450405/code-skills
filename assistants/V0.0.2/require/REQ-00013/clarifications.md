# 澄清记录 — REQ-00013

更新时间:2026-06-04 15:25

## 2026-06-04 15:25(本轮)

### Q-1:"标题"字段从哪里来
- **问题**:"标题"字段从哪里来?
- **选项**:
  - A. 从已有内容派生(推荐)— `require/.../RESULT.md` 第 1 行 + `plan/.../PLAN.md` 任务总览"标题"列已是标题
  - B. 新增"## 标题"小节
  - C. 看板新增"标题"列
- **用户回答**:A,**从已有内容派生**
- **影响**:RESULT.md §FR-1(标题来源);§8 数据(标题字段位置);§9 边界
- **关键解读**:**零规范变更** — 不修改 `RESULT.md` 模板 / 不修改 `PLAN.md` 模板 / 不修改看板;只在"显示"环节启用

### Q-2:显示格式
- **问题**:"编号+标题"的拼接格式是?
- **选项**:
  - A. `REQ-00001 · 标题`(推荐)— 中点 `·` 分隔
  - B. `REQ-00001 标题` — 空格分隔
  - C. `REQ-00001 (标题)` — 括号包裹
- **用户回答**:A,**`REQ-00001 · 标题`**
- **影响**:RESULT.md §FR-2(显示格式);§6 页面(显示模板)
- **关键解读**:` · ` 中点 `·`(U+00B7)在中文/英文下都清晰,**统一**为这一格式
  - 需求:`REQ-NNNNN · <需求标题>`
  - 任务:`TASK-REQ-NNNNN-NNNNN · <任务标题>`
  - 缺陷:`BUG-NNNNN · <缺陷标题>`

### Q-3:"30 字以内"计量
- **问题**:"30 字以内"怎么计量?
- **选项**:
  - A. 字符数 ≤ 30(推荐)— 所有字符(汉字 / 字母 / 数字 / 标点)合集
  - B. 汉字数 ≤ 30
  - C. 总长 ≤ 30(包括"REQ-NNNNN · ")
- **用户回答**:A,**字符数 ≤ 30**
- **影响**:RESULT.md §FR-1(标题长度限制);§8 数据
- **关键解读**:用户原话"30 字以内"按现代 UI 习惯解读为"30 个字符"

### Q-4:升级范围
- **问题**:本轮升级哪些技能启用"编号 · 标题"显示?
- **选项**:
  - A. 本轮升级 6 个技能显示格式(推荐)— `code-require` / `code-plan` / `code-fix` + `code-it` / `code-unit` / `code-review` / `code-auto`
  - B. 只升级 3 个生成源
  - C. 仅在 `code-dashboard` 启用
- **用户回答**:A,**本轮升级 6 个技能**
- **影响**:RESULT.md §FR-2(升级范围);§FR-3~§FR-8(每个技能的具体升级)
- **关键解读**:
  - 3 个**生成源**:`code-require` / `code-plan` / `code-fix` — 确保产物的"标题"字段存在
  - 3 个**消费方**:`code-it` / `code-unit` / `code-review` + `code-auto` — 在用户交互/报告中显示"编号+标题"
  - **不**在 `code-dashboard` 升级(看板的"标题"列已存在)

### Q-5(本轮未结构化提问,默认采纳):历史回填
- **建议默认**:现有 12 个 V0.0.0~V0.0.2 需求(REQ-00001~00012)+ 19 个 V0.0.1 任务**均已含"标题"** — 本需求**不**需手动回填,显示环节自动启用
- **状态**:采纳默认,不阻塞

### Q-6(本轮未结构化提问,默认采纳):与 `code-review` 派生任务的"标题"
- **建议默认**:派生任务由 `code-review` 写入 `PLAN.md` 任务总览,**本轮** `code-review` 升级"派生任务标题"格式(确保标题 < 30 字)
- **状态**:采纳默认,不阻塞

### Q-7(本轮未结构化提问,默认采纳):与 `code-publish` / `code-dashboard` 协同
- **建议默认**:
  - `code-publish` 前置检查报告中"未完成项"显示"REQ-NNNNN · 标题"(FR-2 已覆盖)
  - `code-dashboard` 现有"标题"列已存在,**不**触发
- **状态**:采纳默认,不阻塞

### Q-8(本轮未结构化提问,默认采纳):与 `code-fix` 的关系
- **建议默认**:`code-fix` 是 V0.0.0 起的 10 个技能之一(SKILL.md 17,878 bytes);**本轮**首次升级 `code-fix`,让 `code-fix` 产出 `fix/.../RESULT.md` 含"## 标题"小节
- **状态**:采纳默认,不阻塞

### Q-9(本轮未结构化提问,默认采纳):与 REQ-00005 协同
- **建议默认**:`code-require` / `code-plan` 不在 REQ-00005 改写范围(REQ-00005 改的是"首步拉取+末步提交"3 技能),本需求是"独立"新增
- **状态**:采纳默认,不阻塞

### Q-10(本轮未结构化提问,默认采纳):`commit-conventions.md` 与 CLAUDE.md 追加
- **建议默认**:**不**主动追加(由 `code-rule` 沉淀,本需求不触发 `dashboard-conventions §规则 1`)
- **状态**:采纳默认,不阻塞
- **建议派生**:在 `code-review REQ-00013` 阶段,可派生"用 `code-rule` 沉淀 '标题字段约定'" 任务
