# 澄清记录 — REQ-00006

更新时间:2026-06-04 13:45

## 2026-06-04 13:45(本轮)

### Q-1:发布前置检查的严格度
- **问题**:"解决状态"应达到多严?
- **选项**:
  - A. 全检查(最严,推荐)— 需求=已完成 ∧ 任务开发=已完成 ∧ 测试∈{已运行-通过, 不适用} ∧ 缺陷=已修复
  - B. 只检任务开发状态(最宽松)
  - C. 只检需求与缺陷
- **用户回答**:A,全检查(最严)
- **影响**:RESULT.md §FR-1(前置检查);§8 数据(检查规则表);§9 边界(检查通过/不通过状态)

### Q-2:`assistants/qanda/` 目录不存在的处理
- **问题**:`qanda/` 不存在,如何处理?
- **选项**:
  - A. 本需求顺带创建 `qanda/` 目录与 README.md(推荐)
  - B. 不创建,仅在 Q&A 手册中提示
  - C. Q&A 全部入 `publish/`,不提 `qanda`
- **用户回答**:A,本需求顺带创建 `qanda/` 目录与 README.md;**后续可由其他新技能管理 Q&A 数据,暂时考虑人工整理进去**
- **影响**:RESULT.md §FR-4(创建 `qanda/` 骨架 + README);§8 数据(`qanda/` 目录结构)
- **关键解读**:用户强调"人工整理进去" — 即本需求**不**实现 Q&A 内容的自动生成/管理;只创建结构骨架

### Q-3:基线版本(V0.0.0 / V0.0.1)时 UPDATE.md 的处理
- **问题**:首个发布(无"上一版本")时 UPDATE.md?
- **选项**:
  - A. 不生成 UPDATE.md(推荐)
  - B. 生成空占位
  - C. 生成多路径升级手册
- **用户回答**:A,不生成 UPDATE.md
- **影响**:RESULT.md §FR-2.AC-2.3(本版本是基线时跳过 UPDATE.md);§9 边界(E-3)

### Q-4:`code-publish` 的发布对象
- **问题**:本仓库是不是被发布的"软件"?
- **选项**:
  - A. 本仓库本身就是被发布对象
  - B. 本仓库是发布脚手架
  - C. 通用发布手册骨架(推荐)— 用户自行补全
- **用户回答**:C,通用发布手册骨架;**用户开发软件各异、发布环境不同,由用户在自己项目里拉起后补全软件运行环境信息,或一些平台的操作手册**;**最灵活但最需用户动手完善,否则按最常见的部署方式生成内容**
- **影响**:RESULT.md §FR-2(DEPLOY.md 内容来源 — "默认内容 = 最常见部署方式,用户补全"原则);§6 页面(DEPLOY.md 模板);§8 数据
- **关键解读**:`code-publish` 是**通用脚手架**,不强绑到 `code-skills` 自身发布;手册中所有"具体命令/路径/服务名"应**留作 placeholder + 默认示例**,由用户在调用时填入

### Q-5(本轮未结构化提问,默认采纳):里程碑字段是否纳入检查
- **建议默认**:不纳入(看板"里程碑"是规划字段,不是"现状"字段;前置检查只看"现状 = 解决"即可)
- **状态**:采纳默认,不阻塞
- **影响**:RESULT.md §FR-1(只检查 3 大区段,不检查里程碑)

### Q-6(本轮未结构化提问,默认采纳):3 份手册的"内容来源"是否自动从代码/规范抽取
- **建议默认**:**不**自动抽取;DEPLOY.md / UPDATE.md 提供"最常见部署方式"的默认内容 + 大量 placeholder(留给用户);Q&A 手册从 `qanda/` 抽取(Q-2 锁定 A 创建骨架)
- **状态**:采纳默认,不阻塞

### Q-7(本轮未结构化提问,默认采纳):`code-publish` 是否参与 REQ-00005 的"首步拉取+末步提交"
- **建议默认**:**不**参与(本需求是独立新增;`code-publish` 不在 REQ-00005 改写范围)
- **状态**:采纳默认,不阻塞
- **影响**:RESULT.md §NFR(不依赖 REQ-00005;但用户应在最新上下文中调用)

### Q-8(本轮未结构化提问,默认采纳):本需求是否在 `plugins/code-skills/CLAUDE.md` "AI 工作约定"小节追加
- **建议默认**:**不**追加(由 `code-rule` 沉淀,本需求不触发 `dashboard-conventions §规则 1`)
- **状态**:采纳默认,不阻塞

### Q-9(新增):`code-publish` 自身的"末尾提交"行为
- **建议默认**:`code-publish` 自身**不**自动 commit(沿用 V0.0.0~V0.0.1 实践,涉及 publish/ 与 qanda/ 的 commit 由用户手动完成;**不**期望 commit 中混入手册内容)
- **理由**:`code-publish` 是"读 + 写 publish/ + 写 qanda/";"写"产生新文件,但**写的内容需要用户审阅**(Q-4 锁定 C:"最灵活但最需用户动手完善")— 立刻 commit 会"封死"用户补全空间
- **状态**:采纳默认,不阻塞
- **影响**:RESULT.md §NFR(`code-publish` 不 commit);§9 边界(E-8 "用户审阅后再手动 commit")
