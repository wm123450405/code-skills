# 关联需求 — REQ-00014

更新时间:2026-06-05 12:10
版本:V0.0.2
需求:REQ-00014 优化技能 `/code-plan` 的任务拆分维度

## 扫描范围

- **同版本**:`./assistants/V0.0.2/require/*/RESULT.md` — **已扫描**(10 个需求)
- **跨版本**:无直接关联(本需求是元需求,优化工作流本身)
- **元层级**:本需求修改 `plugins/code-skills/skills/code-plan/SKILL.md`(不修改 `assistants/` 任何文件)

## 关联需求清单

### REQ-00004(V0.0.2)— 添加 `/code-dashboard` 开发看板技能
- **关联点**:`code-plan` SKILL.md 是 `code-dashboard` 的上游(`code-dashboard` 看板中"任务清单"区段的字段定义沿用 `code-plan` 的"任务编号"格式)
- **影响**:**无影响**(本优化不修改任务编号格式,只改拆分维度)
- **本优化对其影响**:0
- **来源**:扫描 `./assistants/V0.0.2/require/REQ-00004/RESULT.md`

### REQ-00005(V0.0.2)— 优化 3 技能,加首步拉取+末步提交
- **关联点**:本需求**不**触达 `code-require` / `code-design` / `code-plan` 的工作流步骤
- **影响**:**无影响**
- **本优化对其影响**:0

### REQ-00006(V0.0.2)— `/code-publish` 发布部署技能
- **关联点**:`code-publish` 的"前置检查"读取 `code-plan` 输出的 PLAN.md 中的任务状态
- **影响**:**无影响**(本优化不修改任务状态语义)
- **本优化对其影响**:0

### REQ-00007(V0.0.2)— `/code-auto` 自动开发技能
- **关联点**:`code-auto` 调用 6 个子技能,包括 `code-plan`;`code-auto` 任务循环依赖 PLAN.md 的"任务总览"区段
- **影响**:**无影响**(本优化不修改 PLAN.md 的字段或语义)
- **本优化对其影响**:0
- **特别观察**:REQ-00007 自己的 PLAN.md 偶然"按功能点拆分"(5 任务 = 写 SKILL.md / 改 JSON / 改 README / 改看板 / 自检) — 是本优化"目标效果"的范例(详 `analysis-notes.md`)

### REQ-00008 ~ REQ-00013(V0.0.2)— 各类优化与新建
- **关联点**:`code-review` 派生任务
- **影响**:**无影响**(本优化不修改 `code-review` 行为)
- **本优化对其影响**:0

## 既有 7 个已拆分 PLAN 的处理

| 需求 | 现有 PLAN 任务数 | 是否追溯重拆 |
| --- | --- | --- |
| REQ-00004 | 3 | ❌ 不追溯(Q-A3 锁定 A) |
| REQ-00005 | 5 | ❌ 不追溯 |
| REQ-00006 | 9 + 1 派生 = 10 | ❌ 不追溯 |
| REQ-00007 | 5 | ❌ 不追溯 |
| REQ-00008 | (待 code-plan) | N/A |
| REQ-00009 | (待 code-plan) | N/A |
| REQ-00010 | (待 code-plan) | N/A |
| REQ-00011 | (待 code-plan) | N/A |
| REQ-00012 | (待 code-plan) | N/A |
| REQ-00013 | (待 code-plan) | N/A |

**理由**:
1. 重拆 7 个既有 PLAN 会破坏历史状态(开发状态/测试状态/提交哈希) — 不可逆
2. 既有 PLAN 已实施完成,无追溯必要
3. 实施成本高(每个 PLAN 需重写任务详情 + 状态推进 + 看板同步)
4. 仅未来需求应用新规则 — 最小变更 + 最大收益
5. 由 `code-review` 评审的"状态字段不可逆"原则保证

## 跨需求聚合(本需求特有的)

| 维度 | 涉及需求 | 共性 | 处理建议 |
| --- | --- | --- | --- |
| 元需求(优化 code-plan 拆分规则) | 无 | N/A | 独立处理,不影响其他需求 |
| 未来需求应用新规则 | REQ-00008+ | 未来所有需求 | 强制应用新规则 |
| 现有 PLANS 不追溯 | REQ-00004/05/06/07 | 已拆分完成 | 不追溯 |
| `code-plan` SKILL.md 修改范围 | 元需求 | 仅 §10A | 不改其他 18 章节 |

## 总结

- **0 强关联** = 本需求对所有其他需求的影响均为 0
- **元需求特性** = 优化工作流本身,**不**产生新功能/新接口/新数据
- **作用范围** = `plugins/code-skills/skills/code-plan/SKILL.md` §10A 单章节修改
- **生效范围** = V0.0.2 未来需求(REQ-00008+)
- **追溯范围** = 0(Q-A3 锁定 A)
