# 分析笔记 — REQ-00019

## 思路演进

1. **初判(用户原文 1 句话)**:优化 `/code-plan`,BUG 模式产出物与 REQ 模式一致;区别只有参考来源
2. **歧义识别**:用户说"产出物一致",但 `code-it` BUG 路径已硬编码读 `fix-plan.md`,需澄清
3. **第 1 轮澄清(3 选 1)**:
   - A 完全同构 + 路径全替(改 `code-it`)
   - B 同构 + 双文件并存
   - C 沿用 `fix-plan.md` + 补过程文档
   - 用户选 A
4. **第 2-3 轮澄清(任务编号 + 看板位置)**:
   - 任务编号:用户选新格式 `TASK-BUG-NNNNN-NNNNN`
   - 看板位置:用户选原"任务清单"区段收录(0 触发 §规则 1)
5. **第 4-5 轮澄清(`code-it` 改造深度 + 历史 BUG 处理)**:
   - `code-it` 改造:用户选彻底同步升级(4 项联动全选)
   - 历史 BUG-00001:用户选不迁移
6. **第 6-7 轮澄清(`code-it` 锚点 + 任务呈现)**:
   - `code-it` 锚点:用户选全部 4 项(步骤 17/24/25 + 过程文档去 `fix-` 前缀 + frontmatter 同步)
   - 任务呈现:用户选原"任务清单"区段

## 关键权衡

| 权衡点 | 选 A 路径(本需求选定) | 选 C 路径(用户已否决) |
| --- | --- | --- |
| 实施量 | 2 个 SKILL.md 联动改(`code-plan` / `code-it`)+ 11 项 INV 自检 | 1 个 SKILL.md 增量改(`code-plan`)+ 简化 INV |
| 既有 BUG-00001 兼容 | 需在 `code-plan` 步骤 23 + `code-it` 步骤 17 步骤 22 步骤 24 增加 E-1 / E-7 / E-9 / E-11 历史检测(4 处) | 无需历史检测(沿用 `fix-plan.md` 输入) |
| 看板复杂度 | 1 处"任务清单"区段(同表) | 1 处"缺陷清单"区段(既有) |
| 后续 `code-review` 评审 BUG 任务 | 沿用既有 `PLAN.md` 任务总览解析 | 需新解析 `fix-plan.md` 内"步骤"列表 |

## 候选方案细节(否决理由记录)

### 候选 A1(环境变量) — 由 BUG-00001 否决,本需求不涉及
### 候选 A2(argv 检查)— 由 BUG-00001 否决,本需求不涉及
### 候选 B(双文件并存)— 本需求第 1 轮澄清用户否决
- 否决理由:增加认知负担 + 增加 `code-it` 步骤 17 复杂度(需双输入兼容)
### 候选 C(沿用 `fix-plan.md` + 补过程文档)— 本需求第 1 轮澄清用户否决
- 否决理由:沿用 `fix-plan.md` 仍是"特殊路径",与"形式同构"目标不符
### 候选 B-1(任务编号两种并存)— 本需求第 2 轮澄清用户否决
- 否决理由:增加解析复杂度;LLM 拆分时难以自动判定粒度
### 候选 C-1(两区段并存)— 本需求第 3 轮澄清用户否决
- 否决理由:增加看板同步复杂度;有重复风险
### 候选 B-2(双文件过渡期)— 本需求第 4 轮澄清用户否决
- 否决理由:增加 `code-it` 步骤 17 复杂度(优先读 PLAN.md,兼容读 fix-plan.md);forward compat 收益不抵复杂度
### 候选 C-2(改造读取,保留 fix- 前缀)— 本需求第 4 轮澄清用户否决
- 否决理由:`TASK-BUG-` 编号与 `fix-` 前缀过程文档混用,命名不一致
### 候选 B-3(迁移 BUG-00001)— 本需求第 5 轮澄清用户否决
- 否决理由:BUG-00001 已修复-待验证,迁移可能引入新偏离;用户偏好最小变更
### 候选 C-3(提供迁移工具)— 本需求第 5 轮澄清用户否决
- 否决理由:增加实施量;用户偏好"不主动迁移"原则

## 实施期关键决策(待后续阶段锁定)

| 决策编号 | 内容 | 现状 |
| --- | --- | --- |
| **D-1** | `code-plan` SKILL.md 步骤 19-28A 重构后行数 | 估计 +50 ~ +100 行(基线 945 行,允许 +20% 上限 = 1134 行) |
| **D-2** | `code-it` SKILL.md BUG 路径步骤 17-25 改造后行数 | 估计 0 净增(替换既有锚点文字,不改步骤数) |
| **D-3** | `templates/fix-plan.md` 是否删除 | 不删除,留作历史;`code-plan` 步骤 26A 改为引用 `templates/plan.md` + `templates/task-plan.md` |
| **D-4** | BUG 任务编号起始 | `TASK-BUG-NNNNN-00001`(5 位,与 REQ 任务一致) |
| **D-5** | BUG 任务"需求"列填法 | `BUG-NNN`(3 位,与 REQ 任务的 5 位不同;用户接受) |
| **D-6** | 看板"任务清单"区段 BUG 任务行"关联任务"列 | 填本缺陷 `BUG-NNN`(自查) |
| **D-7** | `code-fix` SKILL.md 是否修改 | **不修改**(FR-8 强约束;0 改动) |
| **D-8** | 实施时机 | 本需求走标准流程:`code-design` → `code-plan` → `code-it`(2 任务:`code-plan/SKILL.md` 改造 + `code-it/SKILL.md` 改造)→ `code-review` |
