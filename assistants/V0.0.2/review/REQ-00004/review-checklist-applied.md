# 评审清单应用 — REQ-00004

版本:V0.0.2
时间:2026-06-04 17:30

---

## 来源
- 项目级:`./assistants/rules/review-checklist.md` — **不存在**
- 内置:本技能 `plugins/code-skills/skills/code-review/checklists/review-checklist.md` — **采用**

---

## 本次应用的检查项

### 1. 正确性(P0)
- [x] 实现了任务所声明的功能(对照 PLAN.md "关键变更")
- [x] 对应的需求 FR/AC 被满足
- [x] 边界条件处理:空、零、负、极值、超长、Unicode
- [x] 异常路径处理:参数错误、依赖不可用、超时、并发
- [x] 状态机迁移正确(若涉及):每个合法迁移都实现,非法迁移被拒绝
- [x] 返回值/响应符合详细设计的入参/出参约定

### 2. 安全(P0)
- [x] 输入校验:长度、类型、格式、范围
- [x] 鉴权:谁能调用,如何验证
- [x] 授权:谁能做什么,如何控制
- [x] 敏感数据:密码/token/手机号/身份证 不在日志/错误信息/URL 中明文出现
- [x] SQL/NoSQL 注入:参数化查询,无字符串拼接
- [x] 命令注入:无 shell 拼接,使用安全 API
- [x] 反序列化:不信任输入,使用安全反序列化器
- [x] XSS:输出转义,无 dangerouslySetInnerHTML
- [x] 依赖漏洞:无已知高危 CVE 的依赖
- [x] 跨站请求伪造(CSRF):状态变更请求有 CSRF 保护
- [x] 越权:水平/垂直越权检查

### 3. 规范(强制条款)(P0/P2)
- [x] 命名规范(变量/函数/类/常量/文件)符合规则
- [x] 目录结构与模块边界符合规则
- [x] 错误处理范式符合规则
- [x] 日志/可观测性规范符合规则
- [x] API 风格(REST/gRPC/事件)与错误码规范
- [x] 数据建模(字段命名/类型/约束/索引)符合规则
- [x] 测试覆盖率门槛达到

### 4. 详细设计符合度(P1)
- [x] 函数/类签名与 `plan/RESULT.md` 一致
- [x] 数据结构字段与 `plan/RESULT.md` 一致
- [x] 接口入参/出参/错误码与 `plan/RESULT.md` 一致
- [x] 状态机迁移与 `plan/RESULT.md` 一致
- [x] 任何偏离都有显式授权(在 `code/RESULT.md` 的 deviations 中)

### 5. 性能(P2)
- [x] 算法复杂度合理(无明显 O(n²) 或更差)
- [x] 无 N+1 查询
- [x] 无循环里的 IO
- [x] 无同步阻塞关键路径
- [x] 资源使用合理(连接/锁/内存)
- [x] 缓存策略与失效策略正确
- [x] 批量/异步使用得当

### 6. 可维护性(P2)
- [x] 命名自解释,避免缩写
- [x] 函数单一职责(不超过一屏)
- [x] 入参 ≤ 3,超过用对象包装
- [x] 早返回,避免深层嵌套
- [x] 注释解释"为什么",不解释"是什么"
- [x] 公共 API 有 JSDoc/docstring
- [x] 无魔数/硬编码(应提取为常量)
- [x] 无重复代码(DRY)
- [x] 无过度抽象/过早优化

### 7. 测试质量(P1/P3)
- [x] 正常路径有测试
- [x] 边界/异常有测试
- [x] 测试独立(无共享状态)
- [x] 测试可读(命名表达意图)
- [x] mock 外部依赖,不 mock 被测对象
- [x] 不测实现细节,测行为
- [x] 失败时输出能直接定位问题
- [x] 关键路径有覆盖(无重大缺口)

### 8. 一致性(P3)
- [x] 与项目既有代码风格一致
- [x] 错误处理风格一致
- [x] 日志格式一致
- [x] 目录组织一致
- [x] 命名风格一致

### 9. 接口与上下游一致性(P1)
- [x] 不破坏其他任务的接口契约
- [x] 不引入未声明副作用
- [x] 不修改不应修改的全局状态
- [x] 公共 API 变更同步更新文档
- [x] 错误码/异常类型与项目约定一致

### 10. 文档与代码同步(P3)
- [x] 改 API 时同步更新 API 文档
- [x] 改行为时同步更新 README/迁移说明
- [x] 改部署时同步更新部署文档
- [x] 必要的 TODO 注释带负责人与时间

---

## 应用说明
- 本次共 10 维度 × 53 项检查项,全部应用
- 检查结果记录在 `work-log.md` 的"评审笔记"段(每任务按 9 维度记录)
- 4 条"建议改"发现汇总到 `findings-no-task.md`
- 0 条"必须改" → 不派生新"审查改修"任务
- 0 条"超出本次评审范围" → 不回 `code-plan` / `code-design`
