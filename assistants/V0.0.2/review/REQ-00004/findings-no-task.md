# 未派生任务的发现 — REQ-00004

版本:V0.0.2
时间:2026-06-04 17:40

> 本评审共发现 **4 条"建议改"** + **0 条"必须改"** + **0 条"可选"**。
> 经用户授权("全部留作 follow-up"),**全部 4 条"建议改"留作 follow-up**,不派生新"审查改修"任务。
> 详情见 `work-log.md` + `REVIEW-REPORT.md`。

---

## 类别:建议改(共 4 条)

### T-001:F1 — SKILL.md 步骤 5"全版本已完成"触发条件不完整
- **位置**:`plugins/code-skills/skills/code-dashboard/SKILL.md` 步骤 5 "特殊"段
- **类别**:正确性(P0) / 可维护性(P2)交叉
- **维度**:§1 正确性
- **描述**:SKILL.md 步骤 5 中"全版本已完成 → 建议 /code-version V0.0.x" 触发条件只列了"所有需求=已完成 ∧ 所有任务=已完成",**未提及无 P0/P1 待修复缺陷**。但 §9 边界 E-8 显式说明需"所有任务+需求已完成 + 无 P0/P1 待修复缺陷"3 个条件同时满足
- **影响**:用户看步骤 5 时可能误以为"需求任务全完成就升级版本"——若此时有 P0 缺陷未修,会漏检(虽然 §9 E-8 提了,但分布在不同章节)
- **严重程度**:建议改(非阻塞)
- **建议改修方案**:
  - 在 SKILL.md 步骤 5 "特殊"段中,补一句:"含 无 P0/P1 待修复缺陷"
  - 或在 §9 边界 E-8 中加引用:"参见步骤 5 特殊段"
- **决定**:留作 follow-up(用户授权)

### T-001/T-002:F2 — SKILL.md 与 CLAUDE.md 解析锚点正则字面略不一致
- **位置**:
  - `plugins/code-skills/skills/code-dashboard/SKILL.md` 步骤 3 / 算法 1:`^## (.+)$`
  - `plugins/code-skills/CLAUDE.md` 指引 N:解析锚点行 `按 ^## .*$ 定位 + 表格行 ^\| .* \|$ 匹配`
- **类别**:一致性(P3) / 规范(强制)(P0/P2)交叉
- **维度**:§3 规范 + §8 一致性
- **描述**:两者正则字面略不同(`(.+)` vs `.*`),实际效果等价(因为 `.+` 要求非空而 `.*` 允许空)
- **影响**:低(行为等价,但字面不一致会让 AI 协作者困惑"哪个是权威源")
- **严重程度**:建议改(非阻塞)
- **建议改修方案**:
  - 选项 A:CLAUDE.md 指引 N 改为"按 `^## (.+)$` 定位"以与 SKILL.md 严格一致
  - 选项 B:反向(SKILL.md 改为 `^## .*$`)
  - 选项 C:在两文件都加注释"以 SKILL.md 步骤 3 / 算法 1 为权威源"
- **决定**:留作 follow-up(用户授权)

### T-002:F3 — CLAUDE.md 指引 N 编号占位说明不显式
- **位置**:`plugins/code-skills/CLAUDE.md` 末段 `### 指引 N: \`code-dashboard\` 行为约定`
- **类别**:一致性(P3) / 可维护性(P2)交叉
- **维度**:§8 一致性 + §6 可维护性
- **描述**:`### 指引 N` 中的"N"是占位,不是确定编号 2 或 11;但 CLAUDE.md 引用语"本小节由 `code-rule` 技能维护;手动修改可能被 `code-rule` 覆盖"中**未明示**"指引编号由 `code-rule` 决定;手动追加时使用 `指引 N` 占位即可"
- **影响**:低(用户后续 `code-rule` 沉淀新指引时,需要手动调整 `N` 为实际编号)
- **严重程度**:建议改(非阻塞)
- **建议改修方案**:
  - 在 CLAUDE.md 引用语后追加一句:"指引编号由 `code-rule` 决定;手动追加时使用 `指引 N` 占位即可"
  - 或在 `### 指引 N: ...` 标题后加注释(`<!-- 占位:N 由 code-rule 填充 -->`)
- **决定**:留作 follow-up(用户授权)

### T-003:F4 — README 技能清单"下游"列语义与既有 10 行略不同
- **位置**:
  - `plugins/code-skills/README.md` 行 38(技能概览末行)`code-dashboard` 行的"下游"列
  - `plugins/code-skills/README.en.md` 行 38 同
- **类别**:详细设计符合度(P1) / 一致性(P3)交叉
- **维度**:§4 详细设计符合度 + §8 一致性
- **描述**:既有 10 行的"下游"列描述"本技能**被谁**调用"(如"code-review"行的"code-it(改修任务)");本任务的"下游"列内容"(引导用户调 `code-require` / `code-design` / ...)"描述"本技能**引导用户去**调用谁"——性质不同
- **影响**:低(可读性;用户可能困惑"下游"是"被谁调"还是"引导谁")
- **严重程度**:建议改(非阻塞)
- **建议改修方案**:
  - 选项 A:将本行"下游"列内容改为"(在长会话中引导用户决策)"或"无(本技能为消费方)"
  - 选项 B:保留当前"引导用户调 X / Y / Z"写法(清晰传达引导语义)
  - 选项 C:在 README "技能概览"表头增加"下游"列的语义说明(如"被谁调用 / 引导谁")
- **决定**:留作 follow-up(用户授权)

---

## 类别:可选
**无**

---

## 类别:必须改
**无**(本评审 0 条必须改,直接进入"全部留作 follow-up"决策)

---

## 总览
- 本次评审 **0 条派生任务**(无必须改)
- **4 条"建议改"全部留作 follow-up**(用户授权决策)
- **0 条"超出本次评审范围"**(无上游需求/设计问题)
- **整体结论:✅ 通过**(非阻塞,REVIEW 完整)
