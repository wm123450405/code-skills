# 偏离记录 — TASK-REQ-00006-00008

版本:V0.0.2

## 偏离 0 项

**结论**:本任务**无任何偏离**(0 项 / 0 项)。

具体对照:
- **设计要求 vs 实际做法**:完全遵循 `./assistants/V0.0.2/plan/REQ-00006/PLAN.md §3 关键变更` + `doc-conventions.md §规则 1`
- **规范要求 vs 实际做法**:完全遵循 `doc-conventions §规则 1`(中英结构对仗 + 同次提交)
- **任务范围**:仅修改 `plugins/code-skills/{README.md, README.en.md}` 2 个文件,各追加 1 行
- **跨任务改动**:**0**(未触及 T-001~T-007 任何产出; T-008 是本需求"最后 1 任务",与其他任务无重叠)

## 本任务的关键选择(非偏离,仅说明)

| 选择 | 理由 | 依据 |
| --- | --- | --- |
| 在表格末尾追加(第 12 行)而非中间插入 | `code-publish` 在用户使用流中是"最后一步"(review → publish);与用户心智模型一致 | NFR-5(简单优于复杂) |
| `git add` + `git commit` **不**由本任务执行 | NFR-3(不自动 commit);"同次提交"指用户后续的 1 次 commit | NFR-3 |
| 行内容**精炼**版(1 句用途 + 4 列紧凑) | 与既有 10 行风格一致;详细描述由 SKILL.md 承担 | NFR-9 |
| `下游` 列填"(运维 / 现场支持 — 部署后查阅手册)" | 与既有"横向"技能(code-init / code-version / code-rule)的 `下游` 列风格一致 | 与既有风格一致 |
| 不增加"## 7. 横向技能"独立章节 | 既有 10 技能混排在表中;不分组;0 H2 变化 = `doc-conventions §规则 1` 0 触发 | `doc-conventions §规则 1` 0 触发 |
| 不**反向引用** SKILL.md 详细描述 | 既有 10 行的 `用途` 列都不引用 SKILL.md | 与既有风格一致 |

## 若未来有偏离,在本节追加

格式:
```
## 偏离 N:<一句话描述>
- **类别**:代码偏离设计 / 代码偏离规范 / 任务范围扩展 / 其他
- **依据**:...
- **实际做法**:...
- **偏离理由**:...
- **影响**:...
- **授权**:用户口头确认 / 用户在 §X 预先授权 / 暂未授权
- **时间**:YYYY-MM-DD HH:mm
```
