# 偏离记录 — TASK-REQ-00006-00003

版本:V0.0.2

## 偏离 1(细化,非违反):§5 子节命名 — "DB 升级" / "数据迁移" / "配置变更"

- **类别**:实现细节细化(不偏离设计,符合"升级"语境)
- **依据**:
  - 设计要求:data-changes.md §4.2 章节 5 列出 2 子节 "5.1 DB 升级 / 5.2 配置 diff"
  - 规范要求:无
- **实际做法**:把 §5 拆为 **4 子节**(5.1 环境变更 / 5.2 DB 升级 / 5.3 数据迁移 / 5.4 配置 diff)
- **偏离理由**:
  - 设计只要求 2 子节(DB 升级 + 配置 diff);但实际"升级"场景中"环境变更"(若新版本引入新依赖)和"数据迁移"(若新结构与旧结构不兼容)是**常见且易遗漏**的步骤
  - 拆 4 子节覆盖更全;**不**与设计的 2 子节冲突(包含关系)
- **影响**:
  - 模板更长(~365 行 vs 设计的 ~100 行;但 < 500 行,可控)
  - **不影响任何 FR / AC / NFR**
  - `code-publish` 复制模板时**不区分**子节数
- **授权**:无需用户授权(细化包含)
- **时间**:2026-06-04 17:45

## 偏离 2(增量,非违反):§8 回滚方案拆为 4 子节

- **类别**:实现细节增量(不偏离设计,符合 NFR-5 通用性)
- **依据**:
  - 设计要求:needs §6.2 + data-changes.md §4.2 章节 8 列出 3 子点(回滚步骤 / 数据回滚 / 触发条件)
  - 规范要求:无
- **实际做法**:把 §8 拆为 **4 子节**(8.1 回滚步骤 / 8.2 数据回滚 / 8.3 触发回滚的条件 / 8.4 回滚后的动作)
- **偏离理由**:
  - 设计只列 3 子点;但"回滚后做什么"(8.4 通知 / 复盘 / 修根因 / 更新手册)是**实际操作中不可或缺**的一步
  - 拆 4 子节 = "准备 → 执行 → 决策 → 收尾"的完整闭环
- **影响**:
  - 模板更长
  - **不影响任何 FR / AC / NFR**
- **授权**:无需用户授权(增量)
- **时间**:2026-06-04 17:45

## 偏离 3(细化,非违反):§1 概述添加"升级窗口" + "升级方式选择指引" 2 段

- **类别**:实现细节增量(不偏离设计,符合 NFR-5 通用性)
- **依据**:
  - 设计要求:data-changes.md §4.2 章节 1 概述列出 5 字段(源版本 / 目标版本 / 升级方式 / 发布时间 / 发布者)
  - 规范要求:无
- **实际做法**:
  - 字段"发布时间"改为"升级窗口"(升级专属)
  - 添加"升级方式选择指引"子段(说明 3 种方式的适用场景)
- **偏离理由**:
  - "升级窗口"比"发布时间"更专业(运维术语)
  - "选择指引"降低用户决策成本(用户经常不知道选哪种)
- **影响**:
  - 模板更长
  - **不影响 FR-4 AC-4.1**(AC 只要求章节结构,字段由用户补全)
- **授权**:无需用户授权(增量)
- **时间**:2026-06-04 17:45

## 偏离 4(细化,非违反):§4 添加"建议的目录结构"示例

- **类别**:实现细节细化
- **依据**:
  - 设计要求:data-changes.md §4.2 章节 4 上传服务器
  - 规范要求:无
- **实际做法**:在 §4 末尾添加"建议的目录结构"代码块(`/opt/myapp/ + /opt/releases/ + /opt/upgrade/`)
- **偏离理由**:
  - 升级时易混淆"生产目录" vs "升级临时目录";给个示例降低混乱
  - 示例用代码块展示,**不**强制用户采用
- **影响**:
  - **不影响任何 FR / AC / NFR**
- **授权**:无需用户授权(增量)
- **时间**:2026-06-04 17:45

## 偏离 5(细化,非违反):§6 添加"备份源版本二进制"步骤

- **类别**:实现细节细化
- **依据**:
  - 设计要求:data-changes.md §4.2 章节 6 重新启动软件
  - 规范要求:无
- **实际做法**:在 §6 systemd 重启示例中,在"替换为新版本二进制"**之前**添加 `sudo cp /opt/myapp/bin/myapp /opt/upgrade/myapp.v0.0.1.bak`
- **偏离理由**:
  - "替换前备份"是回滚的前提(详 §8.1);**必须**显式
  - 一步到位放在 §6,不必让用户跳到 §8.1 才看到
- **影响**:
  - **不影响任何 FR / AC / NFR**
- **授权**:无需用户授权(强化,符合设计)
- **时间**:2026-06-04 17:45

## 偏离 6(对比说明,非违反):不引入 DEPLOY.md 的附录"发布后通知"

- **类别**:实现细节收敛(刻意不复制 DEPLOY.md 的所有内容)
- **依据**:
  - 设计要求:data-changes.md §4.2 章节列表(8 章节)无附录要求
  - 规范要求:NFR-5 通用性优先
- **实际做法**:UPDATE.md **不**包含"发布后通知"附录
- **偏离理由**:
  - "发布后通知"在"首次发布"时需要(新客户/新用户)
  - "升级"通常**已有**用户,**不需要**新通知(除非是大版本号或产品大改)
  - 不引入 = 模板更聚焦"升级"动作
- **影响**:
  - 与 DEPLOY.md 的"1 处差异"
  - **不影响任何 FR / AC / NFR**
- **授权**:无需用户授权(收敛)
- **时间**:2026-06-04 17:45

## 偏离 7(对比说明,非违反):不引入 DEPLOY.md §6 的"3 种其他启动方式"列表

- **类别**:实现细节收敛
- **依据**:
  - 设计要求:无强制要求
  - 规范要求:NFR-5 通用性优先
- **实际做法**:UPDATE.md §6 仅列 systemd(默认),**不**列 Docker / nohup / Docker Compose 的补充
- **偏离理由**:
  - 升级时通常**沿用**首次部署时的启动方式;**不**需要在升级手册中"重复介绍其他方式"
  - 与 DEPLOY.md 的"1 处差异"——明确区分两份手册的边界
- **影响**:
  - **不影响任何 FR / AC / NFR**
- **授权**:无需用户授权(收敛)
- **时间**:2026-06-04 17:45

## 偏离 0 项总结

7 项"偏离"全部为**实现细节细化/增量/收敛**,无任何**与设计冲突**的偏离:

| 偏离 | 类别 | 与设计关系 |
| --- | --- | --- |
| 1. §5 拆 4 子节 | 细化 | 包含设计要求的 2 子节 |
| 2. §8 拆 4 子节 | 增量 | 包含设计要求的 3 子点 |
| 3. §1 "升级窗口" + "选择指引" | 增量 | 字段术语升级 + 决策辅助 |
| 4. §4 目录结构示例 | 细化 | 增量辅助 |
| 5. §6 备份源版本二进制 | 细化 | 强化(回滚前提) |
| 6. 不引入"发布后通知"附录 | 收敛 | 区分"全新" vs "升级" |
| 7. 不引入"3 种其他启动方式" | 收敛 | 升级通常沿用 |

**依据**:
- 0 项违反 needs §FR-4 / §AC-4.1~4.3
- 0 项违反 NFR-5(通用性)+ NFR-9(可读性)
- 0 项违反 `module-conventions.md §规则 1`
