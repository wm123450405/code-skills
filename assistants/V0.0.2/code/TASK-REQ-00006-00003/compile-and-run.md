# 编译与启动验证 — TASK-REQ-00006-00003

版本:V0.0.2
任务:T-003 `[新增] 写 templates/UPDATE.md 模板`
文档型任务,无 build/run/test 命令可执行

## 构建

- 命令:**N/A**
- 结论:**不适用**(纯文档型,无 build)

## 启动

- 命令:**N/A**
- 结论:**不适用**(模板不"运行",由 `code-publish` 在用户调用时复制 + 替换)

## 测试

- 命令:**N/A**
- 结论:**不适用**(无传统单测;模板的"测试"=人工调用 `code-publish` 生成 publish/UPDATE.md 后目检)

## 静态验证

| 检查项 | 期望 | 实际 | 结论 |
| --- | --- | --- | --- |
| 模板路径 | `plugins/.../templates/UPDATE.md` | 同 | ✓ |
| 8 大章节齐全(1~8) | 1 概述 / 2 打包 / 3 获取成果物 / 4 上传 / 5 初始化 / 6 重启 / 7 验证 / 8 回滚 | 全部齐全 | ✓ |
| §5 聚焦"升级"语境 | 5.2 DB 升级 / 5.3 数据迁移 / 5.4 配置 diff | 全部以"升级"措辞 | ✓ |
| §8 回滚方案为新增 | 独立小节 + 3+ 子点 | 4 子点(步骤/数据/触发/动作) | ✓ |
| §1 含"升级方式" placeholder | 3 选 1 | `<原地 / 蓝绿 / 灰度>` | ✓ |
| 文首"使用说明"段 | `⚠` 警示 | 完整段(含"任何升级都不是不可逆的" 强提醒) | ✓ |
| `<本版本号>` placeholder | 在 H1 标题 + §1 概述 | 多处 | ✓ |
| `<源版本>` placeholder | 在 H1 标题 + §1 概述 + §3 / §8.1 | 多处 | ✓ |
| §8.2 含 `<rollback-db.sql>` placeholder | FR-4 AC-4.1 之一 | ✓ | ✓ |
| §8.3 含"触发回滚的条件" placeholder | FR-4 AC-4.1 之一 | ✓ | ✓ |
| 至少 1 个默认示例每节(AC-3.2) | 满足 | 全部满足 | ✓ |
| 验证清单 checkbox | `- [ ] ...` 格式 | §7 15 个 + §8.3 7 个 + §8.4 4 个 = 26 个 | ✓ |
| 不修改 `module-conventions §规则 1` | 模板在 templates/ | ✓ | ✓ |
| 不修改其他 10 个 `code-*` 技能 | 仅新文件 | `git status` 验证 | ✓ |
| 不修改 `rules/` / CLAUDE.md / README | 仅新文件 | `git status` 验证 | ✓ |

**结论**:**所有静态验证通过**,UPDATE.md 模板可被 `code-publish` 技能直接消费。

## 修复记录

无(无 build/run/test 失败)
