# 改修总结 — TASK-REQ-00006-00003

## 1. 任务信息

- **任务编码**:`TASK-REQ-00006-00003`
- **任务标题**:`[新增] 写 templates/UPDATE.md 模板(8 章节 + §8 回滚)`
- **类型**:新增
- **触发/来源**:需求新增
- **关联任务**:无
- **前置任务**:无
- **来源 PLAN.md**:`./assistants/V0.0.2/plan/REQ-00006/PLAN.md` v1.2
- **完成时间**:2026-06-04 17:45
- **完成人**:wangmiao
- **提交哈希**:`<不提交 — 留 dirty tree 由 T-007 后整体 commit>`

## 2. 改修内容总览

| 类别 | 路径 | 操作 | 大小 |
| --- | --- | --- | --- |
| 新增 | `plugins/code-skills/skills/code-publish/templates/UPDATE.md` | Write | 365 行,~14 KB |

**总计**:1 个新文件,0 个修改文件,0 个删除文件。

## 3. 详细改动

### 新增 `plugins/code-skills/skills/code-publish/templates/UPDATE.md`

#### 3.1 路径合规性

- ✓ 位于 `plugins/code-skills/skills/code-publish/templates/` 子目录
- ✓ 文件名 `UPDATE.md`(kebab-case)
- ✓ 符合 `module-conventions.md §规则 1`

#### 3.2 章节结构(8 大章节 + 11 子章节)

| 章节 | 内容 | 子章节 |
| --- | --- | --- |
| **1. 概述** | 源/目标/升级方式/窗口/发布者 + 选择指引 | — |
| **2. 打包** | 3 种打包方式 | 2.1 / 2.2 / 2.3 |
| **3. 获取成果物** | 本版本 vs 源版本对比 | — |
| **4. 上传服务器** | scp + 建议目录结构 | — |
| **5. 初始化系统(升级)** | 4 步 | 5.1 环境变更 / 5.2 DB 升级 / 5.3 数据迁移 / 5.4 配置 diff |
| **6. 重新启动软件** | systemd 默认 + 3 种其他方式 | — |
| **7. 验证清单** | 15 项 checkbox | — |
| **8. 回滚方案(新增)** | 4 步 | 8.1 步骤 / 8.2 数据回滚 / 8.3 触发条件 / 8.4 后续动作 |

#### 3.3 Placeholder 集合(自动 + 用户)

| Placeholder | 类别 | 出现位置 |
| --- | --- | --- |
| `<本版本号>` | **自动**(`code-publish` 替换) | H1 标题 + §1 概述 + §3 + §6 + §7 |
| `<源版本>` | **自动** | H1 标题 + §1 + §3 + §8.1 |
| `<原地 / 蓝绿 / 灰度>` | 用户 | §1 |
| `<YYYY-MM-DD HH:mm>` (升级窗口) | 用户 | §1 |
| `<执行升级的人员>` | 用户 | §1 |
| `<source_dir>` / `<output>` | 用户 | §2 |
| `<image_name>` / `<version>` | 用户 | §2.3 |
| `<user>` / `<server>` / `/path/to/upgrade/` | 用户 | §4 |
| `<upgrade-db.sql>` | 用户(必备) | §3 + §5.2 |
| `<rollback-db.sql>` | 用户(**必备**) | §3 + §8.2 |
| `<user>` / `<passwd>` / `<dbname>` | 用户 | §5.2 + §8.2 |
| `<new-runtime-package>` | 用户(若有) | §5.1 |
| `<key>` / `<value>` / diff 块 | 用户 | §5.4 |
| `<config.yaml.bak>` | 用户(隐含) | §5.4 + §6 + §8.1 |
| `<extracted-dir>` | 用户 | §6 |
| 15+ 验证 checkbox | 用户 | §7 |
| `<回滚步骤>` / `<DB 回滚脚本>` / `<触发回滚的条件>` | 用户(§8 占位) | §8 |
| 4 项回滚后续动作 | 用户 | §8.4 |

#### 3.4 §8 回滚方案(本模板**专属**内容,DEPLOY.md 没有)

**4 子节**:
- **8.1 回滚步骤**:默认示例(原地升级 → 原地回滚的 6 步);蓝绿/灰度回滚指引
- **8.2 数据回滚(若有)**:默认示例(`mysql < <rollback-db.sql>`);`<rollback-db.sql>` 应在升级前准备好
- **8.3 触发回滚的条件**:7 个 checkbox(无法启动 / 5xx / DB 迁移失败 / 数据丢失 / 安全漏洞 / 监控告警 / 用户报告)+ 决策 SLA 15 分钟
- **8.4 回滚后的动作**:4 项 checkbox(通知相关方 / 修根因 / 保留日志 / 更新手册)

#### 3.5 文首"使用说明"段

```markdown
> ⚠ **本手册为通用升级部署骨架**,由 `code-publish` 技能从 `.../templates/UPDATE.md` 复制生成(**仅当本版本非基线**)。
>
> **请先手动补全所有 `<placeholder>`(除 `<本版本号>` / `<源版本>` 外),再按本手册的步骤执行升级。**
>
> 本手册**默认提供"最常见升级方式"的占位示例**(原地升级 + tar.gz 包替换 + MySQL 数据库);若你使用蓝绿 / 灰度 / 容器化等其他升级方式,请按需调整各章节示例。
>
> **重要:升级前请务必完成第 8 节"回滚方案"的全部准备**(备份 / 回滚脚本 / 决策点);**任何升级都不是"不可逆"的**。
```

**关键警示**:
- "**仅当本版本非基线**" — 与 code-publish 基线判定对齐
- "**任何升级都不是不可逆的**" — 强提醒,促进回滚方案准备

#### 3.6 与 DEPLOY.md 的关键差异(详 `deviations.md` §6/§7)

| 差异点 | DEPLOY.md | UPDATE.md |
| --- | --- | --- |
| H1 | `— <本版本号>` | `从 <源版本> 升级到 <本版本号>` |
| 自动 placeholder | 仅 `<本版本号>` | `<本版本号>` + `<源版本>` |
| §5 子节 | 5.1 环境 / 5.2 DB 建表 / 5.3 DB 初始化 / 5.4 配置 | 5.1 环境变更 / 5.2 DB 升级 / 5.3 数据迁移 / 5.4 配置 diff |
| §6 措辞 | "启动运行" | "重新启动软件" |
| §8 | (无) | **回滚方案(新增)** |
| 附录 | "发布后通知"(可选) | **不引入** |
| §6 其他启动方式列表 | 3 种补充 | **不引入**(升级沿用首次部署) |

**模板独立**:UPDATE.md 与 DEPLOY.md **不**互相引用,改动其中一份不会破坏另一份。

## 4. 关键决策与权衡(8 项)

| # | 决策 | 选定 | 理由 |
| --- | --- | --- | --- |
| IT-1 | H1 标题 | `从 <源版本> 升级到 <本版本号>` | 强调"升级" vs "全新" |
| IT-2 | 复用 DEPLOY.md §2-§7 风格 | 措辞改"升级"语境 | 区分度;但**不**完全相同 |
| IT-3 | §8 拆 4 子节 | 步骤 / 数据 / 触发 / 动作 | 完整闭环 |
| IT-4 | §5.2 "DB 升级"默认 mysqldump + 应用 | 行业最常见模式 | NFR-5 |
| IT-5 | §1 含"升级方式" 3 选 1 | 强提示,降低决策成本 | 升级方式显著影响后续步骤 |
| IT-6 | §8 不给详细命令 | 只给"步骤骨架" + placeholder | 回滚强依赖具体环境 |
| IT-7 | 不引入"维护模式"等高级话题 | 模板聚焦"升级动作" | NFR-5 + NFR-9 |
| IT-8 | 不与 DEPLOY.md "共享太多" | 独立文件,冗余优先于耦合 | 改动一方不会破坏另一方 |

## 5. 偏离设计/规范的地方

详 `deviations.md`。**0 项与设计冲突的偏离**,7 项实现细节细化/增量/收敛(均为 NFR-5 / NFR-9 合规范围内的实现选择)。

## 6. 验证结果(详 `compile-and-run.md`)

| 验证项 | 结论 |
| --- | --- |
| 模板路径合规 | ✓ `plugins/.../templates/UPDATE.md` |
| 8 大章节齐全 | ✓ |
| §5 / §8 子节完整 | ✓ 4 + 4 |
| §8 回滚方案为新增 | ✓ 4 子节 |
| 文首"使用说明" | ✓ |
| placeholder 自动 + 用户 | ✓ 2 自动 + 17+ 用户 |
| checkbox 总数 | ✓ 26 个 |
| `module-conventions §规则 1` | ✓ |
| 0 修改既有 | ✓ |

## 7. 已知问题/未完成项

### 已知问题
- **无**

### 未完成项(由后续任务承接)
- **T-004**(Q&A.md):占位模板
- **T-005**(qanda-README.md):qanda 目录说明
- **T-006**(assistants-layout.md):工作目录约定
- **T-007**:端到端验证 + 不变量自检
- **Q-D-1**(留 v2):`code-publish` 注册到 marketplace.json
- **Q-D-3 ~ Q-D-7**(留 code-review / 其他 REQ)

## 8. 关联任务与提交

- **关联原任务**:无
- **关联后续任务**:T-004, T-005, T-006, T-007, T-008
- **Git 提交**:**未提交**(NFR-3 + 沿用 V0.0.0~V0.0.1 实践)

## 9. 步骤 14 状态更新(PLAN.md)

| 字段 | 旧值 | 新值 |
| --- | --- | --- |
| 开发状态 | 进行中 | **已完成** |
| 完成时间 | — | 2026-06-04 17:45 |
| 完成人 | — | wangmiao |
| 涉及文件 | (空) | `plugins/code-skills/skills/code-publish/templates/UPDATE.md`(365 行,~14 KB) |
| 提交哈希 | (空) | (不提交) |

## 10. 下一步建议

1. **下一任务**:`/code-skills:code-it TASK-REQ-00006-00004`(Q&A.md 模板,0.2d)
2. **可并行**:`/code-skills:code-it TASK-REQ-00006-00005` / `00006` / `00008`(3 任务独立,共 ~0.7d)
3. **收尾**:`/code-skills:code-it TASK-REQ-00006-00007`(不变量自检 + 端到端 3 场景)
