# 偏离记录 — TASK-REQ-00017-00002
版本:V0.0.2

## 偏离 1:P-1 步骤位置与"显式契约"调整

- **类别**:实施期发现详细设计 §3.3 与既有 /code-it 步骤 15 重复
- **依据**:
  - 设计要求:详细设计 §3.3 — P-1 步骤在"末尾兜底 commit 成功后"新增
  - 既有结构:`/code-it` 步骤 15 "同步版本看板" 第 2 款"找到本任务行,更新开发状态"已经在做"推进本任务看板状态"职责
- **实际做法**:
  - 不在 /code-it 中新增"末尾兜底后 P-1"独立步骤(因 /code-it 无显式末尾兜底 commit 步骤)
  - 改为新增"步骤 14.5 推进看板开发状态"小节,**显式引用**步骤 15 + 引用 /code-plan 拆任务约束
  - 步骤 14.5 本身**不重写**步骤 15 的实施细节
- **偏离理由**:
  - /code-it 步骤 15 既有实现已经覆盖 P-1 推进看板的所有算法/边界/失败处理/幂等性
  - 新增独立 P-1 步骤会与步骤 15 重复,增加文档维护成本
  - 调整为"显式契约"既满足"职责衔接"需求,又避免重复
- **影响**:
  - **0 影响**:`/code-it` 整体行为字节级保留(步骤 15 既有逻辑保持)
  - **0 偏离 INV**:INV-1~7 全部满足(INV-2 P-1 推进本任务看板"开发状态"由步骤 15 既有逻辑承担)
  - **0 触发 `dashboard-conventions §规则 1` 3 处同步**:沿用既有"任务完成"事件类型
- **授权**:用户授权(本任务实施期发现,经偏差文档记录,无独立 AskUserQuestion 必要)
- **时间**:2026-06-05 16:45
