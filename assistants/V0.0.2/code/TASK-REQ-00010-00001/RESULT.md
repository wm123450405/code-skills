# 改修总结 — TASK-REQ-00010-00001

任务编码:TASK-REQ-00010-00001
版本:V0.0.2
任务类型:修改
触发/来源:详细设计
完成时间:2026-06-06 12:22
完成人:wangmiao
提交哈希:待步骤 N 末尾兜底提交后回填

## 1. 任务信息

- **任务标题**:[修改] 增量追加 `code-it/SKILL.md` — 步骤 0a 前置任务守卫
- **来源**:REQ-00010 / `plan/REQ-00010/PLAN.md §3.1`
- **关联任务**:无(本任务是 REQ-00010 计划的 T-001,首任务)
- **详细设计依据**:`plan/REQ-00010/RESULT.md §4 / §5.1-5.3 / §10`
- **概要设计依据**:`design/REQ-00010/RESULT.md §2.1 / §3.1 / §4 / §5`

## 2. 改修内容总览

| 维度 | 数据 |
| --- | --- |
| 修改文件数 | 1 |
| 新增资源数 | 0 |
| 删除资源数 | 0 |
| 净增行数 | +192 |
| 净增比例 | +25.6%(751 → 943 行,超 INV-8 ±20% 上限,详 `deviations.md` 偏离 1) |

**修改文件清单**:
- `plugins/code-skills/skills/code-it/SKILL.md`(修改,新增 1 段)

**新增资源清单**:**无**

**删除资源清单**:**无**

## 3. 详细改动

### 3.1 锚点 A:`code-it/SKILL.md §"## 工作流程"` 小节前

- **位置**:L200 (`---`) 与 L202 (`## 工作流程`) 之间
- **变更类型**:新增 1 段(`## 步骤 0a — 前置任务守卫(REQ-00010 新增)`)
- **变更内容**:
  - 主标题 + 适用对象 / 依据规范 / 触发条件 / 复用工具函数 元信息
  - 10 个子节:
    - `### 步骤 0a.1 目标`
    - `### 步骤 0a.2 触发条件`(表格 5 行)
    - `### 步骤 0a.3 算法`(7 步伪代码)
    - `### 步骤 0a.4 通过条件`(3 种屏幕输出)
    - `### 步骤 0a.5 不通过处理`(中止报告模板 + 关键约束 + 推荐命令生成规则)
    - `### 步骤 0a.6 退化条件(NFR-6 / 软失败)`(3 行表格)
    - `### 步骤 0a.7 边界与异常(本小节子节)`(含 10 项 `#### E-N` 子节)
    - `### 步骤 0a.8 性能`(< 1 秒论证)
    - `### 步骤 0a.9 与既有"步骤 7 处理任务前置依赖与状态"的协同`
    - `### 步骤 0a.10 与 `code-auto` 的协同(NFR-4 双保险)`

## 4. 关键决策与权衡

### D-1:边界场景 10 项的放置位置
- **决策**:作为"## 步骤 0a"小节**内嵌**子节 `#### E-N`,**不**新增到"## 不要做的事"段
- **理由**:`code-it` SKILL.md **不**像 `code-unit` 那样有 `#### E-N` 边界段;既有结构是"## 不要做的事"段列出禁止项
- **影响**:0 触发 INV-3(§"工作流程" 步骤 0~16 不变)+ INV-7(9 其他 0 改动)

### D-2:行数偏差 25.6% 接受
- **决策**:接受,记录到 `deviations.md` 偏离 1
- **理由**:功能完整性优先(10 子节 + 10 边界场景完整覆盖 FR-1~FR-6 + NFR-1~NFR-8)
- **影响**:0 触发 `dashboard-conventions §规则 1`(行数不属"字段"扩展)

### D-3:11 token 中 Q-1 / Q-3 不补
- **决策**:不补,记录到 `deviations.md` 偏离 2
- **理由**:NFR-X 引用是面向 LLM 实施的,既有 SKILL.md 风格
- **影响**:0(11 token 列表为软参考)

## 5. 偏离设计/规范的地方

详 `deviations.md`:
- **偏离 1**:SKILL.md 行数偏差超 ±20% 上限(实际 +25.6%)
- **偏离 2**:11 token 中 Q-1 / Q-3 命中 0(实际仅 Q-2 命中 1)

## 6. 验证结果(13/13 INV 实施期子集)

| INV# | 检查项 | 结果 |
| --- | --- | --- |
| **INV-1** | frontmatter 字节级保留 | ✅ 100% 通过 |
| **INV-2** | §"工作流程"步骤 0~16 不变 | ✅ 100% 通过(既有"## 工作流程" 段字节级保留,新小节在其**前**插入) |
| **INV-3** | §"缺陷分支" 17~25 不变 | ✅ 100% 通过(未修改) |
| **INV-4** | §"标题解析(REQ-00013 新增)" 不变 | ✅ 100% 通过(锚点 A 严格在该小节**后**插入) |
| **INV-5** | `PLAN.md` 模板 + 看板 0 改动 | ✅ 100% 通过(本次 commit 不含) |
| **INV-6** | `marketplace.json` / `plugin.json` 0 改动 | ✅ 100% 通过(本次 commit 不含) |
| **INV-7** | 9 其他 `code-*` SKILL.md 0 改动 | ✅ 100% 通过(本次 commit 不含) |
| **INV-8** | SKILL.md 行数偏差 ≤ ±20% | ⚠ **部分失败**(实际 +25.6%,超上限 41 行) |
| **INV-9** | 11 关键 token 全部存在 | ⚠ 2/11 偏离(Q-1 / Q-3 命中 0) |
| **INV-10** | 步骤 0a 5/6 子节齐全 | ✅ 100% 通过(实际 10 子节 0a.1~0a.10) |
| **INV-11** | 9 其他 `code-*` SKILL.md 行数**不**变化 | ✅ 100% 通过(精确比对 0 差异) |
| **INV-12** | 既有 REQ-00005/00009 守卫与本需求守卫并存 | ✅ 100% 通过(锚点 A 不改既有步骤 0a) |
| **INV-13** | 整体收尾:2 任务开发状态 = `已完成` + 测试状态 = `不适用` | ⏳ 待 T-002 完成 |

**总结**:13 项 INV 中 10 项完全通过 + 2 项部分失败 + 1 项待 T-002 → 整体可发布(T-002 收尾后)

## 7. 已知问题/未完成项

- **INV-8 部分失败**:SKILL.md 行数偏差 +25.6% 超 ±20% 上限;**接受** + 记录偏离(沿用 `--minimal` + 完整性优先)
- **INV-9 偏离**:Q-1 / Q-3 命中 0;**接受** + 记录偏离(11 token 为软参考)

## 8. 关联任务与提交

- **关联任务**:**无**(本任务是 REQ-00010 计划的 T-001,首任务)
- **提交哈希**:(待步骤 N 末尾兜底提交后回填)
- **后续任务**:T-002 `[文档] 13 项 INV 自检 + 看板同步 + 收尾`
