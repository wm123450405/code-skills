# TASK-REQ-00019-00001 改修总结

- 任务编码:`TASK-REQ-00019-00001`
- 任务标题:`[修改] 增量追加 code-plan/SKILL.md(5 锚点改造)`
- 类型:修改
- 触发/来源:需求新增(REQ-00019)
- 需求:REQ-00019
- 详细设计:./assistants/V0.0.2/plan/REQ-00019/RESULT.md §4 模块 1 / §6 接口 1
- 状态:**已完成**
- 开始时间:2026-06-06 15:30
- 完成时间:2026-06-06 15:45
- 责任人:wangmiao
- 提交哈希:`<TBD>`(末尾兜底提交后回填)

## 1. 改修内容总览

修改文件:**1 个**

| 文件 | 变更类型 | 净增 | 关键变更 |
| --- | --- | --- | --- |
| `plugins/code-skills/skills/code-plan/SKILL.md` | 修改 | +88/-24 = +64 行 | 5 锚点改造:锚点 A/B/C/D/E |

## 2. 详细改动

### 2.1 锚点 A:§"缺陷分支" 起始引言 3 条修订(L588-595)

- **既有**:"故 `RESULT.md` 简化为 `fix-plan.md`(单文件)" / "不需要拆分为多个 `PLAN.md` 任务" / "任务编号可省略"
- **改为**:产出物升级为 `RESULT.md` + `PLAN.md` + 7 份过程文档,与 REQ 模式完全同构 / 拆分为 `TASK-BUG-NNNNN-NNNNN` 任务(5+5 位嵌套式) / BUG 任务沿用 `TASK-...` 编号体系新格式

### 2.2 锚点 B:步骤 23 末追加 E-1 边界(L623-627 → 步骤 23+)

- **新增子节**:`### 步骤 23+ — 步骤 23 修订 E-1 边界(本需求 REQ-00019 新增,2026-06-06 起生效)`
- **关键内容**:检测 `fix-plan.md` 存在(仅 BUG-00001)→ 提示 + 3 选 1(继续/手动迁移/中止)
- **依据规范**:`./assistants/rules/module-conventions §规则 1`

### 2.3 锚点 C:步骤 24A 重写 + 步骤 24A+1 产出 9 份文档同构(L629-653)

- **步骤 24A 重写**:
  - 既有 4 子任务保留(根因定位 / 修复方案 / 测试方案 / 风险与回退)
  - 第 5 子任务"若修复跨多步" → 改为"任务拆分"(拆为 `TASK-BUG-NNNNN-NNNNN`)
- **步骤 24A+1 新增**:`### 步骤 24A+1 — 产出 9 份文档同构于 REQ 模式(本需求 REQ-00019 新增,2026-06-06 起生效)`
- **关键内容**:9 份文档产出清单 + 任务编号分配逻辑(沿用步骤 9B)

### 2.4 锚点 D:步骤 26A 改模板引用(L660-676)

- **既有**:"按 `templates/fix-plan.md` 的章节结构生成" + 10 章节清单
- **改为**:"按 `templates/plan.md` + `templates/task-plan.md` 同构产出" + 14 + 8 章节清单 + 关键差异(vs REQ 模式)
- **`fix-plan.md` 不再生成**;`templates/fix-plan.md` 留作历史不删

### 2.5 锚点 E:步骤 28A 末追加步骤 28A+1 同步看板"任务清单"(L733-742)

- **新增子节**:`### 步骤 28A+1 — 同步版本看板'任务清单'区段(本需求 REQ-00019 新增,2026-06-06 起生效)`
- **关键内容**:6 步同步逻辑(读 RESULT.md / 读 PLAN.md / 追加 N 行 / 失败处理 / 屏显 / 关键约束)
- **关键约束**:0 触发 `dashboard-conventions §规则 1` 三同步(0 新增字段/枚举/区段)

## 3. 关键决策与权衡

- **D-1 锚点 A 修订 3 条**:既保留既有"关键简化"框架,又满足"形式同构"目标
- **D-2 锚点 B 单独新增 E-1 边界**(步骤 23+):为 BUG-00001 历史 `fix-plan.md` 提供兼容检测,**不**破坏既有数据
- **D-3 锚点 C 重写步骤 24A + 新增步骤 24A+1**:任务拆分在 24A 末尾;9 份文档产出逻辑在 24A+1(职责分离)
- **D-4 锚点 D 改模板引用**:`fix-plan.md` 退场;`templates/fix-plan.md` 留作历史
- **D-5 锚点 E 单独新增步骤 28A+1**:BUG 任务同步与 28A 缺陷清单同步**正交**,互不干扰

## 4. 偏离设计/规范的地方

无(详 `deviations.md`:0 偏离;INV-1~13 全部通过)。

## 5. 验证结果

- **静态自检 9/9 全部通过**(详 `compile-and-run.md`)
- **行数偏差 +6.7%**(945 → 1008,远低于 +20% 上限)
- **frontmatter 字节级保留**(md5 `375aa57708e81312e42532172a7a74ef` 一致)
- **5 锚点全部命中且定位精确**

## 6. 已知问题/未完成项

- V-1~V-5 + V-7 端到端验证需 T-002 完成后执行(本任务**不**涉及 `code-it` 改造,故不阻塞)

## 7. 关联任务与提交

- **关联任务**:T-002(`code-it/SKILL.md` 7 锚点改造,依赖本任务)
- **提交哈希**:`<TBD>`(末尾兜底提交后回填)
- **看板同步**:任务清单 T-001 行 开发状态=已完成;文档头"最近更新"=2026-06-06 15:45;变更记录追加
