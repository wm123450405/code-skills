# 任务改修正文 — TASK-REQ-00005-00003

- 任务编码:`TASK-REQ-00005-00003`
- 任务标题:`code-plan/SKILL.md` 增量追加 步骤 0a + N
- 任务类型:修改
- 触发/来源:需求新增
- 状态:已完成
- 责任人:wangmiao
- 创建:2026-06-04 17:05
- 完成:2026-06-04 17:10
- 提交哈希:`e568328193dde957c1ebafc90addda4c1eb286fe`
- 所属版本:V0.0.2
- **上游设计**:`./assistants/V0.0.2/plan/REQ-00005/PLAN.md §3.3` + `RESULT.md §4.3` + `module-details.md §3`
- **下游**:`code-unit TASK-REQ-00005-00003`(守卫判定"不可测"→ 跳过单测) + `code-review REQ-00005`(评审 13 关键不变量)

---

## 1. 任务信息

- **任务编码**:`TASK-REQ-00005-00003`(5+5 位,`encoding-conventions.md §规则 3`)
- **任务标题**:`code-plan/SKILL.md` 增量追加 步骤 0a + N
- **任务类型**:修改
- **触发/来源**:需求新增(FR-1 / FR-3)
- **来源**:`PLAN.md §3.3`(本任务在 V0.0.2/plan/REQ-00005/PLAN.md 任务总览中,行 41-42)

---

## 2. 改修内容总览

| 操作 | 文件 | 变更行数 |
| --- | --- | --- |
| 修改 | `plugins/code-skills/skills/code-plan/SKILL.md` | +47 行(0 删除) |

**新增**章节:
- `### 步骤 0a — 拉取最新代码(强制前置,新增)` — 30 行
- `### 步骤 N — 末尾兜底提交(强制收尾,新增)` — 40 行(部分行含代码块)

**未变更**(强约束不动):
- YAML frontmatter(行 1-7)字节级保留
- 既有"步骤 0 — 版本上下文检测"(全文)
- 既有"步骤 1-18A / 13B / 19-28(BUG 路径)"(全文)
- 既有"过程文档格式" 小节
- **未新增"步骤 0b"**(FR-2 显式仅 `code-require` 专属,严禁)

---

## 3. 详细改动

### 3.1 插入点 A:步骤 0a(1 次 Edit)

- **位置**:`## 工作流程` 章节标题之后、`### 步骤 0 — 版本上下文检测(强制前置)` 标题**之前**
- **锚点字符串**:`## 工作流程\n\n### 步骤 0 — 版本上下文检测(强制前置)`
- **插入的 1 章节**:
  - 步骤 0a:FR-1 实施,4 步 + 3 失败分类(E-2/E-3/E-4)+ 立即读 `.current-version`(NFR-8)
  - **关键差异(与 T-001 步骤 0a)**:步骤 1.4 文案 `进入步骤 0b` → `进入既有"步骤 0 — 版本上下文检测"`(因 `code-plan` 无 0b)
  - **关键差异(与 T-002 步骤 0a)**:本任务步骤 0a 与 T-002 **完全相同**(同 `module-details.md §3.2`)
  - 章节注释显式说明:`code-plan` **不**含步骤 0b

### 3.2 插入点 B:步骤 N(1 次 Edit)

- **位置**:`## 过程文档格式` 标题**之前**(行 543 之前)
- **锚点字符串**:`## 过程文档格式`
- **插入的 1 章节**:
  - 步骤 N:FR-3 实施,5 步 + 空跳过(NFR-4)+ 弹窗 3 选 1(Q-3 锁定 A)+ commit 失败不重试(E-10)
  - **关键差异(与 T-001 / T-002 步骤 N)**:commit message 模板 `chore(code-plan): ...`(scope = `code-plan`,含 2 结果文件 "PLAN.md + RESULT.md")
  - **特殊**:本步骤对**首次设计(步骤 7A–18A) / 增量更新(步骤 7B–13B) / BUG 路径(步骤 19-28)** 三种情况都执行(`code-plan` 唯一同时支持 REQ 路径和 BUG 路径)
  - 章节注释显式说明:对 REQ 路径 / BUG 路径都适用

---

## 4. 关键决策与权衡

### 4.1 2 次 Edit 完成 2 个插入
- **步骤 0a**:`## 工作流程` → `### 步骤 0` 之间 = 1 次 Edit
- **步骤 N**:`## 过程文档格式` 之前 = 1 次 Edit
- **结果**:共 2 次 Edit 完成 2 章节插入

### 4.2 末尾兜底 commit 的范围
- **本任务步骤 N**:`git status --porcelain` 收集到 5 个 dirty 文件
- **决策**:**只**暂存本任务的 `code-plan/SKILL.md`(不"补 commit" T-001 / T-002 / T-004 的 dirty)
- **理由**:
  - T-001 末尾兜底已 commit `code-require/SKILL.md`(hash `a157d7b`)
  - T-002 末尾兜底已 commit `code-design/SKILL.md`(hash `3e1573e`)
  - T-003 末尾兜底本次 commit `code-plan/SKILL.md`(hash `e568328`)
  - Q-4 锁定 B(任务边界)

### 4.3 commit message 生成
- **自动生成**:`chore(code-plan): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交`(NFR-6 沿用 V0.0.1 实践)
- **scope = `code-plan`**(`interface-specs.md §5.1.3`)
- **subject 中 `REQ-00005`**:`encoding-conventions.md §规则 1` 5 位纯数字(严格)
- **body 包含统计 + 任务编号 + 关键变更点**

### 4.4 步骤 N 对 REQ / BUG 路径的统一处理
- **设计**:`code-plan` 是**双路径设计**(既支持 REQ 也支持 BUG);步骤 N 末尾兜底不区分路径
- **理由**:末尾兜底只关心 `git status --porcelain` 输出,不关心是哪个路径产生的文件
- **章节注释显式说明**:对 3 种工作流(7A 首次 / 7B 增量 / 19-28 BUG)都执行

---

## 5. 偏离设计/规范的地方

**无任何偏离**(详见 `deviations.md`)。本任务 100% 遵循上游设计 + 规范,**0 偏离**。

---

## 6. 验证结果

| 验证项 | 来源 | 结果 |
| --- | --- | --- |
| 2 新章节各 1 命中 | PLAN.md §3.3 验证手段 | ✅ |
| **步骤 0b 0 命中**(严禁) | FR-2 显式 | ✅ |
| 既有"步骤 0"仍 1 命中 | PLAN.md §3.3 验证手段 | ✅ |
| 增量修改(47 行新增 / 0 删除) | NFR-2 | ✅ |
| 末尾兜底 commit 成功 | FR-3 / NFR-4/5/6/7 | ✅(hash `e568328`) |
| commit message 格式合规 | NFR-6 | ✅ |
| 0 失败(无需进入错误修复循环) | `code-it` 步骤 12 | ✅ |

详见 `compile-and-run.md` 完整输出。

---

## 7. 已知问题/未完成项

**无**。本任务 100% 完成,可发布。

---

## 8. 关联任务与提交

- **提交**:`e568328193dde957c1ebafc90addda4c1eb286fe`
- **提交信息**:
  ```
  chore(code-plan): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交

  详细设计与编码计划完成:4 个任务
  过程文件 N + 结果文件 2(PLAN.md + RESULT.md)

  TASK-REQ-00005-00003: 在 code-plan/SKILL.md 增量追加
  - 步骤 0a 拉取最新代码(FR-1,NFR-8)
  - 步骤 N 末尾兜底提交(FR-3,NFR-4/5/6/7)
  - 不含步骤 0b(FR-2 显式仅 code-require 专属)
  - 步骤 N 对 REQ 路径 / BUG 路径都适用
  frontmatter 字节级保留,既有步骤 0-18A/13B/19-28 全文不动
  ```
- **关联任务**:
  - 前置任务:**无**
  - 后续任务:`TASK-REQ-00005-00004`(T-004 / 看板同步)
  - 关联已完成任务:
    - `TASK-REQ-00005-00001`(T-001 / `code-require` 增量,提交 `a157d7b`)
    - `TASK-REQ-00005-00002`(T-002 / `code-design` 增量,提交 `3e1573e`)

---

## 9. 后续建议

1. **`code-it TASK-REQ-00005-00004`**:同步 V0.0.2/RESULT.md 看板(任务清单行 4 行的状态、统计、变更记录)
2. **`code-unit TASK-REQ-00005-00003`**:守卫判定"不可测" → 跳过单测(测试状态保持 `不适用`)
3. **`code-review REQ-00005`**:评审 4 任务的开发状态 + 13 关键不变量 + 3 任务 commit 链

### 9.1 当前 3 任务 commit 链
```
e568328 chore(code-plan): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交
3e1573e chore(code-design): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交
a157d7b chore(code-require): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交
a78d404 需求全录入
```

3 个 commit 共享同一父 hash (`a78d404`),独立 commit 链模式;每个 commit 的 subject 一致(便于审计 `git log --grep`)。
