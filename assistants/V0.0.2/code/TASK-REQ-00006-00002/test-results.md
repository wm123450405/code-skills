# 测试结果 — TASK-REQ-00006-00002

版本:V0.0.2
时间:2026-06-04 17:34
任务:T-002 `[新增] 写 templates/DEPLOY.md 模板`

## 测试命令

**N/A** — 本仓库无 test 命令,纯文档型。

## 不适用说明

本任务**测试状态 = 不适用**的理由(沿用 PLAN.md §3 的"不适用理由"):

1. **本仓库无传统单元测试**(详 `risk-analysis.md §5`)
2. **DEPLOY.md 模板是"静态文档"**:无运行时函数可测
3. **模板的"测试"=人工调用 `code-publish` 生成 publish/DEPLOY.md 后目检**;这由 T-007 收尾任务执行
4. **端到端验证**:
   - 步骤 1:V0.0.2 当前应有未完成任务 → 调 `code-publish` 不会进 Step 2(不写 publish/),**DEPLOY.md 模板本身**不会被实例化
   - 步骤 2:需要"可发布版本"才能实例化;**当前无**(全 V0.0.2 任务进行中)
   - 步骤 3:T-007 收尾时会构造"模拟可发布"场景(或留作 v2 验证)

## 端到端验证场景(将由 T-007 执行)

| 场景 | 输入 | 预期(模板被实例化的部分) |
| --- | --- | --- |
| 1. 可发布版本 + 用户调用 `code-publish` | `/code-publish` | `publish/DEPLOY.md` 第 1 行 H1 = `# 发布部署手册 — V0.0.2`(自动替换 `<本版本号>`);其他 placeholder 保持 |
| 2. 模板可独立打开阅读 | `cat plugins/.../templates/DEPLOY.md` | 8 章节结构清晰,每章节默认示例可执行 |

## 失败用例详情

无(本任务无失败用例;静态结构验证全部通过)
