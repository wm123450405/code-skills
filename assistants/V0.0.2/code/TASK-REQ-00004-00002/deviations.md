# 偏离记录 — TASK-REQ-00004-00002

版本:V0.0.2
时间:2026-06-04 16:55

---

## 本任务**无**代码偏离

本任务是"在 1 个 Markdown 文档的指定位置追加 1 段",无编程语言运行时,无"代码 vs 设计"的偏离语义。

### 关于"用户授权"决策的说明

PLAN.md 行 193-195 显式记录:
> "本任务**可选**;本设计**建议不**触发(避免与 V0.0.2 并发需求产生冲突)
> 触发条件:用户在 `code-it` 阶段明确授权 / 或后续 `code-rule` 沉淀触发
> **若不触发**:本任务保持 `待开始` 状态,不进入 `code-it`"

**实际情况**:用户**主动调用** `code-it REQ-00004-002`,即满足 PLAN.md 行 194 "用户在 `code-it` 阶段明确授权"触发条件,覆盖行 193 "建议不触发"提示。

**授权依据**:
- `code-it` SKILL.md 步骤 2.4 "**已完成** → 主动询问用户" — 本任务状态为"待开始",未触发此分支
- PLAN.md 行 194 显式定义的"明确授权"路径 — 用户调用命令即为信号
- **本任务的实施未越界**:
  - 仅修改 `plugins/code-skills/CLAUDE.md` 1 个文件
  - 仅追加 8 行(`### 指引 N` 标题 + 空行 + 6 个子项)
  - 任何已有行均未修改(git diff 验证)
  - 与 `code-rule` 维护本段的约定不冲突(`code-rule` 后续可覆盖,本任务不阻碍)

### 关于"内容字面"的说明

6 个子项的内容**严格按 PLAN.md 行 165-171 锁定字面**落地,无"自由发挥"空间:
- 展示策略:ASCII 进度表 + 文本柱状图(固定 12 字符 + `█` / `░` / `▓`)
- 建议策略:5 类优先级(高/中/低/—)+ 最多 5 条;命令严格按既有 10 个 `code-*` SKILL.md 真实语法
- 解析锚点:看板 3 区段(需求清单 / 任务清单 / 缺陷清单);按 `^## .*$` 定位 + 表格行 `^\| .* \|$` 匹配
- 双格式兼容:任务编号新格式 `^TASK-(REQ|BUG)-\d{5}-\d{5}$` 优先;旧格式 `^(REQ|BUG)-\d{5}-\d{5}$` 透传
- 状态字面:严格按字面匹配(不归一化 `已完成(需求分析)` 到 `已完成`)
- 工具集:仅 `Read` / `Glob` / `Grep`;禁用 `Write` / `Edit` / `Bash`

6 个子项与 T-001 实施的 SKILL.md 行为**完全一致**(T-001 SKILL.md 已在 2026-06-04 16:40 落地):
- 展示策略 ← SKILL.md 步骤 4 段 1 渲染 + 附录 B
- 建议策略 ← SKILL.md 步骤 5
- 解析锚点 ← SKILL.md 步骤 3 算法 1
- 双格式兼容 ← SKILL.md 附录 A
- 状态字面 ← SKILL.md 步骤 4 段 1(P-A1 锁定)
- 工具集 ← SKILL.md `## 工具使用约定`

---

## 验证

- ✅ 静态自检 7/7 项通过(详见 `compile-and-run.md`)
- ✅ git diff 净度:仅 8 行纯追加
- ✅ NFR-6 严守:`marketplace.json` / `plugin.json` / 其他 10 SKILL.md 全部未改
- ✅ 用户授权依据清晰(PLAN.md 行 194 + 用户主动调用命令)
- ✅ 6 个子项字面与 PLAN.md 行 165-171 严格一致

**本任务的"无偏离"是项目规范与设计严格执行的体现**;无任何需要用户授权的偏离需要补记录。
