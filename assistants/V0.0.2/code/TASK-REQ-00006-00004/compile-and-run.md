# 编译与启动验证 — TASK-REQ-00006-00004

版本:V0.0.2
任务:T-004 `[新增] 写 templates/Q&A.md 模板`
文档型任务,无 build/run/test 命令可执行

## 构建

- 命令:**N/A**
- 结论:**不适用**(纯文档型,无 build)

## 启动

- 命令:**N/A**
- 结论:**不适用**(模板不"运行",由 `code-publish` 在用户调用时复制 + QandaAggregator 渲染)

## 测试

- 命令:**N/A**
- 结论:**不适用**(无传统单测)

## 静态验证

| 检查项 | 期望 | 实际 | 结论 |
| --- | --- | --- | --- |
| 模板路径 | `plugins/.../templates/Q&A.md` | 同 | ✓ |
| H1 含 `<本版本号>` 自动 placeholder | ✓ | 2 处(H1 标题 + 工作流提示) | ✓ |
| 引言段说明"聚合自 + 何时用" | ✓ | `> 本手册聚合自 \`assistants/qanda/\`,供发布部署中遇到问题时查阅。` | ✓ |
| 文首 `⚠` 警示"占位状态" | ✓ | 警示段存在 | ✓ |
| `## 占位:常见问题(待补充)` 章节 | ✓ | 存在 | ✓ |
| 4 步"如何添加 Q&A 内容" | ✓ | 4 步完整 | ✓ |
| 完整生成结果示例(假设 qanda/ 有 2 个文件) | ✓ | 示例 markdown 块 | ✓ |
| 排除规则说明(README.md 不被聚合) | ✓ | 显式说明 | ✓ |
| 排序规则说明(字典序) | ✓ | 显式说明 | ✓ |
| 不修改 `module-conventions §规则 1` | 模板在 templates/ | ✓ | ✓ |
| 不修改其他 10 个 `code-*` 技能 | 仅新文件 | `git status` 验证 | ✓ |
| 不修改 `rules/` / CLAUDE.md / README | 仅新文件 | `git status` 验证 | ✓ |

**结论**:**所有静态验证通过**,Q&A.md 模板可被 `code-publish` 技能直接消费。

## 修复记录

无(无 build/run/test 失败)
