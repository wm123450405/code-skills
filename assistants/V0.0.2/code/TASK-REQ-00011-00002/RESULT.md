# RESULT — TASK-REQ-00011-00002

- 任务编码:TASK-REQ-00011-00002
- 标题:[修改] `code-plan/SKILL.md` 增量追加"步骤 0b 设计目标确认"+ 任务粒度调整段 + 模板顶部预留 + 步骤 0a 小注更新
- 类型:修改
- 触发/来源:详细设计
- 关联需求:REQ-00011
- 前置任务:T-001(已完成)
- 开发状态:**已完成**
- 完成时间:2026-06-05 20:05
- 完成人:wangmiao
- 提交哈希:见末尾(`git commit` 输出)

## 1. 改修内容总览

### 涉及文件(2 个,全部为 `**修改**`)

| 文件 | 变更类型 | 行数变化 | 说明 |
| --- | --- | --- | --- |
| `plugins/code-skills/skills/code-plan/SKILL.md` | 修改 | +33 行 | §步骤 0a L118 既有"不含步骤 0b"小注更新(替换 1 句)+ §步骤 0a L128 末尾追加"执行步骤 0b"提示 + 新增"### 步骤 0b — 设计目标确认"完整章节(L130-143,14 行)+ §步骤 10A 末尾追加"#### 按'## 设计目标'小节调整任务粒度"判定表段(L317-323,7 行) |
| `plugins/code-skills/skills/code-plan/templates/plan.md` | 修改 | +3 行 | "## 文档头"模板区段后 + "## 1. 详细设计概述"前插入"## 设计目标"占位 |

### 不涉及文件(本任务范围外)

- `plugins/code-skills/skills/code-plan/SKILL.md` frontmatter(L1-3)字节级保留(INV-1)
- `plugins/code-skills/skills/code-plan/SKILL.md` 既有"步骤 0 ~ 步骤 18A / 步骤 7B ~ 13B"字节级保留(INV-2)
- `plugins/code-skills/skills/code-plan/SKILL.md` 既有"§步骤 10A 任务拆分"4 个子段(L222-300)字节级保留 — 仅在末尾(L300 后)追加新子段"按'## 设计目标'小节调整任务粒度"
- `plugins/code-skills/skills/code-plan/SKILL.md` 既有"末尾兜底提交"步骤 N 字节级保留
- 其他 11 个 `code-*` 技能 SKILL.md 字节级不变(FR-7.AC-7.1)
- `marketplace.json` / `plugin.json` / `assistants/rules/` 13 文件字节级不变(FR-8.AC-8.1 ~ AC-8.4)
- `code-design/SKILL.md` / `code-design/templates/design.md` 在 T-001 实施(本任务**不**涉及)

## 2. 详细改动

### 改动 1:`code-plan/SKILL.md` §步骤 0a L118 既有"不含步骤 0b"小注更新

- **原内容**:`code-plan` **不**含步骤 0b(FR-2 显式仅 `code-require` 专属)。
- **新内容**:`code-plan` 进入"步骤 0b 设计目标确认"(本需求 REQ-00011 新增,FR-2)。
- **理由**:与 T-001 同步;FR-2 实际只针对原"需求澄清"问询;本需求把"步骤 0b"扩展到 `code-design` + `code-plan` 共 2 个技能
- **INV-1 影响**:frontmatter 字节级保留(L1-3 不动)
- **INV-2 影响**:既有"步骤 0-N"流程仅 L118 这 1 句小注变化,其余 L117 / L119-128 字节级保留

### 改动 2:`code-plan/SKILL.md` §步骤 0a L128 末尾追加"执行步骤 0b"提示

- **原内容**(L128 末尾):"进入既有'步骤 0 — 版本上下文检测'"
- **新内容**:"**执行'步骤 0b 设计目标确认'**(见下),成功后再进入既有'步骤 0 — 版本上下文检测'"
- **理由**:与 T-001 §步骤 0a 末尾的提示对称;让读者在 §步骤 0a 末尾就看到"步骤 0b 在此之后"
- **INV-2 影响**:仅 L128 末尾 1 行追加,其余字节级保留

### 改动 3:`code-plan/SKILL.md` 新增"### 步骤 0b — 设计目标确认"章节(L130-143)

- **位置**:§步骤 0a 之后(L129),§步骤 0 之前(L145)
- **完整内容**:
  ```markdown
  ### 步骤 0b — 设计目标确认(本需求 REQ-00011 新增,FR-2 / FR-3)
  1. 读 `design/<REQ>/RESULT.md` 的"## 设计目标"小节(算法 3 / `readDesignGoalsFromDesign`):
     - **存在** → 屏显"沿用 design 的设计目标:<摘要>" + 复制到 `plan/.../RESULT.md` 顶部"## 设计目标"小节(`writeDesignGoalsSection(..., "code-plan")` / NFR-3 幂等覆盖)
     - **不存在**(E-1 / E-5)→ 屏显"⚠ 未检测到 design/<REQ>/RESULT.md" + 触发 `AskUserQuestion` 1-5 问(同 `code-design` §步骤 0b 步骤 1 的自适应策略,简化版可只问整体)+ 写 `plan/.../RESULT.md`(**不**写 `design/.../RESULT.md`,FR-3.AC-3.3 职责分离)
  2. 据"## 设计目标"小节,**调整**任务拆分粒度(FR-4 / 算法 4 / 详见 §步骤 10A 末尾"按'## 设计目标'小节调整任务粒度"段)
  3. 屏显(沿用/退化分支):
     ```
     === code-plan 设计目标确认 ===
     [沿用 design 的设计目标 / 用户回答摘要]
     已沿用 / 已回写至 plan/<REQ>/RESULT.md "## 设计目标" 小节
     ```
  4. **不**修改 frontmatter(INV-1)
  5. **不**修改"步骤 0"及之后的原有内容(INV-2)
  6. 完成后进入既有"步骤 0 — 版本上下文检测"
  ```
- **对应设计章节**:RESULT.md §4 模块 2,§5 算法 2 + 算法 3 + 算法 4
- **依据规范**:FR-2 / FR-3(沿用或退化)+ FR-5(回写小节)+ NFR-3 幂等 + INV-1 / INV-2 / INV-3 / INV-4 / INV-5

### 改动 4:`code-plan/SKILL.md` §步骤 10A 末尾追加"#### 按'## 设计目标'小节调整任务粒度"判定表段(L317-323)

- **位置**:§步骤 10A 既有"任务触发/来源字段"子段(L291-300)末尾,§步骤 11A(L319 后)之前
- **完整内容**:
  ```markdown
  #### 按"## 设计目标"小节调整任务粒度(FR-4,本需求 REQ-00011 新增)
  读取本需求步骤 0b 写入的"## 设计目标"小节,据此调整任务总览(算法 4 / `adjustTaskGranularityByGoals`):

  | 整体设计目标 | 扩展性优先级 | 任务粒度调整 |
  | --- | --- | --- |
  | `--minimal` | * | 合并同类任务,粒度粗化(轻度合并启发式:P-D3 锁定 — 只合并明显同类的,如"修改文件 A" + "修改文件 A 同字段" → 合并) |
  | `--balanced` | 高/中/低 | 默认粒度(无调整) |
  | `--extensible` | * | 加"扩展架构设计" / "设计模式使用"等扩展性任务(至少 1 个) |
  | `--extensible` | 高 | 至少 3 个扩展性相关任务 |

  > **不**强制加任务的维度:功能性 / 健壮性 / 可维护性(AC-4.4 — 作为参考,不触发任务拆分变化)
  > **位置**:本段在 §步骤 10A 末尾追加,**不**替换既有"任务类型 / 任务编号 / 双状态 / 触发/来源"等 4 个子段(锚点 B 字节级保留)
  ```
- **对应设计章节**:RESULT.md §4 模块 2,§5 算法 4
- **依据规范**:FR-4 强约束 + AC-4.4

### 改动 5:`code-plan/templates/plan.md` 顶部"## 设计目标"占位(L25-27)

- **位置**:"## 文档头"模板区段(L11-24,代码块)后 + "## 1. 详细设计概述"(L28)前
- **完整内容**:
  ```markdown
  ## 设计目标
  <!-- 本节由 code-design / code-plan 步骤 0b 自动生成(写入或沿用),记录用户确认的设计目标;如需手动编辑,保留该注释以便步骤 0b 识别 -->
  ```
- **对应设计章节**:RESULT.md §4 模块 3,§3 D-8
- **依据规范**:`module-conventions.md §规则 1` 资源放 `templates/` 子目录 + INV-3 强约束(顶部"## 设计目标"小节位置)

## 3. 关键决策与权衡

### 决策 1:§步骤 10A 末尾追加"按设计目标调整"段(不替换 4 个既有子段)
- **理由**:REQ-00017 拆任务约束 + 详细设计 P-D2 锁定 — 既有"任务类型 / 任务编号 / 双状态 / 触发/来源"4 个子段必须字节级保留
- **替代方案**:把"按设计目标调整"段插入到 §步骤 10A 中间 — 拒绝理由:会破坏既有 4 个子段的字节级对齐,违反 INV-2

### 决策 2:沿用与退化分支合并到 1 个 §步骤 0b 章节(2 个子点)
- **理由**:沿用(D-4 锁定)+ 退化(FR-3 强约束)在屏显模板上对称(都有"已回写/已沿用"+ 摘要)
- **替代方案**:拆为 2 个 §步骤 0b 子章节 — 拒绝理由:会让 SKILL.md 章节变多,但实际行为是 1 个步骤内分支处理

### 决策 3:任务粒度调整段用 `#### 4 级标题`
- **理由**:与 §步骤 10A 既有"核心原则 / 架构任务 / 生效范围 / 拆任务约束"4 个 4 级子段对齐
- **替代方案**:用 `##### 5 级标题` — 拒绝理由:会让读者在 §步骤 10A 内看到 5 级 + 4 级混排,可读性下降

### 决策 4:`AskUserQuestion` 退化版"简化版可只问整体"
- **理由**:FR-3 + Q-3 锁定;当 `design/.../RESULT.md` 缺失时,用户更关注"整体设计目标",4 维度可由 AI 默认 = 中
- **替代方案**:退化时也强制 5 问 — 拒绝理由:用户体验差;FR-3 强约束"简化版可只问整体"

## 4. 偏离设计/规范的地方

**无**。本任务 100% 沿用概要设计 + 详细设计的指定内容(详情见 §2 详细改动)。

## 5. 验证结果

### 5.1 静态自检

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| INV-1 frontmatter 字节级保留 | ✅ | `code-plan/SKILL.md` L1-3 字节级未动 |
| INV-2 既有"步骤 0-N"流程不变 | ✅ | L117-128 / L145-... 字节级未动(仅 L118 小注 1 句更新 + L128 末尾 1 行追加) |
| INV-3 顶部"## 设计目标"小节位置 | ✅ | `plan.md` L25 在"## 文档头"区段后 + "## 1. 详细设计概述"前 |
| INV-4 沿用或退化 | ✅ | 步骤 0b 步骤 1 显式列出"存在 / 不存在"两个分支 |
| INV-5 幂等 | ✅ | 步骤 0b 步骤 1 明确调 `writeDesignGoalsSection`(算法 2 覆盖前次内容) |
| INV-6 扩展性高 → 加扩展性任务 | ✅ | §步骤 10A 末尾"按设计目标调整"段判定表"--extensible" 行 |
| INV-7 `code-auto` 0 冲突 | ✅ | `code-plan` 步骤 0b 不在 `code-auto` 升级范围;`code-auto` 沿用"总选推荐项" |
| INV-8 不触发 `dashboard-conventions §规则 1` | ✅ | `code-plan/SKILL.md` 不写看板;末尾兜底照常同步"任务清单" + "里程碑" + "变更记录" |
| NFR-5 与 `code-auto` 0 冲突 | ✅ | 同 INV-7 |
| FR-7.AC-7.1 不改 8 其他技能 | ✅ | 本任务**只**改 `code-plan/SKILL.md` + `code-plan/templates/plan.md` 2 个文件;`code-design` 在 T-001 改 |
| FR-8.AC-8.1 ~ AC-8.4 不改 marketplace / plugin / 规范 / README | ✅ | 全部字节级未动 |
| §步骤 10A 既有 4 个子段(L222-300)字节级保留 | ✅ | 仅在 L300 后追加新子段"按'## 设计目标'小节调整任务粒度" |

### 5.2 编译/启动验证

**N/A**(本项目无构建/测试框架,纯 Markdown 技能;`code-unit` 守卫判定"不可测";任务测试状态=`不适用`)

### 5.3 端到端验证(下一步由 `code-review` + `code-auto` 完成)

- 后续由 `code-review` 评审本任务 + T-001 的实施
- 后续由 `code-auto` 调 `code-plan REQ-...` 时验证步骤 0b 触发(读 `design/.../RESULT.md` → 沿用)+ 任务粒度调整(整体=--balanced + 扩展性=中 → 默认粒度,无变化)

## 6. 已知问题/未完成项

- **F-1**:`code-design` 步骤 0b 是否对所有需求都触发 5 问?(沿用,与 T-001 一致)
- **F-2**:`code-plan` 步骤 0b 退化时,`AskUserQuestion` 1 问还是 5 问?(沿用,与 T-001 一致)
- **F-3**:`code-review` 是否应感知"## 设计目标"小节?(沿用,留作后续版本)

## 7. 关联任务与提交

### 提交

- 任务编码:`TASK-REQ-00011-00002`
- 提交哈希:见末尾(`git commit` 输出,`code-it` 末尾兜底步骤自动生成)
- commit message 预览:
  ```
  chore(code-it): TASK-REQ-00011-00002 code-plan 增量追加步骤 0b
  ```

### 关联

- 上游:`plan/REQ-00011/PLAN.md` §TASK-REQ-00011-00002
- 前置:T-001(`code-design/SKILL.md` 增量追加,已完成)
- 下游:**无** — 本需求全部 2 任务已完成
- 横向:T-001(`code-design/SKILL.md` 增量追加)
