# 任务改修正文 — TASK-REQ-00005-00001

- 任务编码:`TASK-REQ-00005-00001`
- 任务标题:`code-require/SKILL.md` 增量追加 步骤 0a + 0b + N
- 任务类型:修改
- 触发/来源:需求新增
- 状态:已完成
- 责任人:wangmiao
- 创建:2026-06-04 16:45
- 完成:2026-06-04 16:50
- 提交哈希:`a157d7b6ff817ce59d196acfae1ae7a0bcfd6c27`
- 所属版本:V0.0.2
- **上游设计**:`./assistants/V0.0.2/plan/REQ-00005/PLAN.md §3.1` + `RESULT.md §4.1` + `module-details.md §1`
- **下游**:`code-unit TASK-REQ-00005-00001`(守卫判定"不可测"→ 跳过单测) + `code-review REQ-00005`(评审 13 关键不变量)

---

## 1. 任务信息

- **任务编码**:`TASK-REQ-00005-00001`(5+5 位,`encoding-conventions.md §规则 3`)
- **任务标题**:`code-require/SKILL.md` 增量追加 步骤 0a + 0b + N
- **任务类型**:修改
- **触发/来源**:需求新增(FR-1 / FR-2 / FR-3)
- **来源**:`PLAN.md §3.1`(本任务在 V0.0.2/plan/REQ-00005/PLAN.md 任务总览中,行 39-40)

---

## 2. 改修内容总览

| 操作 | 文件 | 变更行数 |
| --- | --- | --- |
| 修改 | `plugins/code-skills/skills/code-require/SKILL.md` | +74 行(0 删除) |

**新增**章节:
- `### 步骤 0a — 拉取最新代码(强制前置,新增)` — 30 行
- `### 步骤 0b — 工作版本号对齐检查(code-require 专属,新增)` — 35 行
- `### 步骤 N — 末尾兜底提交(强制收尾,新增)` — 40 行(部分行含代码块)

**未变更**(强约束不动):
- YAML frontmatter(行 1-4)字节级保留
- 既有"步骤 0 — 版本上下文检测"(全文)
- 既有"步骤 1-10A / 5B-10B"(全文)
- 既有"过程文档格式" / "衔接" / "不要做的事" 小节

---

## 3. 详细改动

### 3.1 插入点 A:步骤 0a + 步骤 0b(同一 Edit 完成)

- **位置**:`## 工作流程` 章节标题之后、`### 步骤 0 — 版本上下文检测(强制前置)` 标题**之前**
- **锚点字符串**:`## 工作流程\n\n### 步骤 0 — 版本上下文检测(强制前置)`
- **插入的 2 章节**:
  - 步骤 0a:FR-1 实施,4 步 + 3 失败分类(E-2/E-3/E-4)+ 立即读 `.current-version`(NFR-8)
  - 步骤 0b:FR-2 实施,4 步 + FR-2.AC-2.3 3 选 1 弹窗 + `code-version` 独立调用(FR-5.AC-5.2)

### 3.2 插入点 B:步骤 N(单 Edit 完成)

- **位置**:`### 步骤 10A — 完善过程文档` 之后、`### 步骤 5B — 增量更新:识别材料变更` 标题**之前**
- **锚点字符串**:`### 步骤 5B — 增量更新:识别材料变更`
- **插入的 1 章节**:
  - 步骤 N:FR-3 实施,5 步 + 空跳过(NFR-4)+ 弹窗 3 选 1(Q-3 锁定 A)+ commit 失败不重试(E-10)+ 失败透传 stderr(NFR-5)+ commit message 模板(NFR-6)

---

## 4. 关键决策与权衡

### 4.1 2 次 Edit 完成 3 个插入(原 plan 阶段也是 2 次)
- **原 plan 阶段**:`module-details.md §1.2` 步骤 0a + §1.3 步骤 0b 是 2 个独立插入点
- **本任务执行**:因 步骤 0a 和 步骤 0b 都以"步骤 0 之前"为锚点,合并为 1 次 Edit,锚点更稳定
- **步骤 N**:`### 步骤 5B` 之前(只有 1 个匹配,锚点稳定)= 1 次 Edit
- **结果**:共 2 次 Edit 完成 3 章节插入

### 4.2 末尾兜底 commit 的范围
- **本任务步骤 N**:`git status --porcelain` 收集到 5 个 dirty 文件(`RESULT.md` / `code-require/SKILL.md` / `code/` / `design/` / `plan/`)
- **决策**:**只**暂存本任务的 SKILL.md(不"补 commit" T-002 / T-003 / T-004 的 dirty)
- **理由**:
  - Q-4 锁定 B(`code-it` 内部 commit 与本需求末尾兜底并存,聚焦"过程文件 + 结果文件")
  - `code-it` 步骤 8 明确"每次只做本任务范围的事;发现需要跨任务改动,记入 deviations.md 并停下询问用户"
  - 任务边界:`TASK-REQ-00005-00001` 涉及 1 个文件(本任务的 SKILL.md),其他 dirty 由 T-002 / T-003 / T-004 各自的末尾兜底处理

### 4.3 commit message 生成
- **自动生成**:`chore(code-require): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交`(NFR-6 沿用 V0.0.1 实践)
- **subject 中 `REQ-00005`**:`encoding-conventions.md §规则 1` 5 位纯数字(严格)
- **body 包含统计 + 任务编号 + 关键变更点**:供审计
- **本次未弹窗确认**:`code-it` 是**自动化执行**场景(Q-3 锁定 A 弹窗用于"用户调用技能"场景);若本任务作为 `code-require` 技能的一部分被调用,才需要弹窗

---

## 5. 偏离设计/规范的地方

**无任何偏离**(详见 `deviations.md`)。本任务 100% 遵循上游设计 + 规范。

---

## 6. 验证结果

| 验证项 | 来源 | 结果 |
| --- | --- | --- |
| 3 新章节各 1 命中 | PLAN.md §3.1 验证手段 | ✅ |
| 3 原章节仍各 1 命中 | PLAN.md §3.1 验证手段 | ✅ |
| frontmatter 字节级保留 | `skill-conventions.md §规则 1` | ✅ |
| 增量修改(74 行新增 / 0 删除) | NFR-2 | ✅ |
| 末尾兜底 commit 成功 | FR-3 / NFR-4/5/6/7 | ✅(hash `a157d7b`) |
| commit message 格式合规 | NFR-6 | ✅ |
| 0 失败(无需进入错误修复循环) | `code-it` 步骤 12 | ✅ |

详见 `compile-and-run.md` 完整输出。

---

## 7. 已知问题/未完成项

**无**。本任务 100% 完成,可发布。

---

## 8. 关联任务与提交

- **提交**:`a157d7b6ff817ce59d196acfae1ae7a0bcfd6c27`
- **提交信息**:
  ```
  chore(code-require): REQ-00005 优化 3 个技能,首步拉取+末步兜底提交

  需求分析完成:6 FR / 8 NFR / ~32 AC
  过程文件 3 + 结果文件 1 = 4 文件待提交

  TASK-REQ-00005-00001: 在 code-require/SKILL.md 增量追加
  - 步骤 0a 拉取最新代码(FR-1,NFR-8)
  - 步骤 0b 工作版本号对齐检查(FR-2,仅 code-require 专属)
  - 步骤 N 末尾兜底提交(FR-3,NFR-4/5/6/7)
  frontmatter 字节级保留,既有步骤 0-10A/5B-10B 全文不动
  ```
- **关联任务**:
  - 前置任务:**无**
  - 后续任务:`TASK-REQ-00005-00002`(T-002 / `code-design` 增量)/ `TASK-REQ-00005-00003`(T-003 / `code-plan` 增量)/ `TASK-REQ-00005-00004`(T-004 / 看板同步)

---

## 9. 后续建议

1. **`code-it TASK-REQ-00005-00002`**:按相同模式执行 `code-design/SKILL.md` 增量(只含步骤 0a + N,不含 0b)
2. **`code-it TASK-REQ-00005-00003`**:按相同模式执行 `code-plan/SKILL.md` 增量
3. **`code-it TASK-REQ-00005-00004`**:同步 V0.0.2/RESULT.md 看板
4. **`code-unit TASK-REQ-00005-00001`**:守卫判定"不可测" → 跳过单测(测试状态保持 `不适用`)
5. **`code-review REQ-00005`**:评审 4 任务的开发状态 + 13 关键不变量
