# 8 项 INV 自检报告 — TASK-REQ-00018-00002
版本:V0.0.2
时间:2026-06-06 13:30

## 总览

| INV | 维度 | 自检方法 | 结果 |
| --- | --- | --- | --- |
| **INV-1** | frontmatter 字节级保留 | `head -4 SKILL.md` 与 commit 91ac88d 比对 | ✅ 字节级一致 |
| **INV-2** | 既有"## 工作流程" 小节不改 | `diff` L74-164 旧版/新版 | ✅ 字节级一致 |
| **INV-3** | 既有"## 看板字段约定" 小节不改 | `diff` L335-360 新版(L167-192 旧版) | ✅ 字节级一致 |
| **INV-4** | 7 子节结构完整(7.1~7.7) | `grep -c "^### 7\."` = 7 | ✅ 7 子节全有 |
| **INV-5** | 6 类描述文件全列出 | `grep -c` 各文件名 | ✅ 6 类全有(均 ≥ 4 命中) |
| **INV-6** | 5 类屏幕输出契约全有 | `grep -c` 各模式 | ✅ 5 类全有(均 ≥ 2 命中) |
| **INV-7** | 8 边界 E-1~E-8 全列出 | `grep -E "\| \*\*E-[1-8]\*\*"` | ✅ 8 边界全有 |
| **INV-8** | 0 触发 `dashboard-conventions §规则 1` 3 处同步 | 看板 0 字段扩展 | ✅ 0 触发 |

**总览**:**8/8 INV 100% 通过自检**。

## 详细自检

### INV-1:frontmatter 字节级保留

- 旧版 L1-3(commit 91ac88d):
  ```
  ---
  name: code-version
  description: 版本管理。要求用户提供一个"版本号"...
  ```
- 新版 L1-3:
  ```
  ---
  name: code-version
  description: 版本管理。要求用户提供一个"版本号"...
  ```
- 结论:**✅ 字节级一致**

### INV-2:既有"## 工作流程" 小节不改

- 旧版(commit 91ac88d)L74-164 vs 新版 L74-164
- `diff` 输出为空
- 结论:**✅ 字节级一致**

### INV-3:既有"## 看板字段约定" 小节不改

- 旧版(commit 91ac88d)L167-192 vs 新版 L335-360
- `diff` 输出为空
- 结论:**✅ 字节级一致**
- 备注:行号后移 +165(因前面插入了"## 步骤 7"小节),但内容字节级一致

### INV-4:7 子节结构完整

- `grep -c "^### 7\." SKILL.md` = **7**
- 7.1 目标 / 7.2 触发条件 / 7.3 算法 / 7.4 通过条件 / 7.5 屏幕输出契约 / 7.6 边界与异常 / 7.7 性能 — **全有**
- 结论:**✅ 7 子节齐全**

### INV-5:6 类描述文件全列出

| 描述文件 | 命中数 | 通过 |
| --- | --- | --- |
| `package.json` | 5 | ✅ |
| `pom.xml` | 4 | ✅ |
| `manifest.json` | 4 | ✅ |
| `Cargo.toml` | 4 | ✅ |
| `pyproject.toml` | 4 | ✅ |
| `go.mod` | 8 | ✅ |

- 结论:**✅ 6 类全列出**

### INV-6:5 类屏幕输出契约全有

| 场景 | 模式 | 命中数 | 通过 |
| --- | --- | --- | --- |
| 成功 | `✓ CWD 描述文件同步` | 2 | ✅ |
| 0 命中 | `⚠ CWD 下未检测到` | 3 | ✅ |
| 解析失败 | `⚠ <filename> 格式不可解析` | 2 | ✅ |
| 缺版本号字段 | `⚠ <filename> 未找到版本号字段` | 2 | ✅ |
| `go.mod` 命中 | `⚠ go.mod 无版本号字段` | 2 | ✅ |

- 结论:**✅ 5 类契约全有**

### INV-7:8 边界 E-1~E-8 全列出

```
| **E-1** | CWD 下有多个描述文件(monorepo)
| **E-2** | CWD 下找不到任何描述文件
| **E-3** | 描述文件存在但格式不可解析
| **E-4** | 描述文件存在但**无**"版本号"字段
| **E-5** | `--skip-cwd-sync` CLI 参数(本需求不实现,留作 v2)
| **E-6** | `go.mod` 命中(Go 用 git tag)
| **E-7** | 描述文件无写权限(只读)
| **E-8** | 描述文件被另一个进程修改(并发)
```

- 结论:**✅ 8 边界全列出**

### INV-8:0 触发 `dashboard-conventions §规则 1` 3 处同步

- 看板 0 字段扩展(只追加行,不修改既有字段)
- `dashboard-conventions §规则 1` 强约束:看板字段约定扩展需三方同步(`version-RESULT.md` 模板 + `CLAUDE.md` + `dashboard-conventions.md`)
- 本需求**不**扩展看板字段,**不**触发三同步
- 结论:**✅ 0 触发**

## 自检通过项

- ✅ INV-1 frontmatter 字节级保留
- ✅ INV-2 既有"## 工作流程" 小节不改
- ✅ INV-3 既有"## 看板字段约定" 小节不改
- ✅ INV-4 7 子节结构完整
- ✅ INV-5 6 类描述文件全列出
- ✅ INV-6 5 类屏幕输出契约全有
- ✅ INV-7 8 边界 E-1~E-8 全列出
- ✅ INV-8 0 触发 dashboard 3 处同步

## 自检失败项

无。

## 结论

**8/8 INV 100% 通过自检**。本任务可标记为"已完成"。

## 关联

- 上游:T-001(`[修改] code-version/SKILL.md 增量追加`)
- 看板:M1-REQ-00018-1:本需求可发布 — T-002 完成后同步为"已完成"
