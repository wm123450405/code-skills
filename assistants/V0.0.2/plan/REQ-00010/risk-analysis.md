# 风险分析 — REQ-00010

更新时间:2026-06-06 12:10
版本:V0.0.2
需求编码:REQ-00010

## 异常处理

| 异常路径 | 检测手段 | 处理策略 | 监控指标 | 对应任务 |
| --- | --- | --- | --- | --- |
| 无 `.current-version` | `code-it` 步骤 0 入口 | 由原 `code-it` 步骤 0 处理(守卫不触达) | — | T-001 INV-2 |
| `PLAN.md` 不存在 | `fileExists(planPath)` | 守卫**通过**(NFR-6 退化) | stderr "⚠ PLAN.md 不存在" | T-001 E-3 |
| `PLAN.md` 任务总览区段格式错乱 | 表格行解析失败 | 守卫**通过**(软失败) | stderr "⚠ PLAN.md 解析失败" | T-001 E-5 |
| 任务不在 `PLAN.md` 任务总览中 | `findIndex` 返回 -1 | 由原 `code-it` 步骤 2 报错 | — | T-001 E-4 |
| 任务属于缺陷分支(`TASK-BUG-...`) | 任务编号前缀判定 | 守卫不触达,走 §"缺陷分支" 17-25 | — | T-001 E-9 |
| 任务编码格式不匹配 | 正则失败 | 由原 `code-it` 步骤 1 报错 | — | T-001 E-8 |
| 多个未完成前置 | `unfinished.length > 1` | 打印所有未完成前置,推荐命令**只**指向第一个 | 屏幕输出含 ≥ 2 个 ✗ 行 | T-001 E-7 |
| 守卫不通过 | `unfinished.length > 0` | 打印中止报告 + `exit 1` | 退出码 = 1 | T-001 E-2 |

## 安全边界

- **鉴权要求**:**无**(技能为本地执行,无远程调用)
- **输入校验**:任务编号正则 `^TASK-(REQ|BUG)-\d{5}-\d{5}$` / `^(REQ|BUG)-\d{5}-\d{5}$`(沿用 `encoding-conventions §规则 3`)
- **敏感数据处理**:**无**(本计划不处理敏感数据)
- **防注入**:**无**(无 SQL / 命令注入面)
- **审计**:守卫不通过时在 `RESULT.md` 变更记录追加(沿用 `code-auto` 既有)+ 中止报告打印到屏幕

## 性能与资源

- **关键路径耗时目标**:**< 1 秒**(NFR-8 强约束)
  - 单 `PLAN.md` 解析:Read 一次(典型 5-50 KB,<< 1 秒)
  - 任务总览表格行扫描:O(n),n = 任务数(典型 1-20)
  - 任务比较:O(n)
  - 总耗时:**<< 1 秒**(实际 0.1 秒级)
- **并发上限**:**1**(单次执行,无并发)
- **资源限制**:**无**(无内存/连接限制)
- **缓存策略**:**无**(无重复读需求,每次守卫独立读)
- **批量/异步**:**不适用**(单次执行)
- **降级策略**:`PLAN.md` 缺失 / 解析失败 → 守卫**通过**(不阻断 `code-it`)

## 回退策略

- **触发条件**:T-001 实施后发现守卫误判 / 阻塞合法调用
- **步骤**:
  1. `git revert <commit-hash>` 回退 T-001 的 1 个 commit
  2. 验证:`code-it` 恢复无守卫原行为(由原步骤 7 检查"显式前置任务"字段,若有)
- **验证**:
  - 调 `code-it TASK-...` 任意任务 → 不再触发守卫
  - 看板"任务清单"2 行仍存在(任务编号 + 状态)
  - 其他 9 个 `code-*` 技能 + `marketplace.json` / `plugin.json` / `assistants/rules/` 均不受影响
- **影响面**:**仅 1 个 SKILL.md 增量回退**,无连锁影响

## 测试要点(本计划测试状态 = `不适用`,纯文档型)

> **不适用理由**:本需求 = `code-it/SKILL.md` 增量追加,纯文档,无可测代码载体。
> 仓库**不**含可测载体(`code-unit` Q-1 守卫判定"不可测" → 测试状态 = `不适用`)。
> 沿用 V0.0.2 既有 11 个 `code-*` 实践。

- **单元测试**:**不适用**(无代码)
- **集成测试**:**不适用**(无代码)
- **端到端测试**:**不适用**(无代码)
- **性能/压力测试**:**不适用**(无代码)

### 替代验证手段(纯文档型,本计划实施期使用)

- **静态自检(INV-1~13)**:T-002 收尾时 13/13 通过
- **关键字 token 自检**:`grep -c` 各 token 命中数 ≥ 1
- **行数偏差**:`wc -l code-it/SKILL.md` 应在既有 752 ± 20% 范围(预估 +100 行)
- **既有章节字节级保留**:`git diff code-it/SKILL.md` 应只有 + 净增,无既有行修改

## 8 项关键决策风险分析(沿用概要设计 §3.1)

| 决策 | 风险 | 缓解 |
| --- | --- | --- |
| 1. 步骤 0a 插入 | 与 `code-unit` 既有"步骤 0a"模式重复 → 命名冲突 | 命名相同但**职责正交**:`code-unit` 看"项目可测性"(7 项文件检查),`code-it` 看"前置任务"(PLAN.md 行序) |
| 2. PLAN.md 文件行序判定 | 文件被人为编辑打乱顺序 → 误判 | 守卫**通过**退化(E-5);**不**新增"前置任务"字段,无规范违反 |
| 3. 开发状态判定 | 状态字面值不归一化 | 沿用 `dashboard-conventions §规则 1` 既有枚举(6 个) |
| 4. 退出码 1 | 与其他错误码混淆 | 屏幕输出前缀"⛔ code-it 中止(存在未完成的前置任务)"足够区分 |
| 5. PLAN.md 缺失退化 | 错过守卫窗口 | 守卫**通过**而非"不通过",不阻断 `code-it`;原行为由 `code-it` 步骤 2 报"任务不存在" |
| 6. 与"步骤 7 显式字段"并存 | 两种机制可能矛盾 | 步骤 7 是"显式字段"判定(可选),步骤 0a 是"行序"判定(必跑);矛盾时步骤 0a 先判定 |
| 7. 不引入新依赖 | — | — |
| 8. 不修改 frontmatter | frontmatter 字节级破坏 → 触发 `skill-conventions §规则 1` 违反 | T-001 锚点 A 严格在 frontmatter **之后**插入(沿用 REQ-00013 模式) |
| 9. 不引入新参数 | 用户有"跳过守卫"需求 | 留作 v2 follow-up;NFR-7 锁定 |
