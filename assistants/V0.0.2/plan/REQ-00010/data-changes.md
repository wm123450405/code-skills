# 数据结构完整变更 — REQ-00010

更新时间:2026-06-06 12:10
版本:V0.0.2
需求编码:REQ-00010

## 总览

**无新增 / 修改实体**(NFR-3 零规范变更 + FR-5.AC-5.2/5.3/5.4)。

## 新增实体
**无**

## 修改实体
**无**

## 数据迁移
**不适用**

## 复用既有数据结构(本计划新增守卫所消费)

### `PLAN.md` 任务总览表格(既有,本计划**只读**消费)

| 列 | 用途 | 本计划使用 |
| --- | --- | --- |
| 任务编号(列 1) | 任务标识 | ✓ 用于定位当前任务 + 提取 reqNum |
| 需求(列 2) | 父级需求 | — |
| 类型(列 3) | 任务类型 | — |
| 触发/来源(列 4) | 任务来源 | — |
| 标题(列 5) | 任务标题 | ✓ 用于中止报告(沿用 REQ-00013 工具函数) |
| **开发状态**(列 6) | 任务开发状态 | ✓ **本计划核心判定字段** |
| 测试状态(列 7) | 任务测试状态 | ✗ 不使用(Q-2 锁定) |
| 涉及文件(列 8) | 涉及文件 | — |
| 完成时间(列 9) | 完成时间 | — |
| 提交哈希(列 10) | 提交哈希 | — |
| 关联任务(列 11) | 关联任务 | ✗ 不使用(Q-1 锁定 — 零字段变更) |

### 内部数据结构(守卫实现,本计划伪代码层)

```ts
interface TaskOverview {
  num: string       // 任务编号,如 "TASK-REQ-00001-00001"
  title: string     // 任务标题(从列 5 提取)
  devStatus: string // 开发状态(从列 6 提取)
}
```

- **存储**:无(纯函数式守卫,解析自 `PLAN.md` 后立即消费)
- **生命周期**:`code-it` 步骤 0a 入口 → `parsePlanOverview` 解析 → 守卫判定 → 丢弃
- **不**持久化(不留中间产物)

### 沿用既有数据结构(本计划**只读**消费)

- `code-it/SKILL.md` §"标题解析(REQ-00013 新增)" 小节:
  - `truncateTitle(title, maxLen=30): string`
  - `formatTaskTitle(taskNum, title): string`
  - `formatReqTitle(reqNum, title): string`
  - `parsePlanTaskTitle(planPath, taskNum): string`

## 字段约束(本计划新增守卫解析 `PLAN.md` 时的鲁棒性)

| 约束 | 实施 |
| --- | --- |
| 表格行最少列数 | ≥ 6(任务编号 + 标题 + 开发状态 3 列必填) |
| 任务编号正则 | `^TASK-(REQ\|BUG)-\d{5}-\d{5}$`(新格式)+ `^(REQ\|BUG)-\d{5}-\d{5}$`(旧格式,沿用 `code-dashboard` NFR-3 双格式兼容) |
| 开发状态枚举 | `待开始` / `进行中` / `已完成` / `已取消` / `阻塞` / `待重新评估`(沿用 `dashboard-conventions §规则 1` 既有枚举) |
| 标题字符数 | ≤ 30(沿用 NFR-3 / REQ-00013) |
| 缺失字段 | 视为"无法判定",守卫通过(退化) |
