# 接口详细规格 — REQ-00010

更新时间:2026-06-06 12:10
版本:V0.0.2
需求编码:REQ-00010

## 接口总览

**无新增对外接口**。

本需求**仅**修改 `code-it` 技能行为语义,**不**引入:
- 新 CLI 参数
- 新 API 端点
- 新文件 / 新函数(只追加"## 步骤 0a"小节,内含既有工具函数)
- 新事件 / 钩子

## 既有接口(本计划不修改)

| 接口名 | 形式 | 状态 | 备注 |
| --- | --- | --- | --- |
| `code-it TASK-...` | Claude Code 技能调用 | **不修改**(输入签名不变) | 本需求修改其行为语义(增加守卫),**不**改输入输出形式 |
| `code-it BUG-...` | Claude Code 技能调用 | **不修改**(守卫不触达) | 缺陷分支走 §"缺陷分支" 17-25 流程 |

## 屏幕输出契约(对外契约,本计划新增)

### 输出 1:守卫通过(无前置)
```
✓ code-it 前置任务守卫通过
  任务:TASK-REQ-00001-001 · <任务标题>(无前置任务)
```

### 输出 2:守卫通过(全部前置已完成)
```
✓ code-it 前置任务守卫通过
  任务:TASK-REQ-00005-002 · <任务标题>(前置 1 个,全部已完成)
```

### 输出 3:守卫不通过
```
⛔ code-it 中止(存在未完成的前置任务)

正在处理: REQ-00005 · <需求标题>(任务 TASK-REQ-00005-003 · <任务标题>)

前置任务状态:
  ✓ TASK-REQ-00005-001 · <任务标题>(开发状态=已完成)
  ✗ TASK-REQ-00005-002 · <任务标题>(开发状态=待开始)← 未完成
  ✗ TASK-REQ-00005-003 · <任务标题>(当前任务)

推荐执行 /code-it TASK-REQ-00005-002 · <任务标题> 完成后,再执行 /code-it TASK-REQ-00005-003 · <任务标题>
```

### 输出 4:PLAN.md 缺失(退化)
```
⚠ code-it 前置任务守卫:PLAN.md 不存在,守卫通过(退化)
```

## 错误码契约

| 场景 | 退出码 | 理由 |
| --- | --- | --- |
| 守卫通过 | 0 | 走原 `code-it` 流程 |
| 守卫不通过 | **1** | `code-auto` 据此中断(FR-2.AC-2.4 + NFR-4) |
| PLAN.md 缺失(退化) | 0 | 守卫通过,走原 `code-it` 流程 |
| 任务编码不存在(原行为) | 原 `code-it` 退出码(非 0) | 守卫不触达,由原步骤 2 报错 |

**不**引入新退出码(如语义化 17)— 沿用 FR-2.AC-2.4 通用退出码 1。

## 解析锚点(本计划新增,沿用 `dashboard-conventions §规则 1`)

| 解析对象 | 锚点 | 来源 |
| --- | --- | --- |
| 任务总览区段 | `^## 任务总览$` | `code-dashboard` 既有 |
| 表格行 | `^\| .* \|$` | 既有 |
| 任务编号列 | 第 1 列(`^TASK-(REQ\|BUG)-\d{5}-\d{5}$` 双格式) | `encoding-conventions §规则 3` |
| 开发状态列 | 第 6 列(沿用 REQ-00013 `parsePlanTaskTitle` 列号 5) | REQ-00013 沉淀 |
| 标题列 | 第 5 列(沿用 `parsePlanTaskTitle`) | REQ-00013 沉淀 |
