# 设计笔记 — REQ-00008
更新时间:2026-06-05 16:00
版本:V0.0.2
需求编码:REQ-00008
详细设计标题:`/code-review` 整版本模式 — 详细设计与编码计划

---

## 1. 概要设计 → 详细设计的转化

本详细设计在概要设计(15 章节,v1 已完成,hash `9207f8c`)的基础上,把"系统长什么样"细化为"系统怎么写"。核心细化:

| 概要设计章节 | 详细设计对应章节 | 细化要点 |
| --- | --- | --- |
| §1 设计概述 | §1 详细设计概述 | (已对齐) |
| §6 整版本模式状态机 | §5 算法 1-6(伪代码)+ §9 状态机(再 Mermaid 化) | 概要设计是状态机,详细设计**加伪代码**与**确定性顺序** |
| §7 聚合文件 5 章节结构 | §6.1 数据结构 — 字段级表格 | 概要设计是章节级,详细设计**加字段名/示例/去重算法** |
| §8 数据与状态 | §6 数据结构完整变更 | (已对齐) |
| §10.1 SKILL.md 增量追加边界 | §4.1 模块 M-1 + §11.1 SKILL.md 修改字面 | 概要设计是"插入点",详细设计**给完整字面内容** + **锚点行号**(L109 后 / L313 后) |
| §10.2 派生任务字段 | §6.2 派生任务表结构 | (已对齐) |
| §12 边界与异常(E-1 ~ E-9) | §7 异常处理 + §8 风险分析 | 概要设计 9 个边界,详细设计**加伪代码 + 缓解措施** |

**完全无新增模块 / 无新增依赖**;本设计是 1 个 SKILL.md 增量追加(本仓库 11 个技能中既有,本次只是新增"步骤 1.5"和"步骤 2 整版本模式"2 段)。

---

## 2. 关键决策(沿用概要设计 DQ-1 ~ DQ-6 + 本设计新增 PD-1 ~ PD-4)

### 沿用概要设计 6 项 DQ(全部采纳)

| DQ | 概要设计选定 | 详细设计落地 |
| --- | --- | --- |
| DQ-1 | B 增量追加 | §4.1 SKILL.md 增量追加 2 段(锚点 A L109 后 + 锚点 B L313 后) |
| DQ-2 | A 完全复用 + 循环 | §5 算法 2 循环评审 — 内部对模式 1 步骤 4-15 完整复用 |
| DQ-3 | A 版本顶层 | §6.1 REVIEW.md 路径 = `./assistants/<版本号>/REVIEW.md` |
| DQ-4 | A 覆盖 | §6.1 多次执行覆盖(Write 而非 Edit) |
| DQ-5 | B 沿用 `code-plan` 既有规则 | §6.2 派生任务编码由 `code-plan` 生成(本设计不重写) |
| DQ-6 | B 手动 | §5 算法 6 报告末尾不触发 `code-auto` |

### 本设计新增 4 项 PD(关键伪代码 / 字面决策)

| PD | 标题 | 选定 | 影响 |
| --- | --- | --- | --- |
| **PD-1** | 模式选择优先级(无参 vs REQ-NNNNN vs 无效参) | **三态机**:无参 → 整版本 / `^REQ-\d{5}$` → 模式 1 / 其他 → 整版本 + 警告 | §5 算法 1 + 锚点 A 插入字面 |
| **PD-2** | 过滤需求的"已完成"字面解析 | 严格 `==` 字符串匹配 `已完成`,**不**归一化 `已完成(需求分析)` | §5 算法 2 + 沿用 `code-dashboard` 行为 |
| **PD-3** | 聚合 REVIEW.md 的去重算法 | 跨需求按 `(需求编码, 描述前 50 字)` 去重 + 同发现**不**重复追加派生任务 | §5 算法 3 + §5 算法 5 + INV-9 + INV-10 |
| **PD-4** | 整版本模式派生任务追加位置 | 沿用模式 1 既有"任务总览"表最后一行后追加,字段 100% 沿用 | §5 算法 5 + INV-3 |

---

## 3. 待澄清项(本设计 0 新增)

上游 `code-require` 阶段 3 轮 7 项已锁定 + 3 项采纳默认;概要设计 7 项 D-1 ~ D-7 讨论结论已确认;**本详细设计无新增待澄清**。

详细规范遵循记录见 `rule-compliance.md`(本目录)。

---

## 4. 不变量(沿用概要设计 INV-1 ~ INV-10 + 本设计新增 INV-11 ~ INV-13)

### 沿用概要设计 10 项 INV(全部满足)

| INV | 描述 | 验证手段 |
| --- | --- | --- |
| INV-1 | 模式 1 行为完全不变 | 字节级字面保留(锚点 A 之前 L1-110 + 锚点 B 之前 L308-313) |
| INV-2 | 整版本模式 `REVIEW-REPORT.md` 与模式 1 字面 100% 一致 | 模板复用 + 调用方逻辑统一(完全调用模式 1 步骤 12) |
| INV-3 | 派生任务编码沿用 `code-plan` 既有 | PLAN.md 追加行字段 = `code-review` 既有字段 |
| INV-4 | SKILL.md frontmatter 字节级不变 | Edit 工具严格按锚点(不触 L1-3) |
| INV-5 | `marketplace.json` / `plugin.json` / 其他 9 个 SKILL.md 字节级不变 | 本设计**不**写入这些文件 |
| INV-6 | `assistants/rules/` 13 个文件字节级不变 | 本设计**不**写入 `rules/` |
| INV-7 | 聚合文件 `REVIEW.md` 位于版本顶层 | 路径 = `./assistants/<版本号>/REVIEW.md`(NFR-6) |
| INV-8 | 整版本模式多次执行幂等 | Write 而非 Edit + 覆盖策略(NFR-3) |
| INV-9 | 整版本模式发现去重键 = `(需求编码, 描述前 50 字)` | 算法 3 + 算法 5 实现 |
| INV-10 | 派生任务去重键 = `(需求编码, 描述前 50 字)` | 算法 5 实现 |

### 本设计新增 3 项 INV(本阶段细化)

| INV | 描述 | 验证手段 |
| --- | --- | --- |
| **INV-11** | SKILL.md 步骤 1(L106-110)字面字节级不变 | 锚点 A 之前 L106-110 字节级 diff = 0 |
| **INV-12** | SKILL.md 步骤 15(L308-313)字面字节级不变 | 锚点 B 之前 L308-313 字节级 diff = 0 |
| **INV-13** | 整版本模式**不**修改 `code-review` 既有 4 模板(`REVIEW-REPORT.md` / `REVIEW-FIX.md` / `review-checklist.md` / `assistants-layout.md`)| 模板文件字节级 diff = 0 |

### 不变量自检结论

- ✅ 13 项 INV 全部满足(`code-it` 实施 T-001 时验证)
- ✅ 0 偏离
- ✅ 0 冲突
- ✅ 0 授权偏离

---

## 5. 风险与缓解(沿用概要设计 6 类 + 本设计新增 R-7 / R-8)

| 风险 | 严重度 | 缓解 |
| --- | --- | --- |
| **R-1** | 高 | SKILL.md 增量追加错位 | Edit 严格按锚点(锚点 A L109 后 + 锚点 B L313 后);Grep 自检既有 4 个段落(L106-110 / L111-114 / L308-313)字面不变 |
| **R-2** | 中 | 循环评审时某需求失败 | 沿用模式 1 既有 E-2 逻辑(跳过 + 报告 + 继续) |
| **R-3** | 中 | 多次执行产生重复派生任务 | INV-10 去重键 `(需求编码, 描述前 50 字)` |
| **R-4** | 低 | 聚合文件膨胀 | INV-9 跨需求去重 |
| **R-5** | 低 | 派生任务追加位置错乱 | Edit 严格按"任务总览"表最后一行后追加(沿用模式 1 既有锚点) |
| **R-6** | 低 | 看板同步不完整 | 模式 1 既有同步逻辑覆盖所有区段;整版本模式不触发额外区段 |
| **R-7**(本设计新增) | 中 | 锚点 B(L313 后)插入位置偏差,影响 INV-12 | Edit 工具用 L313 `**版本看板同步情况**` 段尾 + 空行 + `---` + 空行 + 新增节作为字面锚点;Grep 自检 L308-313 字面不变 |
| **R-8**(本设计新增) | 低 | 步骤 1.5 + 步骤 2 字面过长,影响 SKILL.md 总行数可读性 | 控制在 60-120 行;若超过 150 行,需要 D-1 决定 B 重新评估"是否需要新增 `REVIEW-ALL.md` 模板" |

### 风险总检

- 6 类概要设计风险 + 2 类本设计新增 = 8 类
- 8/8 全部有缓解措施
- 0 类无缓解
