# 编码计划 — REQ-00008 — `/code-review` 整版本模式(3 任务)

- 需求编码:`REQ-00008`
- 所属版本:`V0.0.2`
- 详细设计:`./assistants/V0.0.2/plan/REQ-00008/RESULT.md`(v1)
- 状态:已对齐(待 code-it 执行)
- **开发完成度**:0 / 3(待开始)
- **测试完成度**:0 / 3(默认 `不适用` — 3 条都是纯文档型;沿用 V0.0.2 既有 11 个 `code-*` 技能实践 + REQ-00009 守卫判定"不可测")
- **真正可发布任务数**:0 / 3(开发=已完成 ∧ 测试∈{已运行-通过, 不适用};待 code-it 推进)
- 创建:2026-06-05
- 最近更新:2026-06-05 16:00
- 当前版本:v1

---

## 1. 计划概述

- **任务总数**:3
- **类型分布**:
  - 修改:1 条(T-001 增量改 `code-review/SKILL.md`)
  - 文档:2 条(T-002 同步 V0.0.2 看板 + T-003 13 项不变量自检 + 收尾)
- **关键里程碑数**:2(M-1 文档就绪 / M-2 本需求可发布)
- **0 架构任务触发**(对照 REQ-00014 触发条件,本需求**不**满足任何一条 — `clarifications.md` P-10 锁定)
- **开发完成度**:0 / 3(待开始)
- **测试完成度**:0 / 3(默认 `不适用` — 3 条都是纯文档型)
- **真正可发布任务数**:0 / 3

---

## 2. 任务总览

**主表,任何变更都必须先更新此表**。

| 任务编号 | 类型 | 触发/来源 | 标题 | 开发状态 | 测试状态 | 涉及文件/模块 | 前置任务 | 估算 | 责任人 | 关联任务 | 对应设计章节 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `TASK-REQ-00008-00001` | 修改 | 需求新增 | [修改] 增量追加 `code-review/SKILL.md`(2 段:步骤 1.5 模式选择 + 步骤 2 整版本模式 + 步骤 3 退化 + 整版本模式附录;锚点 A L109 后 + 锚点 B L313 后;INV-1/4/11/12 字节级保留) | 待开始 | 不适用 | `plugins/code-skills/skills/code-review/SKILL.md`(+60 ~ +120 行) | — | 0.5d | wangmiao | — | RESULT.md §4.1 + §5 算法 1-6 + §7.2-7.4 + §11.1 |
| `TASK-REQ-00008-00002` | 文档 | 需求新增 | [文档] 同步 V0.0.2 看板 5 处(详细设计与任务计划汇总 + 任务清单 3 行 + 里程碑 2 个 + 文档头 + 变更记录) | 待开始 | 不适用 | `assistants/V0.0.2/RESULT.md` | T-001 | 0.2d | wangmiao | — | RESULT.md §11.4 + §16A |
| `TASK-REQ-00008-00003` | 文档 | 需求新增 | [文档] 13 项不变量自检 + 偏差日志 + 收尾 | 待开始 | 不适用 | `code/TASK-REQ-00008-00003/{RESULT,work-log,deviations}.md` | T-001, T-002 | 0.3d | wangmiao | — | RESULT.md §3 + §4.2 + §11.1 + §11.4 |

**统计**:
- 总数:3
- 已完成:0
- 待开始:3
- 真正可发布:0 / 3
- 估算合计:~1.0 天(串行 T-001 → T-002 → T-003)

### 2.1 触发/来源枚举(本计划全部为 `需求新增`)

参考 `templates/task-plan.md` §2.1。本计划 3 条任务全部 `需求新增`(REQ-00008 v1 首次拆分)。

---

## 3. 任务详情

### TASK-REQ-00008-00001:[修改] 增量追加 `code-review/SKILL.md`(2 段)

#### 基础信息
- **类型**:修改
- **触发/来源**:需求新增
- **触发任务**:无(根节点)
- **开发状态**:待开始
- **目标**:在既有 `code-review/SKILL.md` 增量追加 2 段(锚点 A 后 + 锚点 B 后),实现"无参模式 = 整版本评审";frontmatter(L1-3)+ 既有步骤 0-15 字面字节级不变(INV-1/4/11/12)
- **涉及文件/模块**:
  - 修改 `plugins/code-skills/skills/code-review/SKILL.md`
- **前置任务**:无
- **关联任务**:无
- **关键变更**:
  - **锚点 A(插入位置 = L109 上方)**:"步骤 1 收集需求编码"段末尾(`> 错误:上游缺失...`段后) + 空行 + 空行 + 插入 4 段:
    1. `### 步骤 1.5 — 模式选择(本需求新增,整版本模式入口)`(三态机)
    2. `### 步骤 2 整版本模式(本需求新增,完全复用模式 1 步骤 4-15)`(2.1 ~ 2.6)
    3. `### 步骤 3 整版本模式退化报告(本需求新增)`
  - **锚点 B(插入位置 = L313 后)**:步骤 15 末尾(`**版本看板同步情况**`段后) + 空行 + `---` 分隔符 + 空行 + 插入 1 段:
    4. `## 整版本模式 — 评审范围与适用场景` 附录(含 R-1/2/3 / 评审范围 / 与模式 1 关系 / 不触发区段 / Q-8.1/8.2/8.3)
  - **字节级原则**:
    - frontmatter(L1-3)**字节级保留**(INV-4)
    - 步骤 1(L106-110)**字节级保留**(INV-11)— Edit `old_string` 严格匹配
    - 步骤 2(L111-114)既有字面**字节级保留**(INV-1)— 整版本模式"步骤 2"是**新增**,**不**重写模式 1 既有"步骤 2"
    - 步骤 15(L308-313)**字节级保留**(INV-12)— Edit `old_string` 严格匹配
    - 既有 4 模板字节级不变(INV-13)
  - **行数约束**:增量追加 60-120 行(`clarifications.md` P-2 锁定);若 > 150 行,触发 R-8 重新评估"是否需要新增 `REVIEW-ALL.md` 模板"(Q-8.3)
- **边界与异常**:
  - 锚点 A 错位 → Grep 自检 L106-110 字节级不变(INV-11 失败)
  - 锚点 B 错位 → Grep 自检 L308-313 字节级不变(INV-12 失败)
  - frontmatter 改 → INV-4 失败(自检 `name: code-review` + 完整 description 字节级保留)
  - 既有 4 模板被改 → INV-13 失败(自检 `git diff -- templates/ checklists/` 字节级 diff = 0)
- **验证手段**(由 T-003 收尾执行 13 项不变量自检):
  - INV-1:模式 1 行为完全不变(`git diff SKILL.md` 字节级字面保留,锚点 A/B 既有段落 diff = 0)
  - INV-2:`REVIEW-REPORT.md` 与模式 1 字面 100% 一致(完全复用模板,无新模板)
  - INV-3:派生任务编码沿用 `code-plan` 既有(本任务**不**生成 TASK 编码)
  - INV-4:frontmatter 字节级不变(`git diff SKILL.md | grep -E "^(---|\.\.\.)"`)
  - INV-5:其他 9 个 `code-*` SKILL.md + `marketplace.json` + `plugin.json` 字节级不变(`git diff` 无变更)
  - INV-6:`assistants/rules/` 13 文件字节级不变
  - INV-7:REVIEW.md 位于版本顶层(由 T-002 后续 T-001 验证)
  - INV-8:多次执行幂等(由 T-001 验证 Write 覆盖)
  - INV-9:发现去重键 `(需求编码, 描述前 50 字)`
  - INV-10:派生任务去重键 `(需求编码, 描述前 50 字)`
  - INV-11:步骤 1(L106-110)字面字节级不变
  - INV-12:步骤 15(L308-313)字面字节级不变
  - INV-13:既有 4 模板字节级不变
- **回退方式**:`git checkout -- plugins/code-skills/skills/code-review/SKILL.md`(本次为增量追加,直接回退)
- **对应设计章节**:RESULT.md §4.1 + §5 算法 1-6 + §7.2-7.4 + §11.1
- **依据规范**:
  - `skill-conventions §规则 1`(frontmatter 字节级保留)
  - `module-conventions §规则 1`(SKILL.md 放技能根目录)
  - `encoding-conventions §规则 1-4`(派生任务编码沿用 `code-plan` 既有)
  - 概要设计 NFR-1(零新增依赖)+ NFR-2(增量修改 SKILL.md)
- **创建时间**:2026-06-05 16:00
- **最近更新**:2026-06-05 16:00
- **备注**:`clarifications.md` P-1 ~ P-10 10 项讨论结论已锁定;P-2 锁定新增行数 60-120(若 > 150 触发 R-8)

#### 单元测试状态
- **测试状态**:不适用(沿用 V0.0.2 既有 11 个 `code-*` 实践,纯文档型)
- **测试文件**:N/A
- **覆盖的测试场景**:N/A
- **测试用例数**:0
- **测试通过率**:N/A
- **不适用理由**:"纯文档任务 — 修改 SKILL.md 文本,本仓库无构建/测试文件(REQ-00009 守卫判定'不可测'),与 V0.0.2 REQ-00007 PLAN 实践一致(5 任务全部不适用)"
- **测试结果详情**:N/A
- **测试负责人**:N/A

---

### TASK-REQ-00008-00002:[文档] 同步 V0.0.2 看板 5 处

#### 基础信息
- **类型**:文档
- **触发/来源**:需求新增
- **触发任务**:T-001(详细设计完成,本任务同步看板)
- **开发状态**:待开始
- **目标**:在 `assistants/V0.0.2/RESULT.md` 同步 5 处:详细设计与任务计划汇总 + 任务清单(3 行) + 里程碑(2 个) + 文档头"最近更新" + 变更记录(1 行)
- **涉及文件/模块**:
  - 修改 `assistants/V0.0.2/RESULT.md`
- **前置任务**:T-001
- **关联任务**:无
- **关键变更**:
  - **文档头**:`最近更新:2026-06-05 16:00`
  - **版本信息表**:`最近更新:2026-06-05 16:00`
  - **详细设计与任务计划汇总**追加 1 行:
    ```
    | REQ-00008 | `/code-review` 整版本模式(无参评审:增量改 SKILL.md + 0 新增 + 4 复用 + 3 任务 + 13/13 INV) | 已完成(详细设计) | 2026-06-05 16:00 | 2026-06-05 16:00 | [PLAN.md](plan/REQ-00008/PLAN.md) / [RESULT.md](plan/REQ-00008/RESULT.md) |
    ```
  - **任务清单**追加 3 行(T-001 ~ T-003):
    ```
    | `TASK-REQ-00008-00001` | REQ-00008 | 修改 | 需求新增 | [修改] 增量追加 `code-review/SKILL.md`(2 段:步骤 1.5 模式选择 + 步骤 2 整版本模式 + 步骤 3 退化 + 整版本模式附录;锚点 A L109 后 + 锚点 B L313 后) | 待开始 | 不适用 | `plugins/code-skills/skills/code-review/SKILL.md` | — | — | — |
    | `TASK-REQ-00008-00002` | REQ-00008 | 文档 | 需求新增 | [文档] 同步 V0.0.2 看板 5 处 | 待开始 | 不适用 | `assistants/V0.0.2/RESULT.md` | T-001 | — | — |
    | `TASK-REQ-00008-00003` | REQ-00008 | 文档 | 需求新增 | [文档] 13 项不变量自检 + 偏差日志 + 收尾 | 待开始 | 不适用 | `code/TASK-REQ-00008-00003/{RESULT,work-log,deviations}.md` | T-001, T-002 | — | — |
    ```
  - **里程碑**追加 2 个(M-1 / M-2):
    ```
    | M1-REQ-00008-1:文档就绪 | T-001(REQ-00008) | 1 个 SKILL.md 增量追加完成 + INV-1/4/11/12 字节级保留 + 13/13 INV 自检通过 | 待开始 | 2026-06-05 | — |
    | M1-REQ-00008-2:本需求可发布 | M1-REQ-00008-1 + T-002 + T-003 | **3 任务开发状态=已完成 且 测试状态=不适用**,13/13 INV 100% 自检通过 + 看板 5 处一致 | 待开始 | 2026-06-05 | — |
    ```
  - **变更记录**追加 1 行:
    ```
    | 2026-06-05 16:00 | 设计新增 | REQ-00008 详细设计与编码计划完成(共 3 个任务) | REQ-00008 |
    ```
- **边界与异常**:
  - 5 处必须全部同步(任何遗漏 = 本任务未完成)
  - 任务清单 3 行的字段必须与 PLAN.md §2 完全一致
  - 详细设计汇总 + 任务清单 + 里程碑 + 文档头 + 变更记录 5 处都引用相同的 16:00 时间
- **验证手段**:
  - 5 处 diff 检查(任务清单 T-001 ~ T-003 行)
  - T-003 静态自检:任务清单 3 行 = PLAN.md §2 表 3 行
  - 统计行更新("5 个计划 / 共 16 个任务" → "6 个计划 / 共 19 个任务" 视 V0.0.2 看板实际)
- **回退方式**:`git checkout -- assistants/V0.0.2/RESULT.md`
- **对应设计章节**:RESULT.md §11.4 + §16A
- **依据规范**:`dashboard-conventions.md §规则 1`(字段约定不扩展,只追加行)
- **创建时间**:2026-06-05 16:00
- **最近更新**:2026-06-05 16:00
- **备注**:本任务应**最后**执行(收尾同步所有任务状态);与 T-001 之后 T-003 之前

#### 单元测试状态
- **测试状态**:不适用(沿用 V0.0.2 既有 11 个 `code-*` 实践,纯文档型)
- **测试文件**:N/A
- **不适用理由**:"纯文档任务 — 修改 Markdown 看板,无传统单测载体"

---

### TASK-REQ-00008-00003:[文档] 13 项不变量自检 + 偏差日志 + 收尾

#### 基础信息
- **类型**:文档
- **触发/来源**:需求新增
- **触发任务**:T-001, T-002
- **开发状态**:待开始
- **目标**:执行 13 项不变量自检 + 写偏差日志(deviations.md)+ 收尾本需求
- **涉及文件/模块**:
  - 新建 `code/TASK-REQ-00008-00003/{RESULT,work-log,deviations}.md`
  - 修改 `assistants/V0.0.2/RESULT.md`(任务清单 T-003 行的"涉及文件"列)
- **前置任务**:T-001, T-002
- **关联任务**:无
- **关键变更**:
  - **13 项不变量自检**:
    1. INV-1:模式 1 行为完全不变(`git diff SKILL.md` 锚点 A 之前 L1-110 + 锚点 B 之前 L308-313 字节级 diff = 0)
    2. INV-2:`REVIEW-REPORT.md` 与模式 1 字面 100% 一致(完全复用 `templates/REVIEW-REPORT.md`,无新模板)
    3. INV-3:派生任务编码沿用 `code-plan` 既有(本设计**不**自己生成 TASK 编码)
    4. INV-4:frontmatter 字节级不变(`git diff SKILL.md | head -3` 字节级保留)
    5. INV-5:其他 9 个 `code-*` SKILL.md + `marketplace.json` + `plugin.json` 字节级不变(`git diff` 无变更)
    6. INV-6:`assistants/rules/` 13 文件字节级不变(`git diff` 无变更)
    7. INV-7:REVIEW.md 位于版本顶层(`./assistants/V0.0.2/REVIEW.md` 路径正确;由 T-001 验证 SKILL.md 字面包含此路径)
    8. INV-8:多次执行幂等(由 T-001 验证 Write 覆盖;NFR-3 强约束)
    9. INV-9:发现去重键 `(需求编码, 描述前 50 字)`(SKILL.md 字面包含)
    10. INV-10:派生任务去重键 `(需求编码, 描述前 50 字)`(SKILL.md 字面包含)
    11. INV-11:步骤 1(L106-110)字面字节级不变(`git diff` 字节级 diff = 0)
    12. INV-12:步骤 15(L308-313)字面字节级不变(`git diff` 字节级 diff = 0)
    13. INV-13:既有 4 模板字节级不变(`git diff templates/ checklists/` 字节级 diff = 0)
  - **work-log.md**:记录每步自检结果(13 项,逐项展开)
  - **deviations.md**:记录与设计的偏差(若有)
  - **RESULT.md**(本任务自身):总结自检结果 + 收尾
- **边界与异常**:
  - 自检不通过 → 派生"审查改修"任务(后续 `code-review` 阶段)
  - 偏差 > 5 项 → 中断,需人工决策
- **验证手段**:
  - 13 项自检**全部通过**
  - work-log.md + deviations.md 内容完整
  - V0.0.2 看板统计自检:任务清单 3 行 = PLAN.md §2 表 3 行
- **回退方式**:`rm code/TASK-REQ-00008-00003/*`(本次新增,简单删除)
- **对应设计章节**:RESULT.md §3(规范遵循)+ §4.2(不变量自检)+ §11.1(测试要点)+ §11.4(不修改文件清单)
- **依据规范**:13 项自检逐项对应 NFR-1/3/4 + FR-7/8 + skill-conventions + module-conventions + doc-conventions + marketplace-protocol + encoding-conventions
- **创建时间**:2026-06-05 16:00
- **最近更新**:2026-06-05 16:00
- **备注**:本任务**必须**最后执行;沿用 V0.0.2 REQ-00007 / REQ-00014 既有"T-005/T-006 8 项不变量自检"实践;不自动 commit,提交哈希=N/A

#### 单元测试状态
- **测试状态**:不适用(沿用 V0.0.2 既有 11 个 `code-*` 实践,纯文档型)
- **测试文件**:N/A
- **不适用理由**:"纯文档任务 — 写自检日志文档,无传统单测载体"

---

## 4. 任务依赖图

```mermaid
graph LR
    T001[T-001 增量改 SKILL.md<br/>2 段:锚点 A + 锚点 B] --> T002[T-002 同步 V0.0.2 看板 5 处]
    T001 --> T003[T-003 13 项不变量自检 + 收尾]
    T002 --> T003
```

**关键观察**:
- T-001 是**关键路径**(无前置)
- T-002 在 T-001 之后(看板同步需要详细设计 + 任务拆分完成)
- T-003 在 T-001 + T-002 之后(13 项不变量自检需要全部前序任务完成)
- 全部串行,无并行

---

## 5. 里程碑

| 里程碑 | 包含任务 | 完成定义 | 预期时间 | 实际完成 |
| --- | --- | --- | --- | --- |
| **M1-REQ-00008-1:文档就绪** | T-001 | SKILL.md 增量追加完成(2 段:锚点 A + 锚点 B) + INV-1/4/11/12 字节级保留 + 13/13 INV 自检通过(由 T-003 收尾执行) | 2026-06-05 | — |
| **M1-REQ-00008-2:本需求可发布** | T-001 ~ T-003 | **所有 3 任务开发状态=已完成 且 测试状态=不适用**,通过 13 项不变量自检 + 看板 5 处一致 + `code-review` 整版本模式可被触发(由未来 `code-review`(无参)验证) | 2026-06-05 | — |

> 完成定义显式列出两轴状态要求,避免把"开发完成"误当"可发布"。

---

## 6. 状态管理规则

### 6.1 开发状态(主状态)
- **状态推进**:`待开始` → `进行中` → `已完成`,或经 `阻塞` 后回到 `进行中`
- **已完成不可逆**:开发状态为"已完成"的任务,其**描述/关键变更/依赖等字段不可修改**
- **已取消不可逆**:已取消任务作为历史保留,后续任务不应再依赖
- **阻塞**:必须填写阻塞原因,放在"备注"或单独的过程文档
- **状态变更记录**:每次状态变更在"变更记录"中记录(变更类型=开发状态更新)

### 6.2 测试状态(平行状态)
- **初始化**:3 任务**全部 `不适用`**(沿用 V0.0.2 既有 11 个 `code-*` 实践;本仓库无构建/测试文件,REQ-00009 守卫判定"不可测")
- **状态推进**:不适用 → 不适用(无变化)
- **独立于开发状态**:测试状态可独立于开发状态变化 — 但本设计 3 任务全部 `不适用`,不需变化
- **不适用不可逆**:一旦标为 `不适用`,不应再变为其他值(除非业务变化重新评估)
- **状态变更记录**:每次状态变更在"变更记录"中记录(变更类型=测试状态更新)

### 6.3 任务"真正可发布"定义

```
任务真正可发布 ⟺
    开发状态 = 已完成
    ∧ 测试状态 ∈ {已运行-通过, 不适用}
```

- 单看开发状态=已完成,任务只是"开发完成",不是"可发布"
- 单看测试状态=不适用,任务只是"测试不适用",前提是开发也已完成
- 只有两轴同时满足,任务才算真正完成

### 6.4 状态字段更新责任分工
| 字段 | 主要更新方 | 触发时机 |
| --- | --- | --- |
| 开发状态(待开始→进行中) | `code-it` | 步骤 7 任务开始 |
| 开发状态(进行中→已完成) | `code-it` | 步骤 14 任务完成 |
| 测试状态(任意→不适用) | `code-plan`(本设计) | 首次拆分时确认 |
| 任务标题、关键变更等描述 | `code-plan` 增量更新 | 步骤 9B |
| 任务类型 | `code-plan` 增量更新 | 步骤 9B(通常不改) |
| 触发/来源 | `code-plan` 首次拆分 | 3 任务全部 `需求新增` |
| 触发任务 | `code-plan` 首次拆分 | 见 §3 各任务 |

> 状态推进是单向写入,**已完成的开发状态不可回退**;但**测试状态**可以来回推进(因为测试可以重跑、补写)。

---

## 7. 关联计划

| 关联计划编码 | 关联点 | 对本计划的影响 | 链接 |
| --- | --- | --- | --- |
| `REQ-00004`(V0.0.2) | `code-dashboard` 看板 3 区段解析锚点 | T-001 SKILL.md "步骤 2.1 过滤需求" 沿用 `code-dashboard` 算法 1(NFR-8) | [PLAN.md](../REQ-00004/PLAN.md) |
| `REQ-00006`(V0.0.2) | `code-publish` 前置检查 | T-002 看板同步**不**触发 `code-publish` 区段 | [PLAN.md](../REQ-00006/PLAN.md) |
| `REQ-00007`(V0.0.2) | `code-auto` 评审循环 | `code-auto` 继续用模式 1(本设计**不**触发 `code-auto` 升级) | [PLAN.md](../REQ-00007/PLAN.md) |
| `REQ-00014`(V0.0.2) | 任务拆分新规则(按功能点) | 本计划 3 任务全部"按功能点拆";**不**插入架构任务(本需求不满足 3 个架构触发条件) | [PLAN.md](../REQ-00014/PLAN.md) |

---

## 8. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-05 16:00 | v1 | 初始创建 | 完成首次编码计划,共 3 条任务(1 修改 + 2 文档);100% 沿用概要设计 D-1~D-7 + 详细设计 PD-1~PD-4;100% 规范合规 — 0 偏离 0 冲突 0 授权;13/13 INV 全部满足;8/8 风险全部有缓解;2 里程碑(M-1 文档就绪 / M-2 本需求可发布);3 任务测试状态全部 `不适用`(沿用 V0.0.2 既有 11 个 `code-*` 实践);P-1 锁定 A(不新增 `REVIEW-ALL.md` 模板);P-2 锁定 60-120 行新增(若 > 150 触发 R-8);P-3 锁定 C(无效参 → 整版本 + 警告);P-4 锁定 INV-9 + INV-10 同键;P-5 锁定派生任务追加锚点 = 任务总览最末行;P-6 锁定分叉点 = 步骤 1.5 后立即分叉;P-7 锁定 0 触发额外看板区段;P-8 锁定 SKILL.md 增量追加不涉及步骤 13;P-9 锁定锚点 A/B 字面精度(L109 后 + L313 后);P-10 锁定 0 架构任务(本需求不满足 3 架构触发) | wangmiao |
