# 风险分析 — REQ-00008
更新时间:2026-06-05 16:00
版本:V0.0.2
需求编码:REQ-00008

---

## 1. 异常处理

### 异常路径 1:无 `.current-version`
- **触发条件**:`.current-version` 不存在
- **处理**:打印 `✗ 未检测到激活的版本工作空间` + 引导调 `code-version`
- **退出码**:非 0(E-1,沿用模式 1 既有)
- **依据规范**:FR-5.AC-5.1/5.2(沿用既有)

### 异常路径 2:某需求评审失败
- **触发条件**:循环评审时某需求 `code-review REQ-NNNNN` 内部失败
- **处理**:跳过该需求 + 报告 `✗ REQ-NNNNN 评审失败: <err>` + 继续其他需求
- **退出码**:0(警告,不阻塞)
- **依据规范**:E-2(沿用模式 1 既有)

### 异常路径 3:无"已完成"需求
- **触发条件**:步骤 2.1 过滤需求 = 空
- **处理**:打印 `✗ code-review(整版本模式):本版本无可评审的需求` + 退出
- **退出码**:0(无错误,只是无可评审)
- **依据规范**:E-3 + Q-3 采纳默认

### 异常路径 4:单需求 REVIEW-REPORT.md 写入失败
- **触发条件**:`Write` 工具异常
- **处理**:跳过该需求 + 报告 + 继续
- **退出码**:0(警告)
- **依据规范**:E-4(沿用模式 1 既有)

### 异常路径 5:聚合 REVIEW.md 写入失败
- **触发条件**:`Write` 工具异常
- **处理**:报告 `✗ 聚合 REVIEW.md 写入失败`,**不**整体失败(单需求文件已写)
- **退出码**:0(警告)
- **依据规范**:E-5

### 异常路径 6:派生任务编码生成失败(需求内 TASK 已满 99999)
- **触发条件**:`code-plan` 既有规则触发
- **处理**:跳过该发现 + 报告 + 继续
- **退出码**:0(警告)
- **依据规范**:E-6(沿用模式 1 既有)

### 异常路径 7:多次执行整版本模式
- **触发条件**:聚合/单需求文件已存在
- **处理**:全部覆盖
- **退出码**:0
- **依据规范**:E-7 + NFR-3 幂等

### 异常路径 8:`code-auto` 评审循环触发模式 2
- **触发条件**:自动化场景
- **处理**:**不**触发
- **依据规范**:NFR-4 强约束

### 异常路径总检
- 8 条异常路径(7 沿用模式 1 既有 + 1 本设计新增退化)
- 全部有处理策略
- 退出码语义:`0` = 成功(含非阻塞警告) / 非 0 = 致命错误

---

## 2. 安全边界

### 鉴权要求
**N/A**(本需求不涉及鉴权;`code-review` 技能继承 Claude Code 平台用户身份)

### 输入校验
- 步骤 1.5 模式选择:`^REQ-\d{5}$` 严格正则(沿用既有)
- 步骤 2.1 过滤需求:状态字面**严格匹配** `== "已完成"`(**不**归一化)
- 步骤 2.5 派生任务去重键:字符串级 `(需求编码 + "-" + 描述前 50 字)`

### 敏感数据处理
**N/A**(本需求不处理敏感数据;评审发现 = 文本描述,无 PII)

### 审计日志
- 版本看板"变更记录"区段追加每次整版本模式执行记录(由 `code-review` 既有同步逻辑覆盖,本设计**不**触发额外区段)
- `REVIEW.md` 文档头含"评审时间 / 评审人"字段

### 注入防护
**N/A**(本需求不处理用户输入 SQL / shell / 反序列化)

### 跨站请求伪造(CSRF)
**N/A**(本需求无 web 入口)

### 越权
**N/A**(本需求无鉴权层)

### 依据规范
- 既有 `code-review` 步骤 8.4 安全检查项(沿用):"输入校验充分 / 鉴权 / 敏感数据 / SQL 注入 / 命令注入 / 反序列化 / XSS / 依赖漏洞 / CSRF / 越权"
- 本需求的安全边界 = 模式 1 既有安全边界(0 增量)

---

## 3. 性能与资源

### 关键路径预估
- **步骤 1.5 模式选择**:`O(1)`(纯字符串匹配)
- **步骤 2.1 过滤需求**:`O(N)`(N = 看板需求总数;读 RESULT.md 1 次 + Grep 1 次)
- **步骤 2.2 循环评审**:`O(M × T)`(M = 被筛中需求数;T = 每个需求的平均任务数;沿用模式 1 评审)
- **步骤 2.3 写聚合**:`O(M)`(组装 REVIEW.md 5 章节)
- **步骤 2.5 派生任务追加**:`O(K)`(K = 派生任务数;按需求归类后追加到各 PLAN.md)
- **总耗时预估**:`O(M × T + K)`,M ≤ 12(V0.0.2 当前规模),T ≤ 10(单需求典型任务数),K ≤ 20
- **预期 wall clock**:30 秒 ~ 2 分钟(V0.0.2 当前规模)

### 资源限制
- **磁盘 I/O**:聚合 REVIEW.md ≤ 50 KB(V0.0.2 当前规模;随 M 线性增长)
- **内存**:**N/A**(LLM 现场维护,无内存状态)
- **网络**:**0 个**网络调用(纯本地 Read / Write / Grep)

### 缓存策略
**N/A**(本需求不引入缓存;每次执行读最新 RESULT.md)

### 并发
**N/A**(本需求**不**引入并发;沿用 LLM 串行执行模式)

### 性能自检
- ✅ 0 网络 I/O
- ✅ 0 内存状态
- ✅ 0 并发
- ✅ 关键路径 `O(M × T + K)`,V0.0.2 规模下 < 2 分钟
- ✅ 磁盘占用可忽略

---

## 4. 回退策略

### 触发条件
- SKILL.md 增量追加错位(R-1)
- 派生任务追加位置错乱(R-5)
- 聚合文件写入失败(E-5)

### 回退步骤
1. **SKILL.md 增量追加错位**:`Edit plugins/code-skills/skills/code-review/SKILL.md` 移除"步骤 1.5 模式选择" + "步骤 2 整版本模式" + "步骤 3 整版本模式退化报告" + "整版本模式 — 评审范围与适用场景"4 段
2. **派生任务追加位置错乱**:`Edit plan/REQ-NNNNN/PLAN.md` 移除错误的派生任务行(用 Git diff 识别)
3. **聚合文件写入失败**:`rm assistants/V<版本号>/REVIEW.md`(若有部分写入)

### 回退验证
- `git diff --stat` 检查本次变更范围
- `git checkout -- plugins/code-skills/skills/code-review/SKILL.md`(若严重错位,直接回退到上次成功状态)
- `git checkout -- assistants/V<版本号>/plan/REQ-NNNNN/PLAN.md`(若 PLAN.md 派生任务行错乱)

### 不可回退的操作
**无**。本需求所有修改都可通过 `git checkout -- <file>` 回退(沿用 git 原生回退)

### 依据规范
- INV-4 / INV-11 / INV-12(frontmatter / 既有字面字节级不变 → 错位可被 Grep 立即识别)
- 概要设计 NFR-3(幂等 → 多次执行覆盖)

---

## 5. 测试要点

### 单元测试
**N/A**(本需求不涉及代码;沿用既有 11 个 `code-*` 技能的"纯文档型"实践,测试状态=不适用)

### 集成测试
**N/A**(本仓库无构建/测试框架;REQ-00009 守卫判定"不可测")

### 端到端(E2E)测试
- **场景 1**:V0.0.2 当前有 0 个"已完成"需求 → 触发 S-2 退化 → 打印"本版本无可评审的需求" + 退出 0
- **场景 2**:人工调 `/code-review`(V0.0.2 假设已有 ≥ 1 个"已完成"需求)→ 走完整流程 → 写 REVIEW.md + N 份 REVIEW-REPORT.md + 派生任务追加 → 报告
- **场景 3**:第二次调 `/code-review` → 覆盖 REVIEW.md + N 份单需求文件 + 检查派生任务**不**重复追加 → S-3 报告"与前次比较"
- **场景 4**:与模式 1 共存 — 第一次调 `/code-review REQ-NNNNN` → 模式 1 既有行为;第二次调 `/code-review` → 整版本模式覆盖模式 1 既有 REPORT → 模式 1 + 模式 2 互覆盖

### 性能/安全测试
**N/A**(本需求不涉及性能敏感或安全敏感操作)

### 不变量自检(由 `code-it` 任务 T-001 末尾执行 + T-004 收尾执行)
- INV-1:模式 1 行为完全不变(字节级 diff = 0)
- INV-2:`REVIEW-REPORT.md` 与模式 1 字面 100% 一致
- INV-3:派生任务编码沿用 `code-plan` 既有
- INV-4:frontmatter 字节级不变
- INV-5:其他 SKILL.md 字节级不变
- INV-6:`assistants/rules/` 字节级不变
- INV-7:REVIEW.md 位于版本顶层
- INV-8:多次执行幂等
- INV-9:发现去重键 `(需求编码, 描述前 50 字)`
- INV-10:派生任务去重键 `(需求编码, 描述前 50 字)`
- INV-11(本设计新增):步骤 1(L106-110)字面字节级不变
- INV-12(本设计新增):步骤 15(L308-313)字面字节级不变
- INV-13(本设计新增):4 模板字节级不变

### 测试结果预期
- 13/13 不变量自检 100% 通过
- 0 偏离
- 0 冲突
- 0 授权偏离
