# 澄清记录 — REQ-00006(详细设计阶段)

更新时间:2026-06-04 17:01
版本:V0.0.2

## 1. 本阶段澄清状态

详细设计阶段**未发现**新的需要用户澄清的项,因为:

1. 概要设计阶段已完成 8 项决策 D-1 ~ D-8(架构层面)
2. 详细设计阶段的 8 项补充决策 DD-1 ~ DD-8(实现层面)全部可从 design + 项目现状演绎得出(详 design-notes.md)
3. 规范无新增冲突(0 冲突)
4. 任务拆分原则清晰,8 条任务的边界明确

## 2. 详细设计决策映射

| 决策 | 来源 | 是否需要用户澄清 |
| --- | --- | --- |
| DD-1 看板解析的具体 Bash 命令链 | 项目现状(纯文档) + 决策 D-2 | 否 |
| DD-2 字段名识别 vs 列号位置 | NFR-8 强一致 + dashboard §规则 1 演绎 | 否 |
| DD-3 字典序工具选择 | 决策 D-3 + Bash 默认 sort | 否 |
| DD-4 placeholder 替换方式(Claude 渲染) | 简单优于 sed 转义 | 否 |
| DD-5 覆盖前的 ls 预检 | 决策 D-7 透明性 | 否 |
| DD-6 Q&A.md 章节生成规则 | AC-5.4 锁定 | 否 |
| DD-7 错误退出的具体行为 | 模仿其他 7 版本感知技能 | 否 |
| DD-8 幂等性的字节级语义 | NFR-6 演绎 | 否 |
| 任务拆分:T-001 ~ T-008 | 设计层 7 模块 + 5 模板 + 收尾 + 可选 README | 否 |
| 任务编号格式选择 | encoding-conventions §规则 1 + §规则 3 | 否 |

## 3. 已知"未决项",但**不阻塞本计划**

继承自 design 阶段的 7 项 Q-D(Q-D-1 ~ Q-D-7)+ 本阶段新增 1 项:

### Q-D-8(本阶段新增):T-008 双 README 同步是否拆为独立任务?

- **当前状态**:design 阶段判定"可选附加"(`code-plan` 决定)
- **本计划决策**:**拆**(列为 T-008)
- **理由**:
  - 用户需求 §2.3 末尾"若需要,在 `plugins/code-skills/README.md` + `README.en.md` 的'主要能力'中追加一行(同次提交,遵循 `doc-conventions §规则 1`)" → "若需要"表明非强制
  - 但**实际需要**:新增 `code-publish` 后,用户在 README 中找不到该技能的介绍,体感不一致(与 `code-init` / `code-version` / `code-rule` 等横向技能在 README 中都有介绍的现状偏离)
  - 拆为独立任务可使 README 同步显式化(避免在 T-001 中"附带"导致 commit 不规范)
- **后果**:本计划共 8 条任务(原预估 8 条,与此一致)
- **建议派生**:无(已纳入本计划)

## 4. 设计阶段 Q-D 的回应延续

| 编号 | 决策时点 | 本计划状态 |
| --- | --- | --- |
| Q-D-1 注册到 marketplace.json | code-review | 本计划**不**包含;留给 review 派生 |
| Q-D-2 README "主要能力" 追加 | code-plan(本阶段) | **本计划已纳入**(T-008) |
| Q-D-3 沉淀 publish-conventions.md | code-review | 不包含 |
| Q-D-4 dashboard "全完成" 升级 | REQ-00004 设计 | 不包含 |
| Q-D-5 加入 REQ-00005 改写 | code-review | 不包含 |
| Q-D-6 v2 `--force` 实现方式 | v2 | 不包含 |
| Q-D-7 报告升级 REQ-NNNNN·标题 | code-review | 不包含 |

## 5. 设计阶段未与用户做 `AskUserQuestion` 的理由

按本技能 SKILL.md "步骤 8A — 澄清冲突与不确定项"工作流约定:**每轮聚焦 1-3 个最阻塞点**;本计划经全面自检,**0 阻塞点**,因此**不发起** `AskUserQuestion`,直接进入 §10A 任务拆分。

若用户在审阅本 RESULT.md / PLAN.md 时发现需要澄清的实现细节,可:
- 调 `/code-plan REQ-00006`(增量更新模式)+ 在对话中提出
- 或在 `code-review` 阶段提出
