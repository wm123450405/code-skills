# 澄清记录 — REQ-00004(详细设计阶段)

更新时间:2026-06-04 16:10
版本:V0.0.2

---

## 来自上游(`code-require` 锁定 + `code-design` 锁定)
详见 `./assistants/V0.0.2/require/REQ-00004/clarifications.md` 与 `./assistants/V0.0.2/design/REQ-00004/clarifications.md`。

继承上游已锁定的:
- Q-1 / Q-2 / Q-3(`code-require` 锁定)
- Q-4 / Q-5(`code-require` 采纳默认)
- Q-7 / Q-8(`code-require` 未采用)
- Q-D1 / Q-D2 / Q-D3 / Q-D4(`code-design` 阶段新增默认)

## 本次(`code-plan` 阶段)新澄清项

### Q-P1:任务编号是否"重命名"为新格式
- **问题**:V0.0.1 REQ-00001 任务字面是 `REQ-00001-001`(旧格式),与 `encoding-conventions §规则 3` 嵌套式不一致
- **选项**:
  - A. 严格按上游 NFR-3:旧格式透传,不解析(NFR-3 锁定)
  - B. 主动重命名为新格式(改看板字面)
- **本设计选择**:A
- **理由**:B 触发 FR-7 AC-7.1 "不修改看板"违规
- **影响章节**:`module-details §M-1` 算法 4 / `data-changes §D-2 TaskId` / `risk-analysis §8 异常 E-9`
- **解决时间**:2026-06-04 16:10

### Q-P2:V0.0.2 "已完成(需求分析)" 状态子状态的处理
- **问题**:V0.0.2 看板"需求清单"列已用 `已完成(需求分析)`(需求分析阶段完成,但概要设计/详细设计/编码/评审未开始);本技能 `aggregate()` 时如何处理
- **选项**:
  - A. 严格按字面匹配计数(本设计的"已完成" = `已完成` 而非 `已完成(需求分析)`)(推荐)
  - B. 归一化到"已完成"桶
- **本设计选择**:A
- **理由**:B 会导致"code-plan / code-it" 看不到需求,违反"读最新看板"语义
- **影响章节**:`data-changes §D-5 RequirementRow` / `design-notes §算法 3`
- **解决时间**:2026-06-04 16:10

### Q-P3:`code-dashboard` 是否在需求模式显示"里程碑"段
- **问题**:概要设计 §6.1 段 1 / 段 2 / 段 3 之外,有"里程碑"段(Q-D2 锁定为"可选 / 副显")
- **选项**:
  - A. 不显示(Q-D2 默认,推荐)
  - B. 需求模式时显示"该需求相关里程碑"
- **本设计选择**:A
- **理由**:`/code-dashboard <REQ>` 的"需求粒度"语义是"该需求 + 其下任务",里程碑是"版本级"概念,不在该粒度范围
- **影响章节**:`interface-specs §I-1` 需求模式 5 段
- **解决时间**:2026-06-04 16:10

### Q-P4:任务编号解析失败时的退化
- **问题**:V0.0.x 历史任务可能含非标准字面(例如 `EXISTING-001-001` 或旧版 `BUG-001`)
- **选项**:
  - A. 解析失败 → 字面原样显示,不报警(NFR-2 L2 退化,推荐)
  - B. 解析失败 → 标 `?` 提示用户
- **本设计选择**:A
- **理由**:B 会污染"显示策略"(出现 `?` 让用户困惑)
- **影响章节**:`design-notes §算法 4 parseTaskId` / `risk-analysis §8` 异常 E-9
- **解决时间**:2026-06-04 16:10

---

## 留作 follow-up
- F-1:`code-review/SKILL.md` frontmatter `<version>` 笔误(超出本需求范围)
- F-2:`code-dashboard` 是否在"任务清单"段附加"近期提交哈希"列(留作 v2 增量)
- F-3:跨版本汇总能力(用户原话未提;留作 v2)
- F-4:`--json` / `--filter` / `--since` 等 CLI flag(留作 v2)

## 阻塞项
- **无**(本阶段所有 Q-P1 ~ Q-P4 均采纳默认,无用户回复阻塞)
