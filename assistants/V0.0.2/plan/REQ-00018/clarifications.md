# 澄清记录 — REQ-00018
更新时间:2026-06-06 13:15
版本:V0.0.2
需求编码:REQ-00018

## 本轮澄清

**本设计 0 新澄清**(沿用 `code-require` 阶段 2 项锁定 + 5 项采纳默认,沿用 `code-design` 阶段 3 项设计目标确认)。

### 沿用项

- **Q-1**(code-require 锁定 A):通用技能增强 + 扫描 CWD,不改 `code-skills` 自身
- **Q-2**(code-require 锁定 A):`manifest.json` 社区主流拼写
- **Q-3~Q-7**(code-require 采纳默认):项目/模块同时改 / monorepo 不特殊处理 / 版本号默认 = `<版本号>` / 0 命中不阻断 / `--skip-cwd-sync` 不实现
- **Q-8**(code-require 建议派生):沉淀 `cwd-sync-conventions.md` + 升级 CLI 参数,留作 v2 follow-up
- **Q3.1~Q3.3**(code-design 锁定):`--balanced` 粒度 / 功能性高 / 健壮性高

## 本设计**未**提出的新澄清

**说明**:基于上游已锁定的 2 项 + 采纳默认的 5 项 + 设计目标确认的 3 项,本设计**无**新增的待澄清项。

- 5 决策 D-1~D-5:全部已锁定
- 8 边界 E-1~E-8:全部已锁定
- 6 类描述文件解析契约:全部已锁定
- 5 类屏幕输出契约:全部已锁定

## 拆任务决策(`code-plan` 步骤 10A)

### Q-P1:本需求拆几个任务?

- **时间**:2026-06-06 13:15
- **问题**:本需求应拆 1 个 / 2 个 / 3+ 个任务?
- **选项**:
  - A. **2 个任务(推荐)** — T-001 SKILL.md 增量追加 + T-002 INV 自检收尾;严格按"1 任务 = 1 实际产出";`--balanced` 粒度
  - B. 1 个任务 — T-001 合并 SKILL.md 改动 + INV 自检 + 看板同步;粒度粗化
  - C. 3+ 个任务 — T-001 写 SKILL.md + T-002 INV 自检 + T-003 看板同步;粒度过细(`T-002 + T-003` 不满足 REQ-00017"1 任务 = 1 实际产出")
- **用户回答**:A(推荐)2 个任务
- **影响**:
  - 任务总数 = 2
  - 任务类型 = 1 修改 + 1 文档
  - 测试状态 = 2/2 不适用(纯文档,仓库无测试框架)
  - 0 派生"更新看板"任务(REQ-00017 强约束)
  - 0 架构任务(本需求不满足 REQ-00014 3 触发条件)

### Q-P2:测试状态如何设?

- **时间**:2026-06-06 13:15
- **问题**:本需求 2 个任务的测试状态?
- **选项**:
  - A. **不适用(推荐)** — 仓库无测试框架(CLAUDE.md 严守),沿用 REQ-00009 守卫判定"项目不可测"
  - B. 已编写 — 不可能(无测试框架)
  - C. 阻塞 — 不合理(无外部阻塞)
- **用户回答**:A(推荐)不适用
- **影响**:测试状态 = 2/2 不适用;"不适用理由" = 纯文档任务(纯 SKILL.md 修改 + 纯文档同步)

## 本设计 0 冲突 / 0 偏离 / 0 待澄清

| 维度 | 状态 |
| --- | --- |
| 与上游(需求 + 设计)锁定一致 | ✅ |
| 与 13 份项目级规范一致 | ✅ |
| 与 4 份关联设计一致 | ✅ |
| 新增待澄清项 | **0** |

## 后续 follow-up(留作 v2,本需求不阻塞)

- **Q-8 派生建议**:`code-rule` 沉淀 `cwd-sync-conventions.md`
- **Q-8 派生建议**:`code-version` 升级接受 `--cwd` / `--version-string` / `--skip-cwd-sync` 等 CLI 参数
