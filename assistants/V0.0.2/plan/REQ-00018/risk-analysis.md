# 风险分析 — REQ-00018
更新时间:2026-06-06 13:15
版本:V0.0.2
需求编码:REQ-00018

## 异常处理(8 边界,与概要设计 E-1~E-8 严格对应)

| ID | 触发条件 | 屏幕输出 | 阻断? | 监控 |
| --- | --- | --- | --- | --- |
| **E-1** | monorepo 多匹配 | 每文件 1 行 `✓` | 否 | 文件计数 N |
| **E-2** | 0 命中(CWD 无描述文件) | `⚠ CWD 下未检测到...` | 否 | 命中计数 = 0 |
| **E-3** | 格式不可解析(非 JSON/XML/TOML) | `⚠ <filename> 格式不可解析` | 否 | try-catch 计数 |
| **E-4** | 缺版本号字段 | `⚠ <filename> 未找到版本号字段` | 否 | parseVersionField 返回 null 计数 |
| **E-5** | `--skip-cwd-sync` CLI 参数 | (本需求不实现,Q-7) | (留作 v2) | — |
| **E-6** | `go.mod` 命中(Go 用 git tag) | `⚠ go.mod 无版本号字段` | 否 | 文件名 = go.mod 计数 |
| **E-7** | 描述文件无写权限(只读) | `⚠ <filename> 无写权限,跳过` | 否 | writeFile 抛异常计数 |
| **E-8** | 并发修改导致 Edit 失败 | `⚠ <filename> Edit 失败(<错误>)` | 否 | writeFile 抛异常计数 |

**总览**:**0 阻断 + 0 异常退出**。所有边界走 `⚠` 屏幕输出 + 继续流程。

## 安全边界

- **输入校验**:本需求只读 CWD 已知描述文件,无外部输入 → **无新校验需求**
- **鉴权**:本需求是本地文件操作 → **无新鉴权需求**
- **敏感数据**:版本号字符串(非敏感) → **无新敏感数据处理**
- **审计日志**:本需求是辅助能力 → **无新审计日志需求**
- **依赖安全**:0 新增依赖 → **无新依赖安全风险**

## 性能与资源(NFR-7)

### 关键路径预估

| 场景 | 文件数 | 估算耗时 |
| --- | --- | --- |
| 空 CWD(0 命中) | 0 | < 0.5 秒(纯 Glob) |
| 单描述文件 | 1 | < 1 秒 |
| monorepo 中等 | 5 | < 3 秒 |
| monorepo 大型 | 10 | < 5 秒(NFR-7 上限) |
| monorepo 巨型(>20 文件) | > 20 | 退化(屏幕 `⚠ 同步耗时较长` 但不阻断) |

### 资源限制

- **内存**:< 10 MB(只读 1 个文件,内存占用可忽略)
- **CPU**:< 1 %(纯文本匹配)
- **网络**:0
- **磁盘 IO**:每文件 1 次 Read + 1 次 Write(共 2 IOPS)

### 缓存策略

- **无缓存**:本需求每次执行都 Glob 实时扫描
- **理由**:缓存会引入 stale 数据风险(N 个描述文件可能被外部修改),实时扫描更鲁棒

## 回退策略

### 触发条件

- T-001 / T-002 任意 commit 引入严重 bug
- code-review 发现 0 必须改列表非空(罕见)

### 步骤

1. `git log --oneline -5` 找到本任务 commit hash
2. `git revert <commit-hash>` 撤销
3. `git push` 同步远程
4. 验证:调 `code-version V0.0.2` 后 `Read SKILL.md` 恢复原状

### 验证

- 调 `code-version V0.0.2` → 步骤 7 不应执行(已回退)
- `Read SKILL.md` → 与本需求前字节级一致

## 测试要点(本需求 0 单元测试,纯人工场景验证)

仓库**无**测试框架(CLAUDE.md 严守),验证靠**静态 Read + 人工场景验证**。

### 单元测试范围

- **0**:无单元测试框架,改用人工场景验证

### 集成测试范围

- **S-1** Maven Java 项目(`pom.xml` 命中,屏幕 1 行 `✓`,`<version>` 更新)
- **S-2** Node.js 项目(`package.json` 命中,屏幕 1 行 `✓`,`"version"` 更新)
- **S-3** monorepo(多个 `package.json`,屏幕 N 行 `✓`)
- **S-4** 0 命中(屏幕 1 行 `⚠ CWD 下未检测到...`)
- **S-5** CWD = `code-skills` 仓库根(0 命中,屏幕 1 行 `⚠`,**不**改 `code-skills` 自身)
- **S-6** 格式不可解析(屏幕 1 行 `⚠ <filename> 格式不可解析...`)
- **S-7** 缺版本号字段(屏幕 1 行 `⚠ <filename> 未找到版本号字段...`)
- **S-8** `go.mod` 命中(屏幕 1 行 `⚠ go.mod 无版本号字段...`)

### 端到端测试范围

- **失败不阻断**:S-6 场景同时验证 — `.current-version = V0.0.3` 仍被写入,退出 0
- **frontmatter 保留**:调 `code-version V0.0.3` 后 `Read SKILL.md L1-3` → 字节级一致
- **既有步骤 1~6 不变**:调 `code-version V0.0.3` 后 `Read SKILL.md` "## 工作流程" 段 → 字节级一致

### 性能/安全测试

- **性能**:人工 10 个文件的 monorepo 场景,验证 < 5 秒
- **安全**:N/A(本需求 0 安全风险)
