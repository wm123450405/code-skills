# 数据结构完整变更 — REQ-00018
更新时间:2026-06-06 13:15
版本:V0.0.2
需求编码:REQ-00018

## 总览:本需求 0 新增数据结构

| 维度 | 数量 |
| --- | --- |
| 新增实体 | 0 |
| 新增字段 | 0 |
| 修改实体 | 0 |
| 修改字段 | 0 |
| 新增索引 | 0 |
| 新增迁移脚本 | 0 |

## 既有数据(本需求不修改)

| 数据 | 路径 | 用途 | 状态 |
| --- | --- | --- | --- |
| `<版本号>` | `code-version` 步骤 1 收集 | 步骤 7 同步目标 | 既有,沿用 |
| `.current-version` | `./assistants/.current-version` | 当前激活版本标记 | 既有,**不**修改 |
| `<版本号>/RESULT.md` | `./assistants/<版本号>/RESULT.md` | 版本看板 | 既有,**不**扩展字段(NFR-6 严守) |
| 描述文件版本号 | CWD 下的 `package.json` / `pom.xml` 等 | 被同步对象 | 临时(本需求写) |

## 临时数据(只在步骤 7 内部用)

| 变量 | 类型 | 用途 | 生命周期 |
| --- | --- | --- | --- |
| `newVersion` | string | 新激活的版本号 | 步骤 7 函数入参 |
| `cwd` | string | 当前工作目录 | 步骤 7 函数入参 |
| `matched: string[]` | string[] | Glob 命中的文件路径列表 | 步骤 7 局部 |
| `oldVersion: string \| null` | string \| null | 解析得到的旧版本号 | 步骤 7.4 局部 |
| `newContent: string` | string | 替换版本号后的文件内容 | 步骤 7.4 局部 |

## 关系图

```
code-version 步骤 1-6 完成
  ↓
步骤 7 触发
  ↓
Glob CWD 6 类描述文件
  ↓
0 命中 → 屏幕 ⚠ → 退出 0
  ↓
N 命中(N≥1)
  ↓
对每文件:
  Read 内容 → parseVersionField → 旧版本号
  ↓
  Edit 替换 → Write 写回 → 屏幕 ✓
  ↓
退出 0(NFR-8 失败不阻断)
```

## 存储选型

- **不涉及持久化存储**:本需求只读 / 写 CWD 下的描述文件,无新数据库表
- **临时变量**:仅在函数调用栈中,不落盘

## 迁移脚本

- **不涉及**:本需求不修改任何持久化数据结构

## 依据规范

- NFR-6 严守:不修改 `<版本号>/RESULT.md` 看板模板
- NFR-1 严守:不引入新数据结构库(JSON / XML / TOML 解析器)
