# 数据结构完整变更 — REQ-00017
版本:V0.0.2

## SKILL.md 增量追加(2 文件 3 处)

### 1. `/code-plan` SKILL.md 锚点 A
- **路径**:`plugins/code-skills/skills/code-plan/SKILL.md`
- **位置**:`§步骤 10A 任务拆分` → `#### 任务类型` 小节**前**
- **内容**:新增 `#### 拆任务约束(REQ-00017 强约束,2026-06-05 起生效)` 小节
- **关键字段**:
  - 一个任务 = 一个"实际产出"
  - 实际产出候选集:`{代码改写, 测试编写, 文档改写, 数据迁移, 配置变更, 部署脚本}`
  - 不在候选集:`{看板更新, 文档评审, 任务分配}`
  - 看板推进职责:`/code-it` 末尾兜底后 P-1
  - 任务清单"触发/来源"列:全部="详细设计",不出现="更新看板"
- **字节数估算**:+200 ~ +400 行
- **行号影响**:既有内容字节级保留(仅追加新内容)

### 2. `/code-plan` SKILL.md 锚点 B
- **路径**:`plugins/code-skills/skills/code-plan/SKILL.md`
- **位置**:`§步骤 16A 同步版本看板(强制)` → 第 3 款**前**
- **内容**:新增 `2.5. **只追加真实任务**(REQ-00017 强约束,2026-06-05 起生效)` 小节
- **关键字段**:
  - 看板"任务清单"区段**只**追加真实任务
  - **不**追加"更新看板"派生任务
  - 看板"触发/来源"列**全部**="详细设计"
  - 看板推进由 `/code-it` P-1 承担
- **字节数估算**:+30 ~ +80 行
- **行号影响**:既有内容字节级保留(仅追加新内容)

### 3. `/code-it` SKILL.md 锚点 C
- **路径**:`plugins/code-skills/skills/code-it/SKILL.md`
- **位置**:`§末尾兜底提交` → 步骤 5 commit 成功判断**后**
- **内容**:新增 `#### 步骤 P-1 推进看板(REQ-00017 新增,2026-06-05 起生效)` 小节
- **关键字段**:
  - 触发条件:commit 成功
  - 算法伪代码(详 RESULT.md §3.3)
  - 失败处理:不重试不阻断
  - 幂等性:本任务"开发状态"已是"已完成"则跳过
  - 边界处理:5 种边界(详 RESULT.md §3.3)
- **字节数估算**:+200 ~ +500 行
- **行号影响**:既有"步骤 1-5"末尾兜底字节级保留

## 看板字段(本需求 0 修改)

- 任务清单 8 列:**0 新增 0 修改 0 重命名**
- 变更记录 10+ 事件类型:**0 新增**(沿用"任务完成")
- 任务编码双格式:**0 修改**(沿用 `TASK-(REQ|BUG)-\d{5}-\d{5}` 新 + 旧)
- **结果**:**不**触发 `dashboard-conventions §规则 1` 3 处同步
