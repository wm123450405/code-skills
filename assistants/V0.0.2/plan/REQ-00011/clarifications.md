# 澄清记录 — REQ-00011(本轮 code-plan)

更新时间:2026-06-05
版本:V0.0.2
来源:code-plan 步骤 8A(本轮**无**新增澄清问题,沿用上游 Q-1 ~ Q-8 决策)

## 本轮(2026-06-05)澄清记录

**无新增澄清问题**。本轮 `code-plan` 步骤 8A 沿用上游 Q-1 ~ Q-8 锁定决策:
- Q-1:确认环节位置 = "步骤 0b"(沿用)
- Q-2:回写 = 写入 `RESULT.md` 顶部"## 设计目标"小节(沿用)
- Q-3:确认问题形态 = 多个 `AskUserQuestion`(沿用)
- Q-4:`code-auto` 协同 = 不升级(沿用)
- Q-5:与 REQ-00005 协同 = 增量追加"步骤 0b"(沿用)
- Q-6:`commit-conventions.md` 与 CLAUDE.md 追加 = 不追加(沿用)
- Q-7:"设计目标"选项预设 = 整体 3 选项 + 4 维度各 1 问题(沿用)
- Q-8:`code-plan` 读 `design` "## 设计目标"小节 = 读 + 据此调整粒度(沿用)

## 本轮新发现的小问题(非阻塞,留作 follow-up,与设计/plan/上游 §11 一致)

| 编号 | 问题 | 影响范围 | 阻塞方 | 期望回复时间 |
| --- | --- | --- | --- | --- |
| F-1 | `code-design` 步骤 0b 是否对所有需求都触发 5 问? | §5 算法 1 | 用户 | 后续调 `code-design` 时观察 |
| F-2 | `code-plan` 步骤 0b 退化时,`AskUserQuestion` 1 问还是 5 问? | §5 算法 3 | 用户 | 后续调 `code-plan` 时观察 |
| F-3 | `code-review` 是否应感知"## 设计目标"小节(在评审清单中加 1 项)? | §10 状态机 | 用户 | 后续版本(本需求**不**触发) |

## 实施层面新增决策(本轮 `code-plan` 步骤 8A 锁定)

| 决策编号 | 决策 | 依据 |
| --- | --- | --- |
| P-D1 | `code-design` / `code-plan` 步骤 0a 既有"`code-*` **不**含步骤 0b"小注更新 — 删除原句,替换为"步骤 0a 成功后,`code-*` 进入'步骤 0b 设计目标确认'(本需求 REQ-00011 新增)" | 概要设计 D-1 锁定"步骤 0b"位置;本计划作为详细化补充 |
| P-D2 | "## 设计目标"小节锚点定位 = "## 文档头"区段后 + "## 1. ..."章节前(用"## 1."作为锚点定位) | INV-3 强约束 + D-2 锁定 |
| P-D3 | 任务粒度调整中"--minimal 合并同类任务"采用**轻度合并**启发式(只合并明显同类的,如"修改文件 A" + "修改文件 A 同字段" → 合并) | 避免误判,降低对"经验不足的 AI 协作者"的破坏风险 |
| P-D4 | 本项目 2 任务测试状态均 = `不适用`(纯 Markdown 技能,无构建/测试框架) | 沿用 V0.0.2 既有 12 个 `code-*` 实践;`code-unit` 守卫"项目根 7 项"判定本项目为"不可测" |
| P-D5 | 末尾兜底 commit message 预览:`chore(code-plan): REQ-00011 详细设计与编码计划完成\n\n详细设计与编码计划完成:2 个任务\n过程文件 7 + 结果文件 2(PLAN.md + RESULT.md)\n` | 沿用 V0.0.2 既有实践 |
