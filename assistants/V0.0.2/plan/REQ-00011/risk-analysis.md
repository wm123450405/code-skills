# 风险分析 — REQ-00011

更新时间:2026-06-05
版本:V0.0.2

## 异常处理

| 异常路径 | 检测手段 | 处理策略 | 监控指标 | 对应任务 |
| --- | --- | --- | --- | --- |
| 用户取消 `AskUserQuestion`(`code-design` 步骤 0b) | 工具返回取消信号 | 中止 + 回写空"## 设计目标"小节(E-3) | stderr | T-001 |
| `code-plan` 读 `design/.../RESULT.md` 不存在 | `FileExists` 检测 | 退化(FR-3) + 触发 `AskUserQuestion` | 屏显"⚠ 未检测到..." | T-002 |
| `code-plan` 读 `design/.../RESULT.md` 存在但无"## 设计目标"小节 | 正则匹配失败 | 退化(E-5) | 屏显"⚠ 未检测到..." | T-002 |
| 写文件失败(`Write` 工具) | 工具返回失败 | 透传 stderr,中断退出 | stderr | T-001 / T-002 |
| 锚点漂移(`RESULT.md` 缺"## 1. ..."章节) | 正则匹配失败 | 退化为"追加到文件末尾" | stderr 告警 | T-001 / T-002 |
| `AskUserQuestion` 触发次数过多(> 5) | 内部计数 | 强制停止,避免用户疲劳(NFR-8 性能) | 屏显告警 | T-001 / T-002 |
| `code-auto` 调 `code-design` / `code-plan` 步骤 0b 时 `AskUserQuestion` 被拦截失败 | 工具返回非"推荐项" | 沿用 `code-auto` 现行 Q-4 锁定 A"总选推荐项"(NFR-5) | 屏显 | T-001 / T-002 备注 |

## 安全边界

- **鉴权要求**:**无**(本技能由 Claude Code 内部调用,无对外鉴权)
- **输入校验**:`AskUserQuestion` 工具自带选项校验,无需额外校验
- **敏感数据处理**:`Write` 工具写入文件,**不**涉及敏感数据
- **防注入**:**无**(Markdown 内容,无 SQL/NoSQL/命令)
- **审计**:**无**(本技能不涉及审计)
- **依据规范**:**无特殊安全要求**

## 性能与资源

- **关键路径预估**:`AskUserQuestion` 1-5 次 ≤ 5 分钟(用户感知;NFR-8)
- **资源限制**:**无**(纯 Markdown 文本操作)
- **缓存策略**:**无**
- **批量/异步**:**无**
- **降级策略**:**无**

## 回退策略

- **触发条件**:T-001 / T-002 任一失败 / INV 自检不通过 / `code-auto` 调 `code-design` 步骤 0b 拦截失败
- **步骤**:`git revert <commit-hash>` 回退 T-001 / T-002 各自的 commit
- **验证**:`code-design` / `code-plan` 步骤 0b 不再触发(回到 V0.0.2 既有行为)
- **数据恢复**:**N/A**(无持久化数据,`design.md` / `plan.md` 模板的占位可保留;`## 设计目标`小节若已写入,`git revert` 自动删除)

## 测试要点

- **单元测试**:**不适用**(本项目无构建/测试框架,纯 Markdown 技能;沿用 REQ-00009 `code-unit` 守卫"不可测"判定 → 任务测试状态=`不适用`)
- **集成测试**:**无**
- **端到端测试**:**无**
- **性能/压力测试**:**无**
- **安全测试**:**无**
- **回归测试**:
  - NFR-5 验证 `code-auto` 调 `code-design` / `code-plan` 时,`AskUserQuestion` 触发后选"推荐项",**不**中断
  - INV-1 验证 frontmatter 字节级保留
  - INV-2 验证既有"步骤 0-N"流程不变
  - INV-8 验证不触发 `dashboard-conventions §规则 1` 3 处同步

> **关键决策**:本项目所有 12 个 `code-*` 技能任务的测试状态沿用 V0.0.2 既有实践 = `不适用`(纯 Markdown 技能),本需求 2 任务同样 = `不适用`。
