# 模块详细化 — REQ-00005

更新时间:2026-06-04 16:30
版本:V0.0.2

> 本文档是"概要设计 §7 模块划分"的逐模块详化,给出每个插入点的**精确位置 / 上下文锚点 / Edit old_string / new_string 草稿**,供 `code-it` 阶段直接落地。

---

## 模块 #1:`code-require` 技能(对应概要设计 §7.2 / §7.5)

### 1.1 路径与文件信息
- **路径**:`plugins/code-skills/skills/code-require/SKILL.md`
- **当前大小**:13,679 bytes
- **当前 frontmatter**(严禁改动):
  ```yaml
  ---
  name: code-require
  description: 需求分析(版本感知)。要求用户提供"需求编码",**所有产出物写入 `./assistants/<版本号>/require/<需求编码>/`**(由 `./assistants/.current-version` 决定版本号,若未设置则提示先调 `code-version`)。从该目录读取所有需求材料,分析并产出 `RESULT.md` 需求提示词文档。若 RESULT.md 已存在则做增量更新。同步追加/更新版本看板的"需求清单"与"变更记录"区段。在 `code-version` 之后、其他 `code-*` 之前使用。
  ---
  ```

### 1.2 变更点 A:插入"步骤 0a — 拉取最新代码"

#### 1.2.1 插入位置
- **目标行号(预期)**:当前第 79 行(标题 `### 步骤 0 — 版本上下文检测(强制前置)`)**之前**
- **锚点字符串**(用作 Edit old_string):
  ```
  ## 工作流程

  ### 步骤 0 — 版本上下文检测(强制前置)
  ```
- **Edit 操作类型**:`old_string` = 锚点字符串;`new_string` = "步骤 0a 章节 + 锚点字符串"

#### 1.2.2 完整插入内容(精确 Markdown)

```markdown
### 步骤 0a — 拉取最新代码(强制前置,新增)
所有其他 `code-*` 技能的第一步都是这一步。位于既有"步骤 0"之前。
1. 探测 `git` 可用性 → 执行 `Bash: git --version`,失败 → 中断 + 提示"未检测到 git,本需求需要 git,请先安装 git 后重试"(E-12)
2. 执行 `Bash: git pull`(默认 upstream / tracking 分支)
3. 拉取失败 3 种情况分类处理(Q-2 锁定 A:中断 + 报错退出):
   - **冲突**(退出码非 0 + stderr 含 "CONFLICT" / "Merge conflict" / "unmerged")→ 打印 `✗ git pull 失败:存在未解决的冲突,冲突文件:<列出>`,提示"请手动解决冲突后,重跑该技能"(E-2)
   - **网络**(退出码非 0 + stderr 含 "Could not resolve host" / "Connection" / "timeout")→ 打印 `✗ git pull 失败:网络/remote 不可达,远程仓库:<remote-url>`,提示"请检查网络/代理后重试"(E-3)
   - **凭据**(退出码非 0 + stderr 含 "Permission denied" / "Authentication failed" / "403")→ 打印 `✗ git pull 失败:权限/凭据`,提示"请检查 git 凭据(SSH key / token)后重试"(E-4)
   - **其他错误** → 透传 stderr,中断退出
4. 拉取成功(包含 no-op / "Already up to date"):
   - **立即**执行 `Read "./assistants/.current-version"`,记为"拉取后版本"(NFR-8 强约束)
   - 进入步骤 0b
```

#### 1.2.3 关联点
- **对应概要设计**:§7.2 变更点 A / §8.1 步骤 0a / §5.3 控制流
- **对应需求**:FR-1 / NFR-8
- **对应规范**:`skill-conventions.md §规则 1`(frontmatter 不变) + NFR-2(增量)
- **任务编号**:`TASK-REQ-00005-00001` 子任务(详见 `PLAN.md`)

### 1.3 变更点 B:插入"步骤 0b — 工作版本号对齐检查"(仅 `code-require` 专属)

#### 1.3.1 插入位置
- **目标行号(预期)**:在变更点 A 插入的"步骤 0a"**之后**、既有"步骤 0 — 版本上下文检测"**之前**
- **锚点字符串**:变更点 A 插入的最后一行(`4. 拉取成功...进入步骤 0b`) + 既有"步骤 0"标题

#### 1.3.2 完整插入内容(精确 Markdown)

```markdown
### 步骤 0b — 工作版本号对齐检查(`code-require` 专属,新增)
**仅 `code-require` 执行此步骤。`code-design` / `code-plan` 跳过。**
1. 读取"用户意图版本":
   - 读取 `Read "./assistants/.current-version"` 的"拉取前版本"(步骤 0a 之前的快照 — 若本会话内已读取则复用,否则新读)
   - 解析用户原始输入(口头/文本)中的版本号,若有显式指定则取用户指定
   - 默认:用户意图 = 拉取前版本
2. 对比"用户意图版本"与"拉取后版本"(步骤 0a 读取):
   - **一致** → 直接进入既有"步骤 0 — 版本上下文检测"
   - **不一致** → 进入步骤 3(询问)
3. 询问(FR-2.AC-2.3):
   ```
   ✗ 检测到上下文版本不一致
     本地(拉取后):<拉取后版本>
     用户意图:<用户意图版本>
     需求 <需求编码> 在 <用户意图版本> 中可能不存在,但在 <拉取后版本> 中已存在
   ```
   - 弹 `AskUserQuestion`,3 选 1:
     - **A. 切到 <拉取后版本> 后继续(推荐)**
     - **B. 在 <用户意图版本> 中重新创建 <需求编码>**
     - **C. 取消**
4. 选项处理:
   - **选 A** → 调独立技能 `code-version <拉取后版本>`,**不传其他参数**
     - 切换成功 → 打印"已切到 <拉取后版本>,请重跑 `code-require`",**退出** `code-require`,无文件变更(FR-2.AC-2.4)
     - 切换失败 → 透传 `code-version` 错误,提示"请手动运行 `code-version <拉取后版本>` 后重试"
   - **选 B** → 在当前 `<用户意图版本>` 继续,直接进入既有"步骤 0 — 版本上下文检测"(FR-2.AC-2.5)
   - **选 C** → 退出 `code-require`,无任何文件变更(FR-2.AC-2.6)
```

#### 1.3.3 关联点
- **对应概要设计**:§7.2 变更点 B / §8.1 步骤 0b / §5.2 数据流图
- **对应需求**:FR-2 / NFR-3(硬中断)
- **对应规范**:N/A 专属逻辑
- **任务编号**:`TASK-REQ-00005-00001` 子任务

### 1.4 变更点 C:插入"步骤 N — 末尾兜底提交"

#### 1.4.1 插入位置
- **目标行号(预期)**:当前第 186 行(标题 `### 步骤 10A — 完善过程文档`)**之后**,作为文件**最后**一章
- **锚点字符串**:`## 过程文档格式` 章节**之前**(整个 `## 过程文档格式` 小节保留为文档说明,本步骤 N 放在"过程文档格式"前)

#### 1.4.2 完整插入内容(精确 Markdown)

```markdown
### 步骤 N — 末尾兜底提交(强制收尾,新增)
**所有其他步骤执行完毕后的最后一步,覆盖"过程文件 + 结果文件"。**

> 注:本步骤对**首次分析(步骤 7A–9A / 10A)** 和**增量更新(步骤 7B–10B)** 两条分支都执行,均作为收尾。

1. **收集 dirty 文件**:
   - 执行 `Bash: git status --porcelain`
   - 输出为空 → 打印 `无文件变更,跳过末尾提交`,**跳过步骤 2-4,直接退出**(NFR-4 幂等 / AC-3.5)
   - 输出非空 → 进入步骤 2

2. **暂存文件**:
   - 执行 `Bash: git add <所有 dirty 文件>`(从步骤 1 的输出中提取)
   - 失败 → 透传 stderr,中断退出

3. **生成 commit message 预览**(NFR-6 沿用 V0.0.1 实践):
   ```
   chore(code-require): <需求编码> <需求标题>

   需求分析完成:<N> FR / <M> NFR / <K> AC
   过程文件 3 + 结果文件 1 = <N> 文件待提交
   ```
   - `<需求标题>` 从本技能本次执行的需求中提取(若是首次分析,取本轮 RESULT.md 的标题;若是增量更新,取上游 RESULT.md 的标题)
   - 数字 N / M / K 从本轮 RESULT.md 的统计中提取

4. **弹 3 选 1 确认**(Q-3 锁定 A / FR-3.AC-3.4):
   - **A. 确认提交(推荐)** → 进入步骤 5
   - **B. 修改 commit message** → 重新生成预览(可由用户编辑)→ 返回步骤 4
   - **C. 取消** → 跳过 commit,打印"已取消提交,文件保持暂存/工作区状态",**退出**(E-9)

5. **执行 commit**:
   - 执行 `Bash: git commit -m "<message>"`
   - 成功 → 打印 `commit 完成,hash: <hash>`,退出
   - 失败(退出码非 0,pre-commit hook / 其他)→ 打印 stderr,**不重试**(E-10),提示"末尾提交失败,文件已暂存,请手动处理后执行 git commit",退出
```

#### 1.4.3 关联点
- **对应概要设计**:§7.2 变更点 C / §8.1 步骤 N / §5.4 控制流
- **对应需求**:FR-3 / NFR-4 / NFR-5 / NFR-6 / NFR-7
- **对应规范**:NFR-6(commit 格式)
- **任务编号**:`TASK-REQ-00005-00001` 子任务

### 1.5 强约束不动的部分(完整清单)

| 不动部分 | 行号范围(预期) | 字面量 | 依据 |
| --- | --- | --- | --- |
| YAML frontmatter | 1-4 | `---` + `name: code-require` + `description: ...` + `---` | `skill-conventions.md §规则 1` + FR-6 |
| 既有"步骤 0 — 版本上下文检测" | 79-88 | "### 步骤 0 — 版本上下文检测..." 章节全文 | NFR-2 增量 |
| 既有"步骤 1-9A" | 90-184 | "### 步骤 1 — 收集需求编码" 到 "### 步骤 9A — 同步版本看板(强制)" 全文 | NFR-2 |
| 既有"步骤 10A — 完善过程文档" | 186-189 | "### 步骤 10A — 完善过程文档" 全文 | NFR-2 |
| 既有"步骤 5B ~ 10B" 增量分支 | 190-234 | 增量更新流程全文 | NFR-2 |
| 既有"过程文档格式"小节 | 236-280 | materials-index / clarifications / related-requirements / analysis-notes 模板 | NFR-2 |
| 既有"衔接"小节 | 282-285 | "## 衔接" 章节全文 | NFR-2 |
| 既有"不要做的事"小节 | 287-294 | "## 不要做的事" 章节全文 | NFR-2 |

---

## 模块 #2:`code-design` 技能(对应概要设计 §7.3 / §7.5)

### 2.1 路径与文件信息
- **路径**:`plugins/code-skills/skills/code-design/SKILL.md`
- **当前大小**:19,665 bytes
- **当前 frontmatter**(严禁改动):
  ```yaml
  ---
  name: code-design
  description: 概要设计(版本感知)。要求用户提供"需求编码",**所有产出物写入 `./assistants/<版本号>/design/<需求编码>/`**(由 `./assistants/.current-version` 决定版本号,若未设置则提示先调 `code-version`)。先读取 `./assistants/rules/` 下的项目级规范(跨版本共享),再从 `./assistants/<版本号>/require/<需求编码>/RESULT.md` 读取需求分析结果,结合项目结构与已有代码,产出符合编码规范的概要设计。若 RESULT.md 已存在则做增量更新。同步追加/更新版本看板的"概要设计清单"与"变更记录"区段。在 `code-require` 之后、`code-plan` 之前使用。
  ---
  ```

### 2.2 变更点 A:插入"步骤 0a — 拉取最新代码"

#### 2.2.1 插入位置
- **目标行号(预期)**:当前第 85 行(标题 `### 步骤 0 — 版本上下文检测`)**之前**
- **锚点字符串**:
  ```
  ## 工作流程

  ### 步骤 0 — 版本上下文检测(强制前置)
  ```
- **插入内容**:与模块 #1.2.2 步骤 0a **完全相同**,**但**不含"进入步骤 0b"那句话,改为"进入既有步骤 0 — 版本上下文检测"
- **关键差异(全文 1 处)**:
  - 步骤 1.4:`进入步骤 0b` → `进入既有"步骤 0 — 版本上下文检测"`

#### 2.2.2 关联点
- **对应概要设计**:§7.3 变更点 A
- **任务编号**:`TASK-REQ-00005-00002` 子任务

### 2.3 变更点 B:插入"步骤 N — 末尾兜底提交"

#### 2.3.1 插入位置
- **目标行号(预期)**:当前第 271 行(标题 `### 步骤 15A — 完善过程文档与汇报`)**之后**,作为文件**最后**一章
- **锚点字符串**:`## 过程文档格式` 章节**之前**

#### 2.3.2 完整插入内容(精确 Markdown)

```markdown
### 步骤 N — 末尾兜底提交(强制收尾,新增)
**所有其他步骤执行完毕后的最后一步,覆盖"过程文件 + 结果文件"。**

> 注:本步骤对**首次设计(步骤 7A–15A)** 和**增量更新(步骤 7B–10B)** 两条分支都执行,均作为收尾。

1. **收集 dirty 文件**:
   - 执行 `Bash: git status --porcelain`
   - 输出为空 → 打印 `无文件变更,跳过末尾提交`,**跳过步骤 2-4,直接退出**(NFR-4)
   - 输出非空 → 进入步骤 2

2. **暂存文件**:
   - 执行 `Bash: git add <所有 dirty 文件>`
   - 失败 → 透传 stderr,中断退出

3. **生成 commit message 预览**:
   ```
   chore(code-design): <需求编码> <设计标题>

   概要设计完成:<关键决策数> 项决策,<不变量数> 条不变量
   过程文件 N + 结果文件 1
   ```
   - `<设计标题>` 从本轮 RESULT.md 的标题提取
   - `<关键决策数>` / `<不变量数>` 从 RESULT.md §3 / §3.1 提取

4. **弹 3 选 1 确认**:同 `code-require` 步骤 N 步骤 4(Q-3 锁定 A)

5. **执行 commit**:同 `code-require` 步骤 N 步骤 5
```

#### 2.3.3 关联点
- **对应概要设计**:§7.3 变更点 B / §8.1 步骤 N
- **任务编号**:`TASK-REQ-00005-00002` 子任务

### 2.4 强约束不动的部分
- **YAML frontmatter**(1-4 行)
- **既有"步骤 0 — 版本上下文检测"**(85-88 行)
- **既有"步骤 1-14A / 15A"**(90-271 行)
- **既有"过程文档格式"小节**(273-320 行)
- **既有"7B 增量更新"分支**(275-320 行)
- **新增"步骤 0b"**:**禁止**(`code-design` 不做版本对齐)

---

## 模块 #3:`code-plan` 技能(对应概要设计 §7.4 / §7.5)

### 3.1 路径与文件信息
- **路径**:`plugins/code-skills/skills/code-plan/SKILL.md`
- **当前大小**:33,016 bytes
- **当前 frontmatter**(严禁改动):
  ```yaml
  ---
  name: code-plan
  description: 详细设计与编码计划(版本感知)。要求用户提供"需求编码"或"缺陷编号":
  - **需求编码**(如 `REQ-00001`):产出物写入 `./assistants/<版本号>/plan/<需求编码>/`,读取 `./assistants/<版本号>/require/<需求编码>/RESULT.md` + `design/<需求编码>/RESULT.md`
  - **缺陷编号**(如 `BUG-00001`):产出物写入 `./assistants/<版本号>/fix/<缺陷编号>/`(主要是 `fix-plan.md`),读取 `./assistants/<版本号>/fix/<缺陷编号>/RESULT.md` 了解缺陷内容,生成详细的缺陷修改方案
  两者都遵循 `./assistants/rules/` 跨版本规范。增量更新时,已完成任务的开发字段不可改但**测试状态可独立更新**。同步追加/更新版本看板的"详细设计与任务计划汇总" / "任务清单" / "里程碑" / "变更记录"区段(缺陷分支额外同步 `fix/RESULT.md`)。在 `code-design`(需求路径)/ `code-fix`(缺陷路径)之后、`code-it` 之前使用。
  ---
  ```

### 3.2 变更点 A:插入"步骤 0a — 拉取最新代码"

#### 3.2.1 插入位置
- **目标行号(预期)**:当前第 91 行(标题 `### 步骤 0 — 版本上下文检测`)**之前**
- **锚点字符串**:
  ```
  ## 工作流程

  ### 步骤 0 — 版本上下文检测(强制前置)
  ```
- **插入内容**:与模块 #1.2.2 步骤 0a **完全相同**,不含"进入步骤 0b"
- **关键差异**:
  - 步骤 1.4:`进入步骤 0b` → `进入既有"步骤 0 — 版本上下文检测"`
  - 步骤 0 既有逻辑不变:`code-plan` 步骤 0 原文要求"验证 `require/.../RESULT.md` + `design/.../RESULT.md` 都存在" — 步骤 0a 完成后,该验证仍由步骤 0 负责(NFR-2 增量)

#### 3.2.2 关联点
- **对应概要设计**:§7.4 变更点 A
- **任务编号**:`TASK-REQ-00005-00003` 子任务

### 3.3 变更点 B:插入"步骤 N — 末尾兜底提交"

#### 3.3.1 插入位置
- **目标行号(预期)**:作为文件**最后**一章(在"## 过程文档格式"小节**之前**)
- **对 REQ 路径 / BUG 路径都适用**:本步骤 N 不区分路径(因末尾兜底只关心"git status --porcelain"输出,不关心是 REQ 还是 BUG 路径产生的文件)
- **插入内容**:与模块 #1.4.2 步骤 N 结构相同,commit message 模板差异:
  ```
  chore(code-plan): <需求编码> <计划标题>

  详细设计与编码计划完成:<任务数> 个任务
  过程文件 N + 结果文件 2(PLAN.md + RESULT.md)
  ```

#### 3.3.2 关联点
- **对应概要设计**:§7.4 变更点 B
- **任务编号**:`TASK-REQ-00005-00003` 子任务

### 3.4 强约束不动的部分
- **YAML frontmatter**(1-8 行)
- **既有"步骤 0 — 版本上下文检测"**(91-96 行)
- **既有"步骤 1-18A / 13B"**(98+ 行)
- **既有"BUG 路径"(步骤 19-28)**
- **新增"步骤 0b"**:**禁止**(`code-plan` 不做版本对齐)

---

## 模块 #4:版本看板(对应概要设计 §7.5 / §10)

### 4.1 路径与文件信息
- **路径**:`assistants/V0.0.2/RESULT.md`
- **当前大小**:~30KB
- **本计划追加 5 处**(在 design 阶段已追加 1 次;本计划再追加 1 次,因步骤 16A 同步)

### 4.2 追加内容清单(本计划步骤 16A 同步)

| 区段 | 追加内容 | 任务 |
| --- | --- | --- |
| "需求清单" REQ-00005 行的"详细设计"列 | 填 `[RESULT.md](plan/REQ-00005/RESULT.md)` | T-004 |
| "详细设计与任务计划汇总" | 追加 1 行(REQ-00005) | T-004 |
| "任务清单" | 追加 4 行(T-001 ~ T-004) | T-004 |
| "里程碑" | 追加 1 行(M1:可发布) | T-004 |
| "变更记录" | 追加 1 条 | T-004 |

### 4.3 任务编号分配(严格遵循 `encoding-conventions.md §规则 3` 嵌套式 5+5 位)

| 任务编号 | 标题 |
| --- | --- |
| `TASK-REQ-00005-00001` | `code-require/SKILL.md` 增量追加 步骤 0a + 0b + N |
| `TASK-REQ-00005-00002` | `code-design/SKILL.md` 增量追加 步骤 0a + N |
| `TASK-REQ-00005-00003` | `code-plan/SKILL.md` 增量追加 步骤 0a + N |
| `TASK-REQ-00005-00004` | 同步 V0.0.2/RESULT.md 看板 |

> 编号依据:`encoding-conventions.md §规则 3` — `TASK-REQ-00005-NNNNN`,父级 = `REQ-00005`,序号从 00001 起。
> 注:与 V0.0.1 实践的 `REQ-00001-NN` 3 位序号不同 — V0.0.2 自 REQ-00002 起已升级为 5+5 位嵌套式(详见 `migration-mapping.md §规则 1` 落地记录)。

---

## 模块 #5:未触达模块(强约束保留)

| 技能 / 文件 | 不动依据 | 任务影响 |
| --- | --- | --- |
| `code-version/SKILL.md` | FR-5 + NFR-2 | T-001 步骤 0b 调 `code-version`,但**不修改**该文件 |
| `code-it/SKILL.md` | FR-4 + Q-4 锁定 B | T-001 ~ T-003 末尾兜底不重复 `code-it` 已 commit 的文件 |
| `code-unit/SKILL.md` | FR-6 | T-001 ~ T-003 测试状态 = `不适用`(因本仓库无构建/测试文件 — `code-unit` 步骤 0a 守卫判定) |
| `code-review/SKILL.md` | FR-6 | T-004 不在 code-review 范围;code-review 阶段评审本需求 |
| `code-fix/SKILL.md` | FR-6 | 同上 |
| `code-init/SKILL.md` | FR-6 | 同上 |
| `code-rule/SKILL.md` | FR-6 | 同上 |
| `code-dashboard/SKILL.md` | REQ-00004 边界继承 | 同上 |
| `code-publish/SKILL.md` | REQ-00006 边界继承 | 同上 |
| `code-auto/SKILL.md` | REQ-00007 边界继承 | 同上 |
| `plugins/code-skills/.claude-plugin/plugin.json` | FR-6 + `marketplace-protocol.md §规则 1.3` | T-001 ~ T-003 严禁修改 |
| `.claude-plugin/marketplace.json` | FR-6 | T-001 ~ T-003 严禁修改 |
| `plugins/code-skills/CLAUDE.md` | FR-6.AC-6.2 | T-001 ~ T-003 严禁修改(Q-10 留 follow-up) |
| `plugins/code-skills/README.md` / `README.en.md` | FR-6.AC-6.3 + `doc-conventions §规则 1` | T-001 ~ T-003 严禁修改(Q-11 留 follow-up) |
| 13 个规范文件(`assistants/rules/`) | 跨版本共享,本技能**只读** | T-001 ~ T-004 严禁修改 |

## 自检 — 强约束不动

| 检查项 | 状态 |
| --- | --- |
| 3 个 SKILL.md frontmatter 字节级保留 | ✅ `module-details §1.1 / §2.1 / §3.1` 显式列出 frontmatter |
| 既有步骤 0-15A(原)全文保留 | ✅ §1.5 / §2.4 / §3.4 强约束不动表 |
| `code-version` / `code-it` / `code-unit` / `code-review` SKILL.md 不动 | ✅ §5 未触达模块 |
| `marketplace.json` / `plugin.json` 不动 | ✅ §5 |
| `CLAUDE.md` / README*.md 不动 | ✅ §5 |
| 13 个规范文件不动 | ✅ §5 |
| 新增"步骤 0b"仅在 `code-require` | ✅ §2.4 / §3.4 显式禁止 |
| 任务编号遵循 `TASK-REQ-00005-NNNNN` 5+5 位 | ✅ §4.3 |
