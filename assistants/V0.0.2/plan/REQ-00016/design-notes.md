# 设计笔记 — REQ-00016
更新时间:2026-06-05 16:15
版本:V0.0.2

---

## 1. 概要设计 → 详细设计的转化

本详细设计在概要设计(14 章节,v1 已完成,hash `f315bbe`)的基础上,把"系统长什么样"细化为"系统怎么写"。核心细化:

| 概要设计章节 | 详细设计对应章节 | 细化要点 |
| --- | --- | --- |
| §1 设计概述 | §1 详细设计概述 | (已对齐) |
| §4.1 SKILL.md 增量追加边界 | §4.1 模块 M-1 + §11.1 SKILL.md 修改字面 | 概要设计是"插入点",详细设计**给完整字面内容** + **锚点策略** |
| §5 算法 1(模式判定) | §5 算法 1(完整伪代码) | 概要设计是"3 态机",详细设计**加优先级判断** + **非法值处理** |
| §5 算法 6(快模式末尾兜底) | §5 算法 6(完整伪代码) | 概要设计是"跳过 3 选 1 + `git add` + `git commit`",详细设计**加 `git add` 范围定义** |
| §6 快模式状态机 | §9 状态机(Mermaid 再可视化) | (已对齐) |
| §10.1 SKILL.md 修改边界 | §11.1 SKILL.md 修改字面 | 概要设计是"插入点",详细设计**给完整字面** |
| §10.4 不修改文件清单 | §11.4 0 触发 | (已对齐) |
| §11 边界 E-M1 ~ E-M10 | §7 异常处理 + §8 风险分析 | 概要设计 10 个边界,详细设计**加伪代码 + 缓解措施** |

**完全无新增模块 / 无新增依赖**;本设计是 2 个 SKILL.md 增量追加(本仓库 11 个技能中既有,本次只是新增"步骤 0.5"和"步骤 N 步骤 3.5"2 段 + 2 个 SKILL.md 的快模式分支)。

---

## 2. 关键决策(沿用概要设计 4 项 DQ + 本设计新增 PD-1 ~ PD-3)

### 沿用概要设计 4 项 DQ(全部采纳)

| DQ | 概要设计选定 | 详细设计落地 |
| --- | --- | --- |
| DQ-1 | E(环境变量 + CLI 标志组合) | §4.1 + §5 算法 1(优先级 CLI > 环境变量 > 默认) |
| DQ-2 | B(`code-design` 跳 7A-8A + 11A-12A + 13A 仅核心 + 14A 仅 1 行) | §4.1.1 + §5 算法 2-3 |
| DQ-3 | B(`code-plan` 跳 7A + 8A + 12A-13A + 14A/15A 仅核心 + 16A 仅 1 行 + 任务清单逐行 + 不追加里程碑) | §4.1.2 + §5 算法 4-5 |
| DQ-4 | A(快模式完全跳过 3 选 1 + 过程文档必含) | §4.1 + §5 算法 6(快模式末尾兜底) |

### 本设计新增 3 项 PD(关键伪代码 / 字面决策)

| PD | 标题 | 选定 | 影响 |
| --- | --- | --- | --- |
| **PD-1** | 模式选择优先级(无参 vs REQ-NNNNN vs 无效参) | **三态机 + 优先级**:`--fast` / `--full` > `CODE_FAST_MODE=1` / `0` / 未设置 > 默认 FULL | §5 算法 1 |
| **PD-2** | `code-design` 快模式 RESULT.md §1 设计概述字面 | 仅核心 4 章节 + §1 含"项目现状引用"3-5 行(沿用 D-4 决定) | §11.1 字段级 + 详 `design/REQ-00016/clarifications.md` D-4 |
| **PD-3** | `code-plan` 快模式 RESULT.md + PLAN.md 字面 | RESULT.md 仅核心 4 章节(§1 / §2 / §4 / §11) + PLAN.md 仅核心 5 章节(§1 / §2 / §4 / §5 / §8) | §11.1 字段级 |

---

## 3. 待澄清项(本设计 0 新增)

上游 `code-require` 阶段 3 轮 6 项已锁定 + 2 项采纳默认(2026-06-05 16:05);概要设计 6 项 D-1 ~ D-6 讨论结论已确认(2026-06-05 16:10);**本详细设计无新增待澄清**。

详细规范遵循记录见 `rule-compliance.md`(本目录)。

---

## 4. 不变量(沿用概要设计 INV-1 ~ INV-10 + 本设计新增 INV-11 ~ INV-13)

### 沿用概要设计 10 项 INV(全部满足)

| INV | 描述 |
| --- | --- |
| INV-1 | 完整模式 15 步骤(`code-design`)/ 18 步骤(`code-plan`)**全部执行** |
| INV-2 | 完整模式 7 份 / 8 份过程文档**全部生成** |
| INV-3 | 完整模式末尾 3 选 1 确认**完全保留** |
| INV-4 | 完整模式既有 frontmatter **字节级不变** |
| INV-5 | 完整模式既有步骤 0-15 / 0-18 字面**字节级不变** |
| INV-6 | 其他 11 个 `code-*` 技能 SKILL.md **字节级不变** |
| INV-7 | `marketplace.json` / `plugin.json` / `assistants/rules/` / README / CLAUDE.md **字节级不变** |
| INV-8 | 快模式末尾 `git add` 范围 = `git status --porcelain` 输出中与本需求相关的所有 dirty 文件 |
| INV-9 | 快模式跳过 3 选 1(完全跳过,**不**显示 AskUserQuestion) |
| INV-10 | 快模式新增的"步骤 0.5 模式选择"段位于步骤 0 之后、步骤 1 之前 |

### 本设计新增 3 项 INV(本阶段细化)

| INV | 描述 | 验证手段 |
| --- | --- | --- |
| **INV-11** | 完整模式既有 frontmatter 字节级不变(L1-3) | Edit 工具严格按锚点;Grep 自检 L1-3 字面 |
| **INV-12** | 完整模式既有步骤 0-15 / 0-18 字面字节级不变 | Edit 工具严格按锚点;Grep 自检既有步骤字面 |
| **INV-13** | 完整模式既有 5 章节字面字节级不变 | Grep 自检既有 5 章节字面 |

### 不变量自检结论

- ✅ 13 项 INV 全部满足(`code-it` 实施 T-001 + T-002 时验证)
- ✅ 0 偏离
- ✅ 0 冲突
- ✅ 0 授权偏离

---

## 5. 风险与缓解(沿用概要设计 8 类)

| 风险 | 严重度 | 缓解 |
| --- | --- | --- |
| R-1 锚点 A 错位 | 高 | Edit 严格按"步骤 0"字面;Grep 自检 INV-5 / INV-12 |
| R-2 锚点 B 错位 | 中 | Edit 严格按"步骤 N 步骤 3"字面;Grep 自检 INV-3 / INV-12 |
| R-3 过程文档遗漏 | 中 | INV-8 + NFR-2 强约束 + 算法 6 实现 |
| R-4 `git commit` 失败 | 低 | 透传 stderr + 提示用户手动处理 |
| R-5 快模式 + 完整模式混淆 | 低 | 算法 1 优先级严格(CLI > 环境变量 > 默认) |
| R-6 状态字段同步 | 低 | D-3 决定:快模式状态字段 = 完整模式,0 触发 3 处同步 |
| R-7 `code-auto` 子技能传 CLI 标志 | 中 | NFR-4 锁定:用户用环境变量启用(D-5 决定) |
| R-8 误传 `--fast` | 低 | CLI 标志优先级严格;用户**想**走完整模式**不**传 `--fast` 即可 |

### 风险总检

- 8/8 风险全部有缓解
- 0 类无缓解
