# 材料登记 — REQ-00016
更新时间:2026-06-05 16:15
版本:V0.0.2
需求编码:REQ-00016
详细设计标题:`code-design` / `code-plan` 增加"快模式"+ 末尾提交无需确认

---

## 项目级规范

> 13 个规范文件,与 `design/REQ-00016/materials-index.md` 同构,本节简表。

| 规范文件 | 状态 | 本详细设计对应章节 |
| --- | --- | --- |
| `skill-conventions.md` | ✅ 强约束 | §4.1 模块 M-1 + §4.2 INV-4(frontmatter 字节级保留) |
| `module-conventions.md` | ✅ 强约束(DEPRECATED 仍引用) | §4.1 0 新增子目录 |
| `dashboard-conventions.md` | ✅ 强约束 | §11 看板同步(0 触发 3 处同步) |
| `encoding-conventions.md` | ✅ 强约束 | §6.2 0 触发(本设计**不**产生新编码) |
| `marketplace-protocol.md` | ✅ 强约束 | §11.4 0 触发 |
| `doc-conventions.md` | ✅ 强约束 | §11.4 0 主动写 README |
| `migration-mapping.md` | ✅ 强约束(不触发) | (不触发) |
| 6 占位 | ⏸️ 占位 | (不触发) |

---

## 上游需求

- 来源:`./assistants/V0.0.2/require/REQ-00016/RESULT.md`
- 版本:v1(2026-06-05 16:05,hash `c29484a`)
- 提取:**6 FR / 10 NFR / 10 AC / 10 边界 / 6 项 Q-locked + 2 项默认 / 6 项 v1 follow-up**
- 关键交叉点:每条 FR 对应概要设计 §10.1(锚点 A/B 字面)

---

## 上游概要设计

- 来源:`./assistants/V0.0.2/design/REQ-00016/RESULT.md`
- 版本:v1(2026-06-05 16:10,hash `f315bbe`)
- 状态:**已完成(首次概要设计)**
- 提取:**2 修改 + 0 新增 + 5 复用 = 7 模块**
- 关键交叉点:
  - 概要设计 §10.1 SKILL.md 增量追加(2 段:步骤 0.5 模式选择 + 步骤 N 步骤 3.5 模式分支)→ 本设计 §4.1 详细化
  - 概要设计 §5 算法 1(模式判定)+ 算法 6(快模式末尾兜底)→ 本设计 §5 算法 1-6
  - 概要设计 §4.2 INV-1 ~ INV-10 → 本设计沿用 + 新增 3 项 INV(INV-11/12/13)
  - 概要设计 6 项 D-1 ~ D-6 讨论结论 → 本设计沿用
  - 概要设计 §10.4 不修改文件清单 → 本设计 §11.4 沿用
  - 概要设计 §11 边界 E-M1 ~ E-M10 → 本设计 §7 异常处理

---

## 项目现状(实现细节 — 锚定 SKILL.md 既有字面)

### 既有 `code-design` SKILL.md 关键锚点

> `code-design` SKILL.md 既有 15 步骤(0a / 0 / 1-6 / 7A-15A / N);本设计**仅**在"步骤 0"末尾插入"步骤 0.5 模式选择" + 在"步骤 N 步骤 3"末尾插入"步骤 N 步骤 3.5 模式分支判断"。

- **步骤 0 末尾锚点**(插入"步骤 0.5 模式选择"):
  - 既有 1-3 个步骤(实际字数需 `code-it` 实施时 Read 全文确认;锚点策略与 REQ-00008 锚点 A 模式一致)
- **步骤 N 步骤 3 末尾锚点**(插入"步骤 N 步骤 3.5"):
  - 既有步骤 N 步骤 3 是 "git add <所有 dirty 文件>",步骤 4 是 "弹 3 选 1 确认",步骤 5 是 "git commit"

### 既有 `code-plan` SKILL.md 关键锚点

> `code-plan` SKILL.md 既有 18 步骤;本设计**仅**在"步骤 0"末尾 + "步骤 N 步骤 3"末尾各插入 1 段。

- 锚点策略:与 `code-design` 一致

### 既有 frontmatter(2 个文件共享)

- `name: code-design` / `name: code-plan` — 字节级保留(INV-4)
- description 字段 — 字节级保留

### 派生任务追加格式

> 本设计**不**产生新任务编码;`code-plan` 既有规则不变(`encoding-conventions §规则 4`)

### 关联编码计划(本版本其他)

| 关联计划 | 关联点 |
| --- | --- |
| `plan/REQ-00008/`(V0.0.2) | `code-review` 整版本模式,本设计**不**触发 |
| `plan/REQ-00007/`(V0.0.2) | `code-auto` 编排,本设计**不**触发 `code-auto` 升级 |
| `plan/REQ-00014/`(V0.0.2) | "按功能点拆"规则,本设计**沿用**(4 任务 = 4 功能点) |
| `plan/REQ-00016/`(本轮) | (本设计) |

---

## 本次变更源(本设计为首次,无变更源)

| 变更源 | 变化列表 |
| --- | --- |
| 需求侧 | **0**(本设计为 v1 首次) |
| 概要设计侧 | **0**(本设计为 v1 首次) |
| 规范侧 | **0**(13 文件未变) |
| 代码侧 | **0**(`code-design` / `code-plan` SKILL.md 既有未变;既有的 2 模板 + 1 清单 + 5 关联 design 文档未变) |
