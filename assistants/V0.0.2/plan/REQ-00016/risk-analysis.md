# 风险分析 — REQ-00016
更新时间:2026-06-05 16:15
版本:V0.0.2

---

## 1. 异常处理

### 异常路径 1:`--fast` 与 `--full` 同时传
- **触发条件**:CLI 解析冲突
- **处理**:报错"互斥" + 提示用法
- **退出码**:非 0(E-M1)

### 异常路径 2:`CODE_FAST_MODE` 值非法
- **触发条件**:环境变量值不在合法集合内
- **处理**:走完整模式 + 警告"忽略 CODE_FAST_MODE=<value>"
- **退出码**:0(E-M2)

### 异常路径 3:快模式下"非必要步骤"实际产生过程文档写入失败
- **触发条件**:Write 工具异常
- **处理**:透传错误 + 中断
- **退出码**:非 0(E-M3)

### 异常路径 4:快模式末尾 `git add` 失败
- **触发条件**:权限 / 磁盘满
- **处理**:透传 stderr + 中断
- **退出码**:非 0(E-M4)

### 异常路径 5:快模式末尾 `git commit` 失败
- **触发条件**:pre-commit hook / 其他
- **处理**:透传 stderr + 提示"末尾提交失败,文件已暂存"
- **退出码**:非 0(E-M5)

### 异常路径 6:完整模式行为未变
- **触发条件**:既有边界(沿用)
- **处理**:沿用既有 11 个 `code-*` 边界
- **退出码**:沿用

### 异常路径 7:`code-auto` 驱动时本需求 NFR-4 不自动启用快模式
- **触发条件**:`code-auto` 不设置 `CODE_FAST_MODE`
- **处理**:走完整模式(默认)
- **退出码**:0(E-M8)

### 异常路径 8:用户在 `code-auto` 调用前已设置 `CODE_FAST_MODE=1`
- **触发条件**:环境变量传递到子技能
- **处理**:子技能走快模式
- **退出码**:0(E-M9)

### 异常路径 9:快模式下不写的过程文档用户期望保留
- **触发条件**:不可逆
- **处理**:提示"快模式不写 <file>"
- **退出码**:0(E-M10)

### 异常路径总检
- 9 条异常路径(1-5 + 7-9 沿用概要设计 E-M1 ~ E-M10 的子集;6 为既有)
- 全部有处理策略
- 退出码语义:`0` = 成功(含非阻塞警告) / 非 0 = 致命错误

---

## 2. 安全边界

### 鉴权要求
**N/A**(本需求不涉及鉴权;`code-design` / `code-plan` 技能继承 Claude Code 平台用户身份)

### 输入校验
- `--fast` / `--full` CLI 标志严格识别
- `CODE_FAST_MODE` 环境变量值映射(大小写不敏感)
- `REQ-NNNNN` 编码沿用既有 `^REQ-\d{5}$` 正则

### 敏感数据处理
**N/A**(本需求不处理敏感数据)

### 注入防护
**N/A**(本需求不处理用户输入 SQL / shell / 反序列化)

### 跨站请求伪造(CSRF)
**N/A**(本需求无 web 入口)

### 依据规范
- 既有 `code-design` / `code-plan` 步骤 8.4 安全检查项(沿用):输入校验充分 / 鉴权 / 敏感数据 / SQL 注入 / 命令注入 / 反序列化 / XSS / 依赖漏洞 / CSRF / 越权
- 本需求的安全边界 = 既有边界(0 增量)

---

## 3. 性能与资源

### 关键路径预估
- **步骤 0.5 模式选择**:`O(1)`(纯字符串匹配)
- **快模式跳过步骤 7A-8A + 11A-12A**(`code-design`)/ **跳 7A + 8A + 12A-13A**(`code-plan`):节省 ~30-60 秒
- **快模式末尾兜底提交**:与完整模式相同耗时(~5-10 秒)
- **总耗时预估**:`O(M × T + K)`,M ≤ 12(V0.0.2 当前规模),T ≤ 10(单需求典型),K ≤ 20
- **预期 wall clock**:
  - 完整模式:5-10 分钟(典型)
  - 快模式:1-3 分钟(节省 ~50%)

### 资源限制
- **磁盘 I/O**:本需求**不**新增过程文档(快模式减少过程文档)
- **内存**:**N/A**(LLM 现场维护,无内存状态)
- **网络**:**0 个**网络调用

### 缓存策略
**N/A**(本需求不引入缓存)

### 并发
**N/A**(本需求**不**引入并发)

### 性能自检
- ✅ 0 网络 I/O
- ✅ 0 内存状态
- ✅ 0 并发
- ✅ 关键路径 `O(M × T + K)`,V0.0.2 规模下 < 5 分钟(完整模式) / < 3 分钟(快模式)
- ✅ 磁盘占用可忽略

---

## 4. 回退策略

### 触发条件
- SKILL.md 增量追加错位(R-1 / R-2)
- 派生任务追加位置错乱(本需求**不**涉及)
- 聚合文件写入失败(本需求**不**涉及)

### 回退步骤
1. **SKILL.md 增量追加错位**:`Edit plugins/code-skills/skills/code-design/SKILL.md` 移除"步骤 0.5 模式选择" + "步骤 N 步骤 3.5 模式分支判断" 2 段;同样处理 `code-plan/SKILL.md`
2. **完整模式行为被破坏**:`git checkout -- plugins/code-skills/skills/code-design/SKILL.md`(若严重错位,直接回退到上次成功状态)
3. **回退到完整模式**:`unset CODE_FAST_MODE` / 重新调 `/code-design REQ-NNNNN`(无 `--fast`)

### 回退验证
- `git diff --stat` 检查本次变更范围
- `git checkout -- <file>`(若严重错位)
- 重新调完整模式验证

### 不可回退的操作
**无**。本需求所有修改都可通过 `git checkout -- <file>` 回退

### 依据规范
- INV-4 / INV-5 / INV-12(frontmatter / 既有字面字节级不变 → 错位可被 Grep 立即识别)
- 概要设计 NFR-3(幂等 → 多次执行覆盖)

---

## 5. 测试要点

### 单元测试
**N/A**(本需求不涉及代码;沿用既有 11 个 `code-*` 技能的"纯文档型"实践,测试状态=不适用)

### 集成测试
**N/A**(本仓库无构建/测试框架;REQ-00009 守卫判定"不可测")

### 端到端(E2E)测试
- **场景 1**(S-1 完整模式):`/code-design REQ-NNNNN` (无 `--fast`,`CODE_FAST_MODE` 未设置) → 走完整模式 → 写 7 份过程文档 + 14 章节 RESULT.md + 同步看板多区段 + 末尾 3 选 1 确认
- **场景 2**(S-2 快模式):`CODE_FAST_MODE=1 /code-design REQ-NNNNN` → 走快模式 → 写 3 份过程文档 + 4 章节 RESULT.md + 同步看板 1 行 + 末尾跳过 3 选 1 直接 commit
- **场景 3**(S-3 CLI 覆盖):`/code-design --full REQ-NNNNN` (即使 `CODE_FAST_MODE=1`) → 走完整模式(CLI 覆盖环境变量)
- **场景 4**(S-4 互斥):`/code-design --fast --full REQ-NNNNN` → 报错"互斥"(E-M1)
- **场景 5**(S-5 非法值):`CODE_FAST_MODE=abc /code-design REQ-NNNNN` → 警告后走完整模式(E-M2)
- **场景 6**(S-6 `code-auto` 协同):`code-auto` 调 `/code-design REQ-NNNNN`(无 `CODE_FAST_MODE`) → 子技能走完整模式(NFR-4)
- **场景 7**(`code-auto` 协同 + 环境变量):`CODE_FAST_MODE=1 /code-auto "<需求内容>"` → 子技能走快模式(NFR-4 + D-5)

### 性能/安全测试
**N/A**(本需求不涉及性能敏感或安全敏感操作)

### 不变量自检(由 `code-it` 任务 T-004 末尾执行)
- 13/13 INV 全部通过
- 0 偏离
- 0 冲突
- 0 授权偏离

### 测试结果预期
- 13/13 不变量自检 100% 通过
- 0 偏离
- 0 冲突
- 0 授权偏离
