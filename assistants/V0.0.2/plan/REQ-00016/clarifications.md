# 澄清记录 — REQ-00016
更新时间:2026-06-05 16:15
版本:V0.0.2

---

## 概览

本详细设计阶段**未产生新增澄清**(因为:
1. 上游 `code-require` 阶段 3 轮 6 项已锁定 + 2 项采纳默认(2026-06-05 16:05)
2. 概要设计 6 项 D-1 ~ D-6 讨论结论已确认(2026-06-05 16:10)
3. 13 项不变量 + 8 类风险全部已锁定,无新歧义点

上游澄清记录见 `./assistants/V0.0.2/require/REQ-00016/clarifications.md`(v1,2026-06-05 16:05 锁定)。
概要设计澄清记录见 `./assistants/V0.0.2/design/REQ-00016/clarifications.md`(v1,2026-06-05 16:10)。

---

## 本详细设计阶段(2026-06-05 16:15)

### 讨论项 P-1:`code-design` SKILL.md 步骤 0 字面锚点

- **问题**:本详细设计需要锁定"步骤 0 末尾"插入"步骤 0.5 模式选择"的具体字面锚点
- **决定**:`code-it` 实施 T-001 时,**Read** `code-design/SKILL.md` 全文 + 用 Grep 找"步骤 0"实际字面 + Edit 严格按锚点(类似 REQ-00008 锚点 A 模式)
- **理由**:
  1. 本详细设计**不**重写概要设计已确定的"步骤 0.5"字面(概要设计 §10.1 已锁定)
  2. `code-it` 实施时**必须** Read 既有 SKILL.md 全文确认锚点字面精度
  3. INV-12 强约束:既有步骤 0-15 字面字节级不变
- **影响**:T-001 实施时必须 Read SKILL.md 全文 + Grep 自检 INV-12

### 讨论项 P-2:4 任务 vs 5 任务的决定

- **问题**:本设计是 4 任务(T-001 / T-002 / T-003 / T-004)还是 5 任务?
- **选项**:
  - **A. 4 任务**:T-001 增量改 `code-design` SKILL.md / T-002 增量改 `code-plan` SKILL.md / T-003 同步 V0.0.2 看板 5 处 / T-004 13 项不变量自检 + 收尾
  - **B. 5 任务**:T-001 / T-002 / T-003 文档同步 / T-004 文档同步 / T-005 13 项不变量自检
- **决定**:**A(4 任务)**
- **理由**:
  1. 看板同步(详细设计汇总 + 任务清单 + 里程碑 + 文档头 + 变更记录)是 1 个完整功能点,**不**应拆为 2 个任务
  2. 13 项不变量自检 + 偏差日志 + 收尾是 1 个完整功能点(沿用 V0.0.2 既有实践)
  3. 符合"按功能点拆"原则
- **影响**:PLAN.md §2 任务总览 = 4 任务(1 修改 + 1 修改 + 1 文档 + 1 文档)

### 讨论项 P-3:本轮是否插入"架构任务"作为首个任务

- **问题**:按 REQ-00014 新规则,本需求是否满足"架构任务触发条件"?
- **决定**:**不需要架构任务**
- **理由**(对照 REQ-00014 触发条件):
  - **触发 1**(较高扩展性 + 较高复杂度):❌ 不满足 — 本需求是"在 2 个既有 SKILL.md 增量追加 2 段",**不**涉及"较高扩展性"
  - **触发 2**(未来可能的不同实现方式):❌ 不满足 — 既有 `code-design` / `code-plan` 即"既有架构",**不**为未来切换预留扩展点
  - **触发 3**(对接不同三方组件):❌ 不满足 — 本需求**不**对接任何新三方
- **影响**:PLAN.md §3 任务总览 = 4 任务而非 5 任务(无需架构任务作为首个)

### 讨论项 P-4:测试状态字段

- **问题**:本设计 4 任务的测试状态字段值
- **决定**:**全部 `不适用`**(沿用 V0.0.2 既有 11 个 `code-*` 实践;本仓库无构建/测试文件,REQ-00009 守卫判定"不可测")
- **理由**:
  1. 4 任务都是纯文档型(2 修改 SKILL.md + 1 同步看板 + 1 自检收尾)
  2. 沿用 REQ-00008 / REQ-00014 既有 11 个 `code-*` 实践
  3. 无传统单元测试载体
- **影响**:PLAN.md §2 任务总览 4 行的"测试状态"列 = `不适用` × 4

---

## 关联项(影响章节)

| 讨论项 | 影响的章节 |
| --- | --- |
| P-1 | §4.1 模块 M-1(锚点 A 字面精度)+ §11.1 SKILL.md 修改字面 |
| P-2 | §3 任务总览(4 任务) + §4 任务详情 |
| P-3 | §3 任务总览(0 架构任务) + §10 不变量自检 |
| P-4 | §4 任务详情(4 任务测试状态 = `不适用`) |
