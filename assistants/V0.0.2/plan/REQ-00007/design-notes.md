# 设计笔记 — REQ-00007(详细设计阶段)

更新时间:2026-06-05 09:55
版本:V0.0.2

## 概要

本详细设计在概要设计的基础上,把"系统长什么样"细化为"系统怎么写"。重点:
- **5 个任务全部为纯文档型**(写 SKILL.md + 改 README + 改 marketplace.json + 同步看板)
- **0 个代码模块**:`code-auto` 本身是单文件 SKILL.md,无任何 TypeScript / Python / Go 代码
- **0 个新依赖**:完全复用 Claude Code 平台工具集
- **0 个被修改模块**:严格遵循 FR-8.AC-8.1

## 与概要设计的关键差异

**无差异**。本详细设计是概要设计的"展开",无新增模块、无新依赖、无新接口(6 个子技能仍是既有)。

## 详细化要点(逐模块)

### M-1 `code-auto` SKILL.md(由 T-001 实施)

概要设计 §7.2.3 列出的 15 章节 + 行数预算,在本设计中**精确化**:

| 章节 | 行数预算 | 本设计精确化要点 |
| --- | --- | --- |
| §1 目标 | 30 | 一句话讲清"做什么、何时用"(与 code-publish §1 风格一致) |
| §2 适用场景 | 40 | 列 5 个适用场景 + 5 个不适用场景 |
| §3 工作目录约定 | 30 | 含 E-1 缺失版本处理 |
| §4 输入与输出 | 50 | 输入格式严格化:1 字符串 = 完整需求内容;输出格式 = 屏幕进度 + 屏幕报告 + 完成时磁盘报告 |
| §5 状态机总览 | 80 | Mermaid 状态机 + 文字说明(沿用概要设计 §5.1) |
| §6 子技能调用表 | 80 | 表格(7 步 × 4 列:子技能 / 输入 / 期望产物 / 失败处理) |
| §7 工作流步骤 | 200 | 步骤 0a / 0 / 1-3 / 4 / 5-6 / 7 详细描述;每步含"做什么+怎么做+失败处理" |
| §8 数据解析 | 50 | PLAN.md 任务总览解析规则 + REVIEW-REPORT.md"必须改"解析规则(锚点显式) |
| §9 中断与异常 | 40 | SIGINT + 子技能退出码 + 报告留痕(E-1 ~ E-13) |
| §10 报告输出 | 40 | 屏幕报告格式 + 磁盘报告格式(参照本设计 §6.1) |
| §11 边界与异常 | 30 | E-1 ~ E-13 表格(沿用需求 §9) |
| §12 上下游衔接 | 30 | 上游=code-version;下游=code-dashboard / code-publish |
| §13 关联需求 | 20 | REQ-00004 / 05 / 06 简述 |
| §14 工具使用约定 | 30 | Skill / Read / Write / Bash 工具调用模式 |
| §15 变更记录 | 5 | 留空 |
| **合计** | **~755** | (实际由 T-001 调整) |

### M-2 `auto-report.md`(由 T-001 在 step 7 完成后 Write)

详见 `interface-specs.md §1.3` + `data-changes.md §1.1`。

## 关键设计决策回顾(沿用概要设计 D-1~D-7)

| # | 决策 | 本设计落地位置 |
| --- | --- | --- |
| D-1 | `Skill` 工具串行 | M-1 §7 步骤 1-6 |
| D-2 | 选推荐项 = prompt 约束 | M-1 §6.3(注入到子技能 prompt 模板) |
| D-3 | 解析 `REVIEW-REPORT.md` 评审发现汇总 | M-1 §8.2 |
| D-4 | 单文件技能,无子目录 | T-001 任务范围 |
| D-5 | 子技能零修改,无显式契约 | T-001 任务范围(零修改 11 个 SKILL.md) |
| D-6 | auto-report 写入失败警告不中断 | M-1 §7 步骤 7.3 |
| D-7 | SIGINT 不写 auto-report.md | M-1 §7 步骤 7.2 |

## 详细化不变量(任务实施时严守)

1. **SKILL.md frontmatter 字节级保留 `name: code-auto` + 完整 description**(skill-conventions §规则 1)
2. **6 个子技能 SKILL.md 字节级保留**(FR-8.AC-8.1)
3. **marketplace.json `plugins[].skills` 数组追加 1 项,排序按字母序**
4. **中英 README "主要能力"段同步追加 1 行**(doc-conventions §规则 1)
5. **`auto-report.md` 仅在完成时 Write,异常/中止/自身崩溃时均不写**(NFR-7)
6. **看板 5 处同步**:详细设计与任务计划汇总 + 任务清单(5 行) + 里程碑(2 个) + 文档头"最近更新" + 变更记录(1 行)

## 任务拆分原则(本设计)

- **任务粒度**:0.1 ~ 0.5 天/任务(本仓库为文档型,粒度可细)
- **任务顺序**:SKILL.md(T-001) → marketplace.json 追加(T-002) → 中英 README 追加(T-003) → 看板同步 + 自检(T-004) → 文档收尾(T-005)
- **可独立验证**:每条任务都有"完成定义"(frontmatter 字节级保留 / JSON Schema 校验 / 中英同次提交 / 看板 5 处一致)

## 风险与缓解(任务级)

| 风险 | 影响 | 缓解 |
| --- | --- | --- |
| T-001 SKILL.md 行数偏差 | 影响可读性 | T-005 自检章节完整性 + 行数偏差在 ±20% 内 |
| T-002 marketplace.json 数组追加错位 | Claude Code 加载失败 | 严格按字母序插入;同次 commit;JSON Schema 校验 |
| T-003 中英 README 漂移 | 违反 doc-conventions §规则 1 | 中英 H2 数量对仗自检;同次 commit |
| T-004 看板 5 处不同步 | 看板数据不一致 | 显式列出 5 处;T-004 一次性同步 |
| T-005 自检遗漏 | 隐性不变量违反 | 自检 8 项:frontmatter / 6 子技能零修改 / marketplace / 中英 README / 看板 / 字节数 / 字符数 / 章节数 |
