# REQ-00019 — 优化 `/code-plan`,BUG 模式产出物与 REQ 模式同构(详细设计)

- 需求编码:REQ-00019
- 所属版本:V0.0.2
- 上游需求:`./assistants/V0.0.2/require/REQ-00019/RESULT.md` (v1)
- 上游概要设计:`./assistants/V0.0.2/design/REQ-00019/RESULT.md` (v1)
- 遵循规范:`./assistants/rules/` 下 13 个文件(`skill-conventions.md` / `module-conventions.md` / `dashboard-conventions.md` / `encoding-conventions.md` / `dependency-conventions.md` / `marketplace-protocol.md` / `commit-conventions.md` / `doc-conventions.md` / `naming-conventions.md` / `directory-conventions.md` / `coding-style.md` / `framework-conventions.md` / `migration-mapping.md`)
- 状态:已锁定
- 责任人:wangmiao
- 创建:2026-06-06
- 最近更新:2026-06-06 15:30
- 当前版本:v1

## 设计目标

- 整体设计目标:`--minimal`(沿用 `design/REQ-00019/RESULT.md` §设计目标)
- 维度优先级:
  - 功能性:**中**(必须满足 8 项 FR,沿用既有能力)
  - 扩展性:**—**(本轮不引入新抽象层;沿用 V0.0.2 既有的"任务清单解析锚点"模式)
  - 健壮性:**中**(对 BUG-00001 历史 `fix-plan.md` 退化兼容)
  - 可维护性:**—**(2 文件改造,纯文档,0 新增维护负担)

## 1. 详细设计概述

本详细设计在概要设计的基础上,**把 5 项关键设计决策 D-1~D-5 落地为可直接编码的 2 个 SKILL.md 改造点**:`code-plan/SKILL.md` 步骤 19-28A 重构 + 步骤 28A+1 新增,`code-it/SKILL.md` 步骤 17-25 改造 + frontmatter L5 修订。**0 新增模块,0 新增依赖,0 新增资源文件,0 触发 `dashboard-conventions §规则 1` 三同步**。

**任务数**:2 个(沿用 REQ-00010 / REQ-00009 既有 2 任务 + 2 里程碑 + 13 INV 结构)。

**关键算法/数据结构/接口决策**:
- 决策 D-1:`code-plan` 步骤 19-28A 重构范围(8 子段)+ 步骤 28A+1(1 子段新增)
- 决策 D-2:`code-it` 步骤 17-25 改造范围(6 子段:frontmatter L5 + 步骤 17 + 步骤 22-25)
- 决策 D-3:BUG 任务编号 `TASK-BUG-NNNNN-NNNNN`(5+5 位嵌套式,沿用既有 `encoding-conventions §规则 1/3`)
- 决策 D-4:BUG 任务进原"任务清单"区段(沿用既有 12 列字段;类型=`修复`;触发/来源=`缺陷修复`)
- 决策 D-5:BUG-00001 不迁移 + E-1/E-7/E-9/E-11 4 处历史兼容检测

## 2. 上游引用

- **需求**:`./assistants/V0.0.2/require/REQ-00019/RESULT.md` (v1)
  - 关键摘录:8 FR / 11 NFR / ~30 AC / 11 INV;Q-1 ~ Q-7 7 项 Q-locked
- **概要设计**:`./assistants/V0.0.2/design/REQ-00019/RESULT.md` (v1)
  - 关键摘录:5 关键设计决策 D-1~D-5;5 备选方案(全部否决);7 INV;10 风险 R-1~R-7
- **规范**:`./assistants/rules/` 13 份;0 冲突 0 偏离 0 授权

## 3. 规范遵循

| 规范 | 自检 | 影响章节 |
| --- | --- | --- |
| `skill-conventions §规则 1` | ✅ | §4 / §5 / §6 — frontmatter `name` 字段**字节级保留**;L5 description 段可改(本需求改 `code-it` L5) |
| `module-conventions §规则 1` | ✅ | §4 / §5 — SKILL.md 在技能根目录;`templates/fix-plan.md` 留作历史不删 |
| `dashboard-conventions §规则 1` | ✅ | §4 模块 1 步骤 28A+1 / §5 数据结构 — 0 触发(沿用既有 12 列 + 既有 6 类型 + 既有 13 触发/来源) |
| `encoding-conventions §规则 1/3` | ✅ | §4 模块 1 步骤 10A / §5 数据结构 — 5+5 位嵌套式沿用;`TASK-BUG-NNNNN-NNNNN` 与 `TASK-REQ-NNNNN-NNNNN` 风格一致 |
| `dependency-conventions` | ✅ | §7 — 0 新增依赖 |
| `marketplace-protocol` | ✅ | §4 / §7 — 0 改 `marketplace.json` / `plugin.json` |
| `commit-conventions` | ✅ | §13(变更记录)— 沿用 `chore(<scope>): <subject>` |
| `doc-conventions` | ✅ | §4 / §7 — 0 改中英 README |
| `naming-conventions` | ✅ | §5 — 0 新增文件名前缀规则;`TASK-BUG-` 沿用 `TASK-` 已有规则 |
| `directory-conventions` | ✅ | §5 — 过程文档摆放在 `fix/<BUG-NNN>/` 根目录 |
| `coding-style` | ✅ | §4 / §5 — 沿用既有 SKILL.md 风格 |
| `framework-conventions` | — | — 不涉及 |
| `migration-mapping` | — | — 不涉及 |

**总览**:13 份规范全部严守;0 冲突 0 偏离 0 授权。

## 4. 模块详细化

### 模块 1:`code-plan/SKILL.md` 步骤 19-28A+1(对应概要设计 §7 模块 1)

#### 关键类/函数

- **章节**:`## 缺陷分支(从步骤 1.2 判定为 BUG-NNN 时走)`(L588-735)
- **关键子节**:
  - `### 步骤 19 — 定位 / 创建工作目录`(L597-600)
  - `### 步骤 20 — 读取缺陷上下文`(L602-609)
  - `### 步骤 21 — 读取项目级规范(同 REQ 步骤 3)`(L611-613)
  - `### 步骤 22 — 探索项目代码(同 REQ 步骤 5)`(L615-621)
  - `### 步骤 23 — 检查 fix-plan.md 是否存在`(L623-627)**— 本需求修订**
  - `### 步骤 24A — 首次规划:根因定位 + 修复方案`(L629-653)**— 本需求重写**
  - `### 步骤 25A — 与用户对齐(可选但推荐)`(L655-659)
  - `### 步骤 26A — 撰写 fix-plan.md`(L661-676)**— 本需求重写(不再写 fix-plan.md)**
  - `### 步骤 27A — 同步 fix/<BUG-NNN>/RESULT.md 与 fix/RESULT.md(强制)`(L678-695)
  - `### 步骤 28A — 同步版本看板(强制)`(L697-706)
  - `### 步骤 29 — 完善过程文档与汇报`(L727-734)
- **新增子节**(本需求):
  - `### 步骤 23+ — 步骤 23 修订 E-1 边界`(本需求追加)
  - `### 步骤 24A+1 — 产出 9 份文档同构于 REQ 模式`(本需求追加)
  - `### 步骤 26A+1 — 撰写 9 份文档的章节结构`(本需求追加)
  - `### 步骤 28A+1 — 同步版本看板'任务清单'区段`(本需求追加)

#### 内部状态

- **既有 `## 缺陷分支` 段文字**(L589-595)的"关键简化"列表:
  - 既有第 2 条:"故 `RESULT.md` 简化为 `fix-plan.md`(单文件)" → **本需求修订**:改为"故产出物升级为 `RESULT.md` + `PLAN.md` + 7 份过程文档,与 REQ 模式完全同构"
  - 既有第 3 条:"不需要拆分为多个 `PLAN.md` 任务" → **本需求修订**:改为"若修复跨多步,可在 `PLAN.md` 任务总览中拆分为多个 `TASK-BUG-NNNNN-NNNNN` 任务(从 `TASK-BUG-NNNNN-00001` 起递增)"
  - 既有第 4 条:"任务编号可省略" → **本需求修订**:改为"BUG 任务沿用既有 `TASK-...` 编号体系,新格式 `TASK-BUG-NNNNN-NNNNN`(5+5 位嵌套式)"

#### 关键调用顺序

1. `code-plan` 步骤 1.2 判定为 `BUG-NNN`(沿用既有,`code-plan/SKILL.md` L243-244)
2. 步骤 19 创建/复用 `fix/<BUG-NNN>/` 目录
3. 步骤 20 读 `fix/<BUG-NNN>/RESULT.md` + 读 `fix/<BUG-NNN>/investigation.md`(若有)
4. 步骤 21 读 `./assistants/rules/` 13 份规范
5. 步骤 22 探索项目代码(为修复方案提供实现约束)
6. 步骤 23 检测 `fix-plan.md` 存在性 → **E-1 边界**(本需求新增)
7. 步骤 24A 根因定位 + 修复方案设计 + 测试方案 + 风险与回退 → **重写**(本需求)
8. 步骤 25A 用户对齐(可选)
9. 步骤 26A 撰写 `fix/<BUG-NNN>/RESULT.md` + `PLAN.md` + 7 份过程文档 → **重写**(本需求)
10. 步骤 27A 同步 `fix/<BUG-NNN>/RESULT.md` + `fix/RESULT.md`(既有,沿用)
11. 步骤 28A 同步版本看板"缺陷清单"区段(既有,沿用)
12. **步骤 28A+1 同步版本看板"任务清单"区段(本需求新增)**
13. 步骤 29 完善过程文档与汇报(既有,沿用)

#### 并发模型

不涉及(纯文档同步操作,无并发风险)。

#### 资源管理(连接/锁/缓存)

不涉及(纯本地文件 I/O,无外部资源)。

#### 错误处理范式

- 步骤 23 E-1 边界:`fix-plan.md` 存在 → 提示"检测到历史 fix-plan.md,本需求已不再生成该文件;是否继续以本缺陷的 fix-plan.md 为参考?" + 3 选 1
- 步骤 26A 撰写文档失败 → 中断 + 报错(沿用既有)
- 步骤 28A+1 看板同步失败 → 屏幕输出 `⚠` 警告(沿用既有)

#### 日志埋点

- 步骤 26A 完成后屏显:"完成: BUG-NNNNN · <缺陷标题>(拆 N 个任务)"
- 步骤 28A+1 完成后屏显:"已同步 V0.0.2/RESULT.md 任务清单 +N 行"

#### 依据规范

- `skill-conventions §规则 1` — frontmatter `name` 字段字节级保留
- `module-conventions §规则 1` — SKILL.md 在技能根目录
- `dashboard-conventions §规则 1` — 0 触发(步骤 28A+1 仅追加行,不修改字段/枚举/区段)
- `encoding-conventions §规则 1/3` — 5+5 位嵌套式

### 模块 2:`code-it/SKILL.md` 步骤 17-25 + frontmatter L5(对应概要设计 §7 模块 2)

#### 关键类/函数

- **frontmatter L5 description**(L5):
  - 既有第 2 条:"所有产出物写入 `./assistants/<版本号>/fix/<缺陷编号>/`(以 `fix-` 前缀命名的过程文档),从 `./assistants/<版本号>/fix/<缺陷编号>/RESULT.md` 读取缺陷详情,从 `./assistants/<版本号>/fix/<缺陷编号>/fix-plan.md` 读取修复方案" → **本需求修订**:改为"所有产出物写入 `./assistants/<版本号>/fix/<缺陷编号>/`(主详细设计 `RESULT.md` + 任务列表 `PLAN.md`,沿用 REQ 路径同构产出),从 `./assistants/<版本号>/fix/<缺陷编号>/RESULT.md` 读取缺陷详情,从 `./assistants/<版本号>/fix/<缺陷编号>/PLAN.md` 读取修复任务列表"
- **章节**:`## 缺陷分支(从步骤 1.2 判定为 BUG-NNN 时走)`(L638-800)
- **关键子节**:
  - `### 步骤 17 — 校验缺陷与修复方案存在`(L646-659)**— 本需求修订**
  - `### 步骤 18 — 定位 / 创建工作目录`(L661-665)
  - `### 步骤 19 — 读取项目级规范(同任务分支步骤 4)`(L667-669)
  - `### 步骤 20 — 探索项目代码(同任务分支步骤 6)`(L671-675)
  - `### 步骤 21 — 处理缺陷状态与本轮起点`(L677-686)
  - `### 步骤 22 — 实施修复`(L688-709)**— 本需求修订(过程文档路径)**
  - `### 步骤 23 — 编译 / 启动 / 测试验证(同任务分支步骤 9-12)`(L711-755)**— 本需求修订(过程文档路径)**
  - `### 步骤 24 — 同步 fix/<缺陷编号>/RESULT.md 与看板(强制)`(L757-787)**— 本需求修订(不再写 fix-plan.md)**
  - `### 步骤 25 — 完善过程文档与汇报`(L789-800)**— 本需求修订(汇报字段)**

#### 内部状态

- **既有"关键简化"列表**(L641-643):
  - 既有第 1 条:"`code-plan` 已产出 `fix-plan.md`" → **本需求修订**:改为"`code-plan` 已产出 `RESULT.md` + `PLAN.md`(同构 REQ 模式)"
  - 既有第 2 条:"过程文档以 `fix-` 前缀命名" → **本需求修订**:改为"过程文档以 `TASK-BUG-` 任务编号命名(沿用任务路径同套命名);原 `fix-` 前缀退场(本需求后不再使用)"
  - 既有第 3 条:"实施完成时同步推进缺陷状态" → 沿用

#### 关键调用顺序

1. `code-it` 步骤 1.2 判定为 `TASK-BUG-...` 任务编码(沿用既有)
2. 步骤 17 校验 + 读 `PLAN.md` + `RESULT.md` → **修订**(本需求)
3. 步骤 18 定位工作目录
4. 步骤 19 读规范
5. 步骤 20 探索代码
6. 步骤 21 推进状态
7. 步骤 22 实施 + 写 `work-log.md` → **修订**(过程文档去 `fix-` 前缀)
8. 步骤 23 编译启动测试 + 写 `compile-and-run.md` / `test-results.md` → **修订**
9. 步骤 24 同步 `fix/<BUG>/RESULT.md` + `fix/RESULT.md` + 看板"缺陷清单" + (若有 BUG 任务)看板"任务清单" → **修订**(不再写 `fix-plan.md`)
10. 步骤 25 汇报 → **修订**(汇报字段)

#### 并发模型

不涉及(纯文档同步操作,无并发风险)。

#### 资源管理(连接/锁/缓存)

不涉及。

#### 错误处理范式

- 步骤 17 校验失败:
  - 缺 `RESULT.md` → "请先调 `code-fix <缺陷编号>` 登记缺陷"
  - 缺 `PLAN.md` → "请先调 `code-plan <缺陷编号>` 规划修复方案"
  - 状态不符 → 提示用户
- 步骤 17 E-7 边界:`fix-plan.md` 存在(仅 BUG-00001 历史)+ `PLAN.md` 缺失 → 退化 + 提示"请先用 `code-plan` 产出 `PLAN.md`"
- 步骤 22 E-9 边界:`fix-work-log.md` 存在(仅 BUG-00001 历史)→ 退化 + 提示
- 步骤 24 E-11 边界:同 E-7

#### 日志埋点

- 步骤 17 通过屏显:"✓ 缺陷与修复方案校验通过"
- 步骤 25 汇报:"同步的文件:`fix/<缺陷>/RESULT.md` / `fix/RESULT.md` / `fix/<缺陷>/PLAN.md`(若状态推进) / 版本看板'缺陷清单'+'任务清单'"

#### 依据规范

- `skill-conventions §规则 1` — frontmatter `name` 字段字节级保留
- `module-conventions §规则 1` — SKILL.md 在技能根目录
- `directory-conventions` — 过程文档摆放在 `code/<TASK-BUG-...>/` 根目录

## 5. 数据结构完整变更

### 5.1 新增实体

#### 实体 1:`TASK-BUG-NNNNN-NNNNN` 任务编号

| 字段 | 类型 | 约束 | 说明 |
| --- | --- | --- | --- |
| 任务编号 | string | `TASK-BUG-NNNNN-NNNNN`,5+5 位嵌套式 | BUG 任务唯一标识 |
| 关联 BUG | string | `BUG-NNN`,3 位 | 自查 |

- **关系**:每条 BUG 任务仅属于 1 个 BUG-NNN(0 允许跨多 BUG)
- **存储选型**:`PLAN.md` 任务总览(沿用既有 6 类型 + 13 触发/来源,无新增)
- **索引**:`PLAN.md` 文件行序(沿用既有 `code-plan/SKILL.md` 步骤 9B "任务编号分配"逻辑)
- **迁移脚本**:不涉及
- **依据规范**:`./assistants/rules/encoding-conventions.md §规则 1/3`

### 5.2 修改实体

#### 实体 2:版本看板"任务清单"区段行字段(本需求扩展填法)

| 字段 | REQ 任务填法 | BUG 任务填法(本需求新增) | 字段语义变化 |
| --- | --- | --- | --- |
| 任务编号 | `TASK-REQ-NNNNN-NNNNN` | `TASK-BUG-NNNNN-NNNNN` | 沿用 5+5 位嵌套式,新增 `BUG` 前缀 |
| 需求 | `REQ-NNNNN`(5 位) | `BUG-NNN`(3 位) | **沿用既有列**,列语义不变;值类型从 5 位扩展到 3 位 |
| 类型 | 6 枚举 | **`修复`**(沿用既有 6 枚举之一) | **0 新增枚举** |
| 触发/来源 | 13 枚举 | **`缺陷修复`**(沿用既有 13 枚举之一) | **0 新增枚举** |
| 标题 | 沿用 REQ-00013 标题解析 | 沿用 REQ-00013 标题解析(用 `formatTaskTitle` + `truncateTitle`) | 沿用 |
| 开发状态 | 6 枚举 | 同(沿用) | 沿用 |
| 测试状态 | 6 枚举 | 同(沿用) | 沿用 |
| 涉及文件 | 留空 → `code-it` 完成时填入 | 同(沿用) | 沿用 |
| 完成时间 | `code-it` 完成时填入 | 同(沿用) | 沿用 |
| 提交哈希 | `code-it` 完成时填入 | 同(沿用) | 沿用 |
| 关联任务 | 沿用(审查改修场景) | **`BUG-NNN`**(自查) | 沿用既有"关联任务"列 |

**关键**:所有字段**0 新增**,所有枚举值**0 新增**;仅在既有"任务清单"区段内**新增** BUG 任务行(用既有字段填法)。**0 触发** `dashboard-conventions §规则 1` 三同步。

#### 实体 3:`code-plan/templates/fix-plan.md` 模板

- **既有状态**:10 章节(缺陷摘要 / 根因定位 / 修复方案 / 涉及文件与变更 / 测试方案 / 风险与回退 / 修复步骤 / 规范遵循 / 待澄清 / 变更记录)
- **本需求变更**:**不**删除模板(留作历史);**不**再被 `code-plan` 引用(由 `code-plan/SKILL.md` 步骤 26A 改为引用 `templates/plan.md` + `templates/task-plan.md`)
- **迁移需求**:无(本需求后**不**再生成 `fix-plan.md`;BUG-00001 历史保留)

### 5.3 数据生命周期

| 数据 | 留存期限 | 清理策略 | 备份策略 |
| --- | --- | --- | --- |
| `fix/<BUG-NNN>/RESULT.md` | 永久 | 不清理(沿用) | git 提交历史(沿用) |
| `fix/<BUG-NNN>/PLAN.md`(本需求新增) | 永久 | 不清理 | git 提交历史 |
| `fix/<BUG-NNN>/fix-plan.md`(本需求后**不**再生成) | 历史文件保留 | 不清理(BUG-00001 历史) | git 提交历史 |
| 看板"任务清单"区段 BUG 任务行 | 永久 | 不清理 | git 提交历史 |
| `code/<TASK-BUG-...>/{work-log,compile-and-run,test-results,deviations}.md` | 永久 | 不清理 | git 提交历史 |

## 6. 接口细节

### 6.1 新增接口

#### 接口 1:`code-plan` 步骤 28A+1 — 同步版本看板"任务清单"区段(本需求新增)

- **形式**:函数 / 章节(非 RESTful,文档内函数)
- **路径/签名**:
  ```
  ### 步骤 28A+1 — 同步版本看板'任务清单'区段(本需求 REQ-00019 新增,2026-06-06 起生效)
  ```
- **请求结构**:
  - 输入:`./assistants/<版本号>/fix/<BUG-NNN>/PLAN.md` 任务总览
- **响应结构**:
  - 输出:版本看板 `./assistants/<版本号>/RESULT.md` "任务清单"区段追加 N 行(每条 BUG 任务一行)
- **错误码**:无(纯文档写入)
- **示例**:
  - 正常:`fix/BUG-00010/PLAN.md` 任务总览 1 条 `TASK-BUG-00010-00001` → 看板"任务清单"区段追加 1 行
  - 异常:无
- **版本策略**:本需求后所有新 BUG 适用(FR-3 NFR-3.1)
- **依据规范**:`dashboard-conventions §规则 1`(0 触发)

#### 接口 2:`code-it` 步骤 17 — 校验缺陷与修复方案存在(本需求修订)

- **形式**:函数 / 章节(非 RESTful,文档内函数)
- **路径/签名**:
  ```
  ### 步骤 17 — 校验缺陷与修复方案存在(本需求 REQ-00019 修订,2026-06-06 起生效)
  ```
- **请求结构**:
  - 输入:`./assistants/<版本号>/fix/<缺陷编号>/RESULT.md` + `./assistants/<版本号>/fix/<缺陷编号>/PLAN.md`
- **响应结构**:
  - 校验通过:进入步骤 18
  - 校验失败:报错 + 退出(沿用既有 3 类信息)
- **错误码**:无(纯文档校验)
- **示例**:
  - 正常:`fix/BUG-00010/PLAN.md` + `RESULT.md` 都存在 → 进入步骤 18
  - 异常 1:缺 `RESULT.md` → "请先调 `code-fix <缺陷编号>` 登记缺陷"
  - 异常 2:缺 `PLAN.md` → "请先调 `code-plan <缺陷编号>` 规划修复方案"
  - 异常 3:状态不符(报告/调查中)→ 提示用户:当前状态是 X,需要先 `code-fix` 推进到 `修复规划中` / `修复编码中`
- **版本策略**:本需求后所有新 BUG 适用

#### 接口 3:`code-it` frontmatter description 段(本需求修订)

- **形式**:YAML frontmatter 字段
- **路径/签名**:`plugins/code-skills/skills/code-it/SKILL.md` L5
- **变更前**:
  ```
  - **缺陷编号**(格式 `BUG-NNNNN`,如 `BUG-00001`):所有产出物写入 `./assistants/<版本号>/fix/<缺陷编号>/`(以 `fix-` 前缀命名的过程文档),从 `./assistants/<版本号>/fix/<缺陷编号>/RESULT.md` 读取缺陷详情,从 `./assistants/<版本号>/fix/<缺陷编号>/fix-plan.md` 读取修复方案
  ```
- **变更后**:
  ```
  - **缺陷编号**(格式 `BUG-NNNNN`,如 `BUG-00001`):所有产出物写入 `./assistants/<版本号>/fix/<缺陷编号>/`(主详细设计 `RESULT.md` + 任务列表 `PLAN.md`,沿用 REQ 路径同构产出),从 `./assistants/<版本号>/fix/<缺陷编号>/RESULT.md` 读取缺陷详情,从 `./assistants/<版本号>/fix/<缺陷编号>/PLAN.md` 读取修复任务列表
  ```
- **版本策略**:本需求后所有新 BUG 适用

### 6.2 扩展既有接口

无(本需求只改章节描述与 frontmatter L5;不修改 SKILL.md 暴露的对外接口,即 CLI 调用方式 `code-plan BUG-NNN` / `code-it TASK-BUG-...` 不变)。

### 6.3 修改既有接口

无(同上)。

## 7. 异常处理

### 7.1 异常路径

| ID | 异常路径 | 处理 | 监控 |
| --- | --- | --- | --- |
| E-1 | `code-plan` 步骤 23 检测到历史 `fix-plan.md` 存在(仅 BUG-00001) | 提示"检测到历史 fix-plan.md,本需求已不再生成该文件;是否继续以本缺陷的 fix-plan.md 为参考?" + 3 选 1(继续/手动迁移/中止) | 屏显 |
| E-2 | `code-plan` 步骤 26A 撰写 9 份文档失败(磁盘满 / 权限) | 透传 stderr,中断退出 | 屏显 `✗ 错误` |
| E-7 | `code-it` 步骤 17 `fix-plan.md` 存在 + `PLAN.md` 缺失(BUG-00001 历史) | 退化 + 提示"请先用 `code-plan` 产出 `PLAN.md`" | 屏显 `⚠` |
| E-8 | `code-it` 步骤 17 `PLAN.md` 存在但任务总览为空 | 提示"修复方案待补充任务拆分;请调 `code-plan` 重新规划" | 屏显 `⚠` |
| E-9 | `code-it` 步骤 22 `fix-work-log.md` 存在(仅 BUG-00001 历史) | 提示"新结构 TASK-BUG- 任务编号,过程文档不再使用 fix- 前缀;是否手动迁移?推荐不迁移" | 屏显 `⚠` |
| E-10 | `code-it` 步骤 22-23 实施过程失败 | 沿用既有"最多连续失败 5 次后必须停下询问用户" | `fix-work-log.md` 记录 |
| E-11 | `code-it` 步骤 24 同步失败 | 沿用既有 7 步同步失败处理 | 屏显 `⚠` |

### 7.2 安全边界

- **输入校验**:本需求纯文档同步操作,无外部输入
- **敏感数据处理**:无
- **审计日志**:无
- **依据规范**:不涉及(本需求纯文档重构,无新增鉴权/加密/审计)

## 8. 安全要求

不涉及(本需求纯文档重构,无新增鉴权/加密/审计需求)。

## 9. 状态机/流程

### 9.1 BUG 状态机(沿用既有 7 状态)

```
报告 → 调查中 → 修复规划中 → 修复编码中 → 已修复-待验证 → 已修复-已验证 → 已关闭
```

本需求不修改状态机(由 `code-fix` 维护)。

### 9.2 BUG 任务开发状态机

```
待开始 → 进行中 → 已完成 / 已取消 / 阻塞 / 待重新评估
```

本需求不修改任务开发状态机(沿用既有)。

### 9.3 BUG 任务测试状态机

```
未编写 → 已编写 → 已运行-通过 / 已运行-失败
                  ↓ (不适用)
                不适用 / 阻塞
```

本需求不修改任务测试状态机(沿用既有)。

## 10. 性能与资源

### 10.1 关键路径预估

| 操作 | 预估耗时 | 依据 |
| --- | --- | --- |
| `code-plan` 缺陷分支 9 份文档产出 | < 5 秒 | 9 份 Markdown 文件本地 I/O,远低于 5 秒 |
| `code-it` 缺陷分支 1 任务实施 + 4 份过程文档 | 0.5-2 天/任务 | 沿用既有任务估算 |
| 看板"任务清单"区段追加 N 行 | < 100ms | N 行 Markdown 追加 |
| 屏显(沿用既有) | < 100ms | 沿用既有 |

### 10.2 资源限制

不涉及(纯本地文件 I/O,无网络/CPU/内存限制)。

### 10.3 缓存策略

不涉及(无缓存需求)。

### 10.4 依据规范

不涉及(本需求纯文档重构,无性能/资源约束)。

## 11. 测试要点

### 11.1 单元测试

不涉及(本仓库**不**包含任何测试框架,CLAUDE.md 严守;REQ-00009 守卫判定"不可测";所有任务测试状态 = `不适用`)。

### 11.2 集成测试

不涉及。

### 11.3 端到端测试

**人工场景验证清单**(沿用 REQ-00010 / REQ-00009 既有"静态 Read + 人工场景验证"模式):

| 验证 ID | 场景 | 预期行为 | 验证方法 |
| --- | --- | --- | --- |
| V-1 | 调 `code-plan BUG-00010`(新 BUG,无历史) | 产出 9 份文档 + 看板"任务清单"区段追加 1 行 `TASK-BUG-00010-00001` | 临时新建 BUG-00010,调 `code-plan`;屏显 + 文件状态 |
| V-2 | 验证 `fix/BUG-00010/RESULT.md` 14 章节齐全 | 14 章节齐全(沿用 `templates/plan.md`) | `grep -c "^## " RESULT.md` = 14 |
| V-3 | 验证 `fix/BUG-00010/PLAN.md` 8 章节齐全 | 8 章节齐全(沿用 `templates/task-plan.md`) | `grep -c "^## " PLAN.md` = 8 |
| V-4 | 验证 `fix/BUG-00010/PLAN.md` 任务总览 | 含 1 条 `TASK-BUG-00010-00001`;触发/来源=缺陷修复 | `Read` PLAN.md 任务总览 |
| V-5 | 调 `code-dashboard`(无参) | 看板"任务清单"区段显示 `TASK-BUG-00010-00001` | 调 `code-dashboard`;屏显 |
| V-6 | 调 `code-it TASK-BUG-00010-00001` | 步骤 17 读 `PLAN.md` + `RESULT.md`,过程文档去 `fix-` 前缀 | 调 `code-it`;屏显 + 文件状态 |
| V-7 | 调 `code-plan BUG-00001`(历史) | 步骤 23 检测 E-1 边界,提示 + 3 选 1 | 调 `code-plan`;屏显 |
| V-8 | 调 `code-it TASK-BUG-00001-00001`(若未来重跑) | 步骤 17/22 检测 E-7/E-9 边界,提示 + 退化 | 调 `code-it`;屏显 |

### 11.4 性能/安全测试

不涉及(纯文档重构,无性能/安全测试载体)。

## 12. 规范遵循

详 §3 表格;0 冲突 0 偏离 0 授权。

## 13. 待澄清/未决项

| 编号 | 内容 | 状态 |
| --- | --- | --- |
| (无) | 本需求**0** 待澄清(7 轮 Q-locked 全部锁定 + design 阶段 1 轮 Q-locked `--minimal` 全部锁定) | 0 待澄清 |

## 14. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-06-06 15:30 | v1 | 初始创建:整体设计目标 `--minimal`(沿用 design);2 个 SKILL.md 联动改造 — `code-plan` 步骤 19-28A 重构 + 步骤 28A+1 新增 + `code-it` 步骤 17-25 改造 + frontmatter L5 修订;0 新增模块 0 新增依赖 0 新增资源文件;13 份规范 0 冲突;0 触发 `dashboard-conventions §规则 1` 三同步;0 修改其他 11 个 `code-*` SKILL.md;5 项关键设计决策 D-1~D-5 全部锁定(沿用 design 阶段);7 INV 字节级保留(frontmatter `name` + 既有 12 章节);8 端到端验证 V-1~V-8;8 边界 E-1~E-11;2 任务 0 待澄清;100% 沿用上游 8 FR / 11 NFR / ~30 AC | wangmiao |
