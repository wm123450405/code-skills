# 设计笔记 — REQ-00019

## 关键设计问题清单

### Q-1:`code-plan` 步骤 19-28A 重构范围多大?

- **方案 A**(选定):8 锚点变动 + 1 锚点新增(5 锚点 = A/B/C/D/E)
- **方案 B**(否决):完全重写整个缺陷分支章节(80+ 行)
- **方案 C**(否决):仅修订 26A 步骤(产 fix-plan.md → 产 9 份文档),其他 8 步不动
- **决策依据**:用户 Q-1 锁定"形式同构 + 路径全替";`--minimal` 锁定最小实现;沿用 REQ-00010 既有"5 子节齐全 + 2 退化条件子节"模式

### Q-2:`code-it` 步骤 17-25 改造锚点哪些必须改?

- **方案 A**(选定):7 锚点 = F(frontmatter L5)/ G(步骤 17 改读 + E-7 边界)/ H(步骤 22 去 fix- + E-9 边界)/ I(步骤 23 去 fix-)/ J(步骤 24 不再写 fix-plan.md + E-11 边界)/ K(步骤 25 汇报字段)
- **方案 B**(否决):仅改步骤 17(改读 `PLAN.md`),其他步骤沿用既有
- **方案 C**(否决):全步骤重写(80+ 行)
- **决策依据**:用户 Q-4 锁定"`code-it` BUG 路径全部 4 项联动";Q-6 锁定"4 锚点全选"

### Q-3:任务粒度?(--minimal 锁定粗化)

- **方案 A**(选定):2 任务(T-001 `code-plan` 改造 + T-002 `code-it` 改造)
- **方案 B**(否决):5 任务(T-001~T-005 = frontmatter / 步骤 17 / 步骤 22 / 步骤 24 / 步骤 25 各 1 任务)
- **方案 C**(否决):1 任务(T-001 = 2 个 SKILL.md 一起改)
- **决策依据**:用户 Q-1 锁定"形式同构 + 路径全替";`--minimal` 锁定粗化;沿用 REQ-00010 既有 2 任务模式

## 关键决策(8 项 D-1~D-8)

| 决策 ID | 决策内容 | 选定方案 | 理由 |
| --- | --- | --- | --- |
| **D-1** | `code-plan/SKILL.md` 步骤 19-28A 锚点 A 修订范围 | 修订 3 条(既有第 2/3/4 条) | 既保留既有"关键简化"框架,又满足"形式同构"目标 |
| **D-2** | `code-plan/SKILL.md` 步骤 19-28A 锚点 B/C/D/E 范围 | 锚点 B(步骤 23 E-1)+ 锚点 C(步骤 24A 重写)+ 锚点 D(步骤 26A 改模板引用)+ 锚点 E(步骤 28A+1 新增) | 4 锚点覆盖全部需求点 |
| **D-3** | `code-it/SKILL.md` 锚点 F 范围 | frontmatter L5 description 段修订(整段替换) | 与步骤 17 锚点 G 保持一致 |
| **D-4** | `code-it/SKILL.md` 锚点 G/H/I 范围 | 步骤 17 改读 + E-7 边界;步骤 22 去 fix- + E-9 边界;步骤 23 去 fix- | 3 锚点覆盖消费方改造 |
| **D-5** | `code-it/SKILL.md` 锚点 J/K 范围 | 步骤 24 不再写 fix-plan.md + E-11 边界;步骤 25 汇报字段同步 | 2 锚点覆盖汇报与同步 |
| **D-6** | BUG 任务编号格式 | `TASK-BUG-NNNNN-NNNNN`(5+5 位嵌套式) | 沿用 `encoding-conventions §规则 1/3` |
| **D-7** | 看板"任务清单"区段 BUG 任务行字段填法 | 12 列齐全(沿用既有);类型=`修复`;触发/来源=`缺陷修复`;关联任务=`BUG-NNN` | 0 新增字段/枚举/区段 |
| **D-8** | 任务粒度 | 2 任务(沿用 REQ-00010 / REQ-00009 既有模式) | `--minimal` 锁定粗化;既有 2 任务模式已验证可发布 |

## 候选方案(否决)详述

### 候选 A1(完全重写 code-plan 缺陷分支 — 否决)
- **否决理由**:实施量大 + 风险高 + 偏离既有锚点字节级保留约束
- **影响范围**:D-1 / D-2

### 候选 A2(仅改 code-plan 步骤 26A — 否决)
- **否决理由**:不满足用户 Q-1 锁定(形式同构 + 路径全替);只改 26A 不能解决步骤 19-28 既有文字与新流程不一致
- **影响范围**:D-2

### 候选 A3(完全重写 code-it 步骤 17-25 — 否决)
- **否决理由**:实施量大 + 风险高 + 偏离既有锚点字节级保留约束
- **影响范围**:D-3 / D-4 / D-5

### 候选 A4(仅改 code-it 步骤 17 — 否决)
- **否决理由**:不满足用户 Q-4 锁定(`code-it` BUG 路径全部 4 项联动)
- **影响范围**:D-4

### 候选 A5(5 任务拆分 — 否决)
- **否决理由**:粒度过细,违反 `--minimal` 锁定粗化
- **影响范围**:D-8

## 实施期关键决策(待后续阶段锁定)

| 决策 ID | 内容 | 现状 |
| --- | --- | --- |
| **P-D1** | `code-plan/SKILL.md` 步骤 19-28A+1 重构后行数 | 估计 +100 ~ +200 行(基线 945 行,允许 +20% 上限 = 1134 行) |
| **P-D2** | `code-it/SKILL.md` 步骤 17-25 改造后行数 | 估计 0 净增(替换既有锚点文字,不改步骤数) |
| **P-D3** | `templates/fix-plan.md` 是否删除 | 不删除,留作历史;`code-plan` 步骤 26A 改为引用 `templates/plan.md` + `templates/task-plan.md` |
| **P-D4** | BUG 任务编号起始 | `TASK-BUG-NNNNN-00001`(5 位,与 REQ 任务一致) |
| **P-D5** | BUG 任务"需求"列填法 | `BUG-NNN`(3 位,与 REQ 任务的 5 位不同;用户接受) |
| **P-D6** | 看板"任务清单"区段 BUG 任务行"关联任务"列 | 填本缺陷 `BUG-NNN`(自查) |
| **P-D7** | `code-fix/SKILL.md` 是否修改 | **不修改**(FR-8 强约束;0 改动) |
| **P-D8** | 实施时机 | 本需求走标准流程:`code-it` T-001 → T-002 → `code-review` |
