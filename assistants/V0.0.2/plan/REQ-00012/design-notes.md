# 设计笔记 — REQ-00012
更新时间:2026-06-05
版本:V0.0.2

## 关键设计决策

| ID | 决策 | 依据 |
| --- | --- | --- |
| D-1 | 任务粒度 = 3 个功能点(写中文 README / 写英文 README / 移动 CLAUDE.md) | REQ-00014 §步骤 10A "按功能点拆分" |
| D-2 | 0 个架构任务(简单需求) | 触发条件 1/2/3 均不满足 |
| D-3 | T-001 + T-002 同次提交 | `doc-conventions §规则 1` |
| D-4 | T-003 独立提交(分次) | 不同语义层(文档新增 vs 文件移动);FR-5 AC-5.5 不强求 |
| D-5 | 测试状态统一 = `不适用` | 纯文档 + 仓库无可测载体 + REQ-00009 守卫"不可测" |
| D-6 | 不派生"更新看板"任务 | REQ-00017 强约束:不在候选集 |

## 候选拆分对比

| 候选 | 任务数 | 评估 |
| --- | --- | --- |
| A. 1 任务:做完全部 | 1 | 粒度过粗,违反"0.5-2 天可完成"下限 + 无法独立验证 |
| B. 3 任务:按功能点拆(本方案) | 3 | **选定**,符合 REQ-00014 + 可独立验证 + 测试状态统一 |
| C. 5 任务:写 README + 写 README.en + 中英对仗校验 + git mv + 验证 | 5 | 粒度过细,违反"功能点"原则(校验是任务内部动作,不是任务) |
| D. 2 任务:中英 README(同次提交合并) + git mv | 2 | 略粗,丢失"中英对仗校验"的独立验证点 |

## 跨任务约束
- 中英 README 章节对仗:T-001 与 T-002 之间需"先写中文 → 写英文时镜像章节"
- 字节级保留:T-003 的 CLAUDE.md = 9,418 bytes(以 `wc -c plugins/code-skills/CLAUDE.md` 为准)
- 同次提交:T-001 与 T-002 的 `git add` + `git commit` 必须在 1 个 commit 中完成
