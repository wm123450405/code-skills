# 数据结构完整变更 — REQ-00014

更新时间:2026-06-05 13:30
版本:V0.0.2
需求:REQ-00014(优化技能 `/code-plan` 的任务拆分维度)

## 1. 新增实体(0)

**N/A** — 本需求**不**产生新数据结构。

## 2. 修改实体(1)

### `plugins/code-skills/skills/code-plan/SKILL.md`(SKILL.md 文字)

| 字段 | 类型 | 约束 | 索引 | 变更类型 | 说明 |
| --- | --- | --- | --- | --- | --- |
| §10A L195-199(4 行原则) | 文字(Markdown) | N/A | N/A | **完全替换** | 4 行原则被"按功能点拆分 + 架构任务 + 生效范围"3 个新子节替换(完全替换 = Q-D1 锁定 A) |
| §10A L201+ | 文字(Markdown) | N/A | N/A | **保留** | 任务类型 / 任务编号 / 任务双状态 / 任务触发/来源 全部沿用既有 |

- 关系:N/A(纯文字文件,无实体关系)
- 存储选型:N/A(纯文本)
- 迁移脚本:N/A(纯文字修改,无数据迁移)
- 依据规范:`skill-conventions §规则 1` 不约束工作流文字

## 3. 数据迁移(0)

**N/A** — 本需求**不涉及**数据迁移。

## 4. 修改范围精确化

| 修改文件 | 修改区段 | 修改行数 | 修改内容 |
| --- | --- | --- | --- |
| `plugins/code-skills/skills/code-plan/SKILL.md` | §10A L195-199 | 4 行删除 + ~25 行新增 = **+20 行净增** | 4 行旧原则 → 3 个新子节(核心原则 + 架构任务 + 生效范围) |

## 5. 满足的规范条款

- `skill-conventions.md §规则 1`:本需求**不**改 frontmatter(只改 §10A 工作流文字)
- `module-conventions.md §规则 1`:本需求**不**新增/删除任何资源文件
- `dashboard-conventions.md §规则 1`:本需求**不**改看板字段(只改 `code-plan` 技能,故**不**触发三同步)
- `encoding-conventions.md §规则 1-4`:本需求**不**改任务编号格式(沿用既有 `TASK-(REQ|BUG)-NNNNN-NNNNN`)

## 6. 兼容性

- **向上兼容**:新规则**不**破坏既有 7 个 PLAN 的"已完成"状态
- **向前兼容**:新规则**仅**对未来需求(REQ-00008+)生效,既有 PLANS 保持原样
- **任务编号连续性**:T-001 + T-005(中间 3 个空位 00002-00004 留给未来追加)

## 7. 回滚方案

**3 步回滚**:
1. `git revert <提交哈希>`(整体回滚)
2. 或手动:`Edit` `code-plan/SKILL.md` §10A,删除新增 3 个子节 + 恢复原 4 行
3. 验证:看板 5 处同步记录 `回滚至原 4 行原则`

**回滚无副作用**:
- 6 个子技能 SKILL.md 字节级保留
- 看板"概要设计清单"可保留为历史(无需删除)
- 既有 7 个 PLAN **不**追溯(Q-A3 锁定 A)

## 8. 总结

| # | 实体 | 状态 | 变更字段 | 对应任务 |
| --- | --- | --- | --- | --- |
| 1 | `code-plan/SKILL.md` | 修改 | §10A 4 行 → ~25 行(完全替换) | T-001 |

**0 个数据库表变更,0 个字段类型变更,0 个索引变更**。
