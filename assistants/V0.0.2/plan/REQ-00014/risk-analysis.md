# 风险分析 — REQ-00014

更新时间:2026-06-05 13:35
版本:V0.0.2
需求:REQ-00014(优化技能 `/code-plan` 的任务拆分维度)

## 1. 异常处理(E-1 ~ E-5,沿用需求 §9)

### E-1:需求边界模糊(无法判断是否需要架构任务)
- **触发条件**:用户调用 `code-plan` 时,需求描述模糊,无法判断 3 个触发条件
- **检测手段**:`code-plan` 步骤 8A 澄清机制(沿用既有)
- **处理策略**:与用户澄清(沿用 `code-plan` 步骤 8A)
- **监控指标**:`code-plan` 步骤 8A 触发频率
- **对应任务**:T-001(SKILL.md 沿用既有澄清机制)

### E-2:用户要求修改 `code-plan` 其他章节(非 §10A)
- **触发条件**:用户调 `code-plan` 时,期望修改其他章节(如 §7 工作流步骤)
- **处理策略**:**拒绝**,引导用户另起需求
- **监控指标**:用户拒绝率
- **对应任务**:T-001 实施时,**不**改其他 18 章节

### E-3:既有 7 个 PLAN 用户要求重拆
- **触发条件**:用户调 `code-plan REQ-00004/05/06/07` 时,期望按新规则重拆
- **处理策略**:**拒绝**,引用 Q-A3 锁定 A
- **监控指标**:用户拒绝率
- **对应任务**:N/A(在 `code-plan` 步骤 0/1 拒绝)

### E-4:未来需求拆分时 `code-plan` 应用新规则失败
- **触发条件**:V0.0.2 未来需求(REQ-00008+)调用 `code-plan` 时,新规则评估失败
- **处理策略**:**退回"分层拆分"**(沿用既有)+ 记录到 `analysis-notes.md` 备查
- **监控指标**:退回率
- **对应任务**:N/A(在 `code-plan` 步骤 10A 兜底)

### E-5:`code-plan` SKILL.md 修改后与 `code-design` 冲突
- **触发条件**:本需求修改后,未来需求(REQ-00008+)调 `code-design` + `code-plan` 时,新规则与 `code-design` 输出冲突
- **处理策略**:**暂停**,询问用户
- **监控指标**:E-5 触发频率
- **对应任务**:N/A(由未来需求触发)

## 2. 安全边界

- **鉴权**:N/A(`code-plan` 由 Claude Code 模型层触发)
- **授权**:N/A
- **输入校验**:`code-plan` 拆分任务的输入是需求 + 设计文档,**不**涉及外部输入
- **敏感数据处理**:**不**读任何凭据 / secrets
- **防注入**:N/A
- **审计**:N/A
- **依据规范**:`dashboard-conventions §规则 1`(本需求**不**改字段,沿用既有)

## 3. 性能与资源

- **关键路径耗时**:`code-plan` 整体执行由子步骤决定;§10A 拆分任务耗时 = O(N_feature_points)
- **并发上限**:N/A
- **资源限制**:N/A
- **缓存策略**:N/A
- **批量/异步**:N/A
- **降级策略**:N/A

## 4. 回退策略

- **触发条件**:`code-plan` SKILL.md 改写后,出现"被破坏"或"严重问题"
- **步骤**:
  1. `git revert <提交哈希>`(整体回滚)
  2. 或手动:`Edit` `code-plan/SKILL.md` §10A,删除新增 3 个子节 + 恢复原 4 行
  3. 验证:看板 5 处同步记录 `回滚至原 4 行原则`
- **验证**:
  - 看板"概要设计清单" + "详细设计与任务计划汇总" + "任务清单" + "里程碑" + "变更记录" 5 处一致
  - 既有 7 个 PLAN **不**变(Q-A3 锁定)
- **回滚无副作用**:6 个子技能 SKILL.md 字节级保留

## 5. 测试要点

### 5.1 单元测试
- **N/A**(本仓库无构建/测试文件,REQ-00009 守卫判定"不可测")
- 测试状态 = `不适用`(Q-P3 锁定 A)

### 5.2 集成测试
- **场景 1:本需求自身**(代码 REQ-00014 走新规则)
  - 期望:1-2 任务,无架构任务
  - 验证:`code-plan` 步骤 10A 输出 T-001 + T-005
- **场景 2:未来需求(假设 REQ-00020 = LLM 聊天机器人)**
  - 期望:首个任务 = 架构任务,后续 4-5 任务为各功能点
  - 验证:`code-plan` 步骤 10A 评估 3 触发条件

### 5.3 端到端测试
- **场景 3:既有 7 个 PLAN 保留原样**
  - 期望:既有 PLANS 任务结构**不**变(Q-A3 锁定)
  - 验证:对照 `plan/REQ-00004/PLAN.md` 等的"任务总览"行数

### 5.4 性能/安全测试
- N/A(纯文档型)

### 5.5 回归测试
- **既有 7 个 PLAN**:**不**变(Q-A3 锁定 A)
- **其他 12 个 `code-*` 技能 SKILL.md**:**不**变(FR-4 强约束)

### 5.6 静态自检
- **8 项不变量**(由 T-005 实施):
  1. `code-plan` SKILL.md §10A 4 行原原则**已删除**(完全替换)
  2. 新增"按功能点拆分"子节**存在**且含完整一套(展示/逻辑/说明)
  3. 新增"架构任务作为首个"子节**存在**且含 3 触发条件
  4. 新增"生效范围"子节**存在**且声明"仅未来需求"
  5. 其他 18 章节**未**被修改(FR-4 强约束)
  6. `code-plan` SKILL.md 行数偏差 ±20% 内(原 374 → ~394 = +20 = +5.3%)
  7. 任务编号格式**未**修改(沿用 `TASK-(REQ|BUG)-NNNNN-NNNNN`)
  8. 关键 token 全部存在(§10A 引用 `Q-D1` + `Q-A1/Q-A2/Q-A3` + 13 规范条款)

## 6. 风险评估矩阵

| # | 风险 | 概率 | 影响 | 缓解 | 残留风险 |
| --- | --- | --- | --- | --- | --- |
| R-1 | §10A 改写后与 `code-design` 冲突 | 中 | 中 | E-5 暂停 + 询问用户 | 中 |
| R-2 | 未来需求拆分时新规则评估失败 | 中 | 中 | E-4 退回"分层拆分" | 低 |
| R-3 | 既有 7 个 PLAN 被意外重拆 | 极低 | 高 | Q-A3 锁定 + `code-plan` 步骤 0/1 拒绝 | 极低 |
| R-4 | 其他 12 个 `code-*` 技能被意外修改 | 极低 | 高 | T-005 8 项自检 + FR-4 强约束 | 极低 |
| R-5 | `dashboard-conventions §规则 1` 三同步被意外触发 | 极低 | 中 | 本设计**不**改字段(强约束"不触发") | 极低 |
| R-6 | SKILL.md 体积爆炸 | 低 | 低 | 净增 ~20 行(5.3% 增长,在可接受范围) | 低 |

## 7. 缓解优先级

| 优先级 | 风险编号 | 理由 |
| --- | --- | --- |
| P0 | R-3 / R-4 | 子技能被破坏 = 整个工作流崩溃 |
| P1 | R-1 | 工作流冲突 = 未来需求无法拆分 |
| P1 | R-5 | 看板不一致 = `code-publish` 前置检查失败 |
| P2 | R-2, R-6 | 已知风险,有缓解 |

**残留风险等级**:**低**(所有 P0/P1 风险都有显式缓解 + 自检验证)。
