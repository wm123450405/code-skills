# 材料登记 — REQ-00014

更新时间:2026-06-05 13:10
版本:V0.0.2
需求:REQ-00014(优化技能 `/code-plan` 的任务拆分维度)

## 项目级规范

| 规范文件 | 类别 | 关键约束摘要 | 本需求相关 |
| --- | --- | --- | --- |
| `skill-conventions.md` | 技能编写 | §规则 1:SKILL.md frontmatter 必含 name+description,name 与目录名 kebab-case 严格一致 | ✅ 强约束(本需求**不**改 `code-plan` frontmatter) |
| `module-conventions.md` | 模块规划(DEPRECATED 但沿用) | §规则 1:资源放 `templates/` / `checklists/` / `guidelines/` 固定子目录 | ✅ 强约束(本需求**不**改 `code-plan/` 目录结构) |
| `dashboard-conventions.md` | 看板与版本工作空间 | §规则 1:字段约定扩展需 3 处同步 | ✅ 强约束(本需求**不**改字段,故**不**触发三同步) |
| `doc-conventions.md` | 文档编写 | §规则 1:中英 README 同次 + 结构对仗;§规则 2:README 必须存在并持续维护 | ✅ 不触达 |
| `marketplace-protocol.md` | Marketplace 协议 | §规则 1:`$schema` / `name` / `version` 必填,`source` `./` 开头,`skills` `./` 路径数组,无未知字段 | ✅ 不触达 |
| `encoding-conventions.md` | 编码格式 | §规则 1-4:REQ/BUG 5 位纯数字;TASK 嵌套式 5+5 位;§规则 4 实施流程 | ✅ 强约束(任务编号格式不修改) |
| `migration-mapping.md` | 编码迁移 | §规则 1-4:已落地映射;EXISTING-NNN 不追溯 | ✅ 不触达 |
| 6 个占位规范 | 各类 | 占位 | ❌ 不触达 |

**本设计必须强约束遵守的规范**:`skill-conventions §规则 1`(不修改 code-plan frontmatter)、`module-conventions §规则 1`(code-plan/ 无子目录)、`dashboard-conventions §规则 1`(不修改字段 + 不触发三同步)、`encoding-conventions §规则 1-4`(任务编号双格式兼容正则)

**占位规范 6 个**:不影响本设计

## 上游需求

- **来源**:`./assistants/V0.0.2/require/REQ-00014/RESULT.md`(v1,已锁定)
- **提取的 FR / NFR / AC 数量**:4 FR / 6 NFR / 13 AC
- **关键交叉点**(每条 FR 对应的设计章节):
  - **FR-1** 按"功能点"拆分任务 → 本设计 §4.1 / §5 算法 1(完整一套拆分)
  - **FR-2** 架构任务作为首个任务(条件触发)→ 本设计 §4.2 / §5 算法 1(10A.0 评估)
  - **FR-3** 仅未来需求生效 → 本设计 §4.3 / §13 关联编码计划
  - **FR-4** 不破坏其他 12 个 `code-*` 技能 → 本设计 §4.4 / §11 横向协同(0 影响)

## 上游概要设计

- **来源**:`./assistants/V0.0.2/design/REQ-00014/RESULT.md`(v1,已完成首次)
- **提取的模块拆分 / 接口概要 / 数据结构 / 决策**:
  - M-1 `code-plan/SKILL.md` §10A 修改(单模块,~20 行净增)
  - 1 个可编码算法(算法 1:任务拆分)
  - 0 新增接口契约(本需求不产生新 API)
  - 0 数据结构变更
  - 5 项关键设计决策 D-1 ~ D-5

### 关键交叉点(每条 FR 对应的概要设计章节)
| FR | 概要设计章节 | 本详细设计章节 |
| --- | --- | --- |
| FR-1 | §4.1 M-1 + §5 算法 1(完整一套拆分) | §4.1(本设计) + §5(本设计) |
| FR-2 | §5 算法 1(10A.0 评估) | §4.2(本设计) + §5(本设计) |
| FR-3 | §4.3 生效范围 | §4.3(本设计) + §13 关联编码计划 |
| FR-4 | §4.4 影响范围(0) | §4.4(本设计) + §11 横向协同 |

## 项目现状(实现细节)

### 命名风格(从既有 8 个 `code-*` SKILL.md 提取)
- **技能目录名**:kebab-case(`code-init` / `code-version` / `code-rule` / `code-require` / `code-design` / `code-plan` / `code-it` / `code-unit` / `code-fix` / `code-review` / `code-publish` / `code-auto`)
- **frontmatter 字段**:`name`(必,kebab-case 与目录名一致)+ `description`(必,自然语言一段话,含"做什么+何时用")
- **章节标题**:H1 主标题 + H2 章节 + H3 子章节(与既有 SKILL.md 风格一致)

### 错误处理范式
- **不修改既有子技能** = `code-plan` 自身的错误处理范式独立定义
- `code-plan` 采用:**检测 → 记录 → 跳过**(不抛出异常,因 SKILL.md 是 Markdown 而非代码;错误以**工作流回退**形式呈现 — 步骤 8A 澄清 → 步骤 10A 重新评估)

### 并发原语
- **N/A**:`code-plan` 是单次串行执行,无并发原语
- **不维护内存状态**

### 既有相似功能的实现风格(参考)
- `code-publish/SKILL.md`(V0.0.2,475 行,8 任务):章节结构 12 段(目标 / 适用 / 不适用 / 目录 / 输入 / 输出 / 工具 / 工作流 / 看板 / 衔接 / 不要做),**本设计完全沿用**

### 既有测试用例风格(本仓库无传统单元测试)
- **仓库无构建/测试文件**(代码单元测试状态全部 `不适用`,由 REQ-00009 守卫判定)
- `code-publish` PLAN.md 8 任务测试状态全 `不适用`(纯文档型)
- **本设计 2 个任务全部为"纯文档型"(写 SKILL.md + 8 项自检 + 收尾)**,测试状态**默认 `不适用`**

### 可复用的工具函数(本设计 N/A)
- `code-plan` 本身不写代码,无工具函数需求
- 子技能各自的工作流(`code-require` 等)已存在,本设计**零复用工具**(直接调子技能)

## 关键观察:本需求首次应用新规则

### 本需求**是** REQ-00014 新规则的首个应用!

- 之前 4 个 V0.0.2 已完成 PLAN(REQ-00004/05/06/07)是按"分层拆分"原则拆的
- **本需求(REQ-00014)的 PLAN.md**将是**第一个**应用"按功能点拆分"新规则的 PLAN
- **本需求不需要架构任务**(单一功能点 + 低扩展性 + 无三方组件)— 沿用 0a 评估后"否"的分支
- **本需求的 PLAN.md 任务数 = 2**:
  - T-001 写 `code-plan` SKILL.md §10A 改写(完整一套:展示 = 修改后 §10A 全文;逻辑 = 算法 1 + 调用表;说明 = 工作流锚点引用)
  - T-005 8 项不变量自检 + 收尾(纯文档型,沿用 V0.0.2 REQ-00007 T-005 实践)

## 本次变更源

**N/A(首次详细设计,无变更源对比)**。
