# 接口详细规格 — REQ-00014

更新时间:2026-06-05 13:25
版本:V0.0.2
需求:REQ-00014(优化技能 `/code-plan` 的任务拆分维度)

## 关键说明

本需求**不产生新 API 端点或方法签名**。**所有"接口"都是文字描述级别**——即修改 `code-plan` SKILL.md §10A 中的工作流文字说明。

## 接口 1:`code-plan` 任务拆分工作流(修改)

### 形式
- 文字描述(在 `plugins/code-skills/skills/code-plan/SKILL.md` §10A)

### 修改前(L195-199,共 4 行)
```text
**任务拆分原则**:
- 最小可执行单元:每条任务 0.5–2 天内可完成
- 单一职责:每条任务只做一件事
- 可独立验证:每条任务有清晰的"完成定义"
- 顺序友好:从底层依赖/数据结构 → 核心逻辑 → 集成层 → 测试 → 文档
```

### 修改后(完全替换,共 ~25 行)
```text
**任务拆分原则(REQ-00014 优化后,2026-06-05 生效)**:

#### 核心原则:按"功能点"拆分(必须)

- **1 个任务 = 1 个完整的功能点** = 1 个用户完整可用的能力
- **完整一套**:同一个功能点的**展示效果 + 功能逻辑 + 使用说明**必须在**同一个任务**中完整呈现
- **不拆分到多个任务**:例:"用户登录功能"的 UI / 鉴权逻辑 / README 入门说明 必须在 1 个任务中
- **可独立验证**:每个任务有清晰的"完成定义" — 既包含"产物是什么"也包含"产物如何被验证"
- **0.5–2 天内可完成**

#### 架构任务作为首个任务(条件性)

- **触发条件**(任一满足):
  - **触发 1**:**较高扩展性 / 较高可维护性 / 较高可配置性** + **较高复杂度**
  - **触发 2**:**涉及未来可能的不同实现方式**(为未来切换预留扩展点)
  - **触发 3**:**对接不同三方组件**(为未来替换/升级预留抽象层)
- **架构任务内容**:**架构设计 + 架构骨架** = 抽象层 + 接口契约 + 扩展点(**不**实现具体业务)
- **位置**:**首个任务**(TASK-XXX-00001),后续任务在此架构上填充功能点
- **可省略**:简单需求(单一功能点 + 低扩展性 + 无三方组件)可**不**插入架构任务

#### 生效范围

- **仅** V0.0.2 未来所有需求(REQ-00008+)应用本规则
- 既有 7 个 PLAN(REQ-00004/05/06/07 + 部分其他)**不**追溯重拆
- **不**修改其他 12 个 `code-*` 技能(任务编号/双状态/触发来源 **不**变)
```

### 入参
- 无(SKILL.md 文字修改,无 API 入参)

### 出参
- 无(SKILL.md 文字修改,无 API 出参)
- **副作用**:未来 `code-plan` 调用者读到 §10A 时,会按新原则拆分任务

### 错误码
- **N/A**(本需求**不**改 `code-plan` 错误处理)

### 示例(新原则的应用)

#### 示例 1:简单需求(无架构任务)
- 需求:优化 `code-plan` SKILL.md §10A(本需求自身)
- 拆分:T-001 写 SKILL.md §10A(完整一套:展示/逻辑/说明)+ T-005 自检
- **不**插入架构任务(单一功能点 + 低扩展性 + 无三方组件)

#### 示例 2:多模块需求(有架构任务)
- 需求:开发 LLM 聊天机器人
- 拆分:
  - T-001 架构设计 + LLM 抽象层骨架
  - T-002 聊天功能(完整一套:UI + 流式响应 + 入门)
  - T-003 上下文管理(完整一套)
  - T-004 工具调用(完整一套)
  - T-005 README 同步 + 发布准备

### 版本策略
- **本需求**v1(2026-06-05)
- 未来 v2 改进空间:留作 follow-up(同 `require/REQ-00014/RESULT.md` §FR-3)

### 兼容策略
- **向上兼容**:新规则**不**破坏既有 7 个 PLAN 的"已完成"状态
- **向前兼容**:新规则**仅**对未来需求(REQ-00008+)生效,既有 PLANS 保持原样

### 依据规范
- `skill-conventions §规则 1`(本需求**不**改 frontmatter,沿用既有)
- `dashboard-conventions §规则 1`(本需求**不**改看板字段,故**不**触发三同步)

## 接口 2:步骤 10A 调用顺序(修改)

### 形式
- 文字描述(在 `code-plan` SKILL.md §10A 内部)

### 调用顺序
详见 `module-details.md` §"关键'调用顺序'"。

### 入参/出参
- N/A(纯工作流定义)

### 错误码
- N/A

### 示例
- 简单需求(REQQ-00014 自身):1-2 任务,无架构任务
- 多模块需求(假设 REQ-00020):4-5 任务,首个 = 架构任务

### 依据规范
- 沿用既有 `code-plan` 步骤 11A-18A 的接口契约

## 总结

| 接口 | 形式 | 状态 | 对应任务 | 依据规范 |
| --- | --- | --- | --- | --- |
| 1. `code-plan` 任务拆分工作流 | 文字描述 | 修改 | T-001 | `skill-conventions §规则 1` |
| 2. 步骤 10A 调用顺序 | 文字描述 | 修改 | T-001 | `module-conventions §规则 1`(沿用) |
