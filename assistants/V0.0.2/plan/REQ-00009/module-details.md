# 模块详细化 — REQ-00009
更新时间:2026-06-05 17:20
版本:V0.0.2

## 模块 M-1:`code-unit/SKILL.md`

### 路径
- `plugins/code-skills/skills/code-unit/SKILL.md`

### 状态
- **修改**(增量追加)

### 关键子节

| 编号 | 名称 | 状态 | 锚点 |
| --- | --- | --- | --- |
| §0a.0 | "### 步骤 0a 项目可测性检查" | **新增** | 在 `### 步骤 0 — 版本上下文检测` **之前** |
| §0a.1 | "#### 步骤 0a.1 守卫检查项清单" | **新增** | §0a.0 下 |
| §0a.2 | "#### 步骤 0a.2 守卫判定逻辑" | **新增** | §0a.0 下 |
| §0a.3 | "#### 步骤 0a.3 守卫不通过 → 跳过流程" | **新增** | §0a.0 下 |
| §0a.4 | "#### 步骤 0a.4 屏幕报告格式" | **新增** | §0a.0 下 |
| §0a.5 | "#### 步骤 0a.5 退出码契约" | **新增** | §0a.0 下 |
| E-2/E-8 | "#### E-2 守卫不通过" 边界 | **新增**(编号由 T-001 决定,P-D6) | 既有"边界 E-1/E-3~E-7" 之间或之后 |
| E-8 | "#### E-8 守卫检查项扩展预留" 边界 | **新增**(若 E-2 被守卫不通过占用) | 既有"边界 E-7" 之后 |

### 关键类/函数(伪代码)
- `guard_check_testable(project_root: str) -> tuple[bool, list[tuple[str, bool]]]`(本需求新增)
- `check_package_json_test_script(project_root: str) -> bool`(本需求新增)
- `check_pyproject_toml_test_config(project_root: str) -> bool`(本需求新增)
- `check_build_gradle(project_root: str) -> bool`(本需求新增)

### 调用顺序
- 详见 `plan/REQ-00009/RESULT.md §3.1.1`

### 状态归属
- **不维护内存状态**:`code-unit` 是无状态工作流定义
- **不写代码 / 配置 / 数据库**
- **不持有任何凭据**

### 并发模型
- **N/A**(无运行时)

### 资源管理
- **N/A**(无连接 / 锁 / 缓存)

### 与概要设计的对应
- `design/REQ-00009/RESULT.md §4.1 + §7.1`(模块 M-1 详细化)
- `design/REQ-00009/RESULT.md §7.1 + §7.2`(关键算法与判定)

### 符合的规范
- `skill-conventions §规则 1`:frontmatter 字节级保留 ✓
- `module-conventions §规则 1`:SKILL.md 在技能根目录 ✓
- `dashboard-conventions §规则 1`:0 触发(沿用"不适用"既有枚举)✓

## 模块新增 / 复用 / 修改清单

| 类型 | 模块 | 路径 |
| --- | --- | --- |
| **修改** | `code-unit/SKILL.md` | `plugins/code-skills/skills/code-unit/SKILL.md` |
| **复用** | 既有"步骤 0 版本上下文检测" | (字节级保留,不修改) |
| **复用** | 既有"步骤 1-16 原有单测流程" | (字节级保留,不修改) |
| **复用** | 既有"测试状态"列写入路径 | (沿用,不修改) |
| **复用** | 既有"不适用"枚举 | (V0.0.1 既有,Q-2 锁定) |

**新增模块:0**
**修改模块:1**
**复用模块:5**(均为既有,字节级保留)
