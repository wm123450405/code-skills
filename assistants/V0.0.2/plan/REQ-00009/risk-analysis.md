# 风险分析 — REQ-00009
更新时间:2026-06-05 17:20
版本:V0.0.2

## 异常处理

| 异常路径 | 描述 | 处理 | 监控 |
| --- | --- | --- | --- |
| A-1 | 守卫 7 项检查全部失败(项目不可测) | FR-2 跳过 + 写看板"不适用" + exit 0 | 看板"测试状态"列 + 屏幕输出 |
| A-2 | `package.json` 存在但 `scripts.test` 为空字符串 / null | 视为该项不命中,继续检查下一项 | 守卫检查详情输出 |
| A-3 | `pyproject.toml` 存在但无测试配置 | 视为该项不命中,继续检查下一项 | 守卫检查详情输出 |
| A-4 | 既有 E-2 编号冲突 | 由 T-001 实施时**先 Read SKILL.md** 确认,P-D6 决策 | 静态自检 INV-4 / INV-5 |
| A-5 | 守卫检查中 Read / Glob 失败 | 视为该项不命中(容错) | 屏幕输出该检查项 ✗ |
| A-6 | 看板写入失败(权限/磁盘满) | 屏幕输出错误,守卫跳过流程**部分**完成 | 屏幕错误 |
| A-7 | 任务编码不存在 | 原 `code-unit` 行为(报错,退出码 ≠ 0) | (既有 E-5) |
| A-8 | 无 `.current-version` | 原 `code-unit` 行为(报错,退出码 ≠ 0) | (既有 E-1) |

## 安全边界

| 维度 | 描述 | 依据 |
| --- | --- | --- |
| **鉴权** | 不需要鉴权(本地文件操作) | — |
| **输入校验** | 任务编码格式校验(沿用既有 `步骤 1`) | (既有) |
| **敏感数据处理** | 不涉及(本需求**不**读写敏感数据) | — |
| **审计日志** | 屏幕输出守卫检查结果即可(FR-7.AC-7.1) | FR-7 |
| **代码注入** | 不执行任意代码(只读文件 + 写看板) | FR-2.AC-2.4(不调 Bash) |
| **新攻击面** | **0**(无新增依赖,无新接口对外暴露) | NFR-1 |

## 性能与资源(NFR-7 < 1 秒)

| 步骤 | 操作 | 预估耗时 |
| --- | --- | --- |
| 步骤 1 | `Glob` 7 项文件/目录 | ~50 ms |
| 步骤 2 | `Read` `package.json`(若存在) | ~50 ms |
| 步骤 3 | `Read` `pyproject.toml`(若存在) | ~50 ms |
| 步骤 4 | 判定 + 写看板"不适用"(若守卫不通过) | ~50 ms |
| **合计** | | **~200 ms** |

**远低于 1 秒**(NFR-7 强约束达成)。

### 资源限制
- **内存**:`< 1 MB`(仅 Read 7 个小文件)
- **CPU**:可忽略
- **磁盘 I/O**:`< 1 MB`(7 次 Read)
- **网络**:**0**(NFR-1)

## 回退策略

### 触发条件
- 13/13 INV 自检失败(任一失败)
- `code-auto` 任务循环在守卫不通过场景下异常中断
- `code-publish` 前置检查误判"不适用"为"任务未解决"

### 回退步骤
1. `git revert <commit-hash>` 回退本需求的 1 个 commit
2. 验证 `code-unit` 恢复原行为(无守卫)
3. 验证 `code-auto` 任务循环恢复正常
4. 验证 `code-publish` 前置检查恢复原行为

### 验证
- `grep "步骤 0a" code-unit/SKILL.md` 应为 0 命中(回退后)
- 看板"测试状态"列不出现守卫不通过时写入的"不适用"行(若该任务是回退前守卫不通过时标"不适用",回退后该行的"不适用"由人工/任务修复)
- 既有 11 个 `code-*` SKILL.md 字节级不变

## 测试要点

### 单元测试
- **N/A**(纯文档型,无运行时)— 测试状态 = `不适用`(Q-P3 锁定 A)

### 集成测试
- **N/A** — 测试状态 = `不适用`

### 端到端测试
- **N/A** — 测试状态 = `不适用`

### 性能 / 安全
- **N/A** — 测试状态 = `不适用`

### 静态自检(INV-1~13)
- **必须 13/13 通过**(由 T-002 收尾执行,详 `plan/REQ-00009/PLAN.md §3.2.4`)

### 13 项不变量(INV-1~13)风险评估

| INV | 风险等级 | 失败后果 | 缓解 |
| --- | --- | --- | --- |
| INV-1 (frontmatter 保留) | 中 | frontmatter 损坏 → `code-unit` 不可触发 | T-001 严守不修改 frontmatter |
| INV-2 (5 子节齐全 + 7 项检查) | 高 | 守卫不完整 → 用户困惑 | T-001 严格按 §0a.1-§0a.5 顺序追加 |
| INV-3 (既有 17 章节保留) | 中 | 既有功能被破坏 | T-001 **只增不改**(NFR-2) |
| INV-4 (既有 E-1/E-3~E-7 保留) | 低 | 边界文档丢失 | T-001 不删既有边界 |
| INV-5 (新增 E-2 守卫不通过) | 低 | 文档不完整 | T-001 严格按 P-D6 决策 |
| INV-6 (新增 E-8 扩展预留) | 低 | 文档不完整 | T-001 严格按 P-D6 决策 |
| INV-7 (行数偏差 ≤ ±20%) | 低 | INV 自检失败 | T-001 严守 ~30 行估算 |
| INV-8 (8 token 全部存在) | 低 | INV 自检失败 | T-001 8 token 全部写入 |
| INV-9 (其他 11 SKILL.md 保留) | 中 | 其他技能被破坏 | T-001 任务边界严守 |
| INV-10 (marketplace/plugin 不变) | 中 | 协议被破坏 | T-001 任务边界严守 |
| INV-11 (rules/ 13 文件不变) | 低 | 规范被破坏 | T-001 任务边界严守 |
| INV-12 (测试状态不新增枚举) | 中 | 看板兼容性问题 | T-001 沿用"不适用"既有 |
| INV-13 (2 任务开发状态推进) | 低 | 看板未更新 | T-002 末尾兜底 P-1 |

## 6 项风险(R-1~R-6)与缓解

| 编号 | 风险 | 等级 | 缓解 |
| --- | --- | --- | --- |
| **R-1** | `code-unit/SKILL.md` 既有 E-2 编号冲突 | 中 | T-001 实施时**先 Read SKILL.md** 确认,P-D6 决策 |
| **R-2** | `package.json` 验证伪代码误判 | 低 | 严守 P-D1 判定:"test" in scripts + 非空字符串 |
| **R-3** | `pyproject.toml` 验证伪代码误判 | 低 | 严守 P-D2 判定:4 个测试配置任一存在 |
| **R-4** | 守卫检查项漏写(只写 6 项) | 中 | T-001 严守 7 项(FR-1.AC-1.2) |
| **R-5** | 屏幕输出格式不清晰 | 低 | 严守 §5.2 屏幕输出契约 |
| **R-6** | 看板写入失败 | 中 | A-6 容错:屏幕输出错误,不中断 |

## 触发/来源强约束(REQ-00017)

- 看板"任务清单"区段本计划 2 任务的"触发/来源"列**全部 = "详细设计"**
- **不出现**"= 更新看板"
- 看板推进由 `code-it` 末尾兜底 P-1 承担

## 实施流程约束

- **T-001**(SKILL.md 修改)完成时:**不**自行推进看板(由 `code-it` 末尾兜底 P-1 推进)
- **T-002**(自检 + 看板同步)完成时:**只**追加自检记录 + 看板汇总行 + 任务清单字段(开发/测试状态)
- 看板 5 处同步 = 文档头 + 详细设计汇总 + 任务清单 2 行 + 变更记录(里程碑 0 个不追加)
