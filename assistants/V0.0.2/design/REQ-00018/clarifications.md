# 澄清记录 — REQ-00018
更新时间:2026-06-06 13:00
版本:V0.0.2
需求编码:REQ-00018

## 本轮澄清(沿用 `code-require` 已锁定的 Q-1 / Q-2)

### Q-1:需求范围(沿用 code-require Q1 锁定 A)
- **状态**:已锁定(用户在 `code-require` 阶段回答 A:通用技能增强 + 扫描 CWD 项目类型)
- **本设计影响**:D-1 增量追加 `code-version/SKILL.md` 的"步骤 7"小节;NFR-4 严守"不改 `code-skills` 自身"
- **本设计未提出新澄清**(Q-1 已有结论,沿用)

### Q-2:术语(沿用 code-require Q2 锁定 A)
- **状态**:已锁定(用户在 `code-require` 阶段回答 A:`manifest.json` 社区主流拼写)
- **本设计影响**:扫描清单固定为 `manifest.json`,不保留 `mainfest` 别名
- **本设计未提出新澄清**(Q-2 已有结论,沿用)

## 本设计**未**提出的新澄清

**说明**:基于 `code-require` 阶段已锁定的 2 项 + 已采纳默认的 5 项(Q-3~Q-7),本设计**无**新增的待澄清项。

- Q-3(项目/模块同时支持):采纳默认 — 本设计无歧义
- Q-4(monorepo 不特殊处理):采纳默认 — 本设计无歧义
- Q-5(版本号默认值 = `<版本号>`):采纳默认 — 本设计无歧义
- Q-6(0 命中不报错不阻断):采纳默认 — 本设计无歧义
- Q-7(`--skip-cwd-sync` 不实现):采纳默认 — 本设计无歧义
- Q-8(派生建议:沉淀 `cwd-sync-conventions.md` + 升级 `--skip-cwd-sync` 等 CLI 参数):建议派生,留作 follow-up,本需求**不**阻塞

## 设计目标确认(`code-design` 步骤 0b)

### Q3.1:整体设计目标(粒度倾向)
- **时间**:2026-06-06 13:00
- **问题**:REQ-00018 整体设计目标(粒度倾向)如何?
- **选项**:
  - A. `--balanced`(推荐)— 默认粒度,1~2 任务
  - B. `--minimal` — 1 任务,粒度粗化
  - C. `--extensible` — 3+ 任务,加扩展性任务
- **用户回答**:A(推荐) `--balanced`
- **影响**:本设计**不**引入架构抽象层,Q-8 派生建议留作 v2 follow-up

### Q3.2:功能性优先级(FR 完整度)
- **时间**:2026-06-06 13:00
- **问题**:功能性优先级(FR 完整度)?
- **选项**:
  - A. **高**(推荐) — FR-1~FR-6 全部完整实现
  - B. 中 — 只做 FR-1 + FR-5
  - C. 低 — 仅做 FR-1
- **用户回答**:A(推荐) 高
- **影响**:FR-1~FR-6 全部完整实现;FR-6 `--skip-cwd-sync` 仍按 Q-7 不实现,但其他 5 个 FR 完整

### Q3.3:健壮性优先级(失败/边界覆盖)
- **时间**:2026-06-06 13:00
- **问题**:健壮性优先级(失败/边界覆盖)?
- **选项**:
  - A. **高**(推荐) — 5 类屏幕输出契约全有 + 失败不阻断 + 0 命中不报错
  - B. 中 — 4 类屏幕输出契约,失败不阻断
  - C. 低 — 2 类屏幕输出契约,失败可能阻断
- **用户回答**:A(推荐) 高
- **影响**:5 类屏幕输出契约全有(✓ 成功 / ⚠0 命中 / ⚠解析失败 / ⚠缺版本号字段 / ⚠go.mod);NFR-8 失败不阻断

## 本设计 0 冲突 / 0 偏离 / 0 待澄清

| 维度 | 状态 | 说明 |
| --- | --- | --- |
| 与 `code-require` 阶段 Q-1 / Q-2 锁定一致 | ✅ | 本设计严守 |
| 与 `code-require` 阶段 Q-3~Q-7 采纳默认一致 | ✅ | 本设计严守 |
| 与 13 份项目级规范一致 | ✅ | 本设计严守(rule-compliance.md §6 详) |
| 与 4 份关联设计(REQ-00005/09/15/17)一致 | ✅ | 本设计严守(related-designs.md 详) |
| 新增待澄清项 | **0** | 本设计**不**提出新澄清 |

## 后续 follow-up(留作 v2,本需求不阻塞)

- **Q-8 派生建议**:`code-rule` 沉淀 `cwd-sync-conventions.md`(CWD 描述文件同步约定,工程类型 → 描述文件映射表)
- **Q-8 派生建议**:`code-version` 升级接受 `--cwd=<path>` / `--version-string=<str>` / `--skip-cwd-sync` 等 CLI 参数
- **Q-8 派生建议**:`code-rule` 沉淀"步骤 N 守卫/扩展"统一模式
