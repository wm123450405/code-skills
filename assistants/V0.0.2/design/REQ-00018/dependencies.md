# 三方依赖评估 — REQ-00018
更新时间:2026-06-06 13:00
版本:V0.0.2
需求编码:REQ-00018

## 评估结论:**0 新增依赖**

## 评估明细

### 工具
- `Bash` — 既有
- `Read` — 既有(读 SKILL.md / 描述文件)
- `Glob` — 既有(扫描 CWD 6 类描述文件)
- `Grep` — 既有(可选,辅助解析版本号字段)
- `Edit` — 既有(修改描述文件版本号字段)
- `Write` — 既有(本设计**不**使用,本需求是改既有 SKILL.md)

### 库
- 无(不引入 JSON 解析器 / XML 解析器 / TOML 解析器)
- 改用 `Read` + `Edit` + 行匹配的方式

### 命令
- 无(不调用 jq / xmllint / python 等外部命令)

## 沿用 REQ-00009 模式

REQ-00009 在 `code-unit` 步骤 0a 用 `Glob` 检查项目可测性,7 项文件检查,**0 新增依赖**。

本需求沿用同样的"Glob + 零依赖"模式,在 `code-version` 步骤 7 扫描 CWD 6 类描述文件。

## 依赖管理规范遵循

- ✅ **dependency-conventions**(零新增依赖优先)— **NFR-1 严守**
- ✅ **commit-conventions**(commit 消息格式)— 由 `code-it` / `code-auto` 末步兜底
- ✅ **module-conventions §规则 1**(资源放固定子目录)— 本需求**不**新增资源文件

## 不引入 JSON / XML / TOML 解析器的理由

| 方案 | 优势 | 劣势 | 决定 |
| --- | --- | --- | --- |
| 引入 `jq` / Python `json` | 解析鲁棒 | 引入新依赖(违反 NFR-1) | 否决 |
| 引入 XML 解析器 | 解析鲁棒 | 引入新依赖 | 否决 |
| **用 `Read` + `Edit` + 行匹配** | **0 新依赖;轻量;对齐"Edit 锚点"既有模式** | **对极端非标格式(嵌套 JSON / 注释 JSON)有边界退化(E-3 走 ⚠ 跳过)** | **选定 A** |

**关键约束**:对齐"### 7.3 描述文件解析契约" — 每类描述文件用固定的"版本号字段"锚点(JSON / XML / TOML 都是简单的"version 字段"形式),用 `Read` + 简单行匹配即可处理。

## monorepo 性能边界

- 单文件 ~1 秒:`Read` + `Edit` + `Write` 三步,网络/磁盘 IO 0
- 10 个文件 ~5 秒:线性叠加,无并发必要
- > 20 个文件:退化,屏幕 `⚠` 提示但不阻断
