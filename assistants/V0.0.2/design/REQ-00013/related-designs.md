# 关联概要设计 — REQ-00013
更新时间:2026-06-05 21:00
版本:V0.0.2

## 同版本关联设计

| 需求 | 关联点 | 对本设计的影响 |
| --- | --- | --- |
| REQ-00005 | `code-require` / `code-design` / `code-plan` "步骤 0a 拉取" + "步骤 N 末尾兜底提交" 模式 | 本需求 M-1 / M-2 的"步骤 0.5 标题解析"段沿用同位叠加模式(锚点 = 步骤 0a / 0b 之后)|
| REQ-00007 | `code-auto` 编排器 + 7 步状态机 + 子技能调用表 | 本需求 M-7 `code-auto` 增量追加"子技能标题预读"段,沿用既有屏幕日志格式 `[code-auto] 步骤 N/M:` |
| REQ-00008 | `code-review` 整版本模式 + 派生任务 | 本需求 M-6 `code-review` 增量追加"需求/任务标题解析"段,派生任务"标题"列截断(NFR-10)|
| REQ-00009 | `code-unit` "项目可测性"守卫 + 跳过报告 | 本需求 M-5 `code-unit` 守卫跳过报告含"编号+标题"(FR-7.AC-7.2)|
| REQ-00010 | `code-it` "前置任务"守卫 + 中止报告 | 本需求 M-4 `code-it` 中止报告含"REQ-NNNNN · 标题" + "TASK-... · 标题"(FR-6.AC-6.2)|
| REQ-00011 | `code-design` / `code-plan` 步骤 0b 设计目标确认 | 本需求 M-1 / M-2 增量追加段不与"步骤 0b"冲突(锚点不同)|
| REQ-00014 | `code-plan` 任务拆分维度(按功能点)| 本需求 M-2 不影响"按功能点拆分"规则,只追加"标题解析"段 |
| REQ-00016 | `code-design` / `code-plan` 快模式 | 本需求 M-1 / M-2 在快模式 + 完整模式下行为一致(增量追加段对两种模式通用)|
| REQ-00017 | `code-plan` 不拆"更新看板"派生任务 + `code-it` 末尾兜底推进看板 | 本需求 M-2 / M-4 不影响既有 P-1 推进看板逻辑,只追加"标题解析"段 |

## 跨版本关联(可选)

| 需求 | 所在版本 | 关联点 | 对本设计的影响 |
| --- | --- | --- | --- |
| REQ-00001 | V0.0.1 | V0.0.1 任务的"标题"已存在(在 `plan/.../PLAN.md` 任务总览) | NFR-4 历史自动生效,本需求 0 触发手动回填 |
| REQ-00004 | V0.0.2 | `code-dashboard` 看板"任务清单"区段"标题"列已存在 | NFR-2 零规范变更,本需求 0 触发 `dashboard-conventions §规则 1` 3 处同步 |

## 重点关注

- **REQ-00005(本仓库首个"步骤 0a 拉取+末步兜底提交"需求)**:本需求沿用其增量追加模式,锚点定位 = "## 工作流程"前,与"步骤 0a"段同位叠加
- **REQ-00007(`code-auto` 编排器)**:本需求 M-7 必须沿用 `code-auto` 既有屏幕日志格式(`[code-auto] 步骤 N/M:...`),仅在每个步骤的"子技能名"后追加"REQ-NNNNN · 标题"
- **REQ-00017(`code-it` 末尾兜底推进看板 P-1)**:本需求 M-4 不与 P-1 冲突 — P-1 推进"开发状态"列,本需求仅在屏幕输出位置拼接"编号+标题"
