# 待澄清与决策记录 — REQ-00013
更新时间:2026-06-05 21:00
版本:V0.0.2

## 概要

本需求上游 `require/REQ-00013/RESULT.md` 已锁定 4 项决策(Q-1~Q-4),本设计阶段 0 新增待澄清(全部 8 项设计问题均已在本阶段锁定,详 `design-notes.md`)。

## 已锁定的 4 项需求阶段决策(Q-1~Q-4,沿用上游)

| ID | 问题 | 用户回答 | 影响的 RESULT.md 章节 |
| --- | --- | --- | --- |
| **Q-1** | "标题"从已有内容派生,还是新增字段? | **A. 从已有内容派生,不新增字段**(零规范变更) | §3 业务目标 G-2 / FR-1 / NFR-2 / NFR-4 |
| **Q-2** | 编号+标题的拼接格式? | **A. `REQ-00001 · 标题`**(中点 `·` + 半角空格) | §6 页面与界面 / FR-2 / AC-2.1~2.3 |
| **Q-3** | 标题字符数上限? | **A. ≤ 30 字符**(超 30 截断 + `...`) | §3 G-4 / FR-1.AC-1.4 / NFR-3 / §6.5 |
| **Q-4** | 本轮升级几个技能? | **A. 6 技能**(3 生成源 + 4 消费方,`code-dashboard` 不变) | §2.3 / §11 / FR-3~9 |

## 采纳默认的 6 项需求阶段决策(Q-5~Q-10,沿用上游)

| ID | 问题 | 默认选项 | 影响的 RESULT.md 章节 |
| --- | --- | --- | --- |
| **Q-5** | 历史回填 | **A. 自动生效**(不手动回填) | §2.2 / NFR-4 |
| **Q-6** | `code-review` 派生任务的"标题" | **A. 本轮 `code-review` 升级** | §2.3 / FR-8 / NFR-10 |
| **Q-7** | 与 `code-publish` / `code-dashboard` 协同 | **A. `code-publish` 升级报告;`code-dashboard` 不变** | §2.3 / FR-10 / NFR-5 |
| **Q-8** | 与 `code-fix` 的关系 | **A. 本轮首次升级 `code-fix`** | §2.3 / FR-5 |
| **Q-9** | 与 REQ-00005 协同 | **A. 独立新增** | §2.3 |
| **Q-10** | `commit-conventions.md` 与 CLAUDE.md 追加 | **A. 不追加,留作 follow-up** | §11 / NFR-8 |

## 本阶段新增的设计问题与决策(8 项,详 `design-notes.md`)

| ID | 问题 | 选定方案 | 影响 |
| --- | --- | --- | --- |
| **D-1** | 标题字段从哪些既有内容派生? | **B + C**(多源 + 共享截断函数) | M-1~M-8 各模块设计 |
| **D-2** | 6 技能如何以"非破坏性"方式启用"编号+标题"? | **B**(增量追加,锚点 = 步骤 0 / "## 工作流程"前) | M-1~M-7 锚点定位 |
| **D-3** | 标题截断算法如何保证一致? | **B**(共享 `truncateTitle`) | 所有模块统一算法 |
| **D-4** | 何时显示"无标题"占位? | **B**(显示"(无标题)")| E-3 退化策略 |
| **D-5** | 派生任务的"标题"如何截断? | **A**(`code-review` 写入 `PLAN.md` 时截断) | M-6 锚点位置 |
| **D-6** | `code-fix` 的"## 缺陷标题"小节如何新增? | **B**(增量追加,自生成) | M-3 步骤 1 末尾追加 |
| **D-7** | `code-publish` 报告如何升级? | **B**(PreflightChecker 拼接格式) | M-8 锚点位置 |
| **D-8** | `code-auto` 进度日志如何嵌入? | **A**(自读"标题"源) | M-7 屏幕日志格式 |
