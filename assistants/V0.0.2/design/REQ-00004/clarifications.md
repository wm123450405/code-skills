# 澄清记录 — REQ-00004

更新时间:2026-06-04 15:50
版本:V0.0.2

## 来自上游(`code-require` 已锁定的 Q-1 ~ Q-3)
详见 `./assistants/V0.0.2/require/REQ-00004/clarifications.md` 2026-06-04 12:50 段。

| Q | 决议 | 影响章节 |
| --- | --- | --- |
| Q-1 | ASCII 进度表 + 文本柱状图 | §6.1(段 1 详细布局)+ §7(段 1 渲染器) |
| Q-2 | 可执行命令推荐(> 建议: 执行 /code-it REQ-00004-001) | §6.2(段 4 详细布局)+ §8(建议数据结构) |
| Q-3 | 只突出 P0/P1 高优先级 | §6.1(段 3 缺陷段) |

## 上游采纳的默认(Q-4 / Q-5)
| Q | 默认 | 状态 | 影响 |
| --- | --- | --- | --- |
| Q-4 | 无激活版本时,打印引导 + 提示 `code-version` + 退出 | 已采用,FR-5 落定 | §7.1 启动流程 / §9 边界 E-1 |
| Q-5 | 全部显示,> 20 时加"显示前 10 + 概要 11+"提示 | 已采用,NFR-4 落定 | §6.1 总览 / §7.4 渲染策略 |

## 上游未采用(Q-7 / Q-8)
| Q | 未采用 | 备注 |
| --- | --- | --- |
| Q-7 | "列出所有历史版本"作为附加能力 | 用户原话未提,留作 follow-up |
| Q-8 | "切回上一版本/查看历史版本" | 属 `code-version` 范畴,本需求边界外 |

## 本次(`code-design` 阶段)新澄清项

### Q-D1:本设计的"下一步建议"是否需要在 V0.0.x 已有 REQ-00005 ~ REQ-00013 时(看板已记录)做特殊处理
- **背景**:V0.0.2 看板"需求清单"已登记 REQ-00004 ~ REQ-00013 共 10 条(均为"已完成(需求分析)"),本设计阶段"需求进度"统计将显示"已完成 10"
- **选项**:
  - A. 显示"已完成 10"即可;用户从"下一步建议"判断后续动作(推荐)
  - B. 在"需求进度"段追加一行"含设计完成"(`已完成+概要设计已完成` 的子计)
- **用户回答**:A(默认)
- **影响**:§6.1 段 1 仅显示"需求状态=已完成"计数,不细分设计阶段;§7 建议生成器按"需求=已完成 ∧ 概要设计≠已完成" 时推荐 `/code-design REQ-NNNNN`,按"全完成"时推荐 `/code-version V0.0.x`
- **解决时间**:2026-06-04 15:50

### Q-D2:`code-dashboard` 是否要读"评审发现汇总"区段
- **背景**:V0.0.2 看板的"评审发现汇总"区段由 `code-review` 维护;FR-2 仅列 3 段(需求/任务/缺陷),未提评审
- **选项**:
  - A. 不读(NFR-2 错误处理已覆盖"区段缺失不崩溃";NFR-4 性能约束)
  - B. 读,作为副显(类似"里程碑"在 §8.2 中标注"总览模式副显")
- **用户回答**:A(默认)
- **影响**:§6.1 仅 3 段(需求/任务/缺陷)+ §6.2 建议段;§8.2 解析锚点表"里程碑"段标注降级为"可选";"评审发现汇总"不解析
- **解决时间**:2026-06-04 15:50

### Q-D3:ASCII 比例条宽度(柱状图字符数)
- **背景**:Q-1 锁定 ASCII 柱状图,但字符数未明确;§6.2 示例用 12 字符(`████████████`)
- **选项**:
  - A. 固定 12 字符(短平快,推荐)
  - B. 动态(总计数比例 1:N,N=总任务数)
- **用户回答**:A(默认)
- **影响**:§7.4 渲染器固定 `BAR_WIDTH = 12`;§6.1 详细布局示例按 12 字符
- **解决时间**:2026-06-04 15:50

### Q-D4:需求粒度模式是否复用"显示前 10 + 概要 11+"折叠策略
- **背景**:Q-5 是为"版本总览"模式的多需求场景设计;需求粒度模式任务是"该需求下任务",通常 < 20
- **选项**:
  - A. 不复用(任务数 N 较小,直接全部显示,推荐)
  - B. 复用同一规则
- **用户回答**:A(默认)
- **影响**:§7.4 渲染策略分两模式:总览模式触发 Q-5 折叠;需求模式无条件全显
- **解决时间**:2026-06-04 15:50

## 留作 follow-up
- F-1:`code-review/SKILL.md` frontmatter `<version>` 笔误(超出本需求范围)
- F-2:`code-dashboard` 是否在"任务清单"段附加"近期提交哈希"列(留作 v2 增量)
- F-3:跨版本汇总能力(用户原话未提;留作 v2)
