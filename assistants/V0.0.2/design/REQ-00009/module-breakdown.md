# 模块拆分 — REQ-00009
更新时间:2026-06-05 17:10
版本:V0.0.2

## 总览

| 编号 | 模块 | 路径 | 状态 | 职责 | 涉及规范 |
| --- | --- | --- | --- | --- | --- |
| M-1 | `code-unit` SKILL.md | `plugins/code-skills/skills/code-unit/SKILL.md` | **修改(增量追加)** | 新增"步骤 0a 项目可测性检查"守卫 | `skill-conventions §规则 1` |

**模块新增数:0**
**模块修改数:1**(M-1)
**模块复用数:0**(仅复用既有"步骤 0"及之后的单测流程)

---

## M-1:`code-unit/SKILL.md`(修改)

### 职责
- 在既有"步骤 0 版本上下文检测"**之前**新增"步骤 0a 项目可测性检查"守卫
- 守卫检查项目根 7 项文件/目录
- 守卫通过 → 走原"步骤 0"+ 单测流程
- 守卫不通过 → 跳过 + 写看板"任务清单"区段测试状态 = `不适用` + exit 0

### 对外暴露的接口
- **无新增接口**:`code-unit` 入口签名 = `code-unit <任务编码>`,沿用既有
- **行为契约**:
  - 输入:`code-unit TASK-REQ-00009-00001`
  - 守卫通过:与原 `code-unit` 行为完全一致(FR-4 强约束)
  - 守卫不通过:跳过 + 写看板"不适用" + exit 0(FR-2 + FR-3 + NFR-6)
  - 屏幕输出:守卫检查结果(具体哪些文件命中/未命中)

### 依赖
- **对内其他模块**:**0**(无新增依赖)
- **对外三方**:**0**(无新增依赖)
- **Claude Code 工具**:
  - `Glob` — 检查文件/目录存在
  - `Read` — 必要时验证 `package.json` / `pyproject.toml` 内容
  - `Edit` — 增量追加 SKILL.md 子节
  - **不**调 `Bash` — 守卫不通过时不跑测试命令(FR-2.AC-2.4)

### 关键决策
- **决策 1**:守卫插入"步骤 0"**之前**(沿用 REQ-00005 / REQ-00010 的"步骤 0a"命名约定)
- **决策 2**:`package.json` 需含 `scripts.test` 才算可测(FR-1 显式要求)
- **决策 3**:`pyproject.toml` 需含测试配置才算可测(FR-1 显式要求)
- **决策 4**:守卫不通过时退出码 = 0(NFR-6,让 `code-auto` 继续)
- **决策 5**:守卫不通过时**不**写 `test/<任务编码>/RESULT.md`(FR-3 + Q-3)
- **决策 6**:`--force` 参数**不**提供(NFR-8)

### 修改范围(预估)

| 修改类型 | 位置 | 预估行数 |
| --- | --- | --- |
| 新增子节 | "步骤 0a 项目可测性检查"(在"步骤 0 版本上下文检测"之前) | ~30 行 |
| 新增子节 | "步骤 0a.1 守卫检查项清单"(7 项) | ~15 行 |
| 新增子节 | "步骤 0a.2 守卫判定逻辑" | ~10 行 |
| 新增子节 | "步骤 0a.3 跳过流程" | ~10 行 |
| 新增子节 | "步骤 0a.4 屏幕报告格式" | ~10 行 |
| **删除** | **0 行** | 0 |
| **修改** | **0 行**(既有子节字节级保留) | 0 |
| **净增** | | **~30 行** |

> 实际行数以实施时 SKILL.md 最新版本为准(NFR-2 增量追加)。

### 规范条款自检

| 规范条款 | 本模块满足情况 |
| --- | --- |
| `skill-conventions §规则 1`:frontmatter 必含 name+description | ✅ 字节级保留(FR-1.AC-1.5) |
| `module-conventions §规则 1`:资源放固定子目录 | ✅ 不触达,无资源新增 |
| `dashboard-conventions §规则 1`:字段约定扩展需 3 处同步 | ✅ **不触发**(沿用"不适用"既有枚举) |
| `doc-conventions §规则 1`:中英同次 | ✅ 不触达,无中英 README 变更 |
| `marketplace-protocol §规则 1`:skills 数组以 `./` 开头 | ✅ 不触达,无新增技能 |
| `encoding-conventions §规则 1-4`:任务编号 5+5 位嵌套 | ✅ 不触达,本需求无新编码 |

### 既有内容保留清单(NFR-2 字节级保留)

| 既有内容 | 状态 |
| --- | --- |
| YAML frontmatter(L1-3) | 字节级保留 |
| §目标 / §适用场景 / §不适用 | 字节级保留 |
| §工作目录约定 | 字节级保留 |
| §输入 / §输出 | 字节级保留 |
| §工具使用约定 | 字节级保留 |
| §工作流程 → 步骤 0 版本上下文检测 | 字节级保留 |
| §工作流程 → 步骤 1-16(原 16 步) | 字节级保留 |
| §过程文档格式 | 字节级保留 |
| §衔接 | 字节级保留 |
| §不要做的事 | 字节级保留 |
| 既有 E-1~E-7 边界场景 | 字节级保留(本需求**新增** E-2 / 预留 E-8) |

### 边界场景新增

| ID | 场景 | 处理 |
| --- | --- | --- |
| **E-2(新增)** | 守卫不通过(不可测) | FR-2 跳过 + 看板"不适用" + exit 0 |
| **E-8(预留)** | 守卫检查项扩展(如未来加 `deno.json`) | v2 增量追加,本需求不实现 |

> 边界 E-3~E-7 沿用既有,本需求**不**修改。
