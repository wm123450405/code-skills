# 关联概要设计 — REQ-00009
更新时间:2026-06-05 17:10
版本:V0.0.2

## 横向关联(同版本 V0.0.2)

| 关联需求 | 关联点 | 对本概要设计的影响 |
| --- | --- | --- |
| **REQ-00004** | `code-dashboard` 现有"任务清单"区段 | 沿用既有"不适用"枚举(NFR-3),**0** 改动 |
| **REQ-00005** | "首步拉取 + 末步兜底提交"模式 | 沿用"步骤 0a"命名约定(本需求在 SKILL.md `步骤 0` 之前插"步骤 0a 项目可测性检查") |
| **REQ-00006** | `code-publish` 前置检查 | 沿用"测试∈{已运行-通过, 不适用}"判定(NFR-5),**0** 改动 |
| **REQ-00007** | `code-auto` 编排者 | NFR-6 退出码 = 0,守卫不通过时 `code-auto` 继续 |
| **REQ-00008** | `code-review` 整版本模式 | 本需求**不**影响 review 逻辑(测试状态字段不变) |
| **REQ-00010** | `code-it` 步骤 0a 前置任务检查 | 与本需求**同位**叠加(两个技能各自有"步骤 0a"),**0** 冲突 |
| **REQ-00011** | `code-design` / `code-plan` 步骤 0b 设计目标确认 | 与本需求**0** 关联(设计阶段不涉及) |
| **REQ-00012** | 仓库根 README + CLAUDE.md 移动 | 本需求**不**改 README / CLAUDE.md |
| **REQ-00013** | "编号+标题"显示 | 本需求**0** 关联(本需求不涉及显示优化) |
| **REQ-00014** | `code-plan` 任务拆分维度(按功能点) | 本需求**0** 关联(任务拆分是 plan 阶段) |
| **REQ-00015** | `code-merge` 合并流程 | 本需求**0** 关联 |
| **REQ-00016** | `code-design` / `code-plan` 快模式 | 本需求**0** 关联 |
| **REQ-00017** | `code-plan` 不再为"更新看板"拆派生任务 + `code-it` 末尾兜底后推进看板 | 本需求**协同**:`code-it` 末尾兜底推进"开发状态"(`code-unit` 不感知);`code-unit` 守卫不通过时推进"测试状态=不适用" |

## 跨版本(可选)

- **V0.0.1** — `code-unit` 在 V0.0.1 中 21/21 任务"测试状态=不适用"(全文档/规范类)
  - 本需求落地后,V0.0.1 的 21 任务**不**追溯(守卫仅对 V0.0.2 之后的任务生效)
  - V0.0.1 既有"不适用"枚举在 V0.0.2 复用(NFR-3 强约束)

## 关联点深度分析

### REQ-00005(沿用"步骤 0a"命名)

| 维度 | REQ-00005 既有 | REQ-00009 本需求 |
| --- | --- | --- |
| 步骤 0a 命名 | ✓(`code-require` 步骤 0a 拉取) | ✓(本需求 `code-unit` 步骤 0a 守卫) |
| 位置 | 步骤 0 之前 | 步骤 0 之前 |
| 模式 | 拉取最新代码 | 项目可测性检查 |
| 失败行为 | 中断 + 报错退出(Q-2 锁定) | **跳过 + 写看板"不适用"**(本需求 NFR-6) |
| 退出码 | ≠ 0 | 0 |

> 命名沿用,行为不同(各自的 Q-2 锁定不同)。

### REQ-00010(同位叠加)

| 维度 | REQ-00010 既有 | REQ-00009 本需求 |
| --- | --- | --- |
| 涉及技能 | `code-it` | `code-unit` |
| 步骤 0a 命名 | ✓(前置任务检查) | ✓(项目可测性检查) |
| 失败行为 | 中断 + 报错退出(Q-3 锁定) | **跳过 + 写看板"不适用"** |
| 退出码 | ≠ 0(`code-auto` 据此中断) | 0(`code-auto` 据此继续) |
| 共同点 | 都在"步骤 0"之前插入辅助检查 | 都在"步骤 0"之前插入辅助检查 |

> 命名同位,行为**不同**(两个技能的设计目标不同 — `code-it` 是"前置不完成则中止",`code-unit` 是"项目不可测则跳过")。

### REQ-00007(`code-auto` 协同)

| 维度 | REQ-00007 既有 | REQ-00009 本需求 |
| --- | --- | --- |
| `code-auto` 任务循环 | 调 `code-it` + 条件 `code-unit` | 调 `code-it` + 条件 `code-unit`(**0** 改动) |
| `code-unit` 退出码期望 | 0(继续) | 0(NFR-6,守卫不通过时仍 0) |
| 协同效果 | 反复调 `code-unit` | 守卫让"无意义调用"自动消失 |

> 本需求让 `code-auto` 在"项目不可测"场景下,任务循环中所有 `code-unit` 调用都**自动跳过**,**0** 中断,真正"全自动跑通"。

### REQ-00004 + REQ-00006 + REQ-00008(下游兼容)

| 维度 | 现状 | 本需求影响 |
| --- | --- | --- |
| `code-dashboard` 看板"任务清单"区段 | 测试状态枚举 = `未编写` / `已编写` / `已运行-通过` / `已运行-失败` / `不适用` / `阻塞` | **0** 改动(沿用"不适用"既有枚举) |
| `code-publish` 前置检查 | 任务"测试∈{已运行-通过, 不适用}" → 任务解决 | **0** 改动(本需求让更多任务"测试=不适用",仍属"任务解决") |
| `code-review` 评审逻辑 | 沿用看板"测试状态"列 | **0** 改动(本需求**不**影响 review 字段) |

> 严格遵循 NFR-3 / NFR-4 / NFR-5 三项兼容性约束。

### REQ-00017(`code-it` 末尾兜底推进看板协同)

| 维度 | REQ-00017 既有 | REQ-00009 本需求 |
| --- | --- | --- |
| `code-it` 末尾兜底 P-1 | 推进本任务"开发状态"(待开发 → 已完成) | **0** 冲突(`code-unit` **不**感知 P-1) |
| `code-unit` 守卫不通过 | (无,本需求新增) | 推进"测试状态" = 不适用 |
| 看板写入路径 | `code-it` 写"开发状态"列;`code-unit` 写"测试状态"列 | 各自独立,**0** 冲突 |
| 触发 | 末尾兜底 commit 成功后 | 守卫判定后 |

> 看板"开发状态"和"测试状态"是**正交**两轴,本需求与 REQ-00017 分别推进,**0** 冲突。
