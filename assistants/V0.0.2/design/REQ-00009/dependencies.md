# 依赖评估 — REQ-00009
更新时间:2026-06-05 17:10
版本:V0.0.2

## 总结论
**新增三方依赖:0**(NFR-1 锁零依赖)

---

## 评估明细

### 运行时依赖(守卫实现所需)

| 依赖 | 类型 | 必要性 | 来源 | 维护活跃度 | 体积/性能 | 安全态势 | 项目内替代 | 决策 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `Glob` 工具 | Claude Code 内置 | 检查文件/目录存在 | Claude Code | — | < 50 ms / 7 次调用 | — | 无需替代 | **复用** |
| `Read` 工具 | Claude Code 内置 | 读 `package.json` / `pyproject.toml` 验证 | Claude Code | — | < 100 ms | — | 无需替代 | **复用** |
| `Edit` 工具 | Claude Code 内置 | 增量追加 SKILL.md 子节 | Claude Code | — | < 200 ms | — | 无需替代 | **复用** |
| `Bash` 工具 | Claude Code 内置 | **不调用**(守卫不通过时) | Claude Code | — | 0 ms(不调) | — | — | **不用** |

> 守卫不通过时**不**调 `Bash` 跑测试命令(FR-2.AC-2.4 强约束)。

### 工具集自检

- ✅ `Glob` / `Read` / `Edit` — Claude Code 内置,无需新增
- ✅ `Bash` — 仅在"可测"流程中调用(原行为,本需求不修改)
- ✅ 0 三方 npm/pip/cargo/go/maven 依赖
- ✅ 0 新增容器 / 服务 / 数据库

### 性能估算(NFR-7 < 1 秒)

| 步骤 | 操作 | 预估耗时 |
| --- | --- | --- |
| 步骤 1 | `Glob` 7 项文件/目录 | ~50 ms |
| 步骤 2 | `Read` `package.json`(若存在) | ~50 ms |
| 步骤 3 | `Read` `pyproject.toml`(若存在) | ~50 ms |
| 步骤 4 | 判定 + 写看板"不适用"(若守卫不通过) | ~50 ms |
| **合计** | | **~200 ms** |

**远低于 1 秒**(NFR-7 强约束达成)。

### 安全态势

- **不引入**新依赖 → **不增加**新攻击面
- **不**调网络命令 → **不**触发凭据泄露风险
- **不**写敏感数据 → 看板"不适用"是公开状态
- **不**调 `Bash` 跑任意命令 → 守卫不通过时**不**执行外部命令(FR-2.AC-2.4 强约束)

### 许可协议

- **不适用**(无新增三方依赖)

### 体积/性能影响

- SKILL.md 净增 ~30 行
- 运行时无额外内存/CPU 开销(< 200 ms 一次)

---

## 与既有 11 个 `code-*` 技能的依赖关系

| 技能 | 是否依赖本守卫 | 原因 |
| --- | --- | --- |
| `code-version` | **否** | 独立的版本管理 |
| `code-require` | **否** | 独立的需求分析 |
| `code-design` | **否** | 独立的概要设计 |
| `code-plan` | **否** | 独立的详细设计 / 任务计划 |
| `code-it` | **否** | 独立的开发编码 |
| `code-fix` | **否** | 独立的缺陷登记 |
| `code-review` | **否** | 独立的代码评审 |
| `code-dashboard` | **否** | 沿用"不适用"既有枚举,**0** 改动 |
| `code-publish` | **否** | 沿用"测试∈{已运行-通过, 不适用}"判定,**0** 改动 |
| `code-auto` | **是**(协同,非依赖) | 任务循环据守卫退出码 0 继续(NFR-6) |
| `code-merge` | **否** | 独立的合并流程 |

> **关键**:`code-auto` 与本守卫是**协同**关系(非"依赖")。`code-auto` 不修改,本守卫不修改 `code-auto`。

---

## 横向对比(与 REQ-00005 / REQ-00010 的依赖风格)

| 需求 | 涉及技能 | 依赖 | 备注 |
| --- | --- | --- | --- |
| REQ-00005 | `code-require` / `code-design` / `code-plan` | 0 新增(沿用 `Bash` 调 `git pull`) | "首步拉取 + 末步兜底提交" |
| REQ-00010 | `code-it` | 0 新增(沿用 `Read` 读 `PLAN.md`) | "步骤 0a 前置任务检查" |
| **REQ-00009(本需求)** | `code-unit` | **0 新增(沿用 `Glob` + `Read`)** | **"步骤 0a 项目可测性检查"** |

> 三者风格一致:0 新增三方依赖,沿用 Claude Code 内置工具,**0 触发**任何规范变更。
