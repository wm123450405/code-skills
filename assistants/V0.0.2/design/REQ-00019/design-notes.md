# 设计笔记 — REQ-00019

## 关键设计问题清单

### Q-1:`code-plan` 缺陷分支应产出哪些文档?

- **方案 A**(选定):产出 `RESULT.md` + `PLAN.md` + 7 份过程文档(完全同构 REQ 模式)
- **方案 B**(否决):继续产出 `fix-plan.md` + 补 7 份过程文档(双文件并存,需 `code-it` 步骤 17 双输入兼容)
- **方案 C**(否决):沿用 `fix-plan.md` + 7 份过程文档(保持现状,违反用户"形式同构"目标)
- **决策依据**:用户 Q-1 锁定 A(形式同构 + 路径全替)

### Q-2:BUG 任务应使用什么编号?

- **方案 A**(选定):新格式 `TASK-BUG-NNNNN-NNNNN`(5+5 位嵌套式,沿用 `encoding-conventions §规则 1/3`)
- **方案 B**(否决):沿用 `fix-plan.md` 内"步骤"列表(不进入任务系统)
- **方案 C**(否决):两种并存,大修复走新格式,小修复继续"步骤"列表(粒度难判定)
- **决策依据**:用户 Q-2 锁定 A

### Q-3:BUG 任务应进哪个看板区段?

- **方案 A**(选定):原"任务清单"区段收录(沿用既有区段,0 触发 `dashboard-conventions §规则 1` 三同步)
- **方案 B**(否决):独立"缺陷任务"区段(新增区段,触发三同步)
- **方案 C**(否决):两区段并存(双向链接,信息冗余)
- **决策依据**:用户 Q-3 + Q-7 锁定 A

### Q-4:`code-it` BUG 路径改造深度?

- **方案 A**(选定):彻底同步升级(去掉 `fix-plan.md`,4 项联动全选)
- **方案 B**(否决):双文件过渡期(`code-it` 步骤 17 优先读 `PLAN.md` 兼容读 `fix-plan.md`)
- **方案 C**(否决):改造 `PLAN.md` 读取,保留 `fix-` 前缀过程文档(命名不一致)
- **决策依据**:用户 Q-4 + Q-6 锁定 A

### Q-5:历史 BUG-00001 如何处理?

- **方案 A**(选定):仅未来新缺陷生效,不迁移 BUG-00001
- **方案 B**(否决):迁移现有 BUG-00001(可能引入新偏离)
- **方案 C**(否决):提供迁移工具(用户选)
- **决策依据**:用户 Q-5 锁定 A

## 关键决策(5 项)

| 决策 ID | 决策内容 | 选定方案 | 理由 |
| --- | --- | --- | --- |
| **D-1** | `code-plan/SKILL.md` 步骤 19-28A 重构范围 | 步骤 19-22 微调 + 步骤 23 +E-1 边界 + 步骤 24A 重写 + 步骤 25A 保留 + 步骤 26A 改"按 `templates/plan.md` 同构产出" + 步骤 27A 保留 + 步骤 28A 保留 + 步骤 28A+1 新增 | 既保留既有结构,又满足"形式同构"目标 |
| **D-2** | `code-it/SKILL.md` 步骤 17-25 改造范围 | frontmatter L5 改 + 步骤 17 改读 `PLAN.md` + `RESULT.md` + 步骤 22-25 过程文档去 `fix-` 前缀 + 步骤 24 不再写 `fix-plan.md` + 步骤 25 汇报同步 | 联动改造以保证 BUG 模式上下游一致 |
| **D-3** | BUG 任务编号格式 | `TASK-BUG-NNNNN-NNNNN`(5+5 位嵌套式) | 沿用 `encoding-conventions §规则 1/3`;0 触发三同步 |
| **D-4** | BUG 任务看板呈现 | 原"任务清单"区段收录(沿用既有 12 列字段;类型=`修复`;触发/来源=`缺陷修复`;关联任务=`BUG-NNN`) | 0 新增字段 / 0 新增枚举值;0 触发三同步 |
| **D-5** | 历史 `fix-plan.md` + `fix-` 前缀过程文档 | BUG-00001 保留;`code-plan` 步骤 23 / `code-it` 步骤 17/22/24 增加 E-1/E-7/E-9/E-11 边界(检测 + 提示 + 退化) | 不破坏既有数据;用户偏好最小变更 |

## 候选方案(否决)详述

### 候选 A1(双文件并存 — 否决)
- **否决理由**:增加认知负担;`code-it` 步骤 17 需双输入兼容;与用户"形式同构"目标不符
- **影响范围**:FR-1 / FR-4 / NFR-1

### 候选 A2(`fix-plan.md` 替代 `PLAN.md` — 否决)
- **否决理由**:与既有 REQ 路径的"任务清单"机制脱钩;`code-dashboard` 看板无法展示 BUG 任务进度
- **影响范围**:FR-1 / FR-3

### 候选 A3(新增"BUG 修复"枚举值 — 否决)
- **否决理由**:触发 `dashboard-conventions §规则 1` 三同步;沿用既有"缺陷修复"枚举(在 13 枚举中)
- **影响范围**:FR-3 / NFR-2.1

### 候选 A4(`code-it` 仅读 `PLAN.md` — 否决)
- **否决理由**:与既有 REQ 任务路径**不同构**;`RESULT.md` 包含详细设计点,实施时需读
- **影响范围**:FR-4

### 候选 A5(BUG 任务编号沿用 BUG 编号 — 否决)
- **否决理由**:与 REQ 任务编号分配算法**不同构**;沿用既有 5+5 位嵌套式
- **影响范围**:FR-3 / NFR-2.2

## 实施期关键决策(待后续阶段锁定)

| 决策 ID | 内容 | 现状 |
| --- | --- | --- |
| **P-D1** | `code-plan/SKILL.md` 步骤 19-28A 重构后行数 | 估计 +100 ~ +200 行(基线 945 行,允许 +20% 上限 = 1134 行) |
| **P-D2** | `code-it/SKILL.md` BUG 路径步骤 17-25 改造后行数 | 估计 0 净增(替换既有锚点文字,不改步骤数) |
| **P-D3** | `templates/fix-plan.md` 是否删除 | 不删除,留作历史;`code-plan` 步骤 26A 改为引用 `templates/plan.md` + `templates/task-plan.md` |
| **P-D4** | BUG 任务编号起始 | `TASK-BUG-NNNNN-00001`(5 位,与 REQ 任务一致) |
| **P-D5** | BUG 任务"需求"列填法 | `BUG-NNN`(3 位,与 REQ 任务的 5 位不同;用户接受) |
| **P-D6** | 看板"任务清单"区段 BUG 任务行"关联任务"列 | 填本缺陷 `BUG-NNN`(自查) |
| **P-D7** | `code-fix/SKILL.md` 是否修改 | **不修改**(FR-8 强约束;0 改动) |
| **P-D8** | 实施时机 | 本需求走标准流程:`code-plan` → `code-it`(2 任务:`code-plan/SKILL.md` 改造 + `code-it/SKILL.md` 改造)→ `code-review` |
