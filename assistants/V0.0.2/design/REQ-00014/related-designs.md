# 关联概要设计 — REQ-00014

更新时间:2026-06-05 12:50
版本:V0.0.2
需求:REQ-00014(优化技能 `/code-plan` 的任务拆分维度)

## 扫描范围

- **同版本**:`./assistants/V0.0.2/design/*/RESULT.md` — **已扫描**
- **跨版本**:无直接关联(本需求是元需求,优化工作流本身)
- **元层级**:本需求修改 `plugins/code-skills/skills/code-plan/SKILL.md`(不修改 `assistants/` 任何文件)

## 同版本关联设计清单

| 需求 | 概要设计状态 | 关联点 | 对本设计的影响 |
| --- | --- | --- | --- |
| REQ-00004 dashboard | 已完成(L160) | `code-dashboard` 看板"任务清单"区段字段(任务编号/开发/测试状态)沿用 `code-plan` 输出格式 | **0** 影响(本需求不修改任务编号/双状态/触发来源 — 沿用既有) |
| REQ-00005 首步拉取 | 已完成(L180) | `code-require` / `code-design` / `code-plan` 步骤 0a 拉取 + 末步提交 | **0** 影响(本需求不改工作流步骤 0a) |
| REQ-00006 publish | 已完成(L210) | `code-publish` 读取 `code-plan` 输出的 PLAN.md "任务清单" 作为前置检查 | **0** 影响(本需求不改任务状态语义) |
| REQ-00007 auto | 已完成(L230) | `code-auto` 读取 PLAN.md 任务清单驱动 code-it / code-unit | **0** 影响(本需求不修改任务格式) |
| REQ-00014(本需求) | — | (自身) | — |

## 关键观察

- **所有既有概要设计对本需求 0 影响** — 因为本需求**只**改 `code-plan` 自身的工作流定义,不改其他任何模块
- **本需求**反向被未来需求使用 — V0.0.2 未来所有需求(REQ-00008+)将由新规则拆分
- **新规则的"对照样板"**:REQ-00007 偶然"功能点化"的成功(5 任务 = 写 SKILL.md / 改 JSON / 改 README / 改看板 / 自检)是本优化的"目标效果"参考

## 跨需求聚合(本需求特有的)

| 维度 | 涉及需求 | 共性 | 处理建议 |
| --- | --- | --- | --- |
| 元需求(优化 code-plan 拆分规则) | 无 | N/A | 独立处理 |
| 未来需求应用新规则 | REQ-00008+ | 未来所有需求 | 强制应用新规则 |
| 现有 PLANS 不追溯 | REQ-00004/05/06/07 | 已拆分完成 | 不追溯 |
| `code-plan` SKILL.md 修改范围 | 元需求 | 仅 §10A | 不改其他 18 章节 |

## 总结

- **0 强关联** = 本需求对所有其他需求的影响均为 0
- **元需求特性** = 优化工作流本身,**不**产生新功能/新接口/新数据
- **作用范围** = `plugins/code-skills/skills/code-plan/SKILL.md` §10A 单章节修改
- **生效范围** = V0.0.2 未来需求(REQ-00008+)
- **追溯范围** = 0(Q-A3 锁定 A)
