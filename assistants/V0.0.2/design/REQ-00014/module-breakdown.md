# 模块拆分 — REQ-00014

更新时间:2026-06-05 12:40
版本:V0.0.2
需求:REQ-00014(优化技能 `/code-plan` 的任务拆分维度)

## 关键说明

本需求是**元需求** — 优化 `code-plan` 技能的工作流定义本身。**不涉及**新增/复用/修改任何项目代码模块,只修改 1 个 `code-plan` 技能 SKILL.md 的 1 个章节(§10A)。

## 模块清单

| # | 模块 | 路径 | 状态 | 职责 | 对外接口 | 依赖 | 关键决策 | 符合规范 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| M-1 | `code-plan` 任务拆分原则子节 | `plugins/code-skills/skills/code-plan/SKILL.md` §10A L195-239 | **修改**(L195-199 4 行) | 定义任务拆分的"核心原则"(按功能点)+ "架构任务触发条件" | 文字说明(被未来 `code-plan` 调用者阅读) | (无) | 详细设计 D-1:完全替换原 4 行 | `skill-conventions §规则 1`(不约束工作流文字) |

## 自检 — `module-conventions §规则 1`

| 条款 | 本设计满足度 |
| --- | --- |
| 资源须放 `templates/` / `checklists/` / `guidelines/` 固定子目录 | ✅ N/A(本优化**不**新增/修改任何资源文件,只改 SKILL.md 工作流文字) |
| `SKILL.md` 本身放在技能根目录 | ✅ `code-plan/SKILL.md` 已在根目录 |
| 命名风格:技能名 kebab-case | ✅ `code-plan` 已是 kebab-case |

## 与既有 18 个 `code-*` 技能的影响

| 技能 | 是否受影响 | 理由 |
| --- | --- | --- |
| `code-init` | ❌ | 不依赖任务拆分维度 |
| `code-version` | ❌ | 不依赖 |
| `code-rule` | ❌ | 不依赖 |
| `code-require` | ❌ | 产出"需求分析",**不**读任务拆分 |
| `code-design` | ❌ | 产出"概要设计",**不**读任务拆分 |
| `code-plan`(自身) | ✅ **本需求唯一修改目标** | 修改 §10A 任务拆分原则 |
| `code-it` | ❌ | 读任务编号/双状态/触发来源(沿用),**不**读拆分原则 |
| `code-unit` | ❌ | 同上 |
| `code-fix` | ❌ | 不依赖 |
| `code-init` | ❌ | 不依赖 |
| `code-review` | ❌ | 读任务字段(沿用),**不**读拆分原则 |
| `code-dashboard` | ❌ | 读任务清单(沿用),**不**读拆分原则 |
| `code-publish` | ❌ | 读任务清单(沿用),**不**读拆分原则 |
| `code-auto` | ❌ | 读任务清单(沿用),**不**读拆分原则 |

**结论**:**1 个**模块被修改(M-1),**0 个**新增,**0 个**删除。**

## 实施影响

- `code-plan` SKILL.md 体积变化:5 行(L195-199 删除)+ ~25 行(新增子节)= **+20 行**
- 文件总行数:374 → ~394
- 触发 `dashboard-conventions §规则 1` 三同步:**否**
- 触发其他技能 SKILL.md 联动修改:**否**
- 触发既有 7 个 PLAN 追溯重拆:**否**(Q-A3 锁定)
