# 澄清记录 — REQ-00007(概要设计阶段)

更新时间:2026-06-05 09:15
版本:V0.0.2

> 本文件记录概要设计阶段产生的、与上游 `require/REQ-00007/clarifications.md` 不重复的设计级澄清。

## 2026-06-05 09:15(本轮)

### Q-A1:code-auto/SKILL.md 的工作流描述风格

- **问题**:`code-auto` 需要串行驱动 6 个子技能(代码实现量大),SKILL.md 应如何组织主循环结构?
- **选项**:
  - A. 显式状态机 + 子技能调用表(推荐)
  - B. 抽象工作流 + 子技能名参数化
  - C. 复用现有子技能 SKILL.md 的工作流描述(违反 module-conventions §规则 1)
- **用户回答**:A — 显式状态机 + 子技能调用表
- **影响**:RESULT.md §5 架构总览(状态机图)+ §7 模块划分(SKILL.md 内部结构)+ §6 功能架构(子技能调用表)
- **解读**:与 `code-require` / `code-design` 等子技能的 SKILL.md 风格一致(Mermaid + 步骤化);便于 `code-review` 评审;预计 SKILL.md ~600 行
- **决策依据**:`skill-conventions.md §规则 1`(沿用子技能 SKILL.md 风格);`module-conventions.md §规则 1`(不引入子目录,所有内容放 SKILL.md)

### Q-A2:code-review 评审轮次的"日志记录"位置

- **问题**:`code-auto` 调 `code-review` 时,如何记录"这是第 N 轮"?
- **选项**:
  - A. 写入 `auto-report.md`(完成时)— 内存计数,完成/中断时一并写
  - B. 写入 `plan/REQ-NNNNN/PLAN.md` 任务总览(每轮追加一行)
  - C. 不记录,只输出到屏幕
- **用户回答**:C — 不记录,只输出到屏幕
- **影响**:RESULT.md §7 模块划分(`code-auto` 不维护内存计数器);§6.1 进度输出(每行带"第 N 轮"字面)
- **解读**:最简实现;与"全自动"原意一致(用户不需要看到累积状态,只需要看"现在到哪步")
- **决策依据**:NFR-4(不引入批量模式精神 — 不污染 PLAN.md);NFR-7(中止时不写盘)

## 与上游 `require/REQ-00007/clarifications.md` 的关系

- **Q-1 ~ Q-5**:已锁定(上游) — 本设计直接采纳,无新增澄清
- **Q-6 ~ Q-12**:采纳默认(上游) — 本设计直接采纳,无新增澄清
- **Q-13**:建议派生(上游) — 本设计在 RESULT.md §15 风险与缓解中显式列出
- **Q-A1 / Q-A2**:本轮新增(概要设计阶段)— 涉及 SKILL.md 内部组织风格,**不**与上游冲突

**累计**:5 锁定 + 7 默认 + 1 建议派生 + 2 设计阶段新增 = **15 项**,其中 0 项待澄清阻塞。
