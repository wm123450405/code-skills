# 偏离记录 — BUG-00001
版本:V0.0.2

## 偏离 0:无偏离

- **类别**:N/A
- **说明**:本修复严格遵循 `fix-plan.md §3 修复方案`(方案 A3 脏标记文件) + `§4 涉及文件与变更`(4 个 SKILL.md 修改) + `§7 修复步骤`(4 步),8/8 INV 100% 通过,0 偏离
- **结论**:**0 偏离**。本修复涉及的所有变更(4 步 / 4 文件 / D-5 修订)均严格遵循 `fix-plan.md` 锁定,无新增 / 扩展 / 越界

## 备注

- **fix-plan.md §6 R-3 风险**:`code-auto` 异常崩溃时 `.code-auto-running` 未被清理 — **已缓解**:子技能检测到残留 + 距启动 > 24 小时 → 视为"脏数据",屏幕输出 `⚠ ... 按用户手动调子技能处理`
- **fix-plan.md §6 R-2 风险**:D-5 修订影响 → **已缓解**:D-5 修订**只**放宽"状态文件"类边界,其他契约保持(子技能零修改契约的"状态文件"子集是新增的;子技能 SKILL.md 仍可被 code-auto 修改 — 沿用既有 D-5 "不修改"= 改 SKILL.md 不被 prompt 注入替代)
