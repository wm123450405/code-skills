# TASK-REQ-OPT-00001-00006 — 三态确认契约同步到全部文档

> 所属需求:REQ-OPT-00001
> 任务类型:修改
> 涉及文件:`CLAUDE.md`(根)
> 触发/来源:需求初始

## 1. 任务概述

P1-3.5(默认确认行为在文档之间相互矛盾)的修复:把根 `CLAUDE.md` 的"默认交互确认每阶段"改为契约层 §5 的精确描述 —— 默认阶段自动继续 + 阶段内补充内容仍等待;同时把"复用 /code req 行为"的提示指向契约层 §5。

## 2. 改动内容

- 第 20 行(/code req 描述):"默认交互确认每阶段" → 改为"阶段边界自动继续(默认)/ 每阶段确认(`--confirm`)/ 静默全跑通(`--auto`);阶段内补充内容在默认和 `--confirm` 下等待用户确认,`--auto` 下用推荐项;详见 `skills/code/references/_shared/contracts.md` §5"
- 第 22 行(/code fix 描述):同步追加"三态行为同 `/code req`"
- 第 98-99 行(子命令清单段):同样更新

## 3. 验证

```
grep -nE "默认.*每阶段|每阶段.*询问" CLAUDE.md
→ 无残留旧表述
```

✅ FR-5 落盘。三态契约在 README / CLAUDE.md / req/SKILL.md / fix/SKILL.md / req/common.md / help/SKILL.md 已统一或指向契约层 §5。

## 4. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-07-22 11:30 | v1 | FR-5 三态契约同步根 CLAUDE.md(关键边界:默认 ≠ 全部跳过) | wangmiao |
