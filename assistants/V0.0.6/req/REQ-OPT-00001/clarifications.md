# 需求澄清记录 — REQ-OPT-00001

> 追加式记录,每次用户确认后追加一行。

| 时间 | 问题 | 回答 | 来源 |
| --- | --- | --- | --- |
| 2026-07-22 10:36 | REQ-OPT-00001 的需求范围怎么切? | P0+P1 全部 14 项(P0 4 项 + P1 10 项);P2 / P3 不在本 REQ 范围 | 用户确认 |
| 2026-07-22 10:36 | opt.md 中识别出的 BUG 类问题(P0 第 3.2-3.4 项)是否要拆为单独的 BUG 走 /code fix 流程? | 本 REQ 内一起处理,不另开 BUG-OPT 编号;关联需求章节标注 | 用户确认 |
| 2026-07-22 10:36 | 阶段执行模式用哪种? | --confirm(每阶段完成后强制弹出增强确认) | 用户确认 |
| 2026-07-22 10:39 | 14 项 P0+P1 问题中,FR-12(/code merge 目标分支操作)opt.md 标记为"[需验证]",如何在 REQUIRE 阶段处理? | 标注为"需在 PLAN 阶段单独立项做冒烟验证",不影响 REQUIRE 阶段闭合 | 假设(待确认) |
| 2026-07-22 10:39 | FR-9(/code ver 写权限)opt.md §3.9 推荐方案 2(保留自动同步 + 加确认/回滚),FR-9 当前选择方案 1(改成 --sync-project-version),是否与 opt.md 推荐分歧? | 方案 1 是 opt.md §3.9 的"推荐"方案;本 REQ 与 opt.md 推荐一致 | 已确认(opt.md §3.9 显式推荐) |
| 2026-07-22 10:39 | FR-5(默认模式 = 自动继续)与 opt.md §8.2 备选方案(默认改为阶段确认)的关系? | 用户本 REQ 选 --confirm 模式,默认行为不直接影响当前会话;FR-5 按 opt.md 主推荐写入"默认 = 自动继续";若未来需要改默认,在新 REQ 中调整 | 假设(待确认) |
| 2026-07-22 10:58 | FR-9(/code ver 默认行为)最终方案? | 方案 B(保留自动同步 + 加差异预览/确认/回滚/提交四步);SKILL.md "只读"声明同步改为"默认同步 + 加四步" | 用户确认 |

## 延迟到 DESIGN 阶段(技术选型类)

> 本节按 require.md §5a-1 的过滤规则,登记被技术选型关键词命中的候选选项,延迟到 DESIGN 阶段。

| 时间 | 原始问题 | 命中的关键词 | 延迟原因 |
| --- | --- | --- | --- |
| (无) | — | — | 本轮无技术选型类候选需要过滤;FR 描述中提到的"模板"指 Markdown 模板(已存在技能内嵌模板),非技术选型 |
