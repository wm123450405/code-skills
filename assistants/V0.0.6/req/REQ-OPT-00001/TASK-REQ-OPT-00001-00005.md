# TASK-REQ-OPT-00001-00005 — 统一基线路径

> 所属需求:REQ-OPT-00001
> 任务类型:修改
> 涉及文件:`plugins/code-skills/skills/code/references/ver/common.md`
> 触发/来源:需求初始

## 1. 任务概述

P0-3.4(初始化基线路径与主流程路径不一致)的修复:基线需求从 `require/EXISTING-NNN/` 改为 `req/EXISTING-NNN/`;同时按 NFR-7(V0.0.7 之内有效)增加旧 `require/` 兼容读取;并清理 `backfillDashboard` 中的动态状态列(FR-1 协同)。

## 2. 改动内容

- 第 444-445 行:`require/EXISTING-{feature.id}/` → `req/EXISTING-{feature.id}/`(mkdir + Write)
- 第 444 行后增加兼容读取逻辑:`if exists("assistants/{version}/require/EXISTING-{feature.id}/")` → 自动 mv 到新位置
- 第 451-466 行 `backfillDashboard`:删除"状态/创建时间/完成时间"动态列写入(schema-v2 禁止动态列);需求文档链接从 `require/` 改为 `req/`;保留变更记录追加

## 3. 验证

```
grep -nE "require/EXISTING|状态.*已完成|创建时间.*now" references/ver/common.md
→ 无残留旧路径 / 无残留动态状态列
```

✅ FR-4 落盘。`require/` 兼容读取限 V0.0.7 之内有效(V0.0.8 起移除,符合 NFR-7)。

## 4. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-07-22 11:30 | v1 | FR-4 基线路径 + 兼容读取 + 动态状态列清理 | wangmiao |
