# TASK-REQ-OPT-00001-00013 — /code merge worktree 操作修复

> 所属需求:REQ-OPT-00001
> 任务类型:修改
> 涉及文件:`plugins/code-skills/skills/code/references/merge/SKILL.md`
> 触发/来源:需求初始

## 1. 任务概述

P1-3.12(/code merge 的目标分支操作存在高概率 worktree 冲突)的修复:不在当前 worktree checkout 目标分支;改用 `git worktree list --porcelain` 找到主工作区,在主工作区执行 `git -C <main-worktree> merge <feature-branch> --no-ff`;新增 dirty 检查;unresolved 冲突返回非 0。

## 2. 改动内容

- `merge/SKILL.md` 步骤 FR-7 重写:
  - 删除原"git checkout target → git merge"两行
  - 新增 findMainWorktree(target) 辅助函数(解析 git worktree list --porcelain 找主工作区)
  - 新增"target 主工作区 dirty → 拒绝"前置检查
  - merge 命令改为 `git -C <main-worktree> merge <worktree-branch> --no-ff`
  - 失败分支:unmerged 文件检测(`git diff --name-only --diff-filter=U`),列出冲突文件 + 返回非 0 + 不报告成功
  - 标注 `CODE_MERGE_TARGET` 拆分语义(分支名 / 远端 ref / 本地 checkout 目标)
- 契约层引用 `references/_shared/contracts.md §8`

## 3. 验证

```
grep -nE "git checkout \$\{target\}|git merge \$\{worktree_branch\}" references/merge/SKILL.md
→ 无残留旧代码;FR-7 重写为 git -C <main-worktree> merge 模式
```

✅ FR-12 落盘。
⚠️ **需用户真实 git worktree 环境冒烟验证**(sandbox 无法跑真实 worktree 流程),见 opt.md §7.6 场景矩阵。

## 4. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-07-22 11:55 | v1 | FR-12 merge worktree 主工作区操作 + dirty 检查 + unresolved 非 0 | wangmiao |
