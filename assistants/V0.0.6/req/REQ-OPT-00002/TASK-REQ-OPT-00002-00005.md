# TASK-REQ-OPT-00002-00005 — faq SKILL.md 整治

> 所属需求:REQ-OPT-00002
> 任务类型:修改
> 涉及文件:`plugins/code-skills/skills/code/references/faq/SKILL.md`
> 触发/来源:需求初始

## 1. 任务概述

faq/SKILL.md 8 条"不要做"事项(Q-1~Q-8)全部类 2(可正向改写),无类 1 / 类 3。

## 2. 处置详情

| ID | 原文 | 改写后(必须做) |
| --- | --- | --- |
| Q-1 | 不要在导出模式下修改 ./assistants/ | 导出模式下必须只写用户指定输出路径 |
| Q-2 | 不要在查询模式下修改任何文件 | 查询模式下必须只读 |
| Q-3 | 不要在无 --require/--design 时写出文件 | 无 --require/--design 时必须仅屏显 |
| Q-4 | 不要在 --summary 与 --require 同时指定时生效 | --summary 必须仅对 --design 生效;同时指定时必须报错 |
| Q-5 | 不要臆造源文档中不存在的内容 | 导出 FAQ 内容时必须只复制源文档原文 |
| Q-6 | 不要修改 ./assistants/rules/ | 对 ./assistants/rules/ 必须只读 |
| Q-7 | 不要在 --template 模板不存在时中断 | 模板不存在时必须原样输出 |
| Q-8 | 不要修改 <本仓库> 中除用户指定输出路径以外 | 修改仓库文件时必须只改用户指定输出路径 |

## 3. 验证结果

```
不要做条数:0(原 8)
必须做清单:8 条
```

## 4. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-07-22 14:12 | v1 | FR-1~FR-5 faq SKILL.md 整治;8 → 8 必须做 | wangmiao |
