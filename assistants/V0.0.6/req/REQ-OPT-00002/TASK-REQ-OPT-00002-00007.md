# TASK-REQ-OPT-00002-00007 — merge SKILL.md 整治

> 所属需求:REQ-OPT-00002
> 任务类型:修改
> 涉及文件:`plugins/code-skills/skills/code/references/merge/SKILL.md`
> 触发/来源:需求初始

## 1. 任务概述

merge/SKILL.md 17 条"不要做"事项(G-1~G-17)处置。

## 2. 处置详情

### 类 1(已在流程中,合并入正向)

- G-1(无 current-version)→ 必须做"执行前必须读取"
- G-2(主工作区调用)→ 必须做"当前必须在 worktree 内"
- G-9(不修改既有 SKILL.md)→ 合并入"配置文件"
- G-10/G-11(不修改 marketplace/plugin.json)→ 合并入"配置文件"
- G-12(不修改 rules 已有规范)→ 合并入"配置文件"
- G-14(不接受 --no-worktree)→ 合并入"自动行为边界"
- G-16(不嵌入 git 命令模板)→ 合并入"配置文件"

### 类 2(转必须做)

- G-3(不自动 git push) → "git push 必须由用户手动执行"
- G-4(不回滚已 commit) → "冲突未解决时必须返回非 0,不 git reset"
- G-5(不写过程文件) → "本技能不写任何过程文件 / 结果文件"
- G-6(不调子技能) → "本技能不调任何其他子技能(INV-9 严守)"
- G-7(不自动 --publish/--auto) → 合并入 G-6
- G-13(不用 --squash) → "不接受 --squash 合并,必须用 --no-ff"
- G-15(异常时不 flush) → 合并入 G-5

### 类 3(保留"自动行为边界"作为契约型负向声明)

G-3/G-4/G-5/G-6/G-13/G-14 本质是"绝不做某事"的负向契约 —— 已在必须做清单中以"X 必须由用户手动做 / 本技能不 Y"的形式表述,语义等价但句式更清晰。

## 3. 验证结果

```
不要做条数:0(原 17)
必须做清单:14 条,分 3 子清单(工作区与流程 / 自动行为边界 / 配置文件)
```

## 4. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-07-22 14:16 | v1 | FR-1~FR-5 merge SKILL.md 整治;17 → 14 必须做,分 3 子清单 | wangmiao |
