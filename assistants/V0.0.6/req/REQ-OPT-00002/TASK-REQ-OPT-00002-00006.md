# TASK-REQ-OPT-00002-00006 — rule SKILL.md 整治

> 所属需求:REQ-OPT-00002
> 任务类型:修改
> 涉及文件:`plugins/code-skills/skills/code/references/rule/SKILL.md`
> 触发/来源:需求初始

## 1. 任务概述

rule/SKILL.md 9 条"不要做"事项(U-1~U-9)。U-3 类 1(§0 I-5 已强制),其余类 2(可正向改写)。

## 2. 处置详情

| ID | 原文 | 改写后 |
| --- | --- | --- |
| U-1 | 不要读 .current-version | .current-version 必须仅由 /code ver 读写 |
| U-2 | 不要操作 <版本号>/ | 对 ./assistants/<版本号>/ 必须只读 |
| U-3 | 不要重写既有规范 | §0 I-5 已强制;合并入"修改既有规范文件必须 Edit 末尾追加" |
| U-4 | 不要在分类未确认时写入 | 写入文件前必须完成分类确认(AskUserQuestion) |
| U-5 | 不要混用"必须"/"推荐"头部 | 同一规则文件内必须统一强制级别 |
| U-6 | 不要未澄清强制级别就写入 | 写入规则文件前必须先澄清强制级别 |
| U-7 | 不要没 Read 就 Write | 修改既有文件前必须先 Read 看现状 |
| U-8 | 不要音视频图片作来源 | 规则描述的输入必须可 Read 的文本 |
| U-9 | 不要替用户决定分类 | 新增分类时必须用 AskUserQuestion |

## 3. 验证结果

```
不要做条数:0(原 9)
必须做清单:8 条
```

## 4. 变更记录

| 时间 | 版本 | 变更摘要 | 变更人 |
| --- | --- | --- | --- |
| 2026-07-22 14:14 | v1 | FR-1~FR-5 rule SKILL.md 整治;9 → 8 必须做 | wangmiao |
