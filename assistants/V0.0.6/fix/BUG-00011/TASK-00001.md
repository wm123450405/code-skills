# TASK-00001 — 清理主 SKILL.md 末尾空板块

- 任务编号:TASK-BUG-00011-00001
- 所属需求/缺陷:BUG-00011
- 所属版本:V0.0.6
- 类型:删除
- 状态:已完成
- 创建:2026-07-24 10:20
- 完成:2026-07-24 10:20

## 1. 任务概述

删除 `plugins/code-skills/skills/code/SKILL.md` 末尾的 `## 不要做的事` 空板块(含标题 + 占位行)。该板块不含任何实际约束,仅 1 行占位文字。

## 2. 改动内容

### 文件变更

| 变更类型 | 文件路径 | 说明 |
| --- | --- | --- |
| 删除 | `plugins/code-skills/skills/code/SKILL.md` | 末尾 `## 不要做的事` 板块(标题 + 空行 + 占位行) |

### 详细改动

清理前末尾结构:
```
## 必须做事项清单
...
---

## 不要做的事

- (本节保留位置;具体约束见上文"必须做事项清单")
```

清理后末尾结构:
```
## 必须做事项清单
...
```

## 3. 关键决策

- 删除 `---` 之后的整个空板块(标题 + 空行 + 占位行 + 上一分隔符后的空行)
- 保留 `## 必须做事项清单` 板块原貌,不修改其内容

## 4. 验证结果

| 验证类型 | 命令/方式 | 结果 |
| --- | --- | --- |
| 运行时 | 不适用 | ⛔ unavailable |
| 编译 | 不适用 | ⛔ unavailable |
| 运行 | 不适用 | ⛔ unavailable |
| 测试 | 不适用 | ⛔ unavailable |
| 文本验证 | `grep -n '不要做的事' SKILL.md` | ✅ 0 命中 |
| 文本验证 | `grep -n '本节保留位置' SKILL.md` | ✅ 0 命中 |

## 5. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-07-24 10:20 | v1 | 初始完成 | 任务完成 | wangmiao |