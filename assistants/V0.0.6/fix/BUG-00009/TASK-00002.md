# TASK-BUG-00009-00002: 同步 plugins cache SKILL.md ver 步骤 6B

## 任务摘要

在 `plugins cache .../SKILL.md` 的"步骤 6B — 验证与汇报"小节末尾追加"执行兜底提交(强制,不可跳过)"小节,与 req/fix 的 DONE 阶段对齐。

## 修改文件

- `C:/Users/wm/.claude/plugins/cache/code-skills-marketplace/code-skills/0.0.2/skills/code/SKILL.md`

## 修改前

```
#### 步骤 6B — 验证与汇报

> 详见 references/ver/common.md §3.4

---
```

§3.4 是错误的引用(§3.4 实际是 createNewVersion,§3.5 才是验证与汇报)。

## 修改后

```
#### 步骤 6B — 验证与汇报

> 详见 references/ver/common.md §3.5

**执行兜底提交**(强制,不可跳过,与 `req`/`fix` 的 DONE 阶段对齐):
- 执行 `Bash: git rev-parse --git-dir 2>/dev/null` → 退出码 ≠ 0 则输出"非 git 仓库,跳过提交"
- 执行 `Bash: git status --porcelain` → 输出为空则输出"无文件变更,跳过提交"
- 执行 `Bash: git add -A`
- 生成 commit message(格式:`chore(ver): 版本切换至 <版本号>`)
- `AskUserQuestion` 确认后执行 commit
- 用户选"跳过"则不提交,工作区保持 dirty,屏幕说明

---
```

## 验证

- 章节引用修正:§3.4 → §3.5
- 6 步 git 操作描述清晰
- 强约束表述与 req/fix DONE 阶段一致

## 副作用

修正了原有错误引用 §3.4(实际 §3.4 是 createNewVersion,§3.5 才是 verifyAndReport),这是顺手修复的小问题。
