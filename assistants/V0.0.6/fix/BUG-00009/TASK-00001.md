# TASK-BUG-00009-00001: 修改 references/ver/common.md §3.5 加入 executeFallbackCommit

## 任务摘要

在 `references/ver/common.md` 的 §3.5 验证与汇报函数末尾,新增 executeFallbackCommit 调用,并增加"兜底提交逻辑"小节描述其实现细节。

## 修改文件

- `C:/Users/wm/.claude/plugins/cache/code-skills-marketplace/code-skills/0.0.2/skills/code/references/ver/common.md`

## 修改前

§3.5 verifyAndReport 函数:
```
function verifyAndReport(version):
  // 验证
  currentContent = Read("assistants/.current-version")
  ...
  print("""
  ...
  """)
```
函数以 print 结束,无任何 git 操作。

## 修改后

§3.5 verifyAndReport 函数末尾新增:
```
  // 兜底提交(强制,不可跳过)
  executeFallbackCommit(scope="ver", version=version)
```

并在 §3.5 下新增"兜底提交逻辑"小节,包含 executeFallbackCommit 完整伪代码与 3 条降级路径说明。

## 验证

- 文件已写入,行数从 1299 → 1379(+80 行)
- 语法检查通过(无 Read 报错)
- executeFallbackCommit 函数体包含 6 步:git-dir 检查、status 检查、变更计数、add -A、message 生成、AskUserQuestion + commit

## 备注

- plugins cache 路径修改即时生效(下次 `ver` 调用即用新逻辑)
- CWD 根的 `plugins/code-skills/...` 是否需要同步?——本次只改 cache,因为发布流程以 cache 为运行版本
