# TASK-BUG-00010-00004: 验证修改一致性

## 验证内容

执行 `grep -n "两态行为\|handleOptionalFindings\|executeOptionalFix"` 在两个文件中:

```
references/req/check.md:
  L166: ### 步骤 5 — 处理建议改与可选改(两态行为)
  L179: #### handleOptionalFindings 函数
  L182: function handleOptionalFindings(findings, mode):
  L191:     executeOptionalFix(finding)
  L212:     executeOptionalFix(finding)
  L221: #### executeOptionalFix 函数(新增)
  L226: function executeOptionalFix(finding):
  L254: | 处理函数 | 评审-编码循环(步骤 4) | handleOptionalFindings(步骤 5) |

plugins cache .../SKILL.md:
  L1000: - **建议改/可选改处理(两态行为)**:
  L1314: - **建议改/可选改(两态行为)**:
```

## 一致性检查

| 检查项 | 结果 |
| --- | --- |
| req 子命令 SKILL.md 含"两态行为"描述 | ✓ L1000 |
| fix 子命令 SKILL.md 含"两态行为"描述 | ✓ L1314 |
| check.md 含 handleOptionalFindings 函数 | ✓ |
| check.md 含 executeOptionalFix 函数 | ✓ |
| 两份 SKILL.md 描述一致(用语/语义) | ✓ |
| check.md 三档 source 字段与 SKILL.md 一致 | ✓ |

## AC 验证

| AC | 状态 | 说明 |
| --- | --- | --- |
| AC-1 默认 + 必须改 → 自动 | ✓ | check.md §评审-编码循环 |
| AC-2 默认 + 建议改 → 逐项 AskUserQuestion | ✓ | check.md handleOptionalFindings |
| AC-3 默认 + 可选改 → 同建议改 | ✓ | handleOptionalFindings 同时处理 |
| AC-4 --auto + 必须改 → 自动 | ✓ | 既有评审-编码循环 |
| AC-5 --auto + 建议改 → 全部自动应用 | ✓ | handleOptionalFindings 模式判断 |
| AC-6 --auto + 可选改 → 全部自动应用 | ✓ | 同上 |
| AC-7 source 字段三档区分 | ✓ | 审查改修/建议改改修/可选改修 |
| AC-8 CHECK.md 显示自动应用数与用户跳过数 | 待 CHECK 阶段验证 | 需后续扩展 templates/CHECK.md |

## 结论

✓ 验证通过(7/8 AC),AC-8 留待后续 CHECK 模板扩展。
