# TASK-BUG-00010-00001: 修改 references/req/check.md 步骤 5 + 新增 executeOptionalFix

## 任务摘要

将原"步骤 5 — 处理建议改"小节升级为"步骤 5 — 处理建议改与可选改(两态行为)",新增 handleOptionalFindings 函数和 executeOptionalFix 函数,并补充与必须改评审-编码循环的差异对比表。同步更新"阶段完成确认"中的"建议改确认"为"建议改/可选改确认"。

## 修改文件

- `plugins cache .../references/req/check.md`

## 修改前

§步骤 5 仅描述"处理建议改"——逐项 AskUserQuestion(A/B/C 选项),无两态行为,无 handleOptionalFindings,无 executeOptionalFix,无可选改处理路径。

## 修改后

新增内容:
1. **三档处理矩阵**:默认/`--auto`/`--confirm` 三模式对必须改/建议改/可选改的处理路径
2. **handleOptionalFindings(findings, mode) 函数**:`--auto` 全自动;默认逐项 AskUserQuestion
3. **executeOptionalFix(finding) 函数**:单次 CODING 执行,不进入新一轮 CHECK 循环
4. **与必须改对比表**:处理函数 / source 字段 / 是否重新审查 / 循环上限 / 模式差异

## 验证

- check.md 行数从 268 → 314(+46)
- 函数伪代码格式与既有 handleOptionalFindings / executeCodingTask 风格一致
- source 字段命名:`审查改修` / `建议改改修` / `可选改修`,三档严格区分

## 备注

- executeOptionalFix 复用 executeCodingTask,新增源字段 source 区分三档
- 单次执行不进入循环:避免建议改数量大时无限递归
