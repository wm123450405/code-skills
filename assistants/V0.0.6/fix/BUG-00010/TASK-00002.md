# TASK-BUG-00010-00002: 修改 plugins cache SKILL.md req 子命令步骤 5

## 任务摘要

在 SKILL.md req 子命令的"步骤 5 — CHECK 阶段(代码审查)"小节中,补充"建议改/可选改处理(两态行为)"的明确描述。

## 修改文件

- `plugins cache .../SKILL.md` 第 992-1001 行

## 修改前

```
- 评审-编码循环:存在"必须改"→ 生成改修任务 → CODING 修复 → 重新 CHECK → 循环直到无"必须改"(最多 5 轮)
- 对"建议改"询问用户(非 `--auto`)
```

## 修改后

```
- 评审-编码循环:存在"必须改"→ 生成改修任务(source="审查改修")→ CODING 修复 → 重新 CHECK → 循环直到无"必须改"(最多 5 轮)
- **建议改/可选改处理(两态行为)**:
  - 默认/`--confirm` 模式:逐项 AskUserQuestion(A 修复 / B 跳过);选 A 走 CODING 修复(source="建议改改修"/"可选改修"),选 B 保留
  - `--auto` 模式:全部自动应用所有项,不再弹窗
```

## 验证

- 评审-编码循环的 source 字段补充为"审查改修",与 check.md 一致
- 建议改/可选改的两态行为与 check.md 步骤 5 一致
- source 字段三档(审查改修/建议改改修/可选改修)首次在 SKILL.md 显式出现
