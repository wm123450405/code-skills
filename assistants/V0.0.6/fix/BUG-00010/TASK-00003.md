# TASK-BUG-00010-00003: 修改 plugins cache SKILL.md fix 子命令步骤 5

## 任务摘要

在 SKILL.md fix 子命令的"步骤 5 — CHECK 阶段(代码审查)"小节中,补充"必须改"+"建议改/可选改(两态行为)"的明确描述。

## 修改文件

- `plugins cache .../SKILL.md` 第 1302-1312 行

## 修改前

```
- 对"必须改"自动修复,对"建议改"询问用户(非 `--auto`)
```

## 修改后

```
- **必须改**:评审-编码循环,自动修复直到无必须改(source="审查改修",最多 5 轮)
- **建议改/可选改(两态行为)**:
  - 默认/`--confirm` 模式:逐项 AskUserQuestion(A 修复 / B 跳过);选 A 走 CODING 修复(source="建议改改修"/"可选改修"),选 B 保留
  - `--auto` 模式:全部自动应用所有项,不再弹窗
```

## 验证

- 必须改行为从原"自动修复"升级为"评审-编码循环(source='审查改修',最多 5 轮)",与 check.md 一致
- 建议改/可选改两态行为首次在 fix 子命令 SKILL.md 显式出现
- 与 req 子命令 SKILL.md 步骤 5 用语一致
