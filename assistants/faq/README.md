# FAQ 目录

> 本目录由 `/code ver --publish` 在首次发布时创建,聚合跨版本 FAQ。

## 文件

- `FAQ.md` — 当前激活版本的 FAQ(由发布检查生成)
- 后续版本发布时,历史 FAQ 可在此处归档

## 维护规则

- `FAQ.md` 由 `ver --publish` 自动生成,无需手动编辑
- 如需补充 FAQ,使用 `/code rule` 在 `rules/` 中沉淀规范,或在 `req/<REQ>/` 文档中记录
