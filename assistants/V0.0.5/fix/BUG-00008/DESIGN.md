# 修复设计 — BUG-00008 · 阶段 references 未适配三态确认模式

## 1. 设计概述

将 6 个阶段 reference 文档中的"非 --auto 模式确认"章节统一替换为三态确认模型，与 common.md §11 和 SKILL.md 参数解析保持一致。

## 2. 涉及文件

| 文件 | 章节 | 修改内容 |
| --- | --- | --- |
| `require.md` | §非 --auto 模式确认 + §确认环节约束 | 替换为三态确认 |
| `design.md` | §非 --auto 模式确认 + §确认环节约束 | 替换为三态确认 |
| `plan.md` | §非 --auto 模式确认 + §确认环节约束 | 替换为三态确认 |
| `coding.md` | §非 --auto 模式确认 | 替换为三态确认 |
| `check.md` | §非 --auto 模式确认 + §确认环节约束 | 替换为三态确认 |
| `fix-register.md` | §非 --auto 模式确认 | 替换为三态确认 |

## 3. 统一模板

每个文件的"阶段完成确认"章节统一替换为三态模型描述，核心内容:
- --confirm: 增强确认(路径+重读+继续/中止)
- --auto: 自动继续(前缀输出)
- 默认: 自动继续(无输出)，阶段内内容确认正常触发

## 4. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 |
| --- | --- | --- | --- |
| 2026-06-30 | v1 | 初始创建 | 修复设计完成 |