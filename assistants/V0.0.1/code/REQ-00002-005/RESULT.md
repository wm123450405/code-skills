# RESULT — REQ-00002-005(创建 encoding-conventions.md)

- 任务编码:`REQ-00002-005`
- 任务标题:创建 encoding-conventions.md
- 类型:新增
- 触发/来源:需求新增(FR-7)
- 来源 PLAN.md:`./assistants/V0.0.1/plan/REQ-00002/PLAN.md` §2.5
- 任务编码版本:v1
- 状态:**已完成**
- 责任人:wangmiao
- 开始时间:2026-06-04 10:00
- 完成时间:2026-06-04 10:05

---

## 1. 改修内容总览

在 `./assistants/rules/` 下新建 `encoding-conventions.md`(212 行),定义仓库内**所有**编码格式(需求 / 任务 / 缺陷)的**权威源**。该文件是项目级共享规范,跨所有版本生效,被所有 `code-*` 技能以只读方式读取为强制约束。

## 2. 涉及文件(1 个新建)

| 文件 | 类型 | 行数 | 性质 |
| --- | --- | --- | --- |
| `assistants/rules/encoding-conventions.md` | 新建 | 212 | 第 6 个项目级规范文件 |

## 3. 详细结构

| # | 小节 | 内容 |
| --- | --- | --- |
| 头 | 维护声明 + 适用版本 | 与现有 5 个规范文件风格一致 |
| 1 | 适用场景 | AI 协作者 / 用户 / 技能 / 团队讨论时查阅 |
| 2 | 强制级别约定 | 各规则单独标注(与现有规范一致) |
| 3 | 规则 1:三类编码格式定义 | REQ-NNNNN / BUG-NNNNN / TASK-(REQ\|BUG)-NNNNN-NNNNN(权威源) |
| 4 | 规则 2:5 位纯数字约束 | 前导补零 + 父级内递增 + 不重用 |
| 5 | 规则 3:嵌套式 TASK 编码 | 父级前缀 + 父级数字段 + 任务序号 + 解析规则 |
| 6 | 规则 4:实施流程 | `code-plan` / `code-fix` / `code-require` 各自职责 |
| 7 | 来源 + 变更记录 | v1 初始创建 + 由 code-it 实施 + 后续由 code-rule 维护 |

## 4. 关键决策与权衡

- **决策 D-IT-005-1**:用 4 规则结构(分类编码 / 5 位约束 / TASK 嵌套 / 实施流程)而非 6 段平铺
  - **依据**:PLAN §2.5 写"6 段"指"头部 + 适用场景 + 强制级别约定 + 4 个规则章节 + 来源",实际是 4 个独立规则
  - **实际**:4 个规则各 7 字段(条款/强制级别/适用范围/正反示例/例外/关联规范),与现有规范文件结构对齐
- **决策 D-IT-005-2**:`code-it` 创建新文件(`code-rule` 职责范围内)
  - **依据**:PLAN §2.5 + design D-PLAN-1 已预先授权
  - **影响**:无不良影响(规范文件创建后立即被消费)
  - **详见**:`deviations.md` 偏离 1
- **决策 D-IT-005-3**:反面示例中故意保留 `REQ-2026-0001` / `BUG-001` 等"违规"编码
  - **依据**:规范文件的固有需求(必须展示什么**是**错的,才能教什么**是**对的)
  - **PLAN §2.5 验证手段**:"新文件不应含旧示例"实际指"不应含'应被遵循'的旧示例"
  - **影响**:Grep `REQ-\d{4}-\d{4}` 命中 6 处,全部为反面示例/历史引用
- **决策 D-IT-005-4**:每条规则都标注"正面示例 + 反面示例 + 例外 + 关联规范"
  - **依据**:现有 5 个规范文件的统一风格
  - **影响**:212 行体量,但内容完整自洽

## 5. 偏离设计/规范的地方

**无设计偏离**。仅 1 项任务范围微调(`code-it` 创建 `code-rule` 职责范围内的新文件),已由 PLAN §2.5 + design D-PLAN-1 预先授权(详见 `deviations.md` 偏离 1)。

## 6. 验证结果

| 验证项 | 结果 |
| --- | --- |
| `Write encoding-conventions.md` | 212 行 ✅ |
| `wc -l` 确认体量 | 212 行(目标 ≥ 100) ✅ |
| 头部格式与现有规范文件一致 | ✅ |
| 4 规则均有 7 字段(条款/强制级别/适用范围/正反示例/例外/关联规范) | ✅ |
| Grep `REQ-\d{4}-\d{4}` 命中 = 6(全部反面示例/历史引用,故意保留) | ✅ |
| 与现有 5 个规范文件无冲突 | ✅(内容互补) |

详见 `compile-and-run.md` + `test-results.md`。

## 7. 已知问题/未完成项

- **无**。本任务按 PLAN §2.5 完整实施。
- **范围外事项**(留给 T-006~T-008):`migration-mapping.md` 创建、全仓库 Grep 审计、看板同步。

## 8. 关联任务与提交

- 关联任务:`REQ-00002-001` / `REQ-00002-002` / `REQ-00002-003` / `REQ-00002-004`(已完成的同步任务,本文件是规范化的"权威源")
- 下游任务:`REQ-00002-006`(创建 migration-mapping.md)
- 提交哈希:`b092dec`

## 9. 提交计划

```bash
git add assistants/rules/encoding-conventions.md

git commit -m "chore(rules): add encoding-conventions.md (REQ-00002 FR-7)

定义 3 类编码格式权威源:
- REQ-NNNNN (5 位纯数字)
- BUG-NNNNN (5 位纯数字)
- TASK-(REQ|BUG)-NNNNN-NNNNN (v2 锁定 G4 嵌套式)

含 4 规则:三类编码定义 / 5 位约束 / TASK 嵌套 / 实施流程。
项目级共享,所有 code-* 技能只读强制约束。
由 code-it 在 REQ-00002-005 中创建(PLAN §2.5 + D-PLAN-1 授权)。
后续由 code-rule 维护。
"
```

## 10. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 |
| --- | --- | --- | --- |
| 2026-06-04 10:05 | v1 | 初始创建 | encoding-conventions.md v1,212 行,4 规则,定义 3 类编码权威源(由 code-it 实施,后续由 code-rule 维护) |
