# 偏离记录 — REQ-00003-002
版本:V0.0.1

## 偏离 1:`code-it` 创建新文件(`code-rule` 职责范围内)
- **类别**:任务范围扩展(用户授权的偏离)
- **依据**:
  - 设计要求:PLAN §T-002 "在 ./assistants/rules/ 下创建 6 个新分类文件的空占位骨架"
  - 设计依据:`plan/REQ-00003/RESULT.md` §4 决策 D-PLAN-1 "`code-it` 创建新规范文件,授权"
- **实际做法**:
  - `code-it` 在 `./assistants/rules/` 下创建 6 个新文件
  - 这通常是 `code-rule` 技能的职责范围
- **偏离理由**:
  - REQ-00003 实施时,新分类占位文件的内容由本需求决定
  - 后续 `code-rule` 可基于"占位"补充具体规则
- **影响**:**无不良影响**(占位文件创建后立即被消费为分类依据)
- **授权**:PLAN §T-002 + design D-PLAN-1 已预先授权
- **时间**:2026-06-04 10:53

## 偏离 2:`directory-conventions.md` 头部增加"替代说明"
- **类别**:诚实暴露(无偏离)
- **依据**:
  - 设计要求:PLAN §T-002 "仅含骨架,无预填规则"
- **实际做法**:
  - 在 `directory-conventions.md` 的"## 适用场景"小节追加一句:
    > **迁移说明**:本文件替代旧 `module-conventions.md`(已迁移)。新规则请直接追加到本文件。
  - 1 行非规则内容
- **偏离理由**:
  - 帮助用户理解 C-4 与旧 `module-conventions.md` 的关系
  - 避免未来 `code-rule` 添加"目录与模块"规则时不知道往哪个文件追加
- **影响**:**正面**(可读性提升)
- **授权**:不需要授权(诚实暴露,无功能变更)
- **时间**:2026-06-04 10:53

## 其他
- 本任务**未做设计偏离**。所有 6 个新文件严格遵循 PLAN §T-002 + 详细设计 §3.3.5 骨架模板。
- 范围严格限定在 `./assistants/rules/` 下 6 个新文件,未触及任何既有文件(INV-5)。

## 范围外但被识别的事项(留给后续任务)
- `module-conventions.md` 弃用标记(由 T-003 单独处理)
- 扩展 `templates/rule.md` 支持占位/引导模式(由 T-004 单独处理)
- 追加 `CLAUDE.md` "AI 工作约定"小节(由 T-005 单独处理)
- 全仓库 Grep 审计(由 T-007 单独处理)
- 看板同步(由 T-008 单独处理)

## 时间
2026-06-04 10:53
