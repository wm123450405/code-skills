# 偏离记录 — REQ-00002-003
版本:V0.0.1

## 偏离 1:批量 `replace_all: true` 触发"前序替换消化后序"问题
- **类别**:实现细节微调(非设计偏离)
- **依据**:
  - 设计要求:PLAN §2.3 无具体实现约束,仅要求"中英同次 commit"
- **实际做法**:
  - 第一次 Edit 用 `replace_all: true` 把 `REQ-2026-0001` 全部替换为 `REQ-00001`
  - 后续 3 个 Edit(`REQ-2026-0001-001/003/005` → `TASK-REQ-00001-...`)失败,因字符串已被前序替换消化
  - 补救:对已部分替换的 `REQ-00001-001/003/005` 二次 `replace_all: true` → 成功
- **偏离理由**:
  - 工具语义:先做最长公共前缀替换,会导致后缀子串成为新的待替换值
  - 与"完整字符串"思维不符,但因 `Edit` 是顺序处理,只要补救后效果等价即可
- **影响**:**无影响**(最终 0 命中,两侧行数差 0,符合 PLAN §2.3 所有验证)
- **授权**:不需要授权(实施细节问题,非设计偏离)
- **时间**:2026-06-04 10:00

## 其他
- 本任务**未做设计偏离**。所有替换严格遵循 PLAN.md §2.3 + REQU §FR-1/FR-2 编码映射表。
- 中英对仗严格遵循 `doc-conventions §规则 1`。
- 范围严格限定在 `plugins/code-skills/{README.md, README.en.md}` 2 个文件,未触及 SKILL.md(由 T-001 完成)或 templates/(由 T-002 完成)。

## 范围外但被识别的事项(留给后续任务)
- SKILL.md(由 T-001 完成)✅
- templates/(由 T-002 完成)✅
- CLAUDE.md 核查(由 T-004 单独处理)
- encoding-conventions.md(由 T-005 单独处理)
- migration-mapping.md(由 T-006 单独处理)
- 全仓库 Grep 审计(由 T-007 单独处理)
- 看板同步(由 T-008 单独处理)

## 时间
2026-06-04 10:00
