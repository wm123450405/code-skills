# 偏离记录 — REQ-00002-006
版本:V0.0.1

## 偏离 1:`code-it` 创建新文件(`code-rule` 职责范围内)
- **类别**:任务范围扩展(用户授权的偏离)
- **依据**:
  - 设计要求:PLAN §2.6 "涉及文件:`assistants/rules/migration-mapping.md`(新建)"
  - 设计要求:PLAN §2.6 边界与异常:同 T-5
  - 设计依据:`plan/REQ-00002/RESULT.md` §4 决策 D-PLAN-1 "`code-it` 创建新规范文件,授权"
- **实际做法**:
  - `code-it` 在 `./assistants/rules/` 下创建 `migration-mapping.md`
  - 这通常是 `code-rule` 技能的职责范围
- **偏离理由**:
  - 同 T-5:`code-rule` 创建规则需要"自然语言描述",但本规则的完整定义已在 REQU/PLAN 中,直接落盘更高效
  - `migration-mapping.md` 是 `encoding-conventions.md` 的"追溯补充",两者必须同期落地
- **影响**:
  - **无不良影响**。`migration-mapping.md` 创建后,作为历史编码追溯参考
- **授权**:PLAN §2.6 + design D-PLAN-1 已预先授权
- **时间**:2026-06-04 10:08

## 偏离 2:规则 3 通用公式"与实际映射不一致"显式说明
- **类别**:诚实暴露(非偏离)
- **依据**:
  - 设计要求:PLAN §2.6 "映射表(旧→新 + 命中文件清单 + 处置)"
- **实际做法**:
  - 规则 3 给出了"通用映射公式" `new_NNNNN = (YYYY * 10000 + NNNN) mod 100000`
  - 但表格中显式标注"公式与实际不一致",并解释实际映射是"序号 NNNN 的 5 位纯数字版本"(年份段被丢弃)
- **偏离理由**:
  - 防止后续维护者误用公式去自动计算未知旧编码的新值
  - 增强透明度:把"实际映射"与"理论公式"的不一致显式记录
- **影响**:**正面影响**(避免误用)
- **授权**:本规范自我声明("本规则不强制,仅作说明")
- **时间**:2026-06-04 10:08

## 其他
- 本任务**未做设计偏离**。规范文件内容严格遵循 PLAN §2.6 4 段结构。
- 范围严格限定在 `./assistants/rules/migration-mapping.md` 1 个新文件,未触及任何既有文件。

## 范围外但被识别的事项(留给后续任务)
- 全仓库 Grep 审计(由 T-007 单独处理)— 若发现"实际"命中了理论映射中未列出的旧编码,需补入规则 1
- 看板同步(由 T-008 单独处理)

## 时间
2026-06-04 10:08
