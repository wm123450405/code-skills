# RESULT — REQ-00003-004(扩展 templates/rule.md)

- 任务编码:`REQ-00003-004`
- 任务标题:扩展 `templates/rule.md`(占位 + 引导模式)
- 类型:修改(扩展,不改 8 字段结构)
- 触发/来源:需求新增(FR-3+FR-4)
- 来源 PLAN.md:`./assistants/V0.0.1/plan/REQ-00003/PLAN.md` §T-004
- 任务编码版本:v1
- 状态:**已完成**
- 责任人:wangmiao
- 开始时间:2026-06-04 10:55
- 完成时间:2026-06-04 10:55

---

## 1. 改修内容总览

在 `plugins/code-skills/skills/code-rule/templates/rule.md` 头部(原 "## 适用场景" 之前)插入 2 个新 H2 小节:`## 占位模式` + `## 引导模式`,让 `code-rule` 在创建空占位或引导用户填示例时有模板依据。

## 2. 涉及文件(1 个修改)

| 文件 | 改动 | 统计 |
| --- | --- | --- |
| `plugins/code-skills/skills/code-rule/templates/rule.md` | 头部追加 2 个 H2 小节 | +23 行(0 删除) |

## 3. 详细结构(扩展后)

| 行号 | 小节 |
| --- | --- |
| 1 | `# <分类中文名>规范(<分类英文名>)` |
| 3-5 | 原 frontmatter 引用块(维护声明 / 最后更新 / 适用版本) |
| 7 | `## 占位模式(本文件可作为空占位使用)`(新) |
| 8-15 | 占位模式说明(7 行)+ 适用场景(2 行) |
| 19 | `## 引导模式(本文件可作为引导模板使用)`(新) |
| 20-29 | 引导模式说明(7 行)+ 适用场景(2 行) |
| 30 | "注意:占位模式与引导模式互斥" |
| 32 | `## 适用场景`(原) |
| ... | 原 8 字段结构(分类/规则简称/强制级别/适用范围/条款/正反示例/例外/关联规范/来源) |

## 4. 关键决策与权衡

- **决策 D-IT-003-4-1**:2 个 H2 小节 + 互斥说明(显式标注)
  - **依据**:SKILL.md 步骤 4 子段 4.3 占位/引导模式逻辑(由 T-001 扩展)
  - **影响**:模板自解释,`code-rule` 可直接依模板执行
- **决策 D-IT-003-4-2**:完整描述 2 个模式的"适用场景"
  - **依据**:模板需"自解释",`code-rule` 调用时不需要再回看 SKILL.md
  - **影响**:行数 +26(略超 PLAN 估算),质量更好
  - **详见**:`deviations.md` 偏离 1

## 5. 偏离设计/规范的地方

**1 项**:文档行数 +26 行 与 PLAN 估算略有差异
详见 `deviations.md`。

## 6. 验证结果

| 验证项 | 结果 |
| --- | --- |
| "## 占位模式" 小节就位 | ✅ |
| "## 引导模式" 小节就位 | ✅ |
| 2 小节内容完整,与 SKILL.md 步骤 4 逻辑一致 | ✅ |
| 原 8 字段规则小节保持 | ✅ |
| 互斥说明("二选一")显式标注 | ✅ |
| `git diff --stat` 23+/0- | ✅ |

详见 `compile-and-run.md` + `test-results.md`。

## 7. 已知问题/未完成项

- **无**。本任务按 PLAN §T-004 完整实施。
- **范围外事项**(留给 T-005~T-008):CLAUDE.md 新小节 / 审计 / 看板。

## 8. 关联任务与提交

- 关联任务:`REQ-00003-001`(已完成的 SKILL.md 步骤 4 扩展) / `REQ-00003-002`(已完成的 6 占位文件创建)
- 下游任务:`REQ-00003-005`(追加 CLAUDE.md "AI 工作约定"小节)
- 提交哈希:`2f41bb0`

## 9. 提交计划

```bash
git add plugins/code-skills/skills/code-rule/templates/rule.md

git commit -m "feat(code-rule): extend templates/rule.md with placeholder/bootstrap modes (REQ-00003 FR-3+FR-4)

在 templates/rule.md 头部追加 2 个 H2 小节:
- 占位模式(本文件可作为空占位使用)
- 引导模式(本文件可作为引导模板使用)

2 个模式互斥(用户二选一)。
8 字段规则小节结构保持(INV-1)。
+23 行, 0 删除。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>"
```

## 10. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 |
| --- | --- | --- | --- |
| 2026-06-04 10:55 | v1 | 改修完成 | `templates/rule.md` 头部 +23 行,2 个 H2 小节(占位/引导模式);8 字段结构保持(INV-1) |
