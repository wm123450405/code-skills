# 澄清记录 — REQ-00002
更新时间:2026-06-03 20:25
版本:V0.0.1

## 第 1 轮:本设计阶段(2026-06-03 20:25)

### 处理策略

REQU 文档(REQ-00002/RESULT.md §12)中列出 6 项待澄清 Q-6 / Q-7 / Q-8 / Q-9 / Q-10 / Q-11 / Q-12,共 7 项实际编号。其中:
- **Q-7** 已在 REQU 阶段 v2(2026-06-03 20:18)由用户通过追加指令锁定,本设计阶段不再讨论
- **Q-11**(REQ-00002 与 REQ-00001 实施顺序)在 REQU v3(2026-06-03 20:20)已"措辞调整"为"原先后顾虑部分消解"(因 REQ-00001 部分重命名已提前落地),本设计不视为阻塞

本次设计阶段对剩余 5 项非阻塞 Q(均**有 REQU 文档默认假设**)采用默认方案推进,并显式记录默认值与回退路径。如用户在 `code-plan` 阶段前改变主意,可触发本设计 v2 增量更新。

---

### Q-6:V0.0.0 下 EXISTING-001~010 是否一并纳入"追溯覆盖"?

- **REQU 文档原始选项**:
  - H1. 保留 EXISTING- 前缀(基线特例)
  - H2. 全部改名为 REQ-NNNNN
  - H3. 折中(EXISTING- 改为 REQ- 但标记 `existing=true` 元字段)
- **REQU 文档默认**:H1(基于 REQU FR-10 显式声明"严禁修改 V0.0.0")
- **本设计采用**:**H1(保留 EXISTING- 前缀)**
- **理由**:
  1. 保留 code-init 基线的特殊语义("代码现状的快照" vs "未来需求")
  2. code-init SKILL.md 第 7-8 步改动最小(NFR-5 最小化)
  3. "三类编码统一"目标不彻底 — 通过文档说明(EXISTING- 是基线特例,语义独立)弥补
- **影响章节**:本设计 FR-10(不修改 V0.0.0,除非 Q-6 = H2)
- **回退路径**:若用户在 `code-plan` 前回答"H2(改 REQ-NNNNN)",需新增 FR-11 覆盖 V0.0.0/EXISTING- 全量迁移,工作量约 +10 文件;若 H3,需新增 FR-12 并引入新字段(`existing=true` 元字段),但需评估是否触发 `marketplace-protocol.md §规则 1`(未知字段不允许) → H3 **不推荐**

---

### Q-7:TASK 编码结构(已在 REQU v2 锁定)

- **锁定方案**(2026-06-03 20:18):G4 新嵌套式 `TASK-(REQ|BUG)-\d{5}-\d{5}`
  - 需求任务:`TASK-REQ-<REQ 数字段>-NNNNN`,如 `TASK-REQ-00001-00001`
  - 修复任务:`TASK-BUG-<BUG 数字段>-NNNNN`,如 `TASK-BUG-00001-00001`
  - 任务序号在**父级内**独立递增
  - 正则:`^TASK-(REQ|BUG)-\d{5}-\d{5}$`
- **本设计采用**:**G4(已锁定)**
- **影响章节**:FR-1 / FR-2 / FR-3 / AC-2 / AC-3 / §7 交互 2 / §8 数据与状态 / §9 边界
- **回退路径**:N/A(已锁定)

---

### Q-8:是否新建 `./assistants/rules/encoding-conventions.md` 作为权威源?

- **REQU 文档原始选项**:
  - a. 新建(由 `code-rule` 在实施阶段创建)
  - b. 不新建(各 SKILL.md 散落硬编码)
- **REQU 文档默认**:Q-8 待用户确认;REQU FR-7 隐含倾向 (a)
- **本设计采用**:**(a) 新建**(由 `code-rule` 创建)
- **理由**:
  1. 编码格式有唯一权威源(NFR-1 强烈建议)
  2. 各 SKILL.md 引用该规则,不硬编码 — 减少后续维护成本
  3. 符合"项目级规范"语义边界(`code-rule` 维护)
- **影响章节**:本设计 FR-7 条件触发 / AC-7 条件适用
- **实施约束**:本需求**不直接写入** `./assistants/rules/`(REQU FR-7 显式);由用户在 `code-it` 阶段调 `code-rule` 技能创建
- **回退路径**:若用户在 `code-plan` 前回答"(b) 不新建",FR-7 不落地,AC-7 不适用,各 SKILL.md 必须穷尽格式描述

---

### Q-9:是否持久化 `migration-mapping.md`?

- **REQU 文档原始选项**:
  - a. 写入 `migration-mapping.md`
  - b. 仅 commit message 提及
  - c. 不记录
- **REQU 文档默认**:Q-9 待用户确认
- **本设计采用**:**(a) 写入**
- **理由**:
  1. 追溯可长期查询(NFR-3 可观测性)
  2. 已知映射:`REQ-2026-0001 → REQ-00001`(已部分落地)
  3. (若 Q-6 = H2)可记录 EXISTING- 全部 10 行
- **影响章节**:本设计 FR-8 条件触发 / AC-8 条件适用
- **实施位置**:`./assistants/V0.0.1/require/REQ-00002/migration-mapping.md`(REQU FR-8 显式指定)
- **回退路径**:若用户选 (b) 或 (c),FR-8 不落地,AC-8 不适用

---

### Q-10:cache 同步是否需要在 README 增加显式提示?

- **REQU 文档原始选项**:
  - a. 加(显式提示"实施完成后请执行 /reload-plugins")
  - b. 不加(本仓库默认 b)
- **REQU 文档默认**:b
- **本设计采用**:**b(不加)**
- **理由**:
  1. NFR-5 最小化
  2. README 中"实施后需 /reload-plugins"已隐含(无需显式加段)
  3. cache 同步超出本仓库可控范围,显式提示不解决问题
- **影响章节**:本设计 FR-4 描述(不加该提示)
- **回退路径**:若用户选 a,在 README 加一句"实施完成后请执行 /reload-plugins";doc-conventions §规则 1 同步触发(中英都加)

---

### Q-12:"需求编码"在 TASK 编码中是否含 `REQ-` 前缀?

- **REQU 文档原始选项**:
  - a. 仅数字段(默认),如 `TASK-REQ-00001-00001`
  - b. 含 `REQ-` 前缀,如 `TASK-REQ-REQ-00001-00001`
- **REQU 文档默认**:a(REQU v2 默认采用 a)
- **本设计采用**:**a(仅数字段)**
- **理由**:
  1. 字面简洁(18 字符 vs 23 字符)
  2. 正则更短(`^TASK-(REQ|BUG)-\d{5}-\d{5}$`)
  3. 与分析笔记 §Q-12 推荐一致
- **影响章节**:FR-1 正则字面量 / FR-2 各 SKILL.md 文本 / FR-3 模板示例 / AC-2/AC-3 Grep 模式
- **回退路径**:若用户在 `code-plan` 前回答"b(含 REQ- 前缀)",需批量更新所有正则与示例为 `TASK-REQ-REQ-00001-00001`

---

## 第 2 轮:实施阶段可能的新澄清(预占位)

> 本节为预占位,实际填写在 `code-plan` / `code-it` 阶段。如用户在实施前改主意回答 Q-6/Q-8/Q-9/Q-10/Q-12,在此追加。

(暂无)
