# 澄清记录 — REQ-00003
更新时间:2026-06-03 20:45
版本:V0.0.1

## 第 1 轮:本需求阶段(2026-06-03 20:35)

### 处理策略

REQU 阶段共向用户追问 3 个最阻塞的问题,均已锁定。

| 编号 | 问题 | 锁定答案 | 时间 |
| --- | --- | --- | --- |
| Q-1 | 本需求要为 `code-rule` 增加哪几种"核心编码规范"类型? | 6 类(Type A 内部 6 个分类:框架/三方依赖/语言与命名/目录与模块/代码书写/提交合并,部分条件性;部分"未来可能需要"的建空占位文件) | 2026-06-03 20:35 |
| Q-2 | 用户调用 `code-rule` 时,如何识别"类型 X"? | 两者结合 — `code-rule` 优先从描述的关键词/语义自动推断;推断不出或置信度低时,显式追问用户 | 2026-06-03 20:35 |
| Q-3 | 类型 C(模板内容提示)的插入位置? | 两者都支持 — 末尾追加 + 内联 | 2026-06-03 20:35 |

### Q-1 详细记录

**用户原始回答**:
> 框架规范(如果需要)/三方依赖规范(如果需要)/语言与命名规范/目录与模块规范/代码书写规范/提交与合并规范(如果需要)等,部分规范需要和用户确认是否需要或是否以后需要(当前不需要未来可能需要的可以先建立起简单的规范文件,不设置任何规范内容)

**REQU 阶段对回答的整理**:
- **6 个分类清单**:
  | 编号 | 分类 | 默认 vs 条件性 | 默认对应文件 |
  | --- | --- | --- | --- |
  | C-1 | 框架规范 | 条件性 | `framework-conventions.md` |
  | C-2 | 三方依赖规范 | 条件性 | `dependency-conventions.md` |
  | C-3 | 语言与命名规范 | **默认** | `naming-conventions.md` |
  | C-4 | 目录与模块规范 | **默认** | `directory-conventions.md`(或与 `module-conventions.md` 合并 — 详见 RESULT.md §12 Q-4) |
  | C-5 | 代码书写规范 | **默认** | `coding-style.md` |
  | C-6 | 提交与合并规范 | 条件性 | `commit-conventions.md` |
- **占位规则**(用户明确):"当前不需要未来可能需要的可以先建立起简单的规范文件,不设置任何规范内容"
  - 对 C-1 / C-2 / C-6(条件性),`code-rule` 应**主动追问**用户"现在需要 / 未来占位 / 跳过"
  - 对 C-3 / C-4 / C-5(默认),用户描述命中时直接进入"现在需要"流程(FR-2)

**对 REQU 文档的影响**:
- 写入 FR-2(扩展 Type A 分类识别,支持 6 类)
- 写入 FR-3(条件性分类的占位模式)
- 写入 FR-4(默认分类的引导模式)
- 写入 §12 Q-4(目录与模块规范与现有 `module-conventions.md` 的关系,待确认)

### Q-2 详细记录

**用户原始回答**:两者结合

**REQU 阶段对回答的整理**:
- `code-rule` 优先从描述的关键词/语义自动推断类型
- 推断不出或置信度低时,显式追问用户
- 用户可显式覆盖(例如说"对 X 文件加 Y"形式)

**对 REQU 文档的影响**:
- 写入 FR-7(类型识别方式:自动 + 显式)
- 写入 §7.1 主流程状态机的"类型识别"步骤
- 写入 AC-8(类型识别自动化测试)

### Q-3 详细记录

**用户原始回答**:两者都支持

**REQU 阶段对回答的整理**:
- Type C 末尾追加:在模板文件末尾追加"## 提示: <主题>"小节
- Type C 内联:在指定二级小节下追加提示
- 用户需在调用 `code-rule` 时显式指定插入位置(末尾 vs 内联 + 哪个二级小节)

**对 REQU 文档的影响**:
- 写入 FR-6(Type C 两种插入位置)
- 写入 AC-6(末尾追加验证)
- 写入 AC-7(内联验证)
- 写入 §12 Q-7(标准格式细节,待确认)

## 第 2 轮:code-design 阶段可能的新澄清(预占位)

> 本节为预占位,实际填写在 `code-design` / `code-plan` / `code-it` 阶段。

| 编号 | 问题 | 期望回复时间 |
| --- | --- | --- |
| Q-4 | "目录与模块规范"(C-4)与现有 `module-conventions.md` 的关系(合并 / 独立) | `code-design` 前 |
| Q-5 | 6 个新分类,本需求是"全部建占位"还是"3 个默认分类预填示例" | `code-design` 前 |
| Q-6 | Type B 标准字段是否加"正/反示例" | `code-design` 前 |
| Q-7 | Type C 标准格式细节(末尾 / 内联的标题级别) | `code-design` 前 |
| Q-8 | 现有 5 个规范文件保留 / 部分迁移 / 整体重分类 | `code-design` 前 |

(暂无第 2 轮问答)
