# 改修总结 — TASK-REQ-00044-00006 · 创建 code-faq 技能

## 1. 任务概述
- 所属需求:REQ-00044
- 任务类型:新增
- 涉及文件:5 个新建文件

## 2. 改动内容

| 文件 | 说明 |
| --- | --- |
| `code-faq/SKILL.md` | 知识查询与文档导出技能主文件。支持查询模式(跨版本搜索需求/缺陷)和导出模式(--require/--design/--summary/--template)。遵循渐进式加载模式,参数解析/查询/导出详细逻辑下沉到 references/ |
| `code-faq/references/common.md` | 公共流程文档。含参数解析算法(4 种冲突处理)、查询模式(搜索范围/策略/排序/深度读取/回答组装)、导出模式(源文档定位/概要提取/模板填充/写出文件)、模板占位符映射表(需求导出 8 个 + 设计导出 8 个 + 概要导出 5 个) |
| `code-faq/templates/REQUIRE-EXPORT.md` | 需求文档导出模板。含 8 个占位符:{{REQ_ID}}/{{REQ_TITLE}}/{{需求概述}}/{{FR_LIST}}/{{NFR_LIST}}/{{AC_LIST}}/{{关联需求}}/{{待澄清}} |
| `code-faq/templates/DESIGN-EXPORT.md` | 设计文档导出模板。含 8 个占位符:{{REQ_ID}}/{{REQ_TITLE}}/{{设计概述}}/{{模块列表}}/{{接口列表}}/{{数据结构}}/{{方案选型}}/{{关键流程}} |
| `code-faq/templates/SUMMARY-EXPORT.md` | 概要设计导出模板。含 5 个占位符:{{REQ_ID}}/{{REQ_TITLE}}/{{设计概述}}/{{模块概要}}/{{关键决策}} |

## 3. 关键决策

- **双模式设计**:查询模式(无导出参数,跨版本搜索+屏幕输出)和导出模式(--require/--design/--summary/--template,写出文件),两模式互斥
- **查询模式不强制版本**:查询模式下无激活版本也能执行(全局搜索所有版本),导出模式必须有激活版本
- **--summary 仅对 --design 生效**:从 DESIGN.md 提取概要信息(设计概述+模块名+关键决策),去掉详细接口/数据结构/算法伪代码
- **模板占位符体系**:与 code-req 的 --result 模板填充体系对齐,共用 15 个占位符中的 8 个(需求) / 8 个(设计) / 5 个(概要),新增 3 个 code-faq 专属占位符({{导出时间}}/{{模块概要}}/{{关键决策}})
- **只读约束**:code-faq 不修改 assistants/ 下任何文件,导出文件仅写入用户指定路径
- **与 code-req 的结构对齐**:SKILL.md 约 160 行,采用相同的渐进式加载模式(references/common.md 下沉详细逻辑),工具约定/衔接/不要做的事章节与 code-req/code-fix 保持一致

## 4. 验证结果

- 编译:不适用(纯文档产出)
- 运行:不适用
- 测试:不适用

## 5. 变更记录

| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-30 17:30 | v1 | 初始创建 | code-faq SKILL.md + references/common.md + 3 templates 创建完成 | wangmiao |