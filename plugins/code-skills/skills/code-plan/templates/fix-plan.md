# 缺陷修复方案 — <缺陷编号>:<标题>

> 本文件由 `code-plan` 技能(缺陷分支)产出,作为 `code-it <缺陷编号>` 实施修复的依据。
> 详细缺陷描述见 `./RESULT.md`。
> 最后更新:YYYY-MM-DD HH:mm
> 适用版本:基线版本 `<版本号>`

## 缺陷摘要
- **缺陷编号**:`<缺陷编号>`
- **标题**:<一句话>
- **严重度**:P? (P0/P1/P2/P3)
- **当前状态**:`修复规划中`
- **报告时间**:YYYY-MM-DD HH:mm
- **报告人**:<用户>

> 完整描述、复现步骤、影响范围见 `./RESULT.md`;**本文件不重复这些信息**。

---

## 根因定位

> 关键:根因要具体到"哪个文件:哪一行:什么问题"。

- **缺陷类型**:<如 逻辑错误 / 边界条件 / 性能 / 资源泄漏 / 安全 / 兼容性 / 数据错误>
- **根因**:
 - 文件:`<路径>`
 - 位置:`<路径>:<行号>` 或 `<路径>:<函数名>`
 - 问题:<该处代码做了什么,为什么导致 bug>
- **已排除的可能**:
 - <如 不是 X 模块的问题,因为 Y 现象不匹配>
 - ...

---

## 修复方案

### 候选方案

#### 方案 A(选定)
- **思路**:<一句话>
- **涉及文件**:
 - `<文件路径>` — <变更点>
 - `<文件路径>` — <变更点>
- **优点**:<如 改动小、风险低、不影响其他模块>
- **缺点**:<如 治标不治本 / 需要数据迁移>
- **工作量**:<如 0.5 人天>

#### 方案 B(备选)
- **思路**:<一句话>
- **优点**:<...>
- **缺点**:<...>
- **为何不选**:<如 改动过大、超出本版本范围、需要重写核心模块>

#### 方案 C(备选)
- ...

### 选定:方案 A
- 理由:<综合考虑>

---

## 涉及文件与变更

| 文件 | 当前状态 | 改为 | 理由 |
| --- | --- | --- | --- |
| `<文件路径>` | <如 第 42 行的 `if x == null: return`> | <如 `if x is None: raise ValueError(...)`> | <如 显式抛错更易追踪> |
| ... | ... | ... | ... |

---

## 修复步骤(若跨多步)

> 大多数 bug 修复是单步的(改 1-2 个文件),本节可省略。
> 若修复确实跨多步,按以下格式列出:

### 步骤 1:<一句话>
- 涉及文件:`<文件路径>`
- 关键变更:<一句话>
- 验证:<一句话,如何确认本步成功>
- 预计工作量:<如 0.5h>

### 步骤 2:<一句话>
- ...

---

## 测试方案

### 回归用例
- **新增测试**:
 - `<测试文件>::<测试函数>` — <验证什么>
 - ...
- **既有测试**:
 - 修复后必须仍通过的有:...

### 手动验证
1. <复现 bug 的原始步骤>
2. <应观察到:bug 不再出现>
3. <其他场景也应正常>

### 验证手段
- <如 `npm test` 应全部通过>
- <如 `pytest tests/test_module.py` 应通过>
- <如 启动应用,人工复现>

---

## 风险与回退

### 风险
- <如 修改 `login()` 可能影响所有登录路径,需回归>
- <如 数据迁移可能丢失历史数据>
- ...

### 影响范围
- 直接影响:<模块/接口/用户>
- 间接影响:<可能受影响的相邻模块>

### 回退策略
- 触发条件:<如 修复后启动失败 / 测试失败率上升 / 新 bug>
- 回退步骤:
 1. <如 `git revert <commit-hash>`>
 2. <如 重启服务>
- 验证回退成功:<如 启动日志正常 / 原 bug 仍存在(说明回退生效)>

---

## 规范遵循

- 涉及的规范文件:
 - `./assistants/rules/<file>` §<X> — <如何满足>
- 偏离(若有):
 - <章节>:<偏离内容>
 - 理由:<...>
 - 授权:<用户/时间>

---

## 待澄清 / 未决项

- <如 不确定是否需要数据迁移,需 DBA 确认>
- <如 不确定是否需要通知用户,需 PM 决定>
- ...

---

## 变更记录

> 写入方:`code-plan`(首次/增量)、`code-it`(实施完成)、`code-check`(评审)

| 时间 | 变更类型 | 变更摘要 | 关联缺陷 |
| --- | --- | --- | --- |
| YYYY-MM-DD HH:mm | 修复规划 | code-plan 完成 `<缺陷编号>` 修复方案 | `<缺陷编号>` |
| YYYY-MM-DD HH:mm | 修复完成 | code-it 完成实施,提交 abc1234 | `<缺陷编号>` |
| YYYY-MM-DD HH:mm | 状态推进 | `<缺陷编号>` 状态"修复编码中"→"已修复-待验证" | `<缺陷编号>` |
