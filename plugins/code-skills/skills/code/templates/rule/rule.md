# <分类中文名>规范(<分类英文名>)

> 本规范文件由 `/code rule` 子命令维护,所有 `/code` 子命令在执行时会读取本文件作为强制约束。
> 最后更新:YYYY-MM-DD HH:mm
> 适用版本:跨所有版本共享(项目级)

## 占位模式(本文件可作为空占位使用)

若用户选择"未来占位"模式,可省略"## 规则 1"及之后小节,仅保留:

- 分类标题(本文件行 1)
- "本规范由 `/code rule` 维护"声明(行 3)
- "## 适用场景"占位(下文)
- "## 强制级别约定"说明(下文)
- 1 个"## 规则 1: (待添加)"空小节(下文,作为用户后续追加规则的起点)

适用场景:`/code rule` 收到用户添加条件性分类(框架/三方依赖/提交合并)的规则描述时,先创建空占位,等用户后续填充具体规则。

## 引导模式(本文件可作为引导模板使用)

若用户选择"引导模式"(默认分类首次添加时),可提示用户填 1-2 条示例规则:

- 在"## 规则 1"小节写入用户提供的示例(标准 8 字段)
- 在"## 规则 2"小节可写第二条示例(可选)

适用场景:`/code rule` 收到用户添加默认分类(命名/目录/代码书写)的首条规则时,引导用户填 1-2 条示例,降低未来使用成本。

**注意**:占位模式与引导模式互斥(用户二选一),不可同时启用。

## 适用场景
<本规范文件覆盖什么范围 / 什么时候需要查阅本文件。例:"本规范适用于所有 Python 后端代码,涉及命名/函数签名/错误处理时查阅本文件。">

## 强制级别约定
本文件中各规则的强制级别逐条标注(参见各"规则 N"小节的"强制级别"字段),本文件头部不统一声明。

---

## 规则 1:<规则简称>

### 条款
<用一句或一段清晰的话描述规则,避免歧义。例:"所有公开函数命名必须使用 camelCase,不得使用 snake_case 或 PascalCase。">

### 强制级别
- 必须 / 推荐 / 参考

### 适用范围
- <语言/模块/范围。例:"所有 Python 后端模块"、"所有新增代码(存量不在此范围)">

### 正面示例
\```
<好的代码或做法,带简要注释>
\```

### 反面示例
\```
<坏的代码或做法,带简要注释>
\```

### 例外
- <什么情况下可破例,无则写"无">
- 例:"性能关键路径的内联函数可豁免"

### 关联规范
- ./assistants/rules/<其他分类>.md §规则 M(若有)
- 无 / 待补充

### 来源
- 由 `/code rule` 子命令添加于 YYYY-MM-DD HH:mm
- 用户原始描述:"<用户最初说的话,留作可追溯性>"

---

## 规则 2:<规则简称>
<同上结构,后续规则按编号追加>
