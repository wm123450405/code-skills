# 现有功能需求 — EXISTING-NNN:<标题>

> 本文档由 `code-init` 技能基于项目现有代码生成。
> 描述的是"**代码中已实现**"的功能,而非待开发的功能。
> 未来对该功能的**修改**应通过 `code-require`(增量更新本文件)进行。
> 最后更新:YYYY-MM-DD HH:mm
> 适用版本:基线版本 `<初始版本号>`

## 需求概述
<一两句话清晰说明本功能做什么>

## 现有实现位置

| 维度 | 位置 |
| --- | --- |
| 主要文件 | `<如 src/auth/login.py / src/routes/auth.ts>` |
| 关键函数/类 | `<如 LoginService.login() / verifyToken()>` |
| 涉及文件数 | <数字> |
| 大致代码量 | <如 约 200 行> |

### 关键代码位置(可选)
- `<文件路径>:<行号>` — <该段代码做什么>
- `<文件路径>:<行号>` — <...>
- ...

## 用户角色与场景

### 角色
- <如 终端用户 / 管理员 / 第三方系统>

### 场景
- <如 用户在登录页输入手机号+验证码,完成登录>
- <如 管理员后台查看用户列表>
- ...

## 功能点(FR)

> FR-N 是从现有代码中**可观测到**的功能项,每条都应能通过运行/调用验证。

- **FR-1**:<如 "用户可通过手机号+密码登录,登录成功返回 JWT">
- **FR-2**:<如 "登录失败返回 401 + 错误码 1001(账号不存在)/ 1002(密码错)">
- **FR-3**:<如 "登录态保存在 HTTP-only cookie,有效期 7 天">
- ...

## 关键接口

> 列出本功能对外暴露的接口(API 端点、函数签名、CLI 命令等)。

### HTTP API(若有)
| 方法 | 路径 | 请求 | 响应 | 错误码 |
| --- | --- | --- | --- | --- |
| POST | /api/v1/auth/login | `{phone, password}` | `{token, user}` | 1001, 1002, 1003 |

### 函数签名(若是库)
\```
<函数签名 + 简要说明>
\```

### CLI(若有)
\```
<命令 + 参数 + 简要说明>
\```

## 数据模型(若适用)

| 实体 | 字段 | 约束 |
| --- | --- | --- |
| <如 User> | id: int (PK) | NOT NULL, AUTO_INCREMENT |
| | phone: varchar(11) | NOT NULL, UNIQUE |
| | password_hash: varchar(255) | NOT NULL |
| | created_at: timestamp | NOT NULL, DEFAULT NOW() |
| ... | ... | ... |

## 验收标准(AC)

> AC-N 是从**现有行为**反推出的可验证条件,运行/调用后应满足。

- **AC-1**:<如 "用正确手机号+密码 POST /api/v1/auth/login,响应 200,body 含 token 字段">
- **AC-2**:<如 "用错误密码登录,响应 401,body 含 code=1002">
- **AC-3**:<如 "连续 5 次错误密码后,账号被锁定 15 分钟">
- ...

## 关联功能

> 与其他 `EXISTING-MMM` 的关联(共享模块、数据、接口)。

| 关联编码 | 关联点 | 影响 |
| --- | --- | --- |
| EXISTING-NNN | <如 共享 user 表 / 共享 token 校验中间件> | <如 本功能的修改需同步考虑它> |
| ... | ... | ... |

## 已知限制/技术债

> 本功能自身存在的局限,未来重构时可考虑。

- <如 "未实现短信验证码登录,只支持密码">
- <如 "JWT 未配置刷新机制,过期后必须重新登录">
- <如 "密码强度校验逻辑散落在多处,未抽公共函数">
- ...

> 无明显技术债可写"无显著技术债"。

## 变更记录

| 时间 | 变更类型 | 变更摘要 | 关联项 |
| --- | --- | --- | --- |
| YYYY-MM-DD HH:mm | 需求登记 | code-init 识别并登记现有功能 EXISTING-NNN | EXISTING-NNN |
| <后续如有修改> | 需求变更 | <由 code-require 增量追加> | EXISTING-NNN |
