# <分类中文名>规范(<分类英文名>)

> 本规范文件由 `code-rule` 技能维护,所有 `code-*` 技能在执行时会读取本文件作为强制约束。
> 最后更新:YYYY-MM-DD HH:mm
> 适用版本:跨所有版本共享(项目级)

## 适用场景
<本规范文件覆盖什么范围 / 什么时候需要查阅本文件。例:"本规范适用于所有 Python 后端代码,涉及命名/函数签名/错误处理时查阅本文件。">

## 强制级别约定
本文件中各规则的强制级别逐条标注(参见各"规则 N"小节的"强制级别"字段),本文件头部不统一声明。

---

## 规则 1:<规则简称>

### 条款
<用一句或一段清晰的话描述规则,避免歧义。例:"所有公开函数命名必须使用 camelCase,不得使用 snake_case 或 PascalCase。">

### 强制级别
- 必须 / 推荐 / 参考

### 适用范围
- <语言/模块/范围。例:"所有 Python 后端模块"、"所有新增代码(存量不在此范围)">

### 正面示例
\```
<好的代码或做法,带简要注释>
\```

### 反面示例
\```
<坏的代码或做法,带简要注释>
\```

### 例外
- <什么情况下可破例,无则写"无">
- 例:"性能关键路径的内联函数可豁免"

### 关联规范
- ./assistants/rules/<其他分类>.md §规则 M(若有)
- 无 / 待补充

### 来源
- 由 `code-rule` 技能添加于 YYYY-MM-DD HH:mm
- 用户原始描述:"<用户最初说的话,留作可追溯性>"

---

## 规则 2:<规则简称>
<同上结构,后续规则按编号追加>
