# RESULT.md 模板 — 概要设计

> 本文件是 `code-design` 技能产出的 `RESULT.md` 章节结构与填写规范。
> 模型在生成 `RESULT.md` 时应严格按本结构组织,确保后续 `code-plan` 可直接消费。
>
> **版本感知**:本文件存放在 `./assistants/<版本号>/design/<需求编码>/RESULT.md`,文档头应明确记录版本号。
>
> **本模板只覆盖概要设计**。完整字段/接口 schema/算法伪代码/迁移脚本等详见 `code-plan` 的 `templates/plan.md` §4-§10 + 过程文档(module-details.md / interface-specs.md / data-changes.md)。

---

## 文档头
```
# <需求编码> — <概要设计标题>

- 需求编码:<REQ-YYYY-NNNN>
- 所属版本:<版本号>(如 v1.0.0)
- 上游:./assistants/<版本号>/require/<需求编码>/RESULT.md (v<n>)
- 遵循规范:./assistants/rules/<规范文件1>, <规范文件2>, ...(共 N 个)
- 状态:草稿 / 已评审 / 已锁定
- 责任人:<设计者>
- 创建:<YYYY-MM-DD>
- 最近更新:<YYYY-MM-DD HH:mm>
- 当前版本:v<n>
```

## 设计目标
<!-- 本节由 code-design / code-plan 步骤 0b 自动生成(写入或沿用),记录用户确认的设计目标;如需手动编辑,保留该注释以便步骤 0b 识别 -->

## 1. 设计概述
用 2-5 句话概括:**本设计要解决什么问题,核心架构思路是什么,新增/复用/修改的范围**。电梯演讲版本。

## 2. 需求回顾
引用上游 `RESULT.md`,**不要全文复制**,只摘取关键约束:
- 关键 FR 列表(对应到本设计的哪些模块)
- 关键 NFR(性能/可用性/安全等目标值)
- 关键 AC(影响设计走向的验收点)
- 需求中的"待澄清"项(可能影响设计)

## 2.5 规范遵循
本节是规范约束的**总账**,后面每节涉及规范时只引用本节条目,不再重复描述。

### 2.5.1 适用的规范文件
| 规范文件 | 类别 | 关键约束 | 本设计对应章节 |
| --- | --- | --- | --- |
| ./assistants/rules/architecture.md | 功能架构 | ... | §5, §6, §7 |
| ./assistants/rules/module-conventions.md | 模块规划 | ... | §7 |
| ./assistants/rules/api-standards.md | 接口定义 | ... | §8 |
| ./assistants/rules/data-modeling.md | 数据结构 | ... | §9 |
| ... | ... | ... | ... |

### 2.5.2 规范自检结论
- **完全合规**的章节:§5, §7, §8, ...
- **经用户授权偏离**的章节:见 §2.5.3
- **待澄清冲突**:见 §2.5.4

### 2.5.3 用户授权的偏离
```
- 章节:§X
- 偏离内容:<具体偏离>
- 依据规范:./assistants/rules/<文件> §<章节>(要求 Y)
- 偏离理由:<技术必要 / 业务必要 / 特殊场景>
- 授权时间:YYYY-MM-DD HH:mm
- 影响范围:...
```

### 2.5.4 待澄清的规范冲突
```
- 冲突:规范要求 X,需求 Y 要求 Z
- 阻塞章节:§X
- 已提供选项 / 用户选择 / 状态
```

## 3. 设计目标与非目标
- **目标**:本设计要满足的架构/质量目标(可衡量)
- **非目标**:本设计不解决的问题(与需求的 Out of Scope 对齐)

## 4. 约束清单
- **硬约束**(不可违反):语言/平台/合规/已有接口/部署环境等
- **软约束**(可权衡):性能预算/团队熟悉度/运维成本/上线时间

## 5. 架构总览
**必须**用图描述,文字说明无法替代:
- **组件图**(Mermaid / ASCII):展示本设计涉及的所有组件及其关系
- **数据流图**:关键请求从进入到返回的流转路径
- **控制流图**:关键状态/分支的流向
- **部署视角**(如适用):运行时各组件如何部署

## 6. 功能架构
按"功能域"组织,每个功能域给出:
- 包含的模块
- 模块间的协作方式
- 入口与出口

```
### 功能域 1:<名称>
- **目的**:...
- **涉及模块**:...
- **协作方式**:...
```

## 7. 模块划分

### 7.1 模块总览
| 模块名 | 路径 | 状态(新增/复用既有/修改既有) | 职责 | 依赖 |
| --- | --- | --- | --- | --- |
| ... | ... | ... | ... | ... |

### 7.2 新增模块
对每个新增模块给出:
```
#### 模块名:<名称>
- **路径**:<源代码路径>
- **职责**:...
- **理由**:为什么需要新增?为何这样切?
```

### 7.3 复用既有模块
- <模块名>:<复用方式,直接调用/扩展/包装>
- 给出既有模块的引用位置(文件路径/行号)

### 7.4 修改既有模块
- <模块名>:<变更点>
- 变更对既有调用方的影响评估

### 7.5 设计边界

> 本节是**概要设计 vs 详细设计**的硬切边界。以下细节**不**在 `code-design` 概设阶段展开,由 `code-plan` 详设阶段产出:
>
> - 模块的**内部类/函数签名**(类名、方法名、参数列表、返回类型)
> - 模块的**调用顺序**(方法之间的依赖与时序)
> - 模块的**并发模型**(锁、线程、消息队列、async/await)
> - 模块的**状态归属**(全局状态 / 实例状态 / 无状态)
> - 模块的**错误处理范式**(异常类型 / 返回值 / 全局兜底)
>
> 上述细节在 `plan/.../RESULT.md §4 模块详细化` + `module-details.md` 中展开。

## 8. 接口概要

### 8.1 新增接口
| 接口名 | 形式 | 状态 | 一句话职责 |
| --- | --- | --- | --- |
| ... | ... | ... | ... |

### 8.2 扩展既有接口
- <接口名>:<扩展点,新增字段/方法/参数;向后兼容性>

### 8.3 修改既有接口
- <接口名>:<修改点;迁移路径;废弃计划>

### 8.4 调用外部接口
- <外部系统/API>:<用途;调用方式;错误处理;超时/重试>

### 8.5 设计边界

> 本节是**接口概要 vs 接口细节**的硬切边界。以下细节**不**在 `code-design` 概设阶段展开,由 `code-plan` 详设阶段产出:
>
> - 接口的**完整入参/出参 schema**(字段名、类型、必填、长度、范围、格式)
> - 接口的**错误码**(错误码号、错误码消息、HTTP 状态码)
> - 接口的**示例**(正常请求/响应、异常请求/响应)
> - 接口的**版本策略**(URL 版本 / Header 版本 / 媒体类型版本)
> - 接口的**鉴权**(API Key / OAuth / JWT / IAM 角色)
> - 接口的**限流策略**(QPS 限流 / 并发限流 / 滑动窗口)
> - 接口的**幂等保证**(幂等键 / 去重窗口 / 重试机制)
> - 接口的**链路追踪字段**(trace_id / span_id / request_id)
>
> 上述细节在 `plan/.../RESULT.md §7 接口细节` + `interface-specs.md` 中展开。

## 9. 数据结构

### 9.1 新增实体
| 实体名 | 状态 | 关键字段(≤ 5 个,纯名字列表) | 一句话关系 |
| --- | --- | --- | --- |
| ... | ... | ... | ... |

### 9.2 修改既有实体
- <实体名>:<变更字段/关系;数据迁移需求>

### 9.3 数据生命周期
- 留存期限
- 清理/归档策略
- 备份与恢复策略

### 9.5 设计边界

> 本节是**数据结构概要 vs 数据结构完整变更**的硬切边界。以下细节**不**在 `code-design` 概设阶段展开,由 `code-plan` 详设阶段产出:
>
> - 字段的**类型**(string / number / integer / boolean / datetime / UUID)
> - 字段的**约束**(NOT NULL / DEFAULT / CHECK / UNIQUE / 长度 / 范围)
> - 字段的**索引**(主键索引 / 唯一索引 / 普通索引 / 复合索引 / 全文索引)
> - 实体的**存储选型**(关系型 / 文档型 / 键值型 / 时序型 / 图数据库)
> - 字段的**数据迁移脚本**(初始数据迁移 / 字段重命名 / 类型转换 / 索引重建)
>
> 上述细节在 `plan/.../RESULT.md §6 数据结构完整变更` + `data-changes.md` 中展开。

## 10. 三方依赖

### 10.1 复用既有依赖
- 列出本设计使用的项目已有三方依赖

### 10.2 新增依赖
| 依赖名 | 一句话用途 | 必要性 | 替代评估 |
| --- | --- | --- | --- |
| ... | ... | ... | ... | ... | ... |

### 10.3 拒绝引入的依赖及理由
- 评估过但被否决的依赖,记录理由(避免下次再被提起)

## 11. 集成点
本设计与**既有系统/既有模块**的集成:
- 共享数据/状态
- 调用既有 API
- 提供新 API 给既有调用方
- 部署/网络/鉴权层面的衔接

## 12. 风险与缓解
| 风险 | 可能性 | 影响 | 缓解措施 | 回退方案 |
| --- | --- | --- | --- | --- |
| ... | 高/中/低 | 高/中/低 | ... | ... |

## 13. 备选方案(选填,鼓励保留)
对架构中的关键决策,记录至少 1 个**被否决**的备选方案及理由:
- 决策:...
- 备选:...
- 否决理由:...

## 14. 关联概要设计
| 关联设计编码 | 关联点 | 对本设计的影响 | 链接 |
| --- | --- | --- | --- |
| ... | ... | ... | [RESULT](../<其他设计编码>/RESULT.md) |

## 15. 待澄清 / 未决项
| 编号 | 问题 | 影响范围 | 阻塞方 | 期望回复时间 |
| --- | --- | --- | --- | --- |
| Q-1 | ... | §X, §Y | ... | YYYY-MM-DD |

## 16. 变更记录
| 时间 | 版本 | 变更类型 | 变更摘要 | 变更人 |
| --- | --- | --- | --- | --- |
| 2026-06-03 14:00 | v1 | 初始创建 | 完成首次概要设计 | <用户> |
| 2026-06-05 10:00 | v2 | 增量更新 | 需求侧新增 FR-7;代码侧发现既有用户中心模块可复用,模块拆分更新;**规范侧 api-standards.md 修订,§8 错误码约定同步** | <用户> |

变更类型:
- **新增**:全新内容(对应需求新增 / 发现新依赖 / 规范新增)
- **修改**:基于需求/代码/规范变化修订
- **删除**:需求被取消或既有能力/规范被替换
- **重构**:架构决策调整
- **规范触发**:因项目级规范变化而触发的修订(增量更新时常见)
