---
name: code-design
description: 概要设计(版本感知)。要求用户提供"需求编码",**所有产出物写入 `./assistants/<版本号>/design/<需求编码>/`**(由 `./assistants/.current-version` 决定版本号,若未设置则提示先调 `code-version`)。先读取 `./assistants/rules/` 下的项目级规范(跨版本共享),再从 `./assistants/<版本号>/require/<需求编码>/RESULT.md` 读取需求分析结果,结合项目结构与已有代码,产出符合编码规范的概要设计。若 RESULT.md 已存在则做增量更新。同步追加/更新版本看板的"概要设计清单"与"变更记录"区段。在 `code-require` 之后、`code-plan` 之前使用。
---

# code-design — 概要设计(版本感知)

## 目标
回答"系统长什么样"。在 `code-require` 的需求基础上,结合**当前项目实际状况**(语言/框架/既有模块/既有接口),并**严格遵循 `./assistants/rules/` 下的项目级编码规范**,给出可被评审、可被 `code-plan` 进一步拆解的系统级架构方案。

## 适用场景
- 新模块/新服务的架构设计
- 跨模块的方案选型
- 重大重构的方案论证
- 需求变更后重新评估设计
- 项目级规范新增/修订后,需要重新校准已有设计

## 不适用
- 当前**没有激活的版本工作空间**(请先调 `code-version`)
- 需求尚未通过 `code-require` 形成 RESULT.md
- 已知明确的、只涉及单一文件/单一函数的修改
- 紧急线上修复(走 hotfix 流程)

## 工作目录约定(强制)

**版本工作空间**:`./assistants/<版本号>/`(由 `./assistants/.current-version` 决定)。
本技能的目录粒度是**需求**:
```
./assistants/
├── rules/                          # 项目级规范,跨版本共享,本技能只读
│   ├── architecture.md
│   ├── module-conventions.md
│   ├── api-standards.md
│   ├── data-modeling.md
│   └── ...
└── <版本号>/
    ├── RESULT.md                   # 版本看板(本技能追加"概要设计清单" / "变更记录")
    ├── require/<需求编码>/
    │   ├── RESULT.md                # 上游,本技能只读
    │   └── ...
    └── design/<需求编码>/
        ├── RESULT.md                # 本技能产出
        ├── materials-index.md       # 过程文档
        ├── design-notes.md
        ├── module-breakdown.md
        ├── dependencies.md
        ├── related-designs.md
        ├── rule-compliance.md
        └── clarifications.md
```

- 路径以**当前工作目录(CWD)**为基准
- **本技能不修改** `./assistants/rules/`、`./assistants/<版本号>/require/<需求编码>/`、`./assistants/<版本号>/RESULT.md` 中非本技能负责的区段,只读
- 任何对规范/需求的修改都应回到对应上游技能处理
- **需求编号路径(沿用新规则)**:本技能解析 `<需求编号>` 路径(`design/<需求编号>/`)时,后缀字符集放宽为 `[A-Za-z0-9.\-_]+`(沿用 `encoding-conventions §规则 1` 接收端宽松正则);生成端仍默认 5 位纯数字,接收端可承载第三方平台前缀(已通过 `code-rule` 登记的 `JIRA-` 等)

## 输入
- **需求编码**(必填):用户口头或文本指定
  - **默认**格式:5 位纯数字 `^REQ-\d{5}$`(本仓库主动产出)
  - **接收**可放宽:`^REQ-[A-Za-z0-9.\-_]+$`(前缀 `REQ-` + 后缀 1+ 位字母数字/`.`/`-`/`_`;沿用 `encoding-conventions §规则 1` 接收端宽松正则)
- **上游需求**:`./assistants/<版本号>/require/<需求编码>/RESULT.md`(必须存在)
- **项目级规范**:`./assistants/rules/` 下所有文件(建议存在,缺失时本技能会询问用户)
- **当前项目代码**:CWD 下的所有源文件、配置文件、文档

## 输出
主产出物:`./assistants/<版本号>/design/<需求编码>/RESULT.md`

辅助过程文档(同目录下):`materials-index.md` / `design-notes.md` / `module-breakdown.md` / `dependencies.md` / `related-designs.md` / `rule-compliance.md` / `clarifications.md`

**版本看板同步**:本技能在以下节点同步 `./assistants/<版本号>/RESULT.md`:
- 完成概要设计(首次/增量)→ 在"概要设计清单"追加或更新本条记录
- 任意阶段 → 在"变更记录"追加条目

## 工具使用约定
- 读激活版本:`Read "./assistants/.current-version"`
- 读需求:`Read "./assistants/<版本号>/require/<需求编码>/RESULT.md"`
- 读规范:`Glob "./assistants/rules/**/*"` 列出规范文件,逐一 `Read`
- 探项目结构:`Glob` / `Grep` / `Read`,详见步骤 5
- 跨设计关联:`Glob "./assistants/<版本号>/design/*/RESULT.md"`(同版本)与可选 `Glob "./assistants/*/design/*/RESULT.md"`(跨版本)+ `Read`
- 与用户澄清:优先 `AskUserQuestion` 给结构化选项;自然语言兜底
- 写文档:`Write`(首次)/ `Edit`(增量)

## 过程文档自适应判定(本需求 REQ-00035 新增,2026-06-15 起生效)

> 本节是 `code-design` 启动时执行"过程文档生成判定"的依据。AI 在执行过程中按本节的"不涉及/不适用"判定准则自适应决定本轮是否生成对应过程文档。
> 详见 `templates/process-doc-decisions.md`(决策记录文件模板);本节与之配套。

### 适用过程文档清单

| 过程文档 | 判定准则 |
| --- | --- |
| `materials-index.md` | **始终生成**(项目级规范登记是核心) |
| `design-notes.md` | **始终生成**(设计权衡笔记是核心) |
| `module-breakdown.md` | **模块数 ≥ 2 → 生成;模块数 = 1(单文件改)→ 不生成** |
| `dependencies.md` | **依赖其他模块/服务/中间件 ≥ 1 个 → 生成;无外部依赖(自含/单文件)→ 不生成** |
| `related-designs.md` | **Glob 扫描后,发现 ≥ 1 个关联设计 → 生成;0 个 → 不生成** |
| `rule-compliance.md` | **`./assistants/rules/` 存在且有内容 → 生成;无规范 / 空规范 → 不生成** |
| `clarifications.md` | **本轮有用户问路 → 生成;无 → 不生成** |
| 看板"变更记录" | **本轮有追加 → 生成;无变更 / 幂等空跑 → 不追加** |

### 执行流程

1. 步骤 0(版本检测)完成后,执行本步骤
2. 读上游输入(上游需求 / 项目级规范 / 既有 RESULT.md 等)
3. 对每类过程文档,按"适用过程文档清单"判定
4. 收集决策结果 `decisions: [{type, decision, reason}]`
5. 若 `decisions` 存在"不生成" → `Write process-doc-decisions.md`
6. 若 `decisions` 全为"生成" → **不写** `process-doc-decisions.md`
7. 后续步骤按决策结果执行

### 边界与异常

- **E-1**:`process-doc-decisions.md` 写入失败 → 屏显警告,不阻断
- **E-2**:AI 判定犹豫 → 倾向**生成**
- **E-3**:`code-auto` 上下文 → 本步骤仍执行

### 不变量

- **不**修改 frontmatter(L1-3 字节级保留)
- **不**修改既有"## 工作流程"小节
- **不**修改"## 不要做的事"小节
- **不**触发 `AskUserQuestion`(NFR-3 强约束)
- **不**新增 CLI 参数(NFR-3 强约束)

## 修改文件定位的语义化约定(强制)

为支持对同一文件的**并发开发**(多人/多任务/多会话可能同时编辑同一文件的不同位置),本技能在产出 `RESULT.md` 时,对"需要修改文件内容"的部分,定位方式必须遵循以下规则:

- **禁止**直接使用文件具体的行数(如 "在 `src/foo.ts` 第 42 行")作为唯一或主要定位方式
- **应当**使用具有结构化语义的定位方式,例如:
  - **段落 / 章节**:`<文件路径> §<章节标题>`(如 `src/foo.ts §错误处理小节`)
  - **类型 / 类 / 接口 / 命名空间**:`<文件路径> > <类型名>`(如 `src/foo.ts > UserService`)
  - **方法 / 成员**:`<文件路径> > <类型>.<方法名>`(如 `src/foo.ts > UserService.login`)
  - **函数 / 顶层函数**:`<文件路径> > <函数名>`(如 `src/utils.ts > formatDate`)
  - **分支 / 循环 / 块**:`<文件路径> > <类型>.<方法> 的 <分支/循环名>`(如 `src/foo.ts > Parser.parse 的 try 分支` / `src/foo.ts > List.render 的 for 循环体`)
  - **配置项 / 字段**:`<文件路径> > <配置/对象>.<字段名>`(如 `config.json > database.host` / `package.json > scripts.build`)
  - **导入 / 导出区**:`<文件路径> §导入声明区` / `<文件路径> §导出声明区`
  - **多个匹配需要消歧**时,可附加"上下文锚点":`<文件路径> > <类型>.<方法>(位于 <另一类型> 之后)`
- 涉及"在某方法中插入新分支 / 在某函数前新增函数 / 在某类中新增成员"等典型操作时,**显式说明**:
  - 目标位置(如 "在 `UserService.login` 方法的 `try 分支` 中,`validateToken` 调用之后")
  - 已有结构(被修改的类型/方法/分支)与新增内容的相对位置

**为什么**:行号在并发改动同一文件时极易漂移;以结构化语义为锚点,`code-it` 可在每次实施前先 `Read` 最新文件,基于当前真实结构定位,避免"按错行号"导致误改或合入冲突。

**例外**:当结构化语义无法唯一定位(如修改的是注释/空行/导入顺序)时,允许使用"文件首/末 / 紧邻 <结构化锚点> 的位置"等组合描述;不允许使用行号。

---

## 命令行参数解析(本需求 REQ-00021 新增,FR-1)

> 本小节是 `code-design` 的**可选参数**解析约定。本参数**不**影响主流程,无参时按原行为执行。

### 支持的参数

| 参数 | 性质 | 模板产出物 | 备注 |
| --- | --- | --- | --- |
| `--result <模板文件>` | 可选 | `DESGIN.<ext>`(后缀 = 模板后缀) | 沿用主产出物同目录;**DESGIN 拼写锁定**(用户原文) |

### 解析规则(FR-1)

1. 技能启动时(步骤 0 之前)解析用户输入,识别 `--result <路径>`
2. 校验模板文件存在性:不存在 → 屏显 `⚠ 模板文件不存在:<路径>`,**跳过**模板填充(不阻断主流程)
3. 记录参数到 `materials-index.md` "## 命令行参数"节
4. 无参时:原行为执行,3 技能只产出 `RESULT.md`,**不**产出 `DESGIN.<ext>`

### 边界与异常

- **E-1**:`--result` 缺值(只写 `--result` 不写路径)→ 屏显 `⚠ --result 缺模板文件路径`,跳过
- **E-2**:`--result` 路径含通配符(`*.docx`)→ 屏显 `⚠ 模板路径不支持通配符`,跳过
- **E-3**:`--result` 路径 `../` 跳出工作空间 → 屏显 `⚠ 模板路径不安全`,跳过
- **E-4**:`code-auto` 上下文 → `code-auto` **不**传 `--result`(沿用 REQ-00007 Q-4)

### 模板填充步骤

> 详见 §"## 模板填充步骤(本需求 REQ-00021 新增)"小节(在本 SKILL.md 末尾"## 不要做的事"前)。

---

## 工作流程

### 步骤 0a — 拉取最新代码(强制前置,新增)所有其他 `code-*` 技能的第一步都是这一步。位于既有"步骤 0"之前。步骤 0a 成功后,`code-design` 进入"步骤 0b 设计目标确认"(本需求 REQ-00011 新增,FR-1)。
1. 探测 `git` 可用性 → 执行 `Bash: git --version`,失败 → 中断 + 提示"未检测到 git,本需求需要 git,请先安装 git 后重试"(E-12)
2. 执行 `Bash: git pull`(默认 upstream / tracking 分支)
3. 拉取失败 3 种情况分类处理(Q-2 锁定 A:中断 + 报错退出):
   - **冲突**(退出码非 0 + stderr 含 "CONFLICT" / "Merge conflict" / "unmerged")→ 打印 `✗ git pull 失败:存在未解决的冲突,冲突文件:<列出>`,提示"请手动解决冲突后,重跑该技能"(E-2)
   - **网络**(退出码非 0 + stderr 含 "Could not resolve host" / "Connection" / "timeout")→ 打印 `✗ git pull 失败:网络/remote 不可达,远程仓库:<remote-url>`,提示"请检查网络/代理后重试"(E-3)
   - **凭据**(退出码非 0 + stderr 含 "Permission denied" / "Authentication failed" / "403")→ 打印 `✗ git pull 失败:权限/凭据`,提示"请检查 git 凭据(SSH key / token)后重试"(E-4)
   - **其他错误** → 透传 stderr,中断退出
4. 拉取成功(包含 no-op / "Already up to date"):
   - **立即**执行 `Read "./assistants/.current-version"`,记为"拉取后版本"(NFR-8 强约束)
   - **执行"步骤 0b 设计目标确认"**(见下),成功后再进入既有"步骤 0 — 版本上下文检测"

### 步骤 0b.0 — 调用上下文检测(BUG-00001 新增,2026-06-06)

> 本步骤是 BUG-00001 修复方案 A3(脏标记文件)的子技能侧 — 在触发 `AskUserQuestion` **前**先检测 `code-auto` 上下文。

- **检测机制**:
  ```
  Bash: test -f ./assistants/.code-auto-running && echo "DETECTED" || echo "NOT_DETECTED"
  ```
  - 文件存在 → 自己在被 `code-auto` 调用
  - 文件不存在 → 用户手动调子技能
- **24 小时超时判断**(NFR-3 健壮性):
  - 若 `./assistants/.code-auto-running` 存在 + 距 `code-auto` 启动 > 24 小时 → 视为"脏数据"(可能 `code-auto` 异常崩溃未清理)
  - 此场景下屏幕输出 `⚠ 检测到残留的 .code-auto-running 文件(已超过 24 小时),按用户手动调子技能处理(触发 AskUserQuestion)`
- **决策**:
  - **检测到 `code-auto` 上下文**(非超时)→ 跳过 `AskUserQuestion`,直接采纳 `--balanced` 默认值写入 `design/.../RESULT.md` 顶部"## 设计目标"小节(`writeDesignGoalsSection(designResultPath, "balanced", "code-auto-detected")`),屏显 `⚠ code-auto 上下文:跳过设计目标问路,采纳 --balanced 默认`
  - **未检测到 `code-auto` 上下文** → 正常进入步骤 0b 设计目标确认(用户手动选择 1-5 个 `AskUserQuestion`)
  - **检测失败 / Read 异常** → 屏幕输出 `⚠ 无法检测 code-auto 上下文,默认按用户手动调子技能处理(触发 AskUserQuestion)` + 正常进入步骤 0b(失败降级,不阻断)
- **D-5 修订说明**:本步骤 Read `./assistants/.code-auto-running` 是"状态文件检测",**不**是"prompt 参数解析" — 与 BUG-00001 选定的方案 A3(脏标记文件)配套
- **约束**:**不**修改 frontmatter(L1-3 字节级保留);**不**修改既有"## 工作流程"小节;**不**修改既有"步骤 0b"小节;**不**修改 `code-design` 其他章节

### 步骤 0b — 设计目标确认(本需求 REQ-00011 新增,FR-1,本需求 REQ-00020 简化为 1 维度,本需求 REQ-00030 进一步收敛小需求为 0 问)
1. **扩展性触发判定**(本需求 REQ-00030 FR-1 新增,2026-06-12 起生效):
   ```
   触发扩展性 = 满足任一:
     1. 需求"待评估三方依赖"清单非空
     2. 模块拆分预评估涉及模块数 ≥ 3
     3. 上游需求 FR 含"多实现 / 抽象层 / 可替换 / 多套实现"语义(关键词匹配)
   ```
   - **不触发时**:`AskUserQuestion` **不**展示 `--extensible` / `--minimal`,直接采纳 `--balanced` 默认
   - **触发时**:沿用既有 1-2 问自适应(见步骤 2)
   - **code-auto 上下文**:`AskUserQuestion` 走 0 问默认路径(沿用 REQ-00020);但 `--balanced` 默认值替换为**根据触发条件计算**的结果(若触发 → `--extensible`;若不触发 → `--balanced`)
2. **评估需求规模 + 自适应问题数**(FR-6 强约束 + 本需求 REQ-00030 FR-2 收紧):
   - 小需求(1 任务,0.5-2 天工作量,**且**扩展性不触发):**0** 个 `AskUserQuestion`,直接采纳 `--balanced` 默认
   - 小需求(1 任务,0.5-2 天工作量,**但**扩展性触发):1 个 `AskUserQuestion`(Q1 整体设计目标)
   - 中等需求(2-5 任务):1 个 `AskUserQuestion`(Q1 整体设计目标)
   - 大需求(≥6 任务,或需架构骨架):2 个 `AskUserQuestion`(Q1 整体 + Q2 功能性,本需求改后;**不**展开为 5 个 — 架构维度全部下沉到 `code-plan` 阶段,见 NFR-2.8)
3. 收集用户回答 → 调 `writeDesignGoalsSection(designResultPath, goals, "code-design")` 写入 `design/.../RESULT.md` 顶部"## 设计目标"小节(算法 2 / NFR-3 幂等覆盖)
4. 屏显:
   ```
   === code-design 设计目标确认 ===
   整体设计目标:<--minimal/--extensible/--balanced>
   维度优先级:
     功能性:<高/中/低>
   已回写至 design/<REQ>/RESULT.md "## 设计目标" 小节
   ```
5. 用户取消 `AskUserQuestion` → 中止 + 回写空"## 设计目标"小节(E-3)
6. **不**修改 frontmatter(INV-1)
7. **不**修改"步骤 0"及之后的原有内容(INV-2)
8. 完成后进入既有"步骤 0 — 版本上下文检测"

> **职责分离**(本需求 REQ-00020 锁定,本需求 REQ-00030 强化):`code-design` 是**概设**阶段,只关注"系统长什么样 + 功能性 1 维度";架构维度(扩展性 / 健壮性 / 可维护性 / 封装性 / 可复用性 / 可读性)统一下沉到 `code-plan` **详设**阶段,由 `code-plan` 步骤 0b 询问。依据:在概设阶段过早问"未来怎么扩"会让 AI 倾向过度设计(用户原文:"不在概设阶段考虑架构设计目标")。本需求进一步约束:小需求不询问扩展性(本需求 REQ-00030 FR-2);扩展性询问严格按触发条件(本需求 REQ-00030 FR-1)。

### 步骤 0 — 版本上下文检测(强制前置)
1. 读取 `./assistants/.current-version`
2. **文件不存在** → 立即停下,告知用户先调 `code-version`
3. 读取内容,记为 `<版本号>`,后续所有路径用 `assistants/<版本号>/...`
4. 验证 `./assistants/<版本号>/require/<需求编码>/RESULT.md` 存在(否则提示用户先跑 `code-require`)

### 步骤 1 — 收集需求编码
- 若用户未提供,主动询问
- 校验 `./assistants/<版本号>/require/<需求编码>/RESULT.md` **必须存在**,否则:
  > 错误:未找到 `./assistants/<版本号>/require/<需求编码>/RESULT.md`。请先运行 `code-require` 完成需求分析。

### 步骤 2 — 定位 / 创建工作目录
1. 检查 `./assistants/<版本号>/design/<需求编码>/` 是否存在
2. 不存在 → `mkdir -p` 创建
3. 存在 → 直接进入步骤 3

### 步骤 3 — 读取项目级规范
**本步骤是规范约束的入口,所有后续设计决策都必须参照本步骤提取的规则。**

1. `Glob "./assistants/rules/**/*"` 列出 `./assistants/rules/` 下所有文件
2. **如果目录不存在或为空**:
   - 主动询问用户:
     > 未找到 `./assistants/rules/` 目录或该目录为空。是否继续生成概要设计?
     > - 选项 A:继续,本次设计不参考任何项目级规范
     > - 选项 B:取消,先去定义规范
     > - 选项 C:我马上提供一些规范要点,你临时遵循
   - 用户选择 A → 在 `rule-compliance.md` 中明确记录"本次无规范可参考"
   - 用户选择 B → 停止本次技能调用
   - 用户选择 C → 用户输入的临时要点也写入 `rule-compliance.md`
3. **如果存在规范文件**,对每个文件 `Read`,然后**归类**:
   - 功能架构规范:整体架构风格(分层/微服务/事件驱动/六边形等)、组件划分原则、技术选型偏好
   - 模块规划规范:目录结构、命名约定、模块边界、依赖方向、是否禁止循环依赖
   - 接口定义规范:API 风格(REST/gRPC/GraphQL)、版本策略、错误码规范、命名约定、请求/响应结构
   - 数据结构规范:ORM/不 ORM、字段命名、是否必须软删、主键策略、外键策略、索引规范
   - 安全规范:鉴权/加密/审计/防注入/敏感字段处理
   - 性能规范:缓存策略、批量/分页要求、同步/异步边界
   - 测试规范:覆盖率门槛、必须有的测试类型
   - 可观测性规范:日志格式、关键指标、追踪要求
   - 其他:文件命名、注释规范、提交规范等

4. 把规范摘要写入 `materials-index.md` 的"项目级规范"部分,格式:
   ```
   | 规范文件 | 类别 | 关键约束摘要 |
   | --- | --- | --- |
   | architecture.md | 功能架构 | 分层架构,禁止跨层调用 |
   | api-standards.md | 接口定义 | RESTful,错误码统一 4 位,需含 trace_id |
   | ... | ... | ... |
   ```

### 步骤 4 — 读取需求分析结果
1. `Read "./assistants/<版本号>/require/<需求编码>/RESULT.md"`
2. 重点提取:
   - FR / NFR 列表
   - 验收标准 AC
   - 页面与交互要点
   - 关联需求(用于横向参考)
   - 待澄清项(可能影响设计决策)
3. 写入 `materials-index.md` 的"上游需求"部分(包含版本号)
4. **预检规范 vs 需求冲突**:
   - 扫描需求中明确指定的技术选型/接口风格/数据模型
   - 与步骤 3 提取的规范对比
   - 发现的冲突先记录到 `rule-compliance.md` 的"待澄清冲突"区,稍后步骤 8A 一并询问用户

### 步骤 5 — 探索项目结构与已有代码
**这是本技能的核心步骤,目的是"结合当前项目结构和已有软件功能"。**

依次执行:
1. **项目类型识别**
2. **目录结构**
3. **入口与主流程**
4. **已有模块识别**
5. **已有接口识别**
6. **已有数据模型**
7. **已有第三方依赖**
8. **编码与构建约定**
9. **可复用资产**

**与规范的交叉验证**:
- 项目实际目录结构是否符合 `module-conventions.md` 的目录规范?
- 既有 API 是否符合 `api-standards.md`?
- 既有数据模型是否符合 `data-modeling.md`?
- 不符的地方记录到 `rule-compliance.md` 的"现状偏离"区(供设计参考:可能是历史欠账,本设计可借机纠正)

把发现写入 `materials-index.md` 的"项目现状"部分。

### 步骤 6 — 检查 RESULT.md 是否存在
- **不存在** → 走"**首次设计**"分支(步骤 7A–15A)
- **存在** → 走"**增量更新**"分支(步骤 7B–10B)

### 步骤 7A — 首次设计:架构方案构思
在 `design-notes.md` 中记录:
- 关键设计问题清单(模块切分、状态归属、事务边界、依赖方向、扩展点位置)
- 候选方案(对每个关键设计问题,给出 ≥2 个候选)
- 选定方案与理由
- 备选方案被否决的理由
- **每条决策旁标注"依据规范:<规范文件名 §章节>"**(如适用)

### 步骤 8A — 澄清冲突与不确定项
处理两类待澄清事项:

1. **规范 vs 需求冲突**(从步骤 3/4/5 收集):
   - 用 `AskUserQuestion` 给出选项
   - 用户选择后,把结论写入 `rule-compliance.md` 的"冲突解决记录"区
2. **设计不确定项**(架构走向相关):
   - 性能/可用性目标的优先级权衡
   - 是否允许引入新依赖(与 `dependencies.md` 的规范交叉)
   - 部署形态(单体/微服务/Serverless)
   - 数据存储选型偏好
   - 与既有系统的集成方式
   - 灰色地带的功能边界

**每轮聚焦 1-3 个最阻塞点**;每轮问答追加到 `clarifications.md`,字段:
- 时间 / 问题 / 选项 / 用户回答 / 影响的 RESULT.md 章节

### 步骤 9A — 模块拆分(遵循模块规划规范,本需求 REQ-00030 FR-3 收窄)
基于步骤 5 的"已有模块"和步骤 7A 的方案,产出**新增/复用/修改**三类模块清单:
- 写入 `module-breakdown.md`(**本需求 REQ-00030 FR-3 收窄至 5 列**):
  - 模块名
  - 路径
  - 状态(新增/复用既有/修改既有)
  - 职责(一句话)
  - 依赖(对内其他模块 / 对外三方)
- **不**展开以下字段(本需求 REQ-00030 FR-3 锁定,字段下沉到 `code-plan` 的 `module-details.md`):
  - **不**列"对外暴露的接口"(下沉到 `code-plan` §接口细节)
  - **不**列"关键决策(为什么这样切)"(下沉到 `code-plan` §设计概述)
  - **不**列"符合的规范条款"(下沉到 `code-plan` §规范遵循)

**自检**:对照 `module-conventions.md` 逐条检查:
- 命名是否符合规范
- 目录位置是否符合规范
- 依赖方向是否违反规范
- 是否有被禁止的模式

### 步骤 10A — 接口与数据结构(遵循接口/数据结构规范,本需求 REQ-00030 FR-3 收窄)
- **接口概要**(本需求 REQ-00030 FR-3 收窄至 4 项 / 接口):
  - 接口名
  - 形式(REST / gRPC / 事件 / 函数 / SDK 方法)
  - 状态(新增 / 既有扩展 / 既有修改)
  - 一句话职责
- **不**展开以下字段(下沉到 `code-plan` 的 `interface-specs.md`):
  - **不**列"数据格式与版本策略"
  - **不**列"错误码与异常约定"
  - **不**列"完整入参 / 出参 schema"(下沉到 `code-plan` §接口细节)

- **数据结构**(本需求 REQ-00030 FR-3 收窄至 4 项 / 实体):
  - 实体名
  - 状态(新增 / 修改既有)
  - 关键字段(**≤ 5 个,纯名字列表,不写类型/约束/索引**)
  - 一句话关系
- **不**展开以下字段(下沉到 `code-plan` 的 `data-changes.md`):
  - **不**列"字段类型 / 约束"
  - **不**列"完整字段名 / 类型 / 约束"(下沉到 `code-plan` §数据结构完整变更)
  - **不**列"与既有实体的关系"(一句话关系**保留**)
  - **不**列"存储选型 / 索引"

### 步骤 11A — 三方依赖评估(遵循依赖管理规范,本需求 REQ-00030 FR-3 简化)
- 评估是否需要**新增**第三方依赖
- 对每个新增依赖,记录(**本需求 REQ-00030 FR-3 收窄至 4 项 / 依赖**):
  - 依赖名
  - 一句话用途
  - 必要性(高 / 中 / 低)
  - 替代评估(项目内已有 / 外部无替代)
- **不**展开以下评估项(下沉到 `code-plan` 的 `dependencies.md`):
  - **不**列"版本 / 来源"
  - **不**列"许可协议 / 维护活跃度"
  - **不**列"体积 / 性能影响 / 安全态势"
- 对照规范中的"依赖管理"条款(如是否禁止某类依赖、是否要求内部源、版本号策略)
- 写入 `dependencies.md`

### 步骤 12A — 检索关联概要设计
1. `Glob "./assistants/<版本号>/design/*/RESULT.md"` 列出同版本其他概要设计
2. (可选,跨版本) `Glob "./assistants/*/design/*/RESULT.md"`
3. 重点关注与本设计**模块交集**或**接口约定**的设计
4. 在 `related-designs.md` 记录关联点(含所在版本)

### 步骤 13A — 撰写 RESULT.md
按 `templates/design.md` 的章节结构生成 `RESULT.md`。

**关键原则**:
- **充分引用上游**:文档头标注"上游:`./assistants/<版本号>/require/<需求编码>/RESULT.md`",每节给出对应 FR/NFR 编号
- **充分引用现状**:模块拆分中明确标注"复用既有 <模块>"、"修改既有 <模块>"
- **充分引用规范**:文档头标注"遵循规范:`./assistants/rules/` 下 N 个文件",并在每节给出"规范依据:<规范文件 §章节>"
- **架构图必须**:Mermaid / ASCII 给出组件图与关键数据流
- **接口与数据结构要落到字段**:不能止于"有一个 User 实体"
- **不臆造**:无材料佐证的细节写进"待澄清"
- **不合规就标红**:任何对规范的偏离(无论是技术必要还是用户授权)必须在"规范遵循"章节显式列出

### 步骤 14A — 同步版本看板(强制)
1. `Read "./assistants/<版本号>/RESULT.md"`,定位"概要设计清单"区段
2. 检查本条设计是否已存在:
   - **不存在** → 追加一行,状态填 `已完成`(或其他最终状态)
   - **已存在** → 状态按需更新
3. 在"变更记录"区段追加:
   ```
   YYYY-MM-DD HH:mm  设计新增  <需求编码> 概要设计完成  <需求编码>
   ```
4. 更新文档头"最近更新"时间

### 步骤 15A — 完善过程文档与汇报
- 收尾 `materials-index.md` / `design-notes.md` / `module-breakdown.md` / `dependencies.md` / `related-designs.md` / `rule-compliance.md` / `clarifications.md`
- 向用户汇报:核心架构决策、模块清单、新增依赖、关联设计、**规范遵循与冲突解决**、**版本看板更新点**

### 步骤 7B — 增量更新:识别变更源
1. **需求侧变更**
   - 重读 `./assistants/<版本号>/require/<需求编码>/RESULT.md`,对比"变更记录"识别变化
2. **代码侧变更**
   - 重新执行步骤 5,与 `materials-index.md` 中"项目现状"对比
3. **规范侧变更** ← **本次新增**
   - 重新 `Glob "./assistants/rules/**/*"` 与 `Read`
   - 与 `materials-index.md` 中"项目级规范"对比
   - 识别:新增/修改/删除的规范文件
   - **特别关注**:是否有规范条款变得**更严格**,导致本设计需要回退修正
4. **设计侧回顾**
   - 重读 `RESULT.md` 与所有过程文档

### 步骤 8B — 局部更新 RESULT.md
对每一项变更:
- **需求新增** → 评估对模块/接口/数据结构的影响,追加对应章节
- **需求修改** → 修订对应章节,标注 `(修订于 YYYY-MM-DD, 详见变更记录)`
- **需求删除** → 评估是否仍被设计引用
- **代码变更(复用资产变了)** → 更新"模块拆分";若既有模块被破坏,可能回退到步骤 7A
- **代码变更(新增了已有能力)** → 评估是否可以让本设计少做一点
- **规范变更** ← **本次新增**
  - 新增规范 → 评估本设计是否需要补充对应约束
  - 规范修订 → 评估本设计相关章节是否需要同步修订
  - 规范删除 → 评估本设计中相关引用是否需要移除
  - **任何规范触发的设计修订**都必须在 `rule-compliance.md` 追加记录

更新 `RESULT.md` 时**不重写稳定章节**,只在必要位置编辑。最后:
- 在"变更记录"章节追加本次更新摘要(包含**规范侧**变化):
  - 需求侧变化 / 代码侧变化 / **规范侧变化**列表
  - 影响的章节
  - 新增/修订/废弃的模块
  - 新增/修订的三方依赖
  - 新增的待澄清项

### 步骤 9B — 重做规范自检
- 对本次更新涉及的章节,重新对照 `rule-compliance.md` 中的规范清单逐条自检
- 任何新增的不合规项,要么修设计,要么标"用户授权的偏离"并写明理由

### 步骤 10B — 同步版本看板与补充过程文档
1. `Read "./assistants/<版本号>/RESULT.md"`,更新"概要设计清单"中本条记录(状态、变更摘要)
2. 在"变更记录"追加:
   ```
   YYYY-MM-DD HH:mm  设计变更  <需求编码> 概要设计增量更新(需求/代码/规范侧变化列表)  <需求编码>
   ```
3. 过程文档:`materials-index.md` 刷新"项目级规范"和"项目现状"部分;其他过程文档同步
4. 汇报:**新增 / 修改 / 废弃 / 规范触发的修订 / 未变 / 版本看板更新点**

---

### 步骤 N — 末尾兜底提交(强制收尾,新增)
**所有其他步骤执行完毕后的最后一步,覆盖"过程文件 + 结果文件"。**

> 注:本步骤对**首次设计(步骤 7A–15A)** 和**增量更新(步骤 7B–10B)** 两条分支都执行,均作为收尾。

1. **收集 dirty 文件**:
   - 执行 `Bash: git status --porcelain`
   - 输出为空 → 打印 `无文件变更,跳过末尾提交`,**跳过步骤 2-4,直接退出**(NFR-4)
   - 输出非空 → 进入步骤 2

2. **暂存文件**:
   - 执行 `Bash: git add <所有 dirty 文件>`
   - 失败 → 透传 stderr,中断退出

3. **生成 commit message 预览**:
   ```
   chore(code-design): <需求编码> <设计标题>

   概要设计完成:<关键决策数> 项决策,<不变量数> 条不变量
   过程文件 N + 结果文件 1
   ```
   - `<设计标题>` 从本轮 RESULT.md 的标题提取
   - `<关键决策数>` / `<不变量数>` 从 RESULT.md §3 / §3.1 提取

4. **弹 3 选 1 确认**(Q-3 锁定 A / FR-3.AC-3.4):
   - **A. 确认提交(推荐)** → 进入步骤 5
   - **B. 修改 commit message** → 重新生成预览(可由用户编辑)→ 返回步骤 4
   - **C. 取消** → 跳过 commit,打印"已取消提交,文件保持暂存/工作区状态",**退出**(E-9)

5. **执行 commit**:
   - 执行 `Bash: git commit -m "<message>"`
   - 成功 → 打印 `commit 完成,hash: <hash>`,退出
   - 失败(退出码非 0,pre-commit hook / 其他)→ 打印 stderr,**不重试**(E-10),提示"末尾提交失败,文件已暂存,请手动处理后执行 git commit",退出

## 过程文档格式

### materials-index.md
```
# 材料登记 — <需求编码>
更新时间:YYYY-MM-DD HH:mm
版本:<版本号>

## 项目级规范
| 规范文件 | 类别 | 关键约束摘要 |
| --- | --- | --- |
| architecture.md | 功能架构 | 分层架构,禁止跨层调用 |
| api-standards.md | 接口定义 | RESTful,错误码 4 位,需含 trace_id |
| ... | ... | ... |

## 上游需求
- 来源:./assistants/<版本号>/require/<需求编码>/RESULT.md
- 版本:v<n>(<更新时间>)
- 提取的 FR / NFR / AC 数量

## 项目现状(本次扫描)
### 项目类型
- 语言 / 框架 / 关键依赖
### 目录结构
- 顶层目录与职责简述
### 已有模块
| 模块/路径 | 职责 | 是否可复用 |
| --- | --- | --- |
| ... | ... | 是 / 否 / 需扩展 |
### 已有接口
- ...
### 已有数据模型
- ...
### 已有第三方依赖
- ...
### 编码与构建约定
- ...
```

### rule-compliance.md
```
# 规范遵循记录 — <需求编码>
更新时间:YYYY-MM-DD HH:mm
版本:<版本号>

## 1. 本次参考的规范文件
- ./assistants/rules/architecture.md
- ./assistants/rules/api-standards.md
- ./assistants/rules/data-modeling.md
- ...

## 2. 规范 vs 现状偏离
- <规范条款>:项目现状是 <X>,规范要求 <Y>
- 影响:本设计可以借机纠正 / 不在本设计范围

## 3. 规范 vs 需求冲突
- <规范条款> vs 需求 <FR-X>:冲突
- 选项:...
- 用户选择:...
- 解决时间:YYYY-MM-DD HH:mm

## 4. 用户授权的偏离
- <RESULT.md 章节>:<偏离内容>
- 理由:...
- 授权时间:YYYY-MM-DD HH:mm

## 5. 规范变更响应(增量更新时填写)
- 规范文件:<X>,变更:<新增/修改/删除>
- 时间:YYYY-MM-DD HH:mm
- 对 RESULT.md 的影响:<章节/无>
- 处理动作:<已修订/无需修订/用户授权保留>
```

### 其他过程文档
`design-notes.md` / `module-breakdown.md` / `dependencies.md` / `related-designs.md` / `clarifications.md` 格式同前次,此处略。

---

## 模板填充步骤(本需求 REQ-00021 新增,FR-2)

> 本小节是 `code-design` 在主产出物 `design/.../RESULT.md` 完成后,根据 `--result` 参数执行**可选**的模板填充步骤。
> 本步骤**不**是必需步骤;无 `--result` 时跳过。

### 触发条件

- 用户调 `code-design <REQ> --result <模板文件>` 时触发
- **不**触发条件:无 `--result` 参数 / `code-auto` 上下文 / 模板文件不存在 / 模板为二进制格式

### 执行流程(FR-2)

1. **读取模板**:`Read <模板文件路径>`
   - 可文本化格式(.md / .html / .txt / .json / .xml / .csv / .yaml 等)→ 完整读取
   - 二进制格式(.docx / .xlsx / .pdf / .pptx)→ 屏显 `⚠ 模板格式二进制,跳过填充`,**不**尝试读取
2. **占位符提取**:扫描 `{{...}}` 风格占位符
3. **结构化数据准备**:从 `design/.../RESULT.md` 提取 4 类数据(设计概述 / 模块列表 / 接口列表 / 数据结构)
4. **占位符替换**:按本需求内置的 15 个占位符映射表替换(code-design 用其中 4 个)
5. **写出文件**:`<原主产出物同目录>/DESGIN.<ext>`(后缀 = 模板后缀)
6. **屏显**:
   ```
   === code-design 模板填充 ===
     模板:<路径>
     输出:<路径>(<N> 个占位符已替换)
   ```
7. **过程文档**:在 `materials-index.md` 追加"模板填充结果"节(模板路径 / 输出路径 / 占位符填充数 / 失败占位符列表)

### 占位符映射表(code-design 用 4 个)

| 占位符 | 来源 |
| --- | --- |
| `{{设计概述}}` | `design/.../RESULT.md` §1 |
| `{{模块列表}}` | `design/.../RESULT.md` §模块拆分 |
| `{{接口列表}}` | `design/.../RESULT.md` §接口 |
| `{{数据结构}}` | `design/.../RESULT.md` §数据结构 |

> 完整 15 个占位符映射表见 `code-require/SKILL.md` "## 模板填充步骤" 小节

### 边界与异常 / 不变量

> 沿用 `code-require/SKILL.md` "## 模板填充步骤" 小节的 E-5 ~ E-11 边界 + 不变量(完全同构)

---

## 衔接
- **下游**:`code-plan` 消费本阶段 `RESULT.md` 做实施计划
- **上游**:`code-version`(必须有激活版本);`code-require` 的 `RESULT.md`;项目级规范
- **横向**:通过 `related-designs.md` 与同版本下其他概要设计(可选:跨版本)形成引用网

## 不要做的事
- 不修改 `<本仓库>` 中除了 `./assistants` 目录中的其他代码文件(工程代码改动由 `code-it` 实施,本技能只写 `design/<REQ>/RESULT.md` 等工作空间文档)
- 不要在没有 `./assistants/.current-version` 的情况下继续执行
- 不读上游 `RESULT.md` 就直接做设计
- 不读 `./assistants/rules/` 就直接做设计(在用户未授权跳过的情况下)
- 不探索项目现状就假设是"绿地项目"
- 在不读取现有 `RESULT.md` 的情况下重写整个文件
- 在材料不足以佐证时臆造接口签名或数据结构
- 静默违反规范(必须显式记录"用户授权的偏离"或"待澄清冲突")
- 一次性抛出所有澄清问题
- 修改 `./assistants/<版本号>/require/<需求编码>/` 或 `./assistants/rules/` 下的任何内容(只读)
- 修改 `./assistants/<版本号>/RESULT.md` 中"概要设计清单" / "变更记录"以外的区段
